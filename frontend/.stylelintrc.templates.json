{
  "customSyntax": "postcss-html",
  "ignoreFiles": [
    "build/**",
    "node_modules/**"
  ],
  "rules": {
    "media-feature-range-notation": "context",
    "declaration-property-value-disallowed-list": [
      {
        "/.*/": [
          "/var\\(\\s*--[\\w-]+\\s*,\\s*#[0-9a-fA-F]/"
        ]
      },
      {
        "message": "No hex fallback in var(): every design token is defined in styles.css, and a fallback only hides a renamed or missing token (FE-MOD-01, ui-design-system.md)."
      }
    ]
  }
}
