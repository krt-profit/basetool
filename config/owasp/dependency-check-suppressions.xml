<?xml version="1.0" encoding="UTF-8"?>
<!--
  OWASP Dependency-Check suppression file.

  Wired into the `org.owasp.dependencycheck` plugin in the root build.gradle.kts
  via `dependencyCheck { suppressionFile = ... }`. Every entry below MUST carry
  a `notes` block that explains *why* the finding is suppressed so the next
  weekly Monday scan does not require re-deriving the justification from
  scratch.

  Categories accepted:
    1. CPE confusion - the NVD entry refers to a different product that shares
       a token (e.g. "line", "spring boot tools") with the dependency we ship.
    2. Build-time-only path - the affected JAR is on the buildscript /
       checkstyle / pitest plugin classpath, never reaches the
       runtime classpath of the deployed Spring Boot fat JAR.
    3. Nested JAR inside a build-time tool - same as #2 but the SHA-1 match
       happens because OWASP unpacks a fat tool JAR and inspects the
       embedded JARs.

  Real production CVEs are NEVER suppressed; they are fixed by upgrading the
  dependency to a patched version (see build.gradle.kts dependency overrides).
-->
<suppressions xmlns="https://jeremylong.github.io/DependencyCheck/dependency-suppression.1.3.xsd">

  <!--
    pitest-command-line carries the artifact id "pitest" but the OWASP CPE
    matcher attributes CVE-2015-0897 (a MITM bug in the "LINE" Android/iOS
    messenger app) to it because the messenger's vendor/product CPE happens
    to be "cpe:2.3:a:line:line". The library has no relation to LINE Corp;
    it is a JVM mutation testing tool. CVSS for this CVE is 5.9 (under our
    7.0 fail-build gate) but the SARIF report would still surface the
    finding noisily to GitHub Security every week.
  -->
  <suppress>
    <notes>False positive: CPE confusion. CVE-2015-0897 targets the "LINE" Android/iOS messenger app (cpe:2.3:a:line:line) and has nothing to do with the pitest mutation testing library.</notes>
    <packageUrl regex="true">^pkg:maven/org\.pitest/pitest-command-line@.*$</packageUrl>
    <cve>CVE-2015-0897</cve>
  </suppress>

  <!--
    spring-boot-devtools (Spring Boot 4.x) is wrongly matched against the
    `cpe:2.3:a:vmware:spring_boot_tools` / `cpe:2.3:a:vmware:spring_tools`
    CPEs. CVE-2022-31691 is a deserialization gadget in **Spring Tools 4
    for Eclipse** plus the VSCode "Spring Boot Tools", "Concourse CI Pipeline
    Editor", "Bosh Editor" and "Cloudfoundry Manifest YML Support" extensions
    - none of which we ship. The Spring Boot runtime devtools module is a
    distinct artifact and is gated to the `development` profile only.
  -->
  <suppress>
    <notes>False positive: CPE confusion. CVE-2022-31691 affects Spring Tools 4 for Eclipse and a set of VSCode plugins; it does not apply to the Spring Boot runtime devtools dependency.</notes>
    <packageUrl regex="true">^pkg:maven/org\.springframework\.boot/spring-boot-devtools@.*$</packageUrl>
    <cve>CVE-2022-31691</cve>
  </suppress>

  <!--
    The io.opentelemetry.semconv jars (opentelemetry-semconv /
    opentelemetry-semconv-incubating, pulled by the Boot 4 OpenTelemetry
    starter and by the Keycloak server SPI compile classpath) contain only
    generated semantic-convention CONSTANTS - no network, parsing or
    process-spawning code. The NVD matcher maps them onto the umbrella CPE
    `cpe:2.3:a:opentelemetry:opentelemetry`, which aggregates the whole
    OpenTelemetry product family across languages; every CVE filed against
    the *Go* SDK whose version range happens to contain 1.37/1.41 then
    lights up these Java jars. Concretely flagged in week 2026-W27:
    CVE-2026-29181 (Go baggage-header DoS, fixed in otel-go 1.41.0),
    CVE-2026-24051 and CVE-2026-39883 (Go macOS/BSD PATH-hijack in
    host-resource detection, fixed in otel-go 1.40.0/1.43.0). Suppressing
    the CPE mapping itself (not individual CVEs) is the documented
    dependency-check remedy for CPE confusion and keeps every future
    Go/Collector CVE from re-triggering this false positive. Real CVEs
    against the io.opentelemetry *Java* artifacts use distinct CPEs and
    remain visible.
  -->
  <suppress>
    <notes>False positive: CPE confusion. The opentelemetry-semconv Java jars are constants-only; the umbrella cpe:/a:opentelemetry:opentelemetry attributes OpenTelemetry-Go SDK CVEs (CVE-2026-29181, CVE-2026-24051, CVE-2026-39883) to them by pure version-number overlap.</notes>
    <packageUrl regex="true">^pkg:maven/io\.opentelemetry\.semconv/opentelemetry-semconv.*@.*$</packageUrl>
    <cpe>cpe:/a:opentelemetry:opentelemetry</cpe>
  </suppress>

  <!--
    Belt-and-braces companion to the CPE suppression above: the same five
    OpenTelemetry-GO SDK CVEs pinned by id, in case a future
    dependency-check version changes how partial-CPE matching works. All
    five are Go-SDK-only (baggage-header DoS and macOS/BSD PATH-hijack in
    host-resource detection - see the notes above); none has any Java
    counterpart. CVE-2026-39882 and CVE-2026-41178 sit below the 7.0 gate
    but would still spam the weekly SARIF upload.
  -->
  <suppress>
    <notes>False positive: CPE confusion (companion to the CPE-level entry). CVE-2026-29181, CVE-2026-24051, CVE-2026-39883, CVE-2026-39882 and CVE-2026-41178 are OpenTelemetry-Go SDK vulnerabilities attributed to the constants-only Java semconv jars.</notes>
    <packageUrl regex="true">^pkg:maven/io\.opentelemetry\.semconv/opentelemetry-semconv.*@.*$</packageUrl>
    <cve>CVE-2026-29181</cve>
    <cve>CVE-2026-24051</cve>
    <cve>CVE-2026-39883</cve>
    <cve>CVE-2026-39882</cve>
    <cve>CVE-2026-41178</cve>
  </suppress>

  <!--
    CVE-2026-53914 is an unsafe-deserialization bug in the Kotlin BUILD
    TOOLCHAIN (< 2.4.20): malicious build-cache metadata can execute code
    during compilation. JetBrains' own CNA vector is local/high-privilege
    (CVSS 6.7); the NVD auto-score of 9.8 CRITICAL is what trips our gate.
    The artifact we actually ship is the RUNTIME kotlin-stdlib jar
    (transitive via okhttp/okio, the OTLP exporter's HTTP sender) which
    contains no build-cache handling code, and basetool compiles zero
    Kotlin sources - the vulnerable toolchain path cannot be reached.
    CVE-scoped (not CPE-scoped) on purpose: a future CVE against the
    stdlib itself must still fail the build.
  -->
  <suppress>
    <notes>False positive: CVE-2026-53914 targets the Kotlin build toolchain's build-cache metadata deserialization, not the runtime kotlin-stdlib jar we ship transitively via okhttp/okio; this project compiles no Kotlin sources.</notes>
    <packageUrl regex="true">^pkg:maven/org\.jetbrains\.kotlin/kotlin-stdlib@.*$</packageUrl>
    <cve>CVE-2026-53914</cve>
  </suppress>

  <!--
    CVE-2026-42582 is an allocation-DoS in the QPACK decoder of
    netty-codec-http3 4.2.0.Alpha1 - 4.2.12 (fixed 4.2.13.Final). The
    netty 4.1.x line does not contain the HTTP/3 codec at all - it lives
    in the 4.2+ line - so ANY 4.1.x jar is outside the affected range;
    the broad cpe:2.3:a:netty:netty match flags them anyway. Our 4.1.135
    jars additionally sit only on the keycloak-spi compile classpath
    (Keycloak server internals, container-provided at runtime); the app
    modules resolve netty 4.2.15.Final, which is above the fixed version.
  -->
  <suppress>
    <notes>False positive: CPE confusion. CVE-2026-42582 affects netty-codec-http3 in the 4.2.0-4.2.12 range; the netty 4.1.x line ships no HTTP/3 codec, and these jars are Keycloak-container-provided compile-time deps anyway.</notes>
    <packageUrl regex="true">^pkg:maven/io\.netty/netty\-.*@4\.1\..*$</packageUrl>
    <cve>CVE-2026-42582</cve>
  </suppress>

  <!--
    CVE-2024-7254 (unbounded recursion -> StackOverflowError when parsing
    untrusted nested groups, fixed in protobuf-java 3.25.5) is a REAL
    protobuf-java CVE - but protobuf-java 3.25.1 exists exclusively on the
    keycloak-spi module's compileOnly classpath, dragged in by the Keycloak
    server artifacts (Infinispan/protostream) the SPI compiles against.
    Verified via `dependencyInsight`: no basetool runtime classpath
    (backend/frontend/ingest/keycloak-spi runtime) contains protobuf-java
    in any version. At runtime the SPI runs inside the Keycloak container,
    which supplies its own (Red Hat-patched) protobuf; upgrading the pin in
    our build would change nothing that ships. Version-pinned to 3.25.1 so
    a vulnerable protobuf appearing on a REAL classpath later still fails
    the build.
  -->
  <suppress>
    <notes>Build-time-only: protobuf-java 3.25.1 is a compileOnly transitive of the Keycloak server artifacts the keycloak-spi module compiles against; it is container-provided at runtime and absent from every basetool runtime classpath, so the CVE-2024-7254 parsing path never ships.</notes>
    <packageUrl regex="true">^pkg:maven/com\.google\.protobuf/protobuf-java@3\.25\.1$</packageUrl>
    <cve>CVE-2024-7254</cve>
  </suppress>

  <!--
    plexus-utils 3.3.0 is pulled in transitively by Checkstyle 13.4.2 via
    `org.apache.maven.doxia:doxia-core`/`doxia-module-xdoc` (Checkstyle's
    Javadoc/site-rendering helper), which is exclusively on the
    `checkstyleMain` configuration. It is never on the runtime classpath of
    the deployed application. CVE-2025-67030 is a `Expand.extractFile` zip
    slip - we do not call that method anywhere, and Checkstyle itself only
    invokes it during local report rendering, not when scanning sources.
  -->
  <suppress>
    <notes>Build-time-only: plexus-utils 3.3.0 is a transitive dependency of Checkstyle (doxia-core). The Expand.extractFile zip slip is unreachable through Checkstyle's source-scanning path and the JAR never ships in the deployed Spring Boot fat JAR.</notes>
    <packageUrl regex="true">^pkg:maven/org\.codehaus\.plexus/plexus-utils@3\.3\.0$</packageUrl>
    <cve>CVE-2025-67030</cve>
  </suppress>

  <!--
    CVE-2026-54399 (HTTP/1.1 message parser DoS) and CVE-2026-54428 (HTTP/2
    HPACK decoder unbounded header-list DoS) are both filed against Apache
    HttpComponents *Core 5.x* (httpcore5 / httpcore5-h2, groupId
    `org.apache.hc.core5`), affected range "<= 5.4.2, plus 5.5-beta1". The NVD
    matcher attributes them to our `org.apache.httpcomponents:httpcore` 4.4.14 /
    4.4.16 jars purely because the two product lines share the CPE
    `cpe:2.3:a:apache:httpcomponents_core` and 4.4.x sorts below the 5.4.2 upper
    bound. The legacy 4.4.x line is a distinct codebase:
      - it ships NO HTTP/2 support at all (HTTP/2 / HPACK lives only in the
        5.x httpcore5-h2 artifact), so CVE-2026-54428 is structurally
        inapplicable, and
      - 4.4.16 is the terminal 4.x release - there is and will be no "fixed"
        4.4.x version, because the 4.x parser is not the affected code.
    Confirmed via `./gradlew :keycloak-spi:dependencies`: httpcore 4.4.x is
    dragged in ONLY by `org.apache.httpcomponents:httpclient` on the
    keycloak-spi module's `checkstyle`, `compileClasspath` and `test*`
    configurations (Keycloak server SPI internals the module compiles/tests
    against; container-provided at runtime). It is absent from every basetool
    RUNTIME classpath, and no httpcore5 5.x jar exists anywhere in the build,
    so nothing we ship can reach the vulnerable code. CVE-scoped (not
    CPE-scoped) and pinned to the 4.x line so a genuinely-vulnerable httpcore5
    5.x ever entering the build still fails the gate.
  -->
  <suppress>
    <notes>False positive: CPE confusion + build-time-only. CVE-2026-54399/CVE-2026-54428 affect Apache HttpComponents Core 5.x (httpcore5/httpcore5-h2, HTTP/1.1 parser and HTTP/2 HPACK decoder); our httpcore 4.4.x jars are the legacy 4.x line (no HTTP/2 code path), present only on the keycloak-spi compile/checkstyle/test classpaths and on no basetool runtime classpath. No httpcore5 5.x is shipped.</notes>
    <packageUrl regex="true">^pkg:maven/org\.apache\.httpcomponents/httpcore@4\..*$</packageUrl>
    <cve>CVE-2026-54399</cve>
    <cve>CVE-2026-54428</cve>
  </suppress>

  <!--
    CVE-2026-15075 (the vertx-core HttpClient DefaultRedirectHandler forwards
    Authorization / Cookie / custom headers verbatim across a cross-origin 30x
    redirect - an OUTBOUND HttpClient credential leak) and CVE-2026-15076 (the
    vertx-web-client WebClientSession accepts a Set-Cookie `Domain` for an
    unrelated host - cross-domain cookie injection) both affect Eclipse Vert.x
    4.5.29 and earlier (fixed in 4.5.30). The NVD CPE fans the generic
    "Eclipse Vert.x" product out onto the plain io.vertx:vertx-core / vertx-grpc*
    coordinates; CVE-2026-15076's vulnerable class actually lives in
    io.vertx:vertx-web-client, which is not on any classpath in this build at all.
    Confirmed via `./gradlew :keycloak-spi:dependencyInsight`: vertx-core 4.5.28 and
    the vertx-grpc* jars are dragged in ONLY by
    org.keycloak:keycloak-services:26.7.0 -> keycloak-config-api -> Quarkus
    (quarkus-vertx / quarkus-grpc-common / quarkus-opentelemetry) on the
    keycloak-spi module's compileOnly classpath. They are absent from every
    basetool RUNTIME classpath (backend / frontend / ingest / keycloak-spi
    runtimeClasspath all report "No dependencies matching"). At runtime the SPI
    runs inside the Keycloak (Quarkus) container, which supplies its own
    Red Hat-patched Vert.x, so nothing we ship can reach the vulnerable HttpClient
    redirect or WebClientSession code. CVE-scoped and pinned to the 4.5.x line so a
    genuinely-vulnerable Vert.x ever entering a real classpath still fails the gate.
  -->
  <suppress>
    <notes>False positive: build-time-only + CPE expansion. CVE-2026-15075 (vertx-core HttpClient cross-origin redirect header leak) and CVE-2026-15076 (vertx-web-client WebClientSession cross-domain cookie injection, whose vulnerable class is not even on the classpath) affect Eclipse Vert.x 4.5.29 and earlier. Our vertx-core / vertx-grpc* 4.5.28 jars are compileOnly transitives of keycloak-services 26.7.0 (via Quarkus) on the keycloak-spi module only, absent from every basetool runtime classpath and container-provided by Keycloak at runtime.</notes>
    <packageUrl regex="true">^pkg:maven/io\.vertx/vertx-.*@4\.5\..*$</packageUrl>
    <cve>CVE-2026-15075</cve>
    <cve>CVE-2026-15076</cve>
  </suppress>

  <!--
    Same build-time-only pattern as the Vert.x suppression above, for the July 2026 Netty CVE
    batch fixed in 4.1.136.Final/4.2.16.Final (CVE-2026-56820 OCSP CertificateID-mismatch replay
    bypass, CVE-2026-56819 HTTP/2 codec memory leak, CVE-2026-55833 SPDY header-decoding DoS,
    CVE-2026-56816/-56817/-56745/-56746/-55831/-55851/-44891 from the same release). Unlike the
    2026-42582 suppression above (which argues the 4.1.x line structurally lacks the affected
    HTTP/3 codec), this batch genuinely affects both the 4.1.x and 4.2.x Netty lines - the real,
    shipped 4.2.x occurrence is fixed by the `netty.version` BOM override in build.gradle.kts.
    This suppression covers only the 4.1.135.Final jars, confirmed via
    `./gradlew :keycloak-spi:dependencyInsight` to be compileOnly/testImplementation transitives
    of keycloak-services 26.7.0 (via Quarkus/Vert.x) on the keycloak-spi module, absent from
    every basetool runtime classpath (including `:keycloak-spi:runtimeClasspath` itself) - the
    real Keycloak container supplies its own Netty at runtime. CVE-scoped and pinned to the 4.1.x
    line so a genuinely-vulnerable 4.1.x Netty ever entering a real classpath still fails the gate.
  -->
  <suppress>
    <notes>False positive: build-time-only. The July 2026 Netty CVE batch (CVE-2026-56820, CVE-2026-56819, CVE-2026-55833, CVE-2026-56816, CVE-2026-56817, CVE-2026-56745, CVE-2026-56746, CVE-2026-55831, CVE-2026-55851, CVE-2026-44891 - fixed in 4.1.136.Final/4.2.16.Final) genuinely affects the 4.1.x line too, but our 4.1.135.Final jars are compileOnly/testImplementation transitives of keycloak-services 26.7.0 (via Quarkus/Vert.x) on the keycloak-spi module only, confirmed absent from every basetool runtime classpath; Keycloak supplies its own Netty at runtime. The real, shipped 4.2.15.Final line is fixed via the netty.version BOM override in build.gradle.kts.</notes>
    <packageUrl regex="true">^pkg:maven/io\.netty/netty\-.*@4\.1\.135\.Final$</packageUrl>
    <cve>CVE-2026-56820</cve>
    <cve>CVE-2026-56819</cve>
    <cve>CVE-2026-55833</cve>
    <cve>CVE-2026-56816</cve>
    <cve>CVE-2026-56817</cve>
    <cve>CVE-2026-56745</cve>
    <cve>CVE-2026-56746</cve>
    <cve>CVE-2026-55831</cve>
    <cve>CVE-2026-55851</cve>
    <cve>CVE-2026-44891</cve>
  </suppress>

  <!--
    August 2026 Netty batch, same situation as the July batch directly above and suppressed on the
    same grounds - but re-verified rather than assumed, because the argument only holds while two
    facts stay true:

      1. The SHIPPED line is not affected. Dependency-Check flagged only the `4.1.135.Final` jars
         in this batch; the `4.2.17.Final` jars are on the very same scanned classpaths and were
         NOT flagged, so 4.2.17 is outside the affected range. If a future batch flags the 4.2.x
         jars too, this suppression must NOT be extended - bump `netty.version` instead.
      2. The 4.1.x jars never reach a runtime classpath. Re-confirmed on 2026-08-09 by running
         `dependencyInsight` against the `runtimeClasspath` configuration for the dependency
         `io.netty:netty-common` in all four modules (see the note element below for the exact
         invocation, which cannot be spelled here because XML comments forbid double hyphens):
         backend, frontend and ingest all resolve 4.2.17.Final ("selected by rule", i.e. the BOM
         override), and `:keycloak-spi:runtimeClasspath` contains no Netty at all. The 4.1.135
         jars are compileOnly transitives of keycloak-services 26.7.1 via Quarkus/Vert.x, and the
         real Keycloak container supplies its own Netty.

    Pinned to the exact 4.1.135.Final coordinate and scoped per-CVE, so any other 4.1.x Netty
    entering a real classpath still fails the gate.
  -->
  <suppress>
    <notes>False positive: build-time-only. The August 2026 Netty CVE batch affects the 4.1.x line, but our 4.1.135.Final jars are compileOnly transitives of keycloak-services 26.7.1 (via Quarkus/Vert.x) on the keycloak-spi module only. Re-verified 2026-08-09 with `./gradlew :&lt;module&gt;:dependencyInsight --configuration runtimeClasspath --dependency io.netty:netty-common` across backend/frontend/ingest/keycloak-spi: the first three resolve 4.2.17.Final via the BOM override and keycloak-spi's runtimeClasspath carries no Netty at all. The shipped 4.2.17.Final line is not flagged by this batch.</notes>
    <packageUrl regex="true">^pkg:maven/io\.netty/netty\-.*@4\.1\.135\.Final$</packageUrl>
    <cve>CVE-2026-56821</cve>
    <cve>CVE-2026-56822</cve>
    <cve>CVE-2026-59898</cve>
    <cve>CVE-2026-59899</cve>
    <cve>CVE-2026-59900</cve>
    <cve>CVE-2026-59901</cve>
    <cve>CVE-2026-59919</cve>
    <cve>CVE-2026-59920</cve>
    <cve>CVE-2026-59921</cve>
  </suppress>

  <!--
    CVE-2026-66299 is an uncontrolled-resource-consumption DoS in the WebSocket **chat example**
    of Apache Tomcat (affected: 11.0.0-M20 through 11.0.24; fixed in 11.0.25, unreleased at the
    time of writing). The advisory's own mitigation is "remove the examples web application" -
    and an embedded Tomcat never had it in the first place.

    The examples webapp ships in the standalone Tomcat *binary distribution* under
    `webapps/examples`. It is not part of the `tomcat-embed-*` artifacts, and Spring Boot deploys
    only this application's own servlet context. Verified on 2026-08-09 rather than reasoned
    about: `tomcat-embed-core-11.0.24.jar` contains 1611 entries under exactly three top-level
    package roots (`jakarta/servlet/`, `jakarta/security/`, `org/apache/`) and zero entries
    matching `websocket/chat`, `ChatAnnotation` or `examples`. The vulnerable code is absent from
    the artifact, so no configuration of ours can reach it.

    Same class as the netty HTTP/3 suppression higher up: the broad
    `cpe:2.3:a:apache:tomcat` match attributes a distribution-only vulnerability to the embedded
    library. This is NOT a "real CVE we chose to live with" - CLAUDE.md forbids that, and rightly.

    Pinned to 11.0.24 so the entry lapses on its own: bumping to 11.0.25 (which fixes this
    upstream) stops the match, and the suppression should be deleted in that same change. Any
    other Tomcat CVE, and any other embedded-Tomcat version, still fails the gate.
  -->
  <suppress>
    <notes>False positive: CPE scope confusion. CVE-2026-66299 affects the WebSocket chat example in Tomcat's `webapps/examples`, which ships only with the standalone binary distribution. Verified 2026-08-09 that tomcat-embed-core-11.0.24.jar contains no examples/websocket-chat classes at all (1611 entries, only jakarta/servlet, jakarta/security and org/apache roots), so the vulnerable code is not on any classpath here. Remove this entry when the Boot BOM or our override moves to Tomcat 11.0.25, which fixes it upstream.</notes>
    <packageUrl regex="true">^pkg:maven/org\.apache\.tomcat\.embed/tomcat\-embed\-.*@11\.0\.24$</packageUrl>
    <cve>CVE-2026-66299</cve>
  </suppress>

</suppressions>
