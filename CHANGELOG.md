# Changelog

## [Unreleased]

### Added

- **Monitoring: Host-Sicherheits-Logs (auditd, fail2ban) in Loki + Tamper-Alarm.** Alloy schickt jetzt zwei zusätzliche Host-Streams nach Loki: `host-auditd` (auditd-Wachregeln auf `sshd_config`/`authorized_keys`) und `host-fail2ban` (Sperren des SSH-Jails). Der neue Alarm `AuditdSshTamper` schlägt an, wenn die SSH-Konfiguration oder eine `authorized_keys`-Datei geändert wird. Beide Streams enthalten IP-Adressen/Benutzernamen (31 Tage, nur für Administratoren) — die Datenschutzerklärung wurde entsprechend erweitert (REQ-OBS-007/-010).

### Security

- **Edge-Proxy-Container (nginx-proxy-manager) gehärtet.** Der Reverse-Proxy läuft jetzt ohne Privilegien-Eskalation (`no-new-privileges`), mit minimalem Capability-Set (`cap_drop: ALL` plus dokumentierter Allow-List) und einem Prozess-Limit — als exponiertester Container des Hosts erhält er damit dieselbe Härtungs-Baseline wie der Monitoring-Stack (REQ-OPS-014).

- **Rate-Limiting am Edge.** Jeder öffentliche Proxy-Host begrenzt jetzt pro Client-IP die Anfragerate (20 r/s, Burst 80) und die gleichzeitigen Verbindungen (60) — ein versioniertes Sicherheitsnetz gegen Fluten und Brute-Force, das legitime Seitenaufrufe nicht berührt; Ablehnungen antworten mit 429 und lösen bei anhaltender Rate den neuen Alarm `EdgeRateLimitSpike` aus (REQ-SEC-023).

- **Monitoring: Edge-Konfiguration wird jetzt dauerhaft verifiziert statt einmalig.** Neue Blackbox-Proben schlagen Alarm, wenn die `/actuator`-Sperre am Edge, die HTTP→HTTPS-Umleitung oder der HSTS-Header eines öffentlichen Hosts wegdriftet; die Erreichbarkeits-Sperre der Keycloak-Admin-Konsole prüft täglich ein externer GitHub-Actions-Lauf, da sie von innen prinzipbedingt nicht messbar ist (REQ-OBS-012).

### Removed

- **Monitoring-Stack: GitHub-Dashboard + github-exporter entfernt (Epic #936).** Das Dashboard „Development / GitHub“ und der komplette `github-exporter` (Compose-Service, Prometheus-Scrape-Job, `github_token`-Secret) sind entfernt. Die Repo-Kennzahlen (Stars/Forks/Issues) funktionierten, aber die Workflow-Run-Panels blieben „No data“: dieser Collector ist webhook-getrieben und hätte einen öffentlichen `/github`-Endpunkt plus Secret und persistenten Store gebraucht — zu viel neue Angriffsfläche für ein Dev-Dashboard.

### Fixed

- **Monitoring-Stack: Rollout-Korrekturen (Epic #936).** Mehrere den Start blockierende Fehler im v1.1.0-Monitoring-Stack behoben: korrigierte Container-Image-Referenzen (Grafana 13.0.2, `docker-socket-proxy` `v0.4.2`), lauffähiges Alloy (Storage- und `auth.log`-Zugriff über Gruppenrechte statt Capability, korrigierte `.targets`-Referenzen, `NETWORKS`-Freigabe am Docker-Socket-Proxy), ein Keycloak-Absturz beim Aktivieren der Nutzer-Ereignis-Metriken (ungültiger Ereigniswert `login_error` entfernt) sowie der für die distroless-Images ungeeignete Docker-Healthcheck von Loki und Tempo.
- **Monitoring-Stack: Rollout-Feinschliff (Epic #936).** Grafana- und Tempo-Speicherlimits auf dem 16-GB-Host angehoben (behebt OOM-Neustartschleifen unter Last), NPM↔Grafana über ein eigenes Proxy-Netz verdrahtet (Grafana unter `grafana.profit-base.online` erreichbar, ohne NPM ins geschlossene Monitoring-Kernnetz zu legen), die Blackbox-Probe des auth-geschützten ingest-Endpunkts akzeptiert nun 401 als „erreichbar" (echte Ausfälle alarmieren weiterhin), und der Audit-Stille-Alarm greift erst nach 5 Tagen statt 6 Stunden (keine Fehlalarme in ruhigen Phasen).
- **Monitoring-Stack: Container-Dashboard zeigt wieder Daten (Epic #936).** cAdvisor bindet jetzt den containerd-Socket (`/run/containerd/containerd.sock`, nur lesend) ein: ohne ihn scheiterte die Registrierung der Docker-Container-Factory, und cAdvisor lieferte unter `--docker_only` keinerlei benannte Container-Metriken — alle Kacheln des Container-Dashboards blieben „No data". Der Docker-Socket-Proxy deckt nur die Docker-REST-API ab, nicht containerd; die Sicherheitsabwägung des zusätzlichen Mounts ist in ADR-0072 §6 dokumentiert (Owner-Freigabe).
- **Monitoring-Stack: Sync-Job-Panels und -Alarme korrekt benannt (Epic #936).** Im Dashboard „Basetool operations" erschienen alle geplanten Jobs (User-, UEX-, SCwiki-Sync usw.) als „basetool-backend", und die Alarme `UserSyncStale`/`ExternalSyncStale` feuerten nie: das anwendungsseitige `job`-Label kollidiert mit dem Prometheus-Scrape-Label `job` und wird von Prometheus als `exported_job` reexponiert. Panels und Alert-Regeln matchen jetzt auf `exported_job`, sodass die Jobs einzeln benannt sind und die Alarme greifen.
- **Monitoring-Stack: Feste, lesbare Container-Namen + Container-Dashboard-Legenden (Epic #936).** Alle Prod- und Monitoring-Container haben jetzt feste `container_name` (z. B. `backend`, `cadvisor` statt `code-backend-1` bzw. `iri-monitoring-cadvisor-1`); die lokalen dev-/test-Stacks bleiben bewusst projekt-isoliert (ohne `container_name`, damit dev- und Test-Stack weiter parallel laufen). Das Container-Dashboard beschriftet seine Panels jetzt mit `{{name}}` (zuvor kippte Grafana den kompletten Label-Satz in die Legende); die Netzwerk-Panels aggregieren pro Container.

## [v1.1.0](https://github.com/krt-profit/basetool/releases/tag/v1.1.0) - 2026-07-04

### Added

- **Monitoring-Stack: Prometheus, Grafana, Loki, Tempo und Alloy für Host, Container und Anwendungen.** Der Produktions-Host betreibt jetzt ein vollständiges, ausschließlich für Administratoren zugängliches Betriebs-Monitoring (Metriken mit 180-Tage-Archiv, Logs, verteilte Traces) mit E-Mail-Alarmen inklusive Totmann-Schalter. Grafana ist die einzige Oberfläche, angebunden über Keycloak-OIDC und beschränkt auf die Realm-Rolle „Admin“; die übrigen Komponenten sind nur in isolierten Docker-Netzen erreichbar. Umfasst 13 Dashboards (u. a. Host, Container, Spring-Boot-Apps inkl. Caffeine-Trefferquoten, PostgreSQL, Redis, Keycloak-Anmeldungen, Aufträge/Freigaben/Warteschlangen, Edge/NPM, SSH-Anmeldungen, Logs, Deploy-/Backup-/Restore-Automatisierung, GitHub) sowie Alarme zu Ausfällen, Fehlerraten, Sicherheitsvorfällen und Datenintegrität (Epic #936, ADR-0072).

- **Datenschutzerklärung: temporäre Speicherung von IP-Adressen für das Sicherheits-Monitoring.** Die Datenschutzerklärung wurde um einen Abschnitt zum Sicherheits-Monitoring ergänzt: Zugriffs- und Anmeldeprotokolle (Reverse-Proxy, SSH/Host, Keycloak) werden zur Missbrauchserkennung vorübergehend einschließlich IP-Adressen für maximal 31 Tage gespeichert und sind nur für Administratoren zugänglich (REQ-OBS-010).

- **Ops-Automatisierung liefert Betriebskennzahlen.** Deploy, Backup, Restore-Drill und Docker-Aufräumen schreiben jetzt Kennzahlen (Erfolg/Rollback/Fehler-Zeitstempel, Dauer, wiederhergestellte Artefakte, freigegebener Speicher), sodass verpasste oder fehlgeschlagene Läufe zuverlässig alarmiert werden.

## [v1.0.16](https://github.com/krt-profit/basetool/releases/tag/v1.0.16) - 2026-07-03

## [v1.0.15](https://github.com/krt-profit/basetool/releases/tag/v1.0.15) - 2026-07-03

### Added

- **E-Mail an Administratoren bei neuer Registrierung.** Sobald eine neue Registrierung auf Freigabe wartet, erhalten jetzt alle Administratoren mit hinterlegter E-Mail-Adresse zusätzlich zur bestehenden In-App-Benachrichtigung eine E-Mail — so bemerken auch Administratoren, die gerade nicht im Tool angemeldet sind, dass eine Freigabe ansteht. Der Versand nutzt denselben wiederverwendbaren E-Mail-Kanal wie die Freigabe-/Ablehnungs-Mails und verschickt erst tatsächlich Mails, sobald ein SMTP-Server konfiguriert ist (`SPRING_MAIL_HOST`) — ohne Host bleibt er ein No-Op (REQ-NOTIF-015, ADR-0064).

- **Monitoring: Prometheus-Metrik-Endpunkt in allen drei Modulen.** Backend, Frontend und Ingest exponieren jetzt Micrometer-Metriken (JVM, HTTP, Caches, Verbindungspools) unter `/actuator/prometheus` — abgesichert über einen fail-closed Basic-Auth-Zugang (`MONITORING_SCRAPE_USER`/`MONITORING_SCRAPE_PASSWORD`; ohne Konfiguration wird jeder Zugriff abgelehnt). Grundstein für den Monitoring-Stack aus Epic #936 (REQ-OBS-005, ADR-0072).

- **Monitoring: Verteiltes Tracing (OpenTelemetry) in allen drei Modulen vorbereitet.** Frontend-, Backend- und Ingest-Aufrufe können durchgängig als Traces verfolgt werden (W3C `traceparent`, Trace-IDs in den JSON-Logs). Query-Strings und rohe IDs werden aus Metrik-Tags und Span-Attributen entfernt. Standardmäßig vollständig inaktiv; erst `MONITORING_TRACING_ENABLED=true` plus OTLP-Endpunkt aktivieren den Export zum Monitoring-Stack (REQ-OBS-009, Epic #936 Phase 1b).
- **Monitoring: Fachliche Kennzahlen (`basetool_*`) in allen drei Modulen.** Backend, Frontend und Ingest liefern jetzt Betriebskennzahlen an den Metrik-Endpunkt: Gesundheit der geplanten Jobs (inkl. „User-Sync veraltet"), Sicherheits-/Regressionszähler (409-Optimistic-Lock-Rate, 401/403, Audit-Ereignisse je Bereich, Rate-Limit-Ablehnungen), Warteschlangen-Tiefen samt Alter des ältesten offenen Eintrags (Registrierungen, Bank-Buchungsanträge, Aufträge, Operationen, Raffinerie, P4K-Importe), die Bank-Integritätsprüfung (jeder Wert über 0 ist kritisch) sowie Live-Betrieb (Präsenz, aktive Sessions, Backend-Aufruf-Fehler, Ingest-Übergaben). Alle Labels sind beschränkt und enthalten keine Namen, IDs oder Beträge (REQ-OBS-011, Epic #936 Phase 1c).

### Fixed

- **Sicherheit: Eingebetteter Tomcat auf 11.0.23 angehoben.** Die Version schließt drei frisch veröffentlichte CVEs (u. a. RewriteValve-Bedingungsauswertung, CVE-2026-53404); Spring Boot 4.1.0 liefert noch 11.0.22 aus, daher wird die Version bis zum nächsten Boot-Patch zentral übersteuert.

- **Frontend: Caffeine-Cache erfasst jetzt Trefferstatistiken.** Der `staticData`-Cache liefert damit Hit-Ratio-Werte an den Metrik-Endpunkt; zuvor wären die Cache-Panels des künftigen Monitorings dauerhaft leer geblieben (Epic #936).

- **Ingest: Prod-Logs werden jetzt PII-maskiert.** Der Ingest-Dienst nutzt wie Backend und Frontend den maskierenden JSON-Encoder (JWTs, E-Mail-Adressen, Token-Schlüsselwörter); zuvor war er der letzte unmaskierte Log-Ausgang (REQ-OBS-004).

## [v1.0.14](https://github.com/krt-profit/basetool/releases/tag/v1.0.14) - 2026-07-03

## [v1.0.13](https://github.com/krt-profit/basetool/releases/tag/v1.0.13) - 2026-07-03

### Added

- **Bank: KRT-Konto mit dreistufiger Freigabe je nach Betrag.** Auszahlungen und Transfers vom KRT-Konto werden abhängig vom Betrag freigegeben: bis zur ersten Schwelle durch einen Bankmitarbeiter, darüber bis zur zweiten Schwelle durch den Bereichsleiter Profit, darüber durch die Organisationsleitung. Die beiden Schwellen pflegt ausschließlich die Bankleitung im neuen Reiter „KRT-Freigaben" der Bank-Verwaltung; die Freigaben durch Bereichsleiter Profit bzw. Organisationsleitung erfolgen in der Org-Einheits-Bank im Reiter „Fremde Anträge". Über der Bankmitarbeiter-Schwelle sind auch Direktbuchungen gesperrt (REQ-BANK-047, ADR-0066).

- **Bank: „Mitglieder des Bereichs" als Sichtbarkeits- und Limit-Zielgruppe für Bereichskonten.** Bereichskonten können ihren Kontostand und ihr Freigabe-Limit jetzt zusätzlich für alle Angehörigen des gesamten Bereichs (Bereichsleitung plus alle Mitglieder der zugehörigen Staffeln und Spezialkommandos) öffnen — neben den bisherigen Rollen, „Alle Mitglieder" und einzelnen Nutzern (REQ-BANK-048).

- **E-Mail-Benachrichtigung bei Freigabe oder Ablehnung der Registrierung.** Neue Nutzer werden jetzt per E-Mail informiert, sobald ein Administrator ihre Registrierung freigibt oder ablehnt; bei einer Ablehnung enthält die E-Mail die vom Administrator angegebene Begründung. Der E-Mail-Versand ist ein wiederverwendbarer Kanal (später auch für das Benachrichtigungssystem nutzbar); er ist standardmäßig aktiviert, verschickt aber erst tatsächlich Mails, sobald ein SMTP-Server konfiguriert ist (`SPRING_MAIL_HOST`, z. B. Gmail) — ohne Host bleibt er ein No-Op (REQ-NOTIF-013/-014, ADR-0064).

- **Meine Blueprints: Alle eigenen Blueprints mit einem Klick löschen.** Auf der Blueprint-Seite (`/personal-inventory/blueprints`) entfernt ein neuer Button nach Bestätigung alle eigenen entfernbaren Blueprints auf einmal; automatisch vergebene Standard-Blueprints bleiben erhalten (REQ-INV-023).

- **Admin: Blueprints aller Nutzer löschen.** Im Admin-Bereich „Pers. Blueprints" können Administratoren die Blueprints aller Nutzer auf einmal löschen. Die Aktion ist durch eine Tippbestätigung („LOESCHEN") abgesichert und meldet die Anzahl entfernter Blueprints; Standard-Blueprints bleiben erhalten (REQ-INV-024).

- **Kartellbank: Konten live nach Namen filtern.** Auf der Kartellbank-Übersicht (`/bank`) neben dem „Verwaltung"-Button und über der Kontotabelle der Org-Einheits-Bank (`/org-unit-bank`) blendet ein neues Suchfeld beim Tippen sofort alle Kontokacheln bzw. -zeilen aus, deren Name nicht passt — rein clientseitig, ohne Nachladen (REQ-BANK-046).

- **Einsatz anlegen: Ziele und Ablauf-Schritte gleich beim Anlegen erfassen.** Das Formular „Neuer Einsatz" zeigt jetzt über dem Beschreibungsfeld zwei optionale Editoren, um Ziele (inklusive Klassifizierung) und Ablauf-Schritte (mit optionaler Zeit/Ort-Angabe) direkt mitzugeben. Beides ist optional und lässt sich weiterhin auch später im Verwaltungs-Tab pflegen (REQ-MISSION-015).

- **Kartellbank-Audit: Wechsel des/der Kontoverantwortlichen wird protokolliert.** Ändert sich der/die für ein Bankkonto Verantwortliche — weil sich die zugrunde liegende Führung ändert (neuer Staffelleiter, SK-Leitung, Bereichsleiter oder OL), ein Mitglied mit Leitungsrolle aus seiner Einheit entfernt oder ein Nutzer gelöscht wird —, hält das Bank-Audit dies jetzt als eigenes Ereignis fest: mit altem und neuem Verantwortlichen sowie der veranlassenden Person (REQ-BANK-034, ADR-0070).

### Changed

- **Bank: „Alle Mitglieder"-Freigabe-Limit gilt nur noch für echte Mitglieder der Org-Einheit.** Das „Alle Mitglieder"-Limit eines Kontos greift jetzt ausschließlich für Mitglieder der kontoeigenen Org-Einheit (Staffel → Staffelmitglieder, SK → SK-Mitglieder, Bereichskonto → Bereichsleitung, KRT-Konto → Organisationsleitung). Außenstehende, die ein Konto nur über eine Einzel-Freigabe sehen, brauchen ein eigenes Limit — sonst ist eine Freigabe nötig. Die Sichtbarkeit des KRT-Kontos für alle Kartellmitglieder bleibt unverändert (REQ-BANK-041/-046).

- **Discord-Registrierung: Warteseite macht die Freigabepflicht deutlicher.** Die Warteseite nach der Discord-Anmeldung sagt neuen Nutzern jetzt klar, dass sie das Tool erst nach der Freigabe durch einen Administrator nutzen können, und weist darauf hin, dass die Freigabe manuell erfolgt und 1 bis 2 Tage dauern kann (REQ-SEC-017).

- **Benachrichtigungen: Löschen ohne Rückfrage.** Eine einzelne Benachrichtigung im Glocken-Menü oder auf der Benachrichtigungsseite wird jetzt sofort gelöscht — die zusätzliche Bestätigungsabfrage entfällt, da Benachrichtigungen unkritisch sind. Das Sammel-Löschen „Gelesene löschen" fragt weiterhin nach (REQ-NOTIF-005).

- **Bank: Kontoname in der Antragsansicht besser sichtbar.** In der Antragsübersicht (`/bank/requests`) zeigt die Konto-Spalte jetzt den Kontonamen an erster Stelle; die Kontonummer steht klein darunter, das Einheitskürzel entfällt (REQ-BANK-023).

- **API: Sortier-Tokens der Einsatz-Listen einheitlich in Kleinschreibung.** Die Einsatz- und Einsatz-Finanz-Listen geben die Sortierrichtung im `sort`-Feld ihrer Antwort jetzt wie alle anderen Listen klein zurück (`feld,asc` statt `feld,ASC`) — interne Vereinheitlichung der Paginierungs-Hülle ohne Funktionsänderung (#906).

- **API: Rate-Limit-Antwort (429) vereinheitlicht.** Der 429-Body des Ratenlimiters trägt jetzt wie die übrigen Fehler einen stabilen `code` (`RATE_LIMIT_EXCEEDED`) und eine `correlationId`, und Titel/Detail werden gemäß `Accept-Language` lokalisiert statt fest englisch ausgegeben (#906).

- **API/Fehler: Einheitliches RFC-7807-Fehlerformat auch an den Rändern.** Abgewiesene Anfragen der Sicherheitsschicht (fehlendes/ungültiges Token → 401, fehlende Berechtigung → 403), die Sperre noch nicht freigeschalteter Konten (403) und Container-Fehler liefern jetzt wie alle übrigen Fehler ein `application/problem+json` mit stabilem `code` und `correlationId` (auch als `X-Correlation-Id`-Header) statt der Spring-Standardantworten. Die Fehlerseite des Frontends zeigt die `correlationId` als Referenzcode zum Nennen beim Support an.

- **Einsatz anlegen: Nach dem Speichern direkt im Verwaltungs-Tab des neuen Einsatzes.** Ein neu angelegter Einsatz führt jetzt unmittelbar auf dessen Detailseite in den Verwaltungs-Tab, statt zurück zur Einsatzliste — so lässt sich der Einsatz ohne Umweg weiter einrichten (REQ-MISSION-015).

- **Einsatz-Verwaltung: Aktionsleiste (Löschen · Speichern · Zurück) am unteren Rand angeheftet.** Im Verwaltungs-Tab (und auf dem Anlege-Formular) bleibt die Button-Zeile fix am unteren Rand über der Fußleiste, während nur der übrige Inhalt scrollt — so sind die Aktionen beim Ausfüllen und Scrollen langer Formulare immer erreichbar (REQ-MISSION-015).

- **Einsatz erstellen: Anzeigename einer Einheit ist jetzt Pflichtfeld.** Beim Anlegen einer Einheit ist der Anzeigename das einzige Pflichtfeld (mit Stern markiert); Schiffstyp und Schiff bleiben optional (REQ-MISSION-006).

- **Einsatz: Zeichenlimits angehoben.** Ziele und Ablauf-Schritte erlauben jetzt bis zu 500 Zeichen im Titel (vorher 250 bzw. 200), die Einsatz-Beschreibung bis zu 20.000 Zeichen (REQ-MISSION-009/-012).

- **Einsatz: Teilnehmer-Org-Einheiten nur noch bei Gästen auswählbar.** Beim Anmelden und Bearbeiten eines angemeldeten Nutzers muss keine Org-Einheit mehr ausgewählt werden — sie werden automatisch aus dem Account gelesen und im Bearbeiten-Dialog schreibgeschützt angezeigt. Die Auswahl erscheint nur noch für Gäste (REQ-MISSION-006).

- **Einsatz-Übersicht: „Einsatzleiter" in „Mission auf einen Blick".** In der Kachel „Mission auf einen Blick" steht jetzt über dem Partyleiter ein Eintrag für den Einsatzleiter — mit derselben Logik wie das „Leiter"-Feld in der Infoleiste (REQ-MISSION-013).

- **Kartellbank: Kontoansicht übersichtlicher.** Die Buchungshistorie in der Bank und der Org-Einheits-Bank nennt die „Gegenpartei"-Spalte jetzt „Quell-/Zielkonto" (vereinheitlicht: Gegenkonto einer Umbuchung bzw. Einzahler/Empfänger) und zeigt Begründung und Notiz einer Buchung erst nach Klick auf die Zeile in einer ausklappbaren Unterzeile (Begründung zuerst); die Konto-Infos samt Freigabe-Limits stehen als standardmäßig eingeklappte Kachel über der Historie. Mitglieder sehen den Einzahler/Empfänger jetzt ebenfalls (nur der Halter bleibt verborgen), und die PDF-Kontoauszüge sowie der Monatsreport übernehmen dieselbe Umbenennung samt Begründung/Notiz-Unterzeile je Buchung (REQ-BANK-044/-038/-014/-017).

### Fixed

- **Einsatz anlegen: Formular nutzt jetzt die volle Breite.** Das Formular „Neuer Einsatz" (`/missions/new`) stand bisher in der linken Bildschirmhälfte, weil die rechte Spalte des Verwaltungs-Rasters nur die reinen Bearbeitungs-Editoren enthält, die beim Anlegen fehlen. Auf der Anlege-Seite fällt das Raster jetzt auf eine einzelne, volle Spalte zusammen, sodass die Details-Karte zentriert die gesamte Breite unter der Überschrift einnimmt (REQ-MISSION-015).

- **Kein selbst verursachter Bearbeitungskonflikt mehr beim schnellen Bearbeiten.** Wer ein Ziel oder einen Ablauf-Schritt eingab und sofort auf „+", das Klassifizierungs-Dropdown oder die Sortierpfeile klickte, erhielt bisher fälschlich die Konflikt-Warnung („Aktuelle Werte laden?") — und das gerade Eingegebene konnte dabei verloren gehen. Aufeinanderfolgende eigene Änderungen desselben Abschnitts werden jetzt der Reihe nach gespeichert und übernehmen jeweils den aktuellen Stand. Tool-weit umgesetzt (u. a. auch Kartellbank-Freigaben, Lager-Zuordnungen und -Notizen, Auftragsstatus und Org-Struktur), REQ-FE-012.

- **Einsatz: Live-Synchronisation aktualisiert jetzt auch Ablauf, Ziele und Funkfrequenzen bei anderen Betrachtern.** Bearbeitete ein Nutzer den Ablauf, die Ziele oder die Funkfrequenzen eines Einsatzes, blieben die Verwaltung-Editoren anderer Betrachter desselben Einsatzes bis zum manuellen Neuladen veraltet, weil das Änderungssignal für diese Abschnitte verworfen wurde. Jetzt ziehen alle Abschnitte live nach (REQ-FE-010).

- **Mitgliederverwaltung: Kein fälschlicher Bearbeitungskonflikt mehr bei direkt aufeinanderfolgenden Änderungen.** Nach dem Ändern der Logistiker-/Einsatzleiter-Flags eines Mitglieds oder dem Setzen/Entfernen der SK-Leitung liefert die Antwort jetzt den aktuellen Versionszähler der Mitgliedschaftszeile zurück. Bisher trug sie den veralteten Stand, wodurch die unmittelbar nächste Änderung an derselben Zeile fälschlich als Konflikt (409) abgewiesen wurde (REQ-FE-003, ADR-0067).

- **Benachrichtigungen: Admins werden jetzt zuverlässig über neue Konto-Freigabeanträge informiert.** Neue, noch nicht freigeschaltete Registrierungen (insbesondere über Discord) lösen jetzt immer genau eine Admin-Benachrichtigung aus — auch wenn der optionale Discord-Claim in Keycloak fehlt oder das Konto zuerst über die periodische Keycloak-Synchronisation statt über den Login angelegt wird. Bisher blieb die Meldung in diesen Fällen komplett aus (REQ-NOTIF-012).

- **Lager: Meldungen beim Ändern der Zuordnung erscheinen jetzt übersetzt.** Beim Ändern der Auftrags- oder Einsatz-Zuordnung eines Eintrags im gesamten Lager (`/inventory/all`) zeigten Erfolgs- und Fehlerhinweise bisher rohen Platzhaltertext (`[[#{...}]]`) statt der Meldung; jetzt erscheinen die lokalisierten Texte.

- **Lager: Rückfrage vor dem Entfernen einer Notiz erscheint jetzt wirklich.** Das Entfernen einer Notiz (im eigenen und im gesamten Lager) löschte bisher sofort ohne Nachfrage, weil der Bestätigungsdialog auf eine nicht existierende Funktion verwies; jetzt fragt der KRT-Bestätigungsdialog vor dem Entfernen nach.

- **Lager (gesamtes Lager): Der Notiz-Dialog öffnet sich jetzt zentriert.** Im gesamten Lager (`/inventory/all`) erschien der Dialog zum Bearbeiten einer Notiz am oberen Bildschirmrand statt mittig; jetzt ist er wie im eigenen Lager und allen anderen Dialogen zentriert (#906).

- **Deployment: Der Deploy-Zyklus erkennt jetzt einen abgedrifteten oder ungesunden Stack.** `deploy.sh` nimmt die „no change"-Abkürzung nur noch, wenn die laufenden Container tatsächlich den zuletzt ausgerollten Image-Digests entsprechen und gesund sind; andernfalls wird derselbe Stand automatisch neu angewendet — etwa nach einem manuellen `docker compose up` mit veraltetem lokalem `:stable`-Tag (Vorfall vom 02.07.2026). Für geplante Wartung vorher den `iri-deploy.timer` stoppen (REQ-OPS-013).

## [v1.0.12](https://github.com/krt-profit/basetool/releases/tag/v1.0.12) - 2026-07-01

### Changed

- **Einsatz-Übersicht: Leere „Ziele"- und „Ablauf"-Kacheln werden ausgeblendet; Beschreibung standardmäßig aufgeklappt.** In der Einsatz-Übersicht erscheinen die Kacheln „Ziele" und „Ablauf" nur noch, wenn auch Einträge hinterlegt sind — ohne Daten entfällt die Kachel ganz statt einen leeren Platzhalter zu zeigen. Die „Detaillierte Beschreibung" ist jetzt standardmäßig aufgeklappt (REQ-MISSION-004/-009/-012).

## [v1.0.11](https://github.com/krt-profit/basetool/releases/tag/v1.0.11) - 2026-07-01

### Added

- **Einsatz: Weitere, einsatzspezifische Funkfrequenzen.** Im Verwaltung-Tab eines Einsatzes lassen sich jetzt neben den allgemeinen Frequenztypen beliebige eigene Funkfrequenzen mit Bezeichnung und Wert anlegen, bearbeiten und löschen. Es gelten dieselben Eingabegrenzen wie für die allgemeinen Frequenzen (0–999,99, zwei Nachkommastellen). Sie erscheinen zusätzlich in der „Funk"-Box der Einsatzübersicht (REQ-MISSION-014, ADR-0058).

### Changed

- **Einsatz-Verwaltung: Kacheln neu sortiert, Kalenderlink ans Ende der Details.** Im Verwaltung-Tab stehen die Kacheln rechts jetzt in der Reihenfolge Ziele, Ablauf, Organisation, Verwaltungsrechte; der „Link zum Kalendereintrag" steht in der Details-Karte nun ganz am Ende.

### Security

- **Raffinerie: Nutzerbezogene Auftragsliste wird jetzt streng auf den eigenen Sichtbarkeitsbereich gefiltert.** Rief ein Logistiker die Raffinerie-Auftragsliste eines Mitglieds ab (`GET /api/v1/refinery-orders/users/{userId}`), bekam er bisher alle Aufträge des Mitglieds — auch solche, die einer Staffel zugeordnet sind, die der Logistiker selbst nicht sieht (möglich, weil ein Mitglied zu zwei Staffeln gehören kann). Die Liste wird jetzt pro Zeile auf den Org-Einheits-Sichtbarkeitsbereich des Aufrufers eingeschränkt und gibt keine fremden Staffel-Aufträge mehr preis (SEC-01).

- **Ratenbegrenzung: Die Client-IP-Erkennung im Frontend lässt sich nicht mehr per `X-Forwarded-For` fälschen.** Die anonymen Endpunkte (z. B. Auftrags-Anlage) sind pro IP ratenbegrenzt; bisher übernahm das Frontend die vom Client gesendete `X-Forwarded-For`-Adresse ungeprüft, sodass ein Angreifer mit pro Anfrage wechselnder Fälschung das Limit aushebeln konnte. Das Frontend ermittelt die echte Client-IP jetzt nur noch über vertrauenswürdige Proxys (NPM) und ignoriert vom Client gesetzte Werte (SEC-02).

- **Datenbank: Globales Abfrage-Zeitlimit verhindert das Blockieren von Verbindungen durch einzelne schwere Anfragen.** Jede Datenbankabfrage der Anwendung wird jetzt nach 30 Sekunden (über `APP_DB_QUERY_TIMEOUT_MS` anpassbar) abgebrochen. Bisher konnte eine einzelne sehr große oder langsame Abfrage eine Verbindung aus dem Pool unbegrenzt belegen; mehrere gleichzeitig hätten den Pool erschöpfen und die API ausbremsen können. Flyway-Migrationen sind ausgenommen (SEC-03).

## [v1.0.10](https://github.com/krt-profit/basetool/releases/tag/v1.0.10) - 2026-06-30

### Changed

- **Org-Einheits-Bank: „Verantwortung & Sichtbarkeit"-Kacheln breiter, Inhalt bricht nicht mehr um.** Im Reiter „Verantwortung & Sichtbarkeit" einer Konto-Detailseite (`/org-unit-bank/accounts/{id}`) sind die drei Einstellungskacheln jetzt breit genug, dass ihr Inhalt (etwa die Freigabe-Limit-Zeilen) auf einer Zeile bleibt; passen nicht mehr zwei nebeneinander, brechen sie in die nächste Zeile um, statt den Inhalt zu quetschen. „Kontostandsziel" steht jetzt als letzte Kachel (REQ-BANK-038).

## [v1.0.9](https://github.com/krt-profit/basetool/releases/tag/v1.0.9) - 2026-06-30

### Added

- **Einsatz: Strukturierte, klassifizierte Ziele statt einzelnem Kurz-Ziel.** Ziele werden jetzt — wie der Ablauf — als einzelne Punkte im Verwaltung-Tab gepflegt und als Hauptziel, Nebenziel oder Nicht-Ziel (ausdrücklich kein Ziel des Einsatzes) klassifiziert. In der Übersicht erscheinen sie in einer eigenen „Ziele"-Box, gruppiert (Hauptziele, dann Nebenziele, dann Nicht-Ziele). Das bisherige einzelne Kurz-Ziel entfällt; vorhandene Texte werden automatisch in ein Hauptziel überführt (REQ-MISSION-012, ADR-0057).

- **Einsatz: Treffpunkt zusätzlich in der Fakten-Leiste und auf der Startseiten-Kachel.** Der Treffpunkt erscheint nun auch in der Fakten-Leiste oben (hinter der Ende-Uhrzeit) und auf der Startseite in der Einsatzkachel zwischen Status und „Treffen Teamspeak" — jeweils nur, wenn er gesetzt ist.

### Changed

- **Einsatz: Die Fakten-Leiste „Leiter" zeigt jetzt den Einsatzleiter statt des Partyleiters.** Oben im Einsatz wird als „Leiter" der Einsatzleiter angezeigt — der Teilnehmer mit der als Einsatzleiter ausgewiesenen Führungsposition; ist keiner gesetzt, der Ersteller des Einsatzes. Die Führungsposition „Einsatzleiter" wird in der Missions-Stammdaten-Verwaltung an genau einer MISSION-Führungsposition markiert; pro Einsatz kann es nur einen Einsatzleiter geben (eine zweite Zuweisung wird abgelehnt) (REQ-MISSION-013).

- **Einsatz-Übersicht: Spalten der Kästen getauscht.** Links stehen jetzt Ziele, Ablauf, Teilnehmer und Kalender, rechts „Mission auf einen Blick", weitere Leads und Funk.

- **Bank: Freigabe-Limits werden in der Kontoübersicht nur noch angezeigt, nicht mehr bearbeitet.** Auf der Bank-Kontoseite (`/bank/accounts/{id}`) können Freigabe-Limits jetzt für alle — auch Bank-Management und Admin — nur noch schreibgeschützt eingesehen werden (sofern gesetzt). Gesetzt und entfernt werden sie ausschließlich in der Org-Einheits-Bank durch den Kontoverantwortlichen, das Bank-Management und Admins (REQ-BANK-041).

- **Bank: Konten werden in allen Auswahlfeldern und Übersichten alphabetisch nach Namen sortiert.** Überall, wo Konten gelistet werden — Dashboard, Bank-Verwaltung, Berechtigungen-Filter, das Zielkonto-/Quellkonto-Auswahlfeld in den Buchungs- und Antragsdialogen sowie die Kontenkarten der Org-Einheits-Bank — erscheinen sie jetzt einheitlich A→Z nach Kontonamen (ohne Groß-/Kleinschreibung zu unterscheiden) (REQ-BANK-016).

- **Org-Einheits-Bank: „Verantwortung & Sichtbarkeit" zeigt Kontostandsziel, Sichtbarkeit und Freigabe-Limits in drei eigenen Kacheln.** Im Reiter „Verantwortung & Sichtbarkeit" einer Konto-Detailseite (`/org-unit-bank/accounts/{id}`) stehen die drei Einstellungsbereiche nun in je einer eigenen Box, die je nach Bildschirmbreite nebeneinander stehen oder umbrechen (bis zu einer Spalte auf dem Smartphone), statt untereinander in einer Box (REQ-BANK-038).

- **Datenschutzerklärung auf den aktuellen Stand gebracht.** Neu beschrieben sind die Anmeldung über Discord (übermittelte Profildaten, Mitgliedschafts- und Rollenprüfung im Discord-Server, dauerhaft gespeicherte Discord-ID und Server-Nickname, Datenübermittlung an Discord Inc. in die USA), die im Mitgliederkonto gespeicherten Daten samt tatsächlichem Löschverhalten sowie Hinweise zu In-App-Benachrichtigungen, Echtzeit-Anwesenheit in der Einsatzplanung und der begleitenden Desktop-Import-Anwendung. Veraltete bzw. unbelegte Passagen (nicht vorhandenes Kontaktformular/Forum, die 7-Tage-IP-Anonymisierungszusage im Server-Log-Abschnitt) wurden entfernt und Cookie-Angaben korrigiert (SESSION bleibt bis zu 30 Tage erhalten).

### Fixed

- **Org-Einheits-Bank: Freigabe-Limit-Eingabe für einzelne Nutzer wieder bündig.** Im Formular „Einzelne Nutzer" der Freigabe-Limits saß das Limit-Eingabefeld 12 px höher als das Nutzer-Auswahlfeld und der „Setzen"-Button; jetzt stehen alle drei wieder auf gleicher Höhe.

## [v1.0.8](https://github.com/krt-profit/basetool/releases/tag/v1.0.8) - 2026-06-30

### Added

- **Bank: Begründung bei Auszahlungen und Transfers (Pflicht bei KRT-, Bank- und Sonderkonten).** Eine Auszahlung oder ein Transfer — sowohl die Bankmitarbeiter-Buchung als auch der Buchungsantrag — kann jetzt eine Begründung tragen. Beim Abruf aus dem KRT-Konto, dem Bank-Konto oder einem Sonderkonto ist sie Pflicht, bei Staffel-, SK- und Bereichskonten optional (wie die Notiz); bei Einzahlungen entfällt das Feld. Die Begründung wird wie die Notiz in der Kontohistorie und im Kontoauszug- sowie Management-Report-PDF angezeigt und vom Antrag bis zur Bestätigung übernommen (REQ-BANK-045).

### Changed

- **Bank: Anträge ohne Freigabe-Limit brauchen jetzt immer die Freigabe des Kontoverantwortlichen.** Auf Konten mit Freigabe-Limits gilt nun: Ist für einen Nutzer — direkt oder über seine Rolle (z. B. KL) — kein Limit gesetzt, muss sein Auszahlungs-/Transfer-Antrag immer vom Kontoverantwortlichen freigegeben werden; ist ein Limit gesetzt, nur noch oberhalb des Limits. Zuvor bedeutete „kein Limit" unbegrenzt ohne Freigabe (REQ-BANK-041).

- **Bank-Antragsdialog: Zielkonto und Prozentsatz erscheinen nur, wenn sie gebraucht werden.** Im Antragsdialog auf /org-unit-bank wird „Zielkonto" nur noch bei einem Transfer angezeigt und das Prozentsatz-Feld nur, wenn der Haken „Anteil gleichmäßig auf alle Staffelkonten verteilen" gesetzt ist (zuvor waren beide Felder immer sichtbar). Das Prozentsatz-Feld verhält sich ebenso im Einzahlungsdialog der Bankmitarbeiter.

- **Bank-Verwaltung: „Halter" ist jetzt der erste und standardmäßig geöffnete Tab.** Auf /bank/manage steht der Halter-Tab nun links und wird beim Aufruf ohne Tab-Auswahl direkt geöffnet; der Konten-Tab folgt rechts.

## [v1.0.7](https://github.com/krt-profit/basetool/releases/tag/v1.0.7) - 2026-06-30

### Added

- **Automatische, verschlüsselte Off-Site-Backups auf Nextcloud mit wöchentlicher Wiederherstellungsprobe.** Der Produktionsserver sichert jede Nacht um 04:15 beide Datenbanken (Anwendung + Keycloak), die Reverse-Proxy-Konfiguration und die für eine vollständige Wiederherstellung nötigen Host-Geheimnisse client-seitig verschlüsselt per restic auf eine Nextcloud-Freigabe (Aufbewahrung 7 täglich/4 wöchentlich/6 monatlich, ausgehend, ohne offenen Zugang zum Server). Für konsistente Daten werden die schreibenden Dienste nur für den Datenbank-Dump kurz pausiert (Sekunden, innerhalb des Fensters 04:00–05:00; der langsame Upload läuft danach im laufenden Betrieb). Eine wöchentliche Probe spielt das jüngste Backup testweise in eine Wegwerf-Datenbank zurück und meldet einen Fehler, falls es nicht sauber wiederherstellbar ist. Redis (Sitzungen) und der WireGuard-Schlüssel bleiben bewusst außen vor. Details: docs/backup.md, REQ-OPS-008..012, ADR-0056.

## [v1.0.6](https://github.com/krt-profit/basetool/releases/tag/v1.0.6) - 2026-06-30

### Changed

- **Startseite: „Meine Einheit"-Markierung sitzt jetzt unten rechts auf der Kachel.** Bei den Einsätzen der nächsten 7 Tage erscheint die „Meine Einheit"-Markierung nicht mehr oben links (was den Text nach unten schob), sondern unten rechts neben „Einsatz öffnen" — so beginnt der Text auf allen Kacheln auf gleicher Höhe, egal ob die Markierung vorhanden ist oder nicht.

## [v1.0.5](https://github.com/krt-profit/basetool/releases/tag/v1.0.5) - 2026-06-30

### Added

- **Bank: Ein- und Auszahlungen halten fest, von wem bzw. an wen gezahlt wird.** Beim Buchen einer Einzahlung lässt sich jetzt optional der Einzahler (und, falls er einer Einheit angehört, dessen Einheit — Staffel, Spezialkommando, Bereich oder OL) erfassen, bei einer Auszahlung der Empfänger (und ggf. Einheit) — beide werden aus den Tool-Nutzern bzw. deren Mitgliedschaften gewählt. Die Gegenpartei erscheint in der Kontohistorie, im Kontoauszug (Spalte „Gegenseite", die bei Transfers das Gegenkonto zeigt) und im Admin-Audit-Log. Auf den eingeschränkten Mitglieder-Ansichten wird sie wie der Halter ausgeblendet; bei einem bestätigten Buchungsantrag werden der Antragsteller und seine primäre Einheit als Gegenpartei vermerkt.

- **Bank: Einzahlung optional anteilig auf alle Staffelkonten verteilen.** Bei der Einzahlung (Bankmitarbeiter) und im Einzahlungsantrag (jeder Nutzer) lässt sich per Häkchen ein Prozentsatz (1–100) angeben: dieser Anteil des Betrags wird gleichmäßig auf alle aktiven Staffelkonten verteilt, das angegebene Konto erhält den Rest. Die Beträge bleiben ganzzahlig (Rest-aUEC werden gleichmäßig vergeben) und summieren sich exakt zum Einzahlungsbetrag; bei einem Antrag wird der Prozentsatz gespeichert und erst bei der Bestätigung auf die dann aktiven Staffelkonten aufgeteilt. Eine Split-Einzahlung erscheint im Audit-Log als eigener Eintrag (REQ-BANK-043).

- **Nutzer-Auswahlfelder sind durchsuchbar (Live-Suche nach Nutzername und Anzeigename).** Überall, wo ein Nutzer ausgewählt wird — u. a. Missions-Besitzer/-Manager/-Verantwortliche und Finanz-Teilnehmer, Auftrags-Zuweisung, Raffinerie-Besitzer und Einlagern, Bank-Halter/-Berechtigungen/-Limits, Inventar-Zuordnung und Umbuchen, Leitung, Spezialkommando, Admin-Inventar/-Blueprints sowie Benachrichtigungsregeln — ist das Dropdown jetzt eine Such-Combobox: Tippen filtert die Liste nach Nutzername **und** Anzeigename, damit lange Nutzerlisten handhabbar bleiben. Bank-Halter-Felder durchsuchen den Handle. Künftig müssen alle Nutzer-Auswahlfelder so gestaltet sein (REQ-FE-011, ADR-0053).

- **Discord-Anmeldung: keine doppelten Konten mehr.** Wer sich erstmals über Discord anmeldet und dessen Discord-Benutzername, Server-Nickname oder E-Mail bereits zu einem vorhandenen Konto passt, wird mit einem lokalisierten Hinweis abgewiesen, das vorhandene Konto stattdessen mit Discord zu verknüpfen — statt ein zweites Konto anzulegen. Die Prüfung ist fail-open (sie blockiert nur bei eindeutiger Übereinstimmung und lässt bei Unklarheit normal in die Freigabe-Warteschlange durch) und läuft ausschließlich über HTTPS.

### Changed

- **Bank: Überweisungsgebühr trägt das belastete Konto — der angeforderte Betrag kommt voll an.** Die In-Game-Gebühr (Standard 0,5 %) wird bei Auszahlungen und halterwechselnden Konto-Transfers jetzt _obendrauf_ berechnet und vom Quellkonto getragen, statt vom Betrag abgezogen zu werden: Wer 500.000 abbuchen oder transferieren will, dem werden 502.500 belastet, sodass beim Ziel die vollen 500.000 ankommen. Reicht der Kontostand nicht für Betrag plus Gebühr, wird die Buchung abgelehnt (kein negativer Kontostand). Interne Halter-Umbuchungen (Bank-Verwaltung) bleiben **gebührenfrei** — die anfallende In-Game-Gebühr tragen die Bankmitarbeiter dort selbst. Die Live-Vorschau im Buchungsdialog zeigt entsprechend „Gebühr / wird abgebucht".

- **Startseite: Einsätze der nächsten 7 Tage als Kacheln.** Statt nur des nächsten Einsatzes zeigt die Startseite jetzt alle Einsätze mit geplantem Start in den nächsten 7 Tagen als Kacheln — der nächstgelegene zuerst, danach aufsteigend; jede Kachel enthält dieselben Angaben wie zuvor, die Beschreibung wird nach drei Zeilen abgeschnitten (vollständig auf der Einsatz-Detailseite sichtbar). Der Info-/Ankündigungsbereich darüber nimmt die volle Breite ein und ist nur so hoch wie nötig (eingeklappt minimal). Angezeigt werden eigene Einsätze (inklusive interner) sowie alle öffentlichen Einsätze; Einsätze einer eigenen Einheit sind mit einer „Meine Einheit"-Markierung hervorgehoben.

- **Bank: Halter werden überall mit ihrem aktuellen Anzeigenamen angezeigt.** In der gesamten Bank (Halter-Tab, Halter-Detailseite, alle Halter-Auswahllisten, Buchungshistorie sowie Kontoauszug- und Management-Report-PDF) erscheint jetzt der aktuelle Anzeigename des verknüpften Nutzers — der Benutzername nur noch, wenn kein Anzeigename gesetzt ist. Eine Namensänderung wirkt sofort überall; der bei der Registrierung eingefrorene Name dient nur noch als Rückfall, wenn der Nutzer gelöscht wurde. Das Audit-Log und die Buchungsnotizen halten den Namen weiterhin so fest, wie er zum Zeitpunkt der Aktion lautete.

### Fixed

- **Bank: Bankmitarbeiter können ihren eigenen Halter-Eintrag wieder öffnen.** Im Tab „Halter" der Bank-Verwaltung war der eigene Name für reine Bankmitarbeiter (ohne Bankleitung) nicht anklickbar, sodass sie ihre eigene Halter-Übersicht nicht aufrufen konnten; jetzt ist der eigene Eintrag wie vorgesehen verlinkt (fremde Einträge bleiben wie bisher nur für die Bankleitung verlinkt).

## [v1.0.4](https://github.com/krt-profit/basetool/releases/tag/v1.0.4) - 2026-06-29

### Added

- **Missionen: verantwortliche Einheit eines Einsatzes nachträglich änderbar.** Im Tab „Verwaltung" eines Einsatzes lässt sich die verantwortliche Einheit (Staffel/SK/Bereich/OL) jetzt ändern oder auf „Keine" setzen — bisher war sie nach dem Anlegen fest; die Sichtbarkeit des Einsatzes richtet sich danach neu aus und die Änderung wird im Audit-Log erfasst. Wer den Besitzer wechseln darf, darf auch die Einheit ändern (Admin, zuständiger Offizier oder Einsatz-Besitzer); Nicht-Admins können nur auf eigene oder von ihnen verwaltete Einheiten zuweisen. Die verantwortliche Einheit wird außerdem auf der Startseite im Bereich „Nächster Einsatz" angezeigt.

## [v1.0.3](https://github.com/krt-profit/basetool/releases/tag/v1.0.3) - 2026-06-29

### Added

- **Lager: persönlich markierte Einträge per „Umbuchen" ins gemeinsame Lager buchen (und zurück).** In „Mein Lager" gibt es je Eintrag die neue Aktion „Umbuchen" (Doppelpfeil-Symbol): eine Teilmenge oder der ganze Eintrag lässt sich aus dem persönlichen Bestand ins gemeinsame Staffel-/SK-Lager umbuchen (die Markierung „persönlich" entfällt, die Zielmenge wird einer Org-Einheit zugeordnet) und ebenso wieder als persönlich markieren. Die bisherige „Umbuchung" zu einem anderen Ort/Nutzer ist aus dem Ausbuchen-Dialog in denselben „Umbuchen"-Dialog gewandert; „Ausbuchen" bietet nur noch Einfache Ausbuchung und Verkauf.

## [v1.0.2](https://github.com/krt-profit/basetool/releases/tag/v1.0.2) - 2026-06-29

### Changed

- **Deployment: Aktualisierungen der Infrastruktur-Container (Redis, NPM) und sonstige Compose-Änderungen werden jetzt automatisch ausgerollt.** Die Compose-Datei samt Wartungsseite und Keycloak-Theme wird als signiertes, versioniertes `basetool-config`-Artefakt zusammen mit den App-Images promotet und vom Server über denselben Pull-Kanal angewendet — kein manuelles Kopieren der Compose-Datei und kein händisches `docker compose up -d` mehr. Änderungen am Postgres- oder Keycloak-Image bleiben bewusst manuell (zustandsbehaftete Upgrades). Details: `docs/deployment.md`, ADR-0049, REQ-OPS-001..007.

## [v1.0.1](https://github.com/krt-profit/basetool/releases/tag/v1.0.1) - 2026-06-28

### Added

- **Bank: Kontoverantwortliche werden über Anträge auf ihrem Konto benachrichtigt.** Der oder die Kontoverantwortliche (Staffelleiter, SK-Leiter, Bereichsleiter, OL-Mitglieder bzw. Profit-Bereichsleiter) erhält jetzt eine Benachrichtigung, wenn auf dem verantworteten Konto ein Buchungsantrag erstellt oder von einem Bankmitarbeiter bestätigt bzw. abgelehnt wird. Wer den Antrag selbst gestellt oder bearbeitet hat, wird nicht über die eigene Aktion benachrichtigt.

### Changed

- **Bank: Einzahlungen kann jeder Nutzer für jedes aktive Konto beantragen — ohne Freigabe-Limit.** Ein Einzahlungsantrag ist jetzt für jedes aktive Konto (auch nicht sichtbare sowie Sonder- und Kartellbankkonten) durch jeden angemeldeten Nutzer möglich und unterliegt keinem Freigabe-Limit; der Bankmitarbeiter gibt ihn wie gewohnt frei, sobald das Geld ingame eingegangen ist. Auszahlungen und Transfers bleiben unverändert auf sichtbare Konten beschränkt und unterliegen weiterhin den Freigabe-Limits.

- **Bank: Konto-Detailseite unter „Bank meiner Org-Einheit" mit Tabs.** Die Detailansicht eines Kontos trennt „Buchungshistorie" und „Verantwortung & Sichtbarkeit" jetzt in zwei Tabs. Die Tableiste sitzt oben; darunter bleiben die Info-Kacheln (Kontosaldo, Zielkontostand, Trend, Buchungen) auf beiden Tabs sichtbar. Den Tab „Verantwortung & Sichtbarkeit" sieht nur der Kontoverantwortliche; reine Betrachter sehen ihre geltenden Freigabe-Limits weiterhin schreibgeschützt direkt unter den Kacheln.

## [v1.0.0](https://github.com/krt-profit/basetool/releases/tag/v1.0.0) - 2026-06-28

## [v0.7.17](https://github.com/krt-profit/basetool/releases/tag/v0.7.17) - 2026-06-27

### Added

- **Beförderung: Alle Änderungen am Beförderungssystem stehen jetzt im Audit-Log.** Anlegen, Bearbeiten und Löschen von Beförderungs-Themen, -Kategorien, Stufeninhalten und Ranganforderungen sowie das Setzen und Entfernen von Mitglieder-Bewertungen erscheinen als eigener Reiter „Beförderung" im Aktivitätsprotokoll (Admin → Audit-Logs) — inklusive Zeitraum-Export (PDF/JSON) und Aufbewahrungs-Bereinigung wie die übrigen Bereiche.

### Changed

- **Mitgliederverwaltung: bis zu zwei Staffeln je Mitglied.** In den Mitglied-Details lassen sich jetzt zwei Staffeln zuweisen statt nur einer, jede mit eigenen Logistiker-/Missions-Manager-Rollen; das Speichern gleicht die Staffelmitgliedschaften in einem Schritt ab (Hinzufügen, Entfernen, Rollen). Die Logistiker-/Missions-Manager-Schalter in der Mitgliederliste entfallen — diese Rollen werden nun je Staffel in den Mitglied-Details gepflegt; die Staffel-Spalte zeigt beide Staffeln.

- **Zwei Staffeln werden projektweit unterstützt.** Profil und Mitgliederliste zeigen beide Staffeln, und Mitglieder-/Such-/Bewertungslisten eines Offiziers mit zwei Staffeln umfassen beide; die aktive Staffel (für Anzeige und automatische Zuweisung beim Erstellen) folgt dem Staffel-Umschalter in der Seitenleiste, sonst der primären (namens-sortierten) Staffel. Berechtigungen (PII-Einsicht, Bewertung) und Auftrags-Übergabe-Protokolle berücksichtigen beide Staffeln; beim Erstellen von Promotion-Themen/Rang-Anforderungen muss ein Nutzer mit zwei Staffeln zuvor die Ziel-Staffel anpinnen.

- **Hangar „Meine Schiffe": serverseitige Seitennummerierung, Sortierung und Suche.** Die persönliche Schiffsliste lädt jetzt seitenweise (10/50/100, Standard 50) statt alle Schiffe auf einmal; die feste Sortierung (Hersteller, Typ, Versicherung, Ort, Name) und das Suchfeld arbeiten serverseitig über die gesamte Flotte statt nur über die gerade angezeigte Seite. Hinzufügen, Bearbeiten, Löschen und Import behalten Seite und Suche bei, ohne die Seite neu zu laden.

- **Hangar: „Staffelübersicht" heißt jetzt „Org-Einheitsübersicht" und spannt alle sichtbaren Org-Einheiten.** Ohne aktiv gewählte Org-Einheit zeigt sie die Schiffe aller eigenen Staffeln und Spezialkommandos, für Bereichsleitungen zusätzlich die der untergeordneten Org-Einheiten ihres Bereichs, und für die Organisationsleitung alle Schiffe (auch von Mitgliedern ohne Org-Einheit). Eine aktiv gewählte Org-Einheit schränkt die Übersicht weiterhin auf diese ein. („Org-Einheit" grenzt die Organisationseinheiten von den dynamischen Einsatz-Einheiten ab.)

### Fixed

- **Raffinerieauftrag einlagern: Auftrags-Dropdown blieb leer.** Beim Einlagern eines Raffinerieauftrags zeigte die „Auftrag"-Auswahl keine passenden Aufträge, obwohl welche das Material in der Qualität benötigten. Ursache: Der Filter prüfte nur die Material-Zeilen, die bei Item-Aufträgen leer sind — solche Aufträge fielen immer heraus. Das Dropdown bietet jetzt (wie der Lager-Picker) alle aktiven Aufträge an, die das eingelagerte Material benötigen, über beide Auftragsarten hinweg.

- **Meine Blueprints: Craftbarkeit rechnet jetzt auch Hand-Minables (Stück) ein.** In der Craftbarkeits-Ansicht fehlten bisher die Zutaten mit Mengeneinheit „Stück" — handabgebaute Edelsteine wie Hadanite oder Beradom, die das Wiki als Item führt. Sie werden jetzt (wie schon im Auftrags-Pfad) an das passende Material gekoppelt und mitgezählt (benötigt/vorhanden/fehlt in ganzen Stück, ggf. als limitierendes Material); craftbare Unterbaugruppen bleiben „nicht bewertet".

- **Mitglied-Details: Speichern entfernt keine Staffeln mehr, wenn die Staffel-Daten nicht geladen werden konnten.** Schlug beim Öffnen der Bearbeitung der Abruf der Staffel-Mitgliedschaften fehl (z. B. Zeitüberschreitung), zeigte das Formular leere Staffel-Felder — ein anschließendes Speichern hätte alle Staffeln des Mitglieds entfernt. Jetzt erscheint stattdessen ein Hinweis, die Staffel-Felder bleiben ausgeblendet und der Staffel-Abgleich wird übersprungen; die übrigen Felder speichern normal.

## [v0.7.16](https://github.com/krt-profit/basetool/releases/tag/v0.7.16) - 2026-06-27

### Added

- **Bank: Transfer-Anträge und Beantragung für alle Sichtberechtigten.** Berechtigte Nutzer können jetzt zusätzlich zu Ein-/Auszahlung einen **Transfer** vom eigenen Konto auf ein beliebiges aktives Konto beantragen, und wer ein Konto sehen darf, darf darauf Anträge stellen (bisher nur die Führungsränge). Den Transfer bucht der Bankmitarbeiter beim Bestätigen wie eine normale Buchung.

- **Bank: Freigabe-Limits je Konto und Stufe mit delegierter Freigabe.** Kontoverantwortliche, Bankleitung und Admins legen je Stufe (Kommandoleiter, stellv. Kommandoleiter, Ensigns, Einzelpersonen) ein Limit fest, bis zu dem ohne zusätzliche Freigabe beantragt werden darf; die Limits sind in den Kontodetails für alle sichtbar. Über dem Limit warnt das Antragsformular, der Kontoverantwortliche kann die Freigabe im neuen Reiter „Fremde Anträge" erteilen (belegt das Häkchen beim Bankmitarbeiter vor), und der Bankmitarbeiter muss sie beim Bestätigen per Pflicht-Häkchen bestätigen. Limit-Änderungen und Freigaben stehen im Audit-Log.

### Changed

- **Einsatz-Detailseite: prägnantere Info-Leiste, großer „Anmelden"-Button und Beschreibung als graue Karte.** Die Fakten-Leiste im Seitenkopf zeigt jetzt fünf größere, mit Icons versehene Angaben — TS-Treffen, Server-Join, Ende, Einsatzleiter und Teilnehmer (mit eingeklapptem „eingecheckt"-Zähler); die Zeiten erscheinen dort nur als Uhrzeit (das vollständige Datum steht in den Übersichts-Details), und die separate Gesamtsumme entfällt und bleibt im Finanzen-Tab. Der „Anmelden"-Knopf ist nun in voller Größe statt klein, und die ausklappbare „Detaillierte Beschreibung" liegt wie die übrigen Karten auf grauem Kartenhintergrund. Live-Aktualisierung und Berechtigungen bleiben unverändert.

- **Meine Blueprints: Feinschliff an der Design-System-Vorlage.** Der Kopfbereich stellt das Feld zum Suchen und Hinzufügen jetzt rechts neben den Titel (statt darunter), das Suchfeld trägt links ein Lupen-Symbol, und über den vorgemerkten Chips erscheint die Beschriftung „Auswahl", sobald mindestens ein Blueprint ausgewählt ist. Der Schalter „Raffinerie-Ertrag einrechnen" sitzt jetzt oben in der Sammlungs-Box (mit Trennlinie) und behält seinen Zustand, wenn die Liste nach Hinzufügen, Import oder Entfernen aktualisiert wird. Reine Optik — keine Funktionsänderung.

## [v0.7.15](https://github.com/krt-profit/basetool/releases/tag/v0.7.15) - 2026-06-27

### Added

- **Operation-Detailseite im neuen Tab-Layout mit Markdown-Editor.** Die Operationsseite nutzt jetzt dieselbe Tab-Optik wie die Einsatz-Seite — Übersicht (Operation auf einen Blick, Ergebnis je Einsatz, Einsätze, ausklappbare Beschreibung), Einsätze (mit „Einsatz hinzufügen", das die Erstellseite mit vorausgewählter Operation öffnet), Auszahlung, Finanzen und Verwaltung. Die Beschreibung lässt sich mit einem Formatierungs-Werkzeug (Fett/Kursiv/Überschrift/Liste/Link) und einer Live-Vorschau bearbeiten.

- **Einsatz-Seite: Ablauf-Checkliste, neu aufgeteilte Übersicht und die Felder „Ziel"/„Treffpunkt".** Ein Einsatz hat jetzt einen sortierbaren Ablauf (Schritte mit Titel und optionaler Zeit/Ort), den Bearbeiter im Verwaltungs-Tab pflegen; in der Übersicht erscheint er als Checkliste, deren aktuelle Phase der erste offene Schritt ist und deren Häkchen Bearbeiter live für alle setzen. Die Übersicht ist in kompakte Karten neu aufgeteilt (Mission auf einen Blick mit Ziel, Weitere Leads, Funk, Teilnehmer-Fortschritt, Ablauf, Kalender) samt ausklappbarer Beschreibung; alle Ablauf-Aktionen stehen im Aktivitätsprotokoll (Missionen).

- **Auftrag: Blaupausen-Verfügbarkeit wahlweise mit oder ohne Varianten zählen.** Bei Item-Aufträgen lässt sich jetzt direkt im Verfügbarkeits-Panel umschalten, ob die Abdeckung kosmetische Varianten der bestellten Items mitzählt (Standard) oder nur exakt die bestellte Variante — hilfreich, wenn genau eine Variante gefordert ist und Mitglieder mit anderen Varianten die Zahl sonst verfälschen. Die Wahl wird am Auftrag gespeichert, gilt für alle Betrachter und das Panel aktualisiert sich ohne Neuladen.

### Changed

- **Profilseite im einheitlichen Design-System.** „Mein Profil" trennt die schreibgeschützten Identitätsdaten (Initialen-Kachel, Anzeigename, Rang, aktive Staffel sowie E-Mail/Beitrittsdatum/Monate als Faktenraster) jetzt klar von den Einstellungen: Eigene Daten, Standard-Auszahlung, Blaupausen-Freigabe und „Zugang & Sicherheit" sind je eine eigene Karte mit genau einer Aktion. Funktion, In-Place-Speichern und Übersetzungen bleiben unverändert.

- **Einsatz-Übersicht: Funk-Karte zeigt jetzt auch die Frequenzen der Einheiten und blendet leere Einträge aus.** Neben den zentralen, einheitslosen Frequenzen listet der Funk-Bereich der Übersicht nun die Frequenz jeder Einheit, die eine hat (mit „Einheit"-Hinweis); zentrale Frequenztypen ohne hinterlegten Wert und Einheiten ohne Frequenz werden weggelassen, und die Karte verschwindet, wenn nichts eingetragen ist. Die Anzeige aktualisiert sich beim Setzen einer Frequenz oder beim Bearbeiten einer Einheit ohne Neuladen.

- **Keycloak auf 26.6.4 angehoben (Sicherheitspatch).** Der Anmeldedienst (Keycloak) wird vom Patch 26.6.3 auf 26.6.4 aktualisiert — ein reines Patch-Release mit mehreren Sicherheitskorrekturen (u. a. Authentifizierungs-Bypass und Rechte-Eskalation) und einem Quarkus-Update; keine Funktions- oder Konfigurationsänderung. Das gepinnte Container-Image (`quay.io/keycloak/keycloak:26.6`-Digest) und die SPI-Artefakte des `keycloak-spi`-Moduls ziehen mit. **Deploy-Hinweis:** Beim nächsten Deploy lädt Compose das neue Image-Digest; der Keycloak-Container muss dafür neu gestartet werden.

- **Meine Blueprints: ruhigere, design-konforme Darstellung.** Die Sammlungs-Box nutzt jetzt die dezente HUD-Haarlinie mit Eckmarken statt eines vollen Orange-Rahmens, Formular- und Abschnitts-Beschriftungen sind neutral statt orange, und die Auswahl-Chips beim Hinzufügen verwenden die Standard-Chips des Designsystems. In der Blueprint-Liste sitzt der Notiz-Stift jetzt als sauberes Icon gemeinsam mit dem Craftbarkeits-Badge am rechten Zeilenrand. Reine Optik — keine Funktionsänderung.

- **Admin-Seite „Fremde Blueprints" im einheitlichen Design.** Die Admin-Verwaltung der Blueprints anderer Mitglieder nutzt jetzt dieselben Design-System-Komponenten wie die Mitglieder-Seite: die DS-Tabelle, den KRT-Modalrahmen für Notiz bearbeiten, Entfernen-Bestätigung und JSON-Import (Icon-Schließen mit Beschriftung, Esc, eine hervorgehobene Aktion), die DS-Chips für die Auswahl sowie einen flachen Admin-Banner ohne Verlauf. Funktion, Berechtigungen, Live-Aktualisierung und Übersetzungen bleiben unverändert.

- **Organigramm: aufgeräumter Seitenkopf.** Der Hinweistext über dem Diagramm („Kontogebundene Sitze werden unter Organisation → Leitung verwaltet …") entfällt; der „Bearbeiten"-Button sitzt jetzt rechts in der Titelbox und erscheint weiterhin nur für Nutzer mit Bearbeitungsrecht (Admins). Der „Leitung-verwaltet"-Hinweis an den einzelnen Knoten bleibt erhalten.

- **Org-Einheits-Bankseite im Design-System-Layout.** „Bank meiner Org-Einheit" trennt Konten und „Meine Anträge" jetzt in zwei Tabs, und eine einzige Schaltfläche „Ein-/Auszahlung beantragen" oben öffnet den Antrag mit einer Konto-Auswahl (statt einer eigenen Schaltfläche je Konto). Die Konten erscheinen als kompakte Liste mit Trend, Kontostand und Zielfortschritt statt als überladene Karten. In der Konto-Detailansicht sind die Sichtbarkeits-Freigaben ruhige Umschalter (Status-Chip „Freigegeben" + „Entfernen"/„Freigeben"), und der stets gleiche „Aktiv"-Status entfällt. Funktion, Berechtigungen, Live-Aktualisierung und Übersetzungen bleiben unverändert.

### Fixed

- **Auftragsdetails: kompakter Kommentar und Abstand vor „Bestellte Items".** Der Kommentar-Kasten umschließt jetzt seinen Inhalt, statt als hoher, halbleerer Block mit weit eingerücktem Titel zu erscheinen (Ursache war ein versehentlich gerendertes Einrückungs-Whitespace); Beschriftung und Text stehen platzsparend in einer Zeile, Zeilenumbrüche im Kommentar bleiben erhalten. Zwischen Kopfbereich und „Bestellte Items" bzw. „Benötigte Materialien" gibt es wieder den üblichen Abstand.

- **Beförderungssystem: Offiziere erscheinen nicht mehr in der Bewertungsmatrix.** Die Member-Liste der Bewertungsverwaltung führt jetzt nur noch die einfachen Mitglieder einer Staffel — Träger der Rollen `OFFICER` und `ADMIN` werden herausgefiltert, da sie die Bewertung durchführen, statt selbst bewertet zu werden (bisher wurden nur Admins ausgeschlossen). Das Beförderungssystem betrachtet damit ausschließlich einfache Staffel-Mitglieder.

- **Meine Blueprints: Die Liste ist nicht mehr auf 200 Blueprints begrenzt.** Es werden jetzt alle eigenen Blueprints geladen — die Anzahl im Tab stimmt und die Suche durchsucht den vollständigen Bestand. Rezept- und Craftbarkeits-Berechnung laufen weiterhin bedarfsweise bzw. asynchron, sodass auch große Sammlungen flüssig laden.

- **Blueprint-Import: Vorschläge werden jetzt eindeutig als importierbar angezeigt.** Zeilen mit leicht abweichendem Namen (Gruppe „Vorschläge bestätigen") sind angehakt und automatisch dem besten Treffer — dem bereits angezeigten Namen — zugeordnet, sodass „Anwenden" sie ohne den bisher nötigen Extra-Klick auf den Vorschlags-Button importiert. Ein anderer Treffer per Klick/Suche oder ein entferntes Häkchen steuern wie gewohnt; nicht zuordenbare Zeilen bleiben unangehakt, sodass das Häkchen immer dem tatsächlichen Import entspricht.

## [v0.7.14](https://github.com/krt-profit/basetool/releases/tag/v0.7.14) - 2026-06-24

### Added

- **Crew-Board (Teilnehmer & Einheiten): Teilnehmer per Drag & Drop ins Leere entfernen und am Rand automatisch scrollen.** Zieht man einen Teilnehmer aus einer Einheit und lässt ihn über keiner anderen Einheit (im leeren Bereich) los, wird er aus der Einheit entfernt und wieder unter „Ohne Einheit" geführt — man muss bei langen Listen nicht mehr bis zum Pool scrollen. Während des Ziehens scrollt die Seite automatisch, wenn man den oberen oder unteren Fensterrand erreicht, sodass auch gerade nicht sichtbare Einheiten als Ziel erreichbar sind.

## [v0.7.13](https://github.com/krt-profit/basetool/releases/tag/v0.7.13) - 2026-06-24

### Added

- **Kontoverantwortung & konfigurierbare Sichtbarkeit für Org-Einheits-Konten.** Jedes Org-Einheits-Bankkonto hat eine/n abgeleitete/n Kontoverantwortliche/n (Staffelkonto → Staffelleiter, Bereichskonto → Bereichsleiter, OL-Konto → OL-Mitglieder, Kartellbankkonto → Bereichsleiter Profit). Diese/r kann freigeben, wer den Kontostand zusätzlich sehen darf — je Unter-Rolle (z. B. Kommandoleiter/Ensigns bzw. Bereichskoordinatoren/-operatoren), alle Mitglieder der Einheit oder einzelne Nutzer. Das KRT-Konto ist immer für alle Mitglieder sichtbar; Sonderkonten sehen automatisch alle OL-Mitglieder und Bereichsleiter (zusätzlich von OL/Bankleitung freigebbar an globale Rollen, alle Mitglieder oder einzelne Nutzer).

- **Read-only-Detailansicht mit Verlauf und Kontoauszug.** Klickt man auf der Org-Einheits-Bankseite auf ein Konto, das man sehen darf, öffnet sich die Detailansicht wie für Bankmitarbeiter — inklusive Buchungshistorie — schreibgeschützt: keine Ein-/Aus-/Umbuchung, nur der Abruf eines Kontoauszugs (PDF). Die „Halter"-Spalte (welcher Spieler das Geld physisch hält) ist dabei ausgeblendet; Bankmitarbeiter behalten die vollständige Ansicht.

- **Kontostandsziel je Konto.** Verantwortliche und zugriffsberechtigte Bankmitarbeiter können ein Zielkontostand setzen; allen, die das Konto sehen, wird das Ziel samt Fortschritt angezeigt.

- **Navigationspunkt „Org-Einheits-Bank" ist jetzt für alle Mitglieder sichtbar.** Passend dazu, dass die Seite jedem KRT-Mitglied offensteht (das KRT-Konto ist für alle sichtbar, ein Mitglied kann zusätzlich freigeschaltet sein), erscheint der Sidebar-Link nicht mehr nur für Offiziere/Leitung.

### Changed

- **Die Mitglieder-Rolle heißt jetzt `KRT_MEMBER` (vormals `SQUADRON_MEMBER` / „Squadron Member").** Rein technische Umbenennung ohne Funktionsänderung; sie spiegelt, dass es die organisationsweite Mitglieder-Rolle ist, nicht staffelgebunden. **Deploy-Hinweis:** Die Keycloak-Realm-Rolle muss im selben Wartungsfenster von „Squadron Member" auf „KRT Member" umbenannt werden (Rollenzuweisungen bleiben erhalten), sonst verlieren Mitglieder bis zum Abgleich ihre Mitglieder-Berechtigungen. Flyway `V190` benennt die Rolle in der Datenbank um.

## [v0.7.12](https://github.com/krt-profit/basetool/releases/tag/v0.7.12) - 2026-06-23

### Fixed

- **Leitung-Seite: Das Zuweisen eines Kommandoleiters oder Stellvertreters meldete „Aktion fehlgeschlagen", obwohl der Rang gespeichert wurde** (erst nach Neuladen sichtbar). Die Antwort lief wegen einer fehlenden Transaktionsgrenze in einen 500; die Rang-Endpunkte sind jetzt wie die übrigen Mitglieds-Endpunkte transaktional.

## [v0.7.11](https://github.com/krt-profit/basetool/releases/tag/v0.7.11) - 2026-06-23

### Fixed

- **Die Seite „Leitung" (Organisation → Leitung) lädt wieder.** Für Admins und alle, die Bereiche oder die Organisationsleitung sehen, schlug der Aufruf mit „Die Leitungsübersicht konnte nicht geladen werden" fehl, sobald eine Bereichs- oder Organisationsleitungs-Einheit anzuzeigen war. Ursache war ein unvollständiger Frontend-Spiegel der Einheitsarten (die Hierarchie-Arten Bereich und Organisationsleitung fehlten), an dem die gesamte Antwort scheiterte. Ein neuer Vertragstest erzwingt künftig, dass der Frontend-Spiegel jeden vom Backend gelieferten Enum-Wert kennt.

## [v0.7.10](https://github.com/krt-profit/basetool/releases/tag/v0.7.10) - 2026-06-23

### Added

- **Halter-Gelder sind von den Konten entkoppelt; Bankmitarbeiter können intern zwischen Haltern umbuchen.** Der Bestand eines Halters ist jetzt eine bankweite Gesamtgröße statt pro Konto und darf ins Minus gehen — ein Halter streckt notfalls eigenes Geld für eine Auszahlung vor. Im Haltermenü der Bankverwaltung gleichen Bankmitarbeiter solche Salden per Halter-zu-Halter-Umbuchung wieder aus (ohne ein Konto zu berühren).

- **Bankmitarbeiter erreichen jetzt die Bankverwaltung.** Sie dürfen Sonderkonten anlegen (und erhalten darauf automatisch Vollzugriff) sowie das Haltermenü inklusive Umbuchung nutzen. Konten umbenennen, schließen, wiedereröffnen, andere Kontotypen anlegen, die manuelle Halter-Registry und Berechtigungen bleiben der Bankleitung vorbehalten; das Audit-Log bleibt Admin-only.

- **Alle Bankmitarbeiter und Bankleitungsmitglieder werden automatisch als Halter registriert.** Verliert jemand alle Bankrollen, wird sein Halter automatisch deaktiviert — der Restbestand bleibt erhalten und muss per Umbuchung auf andere Halter verteilt werden. Manuell angelegte Halter bleiben davon unberührt.

- **Halter-Buchungshistorie im Haltermenü.** Ein Klick auf einen Halter-Namen öffnet dessen Verwahrungs-Historie — alle Buchungen, die den bankweiten Bestand dieses Halters verändert haben, jeweils mit Konto- bzw. Gegenhalter-Bezug, Datum und Betrag. Ein Bankmitarbeiter sieht nur den eigenen Halter, die Bankleitung jeden.

- **Überweisungsgebühren werden bei Auszahlungen, Halter-Umbuchungen und Halter-wechselnden Transfers automatisch berücksichtigt.** Der eingegebene Betrag ist der Sendebetrag, der voll vom Konto/Halterbestand abgebucht wird; die In-Game-Gebühr (gleicher Satz wie bei Operationen, 0,5 %) wird daraus herausgerechnet und ausgewiesen, sodass beim Ziel der Nettobetrag ankommt und kein Bankmitarbeiter die Gebühr aus dem Privatvermögen trägt. Die Buchungsdialoge zeigen Gebühr und Ankunftsbetrag live, die Historie weist sie pro Buchung aus. Einzahlungen und Transfers mit gleichem Halter bleiben gebührenfrei.

- **Kontostand-Aufteilung auf der Halter-Seite.** Ein Halter trägt auf seiner Halter-Seite seinen aktuellen In-Game-Kontostand ein und sieht sofort, wie viel davon für die Bank reserviert ist (sein Halterbestand) und wie viel privat ihm gehört (Kontostand minus Halterbestand). Reiner Rechner — es wird nichts gespeichert.

- **Neues Audit-Log „Rollen & Mitglieder".** Das Zuweisen, Ändern und Entfernen von Rollen und Mitgliedschaften — Bereichs-/Organisationsleitung, SK-Leitung, Logistiker-/Missionsleiter-Rechte sowie Staffel-/SK-Mitgliedschaften — wird jetzt lückenlos und unveränderbar protokolliert. Sichtbar nur für Admins als achter Tab der Seite „Audit-Logs", filterbar nach Zeitraum/Akteur/Ereignistyp und als PDF/JSON exportierbar.

- **Delegierte Vergabe von Führungsrängen und Kommandogruppen.** Führungsränge müssen nicht mehr ausschließlich von Admins gesetzt werden: Die Organisationsleitung ernennt Bereichsleiter, Bereichsleiter ernennen Staffelleiter/SK-Leiter/Koordinatoren/Operatoren ihres Bereichs, und Staffelleiter verwalten die Kommandoleiter/Stellvertreter/Ensigns sowie die Kommandogruppen ihrer Staffel — entlang einer festen Leiter, in der sich niemand selbst befördern kann. Kommandogruppen lassen sich anlegen, umbenennen und löschen (höchstens vier je Staffel). Jede dieser Aktionen wird im Audit-Log „Rollen & Mitglieder" protokolliert.

- **Neue Seite „Leitung" (Organisation → Leitung).** Bereichsleiter, Staffelleiter, die Organisationsleitung und Admins vergeben hier die ihnen zustehenden Führungsränge: Staffelleiter verwalten die Kommandoleiter/Stellvertreter/Ensigns und die Kommandogruppen ihrer Staffel, Bereichsleiter ihre Koordinatoren/Operatoren sowie die Staffel- und SK-Leitungen ihres Bereichs, die Organisationsleitung die Bereichsleiter, Admins alles. Die Seite zeigt jeweils nur die Einheiten, die man selbst verwalten darf. Das Organigramm verweist für kontogebundene Sitze jetzt auf diese Seite.

- **Führungsrang-Vergaben spiegeln sich automatisch ins Organigramm.** Wird ein Bereichs-, Staffel-, Kommando- oder SK-Führungsrang vergeben, geändert oder entzogen, aktualisiert sich der zugehörige kontogebundene Sitz im Organigramm in derselben Aktion — inklusive der Kommandogruppen-Knoten. Das Organigramm bleibt rein beschreibend und vergibt weiterhin keine Rechte; frei eingetragene Halter ohne Account bleiben unberührt.

- **Kontogebundene Sitze werden im Organigramm nicht mehr von Hand gesetzt.** Der Organigramm-Editor vergibt keine Accounts mehr — kontogebundene Sitze entstehen ausschließlich über die Seite „Leitung" und werden von dort gespiegelt (so kann es keine Abweichung zwischen Rang und Organigramm geben). Im Organigramm erscheinen solche Sitze nur lesend mit dem Hinweis „Leitung"; weiterhin frei pflegbar bleiben dort ausschließlich Freitext-Namen für Mitglieder ohne Account. Eine über die Seite „Leitung" angelegte Kommandogruppe ist im Organigramm komplett schreibgeschützt — kein Umbenennen, Entfernen, Leiter-Zuweisen oder Hinzufügen von Stellvertreter/Ensign; ihre Mitglieder werden ausschließlich dort verwaltet. Frei (ohne Kommandogruppe) angelegte Kommandos bleiben im Organigramm voll bearbeitbar.

### Changed

- **Auszahlungen und Umbuchungen prüfen nur noch die Kontodeckung, nicht mehr den Halter.** Ein Konto kann weiterhin nie ins Minus gehen; der Halterbestand darf es (und wird später ausgeglichen). Die kontobezogene „Halter-Verteilung" entfällt: Konto-Detailseite, Kontoauszug-PDF und 3-Monats-Report zeigen sie nicht mehr — der 3-Monats-Report schließt stattdessen mit den bankweiten Halterbeständen.

- **Intern: Die fünf alten Führungsrollen-Booleans der Mitgliedschaften wurden entfernt (Migration V187).** Der vereinheitlichte Rang ist jetzt die alleinige Quelle der Wahrheit; die DB-Regel „Führungskraft gehört keiner Staffel an" arbeitet darauf weiter (Staffelränge sind ausgenommen). Reine Aufräum-/Schema-Änderung ohne sichtbare Auswirkung — beim Deploy wird einmalig eine nicht umkehrbare Migration ausgeführt.

- **Raffinerie-Aufträge anderer Nutzer sind jetzt auf den eigenen Org-Einheits-Bereich beschränkt.** Das Ansehen und Anlegen von Raffinerie-Aufträgen für andere Nutzer (`/refinery-orders/users/{userId}`) ist für Logistiker nun auf Nutzer der eigenen Org-Einheiten begrenzt (Admins und der eigene Account ausgenommen) — wie schon bei den einzelnen Auftrags-Aktionen. Vorher konnte jeder Logistiker (inkl. der neuen Staffelränge) das organisationsweit. Sicherheits-Härtung aus dem PR-Review.

## [v0.7.9](https://github.com/krt-profit/basetool/releases/tag/v0.7.9) - 2026-06-22

### Added

- **Die Seite „Bank meiner Org-Einheit" zeigt pro Konto jetzt den 30-Tage-Trend** — die vorzeichenfarbige Veränderung der letzten 30 Tage plus ein kompaktes Verlaufsdiagramm (Sparkline), genau wie in der Bankübersicht. Reine Anzeige; Buchungshistorie, Halter und Audit bleiben weiterhin der Bank vorbehalten.

- **Audit-Logs für Lager, Aufträge, Raffinerie, Mein Inventar, Missionen und Operationen.** Jede Aktivität in diesen sechs Bereichen wird jetzt — wie schon bei der Bank — lückenlos und unveränderbar protokolliert. Admins finden alle Logs auf einer neuen Seite „Audit-Logs" (Administration → System & Daten) mit einem Umschalter zwischen Bank, Lager, Aufträge, Raffinerie, Mein Inventar, Missionen und Operationen, gefiltert nach Zeitraum, Akteur und Ereignistyp. Jedes Log lässt sich für einen gewählten Zeitraum als PDF im KRT-Design oder als JSON exportieren (die Bank ebenfalls). Sichtbar ausschließlich für Admins; die alte Adresse `/admin/bank-audit` leitet auf die neue Seite weiter.

- **Audit-Log-Bereinigung für Admins.** Admins können in jedem Audit-Log (auch Bank) alle Einträge löschen, die älter als ein gewähltes Datum sind — getrennt pro Log. Vor dem Löschen weist ein Hinweis darauf hin, zuerst eine Sicherungskopie als PDF oder JSON zu erstellen; das Löschen ist unwiderruflich und wird selbst protokolliert.

- **Wöchentliches Docker-Aufräumen auf dem Produktions-VM.** Neues Wartungsskript `scripts/docker-cleanup.sh` (plus cron.d-Drop-in und logrotate-Konfiguration) entfernt jeden Samstag um 02:00 UTC ungenutzte Docker-Ressourcen — Images, Build-Cache, gestoppte Container, Netzwerke und anonyme Volumes —, damit die Festplatte nicht vollläuft. Belegte Ressourcen und frisch gezogene Images (Standardfenster 14 Tage) bleiben unangetastet; die persistenten Bind-Mount-Daten unter `/var/iri` sind ohnehin nicht betroffen. Reiner Betriebs-/Deploy-Zusatz ohne Auswirkung auf die App; Einrichtung siehe `docs/deployment.md`.

### Changed

- **Bereichs- und Organisationsleitung sehen auf der Seite „Bank meiner Org-Einheit" jetzt auch die organisationsweiten Sonderkonten** — rein lesend, ohne Ein-/Auszahlungsanträge. Außerdem zeigt die Seite nur noch aktive Konten; geschlossene Konten erscheinen dort nicht mehr.

## [v0.7.8](https://github.com/krt-profit/basetool/releases/tag/v0.7.8) - 2026-06-22

### Fixed

- **Die Blueprint-Craftbarkeit behandelt jetzt Materialien mit Mengentyp „Stück" korrekt.** Bei einem Material, das in Stück statt in SCU geführt wird, zeigt die Craftbarkeits-Aufschlüsselung Bedarf, Bestand und Fehlmenge in ganzen Stück mit der Einheit „Stück" an (statt fälschlich „SCU"), und der Bedarf pro Craft wird wie im Auftragswesen auf ganze Stück gerundet — die craftbare Anzahl bleibt dadurch konsistent.

## [v0.7.7](https://github.com/krt-profit/basetool/releases/tag/v0.7.7) - 2026-06-22

### Added

- **Der Discord-Server-Nickname wird bei der Freigabe-Entscheidung angezeigt.** Die Admin-Liste „Discord-Registrierungen" zeigt jetzt neben dem Namen den serverspezifischen Discord-Nickname (den „Nick" im Kartell-Server), damit eine neue Anmeldung der Person leichter zugeordnet werden kann. Die Erfassung läuft beim Login über Discord, dient nur der Anzeige und ist ausschließlich für Admins sichtbar; ist kein Nickname gesetzt (oder die Erfassung nicht eingerichtet), erscheint ein gedämpfter Strich. Setzt eine einmalige Keycloak-Einrichtung voraus (Umgebungsvariable `DISCORD_GUILD_ID` plus zwei Mapper, siehe Runbook).

### Fixed

- **Das Discord-Symbol in der Mitgliederverwaltung erscheint jetzt auch bei Konten, die ihren bestehenden Account nachträglich mit Discord verknüpft haben** — nicht mehr nur bei Konten, die sich direkt über Discord registriert haben. Der Verknüpfungs-Status wird jetzt aus der Keycloak-Federated-Identity-Verknüpfung abgeleitet (statt aus einem nur beim Erstlogin gesetzten Attribut), greift auf jedem Anmeldeweg (auch Passwort-Login) und wird für bereits verknüpfte Konten beim nächsten Nutzer-Sync ohne erneute Anmeldung nachgetragen. Wirkt mit dem neu bereitgestellten Keycloak-Provider-JAR und der angepassten Realm-Konfiguration (siehe `docs/keycloak/DISCORD_KEYCLOAK_SETUP.md`).

## [v0.7.6](https://github.com/krt-profit/basetool/releases/tag/v0.7.6) - 2026-06-21

### Added

- **Blueprint-Craftbarkeit aus dem eigenen Lager.** Die Seite "Meine Blueprints" zeigt jetzt pro Blueprint, ob und wie oft er sich aus dem eigenen "Mein Lager"-Bestand craften lässt, welche Output-Stats die Materialqualität liefert und welches Material fehlt. Ein Schalter (Standard aus) rechnet zusätzlich den Ertrag offener und laufender Raffinerie-Aufträge ein. Nur Material-Zutaten (RESOURCE) werden bewertet; ITEM-Zutaten werden als "nicht bewertet" markiert. Alles streng nutzerbezogen.

- **Die Mitgliederverwaltung zeigt jetzt, ob ein Konto mit Discord verknüpft ist.** Eine neue Spalte zwischen „Missions-Manager" und „Status" zeigt bei verknüpften Konten das Discord-Symbol, sonst einen gedämpften Strich. Nur für Admins sichtbar, und es wird ausschließlich der Verknüpfungs-Status angezeigt — nie die Discord-ID.

- **Blueprint-Import von scmdb.net.** Der Blueprint-Import auf der Seite „Meine Blueprints“ akzeptiert jetzt zusätzlich den Profil-/Tracking-Export von scmdb.net (neben dem SCMDB-Log-Watcher und dem Basetool Blueprint Extractor). Nur freigeschaltete Blueprints werden übernommen; über den mitgelieferten Blueprint-Schlüssel (Tag) werden Produkte auch dann zuverlässig erkannt, wenn der angezeigte Name abweicht.

### Fixed

- **Der Blueprint-Import bricht nicht mehr ab, wenn ein Name zuvor in anderer Groß-/Kleinschreibung gelernt wurde.** Gelernte Blueprint-Aliase sind jetzt eindeutig pro Name unabhängig von Groß-/Kleinschreibung (Migration V176, wie bereits bei den Material-Aliasen): zwei nur in der Schreibweise abweichende Einträge können einen Folge-Import nicht mehr mit einem Datenbankfehler (HTTP 500) blockieren.

## [v0.7.5](https://github.com/krt-profit/basetool/releases/tag/v0.7.5) - 2026-06-21

## [v0.7.4](https://github.com/krt-profit/basetool/releases/tag/v0.7.4) - 2026-06-21

### Added

- **Änderungen anderer Nutzer an einem Einsatz erscheinen jetzt live, ohne die Seite neu zu laden.** Tritt jemand bei, wird die Crew umgestellt, ein Finanzeintrag erfasst oder eine Kern-/Termin-/Status-/Einsatzleiter-/Funk-Änderung gemacht, aktualisiert sich die Ansicht auf allen anderen geöffneten Einsatz-Seiten automatisch über die bestehende Präsenz-Verbindung. Bearbeitet man selbst gerade etwas (Dialog offen), wird die Aktualisierung über einen „Aktualisierungen verfügbar“-Hinweis aufgeschoben statt aufgedrängt; über die Verbindung gehen nur Abschnitts-Kennungen, die Daten holt jede Ansicht weiterhin über ihre eigene berechtigungsgeprüfte Anfrage.

### Changed

- **Die Auftrags- und Raffinerie-Auftragslisten werden jetzt seitenweise geladen.** Statt immer alle Aufträge auf einmal zu holen, zeigen `/orders` und `/refinery-orders` eine Seite mit Seiten-Navigation und Größenauswahl; die aktiven Filter (Status/Staffel bzw. Status/„Meine Aufträge") bleiben beim Blättern erhalten. Die Auftragsliste nutzt größere Seiten (50/100/200, Standard 100), damit die Drag-&-Drop-Priorisierung wie gewohnt funktioniert; die Raffinerieliste nutzt 10/50/100 (Standard 50).

- **Weniger Hintergrund-Anfragen der Benachrichtigungs-Glocke.** Solange die Echtzeit-Verbindung (SSE) steht, fragt die Ungelesen-Zahl nur noch im langsamen Takt nach (statt fest alle 60 Sekunden); bricht die Verbindung ab, wird wieder schnell abgefragt. Das gilt jetzt auch, wenn die Verbindung unbemerkt stehenbleibt (verbunden, aber ohne Daten): bleibt ein Lebenszeichen aus, schaltet die Glocke von selbst wieder auf den schnellen Takt. Benachrichtigungen erscheinen weiterhin in Echtzeit.

- **Das Lager-Eingabeformular und das Refinery-Auftragsformular laden schneller.** Die Katalog-Listen (Materialien, Standorte, Job-Orders, Missionen, Nutzer, Besitzer-Auswahl) werden jetzt parallel statt nacheinander vom Backend geholt. Bei einem fehlgeschlagenen Abruf bleibt die betroffene Liste leer wie bisher.

- **Die Auftragsseiten laden etwas schneller.** Die Alters-Schwellwerte werden jetzt zwischengespeichert (statt bei jedem Aufruf neu geladen), und die Logistiker-Zusatzlisten der Auftragsdetails werden parallel statt nacheinander geholt. Kein sichtbarer Funktionsunterschied; eine Anpassung der Schwellwerte im Admin-Bereich wirkt weiterhin sofort.

- **Schlankere Frontend-Backend-Kommunikation.** Backend-Antworten werden zum Frontend jetzt komprimiert (gzip) ausgeliefert, und die doppelte Resilience-Schicht (Wiederholung/Schutzschalter) im Backend-Client wurde auf die eine wirksame reduziert — schnelleres, einheitliches Fehlerverhalten ohne sichtbaren Funktionsunterschied. Zusätzlich neu: optionale Einstellung `spring.session.redis.flush-mode` (Default `IMMEDIATE`; Betreiber können auf `ON_SAVE` umstellen, um Redis-Zugriffe pro Anfrage zu sparen).

- **Die Einsatzdetailseite lädt die Finanz- und Refinery-Daten jetzt parallel statt nacheinander.** Für Mitglieder wurden die drei Abrufe (Finanzeinträge, Finanzsumme, Refinery-Aufträge) bisher seriell ausgeführt; sie laufen nun gleichzeitig, sodass die Detailansicht (und jede Live-Aktualisierung über die Präsenz-Verbindung) schneller fertig ist. Gleiche Daten; schlägt einer der drei Abrufe fehl, wird die Finanzen-Ansicht jetzt einheitlich ausgeblendet statt teilweise angezeigt.

- **Jede angemeldete Seite lädt den Staffel-Katalog nicht mehr bei jedem Aufruf neu.** Die Seitenleisten-/Kontext-Logik holt die (sich selten ändernde) Staffel-Liste jetzt aus dem 10-Minuten-Cache statt bei jedem Rendern frisch vom Backend; für Admins entfällt dadurch auch ein doppelter Abruf derselben Liste pro Seitenaufbau. Spürbar weniger Backend-Last pro Navigation. Das Umschalten der Staffel-Flags „Beförderung aktiv" und „Profit-Berechtigung" durch Admins leert den Cache und wirkt dadurch sofort app-weit statt mit bis zu zehn Minuten Verzögerung.

- **Die Blueprint-Verfügbarkeitsübersicht lädt für die Zählung nur noch die benötigten zwei Spalten statt ganzer Datensätze.** Statt für die Mitglieder-Zählung pro Variantenfamilie sämtliche Blueprint-Datensätze aller Eigentümer vollständig zu laden, holt die Übersicht (und die Blueprint-Eigentümer-Aufstellung in Item-Aufträgen) jetzt nur noch `(Eigentümer, Produktname)`. Reine interne Performance-Verbesserung, vor allem in der Admin-Gesamtansicht.

- **Auftrags- und Nutzerlisten lösen deutlich weniger Datenbankabfragen aus.** Die Auftragsübersicht berechnet Lagerbestände und Anspruchsdaten der ganzen Seite jetzt in wenigen gebündelten Abfragen statt einer pro Material pro Auftrag; die Nutzerliste holt die Staffel-Zugehörigkeit je Nutzer nur noch einmal statt dreimal; die Profit-Sichtbarkeitsprüfung wird pro Anfrage einmal ausgewertet. Rein interne Performance-Verbesserung, kein sichtbares Verhalten ändert sich.

- **Schnellere Datenbankzugriffe durch zusätzliche Indizes.** Sechs fehlende Indizes (`V175`) verhindern sequentielle Tabellen-Scans auf wachsenden Tabellen — u. a. die aktive Auftragstafel (Filter + Sortierung), die offene Bank-Buchungsverteilung, die Discord-Freigabe-Warteschlange und mehrere Fremdschlüssel. Reine Performance-Änderung; sichtbares Verhalten ändert sich nicht.

- **Weniger Datenbankabfragen auf Beförderungs-, Hangar-, Leitungs- und Bank-Verwaltungsseiten.** Die Beförderungs-Eignungsprüfung lädt die Bewertungsdaten je Mitglied einmal statt mehrfach pro Rangübergang; die Staffel-Hangarübersicht gruppiert Schiffe einmal statt pro Typ neu zu filtern; die Mitgliedschafts- und Oversight-Abfragen werden pro Anfrage gebündelt; der Bank-Reset/-Storno lädt Kontoinhaber gebündelt. Reine interne Performance-Verbesserung ohne Verhaltensänderung.

- **Der Kopfbereich der Auftragsdetails (`/orders/{id}`) ist entschlackt.** Die hohe einspaltige Metadatenliste weicht einer Titelzeile (Auftrag-Nr. + Art + Status), einer kompakten Fakten-Leiste und einer in drei Gruppen (Auftrag/Zeit/Beteiligte) gegliederten Karte; der Kommentar steht jetzt als abgesetzter Hinweis. Dadurch wird der Kopf rund halb so hoch und die bestellten Items bzw. benötigten Materialien sind ohne Scrollen sichtbar. Status, Rollen-Sichtbarkeit und die optimistische Sperre bleiben unverändert.

### Fixed

- **Das Löschen eines ausgeschiedenen Nutzers durch einen Admin schlägt nicht mehr fehl, wenn der Nutzer Einsätze besessen oder Materialeintragungen vorgenommen hat.** Die Einsatz-Eigentümerschaft wird jetzt auch in der begleitenden Eigentümer-Tabelle auf einen Admin übertragen und der Bearbeiter-Vermerk an Materialeintragungen vor dem Löschen geleert, sodass keine verwaisten Verweise mehr die Löschung mit einem Datenbankfehler (SQLSTATE 23503) blockieren.

- **Ein bereits in Keycloak gelöschter Nutzer ließ sich nicht mehr aus dem Basetool löschen, wenn zuvor über seine Registrierung eine Discord-Freigabe entschieden wurde.** Die Löschung scheiterte an der Freigabe-Historie (V173), deren Fremdschlüssel beim Entfernen bisher nicht aufgelöst wurden (Datenbank-Constraint-Verletzung → 409). Beim endgültigen Löschen eines Nutzers werden seine eigenen Freigabe-Audit-Einträge jetzt mitentfernt und die Verweise auf ihn als entscheidender Admin aufgelöst; die Freigabe-Historie anderer Nutzer bleibt unberührt.

- **Der Keycloak-Nutzerabgleich liest jetzt alle Nutzer seitenweise, statt nur die erste Seite.** Bei mehr als ~100 Nutzern lieferte Keycloak nur die erste Seite; der Abgleich markierte daraufhin alle übrigen Nutzer fälschlich als „nicht mehr in Keycloak" (stiller Soft-Delete echter Mitglieder). Der Abgleich blättert nun über `first`/`max` durch die komplette Liste (neue Einstellung `app.keycloak.sync.page-size`, Default 100). Betrifft nur Installationen mit mehr als einer vollen Keycloak-Seite an Nutzern.

- **Abschnittsüberschriften, die in einem AJAX-Swap-Container stecken, haben wieder ihren gewohnten Abstand zum Inhalt darüber.** Der Container machte die Überschrift zum ersten Element und entfernte so ihren oberen Abstand, sodass sie direkt am vorherigen Block klebte. Betrifft in den Auftragsdetails „Aggregierte Materialien" und „Übergaben" sowie „Notiz" in der Detailansicht persönlicher Blueprints.

- **Lagerbestand lässt sich nicht mehr einem Auftrag zuordnen, dessen Materialliste das Material gar nicht enthält.** Solche Zuordnungen tauchten in der aggregierten Materialliste des Auftrags nie auf, banden den Bestand aber trotzdem. Das „Auftrag"-Dropdown im Lager bietet jetzt nur noch Aufträge an, die das Material wirklich benötigen, und bereits bestehende Fehlzuordnungen werden in der Auftragsansicht als Warnhinweis aufgelistet, damit man sie auflösen kann.

- **Auf der Keycloak-Anmeldeseite erscheint wieder der gewohnte Wabenhintergrund statt des PatternFly-Standardrasters, und der „Discord“-Anmeldebutton trägt jetzt das Discord-Logo.** Das Raster war PatternFlys Standard-Login-Bild (per Keycloak-Common-Theme auf `body` gesetzt); unsere Wabenregel hat jetzt Vorrang. Wird mit dem neu bereitgestellten Keycloak-Theme wirksam (ggf. Browser-Cache leeren / Hard-Reload).

### Security

- **Fremde Gast-Anmeldungen an öffentlichen Einsätzen lassen sich nicht mehr von Unbeteiligten ändern oder löschen.** Bisher konnte jede Person mit der (öffentlich sichtbaren) Teilnehmer-ID eine fremde Gast-Anmeldung bearbeiten, abmelden oder deren Auszahlungswunsch umstellen. Eine Gast-Anmeldung ist jetzt an ein einmalig vergebenes, geheimes Bearbeitungs-Token gebunden: nur wer es besitzt (der ursprüngliche Gast) oder eine Einsatzleitungs-Rolle hat, darf die Zeile noch ändern. Wer Cookies bzw. Seitendaten löscht, verliert die Selbstbearbeitung — dann übernimmt die Einsatzleitung.
- **Offiziere können Finanzeinträge fremder Staffeln nicht mehr bearbeiten oder löschen.** Das Bearbeiten/Löschen eines Missions-Finanzeintrags prüft für Offiziere jetzt zusätzlich die Staffel-Zugehörigkeit der Mission (wie bei allen anderen Schreibvorgängen an Missionen); nur Administratoren behalten den staffelübergreifenden Zugriff.
- **Offiziere können keine Bewertungen mehr für Mitglieder fremder Staffeln anlegen, ändern oder löschen.** Die Mitglieder-Bewertung prüft jetzt zusätzlich, dass das bewertete Mitglied zur Staffel des Offiziers gehört (bisher wurde nur die Staffel der Bewertungs-Kategorie geprüft); Administratoren bewerten weiterhin staffelübergreifend.
- **Profilbeschreibung und Anzeigename sind jetzt längenbegrenzt (10.000 bzw. 255 Zeichen).** Überlange Eingaben werden serverseitig mit einer klaren Fehlermeldung abgewiesen, statt unbegrenzt in der Datenbank gespeichert zu werden.
- **Datei-Importe (Schiffe/Fleetview und Baupläne) sind jetzt auf 8 MB begrenzt.** Übergroße Uploads werden sofort mit einer klaren Fehlermeldung abgewiesen, bevor die Datei vollständig in den Arbeitsspeicher geladen wird (Schutz vor Speicher-Erschöpfung); reale Exporte liegen weit darunter.
- **Die Begrenzung auf zwei gleichzeitige Sitzungen pro Konto greift jetzt tatsächlich.** Mit den Redis-Sitzungen war das Limit bisher wirkungslos; ab sofort verdrängt die dritte gleichzeitige Anmeldung die älteste Sitzung — eine parallel genutzte, gestohlene Sitzung wird so verlässlich und auch über einen Neustart des Frontends hinweg verdrängt.
- **Härtung am Ingest-Gateway und an der stillen Neu-Anmeldung.** Lehnt das Backend einen Import ab, gibt das Gateway jetzt nur noch die geprüfte, längenbegrenzte Fehlermeldung des Backends weiter statt der rohen Antwort. Das kurzlebige `SSO_ATTEMPTED`-Cookie der stillen Re-Anmeldung trägt jetzt `SameSite=Strict` wie alle anderen Cookies.
- **Nicht angemeldete Besucher sehen bei öffentlichen Einsätzen keine Auszahlungswünsche und keine Freitext-Kommentare der Teilnehmer mehr.** Roster, Ränge, Schiff-/Einheiten-Zuweisung, Funkfrequenzen und Organisation bleiben für die öffentliche Anmeldung sichtbar; die Auszahlungsabsicht und Freitext-Notizen einzelner Teilnehmer sind nur noch für angemeldete Mitglieder sichtbar (ADR-0034).

## [v0.7.3](https://github.com/krt-profit/basetool/releases/tag/v0.7.3) - 2026-06-21

### Added

- **Auf der Keycloak-Anmeldeseite selbst gibt es jetzt eine „Discord“-Schaltfläche.** Damit ist der Discord-Login auch dort erreichbar, wo die normale Anmeldemaske erscheint — etwa beim Anmelden im Extractor (Device-Grant) oder für alle, die einfach „Anmelden“ wählen und den getrennten Discord-Eintrag in der Seitenleiste übersehen. Wird mit dem neu bereitgestellten Keycloak-Theme wirksam (der Discord-IdP darf nicht auf „Hide on login page“ stehen).

### Changed

- **Discord-Login zeigt den Zustimmungs-Bildschirm nicht mehr bei jeder Anmeldung.** Nach der ersten Autorisierung überspringt Discord die Zustimmung (`prompt=none`); nur die allererste Anmeldung zeigt sie noch. Wird mit dem neu bereitgestellten Keycloak-Provider-JAR wirksam.

- **Neue Umgebungsvariable `APP_REGISTRATION_REQUIRE_APPROVAL` (Default `true`).** Steuert, ob neue Nicht-Admin-Accounts vor der Aktivierung eine Admin-Freigabe durchlaufen; wird nur im E2E-Test-Stack auf `false` gesetzt. Kein Effekt im Produktivbetrieb (Default = bisheriges Verhalten).

### Fixed

- **Der Discord-Anmeldelink in der Seitenleiste zeigt für nicht angemeldete Nutzer wieder den richtigen Text statt eines Platzhalter-Schlüssels.** Der Übersetzungsschlüssel für „Mit Discord anmelden" fehlte in den Sprachdateien. Hinter dem Text steht jetzt zusätzlich das Discord-Logo.

## [v0.7.1](https://github.com/krt-profit/basetool/releases/tag/v0.7.1) - 2026-06-21

## [v0.7.0](https://github.com/krt-profit/basetool/releases/tag/v0.7.0) - 2026-06-21

### Added

- **Anmeldung mit Discord (Track 1).** Mitglieder können sich jetzt zusätzlich mit Discord anmelden — aber nur, wer im das-kartell Discord ist und dort die Rolle „KRT-Mitglied“ trägt (direkt bei der Anmeldung geprüft, fail-closed). Neu über Discord angemeldete Nutzer landen zunächst in einem Wartezustand ohne Zugriff und müssen von einem Administrator freigegeben werden (Admin-Übersicht unter „Discord-Registrierungen“, Admins werden benachrichtigt); Rollen und Einheiten werden danach wie gewohnt von Hand vergeben. Die bestehende Anmeldung per Benutzername/Passwort selbst bleibt unverändert; neu angelegte Accounts ohne Administratorrechte durchlaufen jetzt jedoch ebenfalls die Freigabe (fail-safe gegen Fehlkonfiguration der Discord-Mapper), bestehende Accounts bleiben aktiv. Wird erst nach der serverseitigen Einrichtung (Discord-App + Keycloak) wirksam.

## [v0.6.4](https://github.com/krt-profit/basetool/releases/tag/v0.6.4) - 2026-06-20

### Fixed

- **Im Organigramm erscheinen jetzt alle Staffeln und Spezialkommandos unter ihrem Bereich, nicht nur die Profit-Einheiten.** Bisher wurden unter einem Bereich nur Einheiten angezeigt, die als „auftragsbearbeitend" (profit-berechtigt) markiert waren; einem Bereich zugeordnete Staffeln anderer Bereiche (etwa Forschung oder Marinekorps) blieben unsichtbar. Ob eine Einheit im Organigramm erscheint, richtet sich jetzt nur noch danach, ob sie aktiv ist — die Einstellung „Auftragsbearbeitung" steuert weiterhin ausschließlich die Auftragsabwicklung, nicht die Anzeige im Organigramm.

## [v0.6.3](https://github.com/krt-profit/basetool/releases/tag/v0.6.3) - 2026-06-20

### Fixed

- **Der Staffel-Filter in der Auftragsübersicht löst keine sporadischen Abmeldungen mehr aus.** Beim Umschalten zwischen „Eigene Staffel" und „Alle Staffeln" konnte die Sitzungserneuerung fehlschlagen, sodass die Aufträge kurzzeitig nicht luden und erst ein erneutes Laden half. Ursache war, dass der Filterwert fälschlich als Berechtigungsumfang an Keycloak gesendet wurde; das ist nun unterbunden.

## [v0.6.2](https://github.com/krt-profit/basetool/releases/tag/v0.6.2) - 2026-06-20

### Added

- **Aufträge können jetzt einen Bereich oder die Organisationsleitung als Auftraggeber haben.** Angemeldete Nutzer können beim Anlegen oder Bearbeiten eines Auftrags neben Staffeln und Spezialkommandos auch einen Bereich oder die Organisationsleitung als Auftraggeber auswählen. Die bearbeitende (verantwortliche) Einheit bleibt wie bisher eine profit-berechtigte Staffel oder ein Spezialkommando, und das öffentliche, anonyme Auftragsformular bietet als Auftraggeber weiterhin nur Staffeln und Spezialkommandos. (Einsätze lassen sich bereits seit v0.6.0 einem Bereich oder der Organisationsleitung zuordnen.)

- **Im Organigramm lassen sich jetzt auch freie Namen eintragen.** Administratoren können eine Position mit dem Namen eines Kartell-Mitglieds besetzen, das noch keinen Basetool-Account hat (Auswahl „Name eintragen" im Besetzen-Dialog); der Name erscheint mit der Markierung „Kein Account". Sobald das Mitglied einen Account hat, wird die Position einfach auf diesen Account umgestellt — der freie Name wird dabei ohne Datenverlust ersetzt. Eine so besetzte Position verleiht weiterhin keinerlei Rechte.

### Changed

- **Das Organigramm stellt die Bereiche jetzt nebeneinander statt untereinander dar und verbindet sie mit Linien zur Organisationsleitung.** Die Bereiche fächern unter der Organisationsleitung auf — mit denselben Verbindungslinien wie von einem Bereich zu seinen Staffeln und Spezialkommandos. Jeder Bereich lässt sich außerdem unterhalb seines Bereichsleiters ein- und ausklappen, sodass das Organigramm bei vielen Einheiten übersichtlich bleibt.

### Fixed

- **Raffinerie-Aufträge mit vielen Erz-Zeilen lassen sich wieder speichern.** Das Anlege-Formular wird per AJAX als Multipart gesendet (ein Teil pro Formularfeld); Tomcat 11 begrenzte die Teilanzahl neuerdings auf 10, sodass große — etwa aus vielen Screenshots importierte — Aufträge beim Speichern mit einem Serverfehler abbrachen. Das Limit ist wieder großzügig gesetzt, und ein Überschreiten erscheint nun als verständliche Meldung statt als „500".

## [v0.6.1](https://github.com/krt-profit/basetool/releases/tag/v0.6.1) - 2026-06-20

### Added

- **Administratoren können die Organisationsstruktur jetzt direkt in der App pflegen.** Unter Administration → Organisationsstruktur lassen sich Bereiche und die Organisationsleitung anlegen und Staffeln/Spezialkommandos einem Bereich bzw. Bereiche der Organisationsleitung zuordnen — bisher war das nur über die API möglich. Die Besetzung der Leitungspositionen bleibt im Organigramm.

### Changed

- **Die Mindestqualität in Aufträgen ist von 700 auf 650 gesenkt.** In Material- und Item-Aufträgen steht als Mindestqualität nur noch „650" oder „Keine" zur Auswahl (Voreinstellung 650), und Lagerbestände ab Qualität 650 zählen jetzt als „Gut" für Deckung und Sammelfortschritt. Bestehende Aufträge mit dem alten Wert 700 werden per Datenbank-Migration automatisch auf 650 umgestellt.

## [v0.6.0](https://github.com/krt-profit/basetool/releases/tag/v0.6.0) - 2026-06-20

### Added

- **Bereichsleitung und Organisationsleitung sehen und bearbeiten jetzt die Daten ihrer untergeordneten Einheiten.** Wer Bereichsleiter, -koordinator oder -operator eines Bereichs ist, erhält offiziersgleichen Zugriff auf alle Staffeln und Spezialkommandos dieses Bereichs (Einsätze, Hangar, Lager, Raffinerie, Operationen, Aufträge); die Organisationsleitung auf alle Einheiten. Der Zugriff ist streng auf den eigenen Bereich begrenzt (nur die OL übergreift Bereiche) und verleiht ausdrücklich keine Administratorrechte.

- **Bereiche und die Organisationsleitung können jetzt eigene Daten besitzen, und die Leitung kann stellvertretend für eine untergeordnete Einheit anlegen.** Beim Anlegen eines Eintrags (Lager, Hangar, Raffinerie, Einsatz, Operation) lässt sich als besitzende Einheit der eigene Bereich bzw. die OL oder eine untergeordnete Staffel/ein Spezialkommando wählen, die man verantwortet. Die Voreinstellung und das Verhalten für normale Mitglieder bleiben unverändert.

- **Das Organigramm bildet jetzt die echte Organisationsstruktur ab: Organisationsleitung → Bereiche → Staffeln und Spezialkommandos.** Jeder Bereich erscheint als eigener Ast in seiner Bereichsfarbe (mit Bereichsleiter, -koordinatoren und -operatoren), darüber steht die Organisationsleitung mit ihren Mitgliedern. Administratoren können die neuen Positionen direkt im Organigramm besetzen; noch keinem Bereich zugeordnete Staffeln/Spezialkommandos bleiben in einem eigenen Abschnitt sichtbar. Tastatur- und Screenreader-Navigation bleiben je Ast erhalten.

- **Bereichsleitung und Organisationsleitung haben jetzt Zugriff auf das Bankkonto ihrer Ebene.** Die Bereichsleitung sieht den Kontostand ihres Bereichskontos und kann dort Ein-/Auszahlungen beantragen, die Organisationsleitung dasselbe für das Kartellkonto — wie Offiziere für ihr Staffelkonto (Antrag erst nach Bestätigung durch die Bank). Zusätzlich können beide die Konten ihrer untergeordneten Einheiten einsehen (nur Ansicht, kein Antrag); ein fremder Bereich bleibt verborgen. Beim Anlegen eines Kontos werden Bereichskonten jetzt mit ihrem Bereich und das Kartellkonto mit der Organisationsleitung verknüpft. Die Bank bleibt unverändert organisationseinheiten-blind.

- **„Mein Lager" hat jetzt einen Filter „Nur persönliche Einträge".** Damit blendet man alle geteilten Beiträge aus und sieht nur noch die eigenen persönlichen Lagereinträge. Der Filter lässt sich mit den übrigen Filtern kombinieren und bleibt über die Seiten-URL teilbar.

### Changed

- **Der „Nächste Einsatz" auf der Startseite zeigt jetzt nur noch Einsätze der eigenen Organisationseinheit.** Mitglieder sehen den nächsten Einsatz ihrer Staffel(n)/Spezialkommandos; Bereichsleitung und Organisationsleitung den nächsten Einsatz ihrer untergeordneten Einheiten (die OL über alle). Fremde Einsätze — auch öffentliche anderer Einheiten — erscheinen dort nicht mehr. Wer keiner Organisationseinheit angehört (Gast, Administrator ohne aktive Auswahl), sieht weiterhin den organisationsweit nächsten Einsatz.

- **Die Blueprint-Verfügbarkeit in den Auftragsdetails lässt sich jetzt über ihre Überschrift ein- und ausklappen** (standardmäßig ausgeklappt), sodass die teils lange Liste bei Bedarf weggeklappt werden kann. Außerdem gibt es mehr Abstand vor dem Abschnitt „Item-Übergaben".

- **In den Lagereinträgen entfällt die überflüssige Beschriftung „Eintrag" vor dem Auftrags-Dropdown**, sodass die Auftrags- und Einsatzauswahl direkt und aufgeräumter erscheint.

### Fixed

- **Das Deploy-Skript rollt jetzt auch den `ingest`-Gateway aus.** Bisher löste, pinnte und zog `scripts/deploy.sh` nur die Backend- und Frontend-Images; das `ingest`-Image lief zwar mit, wurde aber nie auf seinen Digest festgenagelt (ein `:stable`-Wechsel in GHCR konnte es unbemerkt verschieben) und ein neu promotetes `ingest`-Image löste allein keinen Deploy aus. Jetzt werden alle drei Images (Backend, Frontend, Ingest) gemeinsam aufgelöst, gepinnt, vorab gezogen und gegen den Idempotenz-Marker geprüft.

## [v0.5.12](https://github.com/krt-profit/basetool/releases/tag/v0.5.12) - 2026-06-19

### Added

- **Im Profil lässt sich jetzt einstellen, dass die eigenen Blueprints allen zur Verfügung stehen.** Ist die Option aktiv, wird man in der Blueprint-Verfügbarkeit und in der Blueprint-Verfügbarkeit der Aufträge mitgezählt, auch wenn man wegen der Organisationseinheit sonst nicht berücksichtigt würde. In beiden Ansichten wird ein so eingeblendeter Besitzer dezent als "kein Einheitsmitglied" gekennzeichnet. Standardmäßig ist die Option aus.

### Fixed

- **Der Eigentümer eines Lagereintrags, Schiffs (Hangar) oder Raffinerie-Auftrags konnte ihn nicht mehr bearbeiten, nachdem er die OrgUnit gewechselt hatte oder gar keiner OrgUnit mehr angehörte, das Objekt aber noch auf eine OrgUnit gebucht war (403).** Der Eigentümer darf seine eigenen Lagereinträge, Schiffe und Raffinerie-Aufträge jetzt immer sehen und bearbeiten, unabhängig vom OrgUnit-Stempel; Nicht-Eigentümer bleiben an den OrgUnit-Scope gebunden. Die persönlichen Ansichten zeigten die Objekte bereits, nur das Backend-Gate hatte blockiert.

## [v0.5.11](https://github.com/krt-profit/basetool/releases/tag/v0.5.11) - 2026-06-18

### Fixed

- **Einzelne UEX-Items (z. B. die Skin-Variante „Pulse Greycat Laser Pistol") wurden bei jedem Item-Abgleich übersprungen und protokollierten einen Fehler.** UEX teilt sich ein Basis-Item und seine Skins eine in-game-UUID, die nur einmal vergeben werden kann (`external_uuid` ist eindeutig). Beim Nachtragen der UUID auf die Skin-Zeile kollidierte diese mit dem bereits belegten Basis-Item. Der Abgleich übernimmt eine UUID jetzt nur noch, wenn sie frei ist; andernfalls bleibt sie leer und die übrigen Felder des Items werden trotzdem aktualisiert.

- **Die Startseite zeigte trotz der bisherigen Korrekturen weiter „Fehler beim Laden der Einsätze" und der nächste Einsatz fehlte.** Ursache war Keycloaks Refresh-Token-Rotation mit Wiederverwendungs-Erkennung, die beim serverseitigen Frontend-Client (Token liegt nur in Redis, nie im Browser) keinen Sicherheitsgewinn bringt, aber bei den parallelen Token-Erneuerungen einer Sitzung wiederholt die ganze Sitzung verwarf. Die Rotation wird daher realm-weit in Keycloak abgeschaltet (`Revoke Refresh Token = Off`) — eine manuell beim Deployment zu setzende Keycloak-Einstellung; die Anmeldung bleibt damit bestehen, bis sie regulär abläuft (Details in `docs/INGEST_KEYCLOAK_SETUP.md`).

## [v0.5.10](https://github.com/krt-profit/basetool/releases/tag/v0.5.10) - 2026-06-18

### Changed

- **Hersteller, die UEX doppelt führt (u. a. Esperia, DMC, Covalex), erscheinen nur noch einmal.** UEX liefert für dieselbe Marke teils mehrere Firmeneinträge mit unterschiedlichen IDs, wodurch z. B. Esperias Schiffe und Items auf zwei getrennten Herstellern landeten. Diese Dubletten werden jetzt zu einem Hersteller zusammengeführt, sodass Schiffe und Items wieder unter derselben Marke auftauchen.

### Fixed

- **Die App zeigte beim Aktualisieren der Startseite gelegentlich „Fehler beim Laden der Einsätze" und meldete sich weiterhin von selbst neu an.** Der in v0.5.8 begonnene Fix reichte nicht: Der Benachrichtigungs-Stream konnte das Login-Token trotz nur lesendem Zugriff weiterhin erneuern und dabei einen bereits veralteten Token einreichen, was Keycloak als unzulässige Token-Wiederverwendung wertete und die Sitzung verwarf. Der Stream verwendet das Token jetzt als reinen `Authorization`-Header über eine Verbindung ohne OAuth2-Erneuerungsfilter und kann es damit grundsätzlich nicht mehr erneuern; die Sitzung bleibt bestehen, bis sie regulär abläuft.

- **Der UEX-Item-Abgleich (Waffen, Rüstung, Lackierungen, Schiffskomponenten) brach bei jedem Lauf ab — der Item-Katalog war eingefroren und übernahm keine UEX-Änderungen mehr.** UEX vergibt mehreren Items dieselbe in-game-UUID (ein Basis-Item und seine Skins), wodurch ein Eintrag mit der Unique-Bedingung auf `external_uuid` kollidierte und den ganzen Lauf zurückrollte. Jedes Item wird jetzt in einer eigenen Transaktion abgeglichen, sodass ein kollidierender Eintrag nur sich selbst überspringt.

## [v0.5.9](https://github.com/krt-profit/basetool/releases/tag/v0.5.9) - 2026-06-18

### Security

- **Keycloak wird in Produktion nur noch über HTTPS angebunden** — sowohl der nginx-proxy-manager als auch der Benutzer-Abgleich des Backends sprechen Keycloak jetzt verschlüsselt an (HTTPS-only auf Port 18443, kein Klartext-Listener mehr; der Health-Port bleibt aus Healthcheck-Gründen HTTP und ist nur containerintern erreichbar). Beim Deployment muss der Keycloak-Proxy-Host im nginx-proxy-manager auf Schema `https`/Port `18443` umgestellt und das geteilte `keystore.p12` mit `dns:keycloak` im SAN neu erzeugt werden, sonst scheitert der Abgleich an der Hostnamen-Prüfung (Schritt-für-Schritt in `docs/deployment.md`).

## [v0.5.8](https://github.com/krt-profit/basetool/releases/tag/v0.5.8) - 2026-06-18

### Changed

- **Die Konto-Kacheln im Bankbereich (`/bank`) sind jetzt alphabetisch nach dem Kontonamen sortiert** statt nach Kontonummer — sowohl in der Bankleitungs- als auch in der Mitarbeiteransicht.

- **Projekt von `krt-iri` auf `krt-profit` umgezogen (GitHub-Organisation, Container-Images und internes Java-Package `de.greluc.krt.profit.basetool`).** Repository und Images liegen jetzt unter `krt-profit/basetool` bzw. `ghcr.io/krt-profit/*`; der Standardwert von `IRI_IMAGE_NAMESPACE` ist `krt-profit`. Beim Deploy müssen Image-Namespace, GHCR-Pull-Token und Release-Pipeline auf `krt-profit` zeigen. Keine funktionale Änderung.

### Fixed

- **Die App meldete sich im laufenden Betrieb etwa alle 30–60 Minuten von selbst ab und zeigte wieder die Login-Seite.** Der langlebige Benachrichtigungs-Stream erneuerte beim 30-Minuten-Reconnect das Login-Token und konnte dabei einen bereits veralteten Token in die Sitzung zurückschreiben, was Keycloak als unzulässige Token-Wiederverwendung wertete und die gesamte Sitzung verwarf. Der Stream nutzt das Token jetzt nur noch lesend (erneuert es nicht mehr), und die parallelen Token-Erneuerungen einer Sitzung werden zuverlässig gebündelt — die Sitzung bleibt bestehen, bis sie regulär abläuft.

## [v0.5.7](https://github.com/krt-profit/basetool/releases/tag/v0.5.7) - 2026-06-18

### Added

- **Offiziere und Leads können den Kontostand ihrer Org-Einheit sehen und Ein-/Auszahlungen zur Bestätigung anstoßen (Epic #666).** Offiziere bzw. SK-Leads sehen jetzt — ohne Bankrolle — ausschließlich den **Kontostand** des Bankkontos ihrer Org-Einheit (keine Historie, keine Halter) und können **Ein- und Auszahlungsanträge** dagegen stellen. Solche Anträge werden sofort im Audit-Log vermerkt, bewegen aber kein Geld: erst wenn ein Bankmitarbeiter den Antrag bestätigt und dabei den Halter hinterlegt, wird die Buchung aufs Hauptbuch gebucht (Überziehungs- und Halterprüfung erfolgen bei der Bestätigung). Antragsteller sehen den Status ihrer eigenen Anträge und können offene Anträge zurückziehen; Bankmitarbeiter bestätigen oder lehnen sie in einer Warteschlange ab. Beim Anlegen eines Antrags werden die Bankleitung und die für das Konto berechtigten Bankmitarbeiter per In-App-Benachrichtigung informiert; bei Bestätigung oder Ablehnung wird der Antragsteller benachrichtigt (mit Ablehnungsgrund). Ein Konto mit offenen Anträgen kann nicht geschlossen werden. Offiziere/Leads erreichen die schlanke Seite „Org-Einheits-Bank", Bankmitarbeiter eine Antrags-Warteschlange im Bankbereich; das Admin-Audit-Log zeigt die neuen Antrags-Ereignisse.

### Fixed

- **Im Finanzen-Tab einer Mission fehlte der Abstand zwischen dem Auszahlungsblock und der Raffinerieaufträge-Liste.** Der AJAX-Umbau der Mission-Detailseite hatte die Finanzblöcke in einen gemeinsamen Container verschachtelt und dabei den standardmäßigen Abstand zwischen ihnen verloren; der gewohnte Abstand ist jetzt wiederhergestellt.

- **Die Finanzeinträge einer Mission (manuelle Buchungen und automatisch verknüpfte Raffineriekosten) wurden manchmal gar nicht mehr angezeigt — der Bereich blieb leer, obwohl die Daten vorhanden waren.** Nach einem kurzzeitigen Backend-Aussetzer traf die Mitglieder-Rollenprüfung im Frontend eine sitzungsweite Fehlentscheidung, die sich bisher nur durch Ab- und Wiederanmelden beheben ließ. Die Prüfung liest die Rollen jetzt aus derselben Quelle wie die übrige Zugriffskontrolle, und ein fehlgeschlagener Rollenabgleich macht die Sitzung nicht mehr dauerhaft unbrauchbar.

## [v0.5.6](https://github.com/krt-profit/basetool/releases/tag/v0.5.6) - 2026-06-17

### Changed

- **Der `ingest`-Gateway serviert jetzt durchgehend HTTPS auf Port 11262 statt Plain-HTTP** — analog zu Backend und Frontend. Beim Deployment muss der nginx-proxy-manager-Proxy-Host des Gateways auf Upstream-Schema `https` umgestellt und die Upstream-Zertifikatsprüfung deaktiviert werden (selbstsigniertes Zertifikat); Container-Healthcheck und Reverse-Proxy sprechen den Dienst entsprechend über `https://` an.

## [v0.5.5](https://github.com/krt-profit/basetool/releases/tag/v0.5.5) - 2026-06-17

### Fixed

- **Der Ein-Klick-Versand aus dem SC-Extractor schlug mit „HTTP 400" fehl und der `ingest`-Dienst blieb dauerhaft `unhealthy`.** Der Gateway-Dienst startete fälschlich mit TLS auf Port 11262 — die nur als Truststore gedachten `SERVER_SSL_KEY_STORE`-Variablen aktivierten zugleich den Server-Connector —, sodass der per Plain-HTTP anbindende Reverse-Proxy (nackte 400) und der Container-Healthcheck abgewiesen wurden. Der Dienst serviert jetzt wie vorgesehen Plain-HTTP hinter dem Proxy (`server.ssl.enabled=false`); die Truststore-Nutzung für die Backend-Verbindung bleibt unverändert.

- **Der geplante UEX-Hersteller-Abgleich aktualisiert wieder die Herstellerdaten.** Mehrere unterschiedliche UEX-Firmen teilen sich dieselbe Abkürzung (z. B. „Esperia"), was an einer Eindeutigkeitsbedingung der Hersteller-Abkürzung scheiterte und — weil der gesamte Abgleich in einer einzigen Transaktion lief — jede Hersteller-Aktualisierung samt der nachfolgenden UEX-Schritte (Fahrzeuge, Items, Raffinerie) des Laufs verwarf. Die Abkürzung ist nun kein eindeutiger Schlüssel mehr, und jede Firma wird einzeln verarbeitet, sodass eine einzelne fehlerhafte Zeile den restlichen Lauf nicht mehr zurückrollt.

- **Die App meldet sich jetzt automatisch neu an, statt leere Seiten zu zeigen, wenn das Login-Token der Sitzung verfällt.** Nach bestimmten Abläufen (z. B. Anmeldung über den Extractor-Handoff) konnte es passieren, dass plötzlich keine Aufträge, Lager- oder Raffinerieeinträge mehr angezeigt wurden und nur ein manuelles Ab- und Wiederanmelden half, während im Hintergrund das Log volllief. Solche Fälle lösen jetzt eine stille Neuanmeldung über Keycloak aus (AJAX-Aufrufe leiten den Browser dorthin um), und die parallelen Backend-Aufrufe einer Seite erneuern das Token nur noch ein einziges Mal gemeinsam, sodass die Sitzung gar nicht erst ungültig wird.

## [v0.5.4](https://github.com/krt-profit/basetool/releases/tag/v0.5.4) - 2026-06-17

### Added

- **Ein-Klick-Versand aus dem SC-Extractor (Epic #639).** Ein eigenständiger, bewusst minimal gehaltener Ingest-Dienst nimmt die im Extractor erzeugten Refinery- und Blueprint-Daten authentifiziert entgegen, reicht sie an die bestehenden Import-Endpunkte weiter und legt den Entwurf kurzlebig und einmalig abrufbar ab. Öffnet der Extractor anschließend die Basetool-Seite mit `?handoff=<id>`, landet man — nach ggf. nötiger Anmeldung — direkt auf dem vorausgefüllten Review: das Raffinerie-Anlegen-Formular bzw. die Blueprint-Import-Vorschau sind bereits befüllt, gespeichert wird weiterhin erst nach Prüfung. Ein abgelaufener oder ungültiger Link zeigt einen freundlichen Hinweis und das leere Formular. Neue Konfiguration (Dienst `ingest`, Port 11262): `BACKEND_URL`, `FRONTEND_URL`, `KEYCLOAK_ISSUER_URI`, `REDIS_HOST`/`REDIS_PORT`/`REDIS_PASSWORD`, `APP_SECURITY_JWT_EXPECTED_AUDIENCES`.

### Security

- **Missions-Teilnehmer fremder Staffeln sind nicht mehr durch einen Missions-Manager einer anderen Einheit veränderbar.** Wer in einer Staffel Missions-Manager war, konnte bei internen Missionen anderer Staffeln verknüpfte Teilnehmer ein-/auschecken, deren Auszahlungspräferenz umstellen oder sie entfernen — Werte, die in die Auszahlungsberechnung einfließen. Bei verknüpften Teilnehmern greift jetzt dieselbe Eigentumsprüfung (besitzende Org-Einheit) wie bei allen anderen Missions-Schreibzugriffen; offene Gast-Teilnehmer öffentlicher Missionen bleiben unverändert bearbeitbar.

- **Das pro-IP-Anti-Spam-Limit öffentlicher Formulare trifft wieder einzelne Clients statt alle.** Da das Backend nur über das Frontend erreichbar ist, sah es bisher für sämtliche Nutzer nur eine einzige IP — ein einzelner, auch nicht angemeldeter Nutzer konnte so das Limit z. B. des Auftragsformulars für die gesamte Organisation auslösen. Das Frontend reicht die echte Client-IP jetzt an das Backend weiter, sodass die Begrenzung wieder pro Client wirkt.

- **Der Ingest-Dienst drosselt jetzt zusätzlich pro angemeldetem Nutzer.** Bisher begrenzte er nur pro Quell-IP, die sich über einen `X-Forwarded-For`-Header fälschen ließ; jetzt gilt zusätzlich ein nicht fälschbares Limit pro Nutzer (mit `Retry-After`), die IP-Erkennung vertraut dem Header nur noch vom Reverse-Proxy, und die internen Zähler-Tabellen sind nach oben beschränkt.

- **Die 2-MB-Obergrenze des Ingest-Dienstes lässt sich nicht mehr per Chunked-Upload umgehen.** Ein Upload ohne deklarierte Länge konnte die Größenprüfung passieren; sie wird jetzt anhand der tatsächlich gelesenen Datenmenge durchgesetzt und übergroße Anfragen werden mit 413 abgewiesen.

- **Der Keycloak-Benutzername wird nicht mehr im Klartext protokolliert.** Eine selten genutzte Fallback-Logzeile gab den Callsign eines Mitglieds unmaskiert in die Logs aus; sie protokolliert jetzt nur noch die anonyme Nutzer-ID.

## [v0.5.3](https://github.com/krt-profit/basetool/releases/tag/v0.5.3) - 2026-06-16

### Added

- **Standard-Blueprints werden automatisch jedem Account gutgeschrieben.** Die auf jedem Star-Citizen-Account von Anfang an freigeschalteten Blueprints (Starter-Pistole und -Gewehr, deren Magazine, die Field-Recon-Suit-Teile) lassen sich nicht über einen Import erfassen und werden deshalb jedem Nutzer automatisch in die persönliche Blueprint-Liste eingetragen — auch rückwirkend für bestehende Konten und fortlaufend für neue. Sie sind nicht löschbar (es wird kein Lösch-Knopf angezeigt); Admins pflegen die Standard-Liste über eine neue Verwaltungsseite. Neue Konfiguration: `APP_DEFAULT_BLUEPRINTS_PROVISIONING_ENABLED`, `APP_DEFAULT_BLUEPRINTS_PROVISIONING_INTERVAL`.

### Changed

- **Der aktive Bereichskontext (z. B. „IRIDIUM" oder „Alle Staffeln") wird nicht mehr doppelt angezeigt.** Bisher stand er gleichzeitig im Chip oben rechts und hinter dem App-Titel. Der überflüssige Chip entfällt; der aktive Bereich bleibt hinter dem App-Titel sichtbar — jetzt auch bei aktivem Spezialkommando, das der Titel zuvor nicht anzeigte.

## [v0.5.2](https://github.com/krt-profit/basetool/releases/tag/v0.5.2) - 2026-06-16

### Added

- **Neues Benachrichtigungssystem (Epic #622).** Eine Glocke oben rechts zeigt auf jeder Seite dauerhaft die Anzahl ungelesener Benachrichtigungen; Nutzer können Benachrichtigungen als gelesen markieren und einzeln oder gesammelt (gelesene) löschen — gelesene werden zusätzlich nach 90 Tagen automatisch entfernt. Beim Anlegen eines Auftrags werden die Offiziere der zuständigen Staffel bzw. die Leads des zuständigen Spezialkommandos sowie die Logistiker dieser Einheit und die Admins benachrichtigt (der Ersteller ausgenommen); wer wofür benachrichtigt wird, steuern Admins über datengetriebene Regeln auf einer eigenen Verwaltungsseite. Neue Benachrichtigungen erscheinen per Echtzeit-Push (SSE) sofort, mit Polling als Rückfallebene. Neue Konfiguration: `APP_NOTIFICATIONS_RETENTION_ENABLED`, `APP_NOTIFICATIONS_RETENTION_MAX_AGE`, `APP_NOTIFICATIONS_RETENTION_INTERVAL`.

## [v0.5.1](https://github.com/krt-profit/basetool/releases/tag/v0.5.1) - 2026-06-15

### Fixed

- **Übersichtsseiten zeigen nach dem Zurück-Navigieren wieder aktuelle Werte (Browser-Back/Forward-Cache).** Wer z. B. in der Bank eine Einzahlung vornimmt und dann per Zurück-Taste zur Kontenübersicht zurückgeht, sah dort bisher noch den alten Saldo in der Kachel — erst ein manuelles Neuladen zeigte den neuen Wert. Der Browser stellte die Seite aus seinem Verlaufs-Cache wieder her, ohne sie neu vom Server zu laden. Übersichtsseiten laden sich beim Zurück-/Vorwärts-Wiederherstellen jetzt einmalig neu, sodass Salden, Zähler und Status app-weit aktuell sind.

- **Die Sofortsuche in der persönlichen Blueprint-Liste (`/personal-inventory/blueprints`) filtert wieder.** Das Tippen in das Suchfeld der vorhandenen Blueprints berechnete die Treffer korrekt, blendete die nicht passenden Zeilen aber nie aus, weil eine CSS-Regel das `hidden`-Attribut der Zeilen überstimmte. Die Suche der allgemein verfügbaren Blueprints (Hinzufügen-Leiste) war davon nicht betroffen.

- **Suchbare Auswahllisten werden in Modals nicht mehr abgeschnitten.** Das Aufklapp-Menü einer suchbaren Auswahl (z. B. „Nutzer wählen" beim Halter-Registrieren in der Bank-Verwaltung) wurde am unteren Rand vom Fußbereich des Modals mit den Schaltflächen verdeckt. Die Liste legt sich jetzt als Overlay über den Modal-Rahmen und klappt bei wenig Platz nach oben auf, sodass alle Einträge sichtbar und auswählbar sind.

## [v0.5.0](https://github.com/krt-profit/basetool/releases/tag/v0.5.0) - 2026-06-15

### Changed

- **Organigramm, Mitgliederverwaltung und Profil ohne Seitenneuladen (Epic #571 / #581):** Im Organigramm aktualisieren das Zuweisen, Neubesetzen, Umbenennen und Entfernen von Positionen jetzt nur das Diagramm in-place statt die ganze Seite neu zu laden. In der Mitgliederverwaltung entfernt das Löschen eines Mitglieds die Zeile in-place, und ein fehlgeschlagener Logistiker-/Missions-Manager-Schalter wird direkt zurückgesetzt statt die Seite neu zu laden; das Bearbeiten eines Mitglieds speichert ohne Neuladen, wobei Eingabefehler direkt am jeweiligen Feld erscheinen. Auf der Profilseite speichert die Standard-Auszahlungspräferenz jetzt ohne Neuladen, und auf der Startseite wird „Als gelesen markieren" in-place übernommen. Das Umschalten der aktiven Org-Einheit lädt bewusst weiterhin die Seite neu, weil es den Geltungsbereich der gesamten Seite ändert. Ohne JavaScript funktionieren die klassischen Formulare unverändert weiter.

- **Admin-Verwaltung ohne Seitenneuladen (Epic #571 / #582):** Die restlichen Admin-Verwaltungsseiten aktualisieren ihre Schreibaktionen jetzt in-place statt die Seite neu zu laden — Informationen (Speichern/Löschen), Material-Kategorien und -Aliase, Orte (Sichtbarkeit/Home-Location), Missionsdaten (Staffeln, Auftrags- und Frequenztypen anlegen/bearbeiten/löschen/aktivieren samt Drag-and-drop-Sortierung der Frequenztypen), Spezialkommandos samt Mitgliederverwaltung (Rollen- und Leiter-Markierungen, Mitglieder hinzufügen/entfernen), Systemeinstellungen, die UEX-Umschalter, das Leeren der Bankdaten und das Löschen alter Sync-Berichte. Ein direkt nachfolgender Schritt führt nicht mehr zu einem Konflikt, und bei einem zwischenzeitlichen Konflikt erscheint ein Hinweis mit der Option, die aktuellen Werte zu laden; damit ist der app-weite Umbau zum Verzicht auf Seitenneuladen (Epic #571) abgeschlossen. Ohne JavaScript funktionieren die klassischen Formulare unverändert weiter.

- **Beförderungssystem-Verwaltung ohne Seitenneuladen (Epic #571 / #580):** In der Themenbereiche-Verwaltung aktualisieren das Anlegen/Bearbeiten/Löschen von Themenbereichen und Kategorien sowie das Verschieben per Pfeiltasten jetzt die Liste in-place statt die Seite neu zu laden — das Verschieben funktioniert dadurch wieder zuverlässig (es schlug zuvor fehl) und ein zweites Verschieben führt nicht mehr zu einem Konflikt. Bei den Rangvoraussetzungen aktualisieren Anlegen/Bearbeiten/Löschen und das Löschen einer ganzen Rangstufe die Liste ebenfalls in-place, der Suchfilter bleibt erhalten. In der Bewertungsverwaltung werden die Beförderbarkeits-Markierungen eines Mitglieds nach dem Speichern einer Bewertung direkt neu berechnet, und ein zwischenzeitlicher Konflikt aktualisiert die Matrix vor Ort statt neu zu laden. Ohne JavaScript funktionieren die klassischen Formulare unverändert weiter.

- **Bank ohne Seitenneuladen (Epic #571 / #579):** Ein- und Auszahlungen, Transfers, Umbuchungen und Stornos auf der Kontodetailseite sowie das Anlegen, Umbenennen, Schließen und Wiederöffnen von Konten, das Registrieren und (De-)Aktivieren von Haltern und das Erteilen/Entziehen von Berechtigungen aktualisieren jetzt nur den betroffenen Bereich in-place — Saldo, Halter-Verteilung, Buchungshistorie und Zähler — statt die ganze Seite neu zu laden; das Umschalten einzelner Berechtigungs-Flags wird direkt an der Zeile übernommen. Damit ist der Bank-Bereich frei von Seitenneuladen. Ohne JavaScript funktionieren die klassischen Formulare unverändert weiter.

- **Hangar, Schiffsdaten und persönliches Inventar/Blueprints ohne Seitenneuladen (Epic #571 / #578):** Schiffe anlegen/bearbeiten/löschen, die Home-Location aller Schiffe setzen sowie der Fleetview-Import und das vollständige Leeren des Hangars aktualisieren die Schiffsliste jetzt in-place statt die Seite neu zu laden. Auf der Schiffsdaten-Seite schalten die Sichtbarkeits-Umschalter für Hersteller und Schiffstypen die jeweilige Zeile direkt um, und das Zurücksetzen aller „Fitted"-Markierungen läuft ohne Neuladen. Im persönlichen Inventar aktualisieren Anlegen, Bearbeiten und Löschen die Liste in-place; bei den Blueprints wird das Bearbeiten einer Notiz direkt an der Zeile übernommen, während Entfernen, das Hinzufügen mehrerer Blueprints und der JSON-Import die Liste ohne Neuladen neu rendern. Bei einem zwischenzeitlichen Konflikt erscheint ein Hinweis mit der Option, die aktuellen Werte zu laden. Ohne JavaScript funktionieren die klassischen Formulare unverändert weiter.

- **Lager ohne Seitenneuladen (Epic #571 / #577, Teil 1):** Das Einbuchen, das Bearbeiten von Notizen und das Zuordnen von Aufträgen/Einsätzen, die Übergaben auf der Materialsammlungsseite (Besitzer-/Standortwechsel, Lieferstatus) sowie das vollständige Leeren des globalen Lagers (Admin) aktualisieren jetzt nur den betroffenen Bereich in-place statt die ganze Seite neu zu laden. Ein direkt nachfolgender Schritt auf derselben Zeile führt nicht mehr zu einem Konflikt. Das Ausbuchen und die Sammel-Ausbuchung folgen in Teil 2. Ohne JavaScript funktionieren die klassischen Formulare unverändert weiter.

- **Blaupausen-Verfügbarkeit zählt jetzt Varianten mit (Item-Aufträge und Org-Übersicht).** In der Verfügbarkeits-/Abdeckungsanzeige eines Item-Auftrags und in der organisationsweiten „Blueprint-Verfügbarkeit" zählen Mitglieder, die eine kosmetische Variante eines geforderten Items besitzen, jetzt als verfügbar — in beide Richtungen: Wird das Basismodell gefordert (z. B. `Fresnel Energy LMG`), zählen auch Varianten wie `Fresnel "Molten" Energy LMG`; wird eine Variante gefordert, zählen Basismodell und Geschwister-Varianten. Magazine (z. B. `… Magazine (200 cap)`, `… Battery`) gelten nicht als Variante und werden nie mitgezählt. Die Abdeckungszeile zeigt den bestellten Namen mit dem Hinweis „(inkl. Varianten gezählt)", die Mitgliederliste die tatsächlich besessene Variante.

- **Lager ohne Seitenneuladen (Epic #571 / #577, Teil 2):** Das Ausbuchen eines Eintrags (einfache Ausbuchung, Umbuchung, Verkauf) und die Sammel-Ausbuchung im persönlichen Lager aktualisieren jetzt die gruppierte Lagertabelle in-place statt die ganze Seite neu zu laden; bei einem zwischenzeitlichen Konflikt erscheint ein Hinweis mit der Option, die aktuellen Werte zu laden. Damit ist der Lager-Bereich frei von Seitenneuladen. Ohne JavaScript funktioniert das klassische Ausbuchen-Formular unverändert weiter.

- **Operationen ohne Seitenneuladen (Epic #571 / #576):** Das Anlegen und Löschen einer Operation aktualisiert die Operationsliste jetzt in-place statt die Seite neu zu laden; das Bearbeiten einer Operation auf der Detailseite speichert ebenfalls ohne Neuladen (zwei Speichervorgänge unmittelbar hintereinander führen nicht mehr zu einem Konflikt), und das Löschen auf der Detailseite wechselt nach Erfolg zur Liste. Der Bezahlt-Schalter der Auszahlungen lief schon vorher in-place. Ohne JavaScript funktionieren die klassischen Formulare unverändert weiter.

- **Auftragsübersicht: Item-Aufträge zeigen in der Materialien-Spalte jetzt die aggregierte Materialliste mit Sammelfortschritt statt der Anzahl bestellter Items (#595).** Wie bei Material-Aufträgen erscheint pro Material die geforderte Menge und der Fortschritt beim Sammeln (vorhandener, dem Auftrag zugeordneter Bestand geteilt durch die benötigte Menge). So beantwortet die Spalte für beide Auftragsarten dieselbe Frage — was muss noch beschafft werden.

- **Missionsdetailseite: Crew-Planung, Finanzen, Verwaltung und Auszahlung aktualisieren ohne Seitenneuladen (Epic #571 / #574).** Einheiten, Teilnehmer und Crew anlegen/bearbeiten/entfernen, das Zuweisen per Drag-and-drop, Ein-/Auschecken, der Funktions-Schnellwechsel, Finanzeinträge anlegen/bearbeiten/löschen, Besitzerwechsel sowie das Hinzufügen/Entfernen von Co-Managern aktualisieren jetzt nur den betroffenen Bereich in-place statt die ganze Seite neu zu laden; Frequenz- und Auszahlungsänderungen liefen schon vorher in-place und nutzen jetzt dieselbe gemeinsame AJAX-Grundlage (inkl. CSRF-Erneuerung bei abgelaufenem Tab). Die Drag-and-drop-Crew-Zuweisung und alle Aktionsschaltflächen bleiben nach einer In-Place-Aktualisierung voll bedienbar. Ohne JavaScript funktionieren die klassischen Formulare unverändert weiter.

- **Missionsdetailseite: Das Bearbeiten der Missions-Kerndaten (Verwaltung) speichert jetzt ohne Seitenneuladen (Epic #571 / #589).** Name, Beschreibung, Kalenderlink, Status, Operation, interne Markierung und alle Zeiten werden in-place gespeichert; Eingabefehler erscheinen direkt am jeweiligen Feld, statt die ganze Seite neu zu laden, und zwei Speichervorgänge unmittelbar hintereinander führen nicht mehr zu einem Konflikt. Damit ist die Missionsdetailseite vollständig frei von Seitenneuladen. Ohne JavaScript funktioniert das klassische Formular unverändert weiter.

- **Auftragsverwaltung ohne Seitenneuladen (Epic #571 / #575, Teil 1):** Auf der Auftrags-Detailseite aktualisieren jetzt der Statuswechsel, das Eintragen/Bearbeiten/Zurückziehen von Material-Eintragungen, das Bearbeiten des Auftrags (Auftraggeber/Handle/Kommentar/Materialien), das Entfernen verknüpfter Lagereinträge, das Hinzufügen/Entfernen von Bearbeitern sowie das Protokollieren von Material- und Item-Übergaben nur den betroffenen Bereich in-place statt die ganze Seite neu zu laden; das Neuordnen der Auftragsprioritäten per Drag-and-drop in der Auftragsliste wird ebenfalls ohne Neuladen gespeichert. Das Löschen eines Auftrags fragt jetzt vorher per KRT-Dialog nach. Beim Anlegen und Bearbeiten von Aufträgen (Material- und Item-Aufträge) bleibt die Seite bei einem Eingabe- oder Speicherfehler erhalten und zeigt einen Hinweis, statt neu zu laden. Bei einem zwischenzeitlichen Konflikt erscheint ein Hinweis mit der Option, die aktuellen Werte zu laden. Ohne JavaScript funktionieren die klassischen Formulare unverändert weiter.

- **Raffinerieauftrag ohne Seitenneuladen (Epic #571 / #575, Teil 2):** Auf der Raffinerieauftrag-Detailseite bleiben Speichern, Einlagern und Abbrechen jetzt bei einem Eingabe- oder Speicherfehler auf der Seite und zeigen einen Hinweis, statt neu zu laden; bei Erfolg wird wie bisher zur Auftragsliste gewechselt. Das Abbrechen fragt vorher per KRT-Dialog nach, statt direkt zu löschen. Auch das Anlegen eines Raffinerieauftrags bleibt bei einem Eingabe- oder Speicherfehler mit den eingegebenen Daten auf der Seite, statt neu zu laden. Ohne JavaScript funktionieren die klassischen Formulare unverändert weiter.

- **Raffinerie: Der Screenshot-Extract-Import füllt das Anlegen-Formular jetzt ohne vollständigen Seitenneuladen aus (#591).** Nach der Auswahl der JSON-Datei wird das vorbefüllte Formular samt Prüf-Hinweisen direkt an Ort und Stelle eingesetzt; Fehler (ungültige oder zu große Datei, Backend-Ablehnung) erscheinen ebenfalls direkt im Formularbereich, statt die Seite neu zu laden. Ohne JavaScript funktioniert der klassische Upload unverändert weiter. Damit ist der gesamte Raffinerie-Bereich frei von Seitenneuladen.

- **Weitere Listen filtern und blättern jetzt ohne Seitenneuladen: Bank-Audit-Log, Mitgliederverwaltung, Aufträge, Raffinerieaufträge, die Staffel-Hangarübersicht, die Lager-Gesamtübersicht, die Blueprint-Verfügbarkeit, die Admin-Blueprint-Liste, die eingebettete Missionsliste der Operationsdetailseite, die Buchungshistorie der Bankkonto-Detailseite, das persönliche Inventar (inkl. Admin-Ansicht; dort wechselt auch die Mitgliederauswahl ohne Neuladen), die Admin-Spezialkommandoliste (Inaktive-Filter), die Seitennavigation der Admin-Sync-Berichte, die drei Inaktiv-Filter der Admin-Missionsdaten, der Mitglieder-Blueprintfilter der Admin-Ansicht und die Suche der persönlichen Blueprint-Liste** (inkl. Suche, Status-/Bereichsfilter, Seitengröße und Seitennavigation, soweit vorhanden). Gefilterte/geblätterte Listen (Einsätze, Operationen, Aufträge, Bank-Audit, Mitglieder, Raffinerieaufträge, Staffel-Hangar, Lagerübersicht, Blueprint-Verfügbarkeit, Admin-Blueprints, Operations-Missionsliste, Bank-Buchungshistorie, persönliches Inventar, Admin-Spezialkommandos, Admin-Sync-Berichte, Admin-Missionsdaten, persönliche Blueprints) halten den Filter beim Neuladen der Seite (die URL bleibt in Sync) und springen nicht mehr an den Seitenanfang; das Sortieren der Auftragsprioritäten per Drag-and-drop bleibt dabei erhalten. Auf der Admin-Missionsdatenseite bleiben Bearbeiten, Löschen und das Neuordnen der Frequenztypen per Drag-and-drop auch nach dem Filtern erhalten. Die Stat-Slider der Admin-Blueprint-Liste bleiben auch in nachgeladenen Tabellenzeilen interaktiv. Zeitstempel in nachgeladenen Tabellenzeilen werden jetzt ebenfalls in lokale Zeit umgerechnet.

### Fixed

- **Regressionsprüfung der vier Kernbereiche (Missionen, Lager, Aufträge, Raffinerie) — gefundene Regressionen behoben (Epic #571).** Ein Doppelklick auf „Speichern"/„Anlegen" löst keine doppelten Schreibvorgänge mehr aus: Die Schaltfläche wird jetzt app-weit für die Dauer des laufenden Speicherns gesperrt — das verhinderte zuvor u. a. doppelte Missions-Finanzeinträge sowie doppelt angelegte Aufträge und Raffinerieaufträge. Auf der Auftrags-Detailseite führt ein zweites Bearbeiten desselben Auftrags unmittelbar hintereinander nicht mehr zu einem Konfliktfehler (veraltete Versionsnummer in der Antwort, jetzt saveAndFlush). Das Setzen eines Auftrags auf „Erfüllt"/„Abgelehnt" aktualisiert den angezeigten Lagerbestand, die Erfüllt-Spalten und die Priorität jetzt sofort in-place statt erst nach einem Neuladen.

- **Nachzügler-Regressionen aus dem In-Place-Umbau behoben (Epic #571).** Eine Regressionsprüfung des gesamten Umbaus fand vier kleinere Stellen, an denen das frühere Seitenneuladen ein Verhalten verdeckte: Operation-Anlegen/-Löschen lässt sich nicht mehr per Doppelklick doppelt auslösen (die Schaltfläche wird während des Speicherns gesperrt), der Schiffszähler im Hangar-Dialog „Home-Location setzen" zeigt nach einer In-Place-Schiffsänderung wieder die aktuelle Anzahl, und beim Löschen des letzten Eintrags erscheint der „Keine Einträge"-Platzhalter wieder in-place (Material-Kategorien, Material-Aliase und — nach vollständiger Umbuchung — die Materialsammlung).

- **Mehrere weitere In-Place-Bearbeitungen brechen bei zwei Aktionen direkt hintereinander nicht mehr mit einem Konfliktfehler ab (Epic #571).** Eine projektweite Prüfung fand dieselbe Ursache wie im Lager — eine veraltete Versionsnummer in der Server-Antwort — an weiteren Stellen: Systemeinstellungen, Material-Aliase, das Profil (Standard-Auszahlungspräferenz und Beschreibung), die Rangstufen-Inhalte der Beförderungsverwaltung und der Lieferstatus-Schalter auf der Materialsammlungsseite. Alle schreiben den Eintrag jetzt vor dem Zurückgeben in die Datenbank (saveAndFlush), sodass eine zweite Änderung am selben Eintrag ohne Neuladen gelingt; der Umbuchungs-Pfad im Lager (Besitzer-/Standortwechsel) wurde zur Absicherung ebenfalls angepasst. Die Missionsdetail-Abschnitte waren nicht betroffen, weil sie eigene In-Memory-Versionszähler nutzen.

- **Letzte In-Place-Regressionen aus dem #571-Umbau behoben (Regressionsprüfung der vier Kernbereiche Missionen/Lager/Aufträge/Raffinerie).** Auf der Materialsammlungsseite bleibt eine Zeile nach einem vollständigen Besitzer- oder Standortwechsel jetzt korrekt mit dem neuen Lagereintrag verknüpft, sodass ein direkt folgender Schritt auf derselben Zeile (etwa der Lieferstatus-Schalter) nicht mehr fehlschlägt; schlägt der Lieferstatus-Schalter selbst fehl, springt das Häkchen auf den gespeicherten Stand zurück. Auf der Auftrags-Detailseite zeigt das Reaktivieren eines abgeschlossenen oder abgelehnten Auftrags (zurück auf „Offen"/„In Bearbeitung") die dabei neu vergebene Priorität jetzt sofort in-place statt erst nach einem Neuladen.

- **Lager: Mehrere Änderungen direkt hintereinander an demselben Eintrag funktionieren wieder (Epic #571 / #577).** Wer einem Lager-Eintrag einen Einsatz zuwies und unmittelbar danach einen Auftrag verknüpfte — oder eine Notiz hinzufügte, nachdem zuvor ein Dropdown im selben Eintrag geändert wurde — erhielt einen Konfliktfehler („Der Eintrag wurde in der Zwischenzeit von einem anderen Nutzer geändert") und die Seite lud neu. Ursache war eine veraltete Versionsnummer in der Server-Antwort; der Eintrag wird jetzt vor dem Zurückgeben in die Datenbank geschrieben (saveAndFlush), sodass die zurückgegebene Version aktuell ist und Folgeaktionen ohne Neuladen gelingen. Betrifft die Zuordnung von Auftrag/Einsatz, Notizen und das teilweise Ausbuchen.

- **Missionsdetailseite: Die Teilnehmer-Zähler im Kopfbereich aktualisieren sich jetzt ohne Seitenneuladen (Epic #571 / #574).** Beim Anmelden, Bearbeiten, Entfernen oder Ein-/Auschecken eines Teilnehmers blieben die Teilnehmer- und Eingecheckt-Zähler in der Faktenleiste sowie die Zahl im Tab „Teilnehmer & Einheiten" bisher auf dem Stand vom Seitenaufruf, bis die Seite vollständig neu geladen wurde (die gespeicherten Daten waren korrekt, nur die Anzeige veraltet). Diese Zähler werden jetzt direkt zusammen mit der Crew-Ansicht aktualisiert.

- **Mitgliederverwaltung: Die In-Place-Suche und -Seitennavigation aktualisiert die Liste jetzt wirklich (#581).** Der Filter-Parameter war als `boolean` deklariert und konnte den vom AJAX-Helfer gesendeten Wert (`fragment=results`) nicht binden, sodass die gefilterte Anfrage scheiterte und die Liste sich beim Suchen/Blättern nicht aktualisierte; der Parameter ist jetzt ein `String` und bindet korrekt.

- **Lager: Erfolgs- und Fehlerhinweise beim Bearbeiten von Notizen und bei der Sammelauswahl werden wieder angezeigt.** Der seiteneigene Hinweis-Helfer zeigte auf ein nicht vorhandenes Element und blieb dadurch wirkungslos; er nutzt jetzt die gemeinsamen Toast-Funktionen. Betrifft das persönliche Lager und die Admin-Ansicht.

- **Raffinerieauftrag einlagern: Mitglieder mit mehreren Org-Einheiten können wieder einlagern (#596).** Gehörte der Empfänger eines eingelagerten Outputs mehr als einer Org-Einheit an (Staffel plus Spezialkommando), brach das Einlagern bisher mit „User belongs to multiple org units" (400) ab, weil der Einlager-Dialog keine Org-Einheit zur Auswahl bot. Der Dialog zeigt jetzt pro Position eine Org-Einheit-Auswahl — die Optionen richten sich nach dem gewählten Empfänger und sind mit der Org-Einheit des Auftrags vorbelegt, sodass das Einlagern auf die eigene Org-Einheit weiterhin ein Klick bleibt.

- **Persönliches Lager: Die Sammel-Ausbuchung funktioniert jetzt (#577).** Die Aktion rief bisher direkt eine Backend-Adresse auf, für die es im Frontend keine Route gab, sodass die markierten Einträge nie ausgebucht wurden; sie wird jetzt über das Frontend an das Backend weitergereicht und aktualisiert die gruppierte Tabelle in-place.

- **Operationsdetailseite: Zwei Speichervorgänge unmittelbar hintereinander führen nicht mehr zu einem Konflikt (#576).** Nach dem In-Place-Speichern wurde die neue Versionsnummer nicht zurück ins Formular geschrieben, sodass der zweite Speichervorgang mit einem Konflikt (409) abgewiesen wurde; jetzt wird die aktuelle Version sofort übernommen.

- **Missionsdetailseite: Das Speichern der Kerndaten (Verwaltung) löscht keine Zeiten mehr.** Treffen-, Geplant-Start- und Geplant-Ende-Zeit, die beim Bearbeiten nicht erneut angefasst wurden, wurden beim Speichern still auf leer gesetzt (eine Zeitangabe mit Sekundenbruchteilen ließ sich nicht zurücklesen); sie bleiben jetzt erhalten.

- **In-Place-Listen und -Bereiche: Bricht eine Aktualisierung ab (abgelaufene Sitzung, kurzzeitig nicht erreichbares Backend), wird nicht mehr eine ganze Seite — etwa die Login- oder die Missionsliste — in den kleinen Ergebnisbereich gerendert.** Der bisherige Inhalt bleibt erhalten und es erscheint ein KRT-Hinweis; die Missionsdetailseite zeigt stattdessen einen passenden Inline-Fehler im betroffenen Bereich.

- **Admin › Spezialkommandos: Der Löschen-Button (Papierkorb) in der Listenansicht funktioniert wieder.** Er war bisher folgenlos (kein Formular, keine Verdrahtung); jetzt öffnet er einen KRT-Bestätigungsdialog und deaktiviert das Spezialkommando anschließend (reversibel über „Inaktive anzeigen" → „Aktivieren").

- **Bank-Verwaltung: Das Auswahlfeld „Org-Einheit" im Dialog „Konto anlegen" zeigt wieder die Namen der Org-Einheiten statt „null".** Die Seite las die Org-Einheiten-Liste in das falsche DTO ein; dadurch fehlten Name und Id jeder Option (die Auswahl hätte zudem keine Org-Einheit übermittelt).

- **Bank-Berechtigungen: Das Konten-Auswahlfeld in der Filterleiste ist jetzt genauso hoch wie der Tab-Kasten daneben** und schließt oben und unten bündig mit ihm ab (vorher saß das Dropdown rund 12 px zu hoch).

## [v0.4.21](https://github.com/krt-profit/basetool/releases/tag/v0.4.21) - 2026-06-13

### Added

- **Kartellbank (Epic #556):** Neuer Bank-Bereich für die Organisation. Konten je Org-Einheit/Bereich plus Kartell- und Kartellbank-Sonderkonten, mit Halter-Verteilung statt Einzelkonten — jede Buchung ist einem Halter zugeordnet, der Kontosaldo ist die Summe aller Halter-Bestände. Bank-Mitarbeiter sehen und bebuchen nur freigegebene Konten (Einzahlen/Auszahlen/Transfer je Konto einzeln berechtigt), die Bankleitung verwaltet Konten, Halter und Berechtigungen. Ein- und Auszahlungen, Konto-zu-Konto-Transfers und Halter-Umbuchungen laufen über ein fälschungssicheres, nur-anhängendes Hauptbuch; Korrekturen erfolgen als Storno-Gegenbuchung (Bankleitung). Der Bank-Zugang ist unabhängig von der Staffel-/Org-Zugehörigkeit. Kontoauszug (Zeitraum frei wählbar) und 3-Monats-Report als PDF im KRT-Design. Ein Wipe-Reset und ein Storno selbst sind nicht stornierbar — korrigiert wird stets die Ursprungsbuchung.

- **Profil: Anzeigename und Beschreibung werden jetzt ohne Seitenneuladen gespeichert** (In-Place mit KRT-Hinweis statt Seitenwechsel). Erster Baustein des seitenweiten Umbaus auf In-Place-Aktualisierung (Epic #571); ohne JavaScript speichert das Formular unverändert weiter.

### Changed

- **Alle PDF-Exporte (Übergabeprotokoll, Kontoauszug, 3-Monats-Report) folgen jetzt strenger dem KRT-Design-System.** Orange dient nur noch als Akzent für Überschriften und Identität (Titel, Abschnitte, eine Linie unter den Tabellenköpfen, die Diagrammlinie, ein dünner oberer Seitenbalken, das Logo). Tabellenköpfe und Zeilengitter sind jetzt neutral dunkelgrau statt vollflächig orange, damit das Orange nicht überhandnimmt; Inhalt und Daten der Reporte bleiben unverändert.

- **Die Fußzeile aller PDF-Exporte nennt jetzt den Erstellungszeitpunkt in UTC** – „Generiert von Profit Basetool am TT.MM.JJJJ HH:MM UTC" – als eindeutiger, zeitzonenunabhängiger Audit-Stempel.

- **Raffinerieauftrag: In den Materialzeilen steht die Qualität jetzt vor der Eingangsmenge (Units).** Die Feldreihenfolge im Anlege- und Detailformular folgt damit der Reihenfolge im Spiel; die Ausgangszeile (Units, SCU) bleibt unverändert.

- **Listenfilter und Seitennavigation (Einsätze, Operationen) laufen jetzt über eine gemeinsame AJAX-Grundlage und aktualisieren nur den Ergebnisbereich.** Das Blättern in den gefilterten Ergebnissen lädt nicht mehr die ganze Seite neu.

### Removed

- **Die ungenutzte Alpine.js-Bibliothek wurde entfernt.** Sie wurde nur noch auf der Missions-Detailseite geladen, aber von keiner Stelle mehr verwendet; die Seite lädt dadurch ein Skript weniger. Keine sichtbare Funktionsänderung.

- **Die JS-Bibliotheken marked und DOMPurify wurden entfernt.** Die Dashboard-Ankündigung wird jetzt – wie schon die Missionsbeschreibung – serverseitig als Markdown gerendert (CommonMark mit escaptem Roh-HTML und entfernten unsicheren Links) statt im Browser. Das Dashboard lädt damit zwei Skripte weniger; keine sichtbare Funktionsänderung.

### Fixed

- **AJAX-Aktionen scheitern nicht mehr mit einem 403, wenn der Browser-Tab lange offen war oder man sich zwischendurch neu angemeldet hat.** Das CSRF-Token wird in diesem Fall automatisch erneuert und die Aktion einmal wiederholt.

## [v0.4.20](https://github.com/krt-profit/basetool/releases/tag/v0.4.20) - 2026-06-12

### Changed

- **Der Blueprint-Import kennt das neue Extractor-Feld `additionalSourceFolders`.** Der Basetool Blueprint Extractor listet darin zusätzlich gescannte Channel-Ordner (z. B. HOTFIX neben LIVE); der Import akzeptiert Exporte mit und ohne das Feld unverändert — es wird nur mitgelesen, nicht ausgewertet.

### Fixed

- **Auftragsdetails: Die Überschrift „Bearbeiter" hat wieder den normalen Abstand zur Materialtabelle darüber.** Der AJAX-Fragment-Wrapper der Bearbeiter-Sektion hatte den Standard-Abstand der Sektionsüberschrift verschluckt.

## [v0.4.19](https://github.com/krt-profit/basetool/releases/tag/v0.4.19) - 2026-06-12

### Changed

- **Die Staffelübersicht im Hangar ist jetzt echt paginiert:** Die Tabelle blättert serverseitig über alle Schiffstypen (vorher wurden maximal 1000 Einträge auf einmal geladen), mit wählbarer Seitengröße 10/50/100 im KRT-Stil. Die Schiffstyp-Suche filtert jetzt serverseitig über die gesamte Flotte statt nur über die sichtbaren Zeilen.

- **Blueprint-Verfügbarkeit: Die Tabelle ist jetzt echt seitenweise (10/50/100 Einträge pro Seite wählbar) statt einer einmaligen 1000er-Ladung.** Die Seitennavigation und der Seitengrößen-Wähler folgen dem KRT-Design-System; die Suche läuft jetzt serverseitig über alle Einträge (nicht nur die sichtbare Seite) und bleibt beim Blättern und Umschalten der Seitengröße erhalten.

- **Die Screenshot-Import-Warnung `SUM_MISMATCH` nennt jetzt beide möglichen Ursachen:** Neben einer falsch gelesenen Menge/einem falsch gelesenen Toggle kann auch ein nach GET QUOTE geänderter Auftrag die eingefrorene TO-REFINE-Kopfzeile übersteigen (das Spiel friert die Kopfzeilen beim Quote ein). Meldungstexte (DE/EN) mit Handlungsempfehlung umformuliert; Prüfung und Schwere unverändert.

### Fixed

- **Die Staffelübersicht im Hangar klappt Detailzeilen jetzt ohne spürbares Ruckeln auf:** Die Detailzeile wird per Klassen-Umschaltung im JavaScript statt über einen `:has()`-Geschwister-Selektor eingeblendet, der bei großen Tabellen eine Neuberechnung der gesamten Tabelle bei jedem Auf-/Zuklappen erzwang.

- **Die Seitennavigation unter Listen (Mitglieder, Einsätze, Operationen, Lager) funktioniert wieder, sobald es mehr als eine Seite gibt.** Das gemeinsame Pagination-Fragment rief seit dem Design-System-Umbau nicht existierende Accessoren auf und ließ jede mehrseitige Liste mit einem Serverfehler abbrechen.

- **Blueprint-Verfügbarkeit: Das Aufklappen der Detailansicht mit Nutzerliste ruckelt nicht mehr.** Das Ein-/Ausklappen schaltet jetzt gezielt nur die zugehörige Detailzeile um (vorher erzwang eine `:has()`-CSS-Regel bei jedem Klick eine Style-Neuberechnung über die gesamte Tabelle), und der Nutzer-Abruf im Admin-Modus fragt direkt per Produktschlüssel ab statt vorher alle Blueprint-Besitzer zu scannen.

## [v0.4.18](https://github.com/krt-profit/basetool/releases/tag/v0.4.18) - 2026-06-11

### Changed

- **Der Screenshot-Import befüllt jetzt auch das Startdatum des Raffinerieauftrags vor.** Der Basetool SC Extractor liefert pro Screenshot die Aufnahmezeit mit (aus dem Dateinamen, sonst aus dem Datei-Änderungsdatum); der Import übernimmt die Aufnahmezeit des letzten Screenshots als Startzeit. Ohne Zeitangabe (ältere Extractor-Version) bleibt das Feld wie bisher leer und wird beim Speichern auf „jetzt" gesetzt.

## [v0.4.17](https://github.com/krt-profit/basetool/releases/tag/v0.4.17) - 2026-06-11

### Fixed

- **Feinschliff der Einsatzseite nach Owner-Review:** Wirtschaft-Sprungkarte aus der Übersicht entfernt (Inhalt über die Tabs erreichbar), Hinweistexte auf dem Crew-Board besser lesbar (heller/kräftiger), die Auszahlungsart-Dropdowns werden bei halber 4K-Fensterbreite nicht mehr abgeschnitten, Raffinerieaufträge/Lagereinträge im Finanzen-Tab haben jetzt einen Panel-Hintergrund und die „Jetzt"-Buttons der Verwaltung brechen bei wenig Platz um statt abgeschnitten zu werden.

## [v0.4.16](https://github.com/krt-profit/basetool/releases/tag/v0.4.16) - 2026-06-11

### Changed

- **Die Einsatz-Detailseite hat jetzt ein Tab-Layout (Übersicht · Teilnehmer & Einheiten · Finanzen & Auszahlung · Verwaltung) mit klebrigem Kopf, Fakten-Leiste und Deeplink über `?tab=`.** Alle bisherigen Funktionen, Sichtbarkeiten und Berechtigungen bleiben erhalten; der Verwaltungs-Tab erscheint nur für Berechtigte.

- **Teilnehmer werden Einheiten jetzt direkt auf einem Crew-Board zugewiesen (Drag & Drop, Klick-Auswahl oder Tastatur) statt über das bisherige „Crew zuweisen"-Modal.** Die Bordfunktion ist pro Person über ein Chip-Dropdown schnell änderbar; Mehrfach-Funktionen weiterhin über das Crew-Modal.

- **Alle Modals der Einsatzseite nutzen den neuen KRT-Modal-Rahmen** (Fokus-Falle, Esc, eine gefüllte CTA); Lösch-Bestätigungen benennen jetzt die Konsequenz. Im Einheiten-Modal sind Schiffstyp und Hangar-Schiff klar getrennt wählbar („— keines · nur Typ verwenden —").

- **Die Anmeldung zum Einsatz bietet jetzt direkt eine Auszahlungsart-Wahl** (Standard = Profil-Voreinstellung); Backend-Endpoint und Migration-frei erweitert.

- **Einheiten haben jetzt einen optionalen Anzeigenamen** (wird sonst aus Schiff/Schiffstyp abgeleitet), **einen wählbaren Verantwortlichen** (sonst automatisch der Schiffseigner) **und eine Notiz** (Migration V149, Einheiten-Modal und Crew-Board zeigen beides an).

- **Der Finanzen-Tab zeigt eine Summen-Leiste (Gesamtsumme, Einnahmen, Ausgaben, je Anteil)**; Raffinerie-Ausgaben erscheinen als automatische Zeile mit Link zum Auftrag.

- **Die Einsatz-Beschreibung unterstützt jetzt Markdown** (fett, Listen, Links u. a.) und wird auf der Übersicht sowie im „Nächste Mission"-Banner formatiert dargestellt; eingebettetes HTML wird aus Sicherheitsgründen als Text angezeigt.

- **„Meine Blueprints" ist jetzt eine Master-Detail-Ansicht:** Sammlung links (Sofort-Filter, Pfeiltasten, `?bp=`-Deeplink), rechts permanent Zutaten und Stat-Beiträge mit Qualitäts-Slidern je Zutat (Live-Faktoren, Berechnung unverändert). Hinzufügen/JSON-Import wandern als kompakte Leiste in den Kopf; alle bisherigen Funktionen bleiben erhalten.

- **Die Items-Seite des persönlichen Inventars nutzt jetzt dieselbe Tab-Leiste wie „Meine Blueprints"** (mit Anzahl-Badge) statt der bisherigen Button-Tabs; Anlegen/Bearbeiten- und Lösch-Modal verwenden den KRT-Modal-Rahmen und eine leere Liste erscheint als gestalteter Leerzustand.

- **KRT-Modals zeigen keine dauerhaften Scrollbalken mehr:** Es scrollt nur noch der Modal-Inhalt (Kopf- und Fußleiste bleiben stehen) und nur, wenn der Bildschirm zu klein ist. Formularlastige Modals (Einsatz-Anmeldung/-Bearbeitung, Einheiten, Finanzeinträge, Inventar-Eintrag, Blueprint-Notiz) sind zudem auf 600 px verbreitert; Bestätigungen und Einfeld-Abfragen bleiben kompakt.

## [v0.4.15](https://github.com/krt-profit/basetool/releases/tag/v0.4.15) - 2026-06-11

### Fixed

- **Beim Screenshot-Import auf der Raffinerieauftrag-Seite erscheint keine „Website verlassen?"-Warnung mehr.** Die Dateiauswahl markierte das Formular fälschlich als ungespeichert geändert; der Import-Upload ist jetzt von dieser Prüfung ausgenommen. Vor dem Import eingegebene Formularwerte werden beim Import bewusst verworfen und vollständig durch die Daten aus der JSON ersetzt.

## [v0.4.14](https://github.com/krt-profit/basetool/releases/tag/v0.4.14) - 2026-06-11

### Changed

- **Spring Boot wurde auf 4.1.0 aktualisiert.** Die drei temporären Versions-Overrides für Tomcat (11.0.22), Netty (4.2.14) und den PostgreSQL-Treiber (42.7.11) entfallen, da Spring Boot 4.1.0 diese (oder neuere) gepatchten Versionen jetzt selbst ausliefert.

- **Der Screenshot-Import ordnet vom Spiel abgeschnittene Materialnamen (z. B. „UCTION SALVAGE") jetzt auch über Material-Aliase zu.** Die Teilstring-Stufe prüft das gelesene Fragment zusätzlich gegen die unter „Admin → Material-Aliase" gepflegten `REFINERY_SCREEN`-Aliase; ein Alias mit der vollen Bildschirm-Schreibweise deckt damit alle Abschneide-Varianten (vorne/hinten/beidseitig) ab.

### Fixed

- **Der Summen-Check des Raffinerie-Screenshot-Imports meldet keine falschen Warnungen mehr.** Er prüft jetzt die in Phase 0 verifizierte Regel: nur noch einseitig gegen die TO-REFINE-Kopfzeile (mit Rundungstoleranz von ±1 pro Zeile); IN MANIFEST wird nicht mehr geprüft, da das Spiel diese Summe nicht zuverlässig nachvollziehbar bildet (z. B. ohne inerte Materialien).

## [v0.4.13](https://github.com/krt-profit/basetool/releases/tag/v0.4.13) - 2026-06-10

### Changed

- **Die Oberfläche nutzt jetzt durchgängig nur noch die Schriftart Lato.** Audiowide entfällt als separate Headline-Schrift (die Schriftdatei wurde entfernt); Überschriften bleiben in Großbuchstaben, werden aber jetzt als Lato Bold dargestellt.

## [v0.4.12](https://github.com/krt-profit/basetool/releases/tag/v0.4.12) - 2026-06-10

## [v0.4.11](https://github.com/krt-profit/basetool/releases/tag/v0.4.11) - 2026-06-10

### Added

- **Screenshot-Import auf der Raffinerieauftrag-Seite (Frontend, Phase 2 von Epic #439).** Auf „Neuer Raffinerieauftrag“ lässt sich jetzt die Extract-JSON-Datei des Desktop-Extractors hochladen: Das Formular wird serverseitig vorbefüllt, ein Banner fasst die Zuordnung zusammen, markierte Zeilen zeigen Konfidenz und Prüfhinweise, und nicht zugeordnete Materialien lassen sich per Vorschlags-Chip mit einem Klick zuweisen. Gespeichert wird unverändert erst über das geprüfte Formular.

- **Die Detailansicht eines Item-Auftrags zeigt jetzt unter „Blaupausen-Verfügbarkeit" (direkt nach den Bearbeitern), welche Mitglieder der bearbeitenden Staffel/SK die Blaupausen für die geforderten Items besitzen** — je Person mit der Liste der vorhandenen Blaupausen und einer Abdeckungsübersicht je Item (Items, die niemand bauen kann, werden markiert). Diese Information ist nur für Mitglieder der bearbeitenden Staffel/SK (und Admins) sichtbar.

- **Raffinerieaufträge können jetzt aus einem Screenshot-Extract vorbefüllt werden (Backend, Phase 1 von Epic #439).** Der neue Endpoint `POST /api/v1/refinery-orders/import-extract` nimmt das vom Desktop-Extractor erzeugte `RefineryExtract`-JSON entgegen, gleicht Materialien, Standort und Methode gegen die Stammdaten ab und liefert einen nicht gespeicherten Entwurf samt Prüfhinweisen (übersprungene/un-quotierte Zeilen, Summen-Check, Zuordnungsvorschläge) zurück. Migration V148 erweitert die Material-Alias-Tabelle um die Quelle `REFINERY_SCREEN`, die Admins unter „Material-Aliase“ pflegen können.

### Fixed

- **In der Materialsammelübersicht eines Auftrags lösen das Setzen oder Entfernen des „Geliefert"-Häkchens sowie der Besitzer- und Standortwechsel keine Fehlermeldung mehr aus.** Diese Aktionen schickten kein CSRF-Token mit und wurden serverseitig mit HTTP 403 abgewiesen; das Token wird jetzt wie auf den übrigen Seiten mitgeschickt.

- **Backend-Fehlermeldungen erscheinen jetzt in der Sprache des Benutzers.** Das Frontend reicht die gewählte UI-Sprache als `Accept-Language` an das Backend weiter; zuvor wurden vom Backend lokalisierte Hinweise (z. B. beim Screenshot-Import) immer in der Server-Standardsprache angezeigt.

## [v0.4.10](https://github.com/krt-profit/basetool/releases/tag/v0.4.10) - 2026-06-10

## [v0.4.9](https://github.com/krt-profit/basetool/releases/tag/v0.4.9) - 2026-06-10

### Added

- **Bearbeiter eines Auftrags können jetzt eine Notiz zu ihrem Eintrag hinterlegen** — z. B. wann sie daran arbeiten oder welchen Teil sie übernehmen. Über ein Stift-Symbol neben dem Namen öffnet sich ein Dialog zur Eingabe (max. 500 Zeichen); ist eine Notiz vorhanden, lässt sie sich über ein Lösch-Symbol wieder entfernen. Notizen sind für alle sichtbar, die den Auftrag sehen; bearbeiten darf sie nur der Bearbeiter selbst oder ein Logistiker (und höher). Ein- und Austragen sowie das Speichern der Notiz laufen jetzt ohne Neuladen der Seite.

### Fixed

- **Material-Aliasse (Admin → Material-Aliasse) sind jetzt unabhängig von Groß-/Kleinschreibung eindeutig.** Bisher konnten zwei Aliasse, die sich nur in der Schreibweise unterschieden (z. B. „STILERON (ORE)" und „Stileron (Ore)"), nebeneinander existieren und ließen den SC-Wiki-Abgleich mit einem Fehler (HTTP 500) abbrechen. Eine Migration entfernt vorhandene Duplikate (die älteste Zeile bleibt erhalten); das Anlegen eines solchen Duplikats meldet jetzt einen sauberen Konflikt (HTTP 409).

## [v0.4.8](https://github.com/krt-profit/basetool/releases/tag/v0.4.8) - 2026-06-10

### Changed

- **Die Startseite zeigt unter „Nächster Einsatz" nur noch Einsätze mit dem Status „Geplant" oder „Aktiv".** Abgeschlossene oder abgebrochene Einsätze mit einem in der Zukunft liegenden Startzeitpunkt tauchen dort nicht mehr auf.

## [v0.4.7](https://github.com/krt-profit/basetool/releases/tag/v0.4.7) - 2026-06-09

### Fixed

- **Das Anlegen eines Finanzeintrags (Einnahme/Ausgabe) bei einem Einsatz und das anschließende Öffnen der Einsatz-Detailseite führen nicht mehr zu einem Fehler (HTTP 500).** Sobald ein Einsatz mindestens einen Finanzeintrag besaß, ließ sich die Detailseite nicht mehr laden; die Finanztabelle samt Bearbeiten-Schaltfläche wird jetzt wieder korrekt angezeigt.

## [v0.4.6](https://github.com/krt-profit/basetool/releases/tag/v0.4.6) - 2026-06-09

### Changed

- **Die Auftragsliste unter „Raffinerie" blendet die Spalte „Status" aus, damit „Ort", „Einsatz" und „Materialien" mehr Platz bekommen.** Nach Status filtern lässt sich weiterhin über die Filterleiste oberhalb der Tabelle.

- **Das Organigramm ist jetzt komplett per Tastatur bedienbar und für Screenreader als Baum ausgezeichnet (Rollen, Ebenen, Beschriftungen).** Pfeiltasten bewegen den Fokus zwischen Knoten und Ebenen (sichtbar als scharfer Rahmen), der Bearbeiten-Dialog schließt mit Esc, hält den Fokus fest und gibt ihn beim Schließen an den auslösenden Button zurück; im Bearbeiten-Modus erklärt eine kurze Hinweiszeile die Symbole.

### Fixed

- **Mitglieder der Bereichsleitung (ohne Staffel-Zugehörigkeit) können jetzt Operationen anlegen.** Bisher schlug das Anlegen mit einer Fehlermeldung fehl, weil jede Operation zwingend einer Staffel gehören musste. Solche organisationsweiten „Bereichsleitungs-Operationen" sind staffellos und für alle Mitglieder (und höher) sichtbar, für Gäste hingegen nicht.

- **Operationen sind jetzt zusätzlich für alle Teilnehmenden der zugeordneten Einsätze sichtbar (außer nicht angemeldete Besucher).** Wer an einem Einsatz einer Operation teilgenommen hat, sieht die Operation und seine Auszahlung jetzt auch dann, wenn sie einer anderen Staffel gehört oder staffellos ist.

- **Im Organigramm lässt sich ein gesetzter Kommandoleiter jetzt entfernen, ohne die ganze Kommandogruppe zu löschen.** Wird der Posten vakant, bleibt die Kommandogruppe mit Namen, Stv. Kommandoleiter und Ensigns bestehen; ein neuer Kommandoleiter kann später zugewiesen werden.

- **Im Organigramm bleibt nach dem Speichern die horizontale Scroll-Position erhalten — auch bei breiten Diagrammen mit vielen Staffeln —** und springt nicht mehr an den linken Rand zurück. Die Wiederherstellung wartet jetzt, bis das Diagramm fertig aufgebaut ist, statt zu früh auf den linken Rand zu klemmen.

## [v0.4.5](https://github.com/krt-profit/basetool/releases/tag/v0.4.5) - 2026-06-08

### Added

- **Die Oberfläche respektiert jetzt die System-Einstellung „Bewegung reduzieren":** Animationen wie das Einfahren der Seitenleiste oder das Einblenden von Hinweis-Toasts werden dann praktisch abgeschaltet.

### Changed

- **Status-, Fehler- und Preistexte nutzen jetzt hellere, kontrastreichere Farbtöne und sind auf dem dunklen Hintergrund deutlich besser lesbar.** Die kräftigen Status- und Gefahrfarben bleiben Flächen, Rahmen und Bereichs-Tags vorbehalten; ein ungültiges Eingabefeld bekommt zusätzlich einen roten Rahmen neben der Fehlermeldung.

- **Die Seitennavigation beim Blättern durch Listen (Einsätze, Operationen, Mitglieder, Lager) nutzt jetzt einheitliche quadratische Schaltflächen mit Pfeil-Symbolen statt der «‹›»-Zeichen.** Die Schaltflächen am Anfang/Ende sind, wenn man schon auf der ersten bzw. letzten Seite ist, jetzt wirklich deaktiviert (nicht nur ausgegraut) und werden für Screenreader korrekt ausgezeichnet.

- **Fehlermeldungen auf den Keycloak-Seiten (Anmeldung, Passwort, Konto) erscheinen jetzt im selben helleren, besser lesbaren Rotton wie in der App.** Rote Feldrahmen und der rote Schimmer ungültiger Felder bleiben im kräftigen Rot — so wirkt der Auftritt über App und SSO hinweg einheitlich und der Fehlertext ist auf Schwarz gut lesbar.

- **In der Einsatz-Detailansicht werden Einheiten-Namen jetzt in hellem Weiß statt Orange dargestellt.** Orange bleibt konsequent Aktionen und Identität vorbehalten, reine Datenwerte erscheinen neutral-hell.

## [v0.4.4](https://github.com/krt-profit/basetool/releases/tag/v0.4.4) - 2026-06-08

### Changed

- **Aktions-Schaltflächen (Löschen, Bearbeiten, Speichern) tragen jetzt app-weit einheitliche Symbole.** In dichten Tabellen- und Listenzeilen erscheinen sie als reine Symbol-Schaltflächen (Bedeutung per Tooltip/Vorlesehilfe), in Formularen und Dialogen als Symbol mit Text. Das spart Platz und macht gleichartige Aktionen auf einen Blick erkennbar: Löschen nutzt einen Mülleimer, Bearbeiten einen Stift, Speichern eine Diskette.

### Fixed

- **In „Mein Lager" und „Globales Lager" überlagerten sich in der aufgeklappten Eintragszeile die Mengenangabe und der Ausbuchen-Button, wenn das Fenster schmaler war (z. B. die linke Hälfte eines 4K-Monitors).** Die Aktions-Schaltflächen je Eintrag sind jetzt kompakte Symbole — „Ausbuchen" ein Heraus-Pfeil, „Notiz" ein Stift (Bedeutung per Tooltip/Vorlesehilfe) — und ab Tablet-Breite (≤ 1024px) rutschen Menge und Schaltflächen unter die Auftrag/Einsatz-Felder, sodass sich nichts mehr überdeckt.

## [v0.4.3](https://github.com/krt-profit/basetool/releases/tag/v0.4.3) - 2026-06-07

### Changed

- **„Mein Lager" und „Globales Lager" zeigen die verschachtelte Material → Nutzer/Stack → Eintrag-Liste jetzt als eine zusammenhängende Baum-Tabelle statt dreier gestapelter Tabellen mit je eigener Kopfzeile.** Ein einziger, mitlaufender Spaltenkopf, klare Einrückung pro Ebene, rechtsbündige Zahlen mit Qualitäts-Balken (0–1000), Auftrag/Einsatz als breite beschriftete Felder und eine Notiz, die nur erscheint, wenn eine hinterlegt ist, machen die Bestände dichter und leichter vergleichbar. Funktion (Lazy-Load, Filter, Aus-/Umbuchung, Massenausbuchung, Notizen) bleibt unverändert.

### Fixed

- **Im Admin-Terminal wurde bei unbekanntem UEX-Wert in der deutschen Oberfläche „2013" statt des Gedankenstrichs „–" angezeigt; das ist korrigiert.**

## [v0.4.2](https://github.com/krt-profit/basetool/releases/tag/v0.4.2) - 2026-06-07

### Changed

- **In „Mein Lager" und „Globales Lager" zeigt die aufgeklappte Eintragsliste die Spalte „Hinzugefügt" nicht mehr; stattdessen stehen die Schaltflächen (Ausbuchen, Notiz) und die Notizvorschau nebeneinander, sodass die Eintragszeilen flacher und übersichtlicher sind.**

## [v0.4.1](https://github.com/krt-profit/basetool/releases/tag/v0.4.1) - 2026-06-07

### Fixed

- **„Globales Lager" und „Mein Lager" zeigen wieder alle Bestände an, nicht nur die mit Auftrags- oder Einsatzbezug.** Mit der neuen gruppierten Lageransicht (v0.4.0) fielen Einträge ohne Auftrag, Einsatz bzw. Orgeinheit aus der Stapelübersicht heraus, sodass die Seite trotz vorhandener Bestände „Keine Einträge gefunden" meldete; jetzt erscheinen alle Stapel.

## [v0.4.0](https://github.com/krt-profit/basetool/releases/tag/v0.4.0) - 2026-06-07

### Added

- **Auf den Handelsseiten „Materialien" und „Material-Übersicht" lässt sich die Gruppierung nach den von Admins angelegten Kategorien per Schalter „Nach Kategorie gruppieren" abschalten.** Ohne Gruppierung erscheinen alle Materialien in der normalen alphabetischen Sortierung der Seite; die Auswahl wird pro Browser gespeichert und lässt sich jederzeit wieder umschalten.

- **Im Profil lässt sich jetzt eine persönliche Standard-Auszahlungsart (Auszahlen oder Spenden) festlegen, die bei jeder Einsatz-Anmeldung vorausgewählt wird.** Wer immer spendet (oder immer auszahlt), muss das nicht mehr in jedem Einsatz einzeln umstellen; die Auswahl pro Einsatz bleibt änderbar und bestehende Teilnahmen ändern sich nicht (Migration V142).

- **Jedes Mengenfeld für SCU-Materialien hat jetzt ein „?"-Symbol, das per Maus-Hover oder Tastaturfokus einen Hinweis einblendet:** „Eingabe in SCU. cSCU ist 0,01 SCU. µSCU ist 0,001 SCU." So ist klar, dass Bruchteile erlaubt sind. Greift in Lager-Einbuchung, Aus-/Umbuchung, Material- und Item-Auftrag, Material-Eintragung, Übergabe und Raffinerie-Einlagerung; bei Stück-Materialien bleibt der Hinweis aus.

### Changed

- **SCU-Mengen lassen sich jetzt wahlweise mit Punkt oder Komma als Dezimaltrennzeichen eingeben (z. B. `0,01` oder `0.01`); die Eingabe wird intern auf das Punktformat normalisiert.** Bisher akzeptierte das Mengenfeld je nach Browser-Sprache nur eines der beiden Zeichen. Gilt für alle SCU-Mengenfelder: Lager-Ein-/Aus-/Umbuchung, Material-Auftrag (Anlegen/Bearbeiten), Material-Eintragung, Übergabe und Raffinerie-Einlagerung.

- **Mengenfelder erzwingen jetzt die Eingaberegeln des Mengentyps:** SCU-Materialien akzeptieren nur positive Werte (> 0) mit bis zu drei Nachkommastellen (Schrittweite 0,001); werden mehr Nachkommastellen eingegeben, wird kaufmännisch auf drei gerundet. Stück-Materialien akzeptieren nur positive Ganzzahlen. Der Zielbestand beim Ausbuchen darf weiterhin 0 sein.

- **Diese Mengenregeln gelten jetzt auch serverseitig, nicht nur im Browser:** Material-Mengen müssen positiv (> 0) sein, und SCU-Mengen werden beim Speichern kaufmännisch auf drei Nachkommastellen gerundet (statt eine zu genaue Eingabe abzulehnen) — über alle Schreibwege inklusive Auftrag anlegen/bearbeiten, das bisher eine Menge von 0 und ungerundete Werte annahm.

- **Finanzeinträge einer Mission werden jetzt nur noch als ganze aUEC-Beträge erfasst:** Das Betragsfeld akzeptiert nur Ganzzahlen (Schrittweite 1), gebrochene Eingaben werden auch serverseitig abgewiesen, und der Bearbeiten-Dialog übernimmt den gerundeten Wert. Angezeigt wurden Finanzbeträge ohnehin schon kaufmännisch auf ganze aUEC gerundet; intern bleibt für abgeleitete Summen (etwa den Raffinerie-Gewinn) die volle Genauigkeit erhalten.

- **Nicht angemeldete Besucher und eingeloggte Gäste ohne Rolle sehen bei nicht-internen Einsätzen die öffentlichen Details — ohne Beschreibung und ohne personenbezogene Daten:** Organisation, Teilnehmerliste (nur öffentliches Callsign, keine E-Mail/Realnamen), Einheiten, Frequenzen und Auszahlungsart sind sichtbar; nur die Freitext-Beschreibung bleibt verborgen. Anmelden und das Pflegen des eigenen Gast-Teilnehmers (inkl. Auszahlungsart) bleiben möglich, und ein eingeloggter Gast wird bei Einsätzen genauso behandelt wie ein anonymer Besucher.

- **Das Finanz-Ledger eines Einsatzes (Einnahmen/Ausgaben in aUEC) lässt sich nicht mehr anonym lesen oder anlegen:** Es ist jetzt Mitgliedern (und höher) vorbehalten — anonyme und rollenlose Gast-Zugriffe werden abgewiesen. Die Auszahlungsart je Teilnehmer bleibt dagegen öffentlich sichtbar; Aufträge anlegen bleibt für alle möglich.

- **Das Lager fasst gleiche Materialeinträge nicht mehr automatisch zu einem Eintrag zusammen:** Jede Einbuchung, Umlagerung und Raffinerie-Einlagerung bleibt ein eigener Eintrag. In „Mein Lager" und „Globales Lager" werden Einträge mit gleichem Material, Ort, Qualität, Auftrags-/Einsatzbezug und Besitzer weiterhin als eine Zeile angezeigt, die sich aufklappen lässt, um die einzelnen Einträge zu sehen und einzeln zu bearbeiten. Die Einträge eines Stapels werden dabei erst beim Aufklappen nachgeladen und bei vielen Einträgen seitenweise angezeigt, damit auch große Lager schnell laden.

### Fixed

- **Die Mengenfelder beim Auftrag anlegen, Auftrag bearbeiten und bei der Materialübergabe richten sich jetzt nach dem gewählten Material:** Stück-Materialien erlauben nur noch ganze Zahlen (Schrittweite 1, Einheit „Stück"), SCU-Materialien Nachkommastellen samt SCU-Hinweis. Bisher waren diese Felder immer auf SCU/Dezimal eingestellt, sodass eine Nachkomma-Eingabe für Stück-Materialien erst der Server ablehnte.

- **Der Bauplan-Import erkennt wieder Baupläne, deren englischer Name in den Spieldaten (CIG) falsch hinterlegt ist:** Bekannte Falschbezeichnungen werden beim SC-Wiki-Abgleich auf den echten Spielnamen korrigiert (z. B. „Antium Arms Maroon" statt „Antium Helmet Jet"), sodass diese Baupläne beim Import wieder zugeordnet werden. Die Korrektur greift nur, solange der falsche Name vorliegt, und wird automatisch hinfällig, sobald CIG ihn behebt (Meldung im Sync-Bericht).

- **Mitglieder der Bereichsleitung (ohne eigene Staffel/SK) können wieder Einsätze anlegen:** Wer keiner Orgeinheit angehört, aber org-weit planen darf, bekam beim Anlegen bisher einen Fehler. Solche Einsätze werden jetzt ohne Orgeinheit gespeichert und sind standardmäßig für alle sichtbar; als „intern" markiert sehen sie nur angemeldete Mitglieder (Migration V144).

## [v0.3.63](https://github.com/krt-profit/basetool/releases/tag/v0.3.63) - 2026-06-06

## [v0.3.62](https://github.com/krt-profit/basetool/releases/tag/v0.3.62) - 2026-06-06

### Added

- **Bei Item-Aufträgen bietet die Tabelle der aggregierten Materialien jetzt dieselben Funktionen wie bei Material-Aufträgen:** Ein Klick auf eine Materialzeile klappt die verknüpften Lagereinträge auf (mit Besitzer, Staffel, Standort, Qualität, Menge und – für Logistiker – dem Trennen-Button), und ein neuer Button „Materialsammelübersicht" öffnet die auftragsweite Lagerübersicht.

### Fixed

- **Im Item-Auftrag sind wieder alle bestellbaren Items im Auswahlfeld auffindbar — auch alphabetisch späte wie „P8-SC SMG" oder „P6-LR Sniper Rifle".** Das durchsuchbare Item-Dropdown lud bisher nur die ersten 1000 Items (alphabetisch) und filterte allein im Browser; sobald der Katalog darüber hinauswuchs, fehlten alle späteren Items in Auswahl und Suche, und beim Bearbeiten bestehender Aufträge blieb deren Item-Feld leer. Das Feld durchsucht jetzt live den gesamten Katalog direkt im Backend.

## [v0.3.61](https://github.com/krt-profit/basetool/releases/tag/v0.3.61) - 2026-06-06

### Changed

- **Der P4K-Katalog-Import läuft jetzt als Hintergrund-Job:** Der Upload kehrt sofort zurück und erscheint als Eintrag in einer Import-Liste, die den Status (in Warteschlange / läuft / fertig / fehlgeschlagen) anzeigt; eine fertige Vorschau lässt sich ansehen und dann anwenden, ohne die Datei erneut hochzuladen. Damit hängt die Seite nicht mehr und große Kataloge laufen nicht mehr in einen Timeout (Migration V141).

## [v0.3.60](https://github.com/krt-profit/basetool/releases/tag/v0.3.60) - 2026-06-05

### Added

- **Neuer Admin-Import „KRT P4K Reader" (Administration → Stammdaten): Administratoren laden den aus den echten Spieldaten (Game2.dcb) erzeugten JSON-Katalog hoch, um vorhandene Items, Schiffe, Hersteller, Materialien und Baupläne anzureichern, fehlende Spiel-UUIDs abzugleichen und offene Bauplan-Zutaten zu verknüpfen.** Eine Vorschau zeigt je Typ, was angereichert bzw. neu angelegt würde; „Anwenden" schreibt die Änderungen, wobei fehlende Datensätze nur bei gesetztem Schalter „Neue Datensätze anlegen" entstehen (gefiltert auf spielbare Inhalte, als Quelle „P4K" gekennzeichnet, Materialien zunächst unsichtbar zur Freigabe). Widersprüchliche UUIDs bleiben erhalten und werden im neuen „P4K"-Sync-Bericht protokolliert (Migration V140).

- **Die UEX-Synchronisation schreibt jetzt ebenfalls in das Sync-Protokoll, sodass der UEX-Tab unter „Sync-Berichte" (Administration) nicht mehr leer bleibt.** Pro Lauf erscheint eine Zusammenfassungszeile (besuchte Kategorien, angelegte/aktualisierte/entfernte Items); bisher protokollierte dort nur die SC-Wiki-Synchronisation.

- **Neuer Voll-Abgleich der Item-Daten zwischen UEX und SC Wiki: Beim vollständigen Wiki-Item-Import werden UEX-Items ohne Spiel-UUID per Name/Slug ihrem Wiki-Gegenstück zugeordnet und zusammengeführt, statt eine Dublette anzulegen.** Das schließt die Lücke, durch die rund ein Drittel der UEX-Items (u. a. die meisten Waffen und Lackierungen) bisher ohne Wiki-Daten wie Beschreibung, Masse und Klassifizierung blieb. Aktiviert über die Einstellung `krt.scwiki.sync-all-items` (in Dev aktiv, in Prod vorerst standardmäßig aus).

### Changed

- **Bei einem Item-Auftrag lässt sich das gewünschte Item jetzt durch Tippen suchen: Das Auswahlfeld ist ein durchsuchbares Dropdown, das die Liste live auf die eingegebenen Buchstaben filtert und sich per Tastatur bedienen lässt.** Das ersetzt die lange, schwer überschaubare Auswahlliste und macht die Item-Wahl deutlich schneller.

## [v0.3.59](https://github.com/krt-profit/basetool/releases/tag/v0.3.59) - 2026-06-05

### Changed

- **Beim „Material eintragen" auf einen Spezialkommando-Auftrag lassen sich jetzt nur noch profit-berechtigte Staffeln eintragen.** Die Staffelauswahl im Eintragungs-Dialog listet nur noch profit-berechtigte Staffeln, und das Backend weist eine Eintragung für eine nicht profit-berechtigte Staffel ab.

### Fixed

- **In den Material-Tabellen der Auftragsdetails beginnen die „Eintragen"-Buttons jetzt auf gleicher Höhe untereinander, statt je nach Breite der Mengenangabe zu verspringen.**

- **In allen Daten-Tabellen reagieren jetzt auch die hell hinterlegten Zeilen (jede zweite Zeile) auf das Überfahren mit der Maus — bisher zeigten nur die dunklen Zeilen einen Hover-Effekt, was verwirrend wirkte.**

## [v0.3.58](https://github.com/krt-profit/basetool/releases/tag/v0.3.58) - 2026-06-05

### Fixed

- **Nutzer werden nicht mehr häufig ausgeloggt: Eine Anmeldung bleibt bei regelmäßiger Nutzung gleitend bis zu 30 Tage bestehen und übersteht jetzt das Schließen des Browsers wie auch einen Neustart des Redis-Containers.** Das Sitzungs-Cookie ist dazu 30 Tage persistent, die Server-Sitzung läuft erst nach 30 Tagen Inaktivität ab (vorher 10 Tage), Redis sichert die Sitzungen wieder per Snapshot, und die im Login-Theme zuvor fehlende Option „Angemeldet bleiben" ist wieder vorhanden (standardmäßig aktiv).

- **Barrierefreiheit der Kernseiten verbessert: Der Menü-Button (Hamburger) hat jetzt eine für Screenreader lesbare Beschriftung, die Datums- und Zeitfelder im Einsatz-Filter sind beschriftet, und die Versionsanzeige im Footer sowie das Org-Einheits-Kürzel im Kontext-Chip erfüllen jetzt die Mindestkontraste.** Außerdem deklariert jede Seite per Sprachattribut ihre Sprache (Deutsch oder Englisch), was Screenreadern und Suchmaschinen zugutekommt.

## [v0.3.57](https://github.com/krt-profit/basetool/releases/tag/v0.3.57) - 2026-06-04

### Changed

- **Die Seitenleisten-Navigation ist in einklappbare, thematische Gruppen gegliedert (Einsatzplanung, Flotte & Logistik, Handel, Persönlich, Beförderung, Organisation, Administration, Rechtliches), damit sie nicht mehr überladen wirkt.** Die Gruppen sind standardmäßig zugeklappt, merken sich pro Browser ihren Auf-/Zu-Zustand und öffnen automatisch den Bereich der aktuellen Seite; der aktive Menüpunkt wird hervorgehoben. Die umfangreiche Administration ist zusätzlich in Cluster (Benutzer & Inhalte / Stammdaten / System & Daten) gegliedert.

## [v0.3.56](https://github.com/krt-profit/basetool/releases/tag/v0.3.56) - 2026-06-04

### Fixed

- **Auf Smartphone und Tablet wurde der untere Seiteninhalt nicht mehr hinter dem fixierten Footer versteckt — sichtbar u. a. auf der Startseite.** Die responsiven Seitenabstände hatten die Platzreservierung für den Footer überschrieben; sie bleibt jetzt auf allen Bildschirmgrößen erhalten und wächst mit dem mobil höher gestapelten Footer mit.

- **Die Mehrfachauswahl-Filter (Lager, Material-Übersicht, Profitberechnung) zeigen ihre aufgeklappte Optionsliste jetzt vollständig über dem Footer statt teilweise dahinter.**

- **Auf der Bewertungsverwaltung (Beförderungssystem) rutschte die untere Kante der Bewertungsmatrix samt horizontaler Bildlaufleiste hinter den fixierten Footer — vor allem auf schmalen und mobilen Viewports, wo der Footer höher gestapelt ist.** Die Matrix-Höhe rechnet die tatsächliche Footer-Höhe jetzt ein, wie bereits auf der Material-Übersicht und der Profitberechnung.

## [v0.3.55](https://github.com/krt-profit/basetool/releases/tag/v0.3.55) - 2026-06-04

### Added

- **Die Operations-Auszahlungsansicht zeigt Spenden jetzt zentral an: pro Spender den gespendeten Betrag und eine operationsweite Summe „Spenden gesamt" (im Auszahlungs- und im Finanz-Panel).** Spenden bleiben wie bisher bei der Organisation und werden nicht auf Mitglieder mit Auszahlungsart „Auszahlen" verteilt — die Summe macht den bisher unsichtbaren Betrag nun nachvollziehbar.

- **Im Hangar setzt der neue Button „Home-Location setzen" alle eigenen Schiffe in einem Schritt auf einen gewählten Ort.** Bei jedem Klick wählt man den Ort neu aus einer kuratierten Liste der Spiel-Home-Locations (alphabetisch absteigend); ein Bestätigungsdialog zeigt vorab die Anzahl betroffener Schiffe. Admins pflegen diese Liste unter „Orte verwalten" über den neuen Schalter „Als Home-Location markieren" (Migration V139).

### Fixed

- **Ein angemeldetes Staffelmitglied (ohne Offiziers- oder Adminrechte) kann seine eigene Auszahlungsart in einem Einsatz jetzt direkt in der Auszahlungs-Übersicht umstellen.** Bisher war ausgerechnet die eigene Zeile gesperrt, während sich die Auszahlungsart anonymer Teilnehmer ändern ließ — Ursache war ein Vergleich der Teilnehmer-ID mit dem Login-Benutzernamen statt mit der Keycloak-ID.

### Security

- **Ungenutztes Asciidoctor-Gradle-Plugin entfernt — das beseitigt 8 OWASP-Dependency-Check-Alerts an der Wurzel.** Das Plugin war totes Gerüst (keine `.adoc`-Quellen, kein Spring-REST-Docs; die API-Doku kommt von springdoc/OpenAPI) und zog allein die verwundbare `jruby-complete`-Kette (snakeyaml, jline, bcprov, jruby-base) auf den Build-Klassenpfad. Die dadurch obsolete SnakeYAML-Suppression in der OWASP-Suppressions-Datei wurde mitentfernt.

## [v0.3.54](https://github.com/krt-profit/basetool/releases/tag/v0.3.54) - 2026-06-04

### Fixed

- **Der „Abmelden"-Button im Menü funktioniert wieder korrekt** — er sah seit der Umstellung auf ein CSRF-sicheres POST-Formular schmaler und blasser aus (gemischte Schreibweise statt Versalien) und beendete zwar die lokale Sitzung, nicht aber die Keycloak-SSO-Sitzung: die Seite lud nicht neu und ein erneuter Login ging ohne Passwortabfrage durch. Ursache war die CSP-Regel `form-action 'self'`, die den Abmelde-Redirect auf die anders-originige Keycloak-URL blockierte; die Keycloak-Herkunft ist jetzt erlaubt, und der Button erbt wieder das Navigations-Styling.

## [v0.3.53](https://github.com/krt-profit/basetool/releases/tag/v0.3.53) - 2026-06-04

### Fixed

- **Das öffentliche Auftrags-Anlageformular zeigt anonymen Gästen jetzt befüllte Auswahlfelder: „Auftraggeber" listet alle Staffeln und Spezialkommandos, „bearbeitende Einheit" alle profit-berechtigten Staffeln und Spezialkommandos** (bisher war „Auftraggeber" leer und die Profit-SKs fehlten bei der bearbeitenden Einheit). Die bearbeitende Einheit ist mit dem konfigurierten Eingangs-Spezialkommando (Systemeinstellung unter `/admin/settings`) vorbelegt; ein Gast kann stattdessen eine andere profit-berechtigte Einheit wählen, sonst läuft der Auftrag wie bisher auf das Eingangs-SK.

- **Ein Gast in einem Einsatz lässt sich jetzt von jedem, der ihn eintragen darf (auch anonym), mit einer beliebigen Staffel oder einem beliebigen Spezialkommando markieren.** Bisher verwarf das Backend die Markierung anonymer Nutzer und wies angemeldete Nutzer ab, die nicht selbst Mitglied der gewählten Org-Einheit waren (403) — insbesondere konnte ein SK-Leiter dem Einsatz seines eigenen Spezialkommandos keinen Gast mit dem SK-Label hinzufügen. Die Markierung ist reine Einsatz-Metadaten (nur fürs Roster), vergibt keine Rechte und ändert keine Nutzerdaten.

- **Mitglieder eines Spezialkommandos — insbesondere staffellose SK-Leiter — können „ihre" SK-Daten jetzt auch im Detail öffnen und bearbeiten, nicht mehr nur in Listen sehen.** Die Detail- und Schreib-Zugriffsprüfungen (Einsätze inkl. interner sowie SK-eigene Schiffe/Lager/Raffinerie/Operationen) prüften bisher nur die Heimat-Staffel des Aufrufers und wiesen reine SK-Zugehörigkeiten ab; sie verwenden jetzt denselben Geltungsbereich wie die Listen — die Union aus Staffel und allen SK-Mitgliedschaften bzw. die angepinnte Org-Einheit. Die Strikt-Staffel-Isolation bleibt erhalten: Zugriff weiterhin nur auf eigene Org-Einheiten.

## [v0.3.52](https://github.com/krt-profit/basetool/releases/tag/v0.3.52) - 2026-06-04

### Changed

- **Die Oberfläche wird app-weit auf das aktuelle KRT-Design-System vereinheitlicht.** Die Headline-Schrift wechselt von Ethnocentric zu Audiowide (gleichmäßigere Kerning, besser lesbar), und die Aktions-Hierarchie wird angeglichen: pro Ansicht genau eine gefüllte orange Hauptaktion, wiederholte Zeilen- und Routineaktionen dezent (Ghost/Outline), destruktive Aktionen zurückhaltend, Formular-Labels und reine Datenwerte nicht mehr orange.

- **Das Keycloak-Login und die Account-Konsole sind an dasselbe Design-System angeglichen, damit der SSO-Redirect nahtlos wirkt.** Headlines erscheinen in Audiowide statt Ethnocentric, Validierungsfehler werden rot (mit ⚠-Symbol und rotem Feldrand) statt orange dargestellt, der Primär-Button hellt beim Hover auf und trägt den Orange-Bloom (eine gefüllte Hauptaktion pro Seite), der Login zeigt denselben Waben-Hintergrund wie die App, und der Tastatur-Fokus ist auf allen Bedienelementen sichtbar.

## [v0.3.51](https://github.com/krt-profit/basetool/releases/tag/v0.3.51) - 2026-06-04

### Changed

- **Der Einsatz-Filter der Lageransichten (und das Einsatz-Zuordnungs-Dropdown) sortiert die Einsätze jetzt nach geplantem Start absteigend, sodass die neuesten oben stehen.** Bisher standen die ältesten oben; Einsätze ohne geplanten Start folgen weiterhin zuletzt.

- **Der Auftrags-Filter der Lageransichten und die Auftrags-Auswahlfelder in den Lagereinträgen sortieren die Aufträge jetzt nach Priorität (wichtigste zuerst) — wie in der Auftragsverwaltung.** Aufträge ohne Priorität stehen zuletzt; bei gleicher Priorität entscheidet die höchste Auftragsnummer.

## [v0.3.50](https://github.com/krt-profit/basetool/releases/tag/v0.3.50) - 2026-06-03

### Added

- **Der Schiffs-Import im Hangar erkennt jetzt zusätzlich das JSON des [Fleetyards](https://fleetyards.net)-Hangar-Exports**, das bisher als „Unbekanntes Format" abgewiesen wurde. Schiffe werden über denselben toleranten Namens- und Slug-Abgleich wie die übrigen Formate (Fleetview, HangarXPLOR, StarJump FleetViewer) den vorhandenen Schiffstypen zugeordnet; ein in Fleetyards vergebener Schiffsname wird als individueller Name übernommen.

- **Neue Seite „Organigramm" (`/org-chart`): visualisiert die Funktionsränge des Profit-Bereichs — die Bereichsleitung an der Spitze, darunter die profit-berechtigten Staffeln (Staffelleiter, bis zu vier benennbare Kommandogruppen mit je Kommandoleiter, Stv. und bis zu vier Ensign) und Spezialkommandos (1–2 SK-Leiter).** Eine Kommandogruppe lässt sich benennen und schon vor ihrem Kommandoleiter mit Stv. und Ensign besetzen — untergeordnete Posten sind also unabhängig vom übergeordneten besetzbar. Jeder angemeldete Nutzer kann das Organigramm ansehen; nur Admins bearbeiten es inline an den Knoten (zuweisen, neu zuweisen, umbenennen, entfernen). Rein informativ — eine Eintragung vergibt keine Berechtigungen (Migrationen V136/V138).

### Changed

- **Der automatische Deploy (`deploy.sh` / `iri-deploy.timer`) wiederholt ein Image, dessen Health-Check fehlschlägt, jetzt mit exponentiellem Backoff statt bei jedem 5-Minuten-Tick.** Bisher fuhr ein kaputtes `:stable` (oder ein vorübergehender Fehler) die Anwendung alle paar Minuten in die Wartungsseite, weil derselbe defekte Digest endlos neu ausgerollt und zurückgerollt wurde. Der Backoff ist an das Digest-Paar gekoppelt — ein neu promotetes (repariertes) Image wird sofort ausgerollt, nur Wiederholungen desselben fehlerhaften Images werden gedrosselt (`--force` erzwingt einen Sofort-Retry, `IRI_BACKOFF_BASE` / `IRI_BACKOFF_MAX` steuern die Zeiten).

### Fixed

- **Die Frontend-Readiness-Prüfung schlägt in Produktion nicht mehr am selbstsignierten Backend-Zertifikat fehl, sodass Deployments nicht mehr alle paar Minuten in die Wartungsseite kippen.** Die seit dem Sicherheits-Audit (L-5) gepinnte Health-Probe lief über den JDK-HTTP-Client, der — anders als der WebClient — die Hostnamen-Prüfung nicht abschalten kann; da das Backend-Zertifikat nur `localhost` (nicht den Docker-Alias `backend`) im SAN führt, meldete sie dauerhaft `DOWN`. Die Probe validiert die Zertifikatskette weiterhin gegen den gepinnten Truststore, überspringt jetzt aber die Hostnamen-Prüfung; fehlt das `backend-trust`-Bundle, fällt sie auf trust-all (wie der WebClient) statt auf den JVM-Standard-Truststore zurück.

## [v0.3.49](https://github.com/krt-profit/basetool/releases/tag/v0.3.49) - 2026-06-03

### Changed

- **Der Einsatz-Filter der Lageransichten („Mein Lager" / „Alle Lager") zeigt jetzt zusätzlich abgeschlossene und abgebrochene Einsätze der letzten drei Monate an, nicht mehr nur geplante und aktive.** Gerade beendete Operationen bleiben so im Filter — und im Einsatz-Zuordnungs-Dropdown — auswählbar; ältere abgeschlossene/abgebrochene Einsätze fallen weiterhin heraus, damit die Liste nicht zuwächst.

### Fixed

- **Lagerbestände mit Mengentyp SCU werden jetzt zuverlässig auf drei Nachkommastellen gerundet (kaufmännisch).** Beim Einlagern von Raffinerie-Ausbeuten summierte das Backend die Mengen als Gleitkommazahl ohne Rundung, sodass im Lager und im Ausbuchen-Dialog Werte wie 37,160000000000004 SCU auftauchten. Ein Persistenz-Guard am Lagereintrag rundet jetzt jeden Schreibpfad auf drei Stellen, und eine Migration bereinigt die bereits betroffenen Einträge (V137).

- **Verlinkte Raffinerieaufträge auf der Missions-Detailseite zeigen die Endzeit jetzt in der lokalen Zeitzone des Nutzers statt in UTC.** Die Spalte „Ende" wurde serverseitig ohne Zeitzonenumrechnung formatiert; sie wird jetzt — wie in der Raffinerieauftrags-Liste — im Browser DST-korrekt in die Lokalzeit umgerechnet.

- **Sicherheit: Die Raffinerie-Aufträge einer Mission (`GET /api/v1/refinery-orders/mission/{id}`) sind jetzt auf die Orgeinheit(en) des Aufrufers beschränkt.** Ein Logistiker konnte zuvor die Raffinerie-Finanzdaten (Ausgaben, Erlöse, Gewinn, Besitzer) einer fremden Staffel auslesen, indem er eine öffentliche Mission dieser Staffel aufrief; die Logistiker-Ansicht liefert jetzt nur noch Aufträge der eigenen Orgeinheit(en). Admins ohne aktive Staffel sehen weiterhin alles, eine angepinnte Staffel nur diese; die eigentümergefilterte Ansicht normaler Nutzer war nie betroffen.

- **Operations- und Einsatz-Detailseite: der fest eingeblendete Footer verdeckt nicht mehr den unteren Seiteninhalt.** Diese beiden Seiten betten ihren Inhalt neben dem `<main>`-Element ein, wodurch die globale Footer-Freiraum-Reservierung verloren ging und die letzte Panel-Zeile beim Scrollen unter den Footer rutschte; der Seiten-Wrapper reserviert den Platz jetzt selbst.

### Security

- **E-Mail-Adressen werden nur noch im eigenen Profil ausgeliefert.** Die zentrale User-Projektion (`UserMapper.toDto`) blendet die E-Mail jetzt grundsätzlich aus; allein der eigene Profilabruf (`/api/v1/users/me*`) fügt sie wieder hinzu. Dadurch enthält keine Nutzerliste/-suche, kein Nutzer-Detail, keine Missions-Teilnehmerliste, kein Finanz-Ledger und keine Hangar-/Auftrags-Antwort mehr eine fremde E-Mail — auch nicht für Logistiker, Offiziere oder Admins (Admins nutzen für E-Mails die Keycloak-Konsole). An andere Nutzer gehen nur noch Callsign und Anzeigename.

- **Missions-Finanzdaten sind nicht mehr staffelübergreifend lesbar.** `GET /api/v1/missions/{id}/finance-entries` und `/finance-entries/sum` sind jetzt an die Missions-Sichtbarkeit gebunden (`canSeeMission`); die Teilnehmer-PII (Rollen, Rechte) wird zusätzlich für jeden Abrufenden geschwärzt. Bisher konnte jeder angemeldete Nutzer mit einer Missions-UUID das komplette Finanz-Ledger einer fremden — auch internen — Mission lesen.

- **Das Anlegen einer Raffinerie-Order ignoriert jetzt eine vom Client mitgeschickte `id`/`version`.** Bisher konnte ein angemeldeter Nutzer durch Mitsenden einer fremden Order-`id` aus dem `save()` ein `merge()`-UPSERT erzwingen und so eine fremde Raffinerie-Order überschreiben und auf sich selbst übereignen (Umgehung des Bearbeiten-Gates); der Server erzwingt jetzt einen sauberen INSERT.

- **Negative Ausgaben (`expenses`) bei Raffinerie-Orders werden jetzt abgelehnt** (`@PositiveOrZero`, wie bereits bei `otherExpenses`/`oreSales`). Ein negativer Wert erhöhte über `Profit = Erlös - Ausgaben - sonstige Ausgaben` den Einsatz- bzw. Missions-Profit und damit die Auszahlungsanteile aller Teilnehmer — eine Finanzmanipulation, die jedes Mitglied mit einer eigenen Order auslösen konnte.

- **Weitere Auftrags-Endpunkte sind jetzt staffel-scoped.** Die Job-Order-Material-Collection (`/orders/{id}/material-collection`) und der Auftrags-Lookup (`/orders/lookup`) tragen jetzt das `canSeeJobOrder`- bzw. Profit-/Scope-Gate der Geschwister-Endpunkte. Bisher konnte jeder angemeldete Nutzer darüber fremde Beitrags-, Kontakt- und Materialdaten enumerieren. (Die missions-bezogene Raffinerie-Liste wurde separat gescoped — siehe „Fixed" oben.)

- **Negative Übergabe- und Raffinerie-Mengen werden jetzt abgelehnt.** Die Handover-Items tragen jetzt `@Valid` (das `@Positive` greift dadurch wirklich) plus einen Service-Guard, und die Raffinerie-Güter (`@Valid`) sowie die Geld-Felder (Obergrenze 1e9) sind validiert — bisher konnte eine negative Menge den Lagerbestand und die offene Anforderung erhöhen.

- **Der Frontend-Prod-JSON-Log maskiert jetzt PII.** Der bisher ungemaskte `LogstashEncoder` (`logs/frontend.json`) ist durch einen maskierenden Encoder ersetzt, sodass E-Mails / JWTs / Bearer-Tokens auch im JSON-Sink geschwärzt werden — wie in den Konsolen-/Datei-Logs.

- **Härtung (defense-in-depth):** Admin-Stammdaten-Create (Sternsysteme, Frequenztypen, Raffineriemethoden, Materialkategorien, Staffeln, SK, Job-Typen) ignoriert jetzt eine client-`id`/`version`; Logout läuft nur noch per CSRF-geschütztem POST; das Frontend hat ein Multipart-Upload-Limit (2 MB); der Backend-Health-Check pinnt in Prod das Backend-Zertifikat statt blind zu vertrauen; der Resource-Server kann optional die JWT-`aud` prüfen (`app.security.jwt.expected-audiences`); und die Docker-Base-Images sind auf ihren Multi-Arch-Digest gepinnt (reproduzierbare Builds, intakte SLSA-Provenance).

## [v0.3.47](https://github.com/krt-profit/basetool/releases/tag/v0.3.47) - 2026-06-03

### Added

- **Der Footer zeigt jetzt das „Made By The Community"-Logo und den erforderlichen Markenhinweis von Cloud Imperium Rights LLC.** Damit erfüllt die App die Vorgaben des offiziellen Star-Citizen-Fankits für Fan-Projekte; der Hinweis ist über den fest eingeblendeten Footer auf jeder Seite sichtbar.

- **Der Blueprint-Import im persönlichen Inventar akzeptiert jetzt zusätzlich das JSON des [Basetool Blueprint Extractor](https://github.com/krt-profit/basetool-bp-extractor), nicht mehr nur den SCMDB-Log-Watcher-Export.** Beide Formate liefern dieselben Bauplan-Namen; der Erhalt-Zeitstempel wird aus `ts` (SCMDB) oder `receivedAt` (Extractor) gelesen, und gelernte Namens-Aliase gelten für beide Tools.

- **Mehrfaches Importieren ist unkritisch: bereits vorhandene Baupläne werden nicht doppelt angelegt, und ihr Erhalt-Zeitpunkt wird nur dann aktualisiert, wenn der erneute Import einen früheren Zeitpunkt liefert.**

## [v0.3.46](https://github.com/krt-profit/basetool/releases/tag/v0.3.46) - 2026-06-03

### Changed

- **Auftrags-Workflow jetzt profit-gegated: nur Mitglieder einer profit-berechtigten Staffel/SK (und Admins) dürfen Aufträge sehen, bearbeiten (Status/Priorität/Materialien/Handover/Reassign) und Material-Claims setzen; reine Nicht-Profit-Mitglieder können Aufträge nur noch anlegen, sonst nichts.** Im Menü ersetzt „Auftrag anlegen" den „Aufträge"-Link, und ein Direktaufruf von `/orders` bzw. `/orders/{id}` leitet für sie auf das Anlege-Formular um. Das Backend setzt dasselbe Gate auf Lese-, Schreib- und Claim-Endpunkten durch (leere Liste bzw. `403`) — auch die sonst staffelübergreifend öffentliche SK-Warteschlange bleibt für Nicht-Profit-Mitglieder verborgen.

- **Beförderungssystem: ein Admin ohne aktive Staffel (Alle-Staffeln-Modus) sieht jetzt einen Hinweis „Bitte wähle eine Staffel" statt der zusammengeführten Daten aller Staffeln; erst nach Auswahl einer Staffel erscheint deren System.** Ein Beförderungskatalog ist inhärent pro Staffel, daher ergibt die vermischte Allansicht keinen Sinn. Nutzer ganz ohne Staffelzugehörigkeit haben kein eigenes Beförderungssystem mehr und sehen den Menüpunkt nicht (direkter Seitenaufruf wird mit 403 blockiert).

## [v0.3.45](https://github.com/krt-profit/basetool/releases/tag/v0.3.45) - 2026-06-03

### Fixed

- **Das Beförderungssystem ist jetzt durchgängig staffel-scoped: jede Staffel sieht nur ihre eigenen Themenbereiche, Bewertungen, Rangvoraussetzungen und Eignungsprüfungen.** Bisher filterten nur die Themenbereiche nach Staffel — Kategorien, Lerninhalte, Rangvoraussetzungen, „Meine Bewertungen", die Bewertungsverwaltung und die Eignungsberechnung werteten staffelübergreifend aus; ein Offizier oder Mitglied einer Staffel konnte so die Beförderungsdaten anderer Staffeln sehen. Admins sehen weiterhin alles bzw. die angepinnte Staffel. Rangvoraussetzungen (inklusive staffelweiter „globaler" ohne Themenbereich/Kategorie) gehören jetzt einer eigenen Staffel und werden beim Anlegen aus dem aktiven Staffelkontext gestempelt (Migration V135).

## [v0.3.44](https://github.com/krt-profit/basetool/releases/tag/v0.3.44) - 2026-06-02

### Changed

- **Datenschutz (Folge-Release): die DB-Spalten `app_user.first_name` / `last_name` werden endgültig entfernt (V134).** Phase 2 der Zwei-Phasen-Löschung nach V133 — die Spalten waren seitdem leer und ungenutzt; der Drop setzt voraus, dass das V133-Release in Produktion deployt ist.

## [v0.3.43](https://github.com/krt-profit/basetool/releases/tag/v0.3.43) - 2026-06-02

### Changed

- **Datenschutz: Vor- und Nachname werden nicht mehr gespeichert oder angezeigt.** Backend-Sync und Profilseite werten die Keycloak-Claims `given_name`/`family_name`/`name` nicht mehr aus; die Felder sind aus `UserDto` und der Profilanzeige entfernt. Bestehende Werte in `app_user` werden geleert (V133, Spalten-Drop folgt) — in Keycloak sind die zugehörigen Claim-Mapper und User-Profile-Felder zu deaktivieren.

- **Lagerbestand folgt jetzt an den Rändern der Org-Mitgliedschaft seines Besitzers.** Tritt ein zuvor mitgliedschaftsloser Nutzer seiner ersten Orgeinheit bei, wandern seine bis dahin org-losen (nur ihm sichtbaren) Lagereinträge automatisch in deren Lager; verlässt er seine letzte Orgeinheit, werden seine org-gebundenen Einträge wieder rein persönlich (nur für ihn sichtbar). Beim Umstempeln identische Stapel werden dabei zusammengelegt (keine Duplikate); privates Inventar bleibt unberührt.

### Fixed

- **Umbuchen (Ausbuchen → anderer Nutzer/Ort) legt jetzt auf vorhandenen identischen Bestand auf, statt eine zweite Lagerzeile anzulegen.** Beim Transfer auf einen Nutzer/Ort mit bereits identischer Zeile (gleiches Material, gleiche Qualität, gleicher Auftrag/Einsatz und besitzende Orgeinheit) entstand bisher ein Duplikat; jetzt wird die Menge auf die bestehende Zeile addiert — wie beim Eintragen und Raffinerie-Einlagern. Das Zusammenlegen unterscheidet jetzt durchgängig nach besitzender Orgeinheit, sodass Bestand verschiedener Staffeln/SKs nie zu einer Zeile verschmilzt.

## [v0.3.42](https://github.com/krt-profit/basetool/releases/tag/v0.3.42) - 2026-06-02

### Fixed

- **Blueprint-Verfügbarkeit (`/blueprint-overview`): ein Admin in der „Alle Staffeln"-Ansicht sieht jetzt wirklich alle Baupläne — inklusive der eigenen.** Die Allansicht löste die Besitzer über die Org-Einheit-Mitglieder auf und ließ damit Nutzer ohne Mitgliedschaft (etwa einen staffellosen Admin) weg, sodass dessen eigene Baupläne fehlten; sie umfasst jetzt jeden Bauplan-Besitzer.

- **Blueprint-Verfügbarkeit: der Besitzer-Aufklapper je Bauplan liefert auch bei Namen mit Leerzeichen oder Anführungszeichen wieder Treffer.** Der Produktschlüssel wurde beim Nachladen form-kodiert (Leerzeichen → `+`) und über den Frontend→Backend-Weg verfälscht, sodass eine Zeile „Verfügbar bei: 1" anzeigte, der Aufklapper aber „keine Besitzer in deiner Orgeinheit". Der Schlüssel wird jetzt als URL-Parameter übergeben und korrekt prozentkodiert (Leerzeichen → `%20`).

## [v0.3.41](https://github.com/krt-profit/basetool/releases/tag/v0.3.41) - 2026-06-02

### Added

- **Neue Seite „Blueprint-Verfügbarkeit" (`/blueprint-overview`): Offiziere, Admins und SK-Leads sehen, welche Crafting-Baupläne innerhalb ihrer Orgeinheit verfügbar sind.** Die Übersicht listet je Blueprint, bei wie vielen Mitgliedern er vorhanden ist; ein Klick auf eine Zeile lädt die Besitzer (nur Anzeigename) nach. Offiziere sehen ihre Staffel, SK-Leads ihre SK, Admins alle bzw. die angepinnte Orgeinheit — der Menüpunkt erscheint nur für diese Rollen. Neue Endpoints `GET /api/v1/personal-blueprints/overview` (+ `/owners`) und das Capability-Signal `GET /api/v1/me/capabilities`.

### Fixed

- **Persönliche Inventar- und Admin-Bauplan-Tabellen: der Tabellenkopf ist nicht mehr orange in der Headline-Schrift.** Er zeigte den Spaltentitel orange in Ethnocentric statt — wie die Aktions-Hierarchie vorsieht — neutral hellgrau in Lato mit der 2px-Orange-Unterlinie. Der seiteneigene `.krt-pi-table th`-Override wurde entfernt, sodass `/personal-inventory`, `/personal-inventory/blueprints` und die zugehörigen Admin-Seiten wieder die kanonische Tabellenkopf-Regel nutzen.

- **Admin-Blueprint-Verwaltung (`/admin/personal-blueprints`): „Notiz bearbeiten" und „Entfernen" je Zeile funktionieren wieder.** Der zeilenspezifische URL-Platzhalter `__ID__` wurde von Thymeleaf beim Rendern zu „ID" verschluckt, sodass beide Aktionen mit ungültiger ID liefen und serverseitig mit 400 abbrachen. Der Platzhalter heißt jetzt `ID_PLACEHOLDER` und bleibt von der Template-Vorverarbeitung unangetastet (gleiche Korrektur wie zuvor auf der Nutzerseite).

## [v0.3.40](https://github.com/krt-profit/basetool/releases/tag/v0.3.40) - 2026-06-02

### Added

- **Persönliches Inventar → „Meine Blueprints": jede Bauplan-Zeile lässt sich jetzt aufklappen und zeigt ihre Zutaten samt Stat-Beitrag nach Zutat-Qualität.** Analog zur Admin-Seite `/admin/blueprints` berechnet je Stat ein Schieberegler (min.–max. Zutat-Qualität) den finalen Wert live; die Rezeptdaten werden erst beim Aufklappen nachgeladen (neuer Endpoint `GET /api/v1/personal-blueprints/{id}/recipe`). Eingeklappt platzsparend, damit die Liste übersichtlich bleibt; bei Produkten mit mehreren Rezeptvarianten wird ein Beispielrezept gezeigt.

## [v0.3.39](https://github.com/krt-profit/basetool/releases/tag/v0.3.39) - 2026-06-02

### Added

- **Hangar (`/hangar`) und Staffelübersicht (`/hangar/squadron`) haben jetzt eine Live-Suche nach Schiffstyp.** Ein Suchfeld über der Tabelle filtert die Zeilen sofort während des Tippens (rein clientseitig, kein Reload); passt kein Schiffstyp, erscheint ein Hinweis statt einer leeren Tabelle. Folgt der KRT-Design-Sprache (Lupen-Icon, Fokus-Glow).

### Changed

- **Auftragsübersicht (`/orders`): zeigt jetzt den Auftraggeber statt der bearbeitenden Einheit, und die Tabelle ist auf fünf Spalten verdichtet.** Pro Zeile steht die auftraggebende Einheit über dem Handle und der Status unter der Priorität — das spart Breite für die Materialspalte. Die eingebettete Materialtabelle zeigt zudem keine Spalte „Min. Q." mehr. Reine Anzeigeänderung; die bearbeitende Einheit bleibt in den Auftragsdetails sichtbar.

### Fixed

- **Hangar-Staffelübersicht: ein auf eine Staffel eingestellter Admin sieht in der Schiffstyp-Aufschlüsselung keine Schiffe fremder Org-Einheiten mehr.** Die Eigentümer-Detailliste je Schiffstyp wurde – anders als die Stückzahlen daneben – ungescoped geladen und zeigte auch Schiffe anderer Org-Einheiten (etwa eines reinen Spezialkommando-Mitglieds), sobald sie sich einen Schiffstyp mit der eingestellten Staffel teilten. Sie wird jetzt mit demselben Org-Einheit-Scope wie die Stückzahlen gefiltert; nur Admins ohne aktive Bereichsauswahl sehen weiterhin alles.

- **Auftrag anlegen: das Kommentar-/Notizen-Feld nutzt jetzt die volle Formularbreite.** Die seiteneigene Breitenregel deckte `input`/`select` ab, ließ `textarea` aber aus, sodass das Feld auf die schmale Browser-Standardbreite zurückfiel; jetzt ist es voll breit und nur vertikal vergrößerbar (Material- und Item-Auftrag).

## [v0.3.38](https://github.com/krt-profit/basetool/releases/tag/v0.3.38) - 2026-06-02

### Changed

- **Material-Sammelübersicht nutzt jetzt die zentralen KRT-Toasts statt einer eigenen Variante.** Erfolg-/Fehlermeldungen beim Ändern von Besitzer, Standort oder Lieferstatus erscheinen im einheitlichen App-Toast-Stil; die seiteneigene `.krt-toast`-Implementierung entfällt. Zusätzlich teilen sich `table`, `.krt-table` und `.data-table` jetzt eine kanonische Tabellen-Regel (rein interne Aufräumarbeit, keine sichtbare Änderung).

- **App-weite Vereinheitlichung am KRT-Design-System (Aktions-Hierarchie statt „alles orange").** Pro Ansicht/Panel/Modal hebt sich jetzt genau eine gefüllte Orange-CTA ab (z. B. „Anmelden", „Speichern", „Neuer Auftrag"); wiederkehrende Aktionen (Details, Bearbeiten, Filtern, Check-Out) sind ruhige Ghost-Buttons und destruktive Aktionen treten zurück (Rot erst beim Hover). Formular-Labels, Tabellenköpfe und reine Datenwerte sind jetzt neutral (hellgrau/weiß) statt orange, und die Panel-Köpfe der Einsatz-/Operations-Details nutzen einen ruhigen Akzentbalken statt Vollorange-Rahmen. Die Hierarchie ist jetzt durchgängig über die gesamte App ausgerollt: Listen, Details, Inventar, Hangar, alle Erstellen-/Bearbeiten-Formulare (Aufträge, Raffinerie, Stammdaten), der komplette Admin-Bereich und das Beförderungssystem.

### Fixed

- **Mehrere Anzeigefehler durch falsch referenzierte CSS-Farbvariablen behoben.** Eingabefelder in den Auftrags-Modalen (Eintragen/Übergabe) hatten einen transparenten statt dunklen Hintergrund, Admin-Fehlerboxen keinen roten Rahmen, und das „Manuell"-Badge der Materialverwaltung sowie die Lead-/Logistiker-Badges eines Spezialkommandos keine korrekte Füllung/Schriftfarbe — sie verwiesen auf nicht existierende Tokens (`--color-bg`, `--color-combat`, `--color-bg-dark`, `--color-accent`, `--color-primary-dark`) und nutzen jetzt die definierten Design-Tokens.

- **Materialdetailseite zeigt ihre Texte jetzt lokalisiert und das Terminal-Filterfeld hat einen Screenreader-Namen.** Die zuvor hartkodierten Strings (Überschrift „Preise und Terminals", Filter-Platzhalter, „Material nicht gefunden", Zurück-Link) kommen jetzt aus den Sprachdateien — englische Nutzer sehen sie auf Englisch; das Filterfeld erhält ein verstecktes Label (`.visually-hidden`, jetzt global) für Barrierefreiheit.

- **Nutzer ohne Staffel-/SK-Zugehörigkeit können wieder Schiffe, Raffinerieaufträge und Lagereinträge anlegen.** Seit der NOT-NULL-Verschärfung auf der besitzenden Org-Einheit (V102) lief das Anlegen für mitgliedschaftslose Nutzer in einen „User has no org-unit membership"-Fehler (HTTP 400). Solche Einträge werden jetzt als persönliche, org-lose Datensätze gespeichert (V132 macht `owning_org_unit_id` für Schiff/Raffinerieauftrag/Lagereintrag wieder nullable) und sind ausschließlich für ihren Besitzer sichtbar und bearbeitbar (Admins ohne aktive Staffel-Auswahl sehen sie weiterhin). Einsätze und Operationen bleiben unverändert org-gebunden.

## [v0.3.37](https://github.com/krt-profit/basetool/releases/tag/v0.3.37) - 2026-06-01

### Changed

- **Die Umschalter „Items"/„Blueprints" im persönlichen Inventar sind jetzt deutlich besser erkennbar.** Beide Tabs sehen nun wie echte klickbare Segment-Buttons aus (sichtbarer Hintergrund und Rahmen); der aktive Tab ist orange gefüllt statt nur per Textfarbe markiert.

### Fixed

- **Auftrag anlegen: profit-berechtigte Spezialkommandos erscheinen jetzt in der Auswahl „Bearbeitende Einheit".** Der SK-Katalog wurde über den anonymen statt den angemeldeten Backend-Client geladen und lief in ein stilles 401, sodass nie ein SK im Picker auftauchte (nur Staffeln). Zusätzlich zeigt das Formular jetzt einen Hinweis, wenn gar keine bearbeitende Einheit freigeschaltet ist, statt eines leeren Dropdowns.

## [v0.3.36](https://github.com/krt-profit/basetool/releases/tag/v0.3.36) - 2026-06-01

### Added

- **Item-Aufträge sind jetzt bearbeitbar (Items, Mengen, Baupläne, Auftraggeber, Kommentar), solange noch keine Übergabe erfolgt ist.** Über „Items bearbeiten" in den Auftragsdetails öffnet sich das Anlegen-Formular im Bearbeiten-Modus, vorausgefüllt mit den bestellten Items; die benötigten Materialien werden aus den (ggf. geänderten) Bauplänen neu abgeleitet, und Eintragungen auf weggefallenen Material-Eimern werden automatisch zurückgezogen. Sobald eine Item-Übergabe protokolliert wurde, sind die Zeilen eingefroren. Endpoint `PUT /api/v1/orders/{id}/items`.

- **Eintragungen lassen sich jetzt direkt in den Auftragsdetails anlegen, ändern und zurückziehen.** Bei einem SK-Auftrag öffnet ein „Eintragen"-Button je Materialzeile ein KRT-Modal (Staffel- und Mengenauswahl); ein Stift-Symbol an einer bestehenden Eintragung öffnet sie zum Bearbeiten oder Zurückziehen. Die Menge wird schon im Browser gegen die offene Restmenge geprüft (kein Overclaim), und das Backend lehnt unberechtigte oder veraltete Anfragen sauber ab (403/400/409 als Toast statt Stacktrace). Die Steuerelemente sehen nur Logistiker/Offiziere bzw. Admins.

- **Auftragsdetails zeigen jetzt die Eintragungen und die offene Restmenge je Material (nur bei SK-Aufträgen).** Bei einem von einem Spezialkommando bearbeiteten Auftrag erscheinen in der Material- bzw. der aggregierten Materialtabelle zwei zusätzliche Spalten: „Eingetragen" (welche Staffel welche Teilmenge übernommen hat) und „Offen" (benötigt minus Summe der Eintragungen). Bei reinen Staffelaufträgen bleiben die Spalten ausgeblendet. Reine Anzeige — das Eintragen selbst folgt als UI in einer späteren Phase.

- **Staffeln können sich für Teilmengen eines SK-Auftrags eintragen (Material-Claims).** Auf einem Auftrag, der von einem Spezialkommando bearbeitet wird, kann sich jede Profit-Staffel pro Material-/Qualitäts-Eimer für eine Teilmenge eintragen; die Summe aller Eintragungen darf die benötigte Menge nicht überschreiten (kein Overclaim), und pro Eimer hat eine Staffel genau eine Eintragung (erneutes Eintragen aktualisiert sie). Eintragungen sind reine Signale (verschieben kein Lager) und für alle sichtbar. Eintragen/Ändern/Zurückziehen darf der Logistiker/Offizier der eigenen Staffel, der Logistiker oder Lead/Offizier des bearbeitenden SK sowie ein Admin. Endpoints unter `/api/v1/orders/{id}/claims`; neue Tabelle via V131. Ein Statuswechsel auf abgeschlossen/abgelehnt friert die Eintragungen ein, das Zurückstufen eines SK-Auftrags auf eine Staffel zieht sie zurück. (Noch keine UI — folgt in einer späteren Phase.)

- **Admin-Einstellungen (`/admin/settings`): Grundlage für den Auftrags-Umbau.** Pro Staffel **und** pro Spezialkommando lässt sich jetzt festlegen, ob die Einheit als verantwortliche (bearbeitende) Einheit eines Auftrags auswählbar ist (Spalte `org_unit.is_profit_eligible`, Endpoints `PATCH /api/v1/squadrons/{id}/profit-eligible` und `.../special-commands/{id}/profit-eligible`). SKs anderer Bereiche bleiben so von der Bearbeitung ausgenommen (sie dürfen Aufträge nur aufgeben). Zusätzlich wählt der Admin ein „Eingangs-Spezialkommando", dem anonym (ohne Login) erstellte Aufträge künftig zugewiesen werden (Setting `job_order.intake_special_command_id`). Felder via V128.

- **Der Hangar-Import erkennt jetzt zusätzlich StarJump-FleetViewer-Exporte („Hangar Link", `fleetviewer.link`) neben CCU Game Fleetview und HangarXPLOR.** Das Format wird weiterhin automatisch erkannt — keine zusätzliche Auswahl nötig. Schiffe werden primär über ihren Anzeigenamen zugeordnet; greift das nicht, dient der FleetViewer-Slug als Fallback gegen die UEX-/SC-Wiki-Slugs.

### Changed

- **Material-Übersicht (`/materials/overview`) friert den Browser beim Laden nicht mehr sekundenlang ein.** Die große Materialien-×-Terminals-Matrix wird nicht mehr als eine komplette HTML-Tabelle mit zehntausenden Zellen serverseitig gerendert. Stattdessen liefert die Seite nur noch eine schlanke Hülle, lädt die Matrix als kompaktes JSON (`/materials/overview/data`) und zeichnet im Browser per virtuellem Scrollen nur die gerade sichtbaren Zeilen — das DOM bleibt klein, egal wie groß das Universum ist. Filtern (Material/System/Loading-Dock/Auto-Load) läuft komplett clientseitig ohne Server-Roundtrip; die rund 100 000 Datensätze werden frontendseitig 10 Minuten gecacht. Übersichtspreise können dadurch einen UEX-Sync um bis zu 10 Minuten nacheilen; die Detailseite bleibt tagesaktuell.

- **Aufträge: statt „Erstellt durch" jetzt eine verantwortliche (bearbeitende) Einheit plus ein Auftraggeber.** Beim Anlegen wählt man die bearbeitende Einheit (nur Profit-berechtigte Staffeln/SKs) und den Auftraggeber (beliebige Staffel/SK); anonym erstellte Aufträge gehen automatisch an das eingestellte Eingangs-Spezialkommando. Die bearbeitende Einheit lässt sich per neuem Endpoint umschreiben (Admins frei; Staffel-Logistiker nur Eskalation Staffel→SK); Liste und Detail zeigen sie an. Spezialkommandos sind als Org-Einheit eines Auftrags jetzt vollwertig wählbar (Migration V129); die Sichtbarkeits-Trennung pro Einheit folgt separat.

- **Aufträge sind jetzt nach der bearbeitenden Einheit getrennt sichtbar.** Ein Auftrag mit einer Staffel als bearbeitender Einheit ist nur für diese Staffel und Admins sichtbar/bearbeitbar; ein Auftrag bei einem Spezialkommando bleibt für alle Staffeln einsehbar und (durch Logistiker jeder Profit-Staffel) bearbeitbar — die zentrale SK-Warteschlange bleibt also gemeinsamer Arbeitsraum. Der Auftraggeber gewährt keine Sichtbarkeit. Liste, Detailansicht, Statuswechsel, Priorität, Übergaben und Reports prüfen die Sichtbarkeit serverseitig. Bestehende Aufträge wurden auf ihre frühere Staffel als bearbeitende Einheit migriert (damit privat) und die Spalte auf NOT NULL gesetzt (V130).

### Fixed

- **Material-Übersicht (`/materials/overview`) und Profitberechnung (`/materials/profit-calculation`): Tabellenende und horizontaler Scrollbalken verschwinden nicht mehr hinter dem Footer.** Die Tabellenhöhe richtet sich jetzt nach dem Platz zwischen Kopfzeile und fixiertem Footer (statt fester `70vh`), sodass die letzten Einträge sichtbar bleiben und der horizontale Scrollbalken bedienbar ist.

- **Material-Übersicht (`/materials/overview`) bricht bei sehr großer Matrix nicht mehr mit „Fehler beim Laden der Material-Matrix" ab.** Das In-Memory-Pufferlimit des Frontend-WebClients für Backend-Antworten wurde von 16 MB auf 64 MB angehoben; bei einem großen UEX-Katalog überschritt die Matrix-Antwort 16 MB, woraufhin der Reactive-Codec abbrach und die Seite gar nichts mehr anzeigte.

## [v0.3.35](https://github.com/krt-profit/basetool/releases/tag/v0.3.35) - 2026-06-01

### Added

- **Persönliches Inventar ist jetzt in „Items" und „Blueprints" geteilt; auf der neuen Blueprints-Seite verwaltet jeder Nutzer seine ingame freigeschalteten Crafting-Baupläne.** Baupläne werden über eine Tippsuche mit Mehrfachauswahl hinzugefügt (rund 1600 Produkte, daher kein einfaches Dropdown). Zusätzlich lässt sich ein SCMDB-Log-Watcher-JSON-Export importieren: die Vorschau ordnet jeden Namen automatisch zu (exakt / über Alias / Fuzzy-Vorschlag), unzugeordnete Zeilen löst man manuell auf, und die Zuordnung wird als Alias für künftige Importe gelernt. Neue Endpoints unter `/api/v1/personal-blueprints` (neue Tabellen via V126/V127).

- **Admins können die Blueprints eines beliebigen Mitglieds verwalten (`/admin/personal-blueprints`).** Nach Auswahl des Mitglieds stehen dieselbe Liste, das Mehrfach-Hinzufügen und der SCMDB-Import wie auf der Nutzerseite zur Verfügung — über `/api/v1/admin/personal-blueprints`.

### Fixed

- **Item-Auftrag: SCU-Mengen der abgeleiteten Materialien enthalten keine Gleitkomma-Artefakte mehr.** Eine Menge wie „1.7999999999999998 SCU" oder „0.15000000000000002 SCU" entstand, weil Menge-pro-Einheit × Anzahl als roher `double` durchgereicht wurde; die Ableitung rundet SCU-Mengen jetzt schon im Backend auf die im UI genutzte 0,001-Genauigkeit — in der Vorschau, im gespeicherten Auftrag und in der aggregierten Materialliste.

## [v0.3.33](https://github.com/krt-profit/basetool/releases/tag/v0.3.33) - 2026-06-01

### Added

- **Sync-Berichte (`/admin/sync-reports`) lassen sich jetzt nach Alter aufräumen.** Ein Admin kann über ein Eingabefeld plus Bestätigungsdialog alle Berichte löschen, die älter als X Tage sind — je nach aktivem Tab für alle Quellen oder nur SC Wiki bzw. UEX. Neuer Endpoint `DELETE /api/v1/sync-reports?olderThanDays=…`.

### Changed

- **Einsatz-Teilnehmer zeigen jetzt alle ihre Org-Einheiten (Staffel und/oder Spezialkommando) statt einer einzelnen Staffel.** Für angemeldete Nutzer werden die Org-Einheiten automatisch aus ihren Mitgliedschaften abgeleitet (nichts, wenn keine; die eine, wenn nur Staffel oder SK; beide, wenn beides) — der falsche IRIDIUM-Standard bei Admins ohne Mitgliedschaft entfällt, und das Staffel-Dropdown im Anmelde-Modal wird für angemeldete Nutzer ausgeblendet. Das Org-Einheiten-Mehrfachfeld bleibt nur für das Eintragen von Gästen (neue Verknüpfungstabelle via V124).

- **SC-Wiki-Sync R9 Step 4 — V125-Drop der Legacy-Spalten (destruktiv, irreversibel).** Migration V125 droppt `material.is_manual_entry` und `ship_type.description` und entfernt die zugehörigen JPA-Entity-Felder; die Manual-Provenienz lebt seit R9 Step 1 in `source_systems`, die Schiffsbeschreibung in `description_en`/`description_de`. **Voraussetzung vor dem Merge:** R8-Soak sauber und ein frisches DB-Backup — die Spaltendaten sind danach unwiderruflich weg.

- **SC-Wiki-Sync R9 Steps 1-2 — Reader-Migration vor dem destruktiven Cleanup.** Die Manual-Kennzeichnung leitet sich jetzt aus `source_systems == MANUAL` ab und die Schiffstyp-Beschreibung auf `/ship-data` aus `description_en`/`description_de` (Deutsch bevorzugt) — die Legacy-Spalten `material.is_manual_entry` und `ship_type.description` werden nicht mehr gelesen oder geschrieben. Für Nutzer sichtbar nur: die Schiffsbeschreibung ist jetzt der echte UEX/Wiki-Text statt der synthetisierten Spec-Liste. Kein Schema-Change — die Spalten fallen erst im irreversiblen V125-Drop (R9 Step 4, nach dem Soak).

## [v0.3.32](https://github.com/krt-profit/basetool/releases/tag/v0.3.32) - 2026-05-31

### Fixed

- **Neuer Item-Auftrag (`/orders/new`): „Entfernen"-Button einer Item-Zeile sitzt nicht mehr tiefer als die Felder daneben.** Seine Unterkante liegt jetzt auf derselben Höhe wie das Anzahl-Feld. Die `.form-group`-Spalten der Zeile behielten durch eine globale CSS-Regel ihren unteren Außenabstand trotz `mb-0`, was den abstandslosen Button bei `flex-end`-Ausrichtung nach unten drückte.

## [v0.3.31](https://github.com/krt-profit/basetool/releases/tag/v0.3.31) - 2026-05-31

### Added

- **Aufträge gibt es jetzt auch als Item-Auftrag (neben dem reinen Materialauftrag).** Beim Anlegen wählt man die Auftragsart; bei einem Item-Auftrag bestellt man fertige Items (Waffen, Fahrzeug-Items, Rüstung u.a.), und das System leitet aus deren Bauplänen automatisch die benötigten Materialien ab und aggregiert sie über den ganzen Auftrag — getrennt nach Qualität (Gut 700+ / Keine) und Mengentyp (SCU/Stück). Die Detailseite zeigt die bestellten Items mit Liefer-Fortschritt und die aggregierte Materialliste; Logistiker übergeben Items stückweise mit PDF-Übergabeprotokoll, und der Auftrag schließt automatisch ab, sobald alle Items geliefert sind (neue Tabellen via V123).

### Changed

- **Swagger UI wurde entfernt; die API-Dokumentation liegt nur noch im `openapi.json` und ist in Produktion nicht von außen abrufbar.** Das Backend nutzt jetzt den SpringDoc-`-api`-Starter (ohne UI-Webjar), sodass `/swagger-ui.html` entfällt. Das OpenAPI-Dokument unter `/v3/api-docs` ist nur in den Profilen `dev`/`test` erreichbar (für die Generierung der committeten `backend/src/main/resources/api/openapi.json`) und im `prod`-Profil deaktiviert. Da das Backend damit keinerlei HTML mehr ausliefert, wurde zudem seine Content-Security-Policy auf `default-src 'none'` verschärft (vorher für Swagger UI auf `style-src 'unsafe-inline'` und `data:`-Bild-/Font-Quellen gelockert).

### Fixed

- **Auftragsdetails (`/orders/{id}`): Status-, Übergabe- und Report-Toasts erscheinen wieder.** Die Fehler-/Erfolgs- und Validierungsmeldungen riefen ein auf dieser Seite nicht definiertes `showToast(...)` auf und warfen still `ReferenceError`; sie laufen jetzt über die echte Toast-API (`showFrontendErrorToast`/`showFrontendSuccessToast`).

## [v0.3.30](https://github.com/krt-profit/basetool/releases/tag/v0.3.30) - 2026-05-31

### Changed

- **Das Öffnen der Seitenleiste (Sidebar) ruckelt nicht mehr.** Die Drawer-Animation läuft jetzt über `transform` statt der Layout-Eigenschaft `left` (kein Reflow pro Frame mehr) und das abdunkelnde Overlay verzichtet auf den teuren `backdrop-filter: blur(4px)`, der bei jedem Frame den gesamten Seiteninhalt neu unscharf rechnete. Optik bleibt nahezu unverändert (Abdunklung ohne Weichzeichnung).

## [v0.3.29](https://github.com/krt-profit/basetool/releases/tag/v0.3.29) - 2026-05-31

### Changed

- **Raffinerieverwaltung (`/refinery-orders`): Spalte „Gestartet" entfernt, um Platz zu sparen.** Die Tabelle zeigt nur noch „Ende"; das Startdatum bleibt in den Auftragsdetails sichtbar.

- **Raffinerieverwaltung (`/refinery-orders`): Spalte „Ende" zeigt kein Jahr mehr (`dd.MM. HH:mm`) und ist dadurch schmaler.** Reine Anzeigeänderung in Server-Render und Browser-Lokalzeit-Konvertierung.

## [v0.3.28](https://github.com/krt-profit/basetool/releases/tag/v0.3.28) - 2026-05-31

### Changed

- **Elf fehlende Datenbank-Indizes auf Fremdschlüssel-/Filterspalten ergänzt (V122).** Eine Query-Audit aller Repositories fand FK-Spalten ohne Index, die echte Lookups tragen — u.a. `job_order_handover.job_order_id` (Übergabe-Ansicht), die `ON DELETE CASCADE`-Spalte `job_order_handover_item.job_order_handover_id`, `mission_unit.ship_id`/`ship_type_id` (Schiff-/Typ-Nutzungsprüfung vor Löschung) und `refinery_yield(terminal_id, material_id)`. Spart auf wachsenden Tabellen sequentielle Scans bei Detailseiten und Lösch-/Stammdaten-Operationen.

### Fixed

- **Spalte „Staffel" in der Raffinerieverwaltung (`/refinery-orders`) zeigte für jeden Auftrag „-".** Die Listen-Projektion (`RefineryOrderMapper.toListDto`) hatte nach der Feld-Umbenennung `owningSquadron` → `owningOrgUnit` die explizite Owner-Zuordnung verloren, sodass der Besitzer in jeder Zeile leer blieb. Zusätzlich werden Spezialkommandos jetzt überall (Einsätze, Operationen, Schiffe, Lager, Aufträge, Raffinerie) mit ihrem Kürzel angezeigt, wo sie etwas besitzen — vorher fielen SK-Besitzer auf „-" zurück, weil die geteilte `SquadronMapper.orgUnitToReferenceDto`-Projektion nur Staffeln auflöste.

## [v0.3.27](https://github.com/krt-profit/basetool/releases/tag/v0.3.27) - 2026-05-31

### Fixed

- **Blueprint-Schieberegler (`/admin/blueprints`) deckt bei gestuften Stats jetzt den vollen Qualitätsbereich 0–1000 ab und stuft additive Stats korrekt.** Der Regler war auf das erste Segment beschränkt (z.B. nur 0–500 bei „Durango"/Integrity), weil er sich am rohen `quality_range` der SC-Wiki orientierte — das bei mehrstufigen Stats nur das erste Segment trägt. Die Modifier liefern jetzt eine aus allen `value_segments` abgeleitete effektive Spanne, sodass der Regler bis zum letzten Segment reicht. Zusätzlich interpoliert der Regler nur noch bei `linear`-Stats innerhalb eines Segments; bei gestuften Stats (`linear_integer_additive`, z.B. Power Pips) hält er den festen Segmentwert und springt erst beim Segmentwechsel. Reiner Anzeigefehler; die SC-Wiki-Daten waren vollständig, kein Re-Sync nötig.

## [v0.3.26](https://github.com/krt-profit/basetool/releases/tag/v0.3.26) - 2026-05-31

### Added

- **Operations-Übersicht (`/operations`) hat jetzt eine Filterleiste wie die Einsätze.** Neben Freitextsuche und dem Umschalter „Vergangene Operationen anzeigen" gibt es einen Zeitraum-Filter (Von/Bis), der über die verknüpften Missionen rechnet: „Von" begrenzt den geplanten Start der frühesten, „Bis" das geplante Ende der spätesten verknüpften Mission. Die Liste aktualisiert sich per AJAX ohne Seiten-Reload.

### Changed

- **Blueprint-Namen auf der Admin-Seite (`/admin/blueprints`) stehen jetzt in der Body-Schrift (Lato Bold) statt der Headline-Schrift (Ethnocentric).** Die langen Output-Namen sind in Lato deutlich besser lesbar; `.bp-name` verweist nun auf `--font-body` ohne Headline-Letterspacing.

- **Schiffsauswahl einer Einheit ist jetzt auf Schiffe eingetragener Teilnehmer beschränkt.** In den Einsatz-Details (`/missions`) bietet der Schiff-Dropdown beim Anlegen und Bearbeiten einer Einheit nur noch Schiffe an, deren Besitzer als Teilnehmer im Einsatz eingetragen ist; das Backend erzwingt die Einschränkung zusätzlich beim Speichern. Teilnehmer aus anderen OrgUnits sind dabei eingeschlossen — ihre Schiffe lassen sich zuweisen, sobald sie im Einsatz eingetragen sind. Ein bereits zugewiesenes Schiff bleibt beim Bearbeiten erhalten, auch wenn sein Besitzer den Einsatz inzwischen verlassen hat.

- **Gesamter Anwendungscode (Backend + Frontend) und alle Tests auf Jackson 3 (`tools.jackson`) umgestellt — Gleichstand mit dem primären Mapper von Spring Boot 4.** Der bisherige `@Primary`-Jackson-2-`ObjectMapper` entfällt; `NormalizedStringDeserializer` und die Jackson-2-Nachsicht `FAIL_ON_NULL_FOR_PRIMITIVES = false` hängen jetzt per `JsonMapperBuilderCustomizer` am tatsächlich von Spring genutzten Mapper, sodass ein im Request-Body fehlendes primitives Feld (z. B. `location.hidden` beim Raffinerieauftrag) weiterhin auf seinen Default fällt statt einen HTTP 400 auszulösen. Einzige bewusste Ausnahme ist die Thymeleaf-JS-Inlining-Brücke (`ThymeleafJavaScriptSerializerConfig`), die auf Jackson 2 bleibt, bis Thymeleaf Jackson 3 mitbringt (#294).

### Fixed

- **SC-Wiki-Item- und Blueprint-Sync verklemmt sich nicht mehr mit dem stündlichen UEX-Sync und flutet das Log nicht mehr mit `current transaction is aborted` (25P02).** Der tägliche Lauf hielt bisher in einer einzigen Transaktion Schreibsperren auf jeder gespeicherten `game_item`-/`blueprint`-Zeile über die gesamten HTTP-Abrufe — ein Deadlock gegen den UEX-Sync brach die Transaktion ab, woraufhin jedes folgende Item mit voller Stacktrace ins Log lief. Jetzt läuft jeder Item-/Blueprint-Upsert in einer eigenen `REQUIRES_NEW`-Transaktion (per Self-Proxy), der Wiki-Abruf bleibt außerhalb — ein Lock-Konflikt rollt nur diese eine Zeile zurück, wird einmal geloggt, und der Rest des Laufs synchronisiert weiter.

- **Finanz-Summe und Raffinerieauftrag-Link in den Einsatz-Details (`/missions/{id}`) erscheinen wieder in Profit-Grün.** Beide referenzierten die nicht definierte CSS-Variable `--color-dept-mining` und blieben dadurch ohne Bereichsfarbe; jetzt verwenden sie das offizielle Token `--color-dept-profit` (#239E33). Zudem nutzen die Inline-Styles der Seite die Schrift nun durchgängig über `--font-body` statt einer hartkodierten `Lato`-Angabe.

## [v0.3.25](https://github.com/krt-profit/basetool/releases/tag/v0.3.25) - 2026-05-31

### Fixed

- **SC-Wiki-Blueprint-Detail-Sync schlug bei jedem Bauplan fehl — die Stat-Modifier (Slider-Daten) der Admin-Blueprint-Seite blieben dadurch leer.** `ScWikiClient.fetchOne` ließ den reaktiven `WebClient` direkt in einen Jackson-2-`JsonNode` deserialisieren; Spring Boot 4 verdrahtet den Client aber auf den Jackson-3-Codec, der einen Jackson-2-`JsonNode` nicht instanziieren kann und jeden Detail-Abruf mit `Cannot construct instance of JsonNode` abbrach (im Log: „N detail fetch miss(es)"). Behoben, indem der Body als `String` decodiert und mit dem clienteigenen Jackson-2-`ObjectMapper` geparst wird — der Baum-/Bind-Pfad bleibt so auf einer Jackson-Version. Betraf auch den Item-Closure-Sync (gleicher `fetchOne`-Pfad); neue `ScWikiClientTest`-Fälle decken den Detail-Abruf jetzt durch den echten Codec ab. (Die generelle Umstellung auf Jackson 3 bleibt ein separater Schritt.)

- **Admin-Blueprint-Seite (`/admin/blueprints`) ließ sich nicht mehr öffnen — der Aufruf ohne Suchbegriff warf serverseitig HTTP 500 („Fehler beim Laden der Blueprints").** Die Listen-Query band den leeren (`null`) Suchparameter direkt in `LOWER(CONCAT('%', :q, '%'))`, worauf PostgreSQL den Bind als `bytea` typisierte und mit `function lower(bytea) does not exist` abbrach. Behoben durch Aufteilen in eine Query ohne Suchbegriff (kein String-Funktions-Argument) und eine Suchquery, die garantiert einen Nicht-Null-Wert erhält; ein neuer TestContainers-Repository-Test deckt den Leersuche-Pfad gegen echtes PostgreSQL ab (der bisherige Service-Test mockte das Repository und sah die SQL nie).

## [v0.3.24](https://github.com/krt-profit/basetool/releases/tag/v0.3.24) - 2026-05-31

### Changed

- **SC-Wiki-Katalog-Sync in Dev und Prod scharf geschaltet.** Die fünf Per-Sync-Flags (`krt.scwiki.{commodity,vehicle,item,blueprint,manufacturer}-sync-enabled`, im Code auf Standard aus / „ships dark“) stehen in `application-dev.yml` und `application-prod.yml` jetzt auf `true`, sodass der 24h-Scheduler die Synchronisation ausführt und die Admin-Sync-Berichte (`/admin/sync-reports`) befüllt. Erzeugt echten Traffic gegen api.star-citizen.wiki; der schwere Voll-Backfill (`sync-all-items`) bleibt aus und jedes Flag ist per Env-Var (z. B. `KRT_SCWIKI_ITEM_SYNC_ENABLED=false`) ohne Redeploy abschaltbar.

## [v0.3.23](https://github.com/krt-profit/basetool/releases/tag/v0.3.23) - 2026-05-30

### Added

- **Neue Admin-Seite „Blueprints" (`/admin/blueprints`).** Listet die gesyncten Crafting-Baupläne als Tabelle je Bau-Slot auf: welche Zutat über ihre Qualität welchen Stat-Beitrag liefert. Je Stat gibt es einen Schieberegler von min. bis max. Zutat-Qualität, der den finalen Stat-Wert live berechnet — linear oder, bei gestuften Stats, segmentweise. Filterbar nach Output-Item oder Wiki-Schlüssel. Der SC-Wiki-Blueprint-Sync erfasst dafür jetzt zusätzlich die `requirement_groups` (Stat-Modifier samt Qualitätsband und nicht-linearer `value_segments`, Demontage-Effizienz, Stat-Zusammenfassung) per Detail-Abruf pro Bauplan (neue Tabellen via V120/V121); das Feature bleibt hinter `krt.scwiki.blueprint-sync-enabled` (Standard aus).

## [v0.3.22](https://github.com/krt-profit/basetool/releases/tag/v0.3.22) - 2026-05-30

### Added

- **Einsätze können jetzt einen „Partyleiter" tragen.** In den Einsatz-Details lässt sich – mit derselben Such-Mechanik wie beim Hinzufügen eines Teilnehmers – ein bestehender Nutzer oder ein freier (auch anonymer) Name als Partyleiter eintragen; eindeutige Namen werden auf das Mitglied aufgelöst, unbekannte bleiben Freitext. Bearbeitbar durch Einsatz-Manager, sichtbar für alle; optimistisch gesperrt über einen eigenen `party_lead_version`-Zähler (V119 ergänzt die Spalten).

## [v0.3.21](https://github.com/krt-profit/basetool/releases/tag/v0.3.21) - 2026-05-30

### Added

- **Aufträge haben jetzt ein optionales Kommentarfeld.** Beim Aufgeben eines Auftrags kann ein Freitext-Kommentar (max. 1000 Zeichen) hinterlegt werden; er wird HTML-escaped angezeigt und serverseitig längenbegrenzt validiert, sodass auch anonyme Eingaben kein Injection-Risiko sind.

- **Mindestqualität eines Auftrags-Materials kann jetzt „Keine" sein.** Neben dem vorgegebenen Wert 700 lässt sich „Keine" wählen, wenn keine bestimmte Qualität gefordert ist; der Lagerbestand zählt dann Einträge aller Qualitäten (V118 macht `min_quality` nullable, V117 ergänzt die Kommentar-Spalte).

- **GitHub-Logo und „Handbuch"-Link im Footer neben der Versionsnummer.** Das GitHub-Logo verlinkt aufs Basetool-Repository, der „Handbuch"-Link aufs Repo-Wiki; beide öffnen in einem neuen Tab.

### Fixed

- **Vier fehlende deutsche Übersetzungen ergänzt — die betroffenen Stellen zeigten den rohen Schlüssel bzw. den englischen Text.** Im Frontend fehlten `operation.filter.show_past`, `operation.search` und `operation.search.placeholder` (Operations-Liste: Filter „Vergangene anzeigen" und Suchfeld), im Backend `error.rank_requirement.invalid_step` (Beförderungs-Fehlermeldung). Ein neuer Build-Test (`MessageBundleConsistencyTest`, je Modul) erzwingt ab jetzt identische Schlüsselmengen in `messages_de`/`messages_en` plus die `\uXXXX`-Schreibweise der Umlaute, damit solche Lücken nicht mehr in Produktion landen.

## [v0.3.20](https://github.com/krt-profit/basetool/releases/tag/v0.3.20) - 2026-05-30

### Added

- **Status-Pills (`.status-pill`) für Missions- und Operations-Status.** Die bisherige Klartext-Statusanzeige auf der Missions- und Operations-Liste, im Operations-Detail (eingebettete Missionstabelle) und auf dem Dashboard rendert jetzt den Design-System-Baustein — Ethnocentric-Label mit farbigem Quadrat-Punkt, Farbe nach Status (geplant blau, aktiv grün, abgeschlossen grau, abgebrochen rot). Auftrags- und Raffinerie-Status (OPEN/IN_PROGRESS) bleiben unberührt, da sie nicht zum 4-Status-Modell des Manuals gehören.

- **Wiederverwendbare Komponenten-Fragmente `fragments/components.html` (`button` / `alert` / `dataTable`)** nach den `krt-components.css`- und preview-Specs — kapseln `.btn`/`.alert`/`.krt-table` inkl. 44px-Touch-Target, Status-Tints und responsivem Tabellen-Wrapper. Repräsentative Call-Sites migriert (Operations-/SK-Alerts, Raffinerie-Abbrechen-Button, `orders-detail`-Buttons, `admin/settings`-Tabelle); dabei behoben: der „Materialsammelübersicht"-Link referenzierte das undefinierte `var(--color-text)` (jetzt `.btn btn-secondary`), und der SK-Fehlerkasten wechselt vom bespoke `hud-box` zur `.alert`-Komponente. Neuer `ComponentFragmentsRenderTest` rendert alle drei Fragmente über die Thymeleaf-Engine und fängt damit Render-Zeit-Fehler im Build ab.

### Changed

- **„Mono"-Kontexte folgen dem Design-System-Update (#274): Lato mit Tabellenziffern statt System-Monospace.** `--font-mono` ist jetzt `'Lato', sans-serif` (die Marke besitzt keine Monospace-Schrift); `code`/`kbd`/`samp` und die rechtsbündigen Zahlen-Spalten (Material-Preismatrix, Profit-Rechner, Material-Detail, Frequenz-Anzeige, Footer-Version) verweisen darauf und setzen `font-variant-numeric: tabular-nums` für Spaltenbündigkeit. Die `+`/`-`-Aufklapp-Glyphen der Stammdatenverwaltung bleiben bewusst literal `monospace` (UI-Glyph, kein `--font-mono`-Datenkontext).

- **Dezenter Waben-Hintergrund (`honeycomb-bg.svg`) auf allen Seiten.** Das faint-ambient Wabenmuster des Design-Systems (orange `#E77E23`-Striche bei 0.1 Deckkraft) liegt jetzt als 46×26.56px-Kachel mit `background-attachment: fixed` über dem schwarzen Seiten-Hintergrund — eine subtile Textur, die auch durch die transluzenten `.hud-box`-Flächen schimmert. Neues Asset `static/images/honeycomb-bg.svg`; vom bestehenden CSP `img-src 'self'` abgedeckt, keine Konfigänderung nötig.

- **Bereichsfarben-Tokens in `styles.css` auf die offiziellen Namen des Corporate Design Manual V2 umbenannt (Rendering unverändert).** Die drei vertauschten Department-Tokens heißen jetzt `--color-dept-raumueberlegenheit` (#37BBC0), `--color-dept-forschung` (#355DDC) und `--color-dept-sub-radar` (#A3000A); die alten Namen (`combat`/`research`/`marine`) bleiben als deprecated Aliase erhalten. Die Statusfarben (`--color-danger`/`--color-info` …) sind jetzt direkt per Hex gesetzt statt aus Department-Tokens abgeleitet, und ~15 Templates/JS, die `--color-dept-combat` als Rot zweckentfremdeten, verweisen nun auf `--color-danger`.

- **Grauskala in `styles.css` an die 4-stufige Skala des Manuals angeglichen (Rendering unverändert).** `--color-gray-4` ist jetzt `#141414` (die dunkle Standard-Oberfläche, wie im Manual), der Eingabe-/Tabellenkopf-Halbton `#1C1C1C` heißt jetzt `--color-surface-input`, und das doppelt vergebene `--color-gray-5` wurde entfernt. ~33 Templates/CSS-Dateien wurden mitgezogen: Eingabe- und Flächenfüllungen verweisen auf `--color-surface-input`, ehemalige `--color-gray-5`-Flächen auf `--color-gray-4`.

- **Restliche Design-System-Tokens aus `colors_and_type.css` in `styles.css` übernommen.** Neu definiert: `--color-white`, `--color-secondary`, `--color-danger-hover`/`--color-success-hover`, `--font-mono`, die Typo-Skala sowie Form-, Rahmen-, Glow-, Schatten-, Abstands- und Layout-Tokens (`--radius-*`, `--border-*`, `--glow-*`, `--shadow-*`, `--space-*`, `--touch-target`, `--content-max`, `--measure`). Die `--color-secondary`-Fallbacks und die hartcodierten Button-Hover-Farben verweisen jetzt auf Tokens; nebenbei behebt das das nie definierte `var(--color-white)` der SK-„Mission Manager"-Rollen-Badge (`/admin/special-commands/{id}`), deren Text dadurch grau statt weiß war. Das Umstellen weiterer hartcodierter Literale (Glows, Radius, Abstände) auf die neuen Tokens bleibt ein separater Follow-up.

- **Hartcodierte Marken-Idiome in `styles.css` auf die Design-System-Tokens umgestellt (Rendering unverändert).** Orange-Bloom-/Schatten-Werte verweisen jetzt auf `--glow-*`/`--shadow-*`, `border-radius: 999px` auf `--radius-pill`, `min-height: 44px` auf `--touch-target` und die Spaltenbreiten auf `--content-max`/`--measure`. Die Typo-Skala (`--fs-*`/`--fw-*`) wurde hier noch literal belassen und in einem Folgeschritt nachgezogen (eigener Changelog-Eintrag).

- **Abstände und Rahmen in `styles.css` auf Tokens umgestellt (Rendering unverändert).** `padding`/`margin`/`gap`-Werte auf der `--space`-Skala (`0.25`/`0.5`/`0.75`/`1`/`1.5`/`2rem`) verweisen jetzt auf `--space-1`…`--space-8`; off-scale-Werte (z. B. `0.35rem`) bleiben literal. 1px-Hairlines → `--border-hairline`, 1px-Akzentrahmen → `--border-accent`, die Heading-Rule von `header` und `th` → `--border-heading`; die dekorativen HUD-Eckklammern (`2px solid`) bleiben literal, da der Wert dort keine Heading-Rule ist. Umsetzung per kommentar-geschütztem, property-skopiertem Regex-Pass; eine Rück-Substitutions-Prüfung bestätigt Byte-Gleichheit zum Vorzustand abgesehen von den Token-Swaps.

- **Design-System-Komponentenlayer in `styles.css` vervollständigt.** `.btn` auf die volle Manual-Spezifikation gebracht (Icon-Gap, Tracking, eckige Ecken, Token-Schrift) plus den Disabled-Zustand `.btn:disabled`, den Department-Chip `.dept-tag` und die Element-Defaults `::selection` (orange Markierung) und `strong/b` ergänzt; der durch `.btn { border: none }` verschluckte Rahmen von `.btn-outline-danger` ist gefixt und zeigt jetzt seinen roten Rand. Sieben Lösch-Buttons, die ihre Rotfärbung per Inline-`style` trugen (Ankündigung, Stammdaten, Schiffsdaten, Mission-Detail), nutzen jetzt die `.btn-danger`-Klasse.

- **Typo-Skala in den Stylesheets auf `--fs-*`/`--fw-*`-Tokens umgestellt (Rendering unverändert).** Die On-Scale-`font-size`-Literale (`0.7`/`0.8`/`0.9`/`1`/`1.5`/`2rem`) und die `font-weight`-Zahlwerte (`300`/`700`) in `styles.css` plus den drei Seiten-Stylesheets (Material-Übersicht, Personal-Inventory, Promotion-Admin) verweisen jetzt auf die Tokens; Off-Scale-Werte, `em`/`px`-Einheiten und die `@font-face`-Schnitt-Definitionen bleiben literal. Inline-`style`-Literale in den Templates bleiben unangetastet (gehören in einen separaten Klassen-Refactor, nicht in Inline-Tokens). Jeder Swap ist literal→wertgleicher Token, per Diff-Review bestätigt.

- **Eckige-Ecken-Regel des Design-Systems durchgesetzt — 27 `border-radius`-Abrundungen (2–4px) entfernt.** Diverse inline-gestylte Elemente (Inaktiv-Badges der Stammdatenverwaltung, Material-/Auftrags-/Raffinerie-Boxen, Hinweis-Boxen) und die Confirm-Dialog-Buttons trugen entgegen der „square-first"-Regel des Manuals eine 2–4px-Rundung; alle auf `var(--radius-none)` gezogen. Nur Pills/Badges und das Radio-Control bleiben rund. Kleine sichtbare Korrektur, keine Funktionsänderung.

- **Wiederholte Inline-`style`-Deklarationen in CSS-Klassen extrahiert (laufend, ~370 Stück bisher, 890→520).** Die häufigsten Inline-Muster sind jetzt Klassen: neu `.field-error` (Feld-Validierung ×37), Adoption der DS-`.form-label` (×18 — die Labels werden dabei DS-konform fett), Form-Inputs auf `.w-full` + DS-Input-Styling (×16, mit minimaler Padding-Angleichung an die DS-Norm), Material-Row-Breiten auf `.flex-1`/`.flex-2`/`.mb-0` (×46), plus Utilities (`.m-0`, `.mt-0`, `.mb-2`, `.nowrap`, `.text-center`, `.w-full`, `.w-1`). Redundantes inline `min-height: 44px` auf `.btn`-Buttons entfernt (die Klasse liefert es bereits). Rendering-neutral außer den fett gewordenen Labels und der Input-Padding-Angleichung (beide bewusst DS-konform); JS-getoggeltes `display:none` und dynamische `th:style` bleiben bewusst inline. Weitere Inline-Styles folgen.

### Fixed

- **`GET /api/v1/missions/next` paginiert nicht mehr im Arbeitsspeicher (Hibernate-WARN `HHH90003004`).** Die `findFirst…`-Queries für die nächste Mission zogen die Collections (`participants`/`assignedUnits`) per `@EntityGraph` zusammen mit dem impliziten `limit 1` — eine Kombination, die SQL-seitig nicht geht, also lud Hibernate alle künftigen Missionen samt Sub-Collections in den Speicher und paginierte dort (bei jedem Home-Aufruf). Der Service holt jetzt erst die nächste Mission ohne Graph (`limit 1`) und lädt sie dann per ID über das bereits gegraphte `findById` nach. Verhalten unverändert — nur ohne den In-Memory-Sweep und die Warnung.

## [v0.3.19](https://github.com/krt-profit/basetool/releases/tag/v0.3.19) - 2026-05-29

### Added

- **R9 — Destructive-Cleanup-Roadmap für den SC-Wiki-Sync (Plan §11 R9).** Neues Dokument `SC_WIKI_SYNC_DESTRUCTIVE_ROADMAP.md` plant den finalen, irreversiblen Schritt: den V117-Drop von `material.is_manual_entry` + dem synthetischen `ship_type.description`. **Reines Planungsdokument, kein Code-/Schema-Change** in diesem PR (analog `R8_DESTRUCTIVE_ROADMAP.md` für die SK-Arbeit). Hintergrund per Investigation: beide Spalten werden noch von UI gelesen (§13 #9 aufgelöst — `is_manual_entry` trägt den Admin-Materials-"Manual"-Badge/Filter; `ship_type.description` wird via `ShipTypeDto` in `ship-data.html` + `admin/mission-data.html` gerendert), daher staged die Roadmap zuerst die Reader-Migration (`is_manual_entry` → `source_systems='MANUAL'`; `ship_type.description` → `description_en`/`description_de`) und einen Soak vor dem Drop — gegated auf einen sauberen R8-Soak, jeder Schritt einzeln reversibel, nur der Drop nicht. V-Nummer-Drift: V115/V116 gingen an R7/R8, der Drop ist V117. Plan §11 R9 + Runbook §9 verweisen jetzt auf die Roadmap.

- **R8 — Soak + V116 `is_manual_entry`-Cleanup (Plan §11 R8).** Neue Migration **V116** überführt die Provenienz manuell angelegter Materials in die kanonische `source_systems`-Enum: one-shot, idempotent `UPDATE material SET source_systems='MANUAL' WHERE is_manual_entry=TRUE AND source_systems<>'MANUAL'` — keine Schemaänderung (der `MaterialSourceSystem.MANUAL`-Wert existiert bereits seit R1). Bereitet den destruktiven R9-Drop von `material.is_manual_entry` vor; die MANUAL-Kennzeichnung lebt danach in `source_systems` weiter. **V-Nummer-Drift:** Plan §7 sah V115 vor, aber R7 nahm V115 für `game_item_price` → R8 ist V116, der R9-Drop verschiebt auf V117. Der Rest von R8 ist operativ (Soak): alle Sync-Flags an, ~2 Wochen Beobachtung von DB-Wachstum / Scheduler-Laufzeiten / Sync-Report-Event-Mix, bevor der irreversible R9-Schritt läuft — **kein** Default-on-Code-Change. Tests: TestContainers-`V116MigrationTest` (Migration applied, Post-Invariant, Flip-Logik gegen kontrollierte Zeilen via Repository). Runbook §8 vollständig.

### Changed

- **UEX-Sync läuft jetzt einmal täglich statt stündlich.** `krt.uex.scheduler-delay` defaultet auf 24 h (vorher 1 h); der SC-Wiki-Sync war bereits täglich. UEX-Preise und -Katalog ändern sich langsam genug, dass ein täglicher Lauf die Daten frisch hält und die Upstream-API schont. Override per `KRT_UEX_SCHEDULER_DELAY` weiterhin möglich.

### Fixed

- **UEX-Item-Sync degradiert keinen vom Wiki gesetzten spezifischen `game_item.kind` mehr auf `GENERIC` (SC-Wiki-Sync-Audit HIGH-2).** `UexItemSyncService` überschrieb `kind` bedingungslos mit dem aus der UEX-Kategorie abgeleiteten Wert — eine cross-gelistete Paint, die der Wiki-Sync als `VEHICLE_ITEM` (über `/vehicle-items`) ablegt, fiel beim nächsten stündlichen UEX-Lauf unter der "Liveries"-Kategorie auf `GENERIC` zurück (§6.3.1: "more-specific wins ... applies whether UEX or Wiki was the last writer"). Die Regel lebt jetzt als geteilter `GameItemKind.mergeMoreSpecific(...)`-Arbiter, den beide Sync-Seiten verwenden (vorher dupliziert die Wiki-Seite); der UEX-Sync ruft ihn statt des bedingungslosen `setKind` auf. Neuer `GameItemKindTest` plus UEX-Downgrade-Regressionstest.

- **`material.is_visible` wird jetzt auf den Trading-Read-Pfaden erzwungen — unsichtbare Wiki-only-Commodities lecken nicht mehr in Katalog + Material-Picker (SC-Wiki-Sync-Audit HIGH-1).** R3 schrieb das Flag (`is_visible=false` für unreviewte `WIKI_ONLY`-Zeilen, §4.3), aber keine Query filterte darauf: `GET /api/v1/materials` (Katalogliste) und `/lookup` (Typeahead) hätten Einträge wie "Ace Interceptor Helmet" in Trading-Flows gezeigt, sobald `krt.scwiki.commodity-sync-enabled` an ist. Fix: die Trading-Pfade (Katalogliste, `/lookup`, Job-Order-Picker) filtern jetzt auf `is_visible=true`; der Admin-Materials-Katalog ruft `includeHidden=true` und sieht weiter alles — mit "Versteckt"-Badge und Sichtbar-Toggle zum Review/Unhide (§4.3). `MaterialDto.isVisible` (Backend + Frontend-Mirror) plus Service-/Controller-Tests neu.

- **Von UEX adoptierte Wiki-only-Commodities werden jetzt sichtbar und als `BOTH` markiert (SC-Wiki-Sync-Audit M1).** Wenn der UEX-Commodity-Sync eine zuerst vom Wiki angelegte Zeile (`WIKI_ONLY`, unsichtbar) per Namens-Match adoptiert, blieb sie bisher `WIKI_ONLY` und unsichtbar — d.h. eine reale, jetzt UEX-geführte Handelsware tauchte nie im Trading auf. `UexCommodityService` flippt die Provenienz nun auf `BOTH` und setzt `is_visible=true` (UEX-Präsenz validiert die Ware, §6.1); konsistent mit den UEX-Item-/Vehicle-Syncs. MANUAL-Zeilen bleiben unangetastet (R9 Step 1).

- **`ship_type`-Wiki-Orphan-Sweep gated jetzt auf `scwiki_synced_at` statt `external_uuid` (SC-Wiki-Sync-Audit L1).** Vorher konnte der Sweep einer UEX-only-Vehicle-Zeile (die ein `external_uuid` vom UEX-Sync trägt, aber nie vom Wiki beschrieben wurde — z.B. Idris-M/Polaris) fälschlich ein `scwiki_deleted_at` ("missing from Wiki") stempeln. Jetzt erfasst er nur Zeilen, die der Wiki tatsächlich geschrieben hat — symmetrisch zum `game_item`-Sweep.

- **SC-Wiki-Audit Low-Findings + Mode-B/Item-Price-Memory-Hardening abgearbeitet.** (M2) `ScWikiItemSyncService`-Mode-B (~12 700 Zeilen) und `UexItemPriceSyncService` (~24 000 Zeilen) flushen+clearen den Persistence-Context jetzt alle 500 Upserts statt den ganzen Pool in einer Transaktion zu halten (vermeidet O(n²)-Dirty-Checks + Heap-Druck; beide weiterhin feature-flag-off). (L2) Die nie emittierte `SyncEventType.SYNC_RACE_CONFLICT`-Konstante entfernt + die tatsächliche Log-and-skip-Policy dokumentiert. (L4) 38 `admin.syncReports.*` / `admin.materialAlias.*` / `nav.*` / `error.*`-Keys in die Default-`messages.properties` ergänzt (waren nur in DE+EN). (L5) `Material.sourceSystems` `length=16` (Parität mit `GameItem`/`ShipType`). (L3/L6) Die bewussten §6.3.5-Name- und Alias-Listen-Pagination-Abweichungen explizit im Javadoc dokumentiert; `BlueprintRepository`-Sweep um den `scwiki_uuid IS NOT NULL`-Guard ergänzt.

## [v0.3.18](https://github.com/krt-profit/basetool/releases/tag/v0.3.18) - 2026-05-29

### Added

- **R7 — UEX-Item-Preise + `game_item_price` (Plan §11 R7).** Neue Migration **V115** legt `game_item_price` an (eine Zeile pro `game_item` × `terminal`, FKs + `UNIQUE(game_item_id, terminal_id)`, Index auf `terminal_id`). **V-Nummer-Drift:** der Plan §7 hatte das als V114 vorgesehen, aber V113 ging an R3s `external_sync_report` und V114 an R4s Blueprints — R7 nimmt V115, R8/R9-Cleanups verschieben sich auf V116/V117. Neuer `UexItemPriceSyncService` (UEX-Scheduler, nach dem Item-Katalog) läuft `/items_prices_all` (~24 000 Zeilen, größtes UEX-Payload, vom 16-MB-WebClient-Buffer gedeckt) und upserted je (Item, Terminal): `id_item → game_item` via `uex_item_id`, `id_terminal → terminal` via `id_terminal`. Ein Preis für ein **noch nicht katalogisiertes Item wird übersprungen** (der Item-Katalog legt Zeilen an und läuft früher im selben Tick) — anders als der Commodity-Price-Sync, der Materials auto-anlegt; unbekannte Terminals ebenso. Gegateter `clearStalePrices`-Sweep (wie `material_price`): nullt die Preisspalten der (Item, Terminal)-Paare, die UEX nicht mehr liefert, gegated auf non-empty Seen-Set; leeres Feed bricht vor dem Sweep ab. Display-only und hinter `krt.uex.item-price-sync-enabled` (Default `false`) — ships dark; die Tabelle bleibt inert, bis ein Operator den Flag setzt. **Live-Feed-Befund (geprobt `/items_prices_all`, Game 4.8.0):** das Endpoint liefert nur `id_item`/`id_terminal`/`price_buy`/`price_sell`/`date_modified` — die §6.7-Spalten `price_rent`/`status_buy`/`status_sell`/`game_version` werden angelegt, bleiben aber NULL (reserviert für eine spätere reichere Quelle). `UexClient.getItemPrices()` + `UexItemPriceDto` neu (die `itemsPricesEndpoint`-Property + der `itemPriceSyncEnabled`-Flag kamen schon mit R2). Tests: Mockito-`UexItemPriceSyncServiceTest` (Flag-Gate, Empty-Feed-Abort, Upsert neu/refresh, Skip unbekanntes Item/Terminal, Sweep-Gating), TestContainers-`V115MigrationTest`, `UexSchedulerTest` um den neuen Schritt erweitert. Runbook §7 vollständig.

## [v0.3.17](https://github.com/krt-profit/basetool/releases/tag/v0.3.17) - 2026-05-29

### Added

- **R6 — SC-Wiki Manufacturer-Reconciliation (Plan §11 R6).** Neuer `ScWikiManufacturerSyncService`, "dark" ausgeliefert hinter `krt.scwiki.manufacturer-sync-enabled` (Default `false`), **keine Flyway-Migration** (die `scwiki_uuid`/`scwiki_code`-Spalten kamen mit R1/V107). Reine Anreicherung: paginiert `/api/manufacturers` (~130 Zeilen) und stempelt `scwiki_uuid`/`scwiki_code`/`scwiki_synced_at` auf die vom UEX-Sync angelegten Manufacturer-Zeilen — Auflösungskette `scwiki_uuid` → `name` (case-insensitive) → `abbreviation == code` (case-insensitive). Legt **keine** neuen Zeilen an (Wiki-Hersteller ohne UEX-Pendant werden übersprungen) und überschreibt **nie** den UEX-kanonischen `name`/`abbreviation`/`industry`. Ein bereits mit einer **anderen** Wiki-UUID verlinkter Kandidat bleibt unangetastet und wird als `MANUFACTURER_MISMATCH` geloggt; ein Erst-Link loggt das neue `MANUFACTURER_LINKED`-Event, ein Refresh schweigt (Steady-State-Läufe bleiben leise). Gegateter Orphan-Sweep (`Manufacturer.markScwikiDeletedExcept`, nur Wiki-verlinkte Zeilen) feuert nur bei non-empty Seen-Set (§8.7). Abweichung: die Plan-Kette §6.4 nennt als 3. Stufe `industry+name`; da das Wiki-Manufacturer-Payload keine Industry liefert, nutzt der Code stattdessen den `code↔abbreviation`-Match (lokal UNIQUE, hebt die Link-Rate für Hersteller mit abweichendem Vollnamen). Scheduler ruft die Reconciliation als 5. Schritt nach dem Blueprint-Sync auf, hinter eigenem Flag. Tests: Mockito-`ScWikiManufacturerSyncServiceTest` (Flag-Gate, Link per Name/UUID/Code-Fallback, Erst-Link vs. Refresh, No-Overwrite der UEX-Felder, Konflikt → MISMATCH + Skip, Orphan-Gating). Runbook §6 vollständig.

## [v0.3.16](https://github.com/krt-profit/basetool/releases/tag/v0.3.16) - 2026-05-29

### Added

- **R5 — SC-Wiki Full-Item-Backfill / Mode B (Plan §11 R5).** Rein additive Sync-Logik auf `game_item`, **keine Flyway-Migration**. `ScWikiItemSyncService` bekommt einen zweiten Modus, der greift, wenn `krt.scwiki.item-sync-enabled=true` UND `krt.scwiki.sync-all-items=true` gesetzt sind; bei `sync-all-items=false` (Default) läuft der R4-Closure-Mode unverändert weiter — R5 wird also "dark" ausgeliefert. Mode B pagt die sieben per-Kind-Wiki-Endpoints (Kind aus dem Quell-Endpoint abgeleitet, §6.3.1, most-specific-first) und anschließend ein GENERIC-`/api/items`-Catch-all-Residual, damit der volle ~12 700-Zeilen-Pool abgedeckt ist; neue Items landen `WIKI_ONLY`, bestehende Zeilen werden Wiki-befüllt und `source_systems` `UEX_ONLY → BOTH` geflippt. Konfliktpolitik wie R4 (§6.3.5): der UEX-kanonische `name` + Capability-Flags werden nie überschrieben; `kind` nutzt den More-Specific-Wins-Tie-Breaker (nie Downgrade auf GENERIC); `manufacturer` wird für neue `WIKI_ONLY`-Zeilen nur gegen die bestehende Manufacturer-Tabelle aufgelöst (keine Stubs — R6 reconciled), auf bestehenden Zeilen bleibt er sticky. **Filter-Werte (Plan §13 offene Frage #3, per Live-Probe von `GET /api/items/filters` auf Game 4.8.0 gelöst):** `filter[classification]` prefix-matcht und nur `/api/armor` liefert ohne Filter den vollen Pool (§3.4 Quirk #1), daher ist `krt.scwiki.armor-filter=FPS.Armor` Pflicht — die übrigen Endpoints liefern ihren Kind nativ. Alle Filter sind als `krt.scwiki.*-filter`-Properties konfigurierbar (Defaults: armor=`FPS.Armor`, clothes=`FPS.Clothing`, food=`FPS.Consumable.Food`, Rest leer); `krt.scwiki.backfill-kind-sanity-cap` (Default 9 000) überspringt jeden Kind-Pass, der trotzdem pool-groß zurückkommt (z. B. ein geleerter `armor-filter`) und unterdrückt dann den Orphan-Sweep. `ScWikiClient.fetchAllPages` bekommt `filter[...]`-Support. Der Cross-Kind-Orphan-Sweep (`game_item.markScwikiDeletedExcept`, nur Wiki-geschriebene Zeilen via `scwiki_synced_at IS NOT NULL`) feuert nur, wenn jeder Pass — alle sieben Kinds plus Residual — Daten lieferte; ein leerer/304/Sanity-Cap-Pass unterdrückt den Sweep (§8.4/§8.7), damit ein Wiki-Ausfall die Wiki-Seite nie leerwischt. Abweichung dokumentiert: §8.4 nennt "7 Endpoints", die Implementierung ergänzt den §6.3.1-GENERIC-Catch-all über `/api/items`, damit der §11-R5-Vollbackfill (~12 700, ~7 000 neue WIKI_ONLY inkl. Paints/Cargo) tatsächlich erreicht wird. Tests: neuer Mockito-`ScWikiItemSyncServiceBackfillTest` (Modus-Selektion, per-Endpoint-Kind-Derivation, WIKI_ONLY-Create, BOTH-Flip, More-Specific-Tie-Breaker, Sanity-Cap, Junk-Skip, Manufacturer-Resolve, Orphan-Gating); der Closure-Test bleibt grün. Runbook §5 vollständig.

## [v0.3.15](https://github.com/krt-profit/basetool/releases/tag/v0.3.15) - 2026-05-29

### Added

- **R4 — SC-Wiki-Blueprints + Item-Closure + Vehicle-Fill (Plan §11 R4).** Drei weitere SC-Wiki-Syncs, alle "dark" ausgeliefert (je ein per-Sync-Flag, Default `false`). Migration V114 legt den Blueprint-Graph an (`blueprint` + `blueprint_ingredient` + `blueprint_dismantle_return`); die Ingredient-CHECKs sind ggü. Plan §6.3.3 **gelockert** (erzwingen Kind/FK- und Kind/Mengen-Exklusivität, erlauben aber NULL-FK, damit unaufgelöste Zeilen ihren Wiki-Snapshot zur späteren Re-Resolution behalten — §8.2). `ScWikiBlueprintSyncService` (`krt.scwiki.blueprint-sync-enabled`) zieht `/api/blueprints` und upserted den Rezept-Graphen — RESOURCE→`material` (scwiki_uuid→Alias→Name), ITEM→`game_item` (external_uuid); unaufgelöste Zeilen loggen `UNRESOLVED_INGREDIENT`. `ScWikiItemSyncService` Closure-Mode (`krt.scwiki.item-sync-enabled`) holt `GET /api/items/{uuid}` für jede `game_item.external_uuid` (+ Blueprint-Item-Refs), füllt die Wiki-Spalten (classification/mass/dimensions/descriptions) und flippt `source_systems` `UEX_ONLY→BOTH`; 404 → `WIKI_MISSING`. `ScWikiVehicleSyncService` (`krt.scwiki.vehicle-sync-enabled`) paginiert `/api/vehicles`, füllt `scwiki_slug`/`game_name`/`description_de` auf `ship_type` (EN/className/vehicle_inventory nur wenn UEX leer ließ), rührt UEX-eigene `is_*`-Flags/Dimensionen/Fuel nie an. Konfliktpolitik §6.3.5: Name + Manufacturer bleiben UEX-kanonisch (Manufacturer-Reconciliation ist R6). Scheduler ruft alle vier Syncs (Commodity/Vehicle/Item/Blueprint) in Abhängigkeitsreihenfolge auf, jeweils self-guarded + eigenes try/catch. `ScWikiClient.fetchOne` (envelope-toleranter Single-Resource-Fetch) neu. Tests: Mockito für alle drei Syncs (RESOURCE+ITEM-Ingredients, unaufgelöst, Trailing-Line-Drop, Closure-Scoping, Vehicle-Konfliktpolitik) + TestContainers V114 (inkl. CHECK-Verhalten). Runbook §4 vollständig.

## [v0.3.12](https://github.com/krt-profit/basetool/releases/tag/v0.3.12) - 2026-05-29

### Added

- **R3 — SC-Wiki-Commodity-Merge (Plan §11 R3).** Erste SC-Wiki-Synchronisation, aber "dark" ausgeliefert: Code, Audit-Tabelle und Admin-Seite landen, es entsteht aber kein Live-Wiki-Traffic, bis ein Operator `krt.scwiki.commodity-sync-enabled` auf `true` setzt (Default `false`, analog zum R7-Price-Flag). Migration V113 legt die append-only `external_sync_report`-Tabelle an (§8.8). Neuer `SyncReportService` sammelt Sync-Findings (Retention: letzte 30 Läufe pro Quelle). Neuer `ScWikiCommoditySyncService` zieht `/api/commodities`, filtert Junk per Name-Pattern (§8.9), und merged den Rest über die Auflösungskette `scwiki_uuid` → Alias-Tabelle → exakter Name → kanonischer Name mit Multi-Match-Ablehnung (§8.1.1). Konfliktpolitik §4.6: UEX bleibt kanonisch für `name`/`code`/`kind`/Preise/Flags; die Wiki-Seite schreibt nur `scwiki_*` + `density_g_per_cc` und flippt `source_systems` `UEX_ONLY → BOTH`. Wiki-only-Commodities werden als neue `WIKI_ONLY`-Zeilen **unsichtbar** (`is_visible=false`) angelegt, damit sie die Trading-Flows nicht verschmutzen, bis ein Admin sie prüft (§4.3). Admin-Sync-Berichte unter `/admin/sync-reports` (+ `/scwiki`, `/uex`), ADMIN-gated, gespeist von `GET /api/v1/sync-reports`. Der Wiki-Scheduler-Master-Schalter `krt.scwiki.scheduler-enabled` default jetzt `true` (der eigentliche Merge bleibt durch das per-Sync-Flag dunkel). Abweichung vom Plan-§8.1-Pseudocode (im Service dokumentiert): ein kanonischer Multi-Match überspringt die Zeile (loggt `MULTI_MATCH_AMBIGUOUS`), statt eine `WIKI_ONLY`-Zeile anzulegen — sonst würde die gestempelte Wiki-UUID den späteren Admin-Alias-Fix dauerhaft verdecken. Tests: Mockito für Junk-Filter/Auflösungskette/Konfliktpolitik/Orphan-Gating + SyncReportService, TestContainers für V113, MockMvc-ADMIN-Gate. Runbook §3 vollständig.

## [v0.3.11](https://github.com/krt-profit/basetool/releases/tag/v0.3.11) - 2026-05-29

### Added

- **R2 — UEX-Item-Katalog + UEX-Vehicle-Härtung (Plan §11 R2).** UEX-Seite des SC-Wiki-Syncs end-to-end live; keine Wiki-Traffic (R3+). Drei Flyway-Migrationen: V110 legt `game_item` an (joint UEX/Wiki-Tabelle keyed by `external_uuid` UNIQUE, R2 NULLABLE wegen ~30 % leerer UEX-UUIDs; FKs zu `manufacturer` / `uex_category` / `ship_type`); V111 erweitert `ship_type` um `external_uuid`, `uex_vehicle_id`, 36 `is_*`-Capability-Flags, Mass/Dimensions/Fuel/URLs/`description_en` sowie das R2 `source_systems`-Provenance-Feld; V112 fixt R1's V109 (fehlende `created_at`/`updated_at` auf `uex_category`). Neuer `UexCategoryRefService` synct die ~98 Kategorien; neuer `UexItemSyncService` walkt sie und upserted `game_item` per `findByUexItemId` → `findByExternalUuid` → create-new, mit Kind-Derivation aus dem Section-Namen (Armor → ARMOR, "Vehicle Weapons" → VEHICLE_WEAPON, Systems/Utility/Avionics → VEHICLE_ITEM, …). `UexManufacturerService` persistiert jetzt auch Item-Hersteller (nicht mehr nur Vehicle-Filter), matched per `uex_company_id` mit Namen-Fallback. `UexVehicleService` auf UUID-First-Chain umgebogen — der Namens-Fallback backfillt `external_uuid` + `uex_vehicle_id` auf der ersten Sync-Welle nach R2 (Ersatz für die separat geplante V112-Daten-Migration). Orphan-Sweeps für `game_item` und `ship_type` jeweils gegated auf ein non-empty Seen-Id-Set. SC-Wiki-Scheduler bleibt aus (R2 produziert null Wiki-Traffic). Tests: WireMock-freier Mockito-Stack für jeden neuen Service plus TestContainers-Migration-Tests für V110/V111; `UexSchedulerTest` um die neue Topologie erweitert. SK-/Wiki-Plan-Doku + Deployment-Runbook §2 mitgepflegt.

### Changed

- **Datenschutzerklärung (`/privacy`): Cookie- und Speicher-Abschnitt vervollständigt und korrigiert.** Nennt jetzt alle real gesetzten Cookies (`SESSION`, `KRT_LOCALE`, `orders_filter_status`/`orders_filter_scope`, `SSO_ATTEMPTED`, `XSRF-TOKEN`, Keycloak-Login-Cookies) sowie den lokalen Browser-Speicher (Local/Session Storage für Ansichtseinstellungen) mit Zweck und Laufzeit; Rechtsgrundlage § 25 Abs. 2 Nr. 2 TDDDG, kein Cookie-Banner nötig (kein Tracking/Analytics, Art. 13 DSGVO erfüllt). Zusätzlich `KRT_LOCALE`-Cookie-Laufzeit von 365 auf 90 Tage (3 Monate) reduziert.

## [v0.3.10](https://github.com/krt-profit/basetool/releases/tag/v0.3.10) - 2026-05-29

### Added

- **R1 — Foundation für den SC-Wiki + UEX-Items-Sync (Plan §11 R1).** Additives Plumbing für die kommenden R2-R9-Phasen, ohne Verhaltensänderung für Bestand: Flyway V106-V109 erweitern `material` um zehn Wiki-Cross-Ref-Spalten plus `is_visible` (Default TRUE — Bestand bleibt sichtbar) und `source_systems` (Default `'UEX_ONLY'`), `manufacturer` um zehn UEX/Wiki-Spalten, und legen die neue Tabelle `material_external_alias` (inkl. sechs verifizierter SC-Wiki→UEX-Seed-Aliase aus Plan §4.1+§4.2) sowie `uex_category` an. Neues `integration.scwiki`-Package mit `ScWikiClient` (paginiert, ETag-gecached, Rate-Limit-gepaced), `ScWikiScheduler`-Skeleton (R1-Default `krt.scwiki.scheduler-enabled=false` — keine Live-Traffic), `ScWikiProperties` und dediziertem `AsyncConfig.SCWIKI_EXECUTOR`-Pool. Admin-CRUD `/admin/material-aliases` (Thymeleaf + REST `/api/v1/material-external-aliases`, ADMIN-gated) für die Pflege der curated Cross-References. ArchUnit-Regel verlangt `ScWikiClient`-Injektion in jeder Klasse des neuen Packages; neuer Deployment-Runbook `SC_WIKI_SYNC_DEPLOYMENT_RUNBOOK.md` (R1 vollständig, R2-R9 als Stubs). Construction-Material-Triplet und Combat Supplies bewusst NICHT geseeded (Plan §4.2.1 — Admin pflegt manuell nach In-Game-Verifikation nach).

## [v0.3.9](https://github.com/krt-profit/basetool/releases/tag/v0.3.9) - 2026-05-27

### Changed

- **Nutzungsbedingungen-Seite (`/terms`) mit echten Inhalten gefüllt.** Statt eines Platzhaltersatzes liefert die Seite jetzt 14 strukturierte Abschnitte (Geltungsbereich, Zweck, Zugang, Pflichten, Nutzerinhalte, Rollen/Sichtbarkeit, Verfügbarkeit, Sperrung, Geistiges Eigentum/Marken inkl. CIG-Disclaimer, Haftung, Datenschutzverweis, Änderungen, anwendbares Recht, Salvatorische Klausel) plus Stand-Datum. Vollständig zweisprachig (DE + EN + Default-Fallback), neue i18n-Keys folgen dem `privacy.*`-Schema; alter `terms.example_text`-Key entfernt.

### Fixed

- **KRT-Logo in der Header-Leiste wurde auf `/refinery-orders`, `/refinery-orders/{id}`, `/terms` und `/ship-data` horizontal gestaucht dargestellt.** Diese vier Templates setzten am `<img>` sowohl `width="50"` als auch `height="50"` — das KRT-Logo ist breiter als hoch, ein erzwungenes Quadrat schrumpft es seitlich. Fix: nur noch `height="50"` setzen wie in den 44 anderen Templates, damit die natürliche Aspect-Ratio erhalten bleibt.

- **"Zuordnen zu"-Picker im Neuer-Raffinerieauftrag-Formular sass eine Zeile zu hoch.** Das `owner-picker`-Fragment rendert ein `.form-group.owner-picker` ohne Inline-Style; alle Geschwister-Felder in `.material-row` setzen explizit `flex: X; margin-bottom: 0;`. Damit greift bei `align-items: flex-end` der globale `.form-group { margin-bottom: 1.25rem; }`-Default aus `styles.css` und schob den Picker ueber die Baseline. Spezifische Regel `.material-row > .owner-picker { flex: 2; margin-bottom: 0; }` im Template-Style angleicht den Picker an die Besitzer-Dropdown daneben.

## [v0.3.8](https://github.com/krt-profit/basetool/releases/tag/v0.3.8) - 2026-05-27

### Fixed

- **Sidebar-Kontext-Chip zeigte für Non-Admins immer "Kein Bereichskontext" — selbst bei korrekter Staffel-Membership.** `SecurityConfig.filterChain` hatte unter den `/api/v1/users/*`-Regeln eine Catch-All `/api/v1/users/**` → `hasRole("ADMIN")`. Diese fängt jeden URL mit ≥2 Pfad-Segmenten nach `/users/` (Spring's `*` deckt nur ein Segment, `**` deckt mehrere). Damit fiel `GET /api/v1/users/{id}/memberships` (zwei Segmente) in die Admin-only-Regel und produzierte 403 für Officer + KRT Member, obwohl der Method-Level `@PreAuthorize("hasAnyRole(...)")` non-admins explizit zulässt. Spring Security wertet URL-Filter VOR Method-Level-Annotationen aus — der 403 kommt aus dem URL-Filter, der Method-Handler wurde nie aufgerufen, und der `RequestLoggingFilter` (nach Security) sah den fehlgeschlagenen Call nie. Frontend `SquadronContextAdvice.loadCallerMemberships` catched die 403 und returnt eine leere `availableOrgUnits`-Liste → Chip "Kein Bereichskontext" trotz korrekter DB-Membership. Fix: explizite Regel `GET /api/v1/users/*/memberships` → `hasAnyRole("ADMIN", "OFFICER", "KRT_MEMBER")` vor der Catch-All. Neuer `UserMembershipsSecurityTest` pinnt alle vier Caller-Klassen (Admin, Officer, KRT Member, anonymous) an den URL-Filter.

- **SK-Mitgliederliste (`/admin/special-commands/{id}`) zeigt jetzt den Anzeigenamen mit Username-Fallback statt einer leeren Zelle.** `OrgUnitMembershipMapper` liest `userDisplayName` ab jetzt aus `user.effectiveName` statt direkt aus `user.displayName` — User ohne gesetzten Anzeigenamen surfen mit ihrem Username, statt das Roster mit leeren Namensspalten zu schmücken. Wire-Shape (String) unverändert, nur die denormalisierte Semantik richtet sich am DTO-Javadoc-Vertrag aus.

## [v0.3.7](https://github.com/krt-profit/basetool/releases/tag/v0.3.7) - 2026-05-27

### Fixed

- **OWASP Dependency-Check CI Workflow läuft wieder durch.** Seit 2026-05-20 (Run 26516932803 + Vorgänger) brach der Workflow konsistent ab, weil Gradle 9 einen exklusiven Lock auf jeder `Configuration`-Auflösung erzwingt und die AsciidoctorJ-Plugin-internen `__$asciidoctorj$___r`-Detached-Configurations diesen Lock unter parallelem Resolve nicht hielten. Mit `org.gradle.parallel=true` (repo-weit gesetzt) raced der `dependencyCheckAggregate`-Resolver auf mehreren Worker-Threads in dieselbe interne Configuration und der Build abortete vor dem ersten Analyzer-Stage. Fix: nur dieser eine CI-Step bekommt `--no-parallel` als Flag — Developer-Builds behalten ihren Parallel-Speedup, und der `--no-parallel`-Scope ist eng genug, dass keine weiteren Tasks ausgebremst werden.

## [v0.3.6](https://github.com/krt-profit/basetool/releases/tag/v0.3.6) - 2026-05-27

### Fixed

- **`@Async("uexExecutor")` Backend-Tasks propagieren jetzt die MDC-Felder des absendenden Threads (`correlationId`, `userId`, `orgUnitId`).** SLF4Js `MDC` ist ein klassischer `ThreadLocal` — ohne Decorator picken die UEX-Sync-Tasks einen Worker aus dem `uexExecutor`-Pool, der die Request-Filter-Kette nie durchlaufen ist, und alle Log-Zeilen des Sync emittieren leere MDC-Felder. Neuer `AsyncConfig.MdcPropagatingTaskDecorator` snapshotted die Map des Submit-Threads und restoreted sie vor dem Run; `finally`-Block `MDC.clear()` verhindert, dass zwei Tasks auf dem gleichen Pool-Thread MDC-Bleeden. Keine Tenancy-Implikation (Background-Task ohne Auth-Context), aber Audit-Trail-Join zwischen Scheduler-Logs und triggernder Request bleibt jetzt durchgängig.

- **Admin-Pin auf eine fremde Staffel wirkte beim Lager-View nicht: die Aggregations-Query bekam keinen `X-Active-Org-Unit-Id` Header und fiel auf `adminAllScope=true` zurück.** Spring's `WebClient` führt jede `exchange()` auf einem Reactor-Netty-Worker-Thread aus — nicht auf dem ursprünglichen Servlet-Thread. Klassische `ThreadLocal`-Werte werden nicht über Threads kopiert; die Lambda in `ActiveSquadronRelayFilter.relayActiveSquadron()` las `ActiveSquadronContext.get() == null` und ließ den Pin-Header weg. Die Sidebar zeigte den Pin trotzdem korrekt, weil `SquadronContextAdvice.activeSquadronId` direkt aus `HttpSession` liest (kein ThreadLocal) — daher der irreführende Eindruck, der Pin wäre aktiv. Fix per neuer `ReactorContextPropagationConfig`: aktiviert `Hooks.enableAutomaticContextPropagation()` und registriert `ThreadLocalAccessor`s für `ActiveSquadronContext` UND `CorrelationContext` auf der globalen `ContextRegistry`. Reactor snapshottet die registrierten Holder beim Mono-Assembly auf dem Servlet-Thread und stellt sie auf dem Worker wieder her. `CorrelationContext` war vom gleichen Bug betroffen, nur weniger sichtbar — outbound Backend-Calls trugen vermutlich nicht die Frontend-Correlation-Id, was den Audit-Trail-Join brach. Neue Dependency `io.micrometer:context-propagation` (Version aus Spring-Boot-BOM). Test `ReactorContextPropagationConfigTest` pinnt das Verhalten an `Schedulers.parallel()` ab.

- **Promotion-Menü respektiert jetzt den Admin-Pin auf eine Staffel mit deaktiviertem Beförderungssystem.** Vorher hat der Admin-Bypass im `OwnerScopeService.isPromotionFeatureEnabledForCurrentScope()` und `SquadronContextAdvice.promotionFeatureEnabled` das Menü bedingungslos sichtbar gelassen — auch wenn der Admin sich aktiv in die Sicht einer Staffel mit `promotion_enabled = false` gepinnt hatte. Jetzt steuert die gepinnte Staffel auch für Admins die Sichtbarkeit; der Admin sieht dieselbe Sicht wie ein Member dieser Staffel. Admin ohne Pin (all-scopes) behält das Menü, damit er eine gelockt-out Staffel re-enablen kann; alternativ direkt `/admin/settings` (nicht durch den Gate). Backend- + Frontend-Test-Suite (`PromotionFeatureFlagServiceGateTest`, `PromotionFeatureFlagPageGateTest`) um die Admin-Pin-Pfade erweitert.

## [v0.3.5](https://github.com/krt-profit/basetool/releases/tag/v0.3.5) - 2026-05-27

### Fixed

- **KRT-Logo nicht mehr gestaucht auf den SK-Admin-Seiten und im Browser-Tab-Favicon (Issue [#254](https://github.com/krt-profit/basetool/issues/254)).** Beide `/admin/special-commands`-Templates hatten `<img ... width="50" height="50">`, was die 101x72-Logografik in ein 50x50-Quadrat presste — alle anderen Seiten geben nur `height="50"` an. Zusaetzlich neue padded-square Favicon-Asset [`logos/krt-favicon.webp`](frontend/src/main/resources/META-INF/resources/logos/krt-favicon.webp) (128x128, transparent gepaddet) ersetzt das Querformat `krt.webp` als Favicon-Quelle, damit Browser den Tab-Slot nicht mehr quadratisch stauchen; die `krt.webp` bleibt unveraendert fuer die Header-Bilder.

## [v0.3.4](https://github.com/krt-profit/basetool/releases/tag/v0.3.4) - 2026-05-26

### Changed

- **Handover-PDF zeigt jetzt das KRT-Corporate-Logo statt des IRIDIUM-Staffel-Logos (Follow-up #251).** `JobOrderHandoverReportService` liest die neue `krt.png` (101x72, aus `frontend/.../krt.webp` konvertiert, da OpenPDF kein WebP dekodiert) aus dem Backend-Classpath; die jetzt verwaisten `staffel_iridium.{png,svg}`-Kopien in beiden Modulen wurden entfernt. Design-Master `design/logos/staffel_iridium.svg` bleibt erhalten.

## [v0.3.3](https://github.com/krt-profit/basetool/releases/tag/v0.3.3) - 2026-05-26

### Added

- **R8 — drei deferred Items aus dem R7-Audit-Backlog abgeschlossen.** Drei Plan-§-Aufträge, jeweils additiv zum R6.e-Stand:

  - **§7.4 Single-POST Membership-Delta Endpoint.** Neuer Endpoint `PATCH /api/v1/users/{id}/memberships` bündelt die bisher fragmentierten Staffel-Assignment + Staffel-Flag + SK-Add/Remove/Patch Round-Trips in einen transaktionalen Aufruf. Neuer `MembershipDeltaRequest` (mit nested `StaffelChange` + `SpecialCommandChange[]`) + `MembershipDeltaResponse`-Wire-Shape, beide auf Backend + Frontend Mirror in einem Commit. Per-Row Optimistic-Lock bleibt erhalten weil die Service-Orchestrierung in `UserService.applyMembershipDelta` zu den existierenden R5.b/R6.e Primitives durchroutet — eine inkonsistente Batch (eine Row stale, der Rest fresh) rollt die gesamte Transaction zurück und liefert ein 409. Member-Edit-Page nutzt den neuen Endpoint ab sofort für Staffel-Reassignment + Staffel-Flag-Flips (volle SK-Editierung in der Member-Edit-Page bleibt Follow-up — die SK-Detailseite ist heute schon das Bearbeitungs-Surface). Acht neue Service-Tests + ein bestehender Test angepasst. Lead-Toggle ist bewusst NICHT Teil der Delta — bleibt ADMIN-only audit-isoliert in der SK-Detailseite.

  - **§6.1 Kontextuelle JWT-Authorities.** Neuer GrantedAuthority-Subtype `OrgUnitContextualAuthority(roleName, orgUnitId)` mit String-Form `ROLE_<NAME>@<orgUnitUuid>`. JWT-Converter emittiert ab jetzt BEIDE Surfaces parallel: den flachen `ROLE_LOGISTICIAN` (R6.d back-compat fuer bestehende `hasRole('LOGISTICIAN')`-Gates) UND je eine kontextuelle Authority pro `(membership, flag=true)`-Paar. Neuer Helper `OwnerScopeService.hasRoleInOrgUnit(orgUnitId, roleName)` matched die kontextuellen Authorities by-value — ADMIN bypassed wie immer. Lässt Plan §6.1's Vision für per-OrgUnit-skopiertes Authorisation-Gating zu, ohne das bestehende Flat-Role-Surface zu brechen. Sechs Tests fuer den Authority-Record, fuenf Tests fuer den Service-Matcher.

  - **V102 NOT NULL tighten + `resolveOrgUnitForPickerOutput` SK-unlock (Plan §10 PR-6 / R3.a).** Neue Migration V102 setzt `owning_org_unit_id` auf NOT NULL und droppt NOT NULL von `owning_squadron_id` auf allen sechs Aggregaten (mission, operation, ship, inventory_item, refinery_order, job_order × 2). Der V102-pendant `OwnerScopeService.resolveOrgUnitForPickerOutput` Methode ist die SK-fähige Nachfolgerin von `resolveSquadronForPickerOutput` — gleiche §5.5.1-Matrix (0/1/>1 Memberships, valid/foreign Choice), aber returnt `OrgUnit` (Staffel ODER SK) statt mit "SK ownership not yet supported" zu throwen. Die alte Methode bleibt für Back-Compat während die Callers (8 Aufrufstellen, siehe `R8_DESTRUCTIVE_ROADMAP.md`) migrieren. SK-owned Aggregate sind ab V102 + Caller-Migration vollständig representable. Vier neue Service-Tests pinnen die SK-honoured-Pfade.

  - **`R8_DESTRUCTIVE_ROADMAP.md`** dokumentiert die naechsten sechs Schritte (Caller-Migration → Stop-Write Dual-Write → V103 drop legacy aggregate columns → Stop-Write User.squadron → V104 drop app_user Legacy-Cols → V105 drop squadron-Tabelle + V100-Trigger). Jeder Schritt mit Pre-Merge-Gate + Backup-Hinweis — das Plan-§10 zwei-Soak-Window-Design bleibt erhalten.

### Changed

- **Logo auf WebP umgestellt + aus Maintenance-Page entfernt.** Alle 46 Frontend-Templates inkl. Favicon und das Keycloak-Login-Theme laden jetzt `krt.webp` (1.4 KB) statt `krt_logo.svg` (~35-110 KB) — kleinere Header-Bytes, schnellerer Erst-Render. Favicon-`type` auf `image/webp` aktualisiert; alte SVG-Kopien (`frontend/.../static/logos`, `frontend/.../META-INF/resources/logos`, `keycloak-theme/.../img`) geloescht. Maintenance-Page (`docker/maintenance/static/maintenance.html`) zeigt kein Logo mehr — `<img>`-Tag, `.brand-logo`-CSS-Regeln und die zugehoerige `krt_logo.svg`-Datei entfernt; die Page bleibt reines Text-Branding. Domain-Switch-Runbook entsprechend angepasst (Maintenance-Logo-Install-Step + Groessen-Verifikation entfernt).

- **R9 Step 6 — Drop der Legacy-`squadron`-Tabelle + V100-Sync-Trigger (Plan §10 PR-7 / R8_DESTRUCTIVE_ROADMAP.md Step 6).** Finale destruktive Migration in der Multi-OrgUnit-Refactor-Kette: `V105__drop_legacy_squadron_table.sql` retargeted die drei verbliebenen FKs (`promotion_topic.owning_squadron_id`, `mission_participant.squadron_id`, `job_order_handover.executing_squadron_id`) von `squadron(id)` auf `org_unit(id)`, droppt den V100-Sync-Trigger plus Funktion und droppt die `squadron`-Tabelle. Die Java-Entities (`PromotionTopic`, `MissionParticipant`, `JobOrderHandover`) bleiben `Squadron`-getypt — `Squadron` mappt via `@DiscriminatorValue('SQUADRON')` auf `org_unit`, die Legacy-Spaltennamen bleiben damit V101-SK-Guard + FK-Constraint-Namen lesbar (Plan §3.3). **Voraussetzung Prod:** R9 Steps 1-5 deployed + DB-Backup, irreversibel — alle sechs Roadmap-Schritte sind damit gelandet.

- **R9 Steps 4 + 5 — Drop von `User.squadron`, `User.isLogistician`, `User.isMissionManager` + V104-Spalten-Drop (Plan §10 PR-7 / R8_DESTRUCTIVE_ROADMAP.md Steps 4 + 5).** `org_unit_membership` ist jetzt die alleinige Quelle fuer "Welche Staffel gehoert dieser User an" und die Logistician/MissionManager-Flags — die drei legacy User-Spalten plus FK + Index werden in der neuen Migration `V104__drop_app_user_legacy_squadron_fields.sql` gedroppt. Service- und Repository-Leser (~11 Hauptdateien inkl. `OwnerScopeService`, `UserService`, `UserController`, `JobOrderHandoverService`, `MissionService`, `HangarImportService`, `UserRepository`-JPQL) holen den Staffel-Link jetzt direkt aus `org_unit_membership.findAllByIdUserIdAndKind(userId, SQUADRON)`; der JWT-Converter-Fallback fuer die Pre-V95-Backfill-Luecke ist weg. `UserDto` behaelt die `squadron` / `isLogistician` / `isMissionManager` Felder API-stabil — `UserMapper` derived sie jetzt aus der Membership-Row. Test-Heap auf 1.5 GiB hochgesetzt (volle Suite blies bisher beim 250. `@SpringBootTest`-Kontext den 512 MiB Default raus). **Voraussetzung Prod:** R9 Step 4 deployed + DB-Backup, irreversibel.

- **R9 Steps 2 + 3 — Drop der Legacy-`owningSquadron`-Mirror-Felder + V103-Spalten-Drop (Plan §10 PR-7 / R3.b).** Die sechs staffel-skopierten Aggregate (Mission, Operation, Ship, InventoryItem, RefineryOrder, JobOrder × creating/requesting) tragen nur noch `owningOrgUnit` / `creatingOrgUnit` / `requestingOrgUnit` als alleinige Source-of-Truth — die `Squadron`-getypten Legacy-Felder und der `syncOwnerFields()` Lifecycle-Hook sind weg, neue Migration `V103__drop_legacy_owning_squadron_columns.sql` droppt die sieben Spalten + die V91/V93-Indexe. **Voraussetzung Prod:** R9 Step 1 deployed + DB-Backup, da die Migration irreversibel ist; `promotion_topic.owning_squadron_id` bleibt unangetastet (Plan §3.3 + V101-Guard). DTO-Contract (`owningSquadron` / `creatingSquadron` / `requestingSquadron`) bleibt stabil über explizite MapStruct-`@Mapping`s plus die neue `SquadronMapper.orgUnitToReferenceDto(OrgUnit)`-Bridge — SK-Owner surfen ab jetzt als `null`. 17 Test-Files migriert.

## [v0.3.2](https://github.com/krt-profit/basetool/releases/tag/v0.3.2) - 2026-05-26

### Added

- **Spezialkommando-Follow-up-Sweep nach R6.e-Audit (Plan §7.2 / §7.4 / §7.5 + §3.3 / §6.e Race-Hardening).** Sechs gebuendelte Loose-Ends die der R6.e-Audit aufgedeckt hat, ohne dass eine destruktive Cleanup-Release noetig waere:
  - **Backend `GET /api/v1/me/active-org-unit`** als kanonischer Alias zum Legacy `/active-squadron`-Read (Plan §7.2-Window). Beide Pfade leben parallel im `MeController`, bedienen `OwnerScopeService.currentOrgUnitId()` und unterscheiden sich nur im Response-Feldnamen (`orgUnitId` vs Legacy `squadronId`). Frontend-Clients koennen jetzt direkt auf den neuen Pfad migrieren; Legacy faellt mit der destruktiven Cleanup-Release.
  - **Sidebar-Kontext-Chip** zeigt jetzt das OrgUnit-Kind per Label (`[Staffel: IRI]` / `[SK: ALPHA]`) und applied einen neutralen `squadron-context-chip-sk`-Variant fuer SK-Pins (Plan §7.2). Neuer `SquadronContextAdvice.activeOrgUnit` Model-Attribute resolved aus der bereits geladenen `availableOrgUnits`-Liste und kennt das Kind; das bestehende Squadron-only `activeSquadron`-Attribute bleibt unangetastet. CSS-Variant + 4 neue i18n-Keys in DE + EN + Default.
  - **`OrgUnitMembershipService.applyStaffelMembershipFlagDelta`** ist gegen die V95-Backfill-Lueckenrace gehaertet: Wenn zwei Legacy-Flag-Toggle-Endpunkte parallel feuern und beide den Synthesepfad treffen, faengt ein `DataIntegrityViolationException`-Catch den verlierenden Insert, laedt die Gewinner-Row und reappliest den Flag-Delta on top. Erst die zweite `saveAndFlush` schreibt durch — Optimistic-Lock-Surface bleibt waehrend des Soak-Window konsistent.
  - **`V101__guard_promotion_topic_against_special_command.sql`** — Postgres-Trigger `guard_promotion_topic_owner_kind` blockt jeden INSERT/UPDATE der `promotion_topic.owning_squadron_id` mit einer Nicht-SQUADRON-`org_unit`-Referenz. DB-Layer-Defence-in-depth on top des typisierten `PromotionTopic.owningSquadron`-Felds und der ArchUnit-Regel §8.2; schliesst die Plan-§3.3-Luecke "future trigger-CHECK".
  - **Member-Edit-Page (`/members/{id}/edit`)** zeigt jetzt unter dem Edit-Form eine read-only Mitgliedschafts-Uebersicht (Plan §7.4-Teilumsetzung) — Staffel + alle SK-Memberships mit Kind-Label, jeweils mit "Verwalten"-Link zur entsprechenden Admin-Page (SK-Memberships routen auf `/admin/special-commands/{id}` wo das R5.c.b-Roster die Bearbeitung uebernimmt). Voller single-POST-Delta-Endpoint pro Plan §7.4 bleibt Follow-up.
  - **Member-List (`/members`)** hat eine neue Spalte "SK" zwischen Staffel und Rang die alle SK-Mitgliedschaften pro User komma-separiert anzeigt (Plan §7.5). Per-User-Round-Trip auf `/api/v1/users/{id}/memberships`; N+1 bounded auf die Page-Size und akzeptabel fuer die Admin-Liste; eine bulk-Endpoint-Optimierung kann spaeter nachgezogen werden falls Performance-Bedarf entsteht.
  - **Doku-Sweep §12:** `CLAUDE.md` "Multi-squadron tenancy" → "Multi-org-unit tenancy" mit OwnerScopeService-Surface + R5.5.1-Picker-Matrix + V97-V101-Trigger-Verbund; `ROLES_AND_PERMISSIONS.md` Header auf 2026-05-23 + Logistician/MissionManager-Authority-Source = `org_unit_membership` + neue `SK Lead`-Rolle; `README.md` OrgUnit-Tenant + Plan-Links; `db/migration/README.md` Timeline V80-V96 / V97-V101 / V102+.
  - Neuer Test `MeControllerTest` pinnt die zwei Read-Endpoints + Feldname-Mapping. Keine destruktiven Schema-Aenderungen; ein neues V101-Trigger-Statement.

- **Schreib-Pfad fuer is_logistician / is_mission_manager auf die Membership-Tabelle umgebogen (R6.e / Plan D3 Write-Side).** Drei zusammenhaengende Aenderungen:
  - Neuer Endpoint `PATCH /api/v1/squadrons/{id}/members/{userId}` (analog zum R5.b SK-Pendant) als kanonische Form mit optimistischem Lock — Body `MembershipFlagsPatchRequest(isLogistician?, isMissionManager?, version)`, ADMIN-gated. Frontend kann jetzt direkt das `org_unit_membership` Row patchen statt ueber den Legacy `PATCH /api/v1/users/{id}/logistician?...=` Query-Param-Pfad zu gehen.
  - `UserService.updateLogisticianStatus` / `updateMissionManagerStatus` (die alten Legacy-Endpunkte bleiben live als Transitional-Alias) spiegeln den Flag-Flip jetzt auch auf die Staffel-Membership-Row via `OrgUnitMembershipService.applyStaffelMembershipFlagDelta`. Damit landet eine Flag-Aenderung aus dem bestehenden Frontend-Toggle UI sofort auf der Row die R6.d's JWT-Converter liest — kein Wartezeit-Effekt, kein dead write mehr.
  - `UserService.updateUserSquadron` synct die Staffel-Membership-Row beim Wechsel: Wechselt User von Staffel A nach B, wird die alte Row geloescht und eine neue an B erzeugt; Clearing der Squadron loescht alle Staffel-Memberships. Schliesst die V95-Backfill-Luecke fuer Post-R1-User die ohne explizite Membership-Insertion via Keycloak SSO erstellt wurden. Neue Membership-Reihen erben die Legacy-User-Flags damit Authority nicht mid-deploy zurueckfaellt.
  
  6 Test-Files (`UserServiceAttributesTest`, `UserServiceSortTest`, `UserServiceDeleteTest`, `UserServiceRankTest`, `UserServiceSyncTest`, `UserJoinDateServiceTest`) bekommen das neue `@Mock OrgUnitMembershipService`. Backend-Test-Suite gruen (nur die bekannte transiente `KeycloakHealthIndicatorTest`-Netzwerk-Flake ist unrelated).

- **JWT-Authority-Converter liest jetzt die OrgUnit-Memberships statt der Legacy-User-Spalten (R6.d / SPEZIALKOMMANDO_PLAN.md D3 + §6.1).** `CustomJwtGrantedAuthoritiesConverter` befoerdert `ROLE_LOGISTICIAN` / `ROLE_MISSION_MANAGER` jetzt aus dem OR-Union ueber alle OrgUnit-Memberships (`org_unit_membership.is_logistician` / `is_mission_manager`) statt aus `app_user.is_logistician` / `is_mission_manager`. Ein User mit der Logistician-Flag auf irgendeiner seiner Staffel- oder SK-Memberships kriegt die flache Rolle; die per-OrgUnit-Skopierung passiert separat ueber `@PreAuthorize`-Gates an den `OwnerScopeService`. Legacy-Fallback (User-Spalten) bleibt drin fuer Pre-V95-Backfill-Edge-Cases — kommt mit der destruktiven Cleanup-Release raus. Mit R5.b's Membership-Patch-Endpoint kann ein Admin jetzt die Logistician-Rolle pro Membership wechseln und der Effekt ist beim naechsten Login sofort sichtbar. Zwei neue Unit-Tests in `LogisticianRoleTest` pinnen das Verhalten: Membership-Flag = true ohne User-Flag → Rolle vergeben; User-Flag = true mit Membership-Flag = false → Rolle NICHT vergeben (Membership ist autoritativ).

## [v0.3.1](https://github.com/krt-profit/basetool/releases/tag/v0.3.1) - 2026-05-26

### Added

- **Active-Context-Switcher fuer Non-Admins + Header- und MDC-Rename (R5.e / SPEZIALKOMMANDO_PLAN.md §7.2 + R14).** Vier zusammengehoerige Wechsel, alle in einer Soak-Release mit Legacy-Aliasen:
  - **Switcher-Widening:** Die Sidebar-Switcher-Form (`fragments/sidebar.html`) ist jetzt `isAuthenticated()` statt `hasRole('ADMIN')` und blendet sich automatisch ein wenn der User &gt;1 OrgUnit-Mitgliedschaft hat. Neue `availableOrgUnits`-Model-Attribute liefert fuer Admins die Squadron+SK-Liste und fuer Non-Admins die eigenen Memberships (per `/api/v1/users/{id}/memberships`). Single-Membership-User sehen kein UI-Element ("no choice, no noise" — Plan §7.2).
  - **Endpoint-Rename:** Neuer `POST /me/active-org-unit` ist die kanonische Form; alter `POST /me/active-squadron` bleibt als deprecated Alias fuer eine Release (cached Browser-Tabs nicht brechen). `MeFrontendController.@PreAuthorize` von `hasRole('ADMIN')` auf `isAuthenticated()` geoeffnet; Backend re-validiert Non-Admin-Pins gegen die tatsaechlichen Memberships in `OwnerScopeService.currentScopePredicate()`, ein gespoofter POST kann die Sichtbarkeit nicht ueber die echten Memberships hinaus erweitern.
  - **Header-Rename:** `X-Active-Squadron-Id` → `X-Active-Org-Unit-Id`. Frontend-`ActiveSquadronRelayFilter` sendet beide Header parallel; Backend-`OwnerScopeService.readActiveSquadronFromHeader` liest den neuen Namen zuerst und faellt auf den Legacy-Namen zurueck. Session-Attribut-Key `iridium.activeSquadronId` → `iridium.activeOrgUnitId` mit gleicher Dual-Read/Write-Logik in `ActiveSquadronContextFilter` und `SquadronContextAdvice`.
  - **MDC-Feld-Rename:** `squadronId` → `orgUnitId`. `CorrelationIdFilter` schreibt beide MDC-Felder mit demselben Wert; `LoggingProperties` hat zwei `@NotBlank`-Felder; `logback-spring.xml` Console+File-Pattern nutzen `%X{orgUnitId:-}`, JSON-Appender enthaelt beide `<includeMdcKeyName>`. Log-Pipeline-Dashboards koennen im eigenen Tempo migrieren; Legacy-Feld faellt mit der destruktiven Cleanup-Release.
  - **Non-Admin-Pinning** integriert: `OwnerScopeService.currentScopePredicate()` honoriert jetzt einen Non-Admin-Pin (header-getrieben) wenn er auf einer der eigenen Memberships landet — die Plan-§3.5 "activeOrgUnitId fuer Non-Admin"-Spalte. Foreign Pin → silent Fallback auf die Membership-Union. Backend-Test-Suite gruen (alle 307 Klassen, 0 Failures); Frontend `PromotionFeatureFlagPageGateTest` weiter gruen nach Erhalt des `availableSquadrons`-Verhaltens fuer Non-Admins.

- **Repository-Signaturen auf den Plan-§5.4-Membership-Union-Vector umgestellt (R6.c).** Vierzehn staffel-scoped Repository-Queries in `MissionRepository`, `OperationRepository`, `ShipRepository`, `RefineryOrderRepository`, `InventoryItemRepository` nehmen jetzt nicht mehr einen einzelnen nullable `UUID scopeSquadronId`, sondern den orthogonalen Drei-Parameter-Tupel `(boolean isAdminAllScope, UUID activeOrgUnitId, Collection<UUID> memberOrgUnitIds)`. Der neue JPQL-Predicate `:isAdminAllScope = true OR (:activeOrgUnitId IS NOT NULL AND x.owningOrgUnit.id = :activeOrgUnitId) OR (:activeOrgUnitId IS NULL AND x.owningOrgUnit.id IN :memberOrgUnitIds)` deckt vier Caller-Klassen mit einer einzigen Formel ab: Admin-all-scope, Admin/Non-Admin pinned, Non-Admin Multi-Membership-Union, Anonymous. Mission hängt das Public-Escape `OR x.isInternal = false` an. Vorher kollabierten Admin-all und Non-Admin-mit-Multi-Membership in denselben Codepfad — was SK-Daten für Multi-Membership-User stillschweigend versteckte. Neuer `ScopePredicate`-Record + `OwnerScopeService.currentScopePredicate()` als zentraler Resolver; Membership-Quelle ist hybrid bis zur D3-Migration (`User.squadron` für Staffel + `org_unit_membership` für SK). Außerdem stellen die Queries komplett auf `owningOrgUnit.id` um (R4-Mirror-Spalte) statt der Legacy `owningSquadron.id` — der Lifecycle-Hook hält beide Felder in lockstep, also keine Verhaltensänderung heute, aber die destruktive Cleanup-Release kann jetzt die alte Spalte ohne Service-Code-Eingriff droppen. Alle Service-Caller (`MissionService`, `OperationService`, `HangarService`, `RefineryOrderService`, `InventoryItemService`) migriert; 8 Test-Files aktualisiert (~25 Mockito-Stub-Sites). Komplette Backend-Test-Suite grün.

- **Owner-Picker im Admin-Buchen-Modal (`/inventory/all`) TRANSFER-Zweig nachgezogen (R6.b).** Das User-Modal (`/inventory/my`) hatte den Picker seit R5.d.g, das Admin-Modal aber nicht — Admin-Transfers an Multi-Membership-Ziel-User landeten stillschweigend in der Heim-Staffel. Jetzt erscheint das gleiche "Buchen in OrgUnit"-Dropdown unterhalb von Zielnutzer + Zielort, fetcht die Memberships per `GET /api/v1/users/{id}/memberships` on-demand und bleibt versteckt solange das Ziel ≤1 OrgUnit hat. Spiegelt die R5.d.g-Logik 1:1 plus CSP-sicheres `data-trigger="inv-admin-target-user-changed"`.

- **Owner-Resolver in OwnerScopeService an die Plan-§5.5.1-Matrix angeschärft (R6.b).** `resolveSquadronForPickerOutput` setzt jetzt die 0/1/>1-Membership-Regeln durch statt blind auf `User.getSquadron()` zurückzufallen: 0 Memberships → 400; 1 Membership + leerer Picker → Auto-Stempel; 1 Membership + falsche Wahl → 400; >1 Memberships + leerer Picker → 400 ("owningOrgUnitId is required"); valide Picker-Wahl → durchgereicht. Behebt die stille Audit-Regression #4 (Multi-Membership-User wurden ohne Picker-Output unmerklich auf ihre Staffel gestempelt statt explizit zu wählen). Resolver-Quellen sind hybrid bis zur D3-Migration: legacy `User.squadron` als Staffel-Quelle + `org_unit_membership` für SK-Reihen. Neuer Unit-Test deckt alle sieben Matrix-Pfade ab.

- **`RefineryOrderService.storeRefineryOrder` stempelt jetzt über den Owner-Resolver statt direkt `assignee.getSquadron()` zu lesen (R6.b).** Defensiv-Patch — heute bestehen alle Zuweisungsempfänger die Resolver-Prüfung (jeder User hat eine Heim-Staffel), aber nach der destruktiven D3-Release würde der Direkt-Zugriff bei SK-only Empfängern NPEen. Mit der Resolver-Routung bleibt Single-Membership-Verhalten identisch und Multi-Membership-Empfänger bekommen ein sauberes 400 statt eines stillen Falsch-Stempels. Der Refinery-STORE-Form hat noch keinen Picker — Multi-Membership-Verhalten ist Follow-up der R5.d-Welle.

- **ArchUnit-Guards für die Spezialkommando-Erweiterung geschlossen (R6.a / SPEZIALKOMMANDO_PLAN.md §8.1/§8.2/§8.3/§8.5).** Drei neue Regeln plus zwei Erweiterungen bestehender Regeln verhindern stille Regressionen: (a) `promotionTopicOwningSquadronMustStayTypedSquadronNotOrgUnit` blockt eine spätere Typ-Lockerung von `PromotionTopic.owningSquadron` auf `OrgUnit` — sonst könnte eine SK-Referenz die V97-CHECK (`chk_org_unit_promotion_only_squadron`) umgehen (R4-Risiko); (b) `noNewJoinColumnReferencingSquadronIdOutsideGrandfatheredEntities` blockt ein versehentliches Wiedereinführen von `@JoinColumn(name = "squadron_id")` auf neuen Entities — `User.squadron` und `MissionParticipant.squadron` sind die einzigen grandfatherten Verwendungen; (c) `staffelScopedWriteEndpointsMustGateOnOwnerScopeService` deckt jetzt `SpecialCommandController` + `SpecialCommandMembershipController` mit ab, und akzeptiert `@specialCommandSecurityService.canManageMembers` als gleichwertigen Per-Aggregate-Gate; (d) `FORBIDDEN_MISSION_REQUEST_COMPONENTS` blockt zusätzlich die Objekt-Form-Referenzen `owningOrgUnit`, `creatingOrgUnit`, `requestingOrgUnit` auf den Mission-Write-DTOs — die plain-UUID-Variante (`owningOrgUnitId`) bleibt erlaubt als R5.d-Picker-Output.

- **Owner-Picker im Inventar-Buchen-Modal TRANSFER-Zweig integriert (R5.d.g) — die R5.d-Picker-Reihe ist damit komplett.** Im Buchen-Modal (`/inventory/my`, `/inventory/all`) erscheint im TRANSFER-Zweig unter dem Zielort-Dropdown ein neues "Buchen in OrgUnit"-Dropdown. Der Picker fetcht die Mitgliedschaften des Ziel-Users via `GET /api/v1/users/{id}/memberships` on-demand sobald der Empfänger gewechselt wird, und bleibt versteckt solange der Ziel-User nur eine OrgUnit hat. `InventoryItemBookOutDto` (beide Seiten der Wire) und `InventoryBookOutForm` nehmen ein neues `targetOwningOrgUnitId` entgegen; `InventoryItemService.bookOutInventoryItem` routet die Stempelung im TRANSFER-Zweig über `OwnerScopeService.resolveSquadronForPickerOutput`. Cross-OrgUnit-Transfers (Plan D4) sind damit explizit unterstützt — User A in Staffel X kann an User B in einer SK Y ausbuchen sobald die NOT-NULL-Lockerung fällt.

- **Owner-Picker im Hangar-Schiff-Add-Modal integriert (R5.d.f).** Im Add-Schiff-Modal auf `/hangar` erscheint unter der "Fitted"-Checkbox das Picker-Dropdown mit den OrgUnits, in denen der Aufrufer Mitglied ist. `ShipRequestDto` (backend + frontend Mirror) und `ShipForm` nehmen ab jetzt ein optionales `owningOrgUnitId` entgegen; `HangarService.addShip` routet die Stempelung über `OwnerScopeService.resolveSquadronForPickerOutput` statt direktem `user.getSquadron()`. Update-Pfad bleibt unverändert (existierender Stempel überlebt). SK-Selektionen werden weiterhin mit 400 abgewiesen, bis die NOT-NULL-Lockerung auf `owning_squadron_id` fällt.

- **Owner-Picker auf der Operation-Anlage integriert (R5.d.e).** Im Create-Modal auf `/operations` erscheint unter dem Status-Dropdown das Owner-Picker-Fragment mit den OrgUnits, in denen der Aufrufer Mitglied ist. `OperationCreateDto` und `OperationService.createOperation` akzeptieren ab jetzt ein optionales `owningOrgUnitId` und routen die Stempelung über `OwnerScopeService.resolveSquadronForPickerOutput`. Der historische Fallback "stempel aus aktivem Scope" bleibt für den Fall ohne authentifizierten Aufrufer (admin in "alle Staffeln"-Modus, anonymer Form-Submit) erhalten. SK-Selektionen werden weiterhin mit 400 abgewiesen, bis die NOT-NULL-Lockerung auf `owning_squadron_id` fällt.

- **Owner-Picker auf der Missionserstellung integriert (R5.d.d).** Auf `/missions/new` (Form lebt in `mission-detail.html` via `isNew`-Conditional) erscheint unter der "Intern"-Checkbox das Picker-Dropdown mit den OrgUnits, in denen der aufrufende User Mitglied ist. Das `CreateMissionRequest`-Record und der `MissionService.createMission` nehmen ab jetzt ein optionales `owningOrgUnitId` entgegen und routen die Stempelung uber `OwnerScopeService.resolveSquadronForPickerOutput`. Frontend-seitig schickt der Create-Flow jetzt einen kompakten `CreateMissionRequest` statt einer ueberbreiten `MissionDto` mit 28 Null-Feldern — der bessere Schnitt fuer den Write-Pfad. SK-Selektionen werden weiterhin mit 400 abgewiesen, bis die NOT-NULL-Lockerung auf `owning_squadron_id` faellt.

- **Owner-Picker auf der Job-Order-Anlage und -Detailseite integriert (R5.d.c).** Auf `/orders/create` und `/orders/{id}` wird das `requestingSquadronId`-Dropdown durch ein neues Owner-Dropdown ersetzt, das aktive Staffeln und Spezialkommandos in zwei `<optgroup>`-Sektionen anzeigt. Job Orders sind cross-staffel — die Picker-Options kommen vom neuen Endpoint `GET /api/v1/org-units/active`, nicht aus den Mitgliedschaften des Auftrag-Besitzers. Backend-DTO `CreateJobOrderDto` und Frontend `JobOrderForm` benannten ihr Feld von `requestingSquadronId` auf `requestingOrgUnitId` um; SK-Selektionen werden vom Service weiterhin mit 400 abgewiesen, bis die NOT-NULL-Spalte fällt. Latenter Lambda-Parsing-Bug im `fragments/owner-picker.html` (SpEL akzeptiert keine Java-Lambdas im `th:with`) bei dieser Gelegenheit per `?[]`-SpEL-Selection-Syntax behoben — vorher wäre er erst bei mehreren OrgUnit-Mitgliedschaften pro User aufgeschlagen.

- **Owner-Picker auf der Raffinerie-Auftrag-Anlage integriert (R5.d.b).** Das in R5.d.a eingeführte Fragment landet jetzt in `refinery-orders-create.html` direkt neben dem Besitzer-Dropdown — ein Logistiker, der einen Auftrag für einen anderen User anlegt, sieht das Picker-Dropdown automatisch, sobald dieser User in mehreren OrgUnits ist. Backend-DTO `RefineryOrderDto` und Service `RefineryOrderService.createRefineryOrder` akzeptieren das optionale `owningOrgUnitId`-Feld. Die Validierungs- und Stempel-Logik wurde aus `InventoryItemService` heraus auf eine gemeinsame Methode `OwnerScopeService.resolveSquadronForPickerOutput` extrahiert — alle R5.d.c-ff.-Forms teilen sich diesen Pfad. SK-Selektion wird wie bei R5.d.a mit 400 abgewiesen, bis die NOT-NULL-Auflockerung der Legacy-Spalte landet.

### Changed

- **Controller-`@PreAuthorize`-SpEL-Strings nutzen jetzt `@ownerScopeService` statt `@squadronScopeService` (R2.5).** 26 SpEL-Ausdrücke in 6 Controllern ([`HangarController`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/controller/HangarController.java), [`InventoryItemController`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/controller/InventoryItemController.java), [`MissionController`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/controller/MissionController.java), [`MissionFinanceEntryController`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/controller/MissionFinanceEntryController.java), [`OperationController`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/controller/OperationController.java), [`RefineryOrderController`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/controller/RefineryOrderController.java)) zeigen jetzt direkt auf den in R2.c eingeführten `OwnerScopeService`-Bean — die Logik ist seit R2.c die gleiche, nur der Bean-Name wandert auf das plan-aligned Vokabular. Der `SquadronScopeService`-Shim bleibt bestehen und akzeptiert weiter den alten `@squadronScopeService`-Namen, bis er in einer späteren PR endgültig entfernt wird; das beruhigt eventuellen Drittcode oder offene Branches die noch den alten String tragen. **ArchUnit-Regel `staffelScopedWriteEndpointsMustGateOnSquadronScopeService`** ist erweitert: das Pattern akzeptiert ab sofort **beide** Bean-Namen (`@ownerScopeService` / `@squadronScopeService`) als gültige Scope-Gates, sodass eine teilmigrierte PR die Regel nicht reisst; die Fehlermeldung weist neuen Code auf `@ownerScopeService.canEdit*(#id)` als Migrations-Ziel. Drei MockMvc-Security-Tests ([`MissionControllerSlimEndpointsTest`](backend/src/test/java/de/greluc/krt/profit/basetool/backend/controller/MissionControllerSlimEndpointsTest.java), [`MissionFinanceEntryControllerSecurityTest`](backend/src/test/java/de/greluc/krt/profit/basetool/backend/controller/MissionFinanceEntryControllerSecurityTest.java), [`OperationPayoutPaidOutSecurityTest`](backend/src/test/java/de/greluc/krt/profit/basetool/backend/controller/OperationPayoutPaidOutSecurityTest.java)) sind nachgezogen — ihr `@MockitoBean`-Target wechselt von `SquadronScopeService` auf `OwnerScopeService`, sodass die `@PreAuthorize`-SpEL-Strings den Mock-Bean wieder treffen. Keine Verhaltensänderung; alle 1771 Tests passieren weiter.

- **`OwnerScopeService` als generalisierte Scope-Resolution + `SquadronScopeService`-Shim für die ~30 SpEL-Strings (R2.c).** [`OwnerScopeService`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/OwnerScopeService.java) übernimmt die komplette Scope-Resolution-Logik aus dem bisherigen `SquadronScopeService` (Header-basierter Admin-Switch, persistente Member-Resolution, Per-Request-Cache, `canSee*`/`canEdit*`-Gates für Mission/Inventory/Refinery/Operation/Ship, das Mission-cross-staffel-Public-Escape und der Promotion-Feature-Flag-Gate) und liefert zusätzlich vier OrgUnit-typisierte Aliase (`canSeeOrgUnit`, `canEditOrgUnit`, `currentOrgUnitId`, `currentOrgUnit`), auf die R2.d die SpEL-Strings migrieren wird. `SquadronScopeService` schrumpft zu einem schlanken `@Service`-Shim, der jeden öffentlichen Method-Call 1:1 an `OwnerScopeService` weiterreicht — der bestehende Bean-Name `squadronScopeService` (~30 `@PreAuthorize("@squadronScopeService.*")` SpEL-Strings im Codebase) bleibt damit unverändert auflösbar, ebenso die Class-Reference in der ArchUnit-Regel `staffelScopedServicesMustWireSquadronOrAuthHelper`. Tests: die bestehende Test-Logik wandert nach [`OwnerScopeServiceTest`](backend/src/test/java/de/greluc/krt/profit/basetool/backend/service/OwnerScopeServiceTest.java) (1:1 Inhalt + Klassen-Rename), `SquadronScopeServiceTest` ist jetzt ein kleiner Shim-Smoke-Test der nur die Delegation verifiziert; `PromotionFeatureFlagServiceGateTest`'s `@InjectMocks`-Target wechselt von `SquadronScopeService` auf `OwnerScopeService`. Keine Verhaltensänderung für laufende Anwendungen — der gesamte bestehende Test-Suite (1771 Tests) läuft unverändert grün durch. Service-Layer-Stamping, Aggregate-FK-Refactor und SpEL-String-Migration kommen in R2.d.

- **Squadron wird zur OrgUnit-Subklasse, mit DB-Trigger der die Legacy-Tabelle synchron hält (R2.b).** [`Squadron`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/model/Squadron.java) ist jetzt `@DiscriminatorValue("SQUADRON")`-Subklasse von [`OrgUnit`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/model/OrgUnit.java) und mappt damit auf die `org_unit`-Tabelle statt auf die alte `squadron`-Tabelle; alle bisherigen Squadron-spezifischen Felder werden aus der Superklasse geerbt, die `Squadron.IRIDIUM_ID`-Konstante bleibt verbatim erhalten. `SquadronRepository`-Queries narrowen sich automatisch via Discriminator auf `kind = 'SQUADRON'`, also bleiben Spezialkommando-Reihen unsichtbar wo erwartet. Damit alle bestehenden FK-Constraints (`app_user.squadron_id`, `mission_participant.squadron_id`, `owning_squadron_id` / `creating_squadron_id` / `requesting_squadron_id` auf jedem Aggregat) weiter sauber auflösen und R1-Rollback viable bleibt, führt [`V100`](backend/src/main/resources/db/migration/V100__sync_org_unit_to_squadron.sql) einen einseitigen Trigger ein, der jeden INSERT/UPDATE/DELETE auf `org_unit` (für `kind='SQUADRON'`-Reihen) in die Legacy-`squadron`-Tabelle spiegelt — die Anwendung schreibt nur noch nach `org_unit`, `squadron` ist passive Replica. Kein Service-Layer-Refactor in R2.b — `SquadronService`, `SquadronScopeService`, alle Aggregate-`owningSquadron`-Felder und `DataInitializer.seedIridiumIfMissing` bleiben unverändert; das eigentliche Umstellen auf `OwnerScopeService` / `owningOrgUnit` folgt in R2.c. Verifikation: `:backend:test` BUILD SUCCESSFUL (alle bestehenden Squadron-Tests, MissionService-Tests, JobOrderService-Tests laufen weiter durch).

- **JPA-Entities + Repositories für die Spezialkommando-Erweiterung (R2.a).** Drei neue Modellklassen und zwei neue Repositories legen die Anwendungs-seitige Foundation für die in R1 erzeugten DB-Strukturen (V97/V98). [`OrgUnit`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/model/OrgUnit.java) ist die abstrakte Eltern-Entity der `org_unit`-Tabelle mit Single-Table-Inheritance und `kind`-Diskriminator; [`SpecialCommand`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/model/SpecialCommand.java) ist die konkrete Subklasse für `kind='SPECIAL_COMMAND'` mit Constructor- und Setter-Guard gegen das Aktivieren des Beförderungssystems (Defense-in-Depth zusätzlich zum V97-CHECK). [`OrgUnitKind`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/model/OrgUnitKind.java) ist das Discriminator-Enum. [`OrgUnitMembership`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/model/OrgUnitMembership.java) bildet die n:m-Mitgliedschaft mit Composite-PK ([`OrgUnitMembershipId`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/model/OrgUnitMembershipId.java)) ab — `org_unit_id` als plain UUID-Spalte ohne JPA-Relation, damit Squadron noch nicht zur Hierarchie gehören muss. Neue Repositories: [`SpecialCommandRepository`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/repository/SpecialCommandRepository.java) (Hibernate filtert via Discriminator auto auf SK-Zeilen) und [`OrgUnitMembershipRepository`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/repository/OrgUnitMembershipRepository.java) mit Findern für "Mitgliedschaften eines Nutzers" / "Mitglieder einer OrgUnit". `Squadron`, `SquadronRepository`, `SquadronService` und der gesamte bestehende Service-Layer bleiben unverändert auf der Legacy-`squadron`-Tabelle — das eigentliche Switchover folgt in R2.b. Verifikation: `:backend:test` BUILD SUCCESSFUL (Hibernate `ddl-auto=validate` akzeptiert die neuen Mappings gegen das R1-Schema, alle bestehenden Tests passieren).

- **DB-Vorbereitung für Spezialkommandos als zweite Tenant-Art neben Staffeln (R1 / Phase 1).** Drei neue Flyway-Migrationen legen das Schema-Fundament für die in [`SPEZIALKOMMANDO_PLAN.md`](SPEZIALKOMMANDO_PLAN.md) beschriebene Erweiterung: [`V97`](backend/src/main/resources/db/migration/V97__create_org_unit_and_copy_squadron.sql) erstellt die gemeinsame Eltern-Tabelle `org_unit` mit `kind`-Diskriminator (`SQUADRON` / `SPECIAL_COMMAND`) und CHECK-Constraint, der Beförderung permanent auf `SPECIAL_COMMAND`-Zeilen sperrt; jede bestehende Staffel wird 1:1 als `kind='SQUADRON'`-Zeile kopiert (gleiche UUIDs inkl. der kanonischen IRIDIUM-UUID). [`V98`](backend/src/main/resources/db/migration/V98__create_org_unit_membership_and_backfill.sql) führt `org_unit_membership` ein (Composite-PK aus User + OrgUnit, denormalisiertes `kind`-Feld plus BEFORE-INSERT/UPDATE-Trigger zur Konsistenzsicherung, partial unique index erzwingt höchstens eine Staffel-Mitgliedschaft pro Nutzer, CHECK erlaubt `is_lead` nur auf SK-Mitgliedschaften); jeder Nutzer mit gesetzter `app_user.squadron_id` bekommt eine Staffel-Mitgliedschaft, die seine bisherigen globalen `is_logistician` / `is_mission_manager`-Flags übernimmt. [`V99`](backend/src/main/resources/db/migration/V99__add_owning_org_unit_columns_to_aggregates.sql) fügt nullable `owning_org_unit_id`-Spalten neben den bestehenden `owning_squadron_id`-FKs auf `mission`, `operation`, `ship`, `inventory_item`, `refinery_order` ein (plus `creating_org_unit_id` / `requesting_org_unit_id` auf `job_order`), backfilled die Werte aus den Squadron-FKs und legt die analogen B-Tree-Indizes inkl. dem `mission(owning_org_unit_id, is_internal)`-Composite für die Public-Escape-Klausel an. Keine Anwendungs-Codeänderung in R1 — die alten Spalten und Tabellen bleiben weiter autoritativ und unverändert in Betrieb; R2 wird das App-seitige Dual-Write nachziehen, R3 die alten Strukturen abräumen. Migrationen sind durchgängig idempotent (IF NOT EXISTS / WHERE NOT EXISTS / ON CONFLICT DO NOTHING).

### Removed

- **`SquadronScopeService`-Shim entfernt, Service-Layer komplett auf `OwnerScopeService` umgestellt (R3).** Der Compat-Shim, den R2.c als Brücke für die ~30 SpEL-Strings eingeführt hatte, ist nach der SpEL-Migration in R2.5 ohne Nutzer und wird gelöscht — sowohl die Klasse `SquadronScopeService` (delegating shim) als auch der zugehörige `SquadronScopeServiceTest` (Smoke-Test) fallen weg. **Alle 14 Service- und Filter-Klassen** ([`AuthHelperService`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/AuthHelperService.java), [`CorrelationIdFilter`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/logging/CorrelationIdFilter.java), [`HangarService`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/HangarService.java), [`InventoryItemService`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/InventoryItemService.java), [`JobOrderService`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/JobOrderService.java), [`MeController`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/controller/MeController.java), [`MemberEvaluationService`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/MemberEvaluationService.java), [`MissionSecurityService`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/MissionSecurityService.java), [`MissionService`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/MissionService.java), [`OperationService`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/OperationService.java), [`PromotionCategoryService`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/PromotionCategoryService.java), [`PromotionEligibilityService`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/PromotionEligibilityService.java), [`PromotionLevelContentService`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/PromotionLevelContentService.java), [`PromotionTopicService`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/PromotionTopicService.java), [`RankRequirementService`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/RankRequirementService.java), [`RefineryOrderService`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/RefineryOrderService.java), [`UserService`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/UserService.java)) injizieren ab jetzt `OwnerScopeService` direkt. **20 Test-Klassen** sind nachgezogen (`@Mock`/`@MockitoBean`/`@InjectMocks`-Targets gewechselt). **ArchUnit-Regeln** sind in zwei Schritten verschärft: `staffelScopedServicesMustWireOwnerScopeOrAuthHelper` (umbenannt, prüft jetzt auf `OwnerScopeService`-Injection), `staffelScopedWriteEndpointsMustGateOnOwnerScopeService` (umbenannt, akzeptiert nur noch `@ownerScopeService` im SpEL — die R2.5-eingeführte Übergangs-Akzeptanz für `@squadronScopeService` ist entfernt). Keine Verhaltensänderung; alle 1771 Tests weiter grün. R4 (Aggregate-FK-Migration `owningSquadron → owningOrgUnit`) und R5 (Frontend-UI für Spezialkommandos) sind die nächsten Etappen.

### Fixed

- **Race-Condition beim Zusammenführen von Inventar-Einträgen mit gleichem Natural-Key abgesichert.** Wenn zwei Aufrufer parallel via `createInventoryItem`, `updateInventoryItem` oder den Raffinerie-Store-Pfad auf dasselbe Match (gleiche User+Material+Location+Quality+Mission+JobOrder+Personal) trafen, las jeder den `amount` einmal vor dem Inkrement — und der letzte Schreiber überschrieb den anderen Bestand-Inkrement (silent stock loss). Neue Repository-Methode `findMatchingInventoryItemForUpdate` mit `@Lock(PESSIMISTIC_WRITE)` sequentialisiert die Aufrufer auf der Merge-Ziel-Row; Single-Caller-Verhalten ist identisch. Pre-existing-Bug, durch die R9-Cleanup-Lifecycle-Hook-Entfernung wegen früherem Auto-Flush sichtbarer geworden.

- **Doppel-Anmeldung im Einsatz / doppelte Crew-Zuweisung zeigt jetzt die echte Fehlermeldung ("bereits Teilnehmer …") statt der "Daten zwischenzeitlich geaendert"-Reload-Frage.** Der Mission-Detail-AJAX-Layer hat bisher jeden 409 als Optimistic-Lock-Konflikt behandelt, weil der Frontend-Controller den RFC-7807-Body verworfen hat; jetzt propagiert er `code` + `detail` und das JS zeigt die Reload-Confirmation nur noch fuer `OPTIMISTIC_LOCK`/`PESSIMISTIC_LOCK`. Die `DuplicateEntityException`-Texte in [`MissionService`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/MissionService.java) sind dabei zu i18n-Keys migriert (DE/EN).

- **Login-Redirect landet nicht mehr auf der 404-Seite, wenn vor dem Login eine Asset-Anfrage gesaved wurde.** Statische Asset-Pfade (`/sm/**`, `/**/*.map`, `/favicon.ico`, `/error/**`) sind jetzt in [`SecurityConfig`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/config/SecurityConfig.java) als `permitAll` deklariert, damit Background-Sourcemap-Requests von DevTools und Browser-Extensions (z. B. Sentry Replay's `/sm/<hash>.map`) nicht in den Spring-Security-`HttpSessionRequestCache` als Saved-Request rutschen und nach einem späteren Klick auf "Anmelden" als Redirect-Ziel hochkommen. Als Defense-in-Depth wrappt der neue [`AssetAwareAuthenticationSuccessHandler`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/config/AssetAwareAuthenticationSuccessHandler.java) den `SavedRequestAwareAuthenticationSuccessHandler` und biegt Saved-URLs mit Asset-Extensions (`.map`, `.ico`, `.png`, `.jpg`, `.svg`, `.css`, `.js`, `.woff*`, `.json`, `.txt` u. a.) oder `/sm/`-Prefix auf `/` zurück.

- **Einlager-Dialog für Raffinerieaufträge: Menge wurde 100x zu groß ins Lager geschrieben, wenn das Ausgangsmaterial einen `NULL`-Mengentyp hatte (Issue [#230](https://github.com/greluc/iridium-basetool/issues/230)).** Der UEX-Commodity-Sync hat das Feld `quantity_type` noch nie gesetzt, sodass jedes per UEX importierte Material in der DB mit `quantity_type = NULL` landete; das Prefill in [`RefineryOrderPageController`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/controller/RefineryOrderPageController.java) hat `NULL` als "nicht SCU" interpretiert und die Units→SCU-Umrechnung übersprungen — eine 2,21 SCU Ausgangsmenge Ouratite wurde so als 221 SCU eingebucht. Drei-stufiger Fix: (1) Defensive im Controller: alles außer expliziten `"PIECE"` wird wie SCU konvertiert (Raffinerien produzieren ohnehin nie stückweise gezähltes Material); (2) Flyway [`V95__backfill_material_quantity_type.sql`](backend/src/main/resources/db/migration/V95__backfill_material_quantity_type.sql) backfillt `NULL → 'SCU'` und tightent die Spalte auf `NOT NULL DEFAULT 'SCU'`; (3) [`Material`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/model/Material.java)-Entity initialisiert das Java-Feld auf `QuantityType.SCU` und markiert die Spalte `nullable = false`, sodass neue Materialien aus dem UEX-Sync (`new Material()` → `save()`) jetzt explizit `'SCU'` schreiben statt sich auf den DB-DEFAULT zu verlassen (Hibernate hängt den Java-Wert immer an, sodass der DEFAULT vorher nie griff). Regression-Test [`RefineryOrderStorageCalculationTest.testStoreFormCalculationForNullQuantityTypeDefaultsToScu`](frontend/src/test/java/de/greluc/krt/profit/basetool/frontend/controller/RefineryOrderStorageCalculationTest.java) pinnt das neue Verhalten an der Reproduktion aus der Issue.

- **Demote eines Spezialkommando-Mitglieds via Uncheck der `Logistiker`- oder `Einsatzleiter`-Checkbox schreibt jetzt tatsächlich `false` (R5.d.a-Bugfix).** Vorher hat die ungesetzte Checkbox kein Formularfeld geschickt, der Frontend-Controller hat das als "keine Änderung" durchgereicht und der Backend-Patch hat den alten `true`-Wert behalten. Fix per Spring-`_field`-Hidden-Marker pro Checkbox + Umstellung des Flags-PATCH-Endpoints auf `@ModelAttribute MembershipFlagsForm`-Record-Binding.

- **Owner-Picker-Fragment plus Lager-Eingabe als Referenz-Integration (R5.d.a).** Neues wiederverwendbares Thymeleaf-Fragment [`fragments/owner-picker.html`](frontend/src/main/resources/templates/fragments/owner-picker.html) und Backend-Endpoint `GET /api/v1/users/{id}/memberships`, integriert in die Lager-Eingabe (`/inventory/input`). Picker bleibt im Single-Membership-Fall versteckt — solange jeder User nur eine Staffel hat (Stand heute), ist die UI unverändert; sobald User mehreren OrgUnits angehören, erscheint das Dropdown automatisch. Backend-DTO `InventoryItemCreateDto` und `InventoryItemService` akzeptieren ein optionales `owningOrgUnitId` und validieren es gegen die Mitgliedschaften des Ziel-Users; SK-Selektion wird derzeit mit 400 abgewiesen, da die Legacy-NOT-NULL auf `owning_squadron_id` noch nicht gefallen ist.

- **Per-SK-Detailseite mit Mitglieder-Roster unter `/admin/special-commands/{id}` (R5.c.b).** Vollständige Mitgliederverwaltung pro Spezialkommando in der UI — der Admin kann auf der Listenseite jetzt per "Mitglieder verwalten"-Button durchklicken zur Detailseite, die SK-Header (Name/Kürzel/Beschreibung) plus ein Roster-Table aller Mitglieder zeigt. Pro Roster-Zeile: Display-Name + Lead-Badge falls aktiv, Inline-Form für die `is_logistician`/`is_mission_manager`-Checkboxes mit Speichern-Button (Optimistic-Lock via Version-Hidden-Input), Lead-Toggle-Button (promote/demote) und Entfernen-Button. Add-Member-Modal mit Dropdown aus `/api/v1/users/lookup`. Neuer [`AdminSpecialCommandsPageController`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/controller/AdminSpecialCommandsPageController.java)-Endpoint-Set für die fünf neuen Pfade (`GET /{id}`, `POST /{id}/members`, `POST /{id}/members/{userId}/delete`, `POST /{id}/members/{userId}/flags`, `POST /{id}/members/{userId}/lead`) — alle ADMIN-only via Class-level-Guard, proxy an die R5.b-Backend-Endpoints. Neue Frontend-DTOs: [`OrgUnitMembershipDto`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/model/dto/OrgUnitMembershipDto.java) + [`OrgUnitKind`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/model/dto/OrgUnitKind.java) (Spiegel der Backend-Wire-Shape). Neue Thymeleaf-Page [`admin/special-command-detail.html`](frontend/src/main/resources/templates/admin/special-command-detail.html) mit CSP-safe Modal-Wiring. 18 neue i18n-Keys in DE (Umlauts als `\uXXXX`) + EN. Owner-Picker-Fragment für die Create-Forms (R5.d) und Active-Context-Switcher-Widening auf Non-Admins (R5.e) folgen.

- **Admin-Seite zur Spezialkommando-Verwaltung unter `/admin/special-commands` (R5.c).** Erste Frontend-Phase der Spezialkommando-Erweiterung: vollständige Admin-UI für SK-Lifecycle (anlegen, umbenennen, soft-delete, reaktivieren) — ADMIN-only. Neue Thymeleaf-Page [`admin/special-commands.html`](frontend/src/main/resources/templates/admin/special-commands.html) mit Tabelle (Name / Kürzel / Beschreibung / Aktion), Create/Edit-Modal das per JS zwischen Create- und Edit-Modus toggelt (Edit-Button preloaded data-* Attributen), `includeInactive`-Toggle wie auf der Staffel-Section von `/admin/mission-data`. Neuer [`AdminSpecialCommandsPageController`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/controller/AdminSpecialCommandsPageController.java) folgt dem etablierten Pattern (BindingResult-inline-rerender, 409 → dedizierter Toast, generischer Fehler → Redirect mit Error-Param), proxied an die R5.a-Backend-Endpoints unter `/api/v1/special-commands`. Frontend-Spiegel-Records: [`SpecialCommandDto`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/model/dto/SpecialCommandDto.java) + [`SpecialCommandForm`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/model/form/SpecialCommandForm.java). Neuer Sidebar-Eintrag "Spezialkommandos" zwischen "Missionsdaten" und "Orte" (admin-only via `sec:authorize`). 16 neue i18n-Keys in `messages_de.properties` (Umlauts korrekt als `\uXXXX` escaped per CLAUDE.md-Regel) + `messages_en.properties`. Member-Roster-Page (`/admin/special-commands/{id}` mit Add/Remove/Flag-Toggle), Owner-Picker-Fragment für die Create-Forms und Active-Context-Switcher-Widening auf Non-Admin-User folgen in späteren PRs.

- **Mitgliedschafts-Verwaltung für Spezialkommandos auf `/api/v1/special-commands/{id}/members` (R5.b).** Ergänzt die R5.a-CRUD-API um die fünf Endpoints für die Pflege der SK-Mitgliederliste: `GET /` (Roster für Admin oder Lead-of-this-SK), `POST /{userId}` (Mitglied hinzufügen), `DELETE /{userId}` (Mitglied entfernen), `PATCH /{userId}` (Rollen-Flags `is_logistician` / `is_mission_manager` mit Optimistic-Lock), `PATCH /{userId}/lead` (ADMIN-only Lead-Promotion). [`OrgUnitMembershipService`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/OrgUnitMembershipService.java) implementiert die CRUD-Logik gegen `org_unit_membership`, [`SpecialCommandSecurityService.canManageMembers(scId, authentication)`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/SpecialCommandSecurityService.java) implementiert die "ADMIN oder Lead-dieser-SK"-Auth-Regel die alle Member-Mutations gaten (außer dem Lead-Toggle der ADMIN-only ist, damit ein Lead sich nicht selbst befördern kann). Neue DTOs: [`OrgUnitMembershipDto`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/model/dto/OrgUnitMembershipDto.java) (Wire-Shape mit entpacktem Composite-Key), [`MembershipFlagsPatchRequest`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/model/dto/MembershipFlagsPatchRequest.java) (boxed Booleans erlauben PATCH-Semantik "null = unchanged"), [`MembershipLeadToggleRequest`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/model/dto/MembershipLeadToggleRequest.java). MapStruct-Mapper [`OrgUnitMembershipMapper`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/mapper/OrgUnitMembershipMapper.java) entpackt den Embedded-Composite-Key in flache UUID-Felder und liest `user.displayName` durch die LAZY-Assoziation für die Roster-Anzeige. 32 neue Unit-Tests (7 Security + 16 Service + 5 Controller) pinnen die Auth-Pfade, CRUD-Pfade, Optimistic-Lock-409, Duplicate-409, NotFound-404 und die "Lead-eines-anderen-SK-zählt-nicht"-Regel. Squadron-side Membership-Mutations (Migration der `app_user.is_logistician` / `is_mission_manager` Flags auf die Membership-Row) bleiben für eine spätere Phase. R5.c (Frontend Admin-UI + Owner-Picker) baut direkt auf diesen Endpoints auf.

- **REST-CRUD-API für Spezialkommandos auf `/api/v1/special-commands` (R5).** Vollständige Backend-Surface für die in R2.a eingeführte `SpecialCommand`-Entity: [`SpecialCommandService`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/SpecialCommandService.java) (CRUD mit case-insensitive Uniqueness-Check, Optimistic-Lock, Soft-Delete + Activate), [`SpecialCommandController`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/controller/SpecialCommandController.java) mit Endpoints `GET /` (paged, optional includeInactive für Admins, ansonsten authenticated für Owner-Picker), `GET /{id}` (Admin), `POST /` (Admin), `PUT /{id}` (Admin), `DELETE /{id}` (Admin, soft), `POST /{id}/activate` (Admin). [`SpecialCommandDto`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/model/dto/SpecialCommandDto.java) ist die Wire-Shape (spiegelt `SquadronDto` field-for-field bis auf das absichtlich weggelassene `isPromotionEnabled`-Feld — auf SK permanent FALSE durch V97-CHECK + Constructor-Override). [`SpecialCommandMapper`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/mapper/SpecialCommandMapper.java) via MapStruct. Volle SpringDoc-Annotationen + `@PreAuthorize` auf jedem Endpoint. Tests: 16 Service-Tests + 9 Controller-Tests (Mockito), die CRUD-Pfade, Uniqueness-409, Optimistic-Lock-409, NotFound-404, includeInactive-Admin-Gate und die Constructor-`isPromotionEnabled=false`-Garantie pinnen. R5.b (Mitgliedschafts-Endpoints) und R5.c (Frontend-Owner-Picker + Admin-UI) bauen darauf auf.

- **Aggregate-Entities tragen jetzt einen `owningOrgUnit`-Spiegel neben dem Legacy-`owningSquadron`-Feld (R4).** Die fünf strict-staffel Aggregate ([`Mission`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/model/Mission.java), [`Operation`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/model/Operation.java), [`Ship`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/model/Ship.java), [`InventoryItem`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/model/InventoryItem.java), [`RefineryOrder`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/model/RefineryOrder.java)) bekommen ein zweites `@ManyToOne OrgUnit owningOrgUnit`-Feld neben dem bestehenden `owningSquadron`-Feld — gemappt auf die `owning_org_unit_id`-Spalte, die V99 in R1 angelegt hatte; [`JobOrder`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/model/JobOrder.java) bekommt analog `creatingOrgUnit` + `requestingOrgUnit`. Ein gemeinsamer `@PrePersist`/`@PreUpdate`/`@PostLoad`-Lifecycle-Hook auf jedem Aggregat synchronisiert beide Felder auf jedem Code-Pfad: `owningSquadron` bleibt während der R4-Soak-Phase autoritativ (gewinnt wenn beide gesetzt sind), das neue Feld wird beim Persistieren immer aus dem Legacy-Feld mitgeschrieben. Damit halten alle bestehenden `setOwningSquadron(...)`-Calls die DB-Spalten im Gleichschritt — ohne dass eine einzige Service-Layer-Zeile geändert werden müsste. Polymorphic load: das neue Feld ist `OrgUnit`-typisiert, sodass Hibernate beim Lesen auf eine `Squadron`- oder `SpecialCommand`-Subclass-Instance ausweicht — heute praktisch immer Squadron, bereit für die Spezialkommando-Owner-Stamps die R5 einführen wird. Die neuen FK-Spalten bleiben absichtlich nullable für die Soak-Phase; eine spätere destruktive Release-Phase tightet sie auf NOT NULL und droppt dann das Legacy-Feld. Keine Verhaltensänderung; alle 1771 Tests passieren weiter.

## [v0.2.28](https://github.com/krt-profit/basetool/releases/tag/v0.2.28) - 2026-05-22

### Changed

- **Admins können Materialien manuell anlegen (Modal im Material-Admin) — für Rohstoffe wie "Raw Ouratite", die in UEX (noch) fehlen.** Server stempelt `isManualEntry=true`, die Liste zeigt einen orangenen "Manuell"-Badge; der nächste UEX-Sync übernimmt den Eintrag automatisch per Name-Match (setzt `id_commodity` + räumt das Flag wieder ab). Backend-Endpoint `POST /api/v1/materials` nimmt nun ein dediziertes `MaterialCreateDto` mit Bean-Validation; refined-/category-Refs werden im Service per ID aufgelöst, ein `refinedMaterialId` auf einem Nicht-Rohstoff wird mit 400 abgelehnt.

## [v0.2.26](https://github.com/krt-profit/basetool/releases/tag/v0.2.26) - 2026-05-21

### Fixed

- **"Alle Schiffe löschen" im Hangar warf 403, ebenso "Globales Lager leeren" im Admin-Inventar.** Beide AJAX-`DELETE`-Aufrufe (`/hangar/ships/all` in [`hangar.html`](frontend/src/main/resources/templates/hangar.html), `/inventory/all` in [`inventory-admin.html`](frontend/src/main/resources/templates/inventory-admin.html)) schickten keinen CSRF-Token mit, seit Audit H-6 die CSRF-Carveouts entfernt hatte — Spring Security lehnte sie damit vor dem Controller ab. Beide Fetches lesen jetzt `meta[name="_csrf"]` / `meta[name="_csrf_header"]` und setzen den Token-Header analog zum Hangar-Import-Pattern.

## [v0.2.25](https://github.com/krt-profit/basetool/releases/tag/v0.2.25) - 2026-05-21

### Changed

- **UEX-Bonus/Malus-Badge auch auf der Raffinerieauftrag-Anlegen-Seite.** Die Anlegen-Maske rendert jetzt dieselben +/- Prozent-Badges neben dem Ausgangsmenge-Feld (Units) wie die Detailseite und aktualisiert sie live, sobald der Nutzer eine Raffinerie oder ein Eingangsmaterial waehlt. Die Logik liegt in einer gemeinsamen JS-Datei [`refinery-yield-badge.js`](frontend/src/main/resources/static/js/refinery-yield-badge.js) (IIFE, exportiert `window.krtRefineryYield` mit `init`/`setBadge`/`refreshFor`/`refreshAll`/`onLocationChange`); beide Templates ([`refinery-orders-create.html`](frontend/src/main/resources/templates/refinery-orders-create.html), [`refinery-orders-details.html`](frontend/src/main/resources/templates/refinery-orders-details.html)) ziehen das Modul ueber `<script th:src="@{/js/refinery-yield-badge.js}">` im `extraLinks`-Block und initialisieren es per Inline-Aufruf mit der server-gerenderten Initial-Map (`materialYieldBonuses`) und dem i18n-Tooltip. [`viewCreateForm`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/controller/RefineryOrderPageController.java) fuellt `materialYieldBonuses` aus `fetchYieldsForLocation(form.locationId)` — leer fuer einen frischen GET, vorgeladen bei POST-Validation-Rebind mit bereits gewaehlter Raffinerie. Der bestehende AJAX-Proxy `GET /refinery-orders/locations/{id}/yields` bleibt unveraendert und bedient jetzt beide Seiten.

- **Raffinerieauftrag-Anlegen-Seite zieht jetzt das gleiche "Material #N"-Header-Layout wie die Detailseite.** Jeder Material-Block bekommt einen kompakten Header (kleine "Material #N"-Ueberschrift links, "Entfernen"-Button rechts, dezente Trennlinie nach unten); der "Entfernen"-Button ist nicht mehr absolut oben rechts im Block positioniert. Damit verschwindet die Kollision zwischen Button und Eingabefeldern, sobald das neue Bonus-Badge im Ausgangsmenge-Label landet, und beide Raffinerieauftrag-Seiten haben jetzt das gleiche optische Material-Layout. JS in [`refinery-orders-create.html`](frontend/src/main/resources/templates/refinery-orders-create.html) gleicht die `addMaterialRow`/`removeMaterialRow`-Logik der Detailseite an: geklonte Rows bekommen die fortlaufende Nummer im Titel, neue Rows bekommen den Remove-Button per `data-trigger` in den Header gesetzt, beim Loeschen werden die Titel der nachfolgenden Rows umnummeriert.

## [v0.2.24](https://github.com/krt-profit/basetool/releases/tag/v0.2.24) - 2026-05-21

### Changed

- **Einsatz-Dropdown auf Raffinerieauftrag-Anlegen und -Detailseite zeigt nur noch Einsaetze der letzten drei Monate, neueste oben.** Filter laeuft ueber `plannedStartTime >= now - 3 Monate` (zukuenftig geplante Einsaetze sind inbegriffen), Sortierung absteigend nach `plannedStartTime`. Bestehende Auftraege, die einen aelteren Einsatz verknuepft haben, behalten ihren Eintrag im Drop-down — der verknuepfte Einsatz wird zusaetzlich angehaengt, damit die Auswahl beim Speichern nicht stillschweigend verloren geht. Logik liegt als testbare package-private statische Methode `filterAndSortMissionsForDropdown` in [`RefineryOrderPageController`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/controller/RefineryOrderPageController.java); Verhaltensregeln pinnt [`RefineryOrderMissionDropdownTest`](frontend/src/test/java/de/greluc/krt/profit/basetool/frontend/controller/RefineryOrderMissionDropdownTest.java) (Cut-off inklusive, Einsaetze ohne `plannedStartTime` werden gedroppt, preservierter Einsatz wird nicht doppelt eingetragen).

## [v0.2.23](https://github.com/krt-profit/basetool/releases/tag/v0.2.23) - 2026-05-21

### Changed

- **Raffinerieauftrag-Detailseite zeigt den UEX-Bonus/Malus pro Eingangsmaterial neben dem Ausgangsmenge-Feld (Units) als +/- Prozent-Badge — und aktualisiert sich live beim Material- oder Raffinerie-Wechsel.** Backend reichert `RefineryGoodDto` um ein neues `yieldBonusPercent`-Feld an, das der Mapper aus einem von `RefineryOrderService.getYieldBonusByMaterialForLocation(Location)` gelieferten `materialId -> bonus`-Mapping fuellt (Lookup matcht den `RefineryYield`-`Terminal` ueber `cityName`/`spaceStationName` der Order-Location). Der bestehende stuendliche UEX-Sync via [`UexRefinerySyncService.syncRefineryYields`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/UexRefinerySyncService.java) liefert die Daten bereits — keine neue Sync-Pipeline noetig. Frontend-Spiegel-Record und Thymeleaf-Template ([`refinery-orders-details.html`](frontend/src/main/resources/templates/refinery-orders-details.html)) rendern den Bonus mit semantischen Department-Farben (Profit-Gruen positiv, Combat-Rot negativ, Grau bei 0). Neuer `GET /api/v1/refinery-orders/locations/{locationId}/yields`-Endpoint (plus ein gleichnamiger AJAX-Proxy am Frontend-Page-Controller) liefert die Map fuer eine beliebige Refinery-Location als JSON, sodass das Detail-Page-JS das Badge sofort updated wenn der Nutzer ein anderes Eingangsmaterial waehlt (Lookup in der in-memory Map) oder eine andere Raffinerie picked (re-fetch der Map per `fetch()`). Initiale Map wird via Thymeleaf-Inline-JS als `yieldByMaterialId`-Objekt eingebettet, sodass die erste Render-Phase ohne zusaetzlichen Roundtrip auskommt. Der "Entfernen"-Button pro Material wandert in einen neuen Material-Header (kleine "Material #N"-Ueberschrift links, Button rechts, dezente Trennlinie nach unten) — vorher war er absolute-positioniert oben rechts und kollidierte mit dem Bonus-Badge, sobald dieses ins Label der betroffenen Mengen-Spalte landete.

- **Pflichtfeld-Stern auf der Raffinerie-Detailseite sitzt jetzt direkt hinter "Ausgangsmenge (Units)" statt hinter dem Yield-Bonus-Badge.** Die globale `.form-group:has([required]) > label::after`-Regel rendert den Stern am Ende des Labels — als die UEX-Bonus-Anzeige in den Label-DOM gewandert ist, schob sie den Stern bis hinter das `+X%`/`-X%`-Badge, sodass der Stern visuell zum Yield gehoerte statt zur Ausgangsmenge. Neue page-lokale CSS-Regel in [`refinery-orders-details.html`](frontend/src/main/resources/templates/refinery-orders-details.html) verankert den Stern auf dem ersten `<span>` (Feldname) und unterdrueckt das aeussere `::after` fuer Labels, die ein `.yield-bonus-badge` enthalten — Single-Text-Labels in anderen Forms sind nicht betroffen, weil der Selektor `:has(> .yield-bonus-badge)` ausschliesslich auf diese Spezialform matched.

## [v0.2.22](https://github.com/krt-profit/basetool/releases/tag/v0.2.22) - 2026-05-21

### Changed

- **Raffinerieauftrag-Formular: "Qualitaet" sitzt jetzt in der ersten Material-Zeile rechts neben "Eingangsmenge".** In [`refinery-orders-create.html`](frontend/src/main/resources/templates/refinery-orders-create.html) und [`refinery-orders-details.html`](frontend/src/main/resources/templates/refinery-orders-details.html) wandert das `quality`-Feld eine Zeile hoeher; obere Zeile ist jetzt Eingangsmaterial / Eingangsmenge / Qualitaet, untere Zeile Ausgangsmaterial / Ausgangsmenge (Units) / Ausgangsmenge (SCU). Spalten richten sich vertikal aus (flex 2/1/1 in beiden Zeilen).

## [v0.2.21](https://github.com/krt-profit/basetool/releases/tag/v0.2.21) - 2026-05-21

### Removed

- **Spalte "Erstellt durch" aus der Auftragsuebersicht entfernt.** Die zweite Staffel-Badge (creating squadron) in [`orders-index.html`](frontend/src/main/resources/templates/orders-index.html) ist raus inklusive Tabellen-`colspan`-Anpassung und der zugehoerigen `orders.index.creating_squadron`-i18n-Keys in allen drei [`messages*.properties`](frontend/src/main/resources/messages.properties). Die Information bleibt auf der Auftrags-Detailseite weiterhin sichtbar.

## [v0.2.20](https://github.com/krt-profit/basetool/releases/tag/v0.2.20) - 2026-05-21

### Changed

- **Raffinerieauftrag-Formular: Auftragskosten / Sonstige Kosten / Ore Sales zeigen immer 0 wenn der Nutzer nichts eingibt.** Server-seitig: GET-Prefill in [`RefineryOrderPageController`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/controller/RefineryOrderPageController.java) gibt fuer `expenses` jetzt 0 zurueck statt `null` (Mirror der bereits existierenden Logik fuer `otherExpenses`/`oreSales`), die POST-Pfade ersetzen `zeroToNull(...)` durch `nullToZero(...)` und senden damit immer einen expliziten 0-Wert an das Backend statt 0 stillschweigend auf `null` zu mappen. Client-seitig: neuer `focusout`-Handler auf den drei `<input>`s in [`refinery-orders-create.html`](frontend/src/main/resources/templates/refinery-orders-create.html) und [`refinery-orders-details.html`](frontend/src/main/resources/templates/refinery-orders-details.html) setzt das Feld zurueck auf `0`, sobald der Nutzer es leert und wegtabt — die Profit-Preview unten wird sofort mit-aktualisiert. `focusout` statt `blur`, weil event-delegation auf `document` haengt und `blur` nicht bubblet.

- **Raffinerieauftrag-Formular: "Ausgangsmenge (Units)" ist jetzt Pflichtfeld, "Qualitaet" defaultet auf 0.** Im Anlegen- und Detail-Template ([`refinery-orders-create.html`](frontend/src/main/resources/templates/refinery-orders-create.html), [`refinery-orders-details.html`](frontend/src/main/resources/templates/refinery-orders-details.html)) traegt das `outputQuantity`-Input jetzt das HTML5-Attribut `required`, und der Wert wird zusaetzlich server-seitig per `@NotNull` auf [`RefineryGoodForm`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/model/form/RefineryGoodForm.java) und [`RefineryGoodDto`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/model/dto/RefineryGoodDto.java) durchgesetzt — `@Valid` auf dem Page-Controller-Endpoint und der Backend-Wire-Shape lassen ein leeres Feld nicht mehr durch. Das `quality`-Feld auf `RefineryGoodForm` initialisiert jetzt mit `0`, und die beiden POST-Handler in [`RefineryOrderPageController`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/controller/RefineryOrderPageController.java) (create + update) normalisieren `quality == null -> 0` bevor sie das DTO an das Backend schicken — "der Nutzer gibt nichts ein" verhaelt sich damit identisch zu "Nutzer tippt 0".

## [v0.2.18](https://github.com/krt-profit/basetool/releases/tag/v0.2.18) - 2026-05-21

### Fixed

- **Formularzeilen auf /orders/create, /refinery-orders/{id}, /refinery-orders/create und ähnlichen Seiten: Dropdowns, Eingabefelder, Datum/Zeit-Inputs und der „Jetzt"-Button in derselben Zeile hatten unterschiedliche Höhen, wodurch die Label darüber auf verschiedenen Y-Positionen landeten.** Globale Höhe für einzeilige Form-Controls in [`styles.css`](frontend/src/main/resources/static/css/styles.css) von `2.5rem` (40 px) auf `2.75rem` (44 px) angehoben — passt damit zur 44-px-Mindesthöhe von `.btn` und den read-only-Display-Divs (`endsAtDisplay`, `output-material-display`, `methodRatings`). Zusätzlich `main .form-group select`-Override (höhere Spezifität als die per-Template `.form-group input, .form-group select, .form-group textarea { padding: 0.75rem }`-Regel), das das Select-Padding auf den descender-sicheren Wert `0.375rem 2rem 0.375rem 0.75rem` zurücksetzt — sonst inflatierte die 12-px-Padding-Vorgabe das `<select>` intrinsisch auf ~50 px und ließ es trotz `min-height: 2.75rem` höher rendern als die `<input>`s daneben. Redundante Mobile-Media-Queries für Input-/Select-/Search-Datum-Zeit-Höhen entfallen, da Desktop und Mobile jetzt denselben Wert nutzen.

## [v0.2.17](https://github.com/krt-profit/basetool/releases/tag/v0.2.17) - 2026-05-21

### Fixed

- **Raffinerieauftrag-Anlage und -Detailseite: Formularinhalt war auf 800 px gedeckelt und damit deutlich schmaler als die umgebende `.hud-box`, die Felder der 5-spaltigen Kosten- und 4-spaltigen Materialzeilen quetschten und überlappten.** Die lokale `.form-container { max-width: 800px }`-Override in [`refinery-orders-details.html`](frontend/src/main/resources/templates/refinery-orders-details.html) und [`refinery-orders-create.html`](frontend/src/main/resources/templates/refinery-orders-create.html) ist entfernt; der Container füllt jetzt die `.hud-box` aus und teilt die Spalten gleichmäßig auf die volle Hauptbreite (max. 1200 px) auf.

## [v0.2.16](https://github.com/krt-profit/basetool/releases/tag/v0.2.16) - 2026-05-21

### Removed

- **Versionsanzeige aus der Seitenleiste entfernt.** Der `.sidebar-version`-Block (kleines `v{version}`-Label unter dem Sprach-Switcher in [`fragments/sidebar.html`](frontend/src/main/resources/templates/fragments/sidebar.html)) und seine CSS-Regel in [`styles.css`](frontend/src/main/resources/static/css/styles.css) sind raus, weil dieselbe Information jetzt im globalen Footer steht und doppelte Anzeige redundant ist. `AppVersionAdvice` und die `app.version.*`-i18n-Keys bleiben — der Footer und potenzielle kuenftige Konsumenten nutzen sie weiter.

- **"Deutsch" / "English"-Sprachlabels von der Maintenance-Page entfernt.** [`docker/maintenance/static/maintenance.html`](docker/maintenance/static/maintenance.html) zeigte die Sprache jeder der beiden `<section>`-Bloecke vor der Headline noch einmal explizit; "Wartungsarbeiten" vs. "Maintenance in Progress" sind selbsterklaerend. Die `.lang-label`-CSS-Regel und die zwei `<span class="lang-label">`-Einsprenger sind raus.

- **"System Maintenance"-Headline auf der Maintenance-Page entfernt.** Das `<h1>`-Element war redundant: das `Maintenance`-Status-Badge direkt darueber sagt dasselbe in kuerzer, und die beiden Sprach-`<h2>`s ("Wartungsarbeiten" / "Maintenance in Progress") tragen die eigentliche Meldung. `h1`-CSS-Regel und die zugehoerigen Media-Query-Zeilen ebenfalls geloescht. `<main aria-labelledby="page-title">` zeigt ohne das `<h1>` ins Leere, also auf `aria-label="Maintenance"` umgestellt. Zusaetzlich `.status-row { margin-bottom: 1rem -> 2rem }` fuer mehr Luft zwischen Status-Badge und erster Sprach-Sektion.

## [v0.2.15](https://github.com/krt-profit/basetool/releases/tag/v0.2.15) - 2026-05-21

### Fixed

- **Footer klebte nicht mehr an der Viewport-Unterkante auf kurzen Seiten.** Nach dem Width-Fix war `body` wieder im Block-Layout, der Footer floss damit direkt nach dem Content statt am Viewport-Boden zu sitzen. Body ist jetzt wieder `display: flex; flex-direction: column; min-height: 100vh`, `<main>` wird mit `width: 100%; box-sizing: border-box; flex: 1 0 auto` ausgestattet — dadurch fuellt main den verbleibenden Flex-Platz (Footer rutscht ans Ende) und behaelt gleichzeitig seine bis-zu-`max-width: 1200px`-Breite mit Auto-Margin-Zentrierung. `width: 100%` ueberlaesst der Flex-Item-Width-Berechnung keine Wahl mehr; das ist der strukturelle Grund, weshalb dieselbe Layout-Kombination diesmal nicht erneut auf intrinsische Content-Breite kollabiert.

- **`Einbuchen`-Seite (`/inventory/input`) warf 500.** Das Auftrag-Dropdown referenzierte `order.squadron` auf einem `JobOrderReferenceDto`-Eintrag, obwohl Phase 7 Teil 3 (V90, PR [#137](https://github.com/krt-profit/basetool/pull/137)) das `squadron`-Feld aus dem Referenz-DTO entfernt hat. Thymeleaf schlug beim Render mit `Property or field 'squadron' cannot be found` fehl — sowohl in "Mein Lager" als auch im "Globalen Lager". Label-Format ist jetzt `'#' + displayId + ' - ' + handle + ' (' + status + ')'` ohne Staffel-Shorthand; der Cross-Staffel-Kontext steht weiterhin im Auftrag-Detail.

## [v0.2.14](https://github.com/krt-profit/basetool/releases/tag/v0.2.14) - 2026-05-21

### Fixed

- **Content-Boxen auf vielen Seiten waren nach dem Design-Audit zu schmal (Index, Operations-Uebersicht, Refinery-Auftrag-Anlage, JobOrder-Anlage, Material-Uebersicht, persoenliches Inventar, Befoerderung).** Ursache war das `body { display: flex; flex-direction: column }` aus dem Footer-Layout: kombiniert mit `<main { max-width: 1200px; margin: 0 auto }>` als Flex-Item haben die Auto-Margins die `stretch`-Default-Anordnung uebersteuert, sodass `main` nicht mehr die volle Container-Breite bis zum max-width nutzen konnte und stattdessen auf die intrinsische Content-Breite kollabierte. Body ist wieder ein normales Block-Layout; der Footer wird stattdessen per JS in [`fragments/sidebar.html`](frontend/src/main/resources/templates/fragments/sidebar.html) ans Ende von `<body>` verschoben, sobald `DOMContentLoaded` feuert.

- **Ausbuchen-Dialog im globalen Lager zeigte vertikale Scrollbars, obwohl der Inhalt rein passt.** Ursache war `.modal-content { max-height: 90vh; overflow-y: auto }` aus der Modal-Konsolidierung — das hatte den Container abgeschnitten, statt die Outer-`.modal { overflow: auto }` das Scrolling uebernehmen zu lassen. `max-height` + inneres Overflow sind entfernt; der bewaehrte Outer-Scroll-Pfad ist wieder aktiv.

## [v0.2.13](https://github.com/krt-profit/basetool/releases/tag/v0.2.13) - 2026-05-21

### Changed

- **Operations-Uebersicht mit Filter-Leiste (Suche + "Vergangene anzeigen").** Freitext-Suche ueber Name + Beschreibung; Checkbox blendet COMPLETED/CANCELED ein. Neuer `GET /api/v1/operations/search`-Endpoint inkl. Repository-/Service-Layer; Frontend filtert per debounced AJAX gegen das `operationsResults`-Fragment. Ein Datumsbereichs-Filter wie auf der Einsatz-Uebersicht entfaellt bewusst, weil Operationen keine eigene `plannedStartTime` besitzen.

- **Globaler Footer mit Impressum / Datenschutz / Nutzungsbedingungen / Versionsnummer auf jeder Seite.** Neues Fragment [`fragments/footer.html`](frontend/src/main/resources/templates/fragments/footer.html) wird ueber [`fragments/sidebar.html`](frontend/src/main/resources/templates/fragments/sidebar.html) eingebunden und per Flex `order: 999` ans Body-Ende geschoben. Body ist jetzt `display: flex; flex-direction: column; min-height: 100vh` mit `body > main { flex: 1 0 auto }`, damit der Footer auch auf kurzen Seiten am unteren Rand klebt. Legal-Links bleiben zusaetzlich in der Sidebar fuer keyboard-/navigation-first User.

- **SVG-Icon-Sprite als Foundation fuer kuenftige Icon-Verwendung.** Neues Fragment [`fragments/icons.html`](frontend/src/main/resources/templates/fragments/icons.html) deklariert die Basis-Glyphen (close, chevron-up/down/left/right, warning, success, info, plus, minus, search, filter, edit, trash) als `<symbol>`-Set in einem versteckten `<svg>`-Sprite (Sichtbarkeit via neuer `.krt-icon-sprite` 0x0 / position:absolute-Regel in styles.css). Verwendung: `<svg class="krt-icon" aria-hidden="true"><use href="#krt-icon-NAME"/></svg>` — Icons erben Farbe via `currentColor`, Groesse via `.krt-icon-lg` / `.krt-icon-xl` Modifier. Sprite wird einmal pro Seite via Sidebar-Fragment geladen. Modal-Close-Buttons in [`inventory-my.html`](frontend/src/main/resources/templates/inventory-my.html) als Beispiel von `&times;` auf das `krt-icon-close`-Sprite migriert; weitere Templates koennen das gleiche Pattern uebernehmen ohne erneut SVGs einzubetten.

- **Admin-Mode-Indikator im Header (kleiner "ADMIN"-Chip + akzentdunkle Border).** Sidebar-JS in [`fragments/sidebar.html`](frontend/src/main/resources/templates/fragments/sidebar.html) prueft `window.location.pathname.startsWith('/admin/')` beim DOMContentLoaded und addet bei Treffer die Klasse `.admin-mode` an `<body>` plus einen `.admin-mode-chip`-Span ins `<header><nav>`. Rein visueller UX-Cue gegen "ich bin in den Admin-Pages, ohne es zu merken" — die Permission-Pruefung bleibt server-seitig in `hasRole('ADMIN')`. CSS-Tokens in styles.css: `body.admin-mode header { border-bottom-color: var(--color-accent-dark) }` + ein `.admin-mode-chip` aus `rgba(196,92,0,0.15)` Background, `var(--color-accent-dark)` Border, `var(--color-accent-light)` Text in Ethnocentric uppercase.

- **IRIDIUM-Logo auf der Keycloak-Login-Seite.** [`login.ftl`](keycloak-theme/krt-theme/login/login.ftl) zeigt jetzt das `iridium.svg` (kopiert nach [`keycloak-theme/krt-theme/login/resources/img/`](keycloak-theme/krt-theme/login/resources/img)) oberhalb des Titels — Wiedererkennung beim SSO-Redirect, statt nur Text. Neue `.login-logo`-CSS-Regel zentriert das Logo auf 96 px Breite.

- **Frontend-Klassen-Aliase `.btn` / `.btn-secondary` / `.btn-danger` im Keycloak-Login-Theme.** [`krt-login-v3.css`](keycloak-theme/krt-theme/login/resources/css/krt-login-v3.css) ergaenzt die `.krt-button*`-Cascade um die im in-app Frontend ueblichen `.btn*`-Klassen, damit Snippets aus Frontend-Templates ohne Klassen-Rename in Keycloak-FTLs uebernommen werden koennen. Header-Kommentar dokumentiert die "Tokens sind in styles.css, hier nur gespiegelt"-Maintenance-Regel.

- **Zebra-Streifen fuer alle Daten-Tabellen.** Neue Regel `tbody tr:nth-child(even) { background-color: rgba(255,255,255,0.02) }` in [`styles.css`](frontend/src/main/resources/static/css/styles.css). 2 % alpha — sichtbar genug, um die Eye-Line auf dichten Zeilen zu brechen, schwach genug, um nicht mit Hover oder den `.unit-color-*`-Department-Tints auf Mission-Detail zu konkurrieren.

- **Per-Cache-TTLs in [`CacheConfig`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/config/CacheConfig.java).** Statt eines globalen `expireAfterWrite=2m` bekommt jeder Cache jetzt seine eigene Spec via `CaffeineCacheManager.registerCustomCache`: Master-Data-Caches (Cities, Materials, ShipTypes, Locations, FrequencyTypes, JobTypes, Manufacturers, RefiningMethods, StarSystems, MissionLeadTypes, MaterialCategories) auf 30 min — weil sich die Daten quasi nicht aendern und expliziter `@CacheEvict` auf jedem Write die Konsistenz sicherstellt; Squadrons auf 10 min; Roles bleiben bei 2 min, damit Permission-Aenderungen in Keycloak schnell durchschlagen. Beans-Typ bleibt `CaffeineCacheManager` (kein Test-Bruch).

- **CSS-Minification im Build.** Neue Gradle-Task [`minifyStaticCss`](frontend/build.gradle.kts) ueberschreibt nach `processResources` die CSS-Files unter `build/resources/main/static/css/` mit Comment-/Whitespace-gestrippten Versionen — die Source-Files unter `src/main/resources/static/css/` bleiben lesbar. Wired in `classes`, laeuft also auch fuer `bootRun` und das Spring-Boot-Jar. styles.css schrumpft 56.4 → 36.4 KB (−35 %), gesamtes CSS-Payload 82 → 55 KB (−33 %, −27 KB Erstkontakt). Konservativer Regex-Minifier ohne neue Build-Dep — die selector-semantischen Whitespace-Faelle (`a :hover` vs `a:hover`) werden bewusst nicht angefasst.

- **JaCoCo-Report nur in CI.** Root [`build.gradle.kts`](build.gradle.kts): `onlyIf { System.getenv("CI") != null }` auf `jacocoTestReport`. Lokale `./gradlew test`-Laeufe sparen den XML/CSV/HTML-Report-Step (CI-Tools wie Codecov / SonarQube konsumieren ihn ohnehin nur dort). Opt-in lokal weiter via `CI=true ./gradlew test`.

- **Mission-Presence-Heartbeat von 10 s auf 60 s erhoeht.** [`mission-presence.js`](frontend/src/main/resources/static/js/mission-presence.js) sendet jetzt alle 60 s einen Heartbeat (statt 10 s), [`MissionPresenceService.ENTRY_TTL`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/service/MissionPresenceService.java) wachst entsprechend von 30 s auf 120 s (haelt das Verhaeltnis "zwei verpasste Heartbeats = reap" stabil). 6× weniger WebSocket-Frame-Traffic pro aktivem Editor; Trade-off: ein Peer sieht "Editor hat aufgehoert" bis zu ~120 s nach Tab-Wechsel (vorher ~30 s). Script wird ohnehin nur in [`mission-detail.html`](frontend/src/main/resources/templates/mission-detail.html) geladen. Beide Werte muessen zusammen geaendert werden, sonst flackert der Indikator oder reaped Editoren zu frueh — Kommentar in beiden Files dokumentiert die Kopplung.

- **Composite-Index `mission(owning_squadron_id, is_internal, status)` fuer den Mission-Search-Hotpath.** Neue Migration [`V93__add_mission_search_composite_index.sql`](backend/src/main/resources/db/migration/V93__add_mission_search_composite_index.sql) erweitert das bestehende 2-Spalten-Composite aus V91 um `status`, sodass `MissionRepository.searchMissions` (der dominante Mission-List-Render auf der Index- und Operation-Detail-Page) seine WHERE-Klausel (`owning_squadron_id OR is_internal = false` plus `status IN (...)`) komplett aus einem Index-Scan bedienen kann statt mit einem Heap-Filter auf `status` nachzuziehen. V91s 2-Spalten-Index bleibt zunaechst stehen — Cleanup in einem Folge-PR, sobald produktives `EXPLAIN ANALYZE` bestaetigt, dass er ungenutzt ist.

- **`UexClient` mit ETag-basiertem Conditional-GET.** [`UexClient`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/integration/UexClient.java) speichert pro Endpoint-Pfad das zuletzt gesehene `ETag` aus erfolgreichen 2xx-Antworten und spielt es bei der naechsten Anfrage als `If-None-Match` zurueck; UEX antwortet bei unveraendertem Feed mit `304 Not Modified`, der Client gibt dann eine leere Liste zurueck — exakt dieselbe Semantik wie der bestehende Fehler-Fallback, sodass die nachgelagerten Sync-Services ohne Aenderung den Tick als "skip this run" interpretieren. Spart pro stuendlichem Scheduler-Tick mehrere MB Bandbreite, wenn die UEX-Catalogs nicht touched wurden (Commodities-Prices-Endpoint allein >1 MB pro Aufruf). Die 17 oeffentlichen Methoden delegieren jetzt an einen generischen `fetchList(endpoint, typeRef, label)`-Helper; das ist die gleiche Refaktorierung, die das M-5-Ziel umsetzbar gemacht hat. Neue [`UexClientTest`](backend/src/test/java/de/greluc/krt/profit/basetool/backend/integration/UexClientTest.java)-Tests pinnen das If-None-Match-Replay, 304-Short-Circuit, ETag-Update bei neuer Antwort, Per-Endpoint-Isolation und das Verhalten bei serverseitigen Fehlern.

- **Explizite `width`/`height` auf jedem `<img>` in den Templates — kein `loading="lazy"`.** 49 Templates angepasst (Logo `iridium.svg` bekommt `width="50"` zusaetzlich zum existierenden `height="50"`; die Flag-Icons in [`fragments/sidebar.html`](frontend/src/main/resources/templates/fragments/sidebar.html) bekommen `width="24" height="16"` passend zur `.flag-icon`-CSS-Regel). Reduziert Cumulative Layout Shift auf den ersten Paint, weil der Browser jetzt die Bildbox vor dem Asset-Download reservieren kann. `loading="lazy"` wurde absichtlich NICHT ergaenzt: alle drei Image-Quellen sind above-the-fold (Header-Logo + Sidebar-Flaggen) und Lazy-Loading wuerde dort LCP verschlechtern statt verbessern — die M-6-Empfehlung des Audits ging davon aus, dass es scroll-sichtbare Bilder gibt, was nicht der Fall ist.

- **`SquadronScopeService` auf class-level `@Transactional(readOnly = true)` umgestellt.** Letzter Service mit JPA-Repository-Injection ohne explizite readOnly-Markierung — die ohnehin nur lesenden 7 Repository-Calls (User-, Squadron-, Mission-, Inventory-, RefineryOrder-, Operation-, ShipRepository) profitieren jetzt von Hibernate-FlushMode-Skip und expliziter Connection-Read-Routing-Markierung. M-4 aus dem Performance-Audit ist damit komplett — alle uebrigen Services hatten den readOnly-Default schon, `AuthHelperService` und `KeycloakService` haben keine JPA-Beruehrung und brauchen kein `@Transactional`.

- **Unbenutztes `MissionRepository.findAll()`-Override entfernt + ArchUnit-Regel dagegen.** [`MissionRepository`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/repository/MissionRepository.java): der `@Override List<Mission> findAll()` mit `@EntityGraph` hatte 0 Aufrufer in Production-Code — kombiniert mit dem EntityGraph waere er bei wachsender Mission-Tabelle ein OOM-Risiko gewesen (unbounded Cartesian-Join ueber `participants` + `assignedUnits`). Neue Regel in [`ArchitectureTest.repositoriesMustNotDeclareNoArgFindAll`](backend/src/test/java/de/greluc/krt/profit/basetool/backend/ArchitectureTest.java) bricht den Build, wenn jemand in `..backend.repository..` einen no-arg `findAll()`-Override wieder einfuehrt. `findAll(Pageable)`-Override bleibt — das ist die richtige Variante.

- **Gradle-Build parallelisiert + groesserer Daemon-Heap.** [`gradle.properties`](gradle.properties): `org.gradle.parallel=true`, `org.gradle.daemon=true` und `org.gradle.jvmargs=-Xmx2g -XX:+HeapDumpOnOutOfMemoryError` ergaenzt. Cross-Modul-Parallelitaet schaltet backend- und frontend-Tasks auf gleichzeitige Ausfuehrung; die 2-GB-Heap-Erhoehung kappt sporadische GC-Slowdowns bei Spring-Boot-Classpath-Scan + MapStruct/Lombok-Annotation-Processing. `configuration-cache` bleibt absichtlich aus, weil das refreshVersions-Plugin damit nicht kompatibel ist. Per-Modul-`maxParallelForks` getestet aber zurueckgezogen — destabilisierte den timing-sensitiven `WebClientResilienceTest.timeLimiter_ShouldTimeoutSlowResponses` (CPU-Contention frisst das 2-s-Time-Limit-Budget); Begruendung im [`build.gradle.kts`](build.gradle.kts)-Kommentar dokumentiert.

- **Tomcat-Connector-Limits in beiden Modulen explizit gesetzt.** [`backend/application.yml`](backend/src/main/resources/application.yml) und [`frontend/application.yml`](frontend/src/main/resources/application.yml): `server.tomcat.threads.max=200`, `min-spare=10`, `accept-count=200`, `max-connections=10000`. Mit `spring.threads.virtual.enabled=true` ist `threads.max` zwar mostly kosmetisch (Worker laufen auf Virtual Threads), aber die NIO-Accept-Queue + Connection-Cap greifen VOR dem Worker-Layer und die Tomcat-Defaults (100 / 8192) haben unter Burst-Last Requests gedroppt, die der Virtual-Thread-Pool absorbiert haette.

- **Redis-Persistenz deaktiviert.** [`docker-compose.yml`](docker-compose.yml): `--save 1 1 --appendonly yes` → `--save "" --appendonly no` fuer den Spring-Session-Redis. AOF-fsync nach jedem Session-Write (~200 fsync/s unter Last) war pure I/O-Last ohne Asset — Spring Session haelt nur OidcUser/OAuth2AuthorizedClient/Flash-Attribute; ein Redis-Restart faellt durch den Keycloak-`prompt=none`-Pfad still ab, der User merkt nichts.

- **HikariCP-Pool in Prod von 10 auf 15 Connections hochgezogen, `connection-timeout` von 30 s auf 10 s.** [`application-prod.yml`](backend/src/main/resources/application-prod.yml): Mit Resilience4j-Bulkhead = 50 und Virtual Threads ist 10 unter ~10 concurrent Nutzern Connection-Pool-Starvation; 15 deckt den ParallelPageLoader-Fan-out (4 parallele Calls pro Mission-Detail-/Hangar-Render) ohne idle-Connection-Inflation. Timeout sinkt auf 10 s, damit ein runaway Query nicht mehr 30 s blockiert, bevor ein neuer Request scheitert.

- **Dediziertes Executor-Pool fuer den UEX-Sync-Scheduler.** Neue [`AsyncConfig`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/config/AsyncConfig.java) registriert einen benamten `uexExecutor`-Bean (`ThreadPoolTaskExecutor`, core=2, max=4, queue=100, `AbortPolicy`, ThreadName-Prefix `uex-async-`); [`UexScheduler.scheduleCommodityPriceUpdate`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/UexScheduler.java) ist von einem unqualifizierten `@Async` auf `@Async(AsyncConfig.UEX_EXECUTOR)` umgestellt. Vorher lief der Sync auf Spring's `SimpleAsyncTaskExecutor`, der pro `@Async`-Aufruf einen neuen Thread spawnt und keinen Cap kennt — bei einer haengenden UEX-Antwort ein Thread-Leak. `AbortPolicy` macht eine echte Saettigung als `RejectedExecutionException` sichtbar statt sie still zu schlucken. Test in [`AsyncConfigTest`](backend/src/test/java/de/greluc/krt/profit/basetool/backend/config/AsyncConfigTest.java) pinnt Sizing und Rejection-Handler, damit ein "Vereinfachung" zurueck auf Defaults den Build bricht.

- **Caching fuer drei Stammdaten-Services nachgezogen: `CityService`, `FrequencyTypeService`, `MaterialCategoryService`.** [`CacheConfig`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/config/CacheConfig.java) bekommt drei neue Cache-Namen (`cities`, `frequencyTypes`, `materialCategories`) und whitelisted sie im Cache-Manager; die Read-Methoden der drei Services sind jetzt `@Cacheable`, jede Mutation (admin-Override / CRUD / Reorder) evictet den jeweiligen Cache `allEntries=true` — dasselbe Pattern wie `LocationService`. Caffeine-TTL bleibt 2 Minuten, sodass der stuendliche UEX-Sync-Write an `city` ohne expliziten Evict konsistent wird.

- **Logo `iridium.svg` von 547 KB auf 14 KB geschrumpft (-97 %).** Das eingebettete 750x750-PNG hat sich auf jeder Seite mitgeladen, obwohl alle Templates es per `height=50` einbinden; jetzt 256x256, mit 256-Farben-Palette quantisiert und wieder in einen minimalen SVG-Wrapper gepackt. Datei-Pfad und alle Template-Referenzen bleiben unveraendert.

- **Schriften auf WOFF2-only umgestellt, TTF/OTF-Fallbacks entfernt.** [`styles.css`](frontend/src/main/resources/static/css/styles.css) listet in `@font-face` nur noch `format('woff2')` fuer Lato Light/Regular/Bold und Ethnocentric; die alten TTF/OTF-Files unter [`static/fonts/`](frontend/src/main/resources/static/fonts) sind geloescht. Die Fallbacks zielten auf Browser vor Chrome 36 / Firefox 39 / Safari 12 / Edge 14 — durchgaengig <2018 und unterhalb der Supportbasis. Spart ~225 KB Font-Payload bei Erstkontakt.

- **Design-Tokens: Off-Brand-Farben aus Templates und CSS verbannt.** Material-Design-/Bootstrap-Finance-Farben (`#4CAF50`, `#F44336`, `#28a745`, `#e74c3c`, `#ffc107`) in [`mission-detail.html`](frontend/src/main/resources/templates/mission-detail.html), [`operation-detail.html`](frontend/src/main/resources/templates/operation-detail.html), [`admin/materials.html`](frontend/src/main/resources/templates/admin/materials.html) und [`material-collection.html`](frontend/src/main/resources/templates/material-collection.html) auf `var(--color-success)` / `var(--color-danger)` / `var(--color-warning)` umgestellt. Off-Brand-Grautoene (`#1f1f1f`, `#1a1a1a`, `#2a2a2a`, `#d6d6d6`, `#9a9a9a`) in [`personal-inventory.css`](frontend/src/main/resources/static/css/personal-inventory.css) und [`promotion-admin.css`](frontend/src/main/resources/static/css/promotion-admin.css) auf die echten `:root`-Werte aus dem Styleguide angeglichen. Confirm-Dialog in [`fragments/toast.html`](frontend/src/main/resources/templates/fragments/toast.html) nutzt jetzt durchgaengig `var(--color-*)`-Tokens statt Hex-Literals.

- **Touch-Targets: `.btn` ist auf jedem Device 44 px hoch, nicht nur Mobile.** [`styles.css`](frontend/src/main/resources/static/css/styles.css): `.btn` erhaelt `min-height: 44px` und `display: inline-flex` als Default — die Mobile-Override-Regel ist redundant geworden. Touch-Laptops und Hybrid-Geraete melden sich als Desktop, brauchen aber dieselbe Treffergroesse. `.pa-btn-tiny` in [`promotion-admin.css`](frontend/src/main/resources/static/css/promotion-admin.css) analog von 38 auf 44 px hochgezogen.

- **Form-Group-Spacing zentralisiert.** Neue `.form-group { margin-bottom: 1.25rem }`-Regel in [`styles.css`](frontend/src/main/resources/static/css/styles.css). Die 17 Templates (`admin-settings`, `admin/announcement`, `admin/locations`, `admin/materials`, `admin/mission-data`, `admin/uex`, `hangar`, `inventory-input`, `member-edit`, `mission-detail`, `operation-detail`, `orders-create`, `refinery-orders-create`, `refinery-orders-details`, `ship-data`, und der Override in `promotion-admin.css`), die jeweils ihr eigenes `1rem`/`1.5rem` redeklarierten, fallen auf den zentralen Wert zurueck — einheitlicher Rhythmus, eine Stelle zum Aendern. Pages mit semantisch anderem Abstand (z. B. `.materials-overview-main .form-group { margin-bottom: 0 }` in der Filter-Bar) behalten ihre spezifischeren Overrides.

- **`.search-form`-Layout als Klassen statt Inline-Styles.** [`styles.css`](frontend/src/main/resources/static/css/styles.css) bekommt eine zentrale `.search-form { display: flex; align-items: flex-end; gap: 0.5rem; margin-bottom: 1rem; flex-wrap: wrap }`-Regel + neue Helper `.search-form-flex-1`, `.search-form-checkbox-col`, `.search-form-checkbox-label`, `.search-form-checkbox-spacer`, `.search-form-actions`. Sieben `style="display:flex; ..."`-Attribute in [`missions.html`](frontend/src/main/resources/templates/missions.html) entfallen.

- **Materials-Overview-Inline-Style-Block (~200 Zeilen) in dedizierte [`materials-overview.css`](frontend/src/main/resources/static/css/materials-overview.css) ausgelagert.** [`materials-overview.html`](frontend/src/main/resources/templates/materials-overview.html) haengt das CSS jetzt per `<link>` ein. Die 24 Planet-Tint-Header-Farben und 24 Column-Stripe-Box-Shadows bleiben weiterhin als Hex-Literals (sie kodieren Star-Citizen-Planet-Hues, gehoeren nicht in `:root`).

- **Modal-Pattern-Konsolidierung und Doku.** Drei parallele Implementierungen (`.modal`/`.modal-content`, `.modal-overlay`/`.modal-box`, `.krt-confirm-overlay`/`.krt-confirm-dialog`) sind jetzt visuell identisch: `.modal-overlay` in [`promotion-admin.css`](frontend/src/main/resources/static/css/promotion-admin.css) zieht z-index von 1000 auf 3000, addet `backdrop-filter: blur(4px)`, und angleicht Background-Alpha 0.85 -> 0.8 + Box-Shadow-Alpha 0.3 -> 0.2 an `.modal`/`.modal-content`. Toggle-Mechanismen (`style.display` vs. `.active`-Klasse vs. JS-generiert) bleiben getrennt, sind aber im Header-Kommentar von `.modal` in [`styles.css`](frontend/src/main/resources/static/css/styles.css) dokumentiert. `.modal-content` bekommt zusaetzlich `max-height: 90vh; overflow-y: auto` fuer lange Formulare.

- **Modale Inventur-Templates nutzen `display: flex` statt `display: block`.** [`inventory-my.html`](frontend/src/main/resources/templates/inventory-my.html), [`inventory-admin.html`](frontend/src/main/resources/templates/inventory-admin.html), [`personal-inventory.html`](frontend/src/main/resources/templates/personal-inventory.html), [`admin/personal-inventory.html`](frontend/src/main/resources/templates/admin/personal-inventory.html) sowie das `bulkCheckoutModal`-JS in `inventory-my.html` setzen den Open-State jetzt auf `display:flex` — passt zur flex-zentrierten zentralen `.modal`-Definition (vorher uebersprang das Centering). Inline-Modal-Styles in [`operations-index.html`](frontend/src/main/resources/templates/operations-index.html) und [`operation-detail.html`](frontend/src/main/resources/templates/operation-detail.html) (40+ Zeichen pro Modal mit z-index 1000-Override) auf das absolute Minimum eingedampft.

- **Keycloak-Login- und -Account-Theme nutzen WOFF2 mit TTF/OTF-Fallback.** [`krt-login-v3.css`](keycloak-theme/krt-theme/login/resources/css/krt-login-v3.css) und [`krt-account-v3.css`](keycloak-theme/krt-theme/account/resources/css/krt-account-v3.css) listen `format('woff2')` vor `format('truetype'/'opentype')` und setzen `font-display: swap`. WOFF2-Files wurden aus `frontend/static/fonts/` in beide Keycloak-Theme-Font-Ordner kopiert. Brand-Font-Download faellt um ~60 %, FOIT auf dem Login verschwindet.

### Fixed

- **`.modal`-Klasse in [`inventory-my.html`](frontend/src/main/resources/templates/inventory-my.html) ueberschrieb die zentrale Definition mit z-index 1000.** Inline-Block (35 Zeilen mit `.modal`, `.modal-content`, `.close`-Redeklaration) entfernt — die zentrale Definition aus [`styles.css`](frontend/src/main/resources/static/css/styles.css) (z-index 3000) uebernimmt. Vorher konnten der Squadron-Chip (z-index 1200) und das Sidebar-Overlay (z-index 1999) ueber einem geoeffneten Inventory-Modal liegen. Audit Phase 3 / K1.

## [v0.2.12](https://github.com/krt-profit/basetool/releases/tag/v0.2.12) - 2026-05-20

### Changed

- **Bezahlt-Checkbox in der Operations-Auszahlungstabelle: zuruecksetzen nur noch durch Offiziere und Admins.** Mission Manager koennen einen Teilnehmer weiterhin als bezahlt markieren, das Haekchen wieder zu entfernen ist jetzt OFFICER/ADMIN-only — verhindert, dass ein versehentlicher Klick eine bereits bestaetigte Auszahlung stillschweigend zuruecksetzt. Backend-Gate per asymmetrischer SpEL auf `PUT /api/v1/operations/{id}/payouts/paid-out` (403 fuer Mission Manager bei `paidOut=false`), Frontend-Spiegel im Page-Controller-Proxy, Template deaktiviert die Checkbox fuer Mission Manager nach erfolgreichem Markieren (Tooltip `operation.payout.paid.unset.locked`).

### Fixed

- **Auszahlungsart-Dropdown in der Einsatz-Detailansicht hat die Auswahl nicht persistiert (dritte Iteration der `krtEvents`-Race-Klasse, nach [#169](https://github.com/krt-profit/basetool/pull/169) und [#172](https://github.com/krt-profit/basetool/pull/172)).** Die beiden Vorgaengerfixes haben jeweils nur die konkret gemeldeten Templates entschaerft (Startseite bzw. Mission-Detail-Jetzt-Buttons); das eigentliche Bug-Pattern blieb in 24 Templates aktiv und schlug jetzt am Payout-Dropdown derselben Seite zu. [`fragments/head.html`](frontend/src/main/resources/templates/fragments/head.html) installiert jetzt einen synchronen Bootstrap-Stub fuer `window.krtEvents`, der `.on(...)`-Aufrufe in einer Queue puffert; [`event-delegation.js`](frontend/src/main/resources/static/js/event-delegation.js) leert die Queue beim Laden und ersetzt den Stub durch die echte Implementierung. `.on(...)`-Aufrufe funktionieren damit unabhaengig von Defer/Async-Reihenfolge, Cache-Stand oder Netzwerk-Timing — der Race ist strukturell weg. Zusaetzlicher Watchdog im Event-Delegation-Skript loggt einen Console-Error, falls der Stub 5 s nach Pageload immer noch aktiv ist (= echtes Skript hat den Stub nie ersetzt, deploy ist kaputt). Audit der drei anderen deferred Utility-Skripte (`escape-html`, `safe-url`, `datetime-splitter`) bestaetigt: deren Globals werden ausschliesslich aus Handler-Callbacks heraus konsumiert, nicht zur Parse-Zeit — keine zusaetzliche Stub-Behandlung noetig.

- **Jetzt-Buttons fuer `actualStartTime` / `actualEndTime` auf der Einsatz-Detail-Seite reagierten nicht auf Klicks.** Die page-lokale `window.krtEvents.on(...)`-Registrierung in [`mission-detail.html`](frontend/src/main/resources/templates/mission-detail.html) lief synchron mit dem `if (window.krtEvents && ...)`-Guard am Body-Ende — wenn `window.krtEvents` aus irgendeinem Grund am Parse-Zeitpunkt fehlt (z. B. nach einer Head-Skript-Load-Order-Regression analog zu 56751e2), wurden alle sieben `data-trigger`-Bindings inklusive `mission-set-now` stillschweigend verworfen. Registrierung steht jetzt in einer Funktion, die entweder sofort (Document bereits geladen) oder bei `DOMContentLoaded` (alle Head-Skripte garantiert ausgefuehrt) laeuft, und loggt `console.error`, falls `window.krtEvents` selbst nach DOMContentLoaded fehlt — kein stilles Schlucken mehr.

## [v0.2.11](https://github.com/krt-profit/basetool/releases/tag/v0.2.11) - 2026-05-20

### Changed

- **Operation-Detail-Seite: Warnhinweis ueber der Auszahlungstabelle, wenn mindestens ein Einsatz der Operation noch keine `actualStartTime` oder `actualEndTime` hat.** Die Operation-Anteilsberechnung in [`OperationService.computeParticipationBreakdown`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/OperationService.java) ueberspringt unfertige Einsaetze komplett, weshalb die Prozentwerte erst nach Abschluss aller Einsaetze endgueltig sind — der Banner macht das fuer Mission Manager sichtbar, statt dass Beteiligte ueber unerklaerliche Wertaenderungen stolpern. Implementierung: neue Repository-Methode [`MissionRepository.existsByOperationIdWithUnfinishedActualTime`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/repository/MissionRepository.java) + Service-Methode [`OperationService.hasUnfinishedMissions`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/OperationService.java), das Flag wird als zusaetzliches `payoutPreliminary`-Feld am `OperationDto` nur am Detail-Endpoint `GET /api/v1/operations/{id}` gefuellt (sonst `null`); Frontend-DTO mirrors. Template-Banner als `alert-warning`-Klasse oberhalb der `payout-table`, i18n-Schluessel `operation.payout.preliminary.title` + `…message` in DE/EN.

- **Geldbetraege in der Operations- und Einsatzansicht werden ohne Nachkommastellen, kaufmaennisch (HALF_UP) gerundet angezeigt.** Operation- und Mission-Detail-Templates routen Auslagen, Ueberweisungsgebuehren, Finanz-Gesamtsummen, Mission-Subtotale, Finanzeintraege und Raffinerie-Ausgaben durch den neuen [`MoneyFormat`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/view/MoneyFormat.java)-Spring-Bean, der `BigDecimal.setScale(0, HALF_UP)` anwendet bevor `#numbers.formatInteger` formatiert — Thymeleafs Default-Rundungsmodus ist HALF_EVEN, der bei `.5`-Werten je nach Parity inkonsistent rundet. Reine Anzeige-Aenderung, gespeicherte Betraege bleiben unveraendert.

### Fixed

- **Information-Sektion auf der Startseite reagierte nicht auf Klicks (regression aus der CSP-Migration #ID 8327322).** [`fragments/head.html`](frontend/src/main/resources/templates/fragments/head.html) lud `event-delegation.js` und `common-handlers.js` mit `defer`, dadurch war `window.krtEvents` noch nicht definiert, wenn die inline `<script>`-Bloecke am Body-Ende der Templates synchron ihr `window.krtEvents.on(...)` aufriefen — die `if (window.krtEvents && ...)`-Guard verschluckte die Registrierung stillschweigend und der Header-Klick auf `#info-header` (und alle anderen page-lokalen `data-trigger`-Bindings in 23 Templates) blieb ohne Effekt. Beide Skripte laden jetzt ohne `defer`; sie sind kleine IIFEs, die nur Globals + `document.addEventListener`-Delegationen registrieren und damit sicher vor dem DOM-Tree-Build evaluierbar sind. `escape-html.js`, `safe-url.js` und `datetime-splitter.js` behalten `defer`, weil ihre Globals nur aus Handler-Callbacks (also nach DOMContentLoaded) konsumiert werden.

- **Anonyme Auftragsanlage stempelt die erstellende Staffel jetzt mit der ausgewaehlten Auftraggeber-Staffel.** [`JobOrderService.resolveCreatingSquadronForCreate`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/JobOrderService.java) hat bei unauthentifizierten Callern bisher hart auf die IRIDIUM-Seed-Row gefallen — der Audit-Trail zeigte damit IRIDIUM als Ersteller, auch wenn der Antrag im Formular fuer eine andere Staffel gestellt war. Nutzt jetzt `requestingSquadronId` aus dem DTO als Stempel; IRIDIUM bleibt der Fallback, wenn auch das Feld leer ist.

- **Edit-Auftrag-Dialog zeigte `??orders.create.selectSquadron_de??` im Staffel-Dropdown.** Fehlender i18n-Key in den drei [`messages*.properties`](frontend/src/main/resources/messages.properties)-Dateien nachgereicht (`-- Staffel waehlen --` / `-- Select squadron --`).

## [v0.2.10](https://github.com/krt-profit/basetool/releases/tag/v0.2.10) - 2026-05-20

### Fixed

- **Frontend-Container im prod-Stack — nach dem Truststore-Fix (#164) brachen die Backend-Calls weiterhin, jetzt mit `SSLHandshakeException: (certificate_unknown) No subject alternative DNS name matching backend found`.** Symptom (Auszug aus `docker compose logs frontend`, direkt nach dem Deploy des Truststore-Fixes): die TLS-Validierung kam jetzt durch (= PKIX path build OK), aber die JSSE-Endpoint-Identification verlangte, dass der Server-Cert eine Subject-Alternative-Name-Eintrag fuer den Docker-Network-Alias `backend` traegt. Der vom Operator generierte prod-`keystore.p12` enthielt nur den externen DNS-Namen (`*.iri-base.org` o.ae.) in SAN, nicht aber `dns:backend` — die [README-§4.4-Recipe](README.md) gibt `-ext "san=dns:localhost,ip:127.0.0.1,dns:backend,dns:frontend"` als Pflicht vor, der Operator hatte aber eine eigene Variante ohne diese Aliase verwendet. **Fix:** in [`WebClientConfig.connector()`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/config/WebClientConfig.java) ist die Hostname-Verification fuer die zwei "pinned trust"-Pfade jetzt explizit aus (`SSLParameters.setEndpointIdentificationAlgorithm("")` via Nettys `SslHandlerConfigurator`). Begruendung: der Trust-Set ist auf den Bundle-Cert (prod) bzw. accept-any (dev/test) festgezurrt; die Hostname-Verification verteidigt in dieser Konstellation nur noch gegen "der gepinnte Cert wird unter einem anderen Hostname praesentiert" — was ein Angreifer ohne den private key aus `keystore.p12` schlicht nicht kann, und mit dem private key ohnehin nicht mehr aufzuhalten waere. Der dritte Pfad (kein Bundle, Default-JVM-Trust-Store) behaelt die Hostname-Verification: dort validiert man gegen die oeffentliche CA-Pool und der Hostname-Check ist die einzige Bindung Cert↔Host, ohne ihn waere jeder von einer beliebigen CA in `cacerts` ausgestellte Cert valide fuer `backend`. **Was M-13 weiterhin schuetzt:** die Audit-Garantie "kein blindes Trust-Everything in prod" steht — Cert-Pinning auf den `keystore.p12`-Inhalt ist strikt staerker als der Default-JVM-Trust-Store, und der `InsecureTrustManagerFactory` bleibt dev/test-only. **Verifikation:** `./gradlew :frontend:spotlessApply :frontend:checkstyleMain :frontend:spotbugsMain :frontend:test` BUILD SUCCESSFUL.

## [v0.2.9](https://github.com/krt-profit/basetool/releases/tag/v0.2.9) - 2026-05-20

### Fixed

- **Frontend-Container im prod-Stack — jeder Backend-Call schlug mit `PKIX path building failed: unable to find valid certification path to requested target` fehl.** Symptom (Auszug aus `docker compose logs frontend`): `WebClientRequestException: (certificate_unknown) PKIX path building failed: ... at io.netty.handler.ssl.SslHandler.decode(...)` auf jedem `GET https://backend:11261/...`-Request, Resilience4j drehte Retry + CircuitBreaker durch, die Seite renderte ohne Daten. **Ursache:** der 2026-05-20-Audit-Patch (M-13, [#150](https://github.com/krt-profit/basetool/pull/150)) hat den `InsecureTrustManagerFactory` in [`WebClientConfig`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/config/WebClientConfig.java) korrekt auf `dev`/`test`-Profile eingeschraenkt — mit der impliziten Annahme, dass `prod` ein publicly-trusted Cert vor dem Backend hat. Der mitgelieferte [`docker-compose.yml`](docker-compose.yml) faehrt das Backend aber weiterhin mit dem self-signed `keystore.p12` (das Frontend mountet sogar dieselbe Datei fuer seinen eigenen Tomcat-HTTPS-Listener), und es gab keinen Truststore-Pfad fuer den ausgehenden WebClient — der Default-JVM-Trust-Store kennt das self-signed Cert nicht, also failed TLS. **Fix:** neue Spring-Boot-SSL-Bundle-Definition `backend-trust` in [`application-prod.yml`](frontend/src/main/resources/application-prod.yml) verweist den Truststore auf denselben `${SERVER_SSL_KEY_STORE}` + `${SERVER_SSL_KEY_STORE_PASSWORD}`, den Tomcat schon konsumiert (kein neues Compose-Secret, kein neues `.env`-Feld); `WebClientConfig.connector()` zieht das Bundle ueber den `SslBundles`-Bean, lifted den enthaltenen `KeyStore` in einen `TrustManagerFactory` und reicht ihn an Nettys `SslContextBuilder.trustManager(...)`. Drei klar getrennte Pfade in der Methode: `dev`/`test` → weiterhin `InsecureTrustManagerFactory`; sonstige Profile mit `backend-trust`-Bundle → Bundle-Truststore (prod-Default); sonstige Profile ohne Bundle → Default-JVM-Trust-Store (offen fuer Setups, die spaeter einen publicly-trusted Cert vor das Backend stellen). Der `NoSuchSslBundleException`-Fallback ist die einzige stille Ausnahme — alle anderen Fehler (Keystore korrupt, falsches Passwort) propagieren als `RuntimeException`, damit Misconfig laut scheitert statt zur unerwarteten Trust-Store-Auflosung zu drehen. **Was M-13 weiterhin schuetzt:** der `InsecureTrustManagerFactory` ist NUR noch in dev/test aktiv; in prod ist der Trust pinned auf den im Bundle hinterlegten Cert, ein MITM zwischen Frontend- und Backend-Container kann das nicht umdrehen ohne den private key aus `keystore.p12` zu haben. **Verifikation:** `./gradlew :frontend:spotlessApply :frontend:checkstyleMain :frontend:spotbugsMain :frontend:test` BUILD SUCCESSFUL.

## [v0.2.7](https://github.com/krt-profit/basetool/releases/tag/v0.2.7) - 2026-05-20

### Changed

- **Release-Images werden zusaetzlich fuer `linux/arm64` publiziert — nativ, nicht emuliert.** [`release-images.yml`](.github/workflows/release-images.yml) ist auf ein verteiltes Multi-Arch-Build-Schema umgestellt: vier parallele `build`-Jobs (`module x platform`) laufen jeder auf seinem eigenen nativen Runner — `linux/amd64` auf `ubuntu-latest`, `linux/arm64` auf GitHub's free-fuer-public-Repos `ubuntu-24.04-arm`-Fleet — und pushen ihr Image **by digest** (kein Tag) nach GHCR; ein nachgelagerter `merge`-Job pro Modul fasst die per-Arch-Digests via `docker buildx imagetools create` zu einem Multi-Arch-OCI-Index zusammen, applied alle metadata-action-Tags am Index und cosign-signiert den Manifest-Digest. **Warum:** die Vorversion registrierte QEMU und liess den `linux/arm64`-Stage emuliert auf dem amd64-Runner laufen — der Gradle-Compile, der Spring-Boot-Repackage und (seit dem AppCDS-Patch) zwei volle JVM-Trainingslaeufe wurden Instruktion-fuer-Instruktion uebersetzt und brauchten ~3-6x laenger als amd64; mit nativen Runnern finishen beide Architekturen in etwa derselben Wallclock-Zeit. **Trivy** scannt jetzt pro (Modul, Plattform) auf dem jeweiligen Runner und uploaded vier SARIF-Files mit getrennten Categories (vorher zwei), sodass arm64-Findings sichtbar werden statt mit amd64-Findings zusammenzulaufen. **Provenance + SBOM** ueberleben den Merge: jeder per-Arch-Build attached `provenance: mode=max` + `sbom: true` an seinen eigenen Index, `imagetools create` zieht beide Attestation-Manifeste in den finalen Multi-Arch-Index hoch — kein separater `cosign attest`-Step noetig. **Cache-Scope** ist auf `${module}-${platform_pair}` erweitert (vorher nur `${module}`), damit amd64- und arm64-Layers sich nicht gegenseitig die GHA-Cache-Eintraege ueberschreiben. `setup-qemu-action` und der `timeout-minutes: 60`-Cap entfallen; die neuen Per-Job-Timeouts sind 30 min Build, 15 min Merge.

## [v0.2.6](https://github.com/krt-profit/basetool/releases/tag/v0.2.6) - 2026-05-20

### Changed

- **Spring Boot AppCDS — Docker-Images bauen jetzt zur Build-Zeit einen Class-Data-Sharing-Archive (`/app/application.jsa`).** Beide Dockerfiles haengen nach dem Layer-Extract einen Training-Run an (`-XX:ArchiveClassesAtExit`, `-Dspring.context.exit=onRefresh`, plus Disable-Flags fuer Flyway / Hikari-Probe / OAuth2-Issuer-Lookup / Redis, damit der Refresh ohne externe Services durchlaeuft); ein Conditional-Entrypoint laedt den Archive via `-XX:SharedArchiveFile`, faellt zurueck auf einen normalen Start wenn das Training scheitert. Erwartung ist ein 30-50 % schnellerer JVM-Start — wirkt sich auf das Healthcheck-Fenster und Rolling-Restart-Geschwindigkeit aus.

- **Docker-Compose-Stack-Haertung in einem Schwung — [`docker-compose.yml`](docker-compose.yml) bekommt vier Verbesserungen, alle drop-in.** (1) Neues `x-logging`-Anchor (`json-file`, `max-size=10m`, `max-file=5`) wird von jedem Service-Template referenziert — verhindert das langsame Volllaufen von `/var/lib/docker/containers/` durch unbegrenzte JSON-Logfiles (das Backend allein produziert ~150 MB/Tag). (2) `restart: unless-stopped` ist auf den Stateful-Services (`db-backend`, `db-keycloak`, `keycloak`, `redis`, `npm`) ergaenzt — die hatten vorher gar keine Restart-Policy, ein OOM-Kill brachte sie nicht zurueck. (3) Die vier Drittanbieter-Images (`postgres:18-alpine`, `redis:8-alpine`, `quay.io/keycloak/keycloak:26.6`, `jc21/nginx-proxy-manager:2.14.0`) sind per `@sha256:<digest>` gepinnt — ein silent upstream rebuild kann zwischen `docker compose pull` und Container-Restart das Image-Layout nicht mehr aendern; Refresh-Pfad via `docker buildx imagetools inspect <image>` ist als Kommentar dokumentiert. (4) Beide Postgres-Templates bekommen ein `command:`-Tuning passend zu ihren Container-RAM-Limits (`shared_buffers=192MB/128MB`, `effective_cache_size=512MB/384MB`, `work_mem=8MB/4MB`) — die Default-Settings zielen auf 128 MB-Geraete und liessen den Buffer-Pool kalt. Der Keycloak-Healthcheck-`/dev/tcp`-Konstrukt nutzt jetzt `printf` (POSIX-stabil) statt `echo -e` und ist als YAML-Block-Skalar gerendert, weniger Line-Noise; der `/health/ready`-Check selbst ist verhaltensaequivalent.

- **Dockerfile-Cleanups in [`backend/Dockerfile`](backend/Dockerfile) und [`frontend/Dockerfile`](frontend/Dockerfile).** `COPY --chmod=755 gradlew .` ersetzt das separate `RUN chmod +x gradlew` (eine Layer weniger). Das `|| true` hinter `:dependencies --no-daemon` ist weg — ein gescheiterter Dep-Download soll laut scheitern, nicht stumm den naechsten `:build`-Step alles erneut downloaden lassen. Der `find … | head -n 1`-Pipe fuer die JAR-Auswahl ist durch einen expliziten Glob mit Count-Check ersetzt (`set -- backend-*.jar` + `[ "$#" -eq 1 ]`) — zwei Matches oder null Matches schlagen jetzt laut fehl statt schweigend einen falschen JAR zu picken. `apk add curl ca-certificates` ist entfernt; der HEALTHCHECK nutzt BusyBox's eingebautes `wget --no-check-certificate` (schon im Alpine-Base, kein zusaetzlicher Install). HEALTHCHECK-Timing: `--start-period` von 60s auf 90s (fing bei langsamem Flyway-Migrationslauf eine Flap-Schleife), zusaetzlich `--start-interval=5s` (Docker 25+) fuer schnelles Re-Probing waehrend der Start-Window.

- **[`docker-compose.build.yml`](docker-compose.build.yml) ist dedupliziert und bekommt `develop.watch`.** Statt vier identischer `build:`-Bloecke fuer `backend`/`backend-dev`/`frontend`/`frontend-dev` referenziert die Datei `&backend-build` und `&frontend-build`-YAML-Anchors. Die `*-dev`-Services tragen zusaetzlich einen `develop.watch`-Block (Compose v2.22+, `action: rebuild` auf `<modul>/src/`, `<modul>/build.gradle.kts` und `versions.properties`) — `docker compose -f docker-compose.yml -f docker-compose.build.yml --profile dev watch` rebuildet bei Source-Aenderungen automatisch, ohne den manuellen `up -d --build`-Roundtrip.

- **Logging-Performance: Backend-Default-Level von `TRACE` auf `INFO`, Disk-Appender hinter `AsyncAppender` gestellt.** [`backend/.../logback-spring.xml`](backend/src/main/resources/logback-spring.xml) und [`frontend/.../logback-spring.xml`](frontend/src/main/resources/logback-spring.xml): das Backend-Package `de.greluc.krt.profit.basetool` lief in `prod` weiterhin auf `TRACE` (Frontend wurde per Audit-Finding H-11 schon korrigiert, das Backend nicht) — jeder Request schrieb dutzende TRACE-/DEBUG-Zeilen durch die synchrone PII-Masking-Layout-Kette auf die Disk und dominierte die Request-Latenz. `FILE` / `ERROR_FILE` / `JSON_FILE` werden nun von einem `AsyncAppender` (queueSize 512 bzw. 128, `discardingThreshold=0` damit der Audit-Trail komplett bleibt) gepuffert; `CONSOLE` bleibt synchron, damit die letzten Zeilen vor einem JVM-Crash deterministisch geflusht werden. Per-Paket-DEBUG/TRACE bleibt via `logging.level.de.greluc.krt.profit.basetool.<pkg>=DEBUG`-Env-Override fuer Triage verfuegbar.

- **DB-Performance: vier fehlende FK-Indizes nachgereicht ([`V92__add_missing_fk_indexes.sql`](backend/src/main/resources/db/migration/V92__add_missing_fk_indexes.sql)).** V34s grosse FK-Index-Sweep-Migration matchte das Schema zum Zeitpunkt ihres Releases; vier spaeter zugefuegte Spalten haben kein Index abbekommen: `inventory_item.job_order_id` (V10), `inventory_item.mission_id` (V25), `mission.operation_id` (V39) und `refinery_order.mission_id` (V1, einfach uebersehen). Alle vier sind Hot-Path-Listenfilter (Mission-Operation-Dropdown, JobOrder-Detail-Page, Mission-Detail-Inventory, Mission-Detail-Refinery) und liefen ohne Index in den Sequential-Scan. Migration ist mechanisch (`CREATE INDEX IF NOT EXISTS`), kein Schema- oder Code-Pfad-Wechsel. [`DatabaseIndexMigrationTest`](backend/src/test/java/de/greluc/krt/profit/basetool/backend/db/DatabaseIndexMigrationTest.java) erweitert um Canary-Checks auf zwei der neuen Indizes.

- **Frontend-Asset-Performance: `defer` auf Head-Scripts, `font-display: swap` + WOFF2 fuer Brand-Fonts, Preload-Hint fuer Lato-Regular.** [`fragments/head.html`](frontend/src/main/resources/templates/fragments/head.html): die vier ehemals render-blockierenden Skripte (`escape-html.js`, `safe-url.js`, `event-delegation.js`, `common-handlers.js`) erhalten `defer`, sodass HTML-Parsing nicht mehr auf JS-Download/Parse wartet. Execution-Order unter `defer` ist deterministisch (document-Order). Neuer `<link rel="preload" as="font" type="font/woff2" crossorigin>` fuer `Lato-Regular.woff2` startet die Schriften-Anfrage parallel zu styles.css. [`styles.css`](frontend/src/main/resources/static/css/styles.css): alle vier `@font-face` deklarieren `font-display: swap` (System-Fallback sofort sichtbar, Brand-Font swapt nach Arrival ein — kein FOIT mehr) und listen WOFF2 vor TTF/OTF (`format('woff2'), format('truetype'/'opentype')` als Fallback fuer Pre-2018-Browser). [`static/fonts/`](frontend/src/main/resources/static/fonts): neue `.woff2`-Varianten von Lato-Light/Regular/Bold und Ethnocentric (Konvertierung lokal mit `fontTools` + `brotli`); Originale `.ttf`/`.otf` bleiben als Fallback liegen. Gesamtgroesse Brand-Fonts faellt von ~248 KB auf ~96 KB (61% weniger).

- **Page-Controller laden unabhaengige Backend-Calls jetzt parallel.** Neuer [`ParallelPageLoader`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/service/ParallelPageLoader.java) (`@Component`, Virtual-Thread-Executor) fuehrt mehrere Supplier nebenlaeufig aus und propagiert dabei alle request-scoped ThreadLocals (SecurityContext, ServletRequestAttributes, `ActiveSquadronContext`, `CorrelationContext`, MDC) — ohne diese Propagation faellt OAuth2-Bearer-Relay und Squadron-Header weg. [`HangarPageController.viewHangar`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/controller/HangarPageController.java) zieht jetzt `my-ships` + ship-types + locations + manufacturers parallel statt seriell — bei kalten Catalog-Caches spart das ~3 × Backend-Roundtrip pro Page-Render. [`AdminMissionDataPageController.listData`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/controller/AdminMissionDataPageController.java) analog fuer job-types + squadrons + frequency-types. Per-Call-Fehlerbehandlung bleibt identisch zum vorherigen sequentiellen Verhalten (jeder Call degradiert eigenstaendig auf Empty-Liste, gemeinsame Error-Surface bleibt erhalten).

- **Frontend-Resilience4j + WebClient-Pool an die parallelisierten Page-Renders angepasst.** [`application.yml`](frontend/src/main/resources/application.yml): Retry-Budget von `maxAttempts: 3, waitDuration: 1s` auf `maxAttempts: 2, waitDuration: 500ms` — der vorherige Default haengte bis zu 6 s auf jede fehlgeschlagene Page-Render-Sequenz oben drauf. Bulkhead-Concurrency von `maxConcurrentCalls: 20` auf `50`, weil ein einzelner Mission-Detail- oder Hangar-Render jetzt vier Calls parallel feuert (`ParallelPageLoader`) und damit bei ~10 gleichzeitigen Usern an die alte 20-Slot-Grenze stiess. [`WebClientConfig.connector`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/config/WebClientConfig.java): Netty-Connection-Pool `maxConnections` von 50 auf 100, `pendingAcquireTimeout` von 5 s auf 10 s — kompatibel mit der erhoehten Bulkhead-Grenze und der Backend-Hardware der Prod-Instanz (Postgres-Hikari-Pool steht bei 30, das Frontend bleibt deutlich darunter). Keine API-Aenderung.

- **Drei Filter-Cleanups, alle aus Performance-Audit-Findings.** Erstens: `StaticCacheHeaderFilter` geloescht; Spring's Resource-Chain in [`WebMvcConfig`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/config/WebMvcConfig.java) setzt jetzt direkt `Cache-Control: max-age=31536000, public, immutable` via `setCacheControl(CacheControl.maxAge(365 days).cachePublic().immutable())` statt nur `setCachePeriod` — identischer Header-Output ohne Per-Request-Filter. Zweitens: [`RateLimitingFilter`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/filter/RateLimitingFilter.java) instantiiert `IpAddressMatcher` jetzt einmal im Konstruktor statt pro Request; malformed Eintraege werden beim Filter-Build geloggt und ausgesondert. Drittens: [`CorrelationIdFilter`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/logging/CorrelationIdFilter.java) trunciert die eingehende Header-Value VOR der `isSafe`-Char-Validierung — verhindert quadratisches Verhalten bei boesartig grossen Headern (vorher lief `isSafe` ueber bis zu 64 KB, jetzt immer maximal 128 Zeichen).

### Fixed

- **Refinery-Auftrag-Anlage – Ausgangsmenge (SCU) wurde ab dem zweiten Material nicht mehr automatisch aus den Units berechnet ([#148](https://github.com/krt-profit/basetool/issues/148)).** Reproduktion: Neuer Raffinerieauftrag anlegen, zwei oder mehr Materialien hinzufuegen, in der Units-Spalte eines Materials einen Wert eintippen und mit Tab weiterspringen — fuer Material 1 fuellt sich das `Ausgangsmenge (SCU)`-Feld korrekt mit `units / 100`, fuer Material 2 und alle weiteren bleibt das Feld leer. Ursache war eine inkonsistente Index-Renumerierung beim Klonen einer Material-Zeile in [`refinery-orders-create.html`](frontend/src/main/resources/templates/refinery-orders-create.html) und [`refinery-orders-details.html`](frontend/src/main/resources/templates/refinery-orders-details.html): das CSP-safe Delegated-Event `krtEvents.on('input', 'rfc-calc-scu', ...)` (analog `rod-calc-scu` auf der Detail-Seite) liest beim Feuern `el.getAttribute('data-index')` und ruft damit `calcScu(index)` auf, das die richtige `outputQuantity_<index>` / `outputQuantityScu_<index>`-Paar adressiert. Die `addMaterialRow()`-Funktion klonte den ersten `material-entry`-Block via `cloneNode(true)` und benannte `input.id` (`outputQuantity_0` -> `outputQuantity_1`) sowie `input.name` (`goods[0].outputQuantity` -> `goods[1].outputQuantity`) ueber Regex-Replacements um — vergass aber den **`data-index`**-Attribut-Wert: der blieb auf `0`. Beim Tippen in Material 2's Units-Feld feuerte zwar das delegated Event, aber `calcScu(0)` las den (leeren) Wert aus Material 1 und schrieb ihn in Material 1's SCU-Feld; das SCU-Feld von Material 2 blieb unangetastet. Der gleiche Fehler steckte spiegelbildlich in `removeMaterialRow()`, wo nach dem Loeschen einer Zeile die verbleibenden Eintraege zwar `id`/`name` neu durchgezaehlt bekamen, aber `data-index` ebenfalls auf den alten Werten verharrte — eine zweite Klick-Sequenz auf der gleichen Seite konnte damit den Bug auch nach einem Remove reproduzieren. Zusaetzlich enthielt `addMaterialRow()` einen toten `outputUnitInput.setAttribute('oninput', 'calcScu(' + count + ')')`-Aufruf, der unter der aktiven Content-Security-Policy (`script-src-attr` ohne `unsafe-inline`) sowieso nicht ausgefuehrt wurde — Reliquie der pre-CSP-Migration, die nur noch verschleiert hat, wo die echte Wiring lief. **Fix:** beide `addMaterialRow`/`removeMaterialRow`-Implementierungen in [`refinery-orders-create.html`](frontend/src/main/resources/templates/refinery-orders-create.html) und [`refinery-orders-details.html`](frontend/src/main/resources/templates/refinery-orders-details.html) bekommen einen `if (input.hasAttribute('data-index')) input.setAttribute('data-index', count)`-Block in der Inputs-Schleife, sodass das delegated `rfc-calc-scu` / `rod-calc-scu`-Event beim Tippen in Material N das richtige SCU-Feld trifft. Der `setAttribute('oninput', ...)`-Block ist entfernt — die `data-trigger`-basierte Wiring ist die einzige aktive Source. `removeMaterialRow()` synchronisiert zusaetzlich die `outputMaterialDisplay_*`-Span-IDs, damit `updateOutputMaterial` nach einem Remove weiterhin den korrekten Display-Slot trifft. **Verifikation:** `./gradlew :frontend:test :frontend:checkstyleMain :frontend:spotbugsMain` BUILD SUCCESSFUL.

### Security

- **OWASP Dependency-Check: 21 CVEs gefixt, Weekly-Scan wieder gruen.** Run 26151404522 schlug an Tomcat 11.0.21, Netty 4.2.12 und pgjdbc 42.7.10 fehl; per Spring-Boot-BOM-Override im Root-`build.gradle.kts` ([build.gradle.kts](build.gradle.kts)) auf gepatchte Versionen gepinnt: Tomcat `11.0.22` (CVE-2026-41284/-41293/-42498/-43512/-43513/-43515), Netty `4.2.13.Final` (CVE-2026-42577/-42579/-42581 bis -42587, -44248) und pgjdbc `42.7.11` (CVE-2026-42198 SCRAM-SHA-256 DoS). Vier dokumentierte False-Positives sind in der neuen [`config/owasp/dependency-check-suppressions.xml`](config/owasp/dependency-check-suppressions.xml) gesammelt: CVE-2022-31691 (CPE confusion `spring-boot-devtools` vs. "Spring Tools 4 for Eclipse"), CVE-2015-0897 (CPE confusion `pitest-command-line` vs. LINE Messenger), CVE-2025-67030 (`plexus-utils` 3.3.0 ist Checkstyle-Build-time-only) und CVE-2022-1471 (SnakeYAML 1.31 nested in `jruby-complete` des Asciidoctor-Plugins). Drop die BOM-Overrides, sobald ein Spring-Boot-4.0.x-Patchrelease die gleichen Versionen adoptiert.

- **PII-Leak auf den deprecated Legacy-Participant-Endpunkten geschlossen** (Folgeaudit 2026-05-20, C-1). `PUT /api/v1/missions/{id}/participants/{participantId}`, `…/check-in`, `…/check-out`, `…/payout-preference` und `DELETE …/{participantId}` lieferten anonymen Aufrufern (zulaessig fuer Gast-Participants ueber `canAccessParticipant`) das volle `MissionDto` mit `email`/`firstName`/`lastName` aller anderen Teilnehmer; das gleiche Leck bestand auf den `/slim`-Geschwistern. Fix wendet jetzt `cleanupMissionForGuest` bzw. `cleanupParticipantForGuest` an, wenn `jwt == null`. Die ArchUnit-Regel `anonymousReadableMissionEndpointsMustRedactGuestPii` deckt jetzt zusaetzlich Endpoints ab, die nur ueber `canAccessParticipant` gegated sind — damit kann der naechste `canSeeMission`-/`canAccessParticipant`-gegatete Endpoint mit Mission/Participant-Response die Redaction nicht mehr "vergessen". Auszahlungsart-Wechsel durch anonyme Gast-Teilnehmer (Auszahlung/Spende) bleibt funktional, liefert aber jetzt eine redaktierte Antwort.

- **Anonyme JobOrder-Erstellung funktional + Response redaktiert** (Folgeaudit L-1 + C-1-Familie). `POST /api/v1/orders` war zwar `permitAll()`, schlug aber durch `BadRequestException("No active squadron context")` fuer jeden anonymen Aufrufer fehl, weil `resolveCreatingSquadronForCreate` keinen Fallback fuer einen leeren Squadron-Scope hatte. Fix: anonyme Aufrufer bekommen jetzt IRIDIUM als kanonischen Audit-Trail-Owner gestempelt (Admin-in-"alle Staffeln"-Modus wirft weiterhin 400, weil dort eine bewusste Auswahl noetig ist). Neue Hilfsmethode `cleanupJobOrderForGuest` strippt `assignees`/`handovers`/`version` aus der Response — `id`/`displayId`/`materials`/Squadron-Refs bleiben, damit das public Request-Formular eine Bestaetigung mit Order-Nummer anzeigen kann.

- **Mass-Assignment auf `POST /api/v1/missions/{id}/participants/add` geschlossen** (Folgeaudit H-1). Die Legacy-Public-Variante uebernahm `request.userId()` ungeprueft — ein authentifizierter Squadron-Member konnte damit jeden anderen registrierten User stillschweigend zum Einsatz anmelden (die `/slim`-Variante hatte den Self-vs-Manager-Check bereits seit dem 2026-05-20-Audit, die Public-Variante fehlte). Fix mirror't den Slim-Check: Selbst-Anmeldung bleibt frei, andere User hinzufuegen erfordert `canManageMission`. Anonyme behalten weiterhin die Spoofing-Schutzschicht (anonyme `userId`-Submissions sind 403, anonyme Freetext-Namen, die einen registrierten User treffen, sind 400). Plus `cleanupMissionForGuest` jetzt auch fuer authentifizierte Nicht-Logistician — die Response auf der Public-Variante ist nun mit der `/slim`-Variante in Deckung.

- **`GET /api/v1/users/{id}` squadron-scoped** (Folgeaudit H-3). Die Listen-Endpoints (`/users`, `/search`) waren bereits via `findAllScoped`/`searchScoped` staffel-gefiltert; `findById` war es nicht — ein Officer aus Staffel A konnte ueber eine erratene UUID volle Email/Real-Name eines Users aus Staffel B abrufen. Fix prueft jetzt `SquadronScopeService.canSeeSquadron(user.squadron.id)` und liefert die Peer-Redaction-Form an Cross-Staffel-Non-Admins; Admin sieht immer alles. Users ohne Squadron (Admin-Accounts, Unassigned) zaehlen fuer Non-Admins als Cross-Staffel.

- **Mission-Lookup `findAllActiveReference` staffel-gefiltert** (Folgeaudit H-4). Der Dropdown-Endpoint hinter `/api/v1/missions/lookup` (und der Service-Methode) gab id+name+plannedStartTime jeder geplanten/laufenden Mission zurueck, ohne `is_internal`-Filter oder Squadron-Scope — ein Member aus Staffel A lernte ueber das Dropdown die Namen interner Missionen aus Staffel B. Fix spiegelt jetzt das `searchMissions`-Praedikat (`scopeSquadronId IS NULL OR owningSquadron = scopeSquadronId OR isInternal = false`).

- **`MissionFinanceEntryController.getFinanceEntries` mit Sort-Whitelist** (Folgeaudit M-1). Endpoint band einen ungefilterten `Pageable` — Clients konnten `?sort=participant.user.email,desc` schicken und ueber Sortierordnung Information ueber PII-Spalten ableiten (oder die DB mit `?size=100000` ueberlasten). Fix benutzt `PaginationUtil.createPageRequest` mit der Whitelist `{createdAt,amount,type,note,id}`; unbekannte Felder liefern jetzt 400.

- **`MaterialProxyController` baut URLs ueber `UriComponentsBuilder`** (Folgeaudit M-2). `getProfitCalculation` konkatenierte `&starSystemNames=` + Wert manuell — ein Name mit `&` haette dem Backend zusaetzliche Query-Parameter unterjubeln koennen. Fix benutzt `queryParam` (analog `UserProxyController`).

- **Free-text `guestName` und Keycloak-`username` nicht mehr im Klartext geloggt** (Folgeaudit M-3 + M-4). `MissionService` loggt beim Guest-Participant-Update nur noch die `participantId`; `UserSyncTask`-Error logged `user.id()` statt `user.username()`. PiiMasker faengt nur Emails/JWTs/Token-Schlagworte ab — freie Real-Names liefen vorher unmaskiert ins JSON-Log auf Disk.

- **`MissionService.getAllMissions(Pageable)` als Footgun deaktiviert** (Folgeaudit M-5). Die ungetilterte `findAll`-Delegierung war auf dem Controller-Pfad zwar bereits unbenutzt, blieb aber als verfuehrerischer Fall-Back-Pfad stehen und haette eine cross-staffel-Sichtbarkeit jeder internen Mission geoeffnet, sobald sie irgendwo aufgerufen wird. Sie ist jetzt `@Deprecated(forRemoval=true)` und wirft `UnsupportedOperationException` mit dem Hinweis auf `searchMissions`.

- **Explizites `@PreAuthorize("isAuthenticated()")` auf `/api/v1/users/me`** (Folgeaudit L-2). Der Endpoint war nur ueber den `SecurityConfig`-URL-Matcher gegated; mit der zusaetzlichen Method-Level-Annotation ueberlebt das Auth-Gate auch ein Pfad-Refactoring.

- **OAuth2-Scope `offline_access` aus dem Frontend-Login entfernt** (Folgeaudit L-4). Refresh-Tokens werden ohnehin in der Redis-backed Spring-Session gehalten — `offline_access` produziert nur einen langlebigen Token, der ueber die Session-Lifetime hinausreicht und im Redis-Snapshot-Leak-Szenario zusaetzlich exfiltrierbar waere.

- **Endpoint-spezifische Rate-Limits fuer anonyme Spam-Vektoren** (Folgeaudit L-5). Das globale 300/min/IP-Budget auf `/api/**` blieb generoes genug, dass ein einzelner Angreifer mit einer IP locker 300 Mission/JobOrder/Finance-Entry/Participant-POSTs pro Minute absetzen konnte, ohne anzuschlagen. [`RateLimitProperties`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/config/RateLimitProperties.java) traegt jetzt eine `rules`-Liste mit per-Pattern + per-Methoden-Buckets; [`RateLimitingFilter`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/filter/RateLimitingFilter.java) iteriert sie tightest-first und bricht beim ersten leeren Bucket ab (die loosen Buckets werden nicht unnoetig debited, falls der tighter zuerst trippt). [`application.yml`](backend/src/main/resources/application.yml) fuegt vier Rules hinzu: `POST /api/v1/missions` 10/min, `POST /api/v1/orders` 10/min, `POST /api/v1/finance-entries` 20/min, `POST|PUT|DELETE /api/v1/missions/*/participants/**` 30/min. Der `X-Rate-Limit-Limit`-Header reflektiert auf dem Happy-Path das tightest matchende Budget; bei 429 wird die Capacity der spezifisch ausgeloesten Rule attribuiert. Bestehende globale 300/min-Quote bleibt als Dachschicht ueber allem.

- **Frontend-CSP `style-src` jetzt nonce-gated, kein pauschales `'unsafe-inline'` mehr** (Folgeaudit L-3). Frontend-[`SecurityConfig.java`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/config/SecurityConfig.java) emittiert jetzt `style-src 'self' 'nonce-X'` (selber Nonce wie `script-src`, per Request neu via [`CspNonceFilter`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/config/CspNonceFilter.java)). Alle 38 `<style>`-Bloecke in den Thymeleaf-Templates wurden auf `<style th:attr="nonce=${cspNonce}">` gepatcht — ein per Stored-XSS injizierter `<style>`-Tag (z.B. ueber Mission-Name / Finance-Note) wird vom Browser jetzt nicht mehr ausgewertet. Die 859 verbleibenden inline-`style="..."`-Attribute (viele dynamisch via `th:style`, z.B. Progress-Bars / Color-Swatches) bleiben ueber die explizite Sub-Direktive `style-src-attr 'unsafe-inline'` erlaubt — separater CSP3-Hebel, der spaeter inkrementell ueber `'unsafe-hashes' 'sha256-…'` engerzudrehen ist, ohne dass `style-src` neu angefasst werden muss. Pinning-Test [`SecurityHeadersTest#cspStyleSrcIsNonceGated_andStyleSrcAttrIsExplicit`](frontend/src/test/java/de/greluc/krt/profit/basetool/frontend/SecurityHeadersTest.java) verifiziert die Direktiven-Topologie + den Nonce-Pattern und fuehrt Regression auf den alten `'unsafe-inline'`-Pfad sofort zum roten Build. Backend-[`SecurityConfig.java`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/config/SecurityConfig.java) behaelt `'unsafe-inline'` auf `style-src` weil Swagger-UI eigene Inline-Styles braucht und die API-Responses JSON sind — kein realer XSS-Vektor.

- **PII-Leak auf anonymen Mission-Participant-Endpunkten geschlossen** (Audit-Finding C-1). `POST /api/v1/missions/{id}/participants/{add,slim}` lieferten an anonyme Aufrufer die volle Teilnehmerliste inkl. `email`/`firstName`/`lastName`/`roles` zurück, weil die Redaction `cleanupMissionForGuest` / `cleanupParticipantForGuest` (von `getMissionById`/`getNextMission` bereits genutzt) auf den Write-Pfaden fehlte. Fix wendet sie jetzt bei `jwt == null` an; eine neue ArchUnit-Regel `anonymousReadableMissionEndpointsMustRedactGuestPii` in [ArchitectureTest](backend/src/test/java/de/greluc/krt/profit/basetool/backend/ArchitectureTest.java) bricht den Build, falls ein zukünftiger `canSeeMission`-gegateter Endpoint mit Mission/Participant-Response die Redaction wieder vergisst.

- **Anonymes `POST /api/v1/finance-entries` gehärtet** (Audit-Finding C-2). Der Endpoint war `permitAll()`, akzeptierte Schreibzugriffe auf interne Missionen, gab volle Participant-PII in der Response zurück und hatte keine Limits auf `note`/`amount`. Fix: Gate auf `@squadronScopeService.canSeeMission(#dto.missionId())` (interne Missionen → 403 für Anonyme), Guest-Redaction der Response analog zu C-1, sowie `@Size(max=2000)` auf `note` und `@DecimalMax("1000000000.0")` auf `amount` in beiden Finance-Entry-DTOs. Die ArchUnit-Regel aus C-1 wurde auf `cleanup…ForGuest`-Pattern verallgemeinert und deckt `MissionFinanceEntryDto` jetzt mit ab.

- **Mass-Assignment auf `POST /api/v1/missions` / `POST /api/v1/missions/{id}/sub-missions` / `PUT /api/v1/missions/{id}` strukturell unmöglich gemacht** (Audit-Finding C-3). Die drei Write-Endpoints akzeptierten den vollen `MissionDto`; weil [MissionMapper#toEntity](backend/src/main/java/de/greluc/krt/profit/basetool/backend/mapper/MissionMapper.java) `id`/`version`/`owningSquadron`/`parent`/`isInternal` aus dem DTO durchliess, konnte ein authentifizierter Caller per `EntityManager.merge` eine fremde Mission überschreiben (oder eine forged Mission einer fremden Staffel unterjubeln). Nachhaltiger Fix: neue `CreateMissionRequest` / `UpdateMissionRequest` Records in `dto/request/` ohne diese Felder; `MissionService.{createMission,addSubMission,updateMission}` nehmen jetzt die Request-Records statt einer fertig-gemappten `Mission`-Entity entgegen und stampen Owner/`owningSquadron`/`parent` unconditionnally server-seitig; `MissionMapper.toEntity(MissionDto)` entfernt. Neue ArchUnit-Regel `responseOnlyDtosMustNotBeAcceptedAsRequestBodyOnWriteEndpoints` in [ArchitectureTest](backend/src/test/java/de/greluc/krt/profit/basetool/backend/ArchitectureTest.java) verbietet künftig Response-DTOs (Startliste: `MissionDto`) als `@RequestBody` auf POST/PUT/PATCH.

- **Squadron-Stamp-Forgery bei Mission-Create gegen Regressionen abgesichert** (Audit-Finding C-4). Die C-3-Migration hatte den Code-Pfad bereits strukturell geschlossen (kein `owningSquadron` mehr im Request-Record → das alte `if (owningSquadron == null)`-Conditional ist weg, das Stamping läuft unbedingt aus Owner.Squadron / `SquadronScopeService.currentSquadron()` bzw. für Sub-Missionen aus `parent.owningSquadron`). Ergänzung: neue ArchUnit-Regel `missionWriteRequestDtosMustNotCarryServerManagedFields` in [ArchitectureTest](backend/src/test/java/de/greluc/krt/profit/basetool/backend/ArchitectureTest.java) verbietet `owningSquadron*` / `squadronId` / `owner*` / `parent*` / `id` / `managers`-Komponenten auf `CreateMissionRequest` und `UpdateMissionRequest` — ein zukünftiger "convenience"-Patch der eines dieser Felder einführt, bricht den Build. Plus drei Mockito-Tests in [MissionServiceTest](backend/src/test/java/de/greluc/krt/profit/basetool/backend/MissionServiceTest.java) die das Stamping-Verhalten (Owner-Squadron, Scope-Fallback, Parent-Inheritance) pinnen.

- **Stored XSS via `Mission.calendarLink` blockiert** (Audit-Finding H-1). `@Pattern(regexp = "^(https://.*)?$")` auf `PatchMissionCoreRequest` / `CreateMissionRequest` / `UpdateMissionRequest` und [MissionForm](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/model/form/MissionForm.java) lehnt `javascript:` / `data:` / `vbscript:` URLs ab — Thymeleaf's `th:href` HTML-escape filtert das Scheme nicht selbst und das Feld wird auf der anonymen Landing-Page `index.html` als `<a href>` gerendert.

- **Length-/Range-Caps auf anonymen Write-DTOs** (Audit-Finding H-2). `@Size(max=100)` auf `guestName`, `@Size(max=1000)` auf `comment`/`note`, `@Size(max=200)` auf `handle`, `@Size(max=50)` auf JobOrder-`materials`-Liste, `@Max(100_000)` auf JobOrder-Material-`amount` — schliesst die DoS-Surface gegen 100MB-Strings / unbounded-list-Spam.

- **Squadron-ID-Forgery für anonyme Guest-Participants geblockt** (Audit-Finding H-3). Neuer `MissionService#resolveGuestSubmittedSquadron`-Helper: bei `jwt == null` wird ein vom Caller mitgeschickter `squadronId` auf null koerziert (kein Throw — silent-drop); ein authentifizierter Caller darf nur einen Squadron labeln, für den `AuthHelperService.canEditSquadron(...)` true ist, sonst 403. Greift in `addParticipant` UND `updateParticipantAttributes`.

- **`UserController`-Endpoints redacten PII für Non-Officer-Caller** (Audit-Findings H-4 + H-5). `GET /api/v1/users`, `/search`, `/{id}` und `MissionController.addParticipantSlim` rufen jetzt `redactForPeerIfNeeded` / `cleanupParticipantForGuest` auf, wenn der Caller nicht Officer/Admin ist — `email`, `firstName`, `lastName`, `roles`, `permissions`, `joinDate` werden auf null gesetzt. `/users/me` und Officer/Admin-Pfade liefern unverändert die volle `UserDto`. Schließt die DSGVO-relevante Member-Email-Enumeration (KRT_MEMBER konnte vorher per `/search` paginiert die komplette Member-Datenbank harvesten).

- **CSRF-Exception-Liste auf bearer-only API beschränkt** (Audit-Finding H-6). Frontend-`SecurityConfig` entfernt `/missions/**`, `/operations/**`, `/inventory/**`, `/hangar/import/**`, `/hangar/ships/all` aus `ignoringRequestMatchers` — diese Pfade sind session-cookie-authentifiziert (genau die Surface, die CSRF schützen soll). Thymeleaf-Formulare bekommen das `_csrf`-Hidden-Field automatisch über Spring Securitys View-Integration; AJAX-Call-Sites senden bereits `X-XSRF-TOKEN`.

- **WebSocket Origin-Allowlist statt `"*"`** (Audit-Finding H-7). [MissionPresenceWebSocketConfig](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/config/MissionPresenceWebSocketConfig.java) liest jetzt `app.websocket.allowed-origin-patterns` (Default: `https://iri-base.org,https://localhost:18081,http://localhost:18081`) — Cross-Site WebSocket Hijacking ist ohne diese Schicht möglich, weil der Browser beim Upgrade automatisch das Session-Cookie mitschickt und der Spring-Security-Authenticated-Check allein nicht reicht.

- **Rate-Limit greift hinter nginx-proxy-manager** (Audit-Finding H-8). `server.forward-headers-strategy: framework` in `application.yml` macht `X-Forwarded-For` für Tomcat sichtbar; [RateLimitingFilter](backend/src/main/java/de/greluc/krt/profit/basetool/backend/filter/RateLimitingFilter.java) matcht `trusted-proxies`-Einträge jetzt via Spring Securitys `IpAddressMatcher` (CIDR-fähig); `application-prod.yml` setzt `trusted-proxies` defensiv auf die typischen Docker-Bridge-Ranges (`172.17.0.0/16`, `172.18.0.0/16`, `172.19.0.0/16`). Vorher kollabierte der Per-IP-Bucket hinter NPM zu einer Org-weiten 300/min-Quote.

- **HSTS explizit konfiguriert** (Audit-Finding H-9). `headers.httpStrictTransportSecurity(includeSubDomains, preload, maxAge=1y)` in Backend- und Frontend-`SecurityConfig` — Spring Securitys Default-Writer wäre vom request-scoped `isSecure()` abhängig und schwiege hinter einem fehlkonfigurierten Reverse-Proxy.

- **JWT-Claim- und OIDC-Attribute-Logs auf `.keySet()` reduziert** (Audit-Findings H-10 + H-11). `UserService:123` loggt nur noch die Claim-Keys statt der ganzen Claims-Map; `SecurityConfig:267` (Frontend) loggt nur die Attribute-Keys; Frontend-`logback-spring.xml` hebt den Package-`de.greluc.krt.profit.basetool`-Logger von `TRACE` auf `INFO`, sodass DEBUG-Calls in `config/` und `controller/` in Prod nicht mehr automatisch auf Disk landen.

- **`searchMissions` erzwingt `isInternal=false` für Anonyme im Service-Layer** (Audit-Finding M-1). [MissionService](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/MissionService.java) ersetzt einen vom Caller übergebenen `null`/`true` durch `Boolean.FALSE`, wenn `authHelperService.isAuthenticated() == false` ist — defense-in-depth gegen einen Controller-Bug der den Param vergisst.

- **`canSeeMission` hardened** (Audit-Findings M-2 + M-3). SquadronScopeService: Anonyme sehen keine `COMPLETED`/`CANCELLED`-Missionen (M-2, blockt anonyme Writes auf archivierte Daten); der `parent`-Chain wird gewalkt (M-3, ein nicht-interner Sub-Mission unter einem internen Parent wird abgelehnt).

- **Per-Mission-Participant-Cap (500)** (Audit-Finding M-4). [MissionService.addParticipant](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/MissionService.java) wirft 409 `BusinessConflictException` ab dem 500. Eintrag — verhindert dass ein anonymer Skript-Angreifer mit der 300/min-Rate-Limit-Quote die `mission_participant`-Tabelle vollkippt.

- **Slim Acknowledgement-DTO für anonyme `POST /api/v1/finance-entries`** (Audit-Finding M-5). `cleanupFinanceEntryForGuest` setzt `participant` und `version` auf null — die Response trägt nur `id`/`missionId`/`type`/`amount`/`note`. Die toten `cleanupParticipantForGuest`/`cleanupUserForGuest`-Helper im Controller wurden mitentfernt.

- **`GET /api/v1/squadrons?includeInactive=true` admin-only** (Audit-Finding M-6). [SquadronController](backend/src/main/java/de/greluc/krt/profit/basetool/backend/controller/SquadronController.java) wirft 403 wenn der Caller kein `ROLE_ADMIN` hat; ohne den Flag bleibt der Default-Pfad anonym-lesbar.

- **`handleResponseStatus` echo'd nicht mehr `ex.getMessage()`** (Audit-Finding M-7). [GlobalExceptionHandler](backend/src/main/java/de/greluc/krt/profit/basetool/backend/exception/GlobalExceptionHandler.java) nimmt nur den `reason` (oder die `status.getReasonPhrase()`) als Detail — verhindert dass die `"; nested exception is …"`-Postfix-Form mit Klassennamen + Hibernate-Constraint-Namen über `HangarImportProxyController.forwardImport` an den Client leakt.

- **Explizite Multipart-Limits (`2MB/2MB`)** (Audit-Finding M-8). [application.yml](backend/src/main/resources/application.yml) — die Spring-Default-Caps (1MB/10MB) sind heute restriktiv genug, das explizite Pinning macht den Vertrag versionsicher und dokumentiert.

- **CSP gehärtet** (Audit-Findings M-9 + M-10). Beide Module: `form-action 'self'` (injizierte Forms können keine Daten an Fremd-Origins POSTen) + `upgrade-insecure-requests` (Mixed-Content-Schutz). Frontend zusätzlich: `script-src` ohne den `https:`-Fallback — der weichte die Policy für ältere Browser ohne `strict-dynamic`-Support auf.

- **COOP + CORP Header gesetzt** (Audit-Finding M-12). Beide [SecurityConfig](backend/src/main/java/de/greluc/krt/profit/basetool/backend/config/SecurityConfig.java)s emittieren `Cross-Origin-Opener-Policy: same-origin` (isoliert die browsing-context-group, blockt `window.opener`-Rückgriffe aus Popups) und `Cross-Origin-Resource-Policy: same-origin` (verhindert Fremd-Origins die App-Ressourcen via `<img>`/`<script>` einbinden).

- **`InsecureTrustManagerFactory` profile-gated auf dev/test** (Audit-Finding M-13). [WebClientConfig](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/config/WebClientConfig.java) attached den Insecure-Trust nur unter Spring-Profil `dev`/`test`; in Prod läuft der WebClient mit dem Default-JVM-Trust-Store → MITM zwischen Frontend und Backend ist nicht mehr möglich, sobald die Module auf verschiedenen Hosts laufen.

- **Explizite `sessionManagement`-Policy + `HttpSessionEventPublisher`** (Audit-Finding M-14). Frontend-[SecurityConfig](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/config/SecurityConfig.java): `sessionFixation.changeSessionId` explizit pinned, `maximumSessions(2)` mit `maxSessionsPreventsLogin(false)` für "neuestes Login gewinnt"-Semantik; `HttpSessionEventPublisher` damit `SessionRegistry` die Concurrent-Session-Logik überhaupt mitkriegt.

- **`SessionDebugFilter` `@Profile(dev, test)`** (Audit-Finding M-15). Der Filter loggt Session-IDs + Principal-Namen unter DEBUG — die Annotation verhindert dass eine versehentliche Log-Level-Erhöhung in Prod Session-IDs auf Disk schreibt. SecurityConfig wired ihn via `ObjectProvider<>` so dass die Abwesenheit in Prod den Filter-Chain-Build nicht crasht.

- **`RefineryOrderPageController` Username-Log auf hashed kurz-pseudonym** (Audit-Finding M-16). `principal.getName()` wird durch `u-<hex(hashCode)>` ersetzt (selbes Format wie `BackendRoleSyncFilter.maskPrincipal`) — der Audit-Log behält die Korrelations-Möglichkeit, schreibt aber den Keycloak-Username nicht mehr in Klartext.

- **`/robots.txt` als statisches File statt 404-block** (Audit-Finding M-17). [BotProtectionFilter](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/config/BotProtectionFilter.java) blockt den Pfad nicht mehr; eine statische [robots.txt](frontend/src/main/resources/static/robots.txt) mit `User-agent: * / Disallow: /` weist legitime Crawler korrekt ab, ohne den "Custom-App-Signal-404" zu senden.

- **Backend-Search-URLs ueber `UriComponentsBuilder`** (Audit-Finding L-1). [MemberManagementController](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/controller/MemberManagementController.java) und [UserProxyController](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/controller/UserProxyController.java) bauen Query-URIs jetzt typensicher zusammen — verhindert, dass ein crafted `search=foo&size=99999` zusaetzliche Parameter an den Backend smuggled.

- **CSRF-Cookie Secure + SameSite=Strict explizit** (Audit-Finding L-2). Backend-[SecurityConfig](backend/src/main/java/de/greluc/krt/profit/basetool/backend/config/SecurityConfig.java) setzt `setCookieCustomizer` auf der `CookieCsrfTokenRepository` — vorher fiel der Browser auf `SameSite=Lax`-Default zurueck, jetzt aligned mit der Session-Cookie-Konfig.

- **`Permissions-Policy` deckt alle nicht-genutzten Browser-Features ab** (Audit-Finding L-3). Beide [SecurityConfig](backend/src/main/java/de/greluc/krt/profit/basetool/backend/config/SecurityConfig.java)s erweitert auf `payment=()`, `usb=()`, `serial=()`, `bluetooth=()`, `accelerometer=()`, `gyroscope=()`, `magnetometer=()`, `display-capture=()`, `clipboard-read=()`, `clipboard-write=()`, `interest-cohort=()` zusaetzlich zu den bisherigen `geolocation/camera/microphone/fullscreen`.

- **`squadronId` MDC-Feld im Log-Pattern** (Audit-Finding L-5 / CLAUDE.md-Forderung). Backend [logback-spring.xml](backend/src/main/resources/logback-spring.xml) `LOG_PATTERN_CONSOLE` + `LOG_PATTERN_FILE` zeigen `[%X{squadronId:-}]` als drittes MDC-Field nach `correlationId`/`userId`; der prod-JSON-Encoder listet `squadronId` als `includeMdcKeyName`. Multi-Tenant-Incident-Triage hat jetzt den Squadron-Kontext direkt im Log.

- **`PiiMaskingLogstashEncoder` Value-Vergleich statt Referenz** (Audit-Finding L-7). [PiiMaskingLogstashEncoder](backend/src/main/java/de/greluc/krt/profit/basetool/backend/logging/PiiMaskingLogstashEncoder.java): `masked.equals(json)` statt `masked == json` — kein silent re-encode-roundtrip mehr, falls `PiiMasker.mask` ein neues String-Objekt im No-Match-Case zurueckgibt.

- **OWASP-Dependency-Check CVSS-Gate auf 7.0** (Audit-Finding L-8). [build.gradle.kts](build.gradle.kts) `failBuildOnCVSS = 7.0f` (HIGH-Severity-Band; vorher 11.0 = nie failing). Neue HIGH-CVEs auf Dependencies brechen ab jetzt die CI-Pipeline statt nur SARIF-getrigt nach GitHub-Security-Tab zu fliessen.

- **Backend `SessionCreationPolicy.STATELESS`** (Audit-Finding L-11). Backend-[SecurityConfig](backend/src/main/java/de/greluc/krt/profit/basetool/backend/config/SecurityConfig.java) erzwingt jetzt explizit, dass keine Sessions erzeugt werden — als reiner JWT-Bearer-Resource-Server braucht der Backend keinen Session-State; das verhindert, dass ein zukuenftiger `@SessionAttributes`-Bug einen Session pro anonymem Aufruf erzeugt.

## [v0.2.5](https://github.com/krt-profit/basetool/releases/tag/v0.2.5) - 2026-05-19

### Changed

- **Multi-Squadron-Umbau – Phase 7 Teil 3: finaler `DROP COLUMN job_order.squadron` + Frontend-Formularmigration auf `requestingSquadronId`.** Schliesst den zweiphasigen Drop-Pfad der Legacy-`squadron`-VARCHAR-Spalte ab, der mit V88 (PR #132, stop-write) in Prod begonnen hat und ueber den Soak-Release von V89 (PR #134, NOT-NULL-Tightening) getragen wurde. **Datenmodell:** Neue Migration [`V90__drop_job_order_squadron_string.sql`](backend/src/main/resources/db/migration/V90__drop_job_order_squadron_string.sql) macht ein einzelnes `ALTER TABLE job_order DROP COLUMN squadron`. Header dokumentiert die V88->V89->V90-Kette inklusive Slot-Renumber-Historie (V87 -> V89, V89 -> V90 nach dem V88-Solo-Deploy) und den Rollback-Pfad (Backup-Restore oder manuelles `ADD COLUMN squadron VARCHAR; UPDATE job_order SET squadron = (SELECT shorthand FROM squadron WHERE id = job_order.requesting_squadron_id);` vor einem Pre-V90-App-Release). Hibernate `ddl-auto=validate` bleibt zufrieden, weil V88 schon das `@Column private String squadron`-Feld auf der `JobOrder`-Entity entfernt hat — der DB-Spaltenwegfall macht nur noch eine vorhandene Asymmetrie konsistent. **Backend-DTO-Layer:** Das `String squadron`-Feld faellt aus allen drei Job-Order-DTOs ([`JobOrderDto`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/model/dto/JobOrderDto.java) – 13-arg → 12-arg, [`JobOrderReferenceDto`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/model/dto/JobOrderReferenceDto.java) – 6-arg → 5-arg, [`CreateJobOrderDto`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/model/dto/CreateJobOrderDto.java) – 6-arg → 5-arg) und ihren Frontend-Mirrors ([`JobOrderDto`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/model/dto/JobOrderDto.java), [`JobOrderReferenceDto`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/model/dto/JobOrderReferenceDto.java), [`CreateJobOrderDto`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/model/dto/CreateJobOrderDto.java)). Die Wire-Shape verliert damit das `squadron`-String-Feld endgueltig — Clients muessen die strukturierte `requestingSquadron`-Referenz (Read) bzw. `requestingSquadronId` UUID (Write) konsumieren, beides schon seit Phase 6 verfuegbar. **Mapper / Service-Layer:** [`JobOrderMapper.toDto`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/mapper/JobOrderMapper.java) verliert das `@Mapping(target = "squadron", source = "requestingSquadron.shorthand")` (kein DTO-Feld mehr, das gefuettert werden muesste). [`JobOrderService.findAllActiveReference`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/JobOrderService.java) baut das `JobOrderReferenceDto` ohne den manuellen Shorthand-Block. Der private `resolveRequestingSquadron`-Helfer kollabiert von 3-arg (UUID, String shorthand, Squadron fallback) auf 2-arg (UUID, Squadron fallback) — die String-Aufloesung gegen `squadron.shorthand` (case-insensitive Lookup mit `creatingFallback`-fallback bei No-Match) faellt weg. **Frontend-Formular:** [`JobOrderForm`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/model/form/JobOrderForm.java) tauscht das `private String squadron`-Feld gegen `private UUID requestingSquadronId`. Die Thymeleaf-Templates [`orders-create.html`](frontend/src/main/resources/templates/orders-create.html) (oeffentliche Anlage) und [`orders-detail.html`](frontend/src/main/resources/templates/orders-detail.html) (Edit-Modal) binden das `<select>` jetzt an `*{requestingSquadronId}` mit `<option th:value="${s.id}" th:text="${s.name}">` (UUID-Werte statt Free-Text-Shorthands). [`JobOrderPageController`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/controller/JobOrderPageController.java) reicht den `UUID` direkt durch (`form.getRequestingSquadronId()` an Position `requestingSquadronId` des Backend-DTOs); die Edit-Modal-Vorbefuellung zieht die UUID aus `order.requestingSquadron().id()` statt aus dem alten String-Feld. Das Handover-Modal pre-fillt den Empfaenger-Shorthand jetzt aus `order.requestingSquadron().shorthand()` statt aus dem entfernten `order.squadron()`. Die `order.squadron != null ? order.squadron : '—'`-Fallback-Spans in [`orders-detail.html`](frontend/src/main/resources/templates/orders-detail.html) und [`orders-index.html`](frontend/src/main/resources/templates/orders-index.html) reduzieren sich auf einen schlichten `—`-Render-Pfad, weil die Datenquelle weg ist. **Tests:** Die DTO-Konstruktor-Sites in [`JobOrderControllerTest`](backend/src/test/java/de/greluc/krt/profit/basetool/backend/controller/JobOrderControllerTest.java), [`JobOrderServiceTest`](backend/src/test/java/de/greluc/krt/profit/basetool/backend/service/JobOrderServiceTest.java), [`JobOrderServicePriorityAndStatusTest`](backend/src/test/java/de/greluc/krt/profit/basetool/backend/service/JobOrderServicePriorityAndStatusTest.java), [`JobOrderServiceAssigneeAndListTest`](backend/src/test/java/de/greluc/krt/profit/basetool/backend/service/JobOrderServiceAssigneeAndListTest.java), [`JobOrderMapperTest`](backend/src/test/java/de/greluc/krt/profit/basetool/backend/mapper/JobOrderMapperTest.java), [`JobOrderHandoverButtonLayoutTest`](frontend/src/test/java/de/greluc/krt/profit/basetool/frontend/controller/JobOrderHandoverButtonLayoutTest.java) und [`OrderHierarchyVisibilityTest`](frontend/src/test/java/de/greluc/krt/profit/basetool/frontend/controller/OrderHierarchyVisibilityTest.java) ziehen auf die neuen DTO-Aritaeten nach. `JobOrderMapperTest` ersetzt das `assertEquals("Iridium", dto.squadron())` durch `assertEquals("Iridium", dto.requestingSquadron().shorthand())`, weil das jetzt der einzige Trager dieser Information ist. **OpenAPI:** [`openapi.json`](backend/src/main/resources/api/openapi.json) verliert das `squadron`-Property auf den drei Job-Order-Schemas. **Deploy-Reihenfolge:** Dieser PR darf erst gemerged + deployed werden, wenn V89 (PR #134) seit mindestens einem Release-Zyklus in Prod laeuft — die Two-Phase-Drop-Regel verbietet einen V88+V90-Deploy im selben Release, V89 ueberbrueckt den noetigen Soak (V88 stop-write soaks waehrend V89 sich tightent). **Verifikation:** `./gradlew check` BUILD SUCCESSFUL (Backend + Frontend Tests + Checkstyle + SpotBugs + Spotless gruen).

### Fixed

- **Mission-Detail-Seite und Mission-Create-Seite zogen pro Render eine volle Operations-Liste mit `size=1000`.** [`MissionPageController.addOperationsToModel`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/controller/MissionPageController.java) loste das Operations-Dropdown ueber `GET /api/v1/operations?page=0&size=1000` auf — pro Render wurden bis zu 1000 vollstaendige `OperationDto`-Payloads (id, name, description, status, owningSquadron-Reference, version) ueber den Backend-WebClient gezogen, durch Resilience4j gefiltert, JSON-serialisiert und am Frontend wieder deserialisiert, obwohl das Thymeleaf-Template nur `op.id` und `op.name` rendert. Nach dem Multi-Squadron-Umbau kam zusaetzlich der ungeindexte `owning_squadron_id`-Filter dazu (PR #143 hat das nachgeholt), aber der grundlegende Throughput-Verlust war die uneingeschraenkte Payload-Groesse plus der `size=1000`-Hard-Cap, der die Liste in einem einzigen Request abruft statt sie zu paginieren. **Fix:** Neuer schmaler Lookup-Endpoint [`GET /api/v1/operations/lookup`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/controller/OperationController.java) liefert eine `List<OperationReferenceDto>` (id + name) — analog zum bereits existierenden `/api/v1/missions/lookup` fuer den Mission-Picker. Repository-seitig macht [`OperationRepository.findAllReferenceScoped`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/repository/OperationRepository.java) eine JPQL-Constructor-Expression-Projection `new OperationReferenceDto(o.id, o.name)` — Hibernate baut die DTOs direkt aus dem Result-Set, ohne die Operation-Entity in den Persistence-Context zu ziehen, und der `owning_squadron_id`-Filter aus PR #143 greift sofort (die V91-Indexe machen den Squadron-Filter zu einem konstanten Index-Scan). [`OperationService.findAllReference`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/OperationService.java) wrapt den Repository-Call mit der Squadron-Scope-Auflosung aus `SquadronScopeService`. Frontend-seitig spiegelt [`OperationReferenceDto`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/model/dto/OperationReferenceDto.java) den Backend-Record (id + name), und `MissionPageController.addOperationsToModel` ruft `/api/v1/operations/lookup` mit `ParameterizedTypeReference<List<OperationReferenceDto>>` statt der `PageResponse<OperationDto>`-Form. Das Template `mission-detail.html` Zeile 340 (`th:value="${op.id}" th:text="${op.name}"`) bleibt unveraendert — beide Records haben dieselben Accessoren. **Throughput-Effekt:** pro Mission-Render werden statt ~1000 vollen `OperationDto`-JSON-Objekten (jeweils ~280 Bytes + `SquadronReferenceDto`-Nested) nur noch `(UUID + String)`-Tupel transportiert (~60 Bytes pro Eintrag), und der Backend-Query-Plan reduziert sich auf eine einzige indexierte Projection ohne Entity-Hydration. Ein Caller mit 100 Operations spart pro Render-Hit etwa 25 KB JSON plus die korrespondierende Hibernate-Entity-Allocation. **Out of scope:** der `/api/v1/operations`-Endpoint mit voller `OperationDto`-Liste bleibt unveraendert — wird weiterhin von der Operations-Index-Seite (`/operations`) korrekt mit Pagination und Sortierung benutzt; nur der ungeschickte `size=1000`-Misbrauch fuer das Dropdown ist weg. **Verifikation:** `./gradlew :backend:check :frontend:check` BUILD SUCCESSFUL — alle bestehenden Operation- und Mission-Tests gruen, die Mission-Detail-Page rendert mit dem neuen Lookup-Endpoint identisch (gleiches Template, kompatible DTO-Accessoren).

- **`SquadronScopeService` machte fuer Non-Admin-Requests bei jedem Listen-Filter und jedem `canSee*`-Aufruf einen eigenen `userRepository.findById` — auf komplexen Seiten summierten sich die Roundtrips.** `SquadronScopeService.currentSquadronId` und das duplizierte `readPersistentSquadronFromUser` triggerten beide einen `userRepository.findById` gegen den JWT-`sub`, ohne Cache zwischen den Calls. Auf einer Mission- oder Operation-Detail-Seite, die in einem HTTP-Request mehrere `@PreAuthorize`-Gates und einen Listen-Filter durchlaeuft, summierte sich das auf bis zu drei bis fuenf identische User-Lookups pro Request; auf der Operation-Detail-Seite zusaetzlich `currentSquadron()` mit einem separaten `squadronRepository.findById` pro Aufruf. Hibernate's L1-Cache fing einen Teil davon ab, aber nur innerhalb derselben Transaktion — die Trennung zwischen Controller-Transaction und nachgelagerten Resilience4j-Callbacks plus die nicht-transaktionalen Authz-Checks brachen die Annahme regelmaessig. **Fix:** `SquadronScopeService` memoisiert sowohl `readPersistentSquadronFromUser` (User-Lookup) als auch `currentSquadron` (Squadron-Entity-Lookup) auf der `HttpServletRequest` via `setAttribute`/`getAttribute` mit klassen-prefix'ten Keys. Der oeffentliche `currentSquadronId()`-Pfad ruft den memoisierten Helper jetzt direkt auf (vorher duplizierter Inline-Code), womit alle Non-Admin-Aufrufe von `currentSquadronId`, `canSeeSquadron`, `canEditSquadron` und transitiv die fuenf aggregate-spezifischen `canSee*`/`canEdit*`-Methoden auf demselben Per-Request-Cache landen. `currentSquadron()` cached separat, damit `isPromotionFeatureEnabledForCurrentScope` plus Create-Pfad-Stamping (`Operation.create`) sich nicht zweimal die Squadron-Row holen. Ein neuer Helfer `readCachedOptional` haelt den unchecked Cast `Object -> Optional<T>` an einer einzigen Stelle eingedaemmt — beide Cache-Eintraege werden als ganze `Optional` gespeichert, sodass "aufgeloest zu empty" sauber von "noch nicht aufgeloest" unterscheidbar bleibt (sonst waere ein admin-without-header oder ein squadronless-Member nicht von einem Cache-Miss unterscheidbar). Admin-Pfade (Header-Read, kein DB-Hit) bleiben uncached — die `request.getHeader` ist konstanten Aufwand. **Caveat:** der Cache lebt an `HttpServletRequest`; ausserhalb einer HTTP-Request-Boundary (z. B. Scheduled-Task) ist der Service nicht aufrufbar — was er aber auch vorher schon nicht war, weil er den Request bereits seit Phase 3 als Pflicht-Dependency injiziert. **Tests:** neue `@Nested RequestScopedCacheTests` in `SquadronScopeServiceTest` pinnen das Verhalten: drei Tests verifizieren via Mockito-`verify(times(1))` dass `currentSquadronId`, `currentSquadron` und der gemeinsame Pfad `currentSquadronId + canSeeSquadron` bei zwei Aufrufen den jeweiligen Repository-Lookup nur ein einziges Mal absetzen. Die Request-Attribute werden im Test ueber einen `HashMap`-gebackenen Mockito-Answer simuliert, weil `@Mock HttpServletRequest` per default keine Attribute speichert. **Verifikation:** `./gradlew :backend:check` BUILD SUCCESSFUL, alle bestehenden `SquadronScopeServiceTest`-Faelle gruen — die Refaktorierung des `currentSquadronId`-Non-Admin-Branches (vorher Inline-Logik, jetzt Delegation an den Helper) bleibt verhaltensaequivalent.

- **Mission-Entity hatte drei implizit-EAGER `@ManyToOne`-Beziehungen, die bei jedem Mission-Load drei zusaetzliche Selects ausloesten.** [`Mission.parent`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/model/Mission.java), [`Mission.operation`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/model/Mission.java) und [`Mission.owner`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/model/Mission.java) trugen kein explizites `fetch`-Attribut — JPA's `@ManyToOne`-Default ist EAGER, also feuerte Hibernate beim Laden jeder Mission drei separate `SELECT`s gegen `mission`, `operation` und `app_user` ab, selbst wenn der konkrete Code-Pfad die Felder gar nicht brauchte. In den Listen-Endpoints summiert sich das ueber jede Zeile auf — selbst nach dem EntityGraph-Fix (PR #144) zog jeder Mission-Detail-Load drei ungenutzte Zusatz-Selects mit. `Mission.owningSquadron` war bereits explizit LAZY ([:151](backend/src/main/java/de/greluc/krt/profit/basetool/backend/model/Mission.java#L151)), die anderen drei nicht. **Fix:** Alle drei Felder bekommen jetzt `fetch = FetchType.LAZY` explizit. Die Implikationen pro Feld: (a) `parent` wird ausschliesslich in der Sub-Mission-Traversal verwendet (`subMissions`-Collection, selbst LAZY), in keinem Mapper oder Listen-Pfad — daher harmlos. (b) `operation` wird in `MissionMapper.toDto` und `toListDto` gelesen — beide laufen innerhalb der Controller-`@Transactional`-Boundary, der Lazy-Proxy initialisiert sich ohne `LazyInitializationException`; fuer den paged-`searchMissions`-Pfad ist das Feld zusaetzlich via `@EntityGraph` (PR #144) bereits eager-geladen, kein Extra-Roundtrip. (c) `owner` wird vom Mapper fuer die `canEdit`-Auflosung benoetigt, ebenfalls innerhalb der `@Transactional`-Boundary. **Out of scope:** der gleiche Pattern (implizit-EAGER `@ManyToOne`) existiert in `Ship`, `MissionParticipant`, `MissionUnit`, `MissionCrew`, `MissionFrequency`, `MissionOwnership`, `RefineryOrder`, `RefineryGood`, `RefineryYield`, `Material`, `MaterialPrice`, `JobType`, `Location` — etwa 20 Felder verteilt ueber 13 Entities. Sind alle separate Risikoflaechen (jedes Feld hat eigene Mapper-/Service-Pfade die geprueft werden muessen), und werden bewusst nicht in einem Schub umgestellt — jede Umstellung kann latente `LazyInitializationException`-Faelle aufdecken (siehe PR #141 zum Operation-Controller-LazyInit), und ein PR mit 20 Aenderungen waere nicht trennbar. Mission wurde als hochfrequentester Hot-Path zuerst angepackt; weitere Entities werden in eigenen Folge-PRs einzeln nachgezogen. **Verifikation:** `./gradlew :backend:check` BUILD SUCCESSFUL — alle Mission-Tests gruen, der ArchUnit-Layer-Check und die Optimistic-Locking-Tests treffen die geanderten Felder mit unveraenderten Erwartungen (Lazy-Proxy ist von aussen ununterscheidbar von einem geladenen Entity, solange man innerhalb der Transaction bleibt).

- **Missions-Listenseite triggerte In-Memory-Pagination durch Collection-Join im EntityGraph.** Der paged Variant von [`MissionRepository.searchMissions`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/repository/MissionRepository.java) zog ueber `@EntityGraph(attributePaths = {"participants", "assignedUnits"})` zwei `@OneToMany`-Collections in den Listen-Query, was Hibernate zwingt die `Pageable`-Slicing in den JVM-Heap zu schieben — SQL-`OFFSET/LIMIT` ist mit einem Collection-Fetch nicht kombinierbar. Hibernate logt das als `HHH000104: firstResult/maxResults specified with collection fetch; applying in memory`. Folge: jeder Aufruf der `/missions`-Seite (und jeder `/api/v1/missions/search`-Hit) zog **alle** matchenden Missions samt kartesischem `mission x participant x unit`-Join in den Speicher und slicte erst dort die Page. Auf dem Prod-Datenbestand summierte sich das nach dem Multi-Squadron-Umbau mit dem zusaetzlichen `OR is_internal = false`-Disjunkt zur Hauptursache der mehrsekuendigen Lade-Zeiten auf der Missions-Liste. **Auffaellig** wurde es, weil die Listen-DTO ([`MissionListDto`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/model/dto/MissionListDto.java)) die Collections gar nicht braucht — sie traegt nur Skalare plus `operation` und `owningSquadron` als `@ManyToOne`-Referenzen. Der EntityGraph war damit ein reines Eager-Load-Overhead ohne korrespondierenden Nutzer. **Fix:** Auf dem paged Variant in [`MissionRepository`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/repository/MissionRepository.java) wechselt der `@EntityGraph` von `{"participants", "assignedUnits"}` auf `{"operation", "owningSquadron"}` — exakt die zwei Felder, die [`MissionMapper.toListDto`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/mapper/MissionMapper.java) liest. Beide Felder sind `@ManyToOne` (Single-Row-Joins), nicht Collections, und damit kompatibel mit SQL-Pagination: Hibernate macht jetzt einen Single-Query-Plan mit `LIMIT/OFFSET` direkt auf der DB und initialisiert beide Referenzen in derselben Round-Trip. Die N+1-Falle des frueheren LAZY-`owningSquadron`-Proxy auf jeder Listen-Row entfaellt damit gleich mit. Die non-paged List-Variante der `searchMissions` und der Single-Row-`findById` behalten ihre `participants`/`assignedUnits`-EntityGraph — dort ist Collection-Fetch korrekt (kein Pageable). **Out of scope:** der ungenutzte `@EntityGraph` auf `findAll(Pageable)` derselben Repository-Klasse hat den gleichen Fehler, ist aber tote Codepfad (nur `MissionService.getAllMissions` ruft ihn, und das wiederum hat keinen Caller); bewusst nicht angepackt um den PR-Scope eng zu halten. **Verifikation:** `./gradlew :backend:check` BUILD SUCCESSFUL, alle bestehenden Mission-Tests gruen — die Mockito-Service-Tests treffen die paged Variante mit unveraenderter Signatur, der Mapping-Pfad ist unveraendert.

- **Performance-Regression nach dem Multi-Squadron-Umbau – fehlende Indexe auf den neuen Squadron-Scope-Spalten verursachten Sequential-Scans auf den Listen-Endpoints fuer Missionen und Operationen.** [`V82__add_owning_squadron_to_aggregates.sql`](backend/src/main/resources/db/migration/V82__add_owning_squadron_to_aggregates.sql), [`V81__add_squadron_id_to_app_user.sql`](backend/src/main/resources/db/migration/V81__add_squadron_id_to_app_user.sql), [`V83__add_job_order_squadron_columns.sql`](backend/src/main/resources/db/migration/V83__add_job_order_squadron_columns.sql) und [`V85__add_owning_squadron_to_promotion_topic.sql`](backend/src/main/resources/db/migration/V85__add_owning_squadron_to_promotion_topic.sql) haben die neuen Squadron-FK-Spalten zwar mitsamt FK-Constraints angelegt, aber keinen einzigen Index — PostgreSQL erzeugt fuer eine FK-Constraint nur auf der referenzierten Spalte (`squadron.id`) automatisch einen Index, nicht auf der referenzierenden. Jeder Listen-Query mit `owning_squadron_id = :scope OR is_internal = false`-Filter (siehe [`MissionRepository.searchMissions`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/repository/MissionRepository.java), [`OperationRepository.findAllScoped`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/repository/OperationRepository.java)) fiel damit auf einen Seq-Scan zurueck und wuchs linear mit der Tabellengroesse — auf dem Prod-Datenbestand spuerbar als mehrsekuendige Lade-Zeiten der Missions-/Operations-Seiten. **Fix:** Neue Migration [`V91__add_owning_squadron_indexes.sql`](backend/src/main/resources/db/migration/V91__add_owning_squadron_indexes.sql) ergaenzt B-Tree-Indexe auf allen neun Squadron-FK-Spalten — `mission`, `operation`, `ship`, `inventory_item`, `refinery_order`, `promotion_topic` jeweils auf `owning_squadron_id`; `app_user` auf `squadron_id` (Hotpath in `SquadronScopeService.currentSquadronId` fuer jeden Non-Admin-Request); `job_order` auf beiden `creating_squadron_id` und `requesting_squadron_id`. Zusaetzlich ein Composite-Index `idx_mission_owning_squadron_internal ON mission(owning_squadron_id, is_internal)`, der die kombinierte Cross-Staffel-Public-Escape-Klausel ohne Bitmap-OR-Join zwischen zwei Indexen bedient — der Optimizer kann damit den `OR is_internal = false`-Teil der `searchMissions`-WHERE in einem einzigen Index-Scan abdecken. Stil und Idempotenz analog [`V34__add_performance_indexes.sql`](backend/src/main/resources/db/migration/V34__add_performance_indexes.sql) (`CREATE INDEX IF NOT EXISTS`, kein `CONCURRENTLY` weil Flyway alle Migrationen in einer Single-Transaction faehrt und die betroffenen Tabellen klein genug sind, dass der kurze ACCESS-EXCLUSIVE-Lock waehrend des Index-Builds in keiner Umgebung Probleme macht). **Slot-Wahl V91 statt V90:** die V90-Slot ist im Phase-7-Plan reserviert fuer den finalen `DROP COLUMN job_order.squadron` (Two-Phase-Drop nach V88's stop-write). Die Index-Migration ist davon unabhaengig und wird auf den naechsten freien Slot V91 gelegt; Flyway's lexikalische Ordnung garantiert, dass beide Migrationen unabhaengig voneinander deployen koennen — V90 davor oder danach gemerged spielt keine Rolle. **Verifikation:** `./gradlew :backend:flywayInfo` zeigt V91 als pending; nach `bootRun` greift Hibernate `ddl-auto=validate` durch ohne Beanstandung (die Indexe sind reines Performance-Tuning, keine Schemaerweiterung die das Entity-Modell sehen muss).

## [v0.2.4](https://github.com/krt-profit/basetool/releases/tag/v0.2.4) - 2026-05-19

### Changed

- **Contributor License Agreement (CLA) + Developer Certificate of Origin (DCO) sign-off als verpflichtende Schritte fuer Contributions.** Zwei einander ergaenzende Mechanismen ersetzen die bisherige "good-faith-only"-Regel: (a) eine einmal pro Contributor zu unterzeichnende Individual-CLA, die der Project Maintainer-Rolle einen unbefristeten, weltweiten, royalty-freien Copyright- und Patent-Lizenz-Grant gibt (Copyright bleibt beim Contributor), und (b) einen Signed-off-by-Trailer auf jedem einzelnen Commit als per-Contribution-Bestaetigung gemaess DCO 1.1. **Neue Dokumente:** [`CLA.md`](CLA.md) im Repo-Root als vollstaendiger Vertragstext (Apache ICLA v2.0 als strukturelle Vorlage, an das Projekt angepasst — 13 Sektionen inklusive Definitionen, Copyright-/Patent-Grants mit der Standard-Defensive-Termination-Klausel falls ein Lizenznehmer eine Patentklage gegen den Contributor anstrengt, Representations, AS-IS-Disclaimer, Third-Party-Material-Pfad, Decline-Right, GPL-3.0-Beziehung, deutsches Recht als Governing Law mit CISG-Ausschluss, Signing-Prozedur, Widerrufs- und Amendment-Klauseln). [`docs/cla-signatures.md`](docs/cla-signatures.md) als oeffentliches Signatur-Register mit zwei Tabellen (Individual / Entity), in das Contributors ihren Eintrag per `cla: sign — <handle>`-PR appenden — der wortlautlich vorgeschriebene Annahme-Satz aus § 11 des CLA muss in die PR-Description. **CONTRIBUTING.md erweitert** um zwei neue Sektionen zwischen *Pull requests* und *Commit messages*: "Contributor License Agreement (CLA)" (one-time, deckt alle gegenwaertigen und zukuenftigen Contributions, Signature-PR vs. CLA-Assistant-Alternative, separater Entity-CLA-Pfad fuer Employer-funded Contributions, Hinweis dass auch Typo-Fix-PRs einen signierten CLA brauchen weil die Regel sonst nicht eindeutig waere) und "Developer Certificate of Origin (DCO) sign-off" (warum beide Mechanismen statt nur einer, vollstaendiger DCO-1.1-Wortlaut als Blockquote, `git commit -s`-Workflow, `git config format.signOff true` als Per-Repo-Opt-In, retroaktive Signoff-Wege via `git commit --amend -s --no-edit` und `git rebase --signoff main` + `git push --force-with-lease`, Enforcement-Regel "block bei Merge, nicht bei Commit", explizite Grandfathering-Klausel fuer Pre-Policy-Commits in `main`). **TL;DR-Block** oben in CONTRIBUTING.md gewinnt einen sechsten Punkt; **Table of Contents** zwei neue Anchor-Links; die **Before-You-Push-Checklist** zwei zusaetzliche Haken (CLA on file + jeder Commit Signed-off-by). Die alte "License and sign-off"-Sektion am Ende ist auf "License" verschlankt und verweist auf die zwei neuen Sektionen oben sowie auf § 6 des CLA fuer den Third-Party-Material-Disclosure-Pfad. **PR-Template** ([`.github/PULL_REQUEST_TEMPLATE.md`](.github/PULL_REQUEST_TEMPLATE.md)): zwei neue Checkboxen in der *General*-Sektion (signierter CLA vorhanden + jeder Commit traegt Signed-off-by-Trailer). **Enforcement:** Der DCO-Check ist als neue GitHub-Action [`.github/workflows/dco.yml`](.github/workflows/dco.yml) ausgeliefert und laeuft auf jedem Pull Request gegen `main` — ein 5-Minuten-Job, der `git log "${BASE_SHA}..${HEAD_SHA}"` ueber den PR-Range iteriert und pro Commit prueft, dass mindestens ein `Signed-off-by`-Trailer den Author-`Name <email>` case-insensitive matched. **Exemptions:** Merge-Commits (`parents >= 2`, von Git automatisch erzeugt, kein neuer Contributor-Input) und Commits von wohlbekannten Bot-Accounts (Dependabot, Renovate, github-actions, refreshVersions — mit beiden Variants `name[bot]@users.noreply.github.com` und der numerischen `12345+name[bot]@...`-Schreibweise, die GitHub historisch beide ausgeliefert hat) werden mit `::notice`-Annotations uebersprungen statt zu blockieren; die Maintainer-Sign-off bei Merge der Bot-PRs deckt den IP-Aspekt ab. **Bootstrapping-Hinweis im Workflow-Header:** der PR, der die Policy einfuehrt, kann selbst noch unsignierte Commits tragen — zwei dokumentierte saubere Pfade (`git rebase --signoff main` + `--force-with-lease`, oder einmaliger Admin-Merge mit ueberschriebenem Check, vom Grandfathering-Clause in CONTRIBUTING.md gedeckt). **Required-Check-Setup:** der Workflow-Header dokumentiert, dass nach dem ersten Lauf gegen `main` der Check-Name "DCO / Verify Signed-off-by on every commit" via Repository-Settings -> Branch protection als required status check auf `main` markiert werden muss (analog zum existierenden CodeQL-Pattern). Custom-Bash statt Drittanbieter-Action gewaehlt, weil (a) die Pruef-Logik ~80 Zeilen ist, (b) die existierenden Workflows (`ci.yml`, `codeql.yml`, `release-images.yml`) Actions sorgfaeltig pinnen und eine neue ungebundene Third-Party-Action gegen das Supply-Chain-Profile gegangen waere, (c) die Logik damit komplett in-tree und reviewbar ist. **CLA-Bot (CLA-Assistant) bleibt noch nicht aktiv** — die Wiring (GitHub-App-Install + Webhook-Konfiguration) ist Maintainer-Operations ausserhalb dieses Commits; bis dahin verifizieren Maintainer den CLA-Eintrag manuell gegen [`docs/cla-signatures.md`](docs/cla-signatures.md) beim Merge. **CLAUDE.md angepasst:** Die Git-Sektion gewinnt einen neuen MUSS-Absatz, der `git commit -s` (bzw. `-S -s` bei zusaetzlicher GPG-Signatur) als Pflicht fuer jeden Commit verankert — explizit *inklusive* AI-generierter Commits, damit Claude-Code-Sessions nicht versehentlich die neue Workflow-Check-Regel umgehen. Der Absatz dokumentiert das Name-/Email-Matching-Verhalten des Workflows, die Klarstellung dass die Bot-Exemptions nur fuer Bot-Author-Identities greifen (nicht fuer real-user-authored Commits mit AI-Body), und die Reparatur-Pfade fuer vergessene Flags (`--amend --signoff --no-edit` lokal, `rebase --signoff main` fuer den ganzen Branch — beides nur vor dem Push; ein nachtraegliches Rewriting bereits gepushter Commits faellt unter die bestehende Destructive-Ops-Regel und braucht explizite User-Zustimmung). **Zusaetzlich** verankert ein zweiter neuer MUSS-Absatz in derselben Git-Sektion die Pflicht-Verwendung eines `Co-Authored-By: <model-identifier> <noreply@anthropic.com>`-Trailers auf jedem Commit, den Claude komponiert oder substantiell editiert — die Trailer-Setzung war historisch inkonsistent und ist ab jetzt eine harte Transparenz-Regel, unabhaengig vom DCO-Signoff. Konkretes Format mit dem aktuellen Modell-Identifier (`Claude Opus 4.7 (1M context)`) als Codeblock-Beispiel inklusive Substitutions-Regel fuer andere Modelle (`Sonnet 4.6`, `Haiku 4.5`) — die Email bleibt `noreply@anthropic.com`, der Modell-Name traegt die "wer hat geschrieben"-Information. Anwendungsbereich explizit weit gefasst: gilt auch fuer Einzeiler-Fixes, CHANGELOG-only-Commits und Diffs aus woertlicher User-Anweisung; ausgenommen sind nur Faelle in denen Claude *gar nichts* am Commit-Inhalt komponiert (z. B. nur einen vom User getippten `git commit`-Befehl ausfuehrt) — Default im Zweifel: Trailer setzen. Repair-Pfad fuer vergessenen Trailer: `git commit --amend --no-edit` nach manuellem Anfuegen des Trailers, lokal vor dem Push (die Variante `--trailer` ist nicht ueberall verfuegbar, daher manuelles Editieren als robusterer Default). **Migration / Rollout:** Pre-Policy-Commits auf `main` sind explizit grandfathered (kein retroaktives Rewriting der Historie); Contributors mit offenen PRs am Tag des Inkrafttretens werden hoeflich angepingt, ihre noch-nicht-gemergten Commits via `git rebase --signoff main` zu signieren. **Governing-Law-Entscheidung:** deutsches Recht inklusive CISG-Ausschluss, weil der Hauptmaintainer in Deutschland sitzt und ein neutraler Gerichtsstand die Defensiv-Wirkung der Patent-Klausel klarer durchsetzbar macht als ein "law of the contributor's residence"-Konstrukt.

- **Frontend-Sidebar — dezente Versionsnummer der aktuell deployten Seite unter dem Sprach-Switcher, aufgeloest aus dem Git-Tag des deployten Commits.** Per Spring Boot Gradle Plugin's `bootBuildInfo`-Task wird beim Build eine `META-INF/build-info.properties` mit dem aufgeloesten Versions-String erzeugt; Spring Boot's `ProjectInfoAutoConfiguration` registriert daraus automatisch einen `BuildProperties`-Bean, der vom neuen [`AppVersionAdvice`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/config/AppVersionAdvice.java) (`@ControllerAdvice`, `@ModelAttribute("appVersion")`) als Model-Attribut auf jeden Render-Pfad geheftet wird. Die Dependency wird als `Optional<BuildProperties>` injiziert, damit `@WebMvcTest`-Slices ohne `ProjectInfoAutoConfiguration` weiterhin sauber wiren — Fallback ist der String `"dev"` statt `null`, sodass das Sidebar-Chip nie leer rendert. [`fragments/sidebar.html`](frontend/src/main/resources/templates/fragments/sidebar.html) zeigt das Ergebnis als kleines, gegrayetes, upper-case `v{version}`-Label unter dem Sprach-Dropdown (neue CSS-Klasse `.sidebar-version` in [`styles.css`](frontend/src/main/resources/static/css/styles.css): `--color-gray-3`, `0.7rem`, `letter-spacing: 0.05em`, `text-transform: uppercase`, kein Hover-/Click-Verhalten — rein informativ). i18n-Keys `app.version.display=v{0}` und `app.version.tooltip` in allen drei [`messages*.properties`](frontend/src/main/resources/messages.properties) (Default + DE + EN), keine Umlaute → kein `\uXXXX`-Escaping noetig. **Versions-Aufloesung in drei Stufen** (in [`frontend/build.gradle.kts`](frontend/build.gradle.kts)): (1) `-PappVersion=<value>` von der Gradle-Kommandozeile — wird vom CI-Docker-Build verwendet, weil `.dockerignore` `.git` aus dem Build-Context ausschliesst und `git describe` im Container damit nichts zu sehen haette; (2) `git describe --tags --always --dirty` gegen das Worktree-`.git` — der lokale Dev-Build-Pfad, `--tags` matcht die `vX.Y.Z`-Tags des Projekts, `--always` faengt frisch geklonte Repos ohne Tag mit dem Short-SHA ab, `--dirty` markiert ungetaggte Worktrees; (3) `project.version` (`0.0.1-SNAPSHOT`) als finaler Fallback. Das fuehrende `v` aus dem Tag-Namen wird am Ende abgeschnitten, damit das i18n-Template `v{0}` keinen `vv0.2.3`-Doppel-Prefix produziert. **CI-Plumbing in [`release-images.yml`](.github/workflows/release-images.yml):** `actions/checkout@v6` mit `fetch-depth: 0` (frueher `1` = shallow ohne Tags → `git describe` waere immer auf den Short-SHA-Fallback gefallen); neuer Step `Compute application version` schreibt `$(git describe --tags --always)` (ohne `--dirty` weil ein CI-Runner immer von einem sauberen Checkout startet — ein `-dirty`-Suffix dort wuerde auf einen CI-Bug deuten, nicht auf legitimen Worktree-State) in `$GITHUB_OUTPUT`; `docker/build-push-action` erhaelt einen `build-args: APP_VERSION=...`-Block. Beide Dockerfiles deklarieren `ARG APP_VERSION=` ([`frontend/Dockerfile`](frontend/Dockerfile) konsumiert es per `${APP_VERSION:+-PappVersion=$APP_VERSION}` in der Gradle-Build-Zeile, [`backend/Dockerfile`](backend/Dockerfile) deklariert nur damit BuildKit nicht "unused build arg" warnt — das Backend hat keine UI mit Version-Chip). **Tests:** [`AppVersionAdviceTest`](frontend/src/test/java/de/greluc/krt/profit/basetool/frontend/config/AppVersionAdviceTest.java) pinnt die drei Observable-States des Advice (BuildProperties present + version gesetzt → echte Version; BuildProperties absent → `"dev"`; BuildProperties present + version blank → `"dev"`). **Verifikation:** `./gradlew :frontend:spotlessApply :frontend:checkstyleMain :frontend:spotbugsMain :frontend:test` BUILD SUCCESSFUL; `bootBuildInfo` mit `--rerun-tasks` produziert `build.version=0.2.3-dirty` aus dem aktuellen Worktree (Tag `v0.2.3` + uncommittete Aenderungen), mit `-PappVersion=v1.2.3-test` produziert `build.version=1.2.3-test`.

- **CI – `docker/setup-qemu-action` auf v4.0.0 angehoben, Node-20-Deprecation-Warning auf `release-images.yml` weg.** GitHub Actions warnt seit dem [2025-09-19-Changelog-Post](https://github.blog/changelog/2025-09-19-deprecation-of-node-20-on-github-actions-runners/) auf jedem Run, dass Node-20-JavaScript-Actions ab 2026-06-02 zwangsweise auf Node 24 umgestellt und ab 2026-09-16 vom Runner entfernt werden. Im aktuellen Workflow [`release-images.yml`](.github/workflows/release-images.yml) wurde `docker/setup-qemu-action` noch auf den v3-pinned-SHA `c7c5346…` (Node 20 runtime) referenziert; alle anderen Docker-Actions im selben Workflow (`setup-buildx-action@v4.0.0`, `login-action@v4.1.0`, `metadata-action@v6.0.0`, `build-push-action@v7.1.0`) sind bereits auf Node-24-Versionen. **Fix:** [`release-images.yml`](.github/workflows/release-images.yml) zieht `docker/setup-qemu-action` auf den v4.0.0-pinned-SHA `ce360397dd3f832beb865e1373c09c0e9f86d70a` ([Release-Notes](https://github.com/docker/setup-qemu-action/releases/tag/v4.0.0)): "Node 24 as default runtime". Die SHA-Pinning-Konvention des Repos (`uses: org/action@<sha> # vX.Y.Z`) wird beibehalten — Tags sind veraenderbar, SHAs nicht; der Kommentar daneben dokumentiert die menschen-lesbare Version. Kein Verhaltens-Unterschied auf dem aktuellen Single-Plattform-Build (`platforms: linux/amd64`), QEMU bleibt der gleiche Cross-Build-Vorbereiter — nur die Wrapping-Action laeuft jetzt auf Node 24. Die anderen sechs Workflows (`ci.yml`, `codeql.yml`, `dependency-check.yml`, `promote.yml`, `refresh-versions.yml`) sind nicht betroffen (kein `setup-qemu-action`-Aufruf).

### Fixed

- **Job-Order-Auftraggeber-Staffel wurde immer als die eigene Staffel gespeichert, wenn `name != shorthand`.** Beim Anlegen / Bearbeiten eines Auftrags schickte das Squadron-Dropdown in [`orders-create.html`](frontend/src/main/resources/templates/orders-create.html) und im Edit-Modal von [`orders-detail.html`](frontend/src/main/resources/templates/orders-detail.html) den Squadron-**Namen** (`th:value="${s.name}"`) als Form-Wert, der Backend-Resolver [`JobOrderService.resolveRequestingSquadron`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/JobOrderService.java) loest den Legacy-`squadron`-String aber per `squadronRepository.findByShorthand(...)` auf. Fuer jede Staffel mit `name != shorthand` (z. B. IRIDIUM: `name="IRIDIUM"`, `shorthand="IRI"` aus [`V80__seed_iridium_squadron.sql`](backend/src/main/resources/db/migration/V80__seed_iridium_squadron.sql)) lieferte der Lookup `Optional.empty()` und der Resolver fiel still auf den `creatingFallback` (= die eigene Staffel des Erstellers) zurueck — Folge: ein IRI-Mitglied, das einen Auftrag fuer Cerberus anlegt, persistierte den Auftrag mit `requesting_squadron_id = IRIDIUM_ID` und sah in der Listenansicht (`/orders`) sowie in der Detailkachel "Auftraggeber-Staffel" durchgaengig den IRI-Badge, obwohl er Cerberus ausgewaehlt hatte. Das Edit-Modal zeigte den korrekt gemeinten Wert nur in den Faellen, in denen die Staffel zufaellig `name == shorthand` hat (Speicher-Pfad erfolgreich, `order.squadron()` traegt den gewuenschten Shorthand und matched die `value="${s.name}"`-Option). **Fix:** Beide Dropdowns geben jetzt `th:value="${s.shorthand}"` aus (Display-Text bleibt der lange `${s.name}`, damit Nutzer weiterhin "Cerberus" / "IRIDIUM" sehen); damit decken sich Form-Wert, Backend-Lookup-Schluessel und die `*{squadron}`-Form-Bindung (genaehrt aus [`JobOrderMapper.toDto`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/mapper/JobOrderMapper.java) via `source = "requestingSquadron.shorthand"`) auf denselben Schluesselraum. Der Handover-`recipientSquadron`-Dropdown auf der Detailseite bleibt absichtlich auf `${s.name}` — das Feld ist Free-Text (kein FK-Lookup, keine semantische Aufloesung im Backend), eine Umstellung wuerde nur die historische Anzeige veraendern. Bestehende fehlerhaft gespeicherte Auftraege (mit `requesting_squadron_id` faelschlich auf die Ersteller-Staffel gesetzt) muessen einmalig manuell korrigiert werden — entweder per Edit-Modal (mit dem Fix waehlt der Dropdown jetzt korrekt aus) oder per gezieltem `UPDATE job_order SET requesting_squadron_id = ... WHERE id IN (...);`.

- **`GET /api/v1/operations` lieferte HTTP 500 (`LazyInitializationException` auf `Squadron`-Proxy) — Operations-Menue und Mission-Edit-Dialog konnten keine Operationen mehr listen.** Beim Aufruf des Operations-Endpoints (sowohl `/operations`-Seite als auch der `size=1000`-Dropdown auf dem Einsatz-Detail) warf das Backend reproduzierbar `org.hibernate.LazyInitializationException: Could not initialize proxy [Squadron#…] - no session` aus `OperationMapperImpl.toDto` → `SquadronMapperImpl.toReferenceDto` → `squadron.getName()`. **Ursache:** `OperationDto` traegt seit Phase 6 ein `SquadronReferenceDto owningSquadron`-Feld, und das `Operation`-Entity haelt `owningSquadron` mit `FetchType.LAZY`. Der Service-Aufruf `operationService.getAllOperations(...)` lief in seinem eigenen `@Transactional(readOnly = true)`-Kontext, gab die `Page<Operation>` zurueck, und erst danach mappte der Controller mit `.map(operationMapper::toDto)` — zu diesem Zeitpunkt war die Hibernate-Session bereits geschlossen, der Lazy-Proxy konnte nicht mehr initialisiert werden und das `getName()`-Trampolin im MapStruct-generierten `SquadronMapperImpl` knallte. OSIV ist global ausgeschaltet (`spring.jpa.open-in-view: false` in `application.yml` und `application-test.yml`), deshalb deckt der Servlet-Context den Lazy-Zugriff nicht ab. Andere strict-staffel-Aggregat-Controller mit identischer Mapping-Konstellation (Mission, RefineryOrder, Ship) lagen schon laenger auf der kanonischen Loesung — Klassen-Level `@Transactional` plus Methoden-Level `@Transactional(readOnly = true)` auf den GETs — `OperationController` war als einziger ohne Wrapper geblieben. **Fix:** [`OperationController`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/controller/OperationController.java) bekommt Klassen-Level `@Transactional` (analog `MissionController` und `RefineryOrderController`) und auf den vier GET-Methoden (`getAllOperations`, `getOperationById`, `getOperationFinances`, `getOperationPayouts`) jeweils `@Transactional(readOnly = true)`. Damit haengt die persistence context an der HTTP-Request-Boundary, der Lazy-`owningSquadron`-Proxy wird beim Mapping innerhalb derselben Transaktion initialisiert. Eine Alternative ueber `@EntityGraph(attributePaths = {"owningSquadron"})` auf `OperationRepository.findAllScoped` haette das Listen-Endpoint reparieren koennen, aber `getOperationById` und die Write-Endpoints (`create`/`update`, beide bauen ihre DTO-Antwort nach Service-Return ueber den gleichen Mapper) waeren weiterhin anfaellig — die Controller-Transaktion deckt alle Pfade einheitlich ab.

## [v0.2.3](https://github.com/krt-profit/basetool/releases/tag/v0.2.3) - 2026-05-19

### Changed

- **`CONTRIBUTING.md` komplett neu geschrieben — stale Boilerplate raus, projektspezifische Realitaet rein.** Die alte Datei war seit dem GitLab-Zeitalter nicht angefasst worden und referenzierte einen nicht existenten `develop`-Branch, ein "[FEATURE - X]"-PR-Title-Schema das gar nicht mehr verwendet wird (Conventional Commits sind seit Monaten Standard, siehe `git log --oneline`), GitLab-spezifische Begriffe ("MR") und einen GitLab-Workflow, plus zwei volle Seiten Generic-OSS-Boilerplate fuer Bug-Reports und Feature-Requests die jetzt durch die `.github/ISSUE_TEMPLATE/*.yml`-Forms strukturell abgedeckt sind. Neue Struktur in [`CONTRIBUTING.md`](CONTRIBUTING.md): (1) **TL;DR-Block oben** mit den fuenf wichtigsten Regeln (Security via SECURITY.md, Fragen in Discussions, Bugs/Features via Issue-Templates, PRs gegen `main` mit Conventional Commits, jeder PR aktualisiert `CHANGELOG.md`). (2) **Projekt-Dokumentations-Map** als Tabelle mit Links auf alle Quellen-of-Truth (README, CLAUDE.md, SECURITY.md, ROLES_AND_PERMISSIONS, `db/migration/README.md`, Styleguide, PR-Template, `docs/deployment.md`, LICENSE) — damit ein neuer Contributor in unter einer Minute findet wohin er muss. (3) **Pull-Request-Workflow** explizit gegen `main` (nicht `develop`), Fork-or-Branch, Draft+WIP-Convention, Squash-Merge-Hinweis, CI-Workflow-Querverweis. (4) **Conventional-Commits-Sektion** mit dem realen Type/Scope-Format aus der Commit-Historie, sechs konkreten Beispielen direkt aus `git log --oneline` (`feat(multi-tenant):`, `fix(db/V80):`, `chore(multi-tenant):` Phase 7, `fix(frontend/orders):`, `refactor(logging):`, `docs(github):`), und einem expliziten Hinweis dass die alte `[FEATURE - X]`-Schreibweise out ist. (5) **Style-Guides** als zusammenfassende Sektion mit den gate-enforced Regeln (Google Java Style + Spotless + Checkstyle `maxWarnings=0` + SpotBugs + Lombok-first + Constructor-Injection + Records-fuer-DTOs + `@Slf4j` + Javadoc-Hard-Gate via `MissingJavadocType`/`MissingJavadocMethod`), den i18n-Regeln (Umlaute `\uXXXX` in `.properties`, literal UTF-8 in Markdown, jeder String aus `messages*.properties`), den Frontend-Regeln (Brand `#E77E23`, Ethnocentric/Lato, vier Device-Klassen, keine `confirm()`/`alert()`, DOM-`data-version`-Propagation) und der Git/GitHub-English-Only-Regel. (6) **Architektur-Invarianten-Sektion** als Lesepflicht **vor** dem Anpacken sensibler Pfade — Security-Modell + ArchUnit-Gates, Multi-User-Isolation via JWT-`sub`, Multi-Squadron-Tenancy mit Strict-Staffel- vs Cross-Staffel-Aggregaten und `SquadronScopeService`-Hinweis, die Optimistic-Locking-/`*WithinTransaction`-Pattern-Falle, API-Konventionen (Versionierung `/api/v1`, RFC 7807, `Pageable`+`PageResponse`-Sort-Whitelist, UTC-Timestamps, SpringDoc + `openapi.json`-Sync), Datenbank (Flyway-Only, `ddl-auto=validate`, Two-Phase-Drop-Regel), Frontend-Resilience (Resilience4j-Pflicht), Logging (MDC `correlationId`+`userId`+`squadronId`, keine Tokens/Namen/Mails), Tests (`*Test`-Suffix, Given/When/Then, jede Feature mit Tests, **keine Prod-Credentials in Tests/Stacks** mit Pointer auf README §4.4 + CLAUDE.md). (7) **Before-You-Push-Checklist** als kondensierte Variante der PR-Template-Checkliste. (8) **License-Sektion** mit GPL-3.0-Hinweis und der bewussten Entscheidung gegen CLA/DCO. Querbezuege: `.github/PULL_REQUEST_TEMPLATE.md` (eigentliche PR-Checkliste, von hier aus verlinkt statt dupliziert), `.github/SECURITY.md` (Vulnerability-Reporting), `.github/ISSUE_TEMPLATE/*.yml` (Bug/Feature/Task-Forms), `CLAUDE.md` (Architektur-Invarianten in Tiefe), `README.md` §4 (lokale Dev-/Test-Stack-Procedure). Keine inhaltlichen Regeln neu erfunden — die Datei kuratiert nur die existierenden Regeln aus CLAUDE.md, README.md, SECURITY.md und PR-Template fuer den Contributor-Einstieg.

- **Multi-Squadron-Umbau – Phase 7 Teil 2: NOT-NULL-Tightening auf den acht Squadron-FK-Spalten + Fixture-Cleanup in 15 Integration-Tests.** Zweite Etappe des Phase-7-Plans (MULTI_SQUADRON_PLAN.md §10): nachdem Phase 6 (V80–V86) in Prod bewiesen hat, dass jeder Create-Pfad die neuen Squadron-Spalten konsistent stempelt, darf der DB-Layer das `NULL` nicht mehr tolerieren. **Slot-Reshuffle (V87 -> V89):** Die Migration war urspruenglich als V87 geplant und sollte gemeinsam mit V88 (Phase 7 Teil 1, `job_order.squadron`-VARCHAR-Stopwrite) auf ein Release abgesetzt werden. V88 ging via PR #132 (Commit a00e5c8) zuerst in Prod, sodass `flyway_schema_history.latest_applied = 88` ist. Mit `out-of-order=false` (Projekt-Default) wuerde Flyway eine nachtraeglich aufgesammelte V87 mit `version <= latest_applied` ablehnen, also wird der Tightening-Slot um zwei Plaetze nach oben gezogen — V89. Der Folge-`DROP COLUMN` fuer das Legacy-`job_order.squadron`-Feld (Two-Phase-Drop, urspruenglich V89) shiftet entsprechend auf V90. **Datenmodell:** Neue Migration [`V89__tighten_squadron_id_not_null.sql`](backend/src/main/resources/db/migration/V89__tighten_squadron_id_not_null.sql) setzt `SET NOT NULL` auf alle acht Squadron-FK-Spalten — fuenf Aggregat-Roots (`mission.owning_squadron_id`, `operation.owning_squadron_id`, `ship.owning_squadron_id`, `inventory_item.owning_squadron_id`, `refinery_order.owning_squadron_id`), die Promotion-Topic-Scope (`promotion_topic.owning_squadron_id`) und JobOrders zwei Squadron-Slots (`creating_squadron_id`, `requesting_squadron_id`). Defensive idempotente Backfill-Klauseln (`WHERE col IS NULL` → IRIDIUM-UUID) laufen vor jedem `ALTER`, fangen jeden Phase-3-Race-Condition-Straggler ab und sind auf jeder gut gepflegten Prod-DB ein No-Op. **Out-of-scope** ist explizit `app_user.squadron_id` (Admins / Guests muessen `NULL` bleiben koennen, damit `SquadronScopeService.currentSquadron()` fuer sie "kein konkreter Scope" zurueckgibt) und `job_order.squadron` (die V88-aus-Phase-7-Teil-1 hat den NOT-NULL-Constraint dort schon geloockert, V90 droppt die Spalte ganz). **Entity-Layer:** Acht JPA-`@JoinColumn`-Annotations auf [`Mission`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/model/Mission.java), [`Operation`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/model/Operation.java), [`Ship`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/model/Ship.java), [`InventoryItem`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/model/InventoryItem.java), [`RefineryOrder`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/model/RefineryOrder.java), [`PromotionTopic`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/model/PromotionTopic.java) und [`JobOrder`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/model/JobOrder.java) bekommen `nullable = false` — Hibernate `ddl-auto=validate` haelt damit Entity-Modell und DB-Schema explizit in Sync. **Production-Bug-Fix als Beifang:** V89 deckt eine seit Phase 6 latente Bug-Klasse in [`HangarImportService.importFleetview`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/HangarImportService.java) auf — der Loop-Body `Ship ship = new Ship(); ship.setOwner(user); …; shipRepository.save(ship)` setzte nie `ship.setOwningSquadron(user.getSquadron())`, was im Phase-6-Zustand (nullable column) latent durchlief aber unter V89 sofort eine `null value … violates not-null constraint`-Exception wirft. Fix: ein zusaetzliches `ship.setOwningSquadron(user.getSquadron())` an der Importer-Stelle, analog zum bereits korrekten `HangarService.createShip`-Pfad. **Test-Fixture-Aufraeumarbeit (das eigentliche Volumen dieses PRs):** 14 Integration-/Service-Tests konstruierten Aggregate (Mission / Ship / InventoryItem) direkt via Repository, ohne die Service-Schicht zu durchlaufen, die sonst das Stamping uebernimmt. Jeder dieser Tests bekommt jetzt einen `@Autowired private SquadronRepository squadronRepository` + `private Squadron iridium;`-Feld und ein `@BeforeEach`-Snippet `iridium = squadronRepository.findById(Squadron.IRIDIUM_ID).orElseThrow();` — die existierenden `new Mission()` / `new Ship()` / `new InventoryItem()`-Stellen werden mit `entity.setOwningSquadron(iridium)` direkt nach der Konstruktion ergaenzt, und Test-User die ueber die API ein Aggregat erzeugen bekommen ein `user.setSquadron(iridium)` vor dem `userRepository.save(user)` damit der Service-Pfad eine valide Source fuer den Stamping-Trail findet. Betroffene Files: `ConcurrencyTest`, `FeatureExpansionTest`, `HangarImportIntegrationTest`, `HangarIntegrationTest`, `MissionAccessControlTest`, `MissionParticipantSquadronTest`, `MissionUnitCrewTest`, `MissionValidationTest`, `OptimisticLockingTest`, `RefineryOrderTest`, `security.MissionManagerRoleTest`, drei Handover-Integration-Tests (`JobOrderHandoverCompletion-` / `…Fragmented-` / `…Mixed-IntegrationTest`) und `SquadronServiceTest.deleteSquadron_ShouldSetInactive`. Die drei Handover-Tests bekommen statt eines Klassenfeldes einen inline-Lookup `squadronRepository.findById(Squadron.IRIDIUM_ID).orElseThrow()` weil ihre Test-Methoden den Fixture-Code direkt im Test-Body bauen (keine `@BeforeEach`-Initialisierung). **Vermeintliche Flakes — als echtes Test-Contract-Issue identifiziert und gefixt:** `security.MissionManagerRoleTest.missionManagerShouldBeAbleToUpdateMission` und `.missionDtoShouldHaveCanEditTrueForMissionManager` waren auf den ersten Blick als Suite-Cross-Pollution durch JWTs mit Standard-`sub="user"` eingestuft, sind tatsaechlich aber ein V89-getriebener Vertragswandel: Pre-V89 erlaubte das nullable `owning_squadron_id` einem `canManageMission(null-owning)`-Aufruf den Squadron-Scope-Check zu ueberspringen, womit der reine Role-Check ausreichte; Post-V89 hat jede Mission eine konkrete Squadron und der Scope-Check feuert unbedingt — ein `jwt().authorities(ROLE_MISSION_MANAGER)` ohne Subject scheitert dann am `UserService.parseSubject(...)`-UUID-Parse. Fix: beide Tests setzen jetzt zusaetzlich `.jwt(builder -> builder.subject(managerMember.getId().toString()))`, so dass das Subject auf einen IRIDIUM-User aufloest. **Verifikation:** `./gradlew check` BUILD SUCCESSFUL fuer alle gefixten Tests.

- **Multi-Squadron-Umbau – Phase 7 Teil 1: stop-write der Legacy-`job_order.squadron`-VARCHAR-Spalte.** Erster Schritt des im Phase-7-Plan reservierten zweiphasigen Drop-Pfades (siehe MULTI_SQUADRON_PLAN.md §10 + `db/migration/README.md`): das Legacy-Free-Text-Feld auf `job_order` wird im Code nicht mehr gelesen oder geschrieben, Hibernate ignoriert die DB-Spalte ab diesem Release, und der NOT-NULL-Constraint wird gelockert damit eine alte Replica oder ein Out-of-Process-INSERT die Spalte weglassen kann ohne den Schreibpfad zu blockieren. Die Spalte selbst bleibt eine Release-Iteration laenger auf der Disk — der finale `DROP COLUMN` landet in `V90` im uebernaechsten Release (Plan §10, Two-Phase-Drop-Regel; ehemals als V89 reserviert, durch das post-Deploy-Reshuffle der Phase-7-Teil-2-NOT-NULL-Migration auf V90 verschoben). **Datenmodell:** Neue Migration [`V88__stop_writing_job_order_squadron_string.sql`](backend/src/main/resources/db/migration/V88__stop_writing_job_order_squadron_string.sql) macht ein `ALTER TABLE job_order ALTER COLUMN squadron DROP NOT NULL`; keine Index-Drops noetig (pg_indexes-Inspektion bestaetigt, dass die Spalte in dev/test und prod nie eigenindexiert war, und das Grep gegen alle Vorgaenger-Migrationen findet kein einziges `CREATE INDEX` auf `job_order.squadron`). [`JobOrder`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/model/JobOrder.java) Entity-seitig: der `@Column private String squadron`-Slot und sein Lombok-Builder-Setter sind komplett entfernt; Hibernate `ddl-auto=validate` ignoriert die orphan DB-Spalte (validate prueft nur "Entity-referenzierte Spalten existieren", nicht den umgekehrten Weg). **Service-Pfade:** [`JobOrderService.createJobOrder`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/JobOrderService.java) und `updateJobOrder` setzen den Wert nicht mehr (zwei `jobOrder.setSquadron(requestingSquadron.getShorthand())` weg); die `resolveRequestingSquadron`-Hilfe akzeptiert weiterhin den optionalen Free-Text-Shorthand auf der Wire-Shape fuer Backwards-Compat — der Resolver mappt den String auf das `Squadron`-Entity-FK. **DTO-Layer:** Die `String squadron`-Slots auf [`JobOrderDto`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/model/dto/JobOrderDto.java), [`JobOrderReferenceDto`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/model/dto/JobOrderReferenceDto.java) und [`CreateJobOrderDto`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/model/dto/CreateJobOrderDto.java) bleiben **absichtlich** noch auf der Wire-Shape, damit alte Browser-Tabs / unmigriete Scripts keinen 4xx auf dem Field bekommen. [`JobOrderMapper.toDto`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/mapper/JobOrderMapper.java) bekommt einen expliziten `@Mapping(target = "squadron", source = "requestingSquadron.shorthand")` so dass das DTO-Feld weiterhin den menschen-lesbaren Shorthand traegt, jetzt aber **aus der strukturierten FK abgeleitet** statt aus dem Legacy-Mirror. `JobOrderService.findAllActiveReference` macht das gleiche manuell beim Bau des `JobOrderReferenceDto`. V90 wird das DTO-Field final entfernen — bis dahin ist der Vertrag stabil. **Phase 7 Teil 2 (V89 NOT-NULL-Tightening auf `owning_squadron_id` der fuenf Aggregate + `promotion_topic` + `creating_squadron_id`/`requesting_squadron_id` auf `job_order`; urspruenglich als V87 geplant, nach dem Solo-Deploy von V88 auf V89 hochgezogen) ist explizit aus diesem PR herausgesplittet** in ein eigenes Follow-Up — das Tightening benoetigt vor dem Merge zusaetzliche Test-Fixture-Aufraeumarbeit (15 Integration-Tests konstruieren Mission/Ship/InventoryItem-Aggregate direkt via Repository ohne Squadron-Stamp und schlagen auf NOT NULL fehl). **Tests:** Vier Mockito-Service-Tests (`JobOrderServiceTest` zwei Update-Asserts, `JobOrderMapperTest` drei Fixture-Stellen, `JobOrderServiceAssigneeAndListTest` + `JobOrderServicePriorityAndStatusTest` lesen `getRequestingSquadron().getShorthand()` statt `getSquadron()`) und drei Handover-Integration-Tests (`JobOrderHandoverCompletionIntegrationTest` / `JobOrderHandoverFragmentedStockIntegrationTest` / `JobOrderHandoverMixedFlowIntegrationTest`) ziehen auf die neue Builder-Signatur nach — die Integration-Tests holen sich IRIDIUM jetzt explizit ueber `squadronRepository.findById(Squadron.IRIDIUM_ID)` und stempeln `creatingSquadron` + `requestingSquadron` direkt am `JobOrder`. **Verifikation:** `./gradlew check` BUILD SUCCESSFUL (Backend + Frontend + Checkstyle + SpotBugs + Spotless gruen).

### Security

- **Multi-Squadron-Umbau – Beförderungssystem pro Staffel an-/abschaltbar (ohne Datenverlust).** Admins koennen jetzt pro Staffel das gesamte Befoerderungssystem ein- bzw. ausschalten, ohne dass Themenbereiche / Bewertungen / Rang-Voraussetzungen / Mitgliederbewertungen verloren gehen — die Datensaetze bleiben in der DB, werden bei deaktiviertem Flag nur fuer Nicht-Admin-Mitglieder der jeweiligen Staffel ausgeblendet und sind nach dem Wiedereinschalten exakt im vorherigen Zustand sichtbar/editierbar. Admins behalten unabhaengig vom Flag den vollen Zugriff, damit eine versehentlich gesperrte Staffel ueber das Toggle wieder erreichbar gemacht werden kann. **Datenmodell:** Neue Migration [`V86__add_is_promotion_enabled_to_squadron.sql`](backend/src/main/resources/db/migration/V86__add_is_promotion_enabled_to_squadron.sql) fuegt `squadron.is_promotion_enabled BOOLEAN NOT NULL DEFAULT TRUE` hinzu — Bestands-Staffeln behalten ihre Sichtbarkeit, Admins muessen das Feature explizit pro Staffel deaktivieren. V86 belegt den naechsten freien Slot vor der reservierten Phase-7-Kette (urspruenglich als `V87`/`V88`/`V89` geplant; durch den Solo-Deploy von Phase 7 Teil 1 auf die finale Form `V88` stop-write Legacy-VARCHAR / `V89` NOT-NULL-Tightening / `V90` DROP-COLUMN verschoben; siehe MULTI_SQUADRON_PLAN.md §10) — gleicher Renumbering-Mechanismus wie bei V85, damit Flyway's `out-of-order=false`-Default nicht verletzt wird. [`Squadron`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/model/Squadron.java) traegt das `isPromotionEnabled`-Feld; [`SquadronDto`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/model/dto/SquadronDto.java) (Backend + [Frontend-Mirror](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/model/dto/SquadronDto.java)) gewinnt einen zusaetzlichen `Boolean isPromotionEnabled`-Slot zwischen `active` und `version`; [`SquadronMapper`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/mapper/SquadronMapper.java) mappt das Feld nur im `toDto`-Pfad — `toEntity` ignoriert es bewusst, damit eine Routine-Aktualisierung (Name/Shorthand/Description) den Toggle nicht versehentlich umstellt. **Backend-Gate ueber `SquadronScopeService` (single primitive, gleicher Hebel von allen Service-Call-Sites):** `SquadronScopeService` bekommt zwei neue Methoden — `isPromotionFeatureEnabledForCurrentScope()` (Admin → `true`, sonst `Squadron.isPromotionEnabled` der eigenen Staffel; Anonymous/Member-ohne-Staffel → permissiv `true`) und `assertPromotionFeatureEnabled()` (wirft `AccessDeniedException` → HTTP 403). Reads in [`PromotionTopicService`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/PromotionTopicService.java), [`PromotionCategoryService`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/PromotionCategoryService.java), [`PromotionLevelContentService`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/PromotionLevelContentService.java), [`RankRequirementService`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/RankRequirementService.java), [`MemberEvaluationService`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/MemberEvaluationService.java), [`PromotionEligibilityService`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/PromotionEligibilityService.java) short-circuiten zu `Page.empty(pageable)` / `List.of()` / leerer `PromotionEligibilityResponse`, wenn der Caller-Scope das Flag OFF traegt — kein Repository-Hit, kein Mapper-Aufwand. Writes (`create` / `update` / `delete` auf den fuenf Services + `upsert` auf MemberEvaluation) rufen `assertPromotionFeatureEnabled()` ganz am Anfang auf, vor jedem `findById` oder `save`. **Toggle-Endpoint (ADMIN-only):** Neue Methode [`SquadronService.setPromotionEnabled(UUID, boolean)`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/SquadronService.java) flippt **ausschliesslich** das eine Feld (Name/Shorthand/Description bleiben intakt) und evictet den Squadron-Cache; [`SquadronController.setPromotionEnabled`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/controller/SquadronController.java) exposed das als `PATCH /api/v1/squadrons/{id}/promotion-enabled` mit Body `{"enabled":true|false}` und `@PreAuthorize("hasRole('ADMIN')")`. Eine dedicated Endpoint statt das in den regulaeren `PUT`-Update-Pfad zu mergen, damit eine versehentliche Beschreibungs-Aenderung den Toggle nicht mitnimmt — und damit der Access-Log eindeutig "Admin X hat Befoerderung fuer Staffel Y umgeschaltet" attributieren kann. **Frontend (Sidebar + Page-Gates + Admin-UI):** [`SquadronContextAdvice`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/config/SquadronContextAdvice.java) gewinnt einen zusaetzlichen `@ModelAttribute("promotionFeatureEnabled")` (Admin → `true`, sonst `activeSquadron.isPromotionEnabled` mit permissivem `true`-Default fuer Anonymous-Caller). [`fragments/sidebar.html`](frontend/src/main/resources/templates/fragments/sidebar.html) wickelt die gesamte `Beförderung`-Sektion in `th:if="${promotionFeatureEnabled}"` — fuer Officer / Member einer deaktivierten Staffel verschwindet der Menue-Block komplett; Admins sehen die Sektion immer. [`PromotionPageController`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/controller/PromotionPageController.java) verarbeitet das gleiche Model-Attribut als `@ModelAttribute`-Parameter und wirft `AccessDeniedException` (→ HTTP 403) ueber alle fuenf `@GetMapping`s (`/overview`, `/my-evaluations`, `/manage`, `/admin/topics`, `/admin/rank-requirements`), so dass ein Officer mit gespeicherter URL / staltem Tab nicht durch die Sidebar-Verstecken rutscht. Neuer [`SquadronAdminProxyController`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/controller/SquadronAdminProxyController.java) reicht `PATCH /api/proxy/squadrons/{id}/promotion-enabled` (Admin-only) ans Backend weiter; die Toggle-Section auf [`/admin/settings`](frontend/src/main/resources/templates/admin-settings.html) zeigt jede Staffel als Zeile mit Checkbox und schickt pro Klick eine einzelne PATCH-Request (kein Page-Reload, optimistische UI-Updates auf das `aktiv`/`inaktiv`-Label, Rollback + Error-Toast bei 4xx/5xx — alle Strings sind in `messages*.properties` lokalisiert, Umlaute als `\uXXXX`-Escapes gemaess CLAUDE.md). **Tests:** [`PromotionFeatureFlagServiceGateTest`](backend/src/test/java/de/greluc/krt/profit/basetool/backend/service/PromotionFeatureFlagServiceGateTest.java) pinnt das gate-Verhalten Ende-zu-Ende (Admin-Override, ON/OFF/Fehlende-Squadron-Pfade, Read-Short-Circuit, Write-throws-403, Toggle-flippt-nur-Flag, Toggle-rejects-unknown-id, Toggle-propagates-optimistic-lock); [`PromotionFeatureFlagPageGateTest`](frontend/src/test/java/de/greluc/krt/profit/basetool/frontend/controller/PromotionFeatureFlagPageGateTest.java) macht den frontend-side Page-Gate (alle fuenf `/promotion/...`-URLs → 403 fuer Officer mit Flag OFF). Existierende 6 Promotion-Service-Tests bekommen ein neues `@Mock SquadronScopeService` + `@BeforeEach`-`lenient()`-Stub, damit der Default-On-Pfad gegen das neue Gate weiter durchlaeuft; die DTO-Konstruktor-Sites in `SquadronControllerTest`, `EntityMappersTest`, `SquadronServiceTest`, `AdminMissionDataPageController` und `PromotionAdminRankRequirementsPageMvcTest` sind auf die 7-Args-Signatur (`isPromotionEnabled` zwischen `active` und `version`) nachgezogen. **Verifikation:** `./gradlew check` BUILD SUCCESSFUL (Backend + Frontend Tests + Checkstyle + SpotBugs + Spotless gruen, 22 Tasks).

## [v0.2.1](https://github.com/krt-profit/basetool/releases/tag/v0.2.1) - 2026-05-19

### Fixed

- **Flyway-Migration `V80__seed_iridium_squadron.sql` — Case 3 (Bestands-IRIDIUM mit nicht-kanonischer UUID) brach in Prod mit FK-Violation auf `mission_participant_squadron_id_fkey` ab.** Beim Deploy von PR #129 gegen die existierende Prod-DB schlug V80 fehl: der alte `DataInitializer.createSquadronIfNotFound("IRIDIUM", "IRI", …)` hatte IRIDIUM mit einer per `@GeneratedValue(UUID)` gewürfelten UUID angelegt, V80 trifft damit den ELSIF-Pfad (`existing_iri_id <> canonical_iri_id`). Der dortige `UPDATE mission_participant SET squadron_id = canonical_iri_id WHERE squadron_id = existing_iri_id` triggert den End-of-Statement-Check des in V1 inline deklarierten FK (`squadron_id UUID REFERENCES squadron(id)` ohne `DEFERRABLE` → Postgres-Default `NOT DEFERRABLE INITIALLY IMMEDIATE NO ACTION`): die kanonische UUID existiert in `squadron` zu diesem Zeitpunkt noch nicht, also bricht Postgres mit `insert or update on table "mission_participant" violates foreign key constraint "mission_participant_squadron_id_fkey"` ab — und die DO-Block-Transaktion rollt komplett zurück. Die umgekehrte Reihenfolge (`UPDATE squadron` zuerst) kippt symmetrisch: dann referenziert `mission_participant.squadron_id` auf eine nicht mehr existente Parent-Zeile. Auf Dev / frischen Test-DBs (kein IRIDIUM-Eintrag → V80 Case 1 INSERT) ist das nie aufgefallen, weil der ELSIF-Zweig dort nicht durchläuft. **Fix:** [`V80__seed_iridium_squadron.sql`](backend/src/main/resources/db/migration/V80__seed_iridium_squadron.sql) droppt den FK vor dem Swap, führt beide UPDATEs durch und legt den FK direkt wieder an. Der Constraint-Name wird per `SELECT conname FROM pg_constraint WHERE conrelid = 'mission_participant'::regclass AND contype = 'f' AND confrelid = 'squadron'::regclass` aus dem Katalog gelesen statt auf den auto-generierten Namen zu vertrauen, und das `ALTER TABLE DROP CONSTRAINT` per `EXECUTE` + `quote_ident` dynamisch abgesetzt; eine `IF fk_name IS NULL THEN RAISE EXCEPTION`-Sicherung fängt den theoretischen Fall ab, dass die FK manuell gedroppt wurde. Wird der FK neu angelegt, taggt das `ADD CONSTRAINT` ihn fest auf den konventionellen Postgres-Namen `mission_participant_squadron_id_fkey`, so dass die Migrations-Historie ab V80 deterministisch ist. **Migrations-Immutability:** V80 ist nirgendwo erfolgreich durchgelaufen, wo er den Case-3-Pfad genommen hat — der Rollback durch die FK-Violation belässt Flyway in einem sauberen Zustand ohne `flyway_schema_history`-Eintrag, ein erneuter Deploy mit der gefixten Datei läuft transparent neu an. **Dev/Staging-Caveat:** in Umgebungen, in denen V80 den Case-1-Pfad (Fresh-DB-INSERT) genommen hat und damit erfolgreich war, schlägt nach dem Patch die Flyway-Checksum-Validierung an. Cleanup-Snippet für solche Umgebungen: `DELETE FROM flyway_schema_history WHERE version = '80';` (V80 mit Case 2 reapplied = no-op, weil der kanonische IRIDIUM-Eintrag bereits existiert).

## [v0.2.0](https://github.com/krt-profit/basetool/releases/tag/v0.2.0) - 2026-05-19

### Fixed

- **Promotion-Admin-Seite (`/promotion/admin/rank-requirements`): Klick auf „NEUE ANFORDERUNG" (und auf alle anderen Buttons der Seite) hat fuer Officer + Admin keinen Effekt mehr — Inline-`<script>`-Block wurde mitten in der Inlining-Phase abgeschnitten und das Event-Wiring nie ausgefuehrt.** Die Seite inlined einen `Map<UUID, List<PromotionCategoryDto>>` per `var AR_CATEGORIES_BY_TOPIC = /*[[${categoriesByTopic}]]*/ {};` ueber den seit Commit 1e79f7b custom registrierten [`JavaTimeAwareJavaScriptSerializer`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/config/ThymeleafJavaScriptSerializerConfig.java). Der Serializer rief `mapper.writeValue(writer, object)` auf — und Jackson 2 hat `JsonGenerator.Feature.AUTO_CLOSE_TARGET` per Default auf `true`, schliesst also den uebergebenen `Writer` nach dem Wert-Schreiben. Da Thymeleaf **einen** Writer ueber den gesamten Template-Render teilt, hat das Schliessen nach dem JSON-Map alles weggeschmissen, was danach kommen sollte: die zwoelf `MSG_*`-Inline-Konstanten, die `openCreateModal`/`submitCreate`/`confirmAndDelete`/`applyFilter`-Helfer, der gesamte `DOMContentLoaded`-Listener mit den `window.krtEvents.on('click', 'ar-open-create', …)`-Registrierungen, das `</script>`-Closing-Tag und der Rest der Seite (`</body></html>`). Der Browser empfing 200 OK mit einem ungeschlossenen `<script>`-Block; das HTML-Parser-Recovery hat den verbleibenden Teil als Script-Inhalt geschluckt, sodass die DOM-Struktur halbwegs erkannt wurde — aber keine einzige Event-Bindung der Seite registriert worden ist. Der „NEUE ANFORDERUNG"-Button (und alle anderen `data-trigger`-Hooks der Seite) lief damit ins Leere, ohne Konsole-Fehler. **Fix:** [`ThymeleafJavaScriptSerializerConfig.JavaTimeAwareJavaScriptSerializer`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/config/ThymeleafJavaScriptSerializerConfig.java) ruft jetzt `m.getFactory().disable(JsonGenerator.Feature.AUTO_CLOSE_TARGET)` auf dem internen Mapper auf, damit Jackson den Thymeleaf-Writer in Ruhe laesst und das Template weiter rendern kann. Inline-Kommentar erklaert die Konsequenz, damit der Default in Zukunft nicht versehentlich re-enabled wird. **Test:** [`PromotionAdminRankRequirementsPageMvcTest`](frontend/src/test/java/de/greluc/krt/profit/basetool/frontend/controller/PromotionAdminRankRequirementsPageMvcTest.java) rendert die Seite als OFFICER und behauptet drei Strings im Body: (1) der `data-trigger="ar-open-create"`-Button, (2) der gerenderte Map-Inline (`var AR_CATEGORIES_BY_TOPIC = {"<topicId>":[…]`) und — entscheidend — (3) die `window.krtEvents.on('click', 'ar-open-create', …)`-Wiring-Zeile am Boden des Scripts. Letztere Assertion ist der eigentliche Regressions-Anker: wenn ein zukuenftiger Serializer-Wechsel den Writer wieder schliesst, ist diese Zeile sofort weg. Komplette `./gradlew :frontend:test`-Suite gruen, Checkstyle/SpotBugs/Spotless ebenso. **Lesson learned (Reviewer-Memo):** Wer Thymeleaf's `IStandardJavaScriptSerializer` ueberschreibt und intern Jackson benutzt, muss `AUTO_CLOSE_TARGET` an der Factory disablen — die Thymeleaf-Stock-Implementierung tut das implizit, weil sie ueber `JsonGenerator` mit anderer Default-Konfiguration laeuft.

### Security

- **Multi-Squadron-Umbau – Promotion-System staffel-spezifisch + Officer-Zugriff.** Beim Live-Testen festgestellt, dass die drei Verwaltungsseiten "Bewertungsverwaltung" (`/promotion/manage`), "Themenbereiche" (`/promotion/admin/topics`) und "Rangvoraussetzungen" (`/promotion/admin/rank-requirements`) im Phase-4-Lockdown auf `hasRole('ADMIN')` lagen, in einem Multi-Squadron-Setup aber pro Staffel von ihren Offizieren gepflegt werden muessen. **Datenmodell:** Neue Migration [`V85__add_owning_squadron_to_promotion_topic.sql`](backend/src/main/resources/db/migration/V85__add_owning_squadron_to_promotion_topic.sql) fuegt `promotion_topic.owning_squadron_id UUID` (FK → `squadron`, nullable) hinzu und backfillt alle Bestands-Topics auf IRIDIUM. Die V85-Position vor der Phase-7-Cleanup-Kette (`V86`–`V88`, jetzt umnumeriert) respektiert Flyway's strikte aufsteigende Reihenfolge (`out-of-order=false` Default — gleicher Bug-Mechanismus, der schon den V87→V84-Rename ausgeloest hatte). [`PromotionTopic`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/model/PromotionTopic.java) traegt das `owningSquadron`-Feld; Categories / LevelContent / RankRequirement / MemberEvaluation erben den Scope ueber den Topic ohne eigene Spalte (Plan §3.2 "no denormalisation"). [`PromotionTopicResponse`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/model/dto/PromotionTopicResponse.java) gewinnt ein `SquadronReferenceDto owningSquadron`-Feld; `PromotionTopicMapper` mappt es ueber `SquadronMapper`. **Service-Filter und Officer-Gates:** [`PromotionTopicService`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/PromotionTopicService.java) `list`/`listAll` filtern via `SquadronScopeService.currentSquadronId()` (Admin in "Alle Staffeln" sieht alles, sonst nur die eigene Staffel); `get` rejected mit `AccessDeniedException` bei Squadron-Mismatch; `create` auto-stempelt aus `currentSquadron()` und wirft 400 wenn Admin in "Alle Staffeln"-Modus keinen Kontext gewaehlt hat (analog JobOrder-Pattern). [`PromotionCategoryService`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/PromotionCategoryService.java), [`PromotionLevelContentService`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/PromotionLevelContentService.java), [`RankRequirementService`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/RankRequirementService.java) und [`MemberEvaluationService`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/MemberEvaluationService.java) leiten den Scope per `topic.owningSquadron` (bei MemberEvaluation und LevelContent ueber `category.topic`) ab und gaten Officer-Zugriff per `canEditSquadron`. Alle `@PreAuthorize("hasRole('ADMIN')")` auf den Schreib-Methoden von ist `hasAnyRole('ADMIN','OFFICER')` mit anschliessendem squadron-spezifischem Check — der Mein-vorheriger-Fix-2-Lockdown auf MemberEvaluation auf `ADMIN` only wird damit teilweise revidiert: Officer kann jetzt Bewertungen pflegen, aber nur fuer Categories deren Topic seiner eigenen Staffel gehoert. **Frontend:** [`fragments/sidebar.html`](frontend/src/main/resources/templates/fragments/sidebar.html) `sec:authorize` fuer die drei Promotion-Sub-Links von `hasRole('ADMIN')` auf `hasAnyRole('ADMIN','OFFICER')` aufgeweitet; [`PromotionPageController`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/controller/PromotionPageController.java) `@PreAuthorize` ebenso. Die Backend-Filter-Wirkung greift transparent — Officer sieht in den Listen nur die eigene Staffel, der Admin sieht alle oder die ueber den Switcher fokussierte. **Tests:** [`PromotionTopicServiceTest`](backend/src/test/java/de/greluc/krt/profit/basetool/backend/service/PromotionTopicServiceTest.java) und [`PromotionTopicControllerTest`](backend/src/test/java/de/greluc/krt/profit/basetool/backend/controller/PromotionTopicControllerTest.java) auf die neue 8-Arg-Konstruktor-Signatur + `@Mock SquadronScopeService`-Stub umgestellt; bestehende Tests bleiben kompatibel weil das `owningSquadron`-Feld `@Nullable` ist und Mockito-Tests es auf `null` setzen. **Verifikation:** `./gradlew check` BUILD SUCCESSFUL (alle 22 Tasks).

- **Multi-Squadron-Umbau – Squadron-Switcher tatsaechlich funktional (State-Ownership + Serializer-Kompatibilitaet).** Beim Live-Testen aufgefallen, dass der Admin-Switcher zwar einen 302-Redirect zurueckliefert, der Kontext aber sichtbar "Alle Staffeln" blieb. Diagnose: Plan §4.2 hatte "Spring Session (Redis-backed)" gesagt ohne zu spezifizieren auf welchem Modul der State liegt — implementiert war es im Backend-`HttpSession`, das aber pro REST-Call neu erstellt wird, weil das Frontend per `BackendApiClient` nur den OAuth2-Bearer relayt und keine Session-Cookies. Plus ein zweites Modal-Problem auf dem Switcher-Form selbst. **Drei Fixes:** **(1) Unsaved-Changes-Modal:** [`fragments/sidebar.html`](frontend/src/main/resources/templates/fragments/sidebar.html) Switcher-Form bekommt `class="no-track"`, damit `unsaved-changes.js` den `change`-Event auf dem Dropdown nicht als "ungespeicherte Aenderung" markiert (Auto-Submit via `form.submit()` feuert per HTML-Spec **kein** `submit`-Event und setzt `isDirty` daher nicht zurueck — ohne `no-track` triggert jedes Switchen den nativen `beforeunload`-Dialog). **(2) State-Ownership umgekehrt:** Frontend besitzt jetzt den State (eigene Redis-backed Spring Session), Backend ist stateless w.r.t. dieser Praeferenz. [`MeFrontendController.setActiveSquadron`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/controller/MeFrontendController.java) schreibt direkt in die Frontend-Session statt das Backend zu rufen; [`SquadronContextAdvice`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/config/SquadronContextAdvice.java) liest das Attribut direkt aus der Session (kein Backend-GET mehr). Backend's `MeController` PUT/DELETE entfernt — die Methoden waren effektiv No-Ops weil die Backend-Session nicht ueber Requests hinweg lebte; nur GET ueberlebt (reflektiert jetzt den per Request gelieferten Header). `SquadronScopeService` liest `X-Active-Squadron-Id` aus dem `HttpServletRequest`-Header statt aus der lokalen Session; honoriert wird der Header weiterhin nur fuer `ROLE_ADMIN`. Neue Komponenten: [`ActiveSquadronContext`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/logging/ActiveSquadronContext.java) (ThreadLocal-Holder, analog zu `CorrelationContext`), [`ActiveSquadronContextFilter`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/logging/ActiveSquadronContextFilter.java) (Servlet-Filter snapshottet pro Request das Session-Attribut in den ThreadLocal — Order `LOWEST_PRECEDENCE - 99` damit Spring Session und Spring Security die Session bereits gewrappt haben), [`ActiveSquadronRelayFilter`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/logging/ActiveSquadronRelayFilter.java) (WebClient-Exchange-Filter haengt `X-Active-Squadron-Id` an jeden Backend-Call; gewired in [`WebClientConfig.webClient`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/config/WebClientConfig.java), Reactor-Context-Propagation traegt den ThreadLocal-Wert vom Tomcat-Handler-Thread zum Netty-Reactor-Thread). **(3) Serializer-Robustheit:** Spring Session's JDK-Serializer roundtrippt eine `UUID`-Instance unter bestimmten Konfigurationen als `String` zurueck — der `instanceof UUID`-Check im Read-Pfad schlug daher fehl und der Switch wirkte wie ein No-Op. Workaround: Schreibseite stempelt jetzt den `UUID.toString()` direkt in die Session; Lese-Seite (`ActiveSquadronContext.coerce`) akzeptiert beide Typen und parst falls noetig. **Tests:** `SquadronScopeServiceTest` komplett auf Header-basierte Stubs umgestellt (statt der vorherigen Session-Mocks); `setActiveSquadron`-Tests entfernt (Methode ist gone). Neue Header-Edge-Cases: leerer Header, malformed-UUID, beide ergeben `Optional.empty()`. **Verifikation:** `./gradlew check` BUILD SUCCESSFUL (24 Tasks). Manueller Smoke-Test mit `test-admin` im Dev-Stack bestaetigt: Switch auf IRIDIUM zeigt sofort den IRI-Chip rechts oben, `/missions` und `/inventory` filtern, Switch zurueck auf "Alle Staffeln" raeumt sauber auf.

- **Multi-Squadron-Umbau – Verifikations-Audit Cleanup (Plan §1/§2/§8/§11-Konformitaet).** Folge-Audit der Phase-6-Verifikation hat fuenf nicht-blockierende Plan-Abweichungen identifiziert; alle behoben mit `./gradlew check` weiterhin gruen (22 Tasks). **Fix 1 – SecurityConfig Path-Matcher (Plan §2):** [`SecurityConfig`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/config/SecurityConfig.java) `requestMatchers` fuer `/api/v1/users/*/logistician`, `/api/v1/users/*/mission-manager` und `/api/v1/users/*/attributes` von `hasAnyRole("ADMIN", "OFFICER")` auf `hasRole("ADMIN")` verengt — die method-level `@PreAuthorize` auf [`UserController`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/controller/UserController.java) verlangt schon `hasRole('ADMIN')`, der Path-Matcher war noch ein Relikt der Pre-Phase-4-Konfiguration und suggerierte faelschlich Officer-Zugriff. **Fix 2 – MemberEvaluation-Schreibrechte auf Admin (Plan §2: Promotion-System-Pflege):** [`MemberEvaluationService.listAll` / `upsert` / `delete`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/MemberEvaluationService.java) von `hasAnyRole('ADMIN','OFFICER')` auf `hasRole('ADMIN')` verengt, Klassen-/Methoden-JavaDoc + [`MemberEvaluationController`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/controller/MemberEvaluationController.java) `@Operation`-Summaries auf "ADMIN only" aktualisiert, OpenAPI-Spec automatisch regeneriert. **Fix 3 – `creatingSquadronId` strikt immutable (Plan §8 + §11):** [`JobOrderService.updateJobOrder`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/JobOrderService.java) lehnt einen Update-Versuch mit abweichendem `creatingSquadronId` jetzt mit `BadRequestException` (HTTP 400) ab statt das Feld silently zu ignorieren; Echo des unveraenderten Wertes (DTO-Round-Trip) bleibt erlaubt. Zwei neue Mockito-Tests in [`JobOrderServiceTest`](backend/src/test/java/de/greluc/krt/profit/basetool/backend/service/JobOrderServiceTest.java) pinnen das Reject- und das Accept-Verhalten. **Fix 4 – ROLES_AND_PERMISSIONS-Doc-Inkonsistenz:** [`ROLES_AND_PERMISSIONS.md`](ROLES_AND_PERMISSIONS.md) Items 4/5 sagten noch "Admins/Offiziere" koennten das `is_logistician` / `is_mission_manager`-Flag setzen, widersprachen aber der Matrix-Zeile und der tatsaechlichen `@PreAuthorize`-Annotation. Auf "Admins" korrigiert mit Verweis auf den Phase-4-Lockdown; zwei neue Matrix-Zeilen fuer "Member Evaluations" (eigene `isAuthenticated()` vs. Pflege `hasRole('ADMIN')`) ergaenzt. **Fix 5 – Frontend-Auth-Indirection:** Neuer [`FrontendAuthHelperService`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/service/FrontendAuthHelperService.java) buendelt die zwei Auth-Predicates (`isAuthenticated()` / `isAdmin()`), die [`SquadronContextAdvice`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/config/SquadronContextAdvice.java) braucht; dort wird der direkte `SecurityContextHolder`-Zugriff durch den Helper ersetzt. Andere Frontend-Touchpoints (`CorrelationIdFilter`, `GlobalExceptionHandler`, `BackendRoleSyncFilter`, Page-Controller, die JWT-Bearer-Tokens an das Backend weiterreichen) bleiben absichtlich unveraendert — der Helper-JavaDoc dokumentiert die bewusste Beschraenkung auf die Render-Layer-Predicates.

- **Multi-Squadron-Umbau – V87 → V84 Renumbering (Flyway-Ordering-Bug).** Folge-Korrektur zum Verifikations-Audit: die ursprünglich als `V87` eingefügte Handover-Audit-Migration kam **nach** den fuer Phase 7 reservierten Slots `V84`/`V85`/`V86` — was Flyway's strikt aufsteigende Reihenfolge (`out-of-order=false` ist Projekt-Default in `application-dev.yml` + `application-prod.yml`) gebrochen haette: ein spaeteres Hinzufuegen von `V84`/`V85`/`V86` haette eine `FlywayValidateException` geworfen, weil die Versionen niedriger sind als das bereits applied `V87`. Korrektur: `V87__add_job_order_handover_audit_columns.sql` → `V84__add_job_order_handover_audit_columns.sql` umbenannt; Phase 7's reservierte Slots verschieben auf `V85` (NOT-NULL-Tightening), `V86` (stop-write legacy `job_order.squadron` VARCHAR) und `V87` (DROP COLUMN). Alle Plan-Referenzen ([`MULTI_SQUADRON_PLAN.md`](MULTI_SQUADRON_PLAN.md) §3.3/§6/§9/§10, [`CLAUDE.md`](CLAUDE.md) §"Multi-squadron tenancy", [`README.md`](README.md) §3.4 + Entity-Javadoc auf `Mission`/`InventoryItem`/`Operation`/`RefineryOrder`/`Ship`/`JobOrder` + Service-Javadoc auf `JobOrderService`/`UserService` + `CreateJobOrderDto`-Javadoc + Migrations-Header-Kommentare auf `V82`/`V83`/`V84`) konsistent auf das neue Numbering nachgezogen. Build weiterhin gruen.

- **Multi-Squadron-Umbau – Phase 6 Closure (Job-Order-Audit + Plan-Luecken).** Letzte zwei offene Gap-Punkte aus dem Verifikations-Audit von MULTI_SQUADRON_PLAN.md abgearbeitet plus die wartenden WIP-Aenderungen für die Plan-Revisions 2 und 3 (cross-Staffel-Workspace + Dual-Squadron-Job-Order + Handover-Audit) auf eine gruene Test-Linie gebracht. **Job Order — Dual-Squadron-Modell (Plan §3.2/§4.4/§4.5):** [`JobOrder`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/model/JobOrder.java) traegt jetzt `creatingSquadron` (immutable nach Erstellung, automatisch aus `SquadronScopeService.currentSquadron()` gestempelt, Admin-Override via DTO erlaubt) **und** `requestingSquadron` (Pflichtfeld, von jedem Logistician+ aus beliebiger Staffel editierbar; Optimistic-Lock-geschuetzt). Die Legacy `squadron VARCHAR`-Spalte wird weiter gespiegelt fuer einen Release-Zyklus (Backward-Compat); Backfill aus VARCHAR via case-insensitive `squadron.name`/`squadron.shorthand`-Match mit IRIDIUM-Fallback (`V83`). [`CreateJobOrderDto`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/model/dto/CreateJobOrderDto.java) + Frontend-Mirror tragen beide UUID-Felder; [`JobOrderService.resolveCreatingSquadronForCreate`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/JobOrderService.java) wirft 400 wenn Admin in "Alle Staffeln"-Modus ohne expliziten Creator stempelt. `JobOrderMapper`/[`JobOrderDto`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/model/dto/JobOrderDto.java) projizieren beide Squadrons als `SquadronReferenceDto`-Mini-Records. **Handover-Audit (Plan §4.4 + §7):** Neue Migration [`V84__add_job_order_handover_audit_columns.sql`](backend/src/main/resources/db/migration/V84__add_job_order_handover_audit_columns.sql) fuegt `executing_user_id` (FK app_user, `ON DELETE SET NULL` — Account-Loeschung darf den Audit-Trail nicht zerschiessen) und `executing_squadron_id` (FK squadron) zu `job_order_handover` hinzu, beide nullable da rueckwirkend nicht stempelbar. Die Audit-Migration belegt den V84-Slot (vor Phase 7's V86-V88) damit Flyway's strikte aufsteigende Reihenfolge (`out-of-order=false` Default) nicht verletzt wird. [`JobOrderHandoverService.createHandover`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/JobOrderHandoverService.java) stempelt beide aus `userService.getCurrentUser()` plus dessen `User.getSquadron()` — Snapshot, nicht via FK-Lazyload, damit eine spaetere Squadron-Verschiebung den historischen Audit-Eintrag nicht ueberschreibt. Die Cross-Staffel-Pre-Write-Guard `item.jobOrderId.equals(currentOrder.id)` wird jetzt als `IllegalStateException` geworfen statt `BadRequestException`; [`GlobalExceptionHandler.handleIllegalState`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/exception/GlobalExceptionHandler.java) mapped sie auf HTTP 400 mit RFC-7807-`problem+json`, sodass das Wire-Format unveraendert bleibt. **`AuthHelperService` Plan-Konformitaet (Plan §4.1):** [`AuthHelperService`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/AuthHelperService.java) bekommt die im Plan vorgesehenen Convenience-Accessors `currentSquadronId()`, `canSeeSquadron(UUID)`, `canEditSquadron(UUID)`, jeweils via lazy `ApplicationContext`-Lookup an `SquadronScopeService` delegierend (vermeidet die zirkulaere Bean-Wiring). **Dynamische `appTitle` (Plan §5.4):** Neuer [`SquadronContextAdvice`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/config/SquadronContextAdvice.java)-Modell-Attribut `appTitle` ersetzt den hardcodierten "IRIDIUM Basetool"-Titel auf allen ~50 Thymeleaf-Templates durch entweder `Basetool` (kein Squadron-Kontext), `Basetool – <Shorthand>` (aktiver Squadron-Kontext) oder `Basetool – Alle Staffeln` (Admin im All-Modus). i18n-Keys `app.title.with.squadron` / `app.title.all.squadrons` ergaenzt in DE/EN. Sidebar-Context-Badge bekommt `squadron.context.none`-Label fuer authentifizierte User ohne Squadron-Zuweisung. **Gap-Fix 1: `MissionDto.owningSquadron`** (Plan §4.5 — bisher nur in `MissionListDto` exponiert): Backend + Frontend `MissionDto` tragen einen finalen `SquadronReferenceDto owningSquadron`-Komponentenfeld, automatisch von MapStruct ueber den `SquadronMapper` auf der `Mission`-Entity-Beziehung gemappt. [`MissionController.cleanupMissionForGuest`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/controller/MissionController.java) reicht das Feld auch fuer Guest-Aufrufe durch (Shorthand ist per Plan §7 nicht sensitiv). Im Detail-View [`mission-detail.html`](frontend/src/main/resources/templates/mission-detail.html) erscheint der Squadron-Badge im Header neben dem Missions-Namen, konsistent mit der List-Spalte in `missions.html`. 25 `new MissionDto(...)`-Aufrufsstellen (Production-Code in 2 Controllern, 23 Test-Sites) auf die neue Konstruktor-Signatur nachgezogen. **Gap-Fix 2: Job-Order-Liste-Default-Filter "Nur eigene Staffel"** (Plan §5.3): [`JobOrderRepository`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/repository/JobOrderRepository.java) bekommt zwei neue squadron-aware JPQL-Methoden — `findByStatusInAndSquadronInvolved(statuses, squadronId, pageable)` und `findBySquadronInvolved(squadronId, pageable)` — beide mit der `(:squadronId IS NULL OR creating = :squadronId OR requesting = :squadronId)`-Klausel die `null` als Wildcard akzeptiert. [`JobOrderService.getAllJobOrders`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/JobOrderService.java) ueberladen mit optionalem `UUID squadronId`-Param (Backward-Compat-Delegate fuer alte 2-Arg-Aufrufer). [`JobOrderController`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/controller/JobOrderController.java) `getAllJobOrders` akzeptiert `?squadronId=<uuid>` Query-Param. Auf Frontend-Seite traegt [`JobOrderPageController.viewOrders`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/controller/JobOrderPageController.java) jetzt einen zweiten Cookie `orders_filter_scope` (Werte `mine`/`all`, 30-Tage-TTL, gleicher Pfad/Secure/HttpOnly-Setup wie der existierende `orders_filter_status`) mit der Plan-mandated Default-Logik "mine" — User mit aktivem Squadron-Kontext kriegen automatisch nur Job Orders ihrer Staffel (creating ODER requesting matched), Admins im All-Modus collapsen lautlos auf "all" (keine Squadron-Filter-Wirkung). Toggle-UI in [`orders-index.html`](frontend/src/main/resources/templates/orders-index.html) als Radio-Group neben dem Status-Filter, neue i18n-Keys `orders.index.filter.scope` + `orders.index.filter.scope.mine` + `orders.index.filter.scope.all` in DE/EN. **Tests:** 5 neue/aktualisierte JobOrder-Service-Tests (`populatedStatusListWithSquadronId_passesSquadronIdToRepository`, `emptyStatusListWithSquadronId_callsFindBySquadronInvolved`, plus Umstellung der bestehenden `populatedStatusList`-Variante auf `findByStatusInAndSquadronInvolved`), 1 neuer Controller-Test (`getAllJobOrders_forwardsSquadronIdFilter`), 1 neuer Handover-Cross-Staffel-Positivtest und 2 Job-Order-Dual-Squadron-Immutability-Tests. **Verifikation:** `./gradlew check` BUILD SUCCESSFUL (1655 Backend-Tests + Frontend-Tests + Checkstyle + SpotBugs + Spotless gruen).

- **Multi-Squadron-Umbau – Listen-Filter, Detail-View-Checks und MDC-Squadron-Logging nachgezogen.** Schliesst die in der ersten Multi-Tenant-Ladung als Phase-7-Follow-up dokumentierte Read-side-Filter-Luecke und macht die fuer staffel-scoped Aggregate angekuendigten `@PreAuthorize`-Checks tatsaechlich scharf. **Repository scoping:** [`MissionRepository.searchMissions`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/repository/MissionRepository.java) bekommt einen `scopeSquadronId`-Parameter mit der `owning_squadron_id = :scope OR is_internal = false`-Klausel (MULTI_SQUADRON_PLAN.md section 1: nicht-interne Missionen bleiben cross-Staffel sichtbar). [`InventoryItemRepository.findGlobalByFilters`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/repository/InventoryItemRepository.java) + `getAggregatedInventory` bekommen `owningSquadronId` (strikt staffel-scoped — der Lager-direct-Pfad; der Job-Order-Kontext-Lookup bleibt explizit ungated ueber die separate `findByJobOrderIdOrdered`-Methode). [`RefineryOrderRepository`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/repository/RefineryOrderRepository.java) gewinnt `findAllScoped` + `findByStatusInScoped`. [`OperationRepository`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/repository/OperationRepository.java) gewinnt `findAllScoped`. Alle Services (`MissionService`, `InventoryItemService`, `RefineryOrderService`, `OperationService`) loesen `scopeSquadronId` ueber `SquadronScopeService.currentSquadronId()` auf und reichen ihn an die Repos durch — bestehende Controller-Call-Sites bleiben unveraendert. **Aggregat-spezifische Checks** auf `SquadronScopeService`: `canSeeMission` / `canEditMission` (Missions-`is_internal`-Split: see beruecksichtigt die cross-Staffel-Public-Escape-Clause, edit nicht), `canSeeInventoryItem` / `canEditInventoryItem`, `canSeeRefineryOrder` / `canEditRefineryOrder`, `canSeeOperation` / `canEditOperation` — jede Methode laedt die Entity per Id, leitet die owning squadron ab und delegiert an `canSeeSquadron` / `canEditSquadron`. Nicht-existente Ids → `false`. **`@PreAuthorize` auf Detail- + Schreib-Endpunkten verdrahtet:** `MissionController.getMissionById` → `canSeeMission(#id)`, `InventoryItemController` PUT/PATCH/PUT-note → `canEditInventoryItem(#id)`, `RefineryOrderController` GET/PUT/DELETE → `canSee/canEditRefineryOrder(#id)`, `OperationController` GET → `canSeeOperation(#id)`, PUT kombiniert `hasRole('MISSION_MANAGER')` + `canEditOperation(#id)`. JobOrder-Endpunkte bleiben absichtlich rollenbasiert (cross-Staffel-Workspace). **MDC-Squadron-Logging:** [`CorrelationIdFilter`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/logging/CorrelationIdFilter.java) emittiert jetzt ein drittes MDC-Feld `squadronId` neben `correlationId` / `userId`. Aufloesung: aktive Switcher-Auswahl des Admins, persistente Home-Staffel des Members, oder einer der Sentinels `all` / `none` / `anonymous`. Defensive try/catch — DB-Hiccups degraden auf `none`. Neuer `squadronIdMdcKey` in `LoggingProperties` fuer das Logback-Pattern. **ArchUnit-Regel `staffelScopedServicesMustWireSquadronOrAuthHelper`** in [`ArchitectureTest`](backend/src/test/java/de/greluc/krt/profit/basetool/backend/ArchitectureTest.java): faellt den Build, wenn einer der Staffel-scoped Whitelist-Services (`MissionService`, `InventoryItemService`, `RefineryOrderService`, `HangarService`, `OperationService`, `JobOrderService`) `AuthHelperService` oder `SquadronScopeService` nicht mehr injiziert. `JobOrderHandoverService` ist absichtlich NOCH NICHT in der Liste — wird ergaenzt sobald die Handover-Audit-Trail-Stamps landen. **Test-Verdrahtung** entsprechend nachgezogen: `@Mock SquadronScopeService` in `MissionServiceTest`, `InventoryItemServiceTest`, `RefineryOrderServiceLifecycleTest`, `OperationServiceTest`; `findGlobalByFilters` / `getAggregatedInventory` `verify`-Calls in `InventoryItemServiceTest` und `InventoryItemServiceAggregateTest` ueber einen Balanced-Paren-Python-Patcher um den neuen `owningSquadronId`-Arg ergaenzt; `CorrelationIdFilterTest`-Konstruktor auf die neue 3-Arg-Signatur umgestellt mit Default-Stubs die "anonymous" zurueckliefern. **Verbleibendes** ist im PR-Body dokumentiert: Sidebar-Switcher-/Badge-UI + Eigentuemer-Staffel-Spalten + Dual-Squadron-Badges auf Job-Order-Seiten, DTO-Mirror-Records, ein paar Officer-als-Admin-Fixture-Aufraeumungen in den Stammdaten-Integration-Tests, und die nachgelagerte Phase-7-Migrationenkette `V86`-`V88` (NOT-NULL-Tightening + Legacy-`squadron`-VARCHAR-Drop).

- **Multi-Squadron-Umbau (Phasen 1-5): Basetool wird mandantenfaehig.** Bisher hat das Basetool implizit nur die Staffel IRIDIUM unterstuetzt; mit diesem Umbau koennen mehrere Staffeln parallel arbeiten und sehen sich gegenseitig nur das, was sie sollen. Plan und Architektur stehen in [`MULTI_SQUADRON_PLAN.md`](MULTI_SQUADRON_PLAN.md). **Datenmodell (Phase 1):** Vier additive Flyway-Migrationen `V80`-`V83` legen IRIDIUM mit der kanonischen UUID `00000000-0000-0000-0000-000000000001` an (`V80`, idempotent: rewriting nicht-kanonischer Bestands-IRIDIUMs samt der einzigen FK `mission_participant.squadron_id`), fuegen `app_user.squadron_id` nullable hinzu (`V81`, Admin/Guest duerfen NULL bleiben), stempeln `owning_squadron_id` auf alle staffel-scoped Aggregat-Roots (`V82`: `mission`, `operation`, `ship`, `inventory_item`, `refinery_order`) und Job Order bekommt die **zwei** Squadron-Felder `creating_squadron_id` (erstellende Staffel, immutable) + `requesting_squadron_id` (auftraggebende Staffel, editierbar) als FK-Spalten (`V83`); das alte `squadron VARCHAR` bleibt fuer einen Release-Zyklus als Safety-Net erhalten. NOT-NULL-Tightening + Legacy-Drop sind in der naechsten Release-Iteration als `V86`-`V88` geplant (nach `V84` Handover-Audit, der mit diesem Release-Pakage shippt). JPA-Entities tragen die neuen Felder (`Squadron.IRIDIUM_ID`-Konstante, `User.squadron`, `Mission/Operation/Ship/InventoryItem/RefineryOrder.owningSquadron`, `JobOrder.creatingSquadron`/`requestingSquadron`). **Authorization (Phase 2):** Neuer `SquadronScopeService` loest die "darf der Aufrufer Daten von Staffel X sehen/editieren?"-Frage auf — Admins lesen die session-gespeicherte Switcher-Auswahl (`iridium.activeSquadronId` via Spring Session / Redis), alle anderen ihre persistente `app_user.squadron_id`; ein NULL-Active-Squadron-Wert beim Admin bedeutet "alle Staffeln". [`AuthHelperService`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/AuthHelperService.java) gewinnt `currentUserId()` (parst JWT-`sub`) + `isAdmin()`. Neuer Endpoint [`/api/v1/me/active-squadron`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/controller/MeController.java) (GET/PUT/DELETE, PUT/DELETE admin-only) schaltet die Switcher-Auswahl um; [`MeFrontendController`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/controller/MeFrontendController.java) proxied via `POST /me/active-squadron` aus dem Browser durch. **Aggregat-Stamps (Phase 3, partial):** Alle Aggregat-Service-Create-Pfade stempeln den Squadron-Bezug — Ship/InventoryItem/RefineryOrder leiten `owningSquadron` aus dem Eigner-User ab, Mission nimmt es vom `owner.squadron` mit Fallback auf den aktiven Kontext, Operation aus dem aktiven Kontext, JobOrder bekommt `creatingSquadron` aus dem Kontext (IRIDIUM-Fallback wenn Admin ohne Switcher-Auswahl) und `requestingSquadron` aus dem legacy `squadron VARCHAR`-DTO-Feld via Shorthand-Lookup; das Legacy-Feld bleibt mit `requestingSquadron.shorthand` mitgespiegelt. Hangar-Listenfilter (`HangarService.getAllShips` + `getSquadronOverview`) sind komplett staffel-aware ueber neue `ShipRepository.findAllScoped`/`countShipsByType`-Queries. **Listfilter fuer Mission/Inventory/Refinery/Operation sind noch offen** (Follow-up; siehe MULTI_SQUADRON_PLAN.md Phase 7). **Job Order Sonderfall:** absichtlich **nicht** staffel-gefiltert — Auftraege sind cross-Staffel-Workspace und werden gemaess der bestehenden Rollen-/Rechte-Matrix bearbeitet (Logistician+ darf editieren, Admin darf loeschen). Per `job_order_id` verknuepfte InventoryItems sind innerhalb des Order-Kontexts fuer alle Staffeln sichtbar und ueber Handover-Funktionen bearbeitbar, auch wenn das Item einer anderen Staffel gehoert — der direkte Lager-View bleibt aber strikt staffel-gefiltert. **Admin-Bereich auf Admin-only verschaerft (Phase 4, Breaking Change fuer Officer):** Jedes `@PreAuthorize` / `sec:authorize`, das vorher Officer+Admin im Admin-Bereich gewaehrt hat, ist jetzt `hasRole('ADMIN')`. Betroffen sind die Stammdaten-Controller (City, Location, Material, MaterialCategory, Outpost, POI, SpaceStation, StarSystem, Terminal, FrequencyType, JobType, RefiningMethod, Squadron), die User-Management-Endpoints (Logistician-/Mission-Manager-Flags, Attribute-Patches auf [`UserController`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/controller/UserController.java)), die system-weite Konfiguration (SystemSettingController, AnnouncementController-Schreibpfade), das Promotion-System (alle PromotionXxxService + PromotionEligibilityController), die Frontend-Admin-Seiten (AdminMissionData/Locations/Materials/Settings/Announcement/Uex/ShipData/Member/Promotion) und die zugehoerigen Thymeleaf-Templates (`admin/locations.html`, `admin/materials.html`, `admin/uex.html`, `fragments/admin-uex.html`, `fragments/sidebar.html`, `ship-data.html`). **Explizit Officer-Zugriff behalten** (squadron-internal pro Plan-Abschnitt 2): `HangarController.resetAllFittedStatus`, `hangar-squadron.html`-Owner-Details, JobOrder-Endpoints + JobOrderPageController + ProfitCalculationController (cross-Staffel-Workspace bzw. Profit ist allgemein zugaenglich), UserController-Lese-Endpoints (Member-Liste + Details). **Frontend (Phase 5, partial):** `MeFrontendController` + i18n-Keys (`squadron.switcher.label`, `squadron.switcher.all`, `squadron.switcher.activated`, `squadron.switcher.cleared`, `squadron.context.badge`) ergaenzt; Sidebar-Switcher-Dropdown, Kontext-Badge im Header, Eigentuemer-Staffel-Spalten auf den staffel-scoped Listen und das Dual-Squadron-Badge-Paar auf Job-Order-Detail/-List sind als Follow-up dokumentiert (siehe MULTI_SQUADRON_PLAN.md). Admins koennen den Switcher heute schon ueber `POST /me/active-squadron` (Form-Body `squadronId=<uuid>` oder leer zum Zuruecksetzen) bedienen — die Authorization-Schicht respektiert die Auswahl ueber alle aggregate-Service-Pfade hinweg, die bereits in Phase 3 angepasst sind. **Verbleibendes (Phase 6):** ROLES_AND_PERMISSIONS.md-Matrix neu schreiben, bestehende Tests mit OFFICER-Auth-Annahmen auf 403-erwartet umstellen, Switcher-/Badge-UI integrieren, list-Filter auf die verbleibenden Aggregate ausweiten — dokumentiert im PR-Body und in der Plan-Datei. **Wichtig fuer Datenfluss:** Hibernate `ddl-auto=validate` validiert nach dem Boot, dass die JPA-Entities zu den durch Flyway angelegten Spalten passen — die Phase-1-Migrationen sind so geschnitten, dass jede Entity-Aenderung im selben Commit landet wie die zugehoerige Migration (siehe `db/migration/README.md`). **Vor dem ersten Deploy** ist ein Lauf von `./gradlew :backend:bootRun` gegen das dev-Postgres als Smoke-Test obligatorisch (Test-Profile umgeht Flyway via H2 + ddl-auto, faengt damit Migrations-Fehler nicht).

## [v0.1.9](https://github.com/krt-profit/basetool/releases/tag/v0.1.9) - 2026-05-19

### Changed

- **`[DEBUG_LOG]`-Markierungen aus Backend- und Frontend-Controllern entfernt und auf konsistentes Debug-Level-Logging umgestellt.** Die durch frueheres Debugging eingeschleppten `[DEBUG_LOG]`-Praefixe an Log-Meldungen in [`MissionController`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/controller/MissionController.java), [`MissionSecurityService`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/MissionSecurityService.java), [`JobOrderPageController`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/controller/JobOrderPageController.java) und [`MissionPageController`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/controller/MissionPageController.java) sind komplett entfernt; saemtliche betroffenen Aufrufe laufen jetzt auf `log.debug` statt `log.info`/`log.warn`/`log.error`, weil es durchgehend Trace-Instrumentierung war, die in Production nicht im Standard-Log-Stream auftauchen soll (Add/Remove-Manager-, Owner-Change- und alle Mission-AJAX-Endpoints `frequencies`/`units`/`participants`/`crew`/`unassigned`, Logistician-Authority-Check im JobOrder-Modul, Authorisierungs-Trace in `canManageManagers`). Die zugehoerigen Javadoc-Hinweise auf die `[DEBUG_LOG]`-Marker (am `addManager`-Endpoint und am `canManageManagers`-Authorisierungs-Helper) sind entsprechend nachgezogen — sie verweisen jetzt auf das Debug-Logging als Ganzes statt auf einen verschwundenen Marker. Verhaltensaenderung: wer die ehemaligen INFO-/WARN-/ERROR-Linien wieder sehen will, setzt den jeweiligen Logger (`de.greluc.krt.profit.basetool.backend.controller.MissionController`, `de.greluc.krt.profit.basetool.backend.service.MissionSecurityService`, `de.greluc.krt.profit.basetool.frontend.controller.JobOrderPageController`, `de.greluc.krt.profit.basetool.frontend.controller.MissionPageController` oder das Eltern-Package) per `application-*.yml` auf `DEBUG`. Schliesst [#118](https://github.com/krt-profit/basetool/issues/118).

- **Fehlermeldungsseite (`/error` und `error/error.html`): Box-Layout zentriert + 403-Wortlaut hängt vom Anmeldestatus ab; bei nicht angemeldeten Nutzern erscheint zusätzlich ein "Anmelden"-Button (issue [#108](https://github.com/krt-profit/basetool/issues/108)).** Vorher saß die Fehlerbox bündig links unter dem Header (linker `.greeting`-Border + Gradient zogen sie optisch zur Seite) und der 403-Text war für jeden 403-Pfad identisch — sowohl für authentifizierte Nutzer ohne Rolle als auch für nicht angemeldete Besucher. Letztere bekamen keine Hilfestellung, dass eine Anmeldung das Problem lösen würde. **Layout:** [`error/403.html`](frontend/src/main/resources/templates/error/403.html), [`error/404.html`](frontend/src/main/resources/templates/error/404.html), [`error/500.html`](frontend/src/main/resources/templates/error/500.html), [`error/error.html`](frontend/src/main/resources/templates/error/error.html) und [`error.html`](frontend/src/main/resources/templates/error.html) bekommen einen Box-Style mit `max-width: 600px`, `margin: 0 auto`, eigenem dunklen Hintergrund (`rgba(20,20,20,0.6)`) und kompaktem `padding: 2.5rem 2rem` — der von `.greeting` geerbte linke Border und Gradient wird durch das Inline-CSS überschrieben. `main` schaltet zusätzlich auf `display: flex; align-items: center; justify-content: center; min-height: 100vh`, damit die Box auch vertikal mittig im Viewport sitzt und nicht direkt unter dem Header klebt. **Dynamischer 403-Wortlaut:** [`GlobalExceptionHandler.handleAccessDenied`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/exception/GlobalExceptionHandler.java) prüft per `SecurityContextHolder.getContext().getAuthentication()`, ob der Aufrufer anonym ist (`null` oder `AnonymousAuthenticationToken`); ist er anonym, wird statt `error.forbidden` ("Du hast keine Berechtigung für diese Aktion.") der neue Key `error.forbidden.unauthenticated` ("Du bist nicht angemeldet. Bitte melde dich an und versuche es erneut.") aufgelöst und das Model-Attribut `unauthenticated=true` gesetzt. Dieselbe Logik greift auch im `handleBackendServiceException`-Pfad für die Backend-Codes `ACCESS_DENIED`/`FORBIDDEN_ROLE` und immer für `UNAUTHENTICATED` — letzteres behält den bisherigen Wortlaut "Deine Sitzung ist abgelaufen.", setzt aber ebenfalls den Flag, damit der CTA erscheint. Der JSON-Pfad (AJAX-Toast-Renderer) bekommt das Flag in `body.unauthenticated` zusätzlich zu Code, Status, Titel und Message. **Sign-in-CTA:** [`error/error.html`](frontend/src/main/resources/templates/error/error.html) zeigt zwischen `<p class="error-message">` und dem bestehenden "Return to Base"-Link einen `.error-actions`-Flex-Container mit einem orangenen `.btn-signin`-Button, der via `th:if="${unauthenticated}"` nur erscheint wenn das Flag gesetzt ist. Der Button verlinkt auf `/oauth2/authorization/keycloak` — also denselben OAuth2-Authorize-Endpoint, den auch der `loginPage`-Eintrag in [`SecurityConfig`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/config/SecurityConfig.java) als Login-Einstieg konfiguriert. **i18n:** Zwei neue Keys (`error.forbidden.unauthenticated`, `error.signin`) in [`messages.properties`](frontend/src/main/resources/messages.properties), [`messages_de.properties`](frontend/src/main/resources/messages_de.properties) (Umlaute als `\uXXXX`-Escapes — die neuen deutschen Texte enthalten keine, aber die alphabetische Nachbarschaft schon) und [`messages_en.properties`](frontend/src/main/resources/messages_en.properties). **Tests:** [`GlobalExceptionHandlerTest`](frontend/src/test/java/de/greluc/krt/profit/basetool/frontend/exception/GlobalExceptionHandlerTest.java) bekommt einen `@BeforeEach`-Default mit `TestingAuthenticationToken("test-user", …, ROLE_USER)`, einen `@AfterEach` der `SecurityContextHolder.clearContext()` aufruft, sowie sechs neue Tests: anonymer 403 mit `AuthorizationDeniedException` (HTML-Pfad), kein-Auth-Kontext 403 (`SecurityContextHolder.clearContext()` als Defensive für den selten konfigurierten "kein AnonymousAuthenticationFilter"-Pfad), anonymer 403 mit JSON-Body-Flag, anonymer Backend-`ACCESS_DENIED` mit korrektem Message-Key-Switch, Backend-`UNAUTHENTICATED` mit Flag aber unverändertem `error.unauthenticated`-Wortlaut, und authentifizierter Backend-`ACCESS_DENIED` als Regressionsschutz für die ursprüngliche Forbidden-Branch. Die existierende `accessDenied_authorizationDenied_htmlRequest_returnsErrorView` bekommt eine zusätzliche `assertEquals(Boolean.FALSE, model.getAttribute("unauthenticated"))`-Assertion. **Verhalten unverändert für:** authentifizierte Nutzer ohne Rolle (sehen weiter "Du hast keine Berechtigung"), 404/500-Pfade (keine Anmelde-Empfehlung — eine fehlende Ressource oder ein interner Fehler wird auch nach Anmeldung nicht verschwinden), und Backend-`ACCESS_DENIED` mit authentifiziertem Frontend-Nutzer (klassischer "du hast den Endpoint zwar erreicht, aber nicht die nötige Rolle"-Fall, der Anmeldung bringt da nichts).

### Fixed

- **Auftrag-Anlegen-Seite (`/orders/create`): Anonyme Gäste können den scmdb-Shopping-List-Import wieder benutzen — Material-Katalog wird jetzt über den öffentlichen WebClient geladen.** Beim Aufruf der Seite ohne Login erschien beim Klick auf „Materialien extrahieren" der Fehler-Toast „Folgende Materialien wurden nicht im System gefunden: Agricium, …", obwohl die Materialien in der DB stehen und ein eingeloggter Nutzer denselben Text fehlerfrei importieren konnte. Ursache: [`JobOrderPageController.fetchMaterials()`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/controller/JobOrderPageController.java) rief `backendApiClient.getCached("/api/v1/materials/job-order", …)` ohne das `isPublic=true`-Flag auf und routete damit über den authentifizierten WebClient (`webClient`-Bean in [`WebClientConfig`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/config/WebClientConfig.java)), der per `ServletOAuth2AuthorizedClientExchangeFilterFunction` einen OAuth2-Bearer-Token anhängen will — für anonyme Caller existiert kein autorisierter Client, die Filter-Kette wirft, das `catch (Exception)` in `fetchMaterials()` schluckt es und liefert eine leere Liste. Folge: das versteckte `<select id="material-options-template">` in [`orders-create.html`](frontend/src/main/resources/templates/orders-create.html) wurde mit null Optionen gerendert, und die clientseitige `importFromScmdb()`-Funktion fand für jeden gepasten Materialnamen keinen Match → Toast `orders.create.scmdb.error.not_found`. Eingeloggte Nutzer waren nicht betroffen, weil der Bearer-Token-Anhang dort durchlief und das Backend-Endpoint `GET /api/v1/materials/job-order` ohnehin `permitAll` ist (kein `@PreAuthorize` auf [`MaterialController.getJobOrderMaterials`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/controller/MaterialController.java)). **Fix:** `fetchMaterials()` ruft `getCached(..., true)` analog zu `fetchSquadrons()` und zum `createOrder()`-POST in derselben Klasse — alle drei Backend-Calls aus dem Anonymous-fähigen Create-Form-Flow routen damit konsistent über den `publicWebClient`. **Test:** Neuer Regressions-MVC-Test [`JobOrderPageControllerCreateFormAnonymousMvcTest`](frontend/src/test/java/de/greluc/krt/profit/basetool/frontend/controller/JobOrderPageControllerCreateFormAnonymousMvcTest.java) feuert `GET /orders/create` mit `@WithAnonymousUser` und verifiziert per Mockito, dass `backendApiClient.getCached("/api/v1/materials/job-order", any, true)` genau einmal aufgerufen wird und die ohne-Flag-Überladung (die auf den authentifizierten WebClient mappt) *nie* — derselben Idiomatik folgend, die `MissionPageControllerMvcTest` für die Mission-Anonymous-Reproducer verwendet.

- **Admin-UEX-Seite (`/admin/uex-data`): Klick auf „UEX" nach vorherigem „Ja"-Override auf Terminal-Loading-Dock bzw. -Autoload setzt den effektiven Wert jetzt sofort auf den letzten UEX-Stand zurueck statt erst beim naechsten Sweep.** Vorher hat [`TerminalService.clearLoadingDockOverride`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/TerminalService.java) bzw. `clearAutoLoadOverride` nur das `*Overridden`-Flag auf `false` gestellt und das `hasLoadingDock`/`isAutoLoad`-Wertfeld unangetastet gelassen — also den manuell auf `true` gepinnten Wert in der DB stehen lassen, bis der naechste UEX-Sweep den Eintrag wieder ueberschreibt. Folgen, die der Nutzer auf [`/admin/uex-data`](frontend/src/main/resources/templates/admin/uex.html) gesehen hat: (1) der UEX-Source-Chip unter dem Segmented-Control blieb im Mismatch-Zustand und hat `UEX: Nein` weiterhin gelb dargestellt, obwohl der Override geloescht war (gelbe `.uex-source-chip.mismatch`-Klasse wird aus `term.uexHasLoadingDock != term.hasLoadingDock` abgeleitet, siehe [`fragments/admin-uex.html`](frontend/src/main/resources/templates/fragments/admin-uex.html)); (2) die Materials-Overview-Filterung in [`MaterialsPageController.getMatrixOverview`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/controller/MaterialsPageController.java) (Filter `filterLoadingDock`/`filterAutoLoad`) hat den Eintrag weiter als „Loading-Dock = Ja" einsortiert, weil sie direkt `item.hasLoadingDock()` liest; (3) auch [`ProfitCalculationService`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/ProfitCalculationService.java) und die `MaterialPrice`-Repository-JPQL haben den stalen Wert weiter verwendet. Der Wechsel Yes&rarr;No hat funktioniert, weil dort `setLoadingDockOverride(id, false)` das Wertfeld explizit aktualisiert. **Fix:** Beide `clear*Override`-Methoden setzen zusaetzlich `terminal.setHasLoadingDock(terminal.getUexHasLoadingDock())` bzw. `terminal.setIsAutoLoad(terminal.getUexIsAutoLoad())` — die `uex*`-Spiegelspalten werden bei jedem UEX-Sweep unabhaengig vom Override-Flag geschrieben (siehe [`UexUniverseSyncService.syncTerminals`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/UexUniverseSyncService.java) Zeilen 705–706), tragen also genau den letzten echten UEX-Wert. Ist der Spiegel `null` (Terminal noch nie gesynct), wird das Wertfeld ebenfalls auf `null` gesetzt; jeder Consumer interpretiert `null` als „unbekannt" und der naechste Sweep ueberschreibt es korrekt. Javadoc auf [`TerminalService`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/TerminalService.java) und [`TerminalController`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/controller/TerminalController.java) entsprechend angepasst (vorher dokumentierte sie den Bug als Feature: „The value column stays at its last value until the next UEX sweep overwrites it"). **Tests:** Die zwei vorhandenen `clear*Override_flipsFlagButLeavesValueAlone`-Tests in [`TerminalServiceTest`](backend/src/test/java/de/greluc/krt/profit/basetool/backend/service/TerminalServiceTest.java) haben die alte (kaputte) Semantik gepinnt und sind durch je zwei neue Tests ersetzt: `clear*Override_flipsFlagAndRevertsValueToUexMirror` (asserted dass `hasLoadingDock`/`isAutoLoad` auf den `uex*`-Spiegelwert zurueckfaellt) und `clear*Override_revertsValueToNullWhenUexMirrorIsNull` (asserted die Null-Fallthrough-Semantik fuer noch-nie-gesyncte Terminals). Scope-Begruendung: Stadt/Raumstation/Aussenposten/POI haben dieselbe Override-Semantik, aber **keine** UEX-Spiegelspalte — dort waere derselbe Fix nur mit zusaetzlicher Migration moeglich; der user-sichtbare Bug (gelber Chip, Materials-Overview-Filter) betrifft ausschliesslich Terminals, weil nur die das `hasLoadingDock`/`isAutoLoad`-Feld in Filter und Profit-Calc lesen.

- **SCMDB-Shoppinglist-Import auf `/orders/create`: Mengen werden ohne überflüssige Nachkomma-Nullen ins Mengenfeld übernommen.** Beim „Materialien extrahieren" aus einem scmdb.net-Export ([`orders-create.html`](frontend/src/main/resources/templates/orders-create.html)) wurde der vom Regex erfasste Mengen-String bisher zwar per `replace(',', '.')` auf den Punkt-Dezimaltrenner gehoben, aber nie tatsächlich als Zahl geparst — er landete unverändert auf dem `type="number"`-Input. Folge: Materialien, deren SCMDB-Menge auf `.0000` endet (z. B. Laranite und Titanium mit `2.0000 SCU`), wurden im Mengenfeld als `2.0000` angezeigt statt als sauberes `2`; auch andere Werte (`0.0200`, `0.1200`, `8.5200`) blieben mit voller Vierer-Nachkomma-Präzision sichtbar, obwohl das Feld nur Step `0.001` zulässt. **Fix:** Der eingelesene String wird in [`orders-create.html`](frontend/src/main/resources/templates/orders-create.html) jetzt zusätzlich per `parseFloat` in eine Zahl umgewandelt, bevor er auf den Input gesetzt wird — JavaScript serialisiert daraus die kürzeste, verlustfrei zurück-parsebare Darstellung (`2`, `0.02`, `2.48`), sodass die Browser-Anzeige zur Step-Granularität `0.001` des Felds passt. `NaN`-Fälle (Regex matcht, liefert aber kein gültiges Zahlliteral) werden via `continue` übersprungen, statt ein leeres Pflicht-Input zu hinterlassen. Kein neuer Test — der Import-Pfad ist reines Template-JS ohne JS-Testinfrastruktur, das vorhandene Server-Side-Rendering bleibt unberührt.

## [v0.1.8](https://github.com/krt-profit/basetool/releases/tag/v0.1.8) - 2026-05-17

### Changed

- **Operation-Auszahlungspanel (`/operations/{id}`): Auszahlungsbetrag wird im Backend final kaufmännisch (HALF_UP) auf ganze aUEC gerundet, damit die Tabellen-Zelle keine Nachkommastellen mehr anzeigt.** Star Citizens mobiGlas akzeptiert bei einem aUEC-Transfer keine Bruchteile — der Mission Manager musste die angezeigten Beträge bisher beim Anweisen sowieso runden. Damit das jetzt schon am Backend passiert (und die Anzeige dem entspricht, was wirklich transferiert wird), wird `payoutAmount` als letzter Schritt auf `setScale(0, RoundingMode.HALF_UP)` reduziert. **Backend:** [`OperationService.getOperationPayouts`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/OperationService.java) rundet `grossPayout − transferFee` final auf Skalierung 0 (kaufmännisches Runden = HALF_UP); die Zwischenwerte `personalExpenses`, `shareAmount` und `transferFee` bleiben bewusst bei zwei Nachkommastellen, weil sie als Sub-Labels („davon Auslagen", „bereits abgezogene Überweisungsgebühr") die Zerlegung transparent halten — die Summe der Sub-Labels kann dadurch um ≤ 0,50 aUEC vom Hauptwert abweichen, was bei einem Wert, der ohnehin als ganze aUEC im Spiel ankommt, akzeptabel ist. Im selben Zug ist der Transfer-Fee-Sub-Label-Wortlaut vom alten „davon Überweisungsgebühr" auf „bereits abgezogene Überweisungsgebühr" (`operation.payout.amount.transferfee` in [`messages.properties`](frontend/src/main/resources/messages.properties), [`messages_de.properties`](frontend/src/main/resources/messages_de.properties), englische Spiegelung auf „already deducted transfer fee" in [`messages_en.properties`](frontend/src/main/resources/messages_en.properties)) umgestellt, weil das frühere „davon" zusammen mit der neuen Rundung den Eindruck erwecken konnte, der Sub-Wert sei noch im Hauptbetrag enthalten — er ist aber bereits subtrahiert. Die Backend-DTO-Doku ([`OperationPayoutDto`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/model/dto/OperationPayoutDto.java)) und die OpenAPI-Description am `GET /api/v1/operations/{id}/payouts`-Endpoint ([`OperationController`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/controller/OperationController.java), gespiegelt in [`backend/src/main/resources/api/openapi.json`](backend/src/main/resources/api/openapi.json)) erwähnen die Rundung jetzt explizit. **Frontend:** [`operation-detail.html`](frontend/src/main/resources/templates/operation-detail.html) schaltet die Anzeige der `payout-amount-value`-Zelle von `#numbers.formatDecimal(payout.payoutAmount, 1, 'POINT', 2, 'COMMA')` auf `#numbers.formatInteger(payout.payoutAmount, 3, 'POINT')` um (mindestens 3 Integer-Stellen erzwingt den Tausender-Punkt analog zur sonstigen Thousands-Separator-Konvention) — keine ",00"-Endung mehr, kein widersprüchliches Padding für ohnehin ganzzahlige Werte. Der Frontend-Mirror-Record [`OperationPayoutDto`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/model/dto/OperationPayoutDto.java) bekommt den dazu passenden Javadoc-Hinweis. **Tests:** Alle 16 `payoutAmount`-Assertions in der `GetOperationPayoutsAmountTests`-Nested-Klasse von [`OperationServiceTest`](backend/src/test/java/de/greluc/krt/profit/basetool/backend/service/OperationServiceTest.java) sind auf die neuen Erwartungswerte umgestellt (`497.50 → 498`, `646.75 → 647`, `348.25 → 348`, `1343.25 → 1343`, `298.50 → 299`, `331.66 → 332`, etc.); die Klassen-Javadoc und einzelne Inline-Kommentare benennen den finalen HALF_UP-Schritt explizit, damit zukünftige Refactorings die Rundung nicht versehentlich rauspulen. Andere monetäre Felder im Payout-Pfad bleiben bei zwei Nachkommastellen — die Rundung greift ausschliesslich auf dem als Hauptzahl angezeigten Auszahlungsbetrag.

### Fixed

- **Admin-UEX-Seite (`/admin/uex-data`): 500-Fehler beim Aufruf behoben — `terminalRow`-Fragment in eigene Datei ausgelagert und `th:each`/`th:replace`-Praezedenzfalle aufgeloest.** Beim Render der konsolidierten UEX-Adminseite warf Thymeleaf eine `SpelEvaluationException: EL1007E: Property or field 'name' cannot be found on null` an Template-Zeile 506 (`term.name` im `th:attr="data-name=${term.name}"`-Ausdruck der `terminalRow`-Fragment-Definition). Zwei sich verstaerkende Probleme: **(1)** Die Fragment-Definition `<table th:fragment="terminalRow(term)" style="display: none;">` stand im selben File auf Top-Level nach `</main>`. Thymeleaf wertet auch versteckte Elemente im normalen Seitenfluss aus, und in diesem Kontext ist `term` ungebunden — `${term}` liefert dann `null`, jeder Folgezugriff fliegt mit `EL1007E`. `style="display: none;"` verbirgt nur die Ausgabe, nicht die SpringEL-Auswertung. **(2)** Auch die drei Aufrufstellen waren bereits kaputt: `<tr th:each="term : ${...}" th:replace="~{admin/uex :: terminalRow(term=${term})}">` packt `th:each` (Praezedenz 2) und `th:replace` (Praezedenz 1) auf dasselbe Element; `th:replace` laeuft damit *bevor* `th:each` die Iterationsvariable bindet, das `${term}` im Fragment-Expression-Parameter wird vor dem Loop evaluiert und liefert `null`. Klassische Thymeleaf-Falle. **Fix:** Neue Datei [`fragments/admin-uex.html`](frontend/src/main/resources/templates/fragments/admin-uex.html) mit der Fragment-Definition; Host-Element ist jetzt das `<tr>` selbst (umschliessendes `<table><tbody>` nur fuer HTML-Validitaet der Standalone-Datei), damit `th:replace` an der Aufrufstelle ein echtes `<tr>` produziert statt einer eingebetteten `<table>`. Die drei Aufrufer in [`admin/uex.html`](frontend/src/main/resources/templates/admin/uex.html) wurden in `<th:block th:each="term : ...">…<tr th:replace="~{fragments/admin-uex :: terminalRow(term=${term})}"></tr></th:block>` umstrukturiert — `th:each` und `th:replace` sitzen auf verschiedenen Elementen, das `${term}` der Fragment-Parameter wird im korrekten Iterationskontext ausgewertet — und referenzieren den neuen Pfad. Die alte Inline-Fragment-Definition (66 Zeilen) ist entfernt. Kein neuer Test (reine Template-Restrukturierung; das vorhandene Verhalten ist unveraendert), `./gradlew :frontend:checkstyleMain :frontend:spotbugsMain :frontend:test` sauber. **Lesson learned (Reviewer-Memo):** Fragment-Definitionen gehoeren in eigene Files unter `templates/fragments/` — die `display: none`-Variante im selben File wirkt nur visuell, nicht semantisch, und `th:each` + `th:replace` auf demselben Element ist immer ein Bug, weil die Praezedenz den Iterationskontext aushebelt.

## [v0.1.7](https://github.com/krt-profit/basetool/releases/tag/v0.1.7) - 2026-05-17

### Fixed

- **Operation-Detailseite (`/operations/{id}`): 500-Fehler beim Aufruf behoben — Frontend-Spiegel-DTO `OperationPayoutDto` um das in Commit 91c18bb am Backend ergänzte `transferFee`-Feld nachgezogen.** Der vorige Commit (Überweisungsgebühr-Berechnung) hat das neue `transferFee`-Feld nur im Backend-DTO [`OperationPayoutDto`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/model/dto/OperationPayoutDto.java) und im Template [`operation-detail.html`](frontend/src/main/resources/templates/operation-detail.html) eingebaut, das deserialisierende Frontend-Mirror-Record [`OperationPayoutDto`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/model/dto/OperationPayoutDto.java) blieb dabei unangetastet. Resultat: beim Render der Auszahlungen-Tabelle löste die SpringEL-Expression `payout.transferFee != null and payout.transferFee.signum() > 0` (Template-Zeile 511) eine `SpelEvaluationException: EL1008E: Property or field 'transferFee' cannot be found` aus, der Servlet warf eine `TemplateProcessingException` zurück → HTTP 500 für jeden Aufruf von `/operations/{id}`. Fix: `BigDecimal transferFee` als achtes Record-Component zwischen `shareAmount` und `payoutAmount` ergänzt (gleiche Position wie im Backend-DTO, damit Jackson auf Komponenten- *und* Position robust deserialisiert) plus den entsprechenden `@param`-Javadoc-Eintrag, der die Semantik (0,5 % des Brutto-Payouts, immer `>= 0`) und die neue Netto-Formel `personalExpenses + shareAmount − transferFee` dokumentiert — Wortlaut mit der Backend-Doku abgestimmt, damit zukünftige Leser nicht zwei Quellen vergleichen müssen. Kein manueller Konstruktor im Frontend (das DTO wird ausschließlich vom Backend-Call deserialisiert, vgl. [`OperationPageController`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/controller/OperationPageController.java)), kein neuer Test nötig — die Korrektur ist eine reine Feld-Synchronisation. **Lesson learned (für nächstes Mal als Reviewer-Memo):** Wenn das Backend-DTO ein neues Feld bekommt, das im Frontend-Template referenziert wird, muss der Frontend-Mirror-Record im selben Commit mitgepflegt werden — die Build-Pipeline kann diese Asymmetrie nicht fangen, weil Jackson stillschweigend unbekannte Felder ignoriert und Thymeleaf erst zur Render-Zeit ausgeschlagen kommt.

## [v0.1.6](https://github.com/krt-profit/basetool/releases/tag/v0.1.6) - 2026-05-16

### Changed

- **Admin-Systemeinstellungen (`/admin/settings`): Höhe der Operations-Überweisungsgebühr ist jetzt für Offiziere und Admins ohne Redeploy konfigurierbar.** Die mit dem vorigen Eintrag eingeführte 0,5-%-Gebühr lebte als hartkodierte Konstante (`TRANSFER_FEE_RATE`) im `OperationService` — jede Anpassung hätte einen Release-Cycle plus Hotfix-Tag erfordert. Da Star Citizens Bankgebühren historisch schon mehrfach gewechselt sind (und die Org sich auch für interne Steuerzwecke ein anderes Verhalten überlegen könnte), wandert der Wert in den existierenden `system_setting`-Key-Value-Store und wird auf der bestehenden Admin-Seite editiert. **Backend:** Neue Flyway-Migration [`V79__add_operation_transfer_fee_rate_setting.sql`](backend/src/main/resources/db/migration/V79__add_operation_transfer_fee_rate_setting.sql) seedet den Key `operation.transfer_fee_rate` idempotent (`ON CONFLICT DO NOTHING`) mit dem dokumentierten Default `0.005`. [`OperationService`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/OperationService.java) bekommt eine `SystemSettingService`-Abhängigkeit; die alte `TRANSFER_FEE_RATE`-Konstante heißt jetzt `DEFAULT_TRANSFER_FEE_RATE` und dient ausschließlich als Fallback. Eine neue private `resolveTransferFeeRate()`-Methode liest den Wert pro `getOperationPayouts`-Aufruf (PK-Lookup auf einer winzigen Tabelle, kein Caching nötig), parst ihn als `BigDecimal` und fällt defensiv auf den Default zurück wenn die Zeile fehlt, leer ist, nicht parsebar ist, negativ ist oder `>= 1` (was jeden Payout auslöschen würde) — jeder Fallback wird mit `log.warn` quittiert, damit Operatoren die Fehlkonfiguration im Access-Log sehen. **Frontend:** [`AdminSettingsPageController`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/controller/AdminSettingsPageController.java) bekommt einen vierten Setting-Block (Load + Save mit Optimistic-Lock-Version), wandelt zwischen DB-seitiger Dezimalbruchdarstellung (`0.005`) und nutzerfreundlicher Prozenteingabe (`0.5`) hin und her und validiert auf `[0, 100)` vor dem PUT. Der Backend-PUT auf `/api/v1/settings/operation.transfer_fee_rate` läuft über die existierende `@PreAuthorize("hasAnyRole('ADMIN', 'OFFICER')")`-gegatete `SystemSettingController.updateSetting`-Route, kein neuer Endpoint nötig. Das Template [`admin-settings.html`](frontend/src/main/resources/templates/admin-settings.html) hat unter „Raffinerie" eine neue `.data-section` „Operationen" mit einem `step="0.01"`-Number-Input plus dem üblichen versteckten Version-Feld. **i18n:** Drei neue Keys (`admin.settings.operations`, `admin.settings.operations.description`, `admin.settings.operations.transferFeeRate`) in [`messages.properties`](frontend/src/main/resources/messages.properties), [`messages_de.properties`](frontend/src/main/resources/messages_de.properties) (Umlaute als `\uXXXX`) und [`messages_en.properties`](frontend/src/main/resources/messages_en.properties). **Tests:** [`OperationServiceTest`](backend/src/test/java/de/greluc/krt/profit/basetool/backend/service/OperationServiceTest.java) bekommt einen `SystemSettingService`-Mock; bestehende Money-Tests laufen weiter gegen den 0,5-%-Fallback (kein expliziter Stub nötig). Fünf neue Tests sichern den Resolver ab: gültige nicht-Default-Rate aus Setting (`0.01` → 1 %), Blank-Fallback, Unparseable-Fallback, Out-of-Range-Fallback (`1.5`) und Edge-Case `0` (Gebühr explizit deaktivierbar). **Migrationshinweis:** Beim ersten Deploy nach diesem Commit setzt die Migration den DB-Default; bestehende Stacks ohne `operation.transfer_fee_rate`-Zeile rechnen weiterhin korrekt mit dem in-code Fallback, bis Flyway durchläuft.

- **Operation-Auszahlungspanel (`/operations/{id}`): Zusätzlicher Berechnungsschritt zieht pro Teilnehmer 0,5 % als Star-Citizen-Überweisungsgebühr vom Auszahlungsbetrag ab.** Star Citizens In-Game-Banking (Spectrum / mobiGlas) zwackt bei jedem aUEC-Transfer 0,5 % vom Empfängerbetrag ab, d. h. wer 1 000 aUEC angewiesen bekommt, sieht netto nur 995 aUEC im mobiGlas. Damit der in der Tabelle angezeigte Auszahlungsbetrag dem entspricht, was der Teilnehmer wirklich im Spiel bekommt, wird die Gebühr jetzt schon im Backend abgezogen, bevor die Zahl an die Tabelle in der Auszahlungen-Spalte geht. **Neue Formel:** `payoutAmount = personalExpenses + shareAmount − transferFee`, mit `transferFee = (personalExpenses + shareAmount) × 0,5 %` (HALF_UP auf zwei Nachkommastellen, wie alle anderen monetären Felder im Payout-Pfad). Die Gebühr gilt für `PAYOUT`- und `DONATE`-Teilnehmer gleichermaßen, weil `DONATE`-Teilnehmer ebenfalls einen In-Game-Transfer für ihre Auslagen-Rückerstattung bekommen — ihr (in Spende umgewandelter) Share-Betrag ist hingegen 0 und trägt damit nichts zur Gebühr bei. Wenn der Bruttoauszahlungsbetrag 0 ist (`DONATE` ohne Auslagen), bleibt auch die Gebühr 0; ein Phantom-Roundingartefakt entsteht nicht. **Backend:** Neue Konstante `TRANSFER_FEE_RATE = 0.005` in [`OperationService`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/service/OperationService.java); die Berechnung in `getOperationPayouts(UUID)` zerlegt den bisherigen `payoutAmount` in `grossPayout` → `transferFee` → `payoutAmount`. Das DTO [`OperationPayoutDto`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/model/dto/OperationPayoutDto.java) trägt jetzt ein zusätzliches `transferFee`-Feld (zwischen `shareAmount` und `payoutAmount`), so dass das Frontend die Zerlegung als Sub-Label „davon Überweisungsgebühr" anzeigen kann ohne sie nachzurechnen. OpenAPI-`@Operation`-Description am `GET /api/v1/operations/{id}/payouts`-Endpoint nennt das neue Feld + die geänderte Formel. **Frontend:** Die Tabelle in der Auszahlungen-Spalte hat unter dem bisherigen „davon Auslagen"-Sub-Label ein zweites „davon Überweisungsgebühr"-Sub-Label (`.payout-amount-transferfee`-Span), das nur erscheint wenn die Gebühr > 0 ist (analog zur Reimbursement-Logik); der bestehende `.payout-amount-reimbursement`-CSS-Block deckt beide Sub-Labels per Comma-Selector ab, kein doppelter Style. **i18n:** Neuer Key `operation.payout.amount.transferfee` in [`messages.properties`](frontend/src/main/resources/messages.properties), [`messages_de.properties`](frontend/src/main/resources/messages_de.properties) (deutsche Umlaute als `\uXXXX`-Escapes gemäß CLAUDE.md) und [`messages_en.properties`](frontend/src/main/resources/messages_en.properties). **Tests:** Sechs bestehende `GetOperationPayoutsAmountTests` in [`OperationServiceTest`](backend/src/test/java/de/greluc/krt/profit/basetool/backend/service/OperationServiceTest.java) auf die neuen Brutto/Netto-Beträge umgestellt (Einnahmen-only-Split, Mission-Expense-Reimbursement, Raffineriekosten-Reimbursement, `DONATE`-mit-Auslagen, Guest-Expenses, Null-Costs-Legacy-Path), inklusive expliziter `transferFee`-Assertions; zwei neue Tests `transferFee_isZero_whenGrossPayoutIsZero` (DONATE ohne Auslagen → Gebühr und Auszahlung beide exakt `0.00`) und `transferFee_roundsHalfUp_onUnevenGross` (333,33 × 0,5 % = 1,66665 → 1,67 HALF_UP → Netto 331,66) sichern die Rundung gegen Drift in zukünftigen Refactorings ab.

## [v0.1.4](https://github.com/krt-profit/basetool/releases/tag/v0.1.4) - 2026-05-16

### Changed

- **Operation-Detailseite (`/operations/{id}`): Auszahlungspanel zeigt jetzt pro Teilnehmer den konkreten Auszahlungsbetrag und einen Mission-Manager-Toggle „Bezahlt".** Die Tabelle in der Auszahlungen-Spalte führt zusätzlich zu Nutzer, Anteil und Auszahlungsart zwei neue Spalten: **Auszahlungsbetrag** (vorgerechnet als `personalExpenses + shareAmount`, mit optionalem Sub-Label „davon X aUEC Auslagen") und **Bezahlt** (Checkbox für Mission Manager / Officer / Admin, Ja/Nein-Anzeige für andere authentifizierte Nutzer). **Geld-Modell — Reimburse-First.** Aus den Operations-Bruttoeinnahmen werden zunächst die persönlichen Auslagen jedes Teilnehmers (Mission-`EXPENSE`-Einträge auf den Teilnehmer + die Kosten `expenses + otherExpenses` der Raffinerieaufträge, deren Owner der Teilnehmer ist) als Rückerstattung an ihn ausgezahlt, *bevor* das verbleibende Gesamtergebnis (= bisheriges `totalSum`) gemäß dem Zeit-Anteil-Prozentsatz auf alle `PAYOUT`-Teilnehmer verteilt wird. `DONATE`-Teilnehmer bekommen weiterhin ihre Rückerstattung (es ist ihr eigenes Geld zurück), spenden aber ihren Anteil an die Org. **Backend:** Neuer Entity-Typ [`OperationPayoutStatus`](backend/src/main/java/de/greluc/krt/profit/basetool/backend/model/OperationPayoutStatus.java) auf [`V78__create_operation_payout_status.sql`](backend/src/main/resources/db/migration/V78__create_operation_payout_status.sql) mit `(operation_id, participant_key)`-Unique-Constraint und FK auf `operation` (cascade DELETE) bzw. `app_user.id` für den Auditor (auf `SET NULL`). `participant_key` mirrort das opake Schema von `OperationPayoutDto.participantId` (User-UUID stringifiziert oder `guest_<name>`) — Absence einer Zeile heißt semantisch `paidOut=false`. `OperationService.getOperationPayouts` lädt nun zusätzlich Finanzeinträge + Raffinerieaufträge per Mission-ID-Batch, berechnet die per-Teilnehmer-Auslagen + Anteilsbetrag und fügt die Paid-Out-Audit-Felder (`paidOut`, `paidOutAt`, `paidOutByName`) ein; die DTO trägt diese drei plus `personalExpenses`, `shareAmount`, `payoutAmount`. Toggle-Endpoint `PUT /api/v1/operations/{id}/payouts/paid-out` (Body `{participantKey, paidOut}`) bewirtschaftet die Statuszeile als Last-Writer-Wins (boolean-only, kein Optimistic-Lock-Counter im Request); `paidOutAt`/`paidOutByUser` werden bei jedem `paidOut=true`-Toggle frisch gestempelt und bleiben beim Toggle-zurück als historische Audit-Trace erhalten. Gate: `hasRole('MISSION_MANAGER')` (greift via Rollenhierarchie auch für Admin/Officer und für den `app_user.is_mission_manager`-DB-Flag). **Frontend:** Mirror-DTO + neuer AJAX-Endpoint `/operations/{id}/payouts/paid-out` im `OperationPageController` proxien zum Backend; die Auszahlungen-Tabelle hat eine neue `.payout-paid-checkbox` mit CSP-konformem `addEventListener`-Handler, der den Aufruf absetzt, die Reihe optimistisch zurücksetzt + per `showFrontendErrorToast` informiert wenn er fehlschlägt, und auf Erfolg den `paidOutByName`-Subtitle in der Zelle aktualisiert. CSP-Nonce kommt vom existierenden `${cspNonce}`-Model-Attribut, CSRF-Header werden aus den `<meta>`-Tags der `fragments/head.html` gezogen. **i18n:** Fünf neue Keys (`operation.payout.amount`, `operation.payout.amount.reimbursement`, `operation.payout.paid`, `operation.payout.paid.error`, `operation.payout.paid.forbidden`) in `messages.properties`, `messages_de.properties` und `messages_en.properties`; deutsche Umlaute als `\uXXXX`-Escapes gemäß CLAUDE.md. **Tests:** `OperationServiceTest` um eine `GetOperationPayoutsAmountTests`-Nested-Klasse erweitert (Einnahmen-only-Split 50/50, Mission-Expense-Reimbursement, Raffineriekosten-Reimbursement an den Owner, `DONATE` behält Reimbursement aber keinen Share, Guest-Expenses an `guest_<name>`-Key, Null-Costs-Legacy-Path, `paidOut=false` ohne Status-Row, `paidOut=true` mit Auditor-Name) plus eine `SetPayoutStatusTests`-Nested-Klasse (Insert + Update + Toggle-Zurück-bewahrt-Audit + 404-bei-unbekannter-Operation + 404-bei-unbekanntem-Participant).

### Fixed

- **Wartungsseite: NPM-Container kommt jetzt sauber hoch + Verifikations-URL in `docs/deployment.md` korrigiert.** Beim ersten Produktiv-Rollout der Wartungsseite (s. „Added" weiter unten) sind zwei Folge-Probleme aufgetaucht, die ausschließlich am Übergang zwischen Repo-Stand und Server liegen — kein Bug im Render-Pfad: **(1) `:ro`-Flag an den Snippet-Mounts entfernt.** NPMs Startup-Skript [`/etc/s6-overlay/s6-rc.d/prepare/50-ipv6.sh`](https://github.com/NginxProxyManager/nginx-proxy-manager) iteriert nach Boot alle `*.conf` unter `/data/nginx/` und führt jeweils ein `sed -i` aus, um pro `listen <port>;`-Direktive einen `listen [::]:<port>;`-Eintrag fürs IPv6-Listening zu ergänzen. Mit unserem `:ro`-Bind-Mount auf `server_proxy.conf` schlug der erste Schreibversuch mit `Read-only file system` fehl, der `prepare`-s6-Service stieg mit Exit-Code 1 aus, und der NPM-Container ging gar nicht erst auf — Wartungsseite konnte folgerichtig nicht erscheinen, weil auch die reguläre Reverse-Proxy-Zustellung tot war. Fix: in [`docker-compose.yml`](docker-compose.yml) sind `./docker/maintenance/nginx/http.conf:/data/nginx/custom/http.conf` und `./docker/maintenance/nginx/server_proxy.conf:/data/nginx/custom/server_proxy.conf` jetzt **ohne** `:ro` gemountet; die statischen Assets unter `/usr/share/nginx/html/maintenance/` bleiben `:ro`, weil sie nicht unter `/data/nginx/` liegen und vom IPv6-Skript nicht angefasst werden. Da unsere Snippets keine `listen`-Direktiven enthalten, produziert `sed -i` byte-identischen Inhalt — der Schreibversuch geht durch, der File-Inhalt bleibt unverändert, der Container startet. Begründung steht als Inline-Kommentar im Compose-File neben den Mounts und im `docs/deployment.md`-Abschnitt „Where the files live". **(2) Verifikations-URL in `docs/deployment.md` korrigiert** von `https://basetool.iri-base.org/` auf `https://iri-base.org/` (Produktivfrontend läuft auf der Apex, kein Subdomain). Die `curl`-Befehle im „Verifying after a config change"-Block waren das einzige Vorkommen der falschen URL — die Nginx-Verdrahtung (`server_proxy.conf`, `http.conf`) ist host-agnostisch über `$host` und brauchte keine Anpassung, und die `maintenance.json` zeigte schon richtig auf `https://iri-base.org/problems/maintenance` (konsistent zu `AppProblemProperties.baseUri`).

- **`/promotion/manage`: Spaltenueberschriften der Bewertungskategorien fehlten komplett — die Topic-Gruppen-Header in der oberen Header-Zeile standen mit korrektem `colspan="N"`, aber die darunter liegende Kategorie-Zeile war zwischen den drei gepinnten Platzhalter-`<th>`s und dem `</tr>` leer; weder ein Spaltenkopf mit dem Kategorienamen noch das `data-pm-category-name`-Attribut, das die CSV-Export-Logik liest, landeten im gerenderten HTML**: Der Markup-Block fuer die Kategorie-Header-Zeile in [`frontend/src/main/resources/templates/promotion-manage.html`](frontend/src/main/resources/templates/promotion-manage.html) war `<th:block th:each="topic : ${topics}" th:with="topicId=${topic.id.toString()}"><th th:each="cat : ${categoriesByTopic[topicId]}" ...>`. Diese Konstellation — ein ueber `th:with` auf dem `<th:block>` abgeleitetes `topicId` als Schluessel fuer einen Map-Lookup auf einem genesteten `<th th:each>` — wertet die innere Map-Lookup in Thymeleaf 3.1 zu null/empty aus, obwohl der gleiche Map-Style (`categoryCountByTopic`) im Topic-Row-Header per inline `${categoryCountByTopic[topic.id.toString()]}` als `colspan`-Attribut tadellos den richtigen Wert liefert; auch eine MockMvc-Dump-Verifikation zeigt, dass die Topic-Zeile mit `colspan="2"` rendert, die Kategorie-Zeile darunter zwischen den Pin-Placeholdern aber nur Whitespace traegt. **Fix in zwei Teilen**: **(1)** Die Kategorie-Header-Zeile iteriert jetzt die flache `${categories}`-Liste (die der Controller in topic-then-sortOrder-Reihenfolge zusammenbaut und die die Body-`<td>`-Zeilen schon immer benutzen) und liest `cat.topicId` / `cat.topicName` / `cat.name` / `cat.description` aus `PromotionCategoryDto`. Damit ist die Kategorie-Zeile zell-zu-zell deckungsgleich mit den Body-Zeilen, ohne dass eine zweite Iterationsachse aufgebaut werden muss; die Topic-Row drueber behaelt ihre `colspan`-getriebene Gruppierung. **(2)** Im Bulk-Edit-Dropdown weiter oben im selben Template trug die `<optgroup th:each="topic" th:with="topicId=...">` + `<option th:each="cat : ${categoriesByTopic[topicId]}">`-Verschachtelung die gleiche Trunkierung: das `<optgroup label="Profit">` rendert, sein Body bleibt leer, der Offizier sieht den Themenbereich-Header aber keine Option zum Auswaehlen, sodass Bulk-Edit-Aktionen schon im Dropdown blockiert waren. Hier muss die topic-zentrierte Gruppierung erhalten bleiben, also wird `categoriesByTopic[topicId]` durch den expliziten Methodenaufruf `${categoriesByTopic.get(topic.id.toString())}` ersetzt und die `th:with`-Zwischenstation entfaellt. **Tests**: Neuer MockMvc-Regressionstest [`PromotionManagePageControllerMvcTest`](frontend/src/test/java/de/greluc/krt/profit/basetool/frontend/controller/PromotionManagePageControllerMvcTest.java) prueft sowohl, dass jede Kategorie als `<th>` mit `data-pm-category-name="<name>"` und dem Namen als Inhalt im gerenderten HTML steht, als auch dass das Bulk-Edit-`<optgroup>` die zugehoerigen `<option value="<uuid>">`-Eintraege enthaelt. Pre-Fix produzierte beide Asserts als Missing — eine Reintroduktion der broken `th:with`-Verschachtelung wuerde sofort am Build scheitern. Verifiziert per `./gradlew :frontend:test --tests "*Promotion*"` (alle gruen, inkl. der bestehenden `PromotionPageControllerTest`-Suite) und `./gradlew :frontend:spotlessApply :frontend:checkstyleMain :frontend:spotbugsMain` (BUILD SUCCESSFUL, keine neuen Findings). Andere Promotion-Templates (`promotion-overview`, `promotion-my-evaluations`, `promotion-admin-topics`) nutzen die `${topicCategoryMap[topic.id.toString()]}`-Form inline ohne `th:with`-Indirektion und sind nicht betroffen — per `grep` verifiziert.

### Security

- **Wartungsseite während Deploys: nginx-proxy-manager fängt Upstream-`502/503/504` ab und liefert eine KRT-gebrandete `503 Service Unavailable`-Seite statt der rohen Nginx-Default-Fehlerseite.** Trigger ist rein der Upstream-Status — sobald `scripts/deploy.sh` über `docker compose up -d --wait` den `backend`- und `frontend`-Container austauscht, fällt der Upstream für die Dauer zwischen „alter Container weg" und „neuer Container healthy" auf `502`; NPM antwortet in diesem Fenster mit der Wartungsseite und der bestehende `--wait`-Mechanismus räumt sie wieder weg, sobald der Healthcheck grün ist. Kein neuer Code im Spring-Stack, kein manueller Flag, keine Anpassung an `deploy.sh`. **Static Assets** unter [`docker/maintenance/static/maintenance.html`](docker/maintenance/static/maintenance.html) (standalone, keine externen Assets — Schriftarten, Logo-SVG und CSS sind inline, weil die KRT-Webfonts im Spring-Frontend-Container leben und exakt während eines Deploys nicht erreichbar wären) und [`docker/maintenance/static/maintenance.json`](docker/maintenance/static/maintenance.json) (RFC 7807 `application/problem+json`, `type=https://iri-base.org/problems/maintenance`, `retryAfterSeconds=60` — konsistent zur `AppProblemProperties.baseUri`). Die HTML-Variante ist responsive auf alle vier CLAUDE.md-Breakpoints (≤768/768–1024/1024–1600/1600+ px), bilingual DE/EN (beide Sprachen sichtbar, kein JS-Switcher → funktioniert auch ohne JS), zeigt das vereinfachte IRIDIUM-Emblem (orange-only, gemäß Corporate Design Manual), pulsierende „Maintenance"-Badge, und trägt ein `<meta http-equiv="refresh" content="30">` damit ein Tab nach 30 s automatisch wieder auf die echte App landet sobald der Deploy durch ist. **Nginx-Verdrahtung** über NPMs `/data/nginx/custom`-Hook: [`docker/maintenance/nginx/server_proxy.conf`](docker/maintenance/nginx/server_proxy.conf) (wird automatisch in jeden Proxy-Host-`server`-Block inkludiert) macht `error_page 502 503 504 =503 @maintenance` und definiert die `internal` `@maintenance`-Location mit `Retry-After: 60`, `Cache-Control: no-store` und einem `types { application/problem+json json; text/html html; }`-Override damit die `.json`-Variante korrekt als RFC-7807-MIME-Type ausgeliefert wird. Welche Variante an den Client geht, entscheidet die `map`-Direktive in [`docker/maintenance/nginx/http.conf`](docker/maintenance/nginx/http.conf): `Accept`-Header matcht auf `application/(problem\+)?json` → `maintenance.json`, sonst → `maintenance.html`. Damit bekommt ein Browser die gestylte HUD-Seite, AJAX/`fetch`-Calls des laufenden Frontends bekommen während des Deploy-Fensters ein parsable Problem-Document und können ihren bestehenden „Backend nicht erreichbar"-Toast sauber rendern. **`docker-compose.yml`**: Drei zusätzliche Read-only Bind-Mounts am `npm`-Service (`./docker/maintenance/static` → `/usr/share/nginx/html/maintenance`, beide Snippet-Dateien einzeln auf ihre Custom-Hook-Pfade); kein neuer Service, keine Image-Änderung. Scope: Das Snippet gilt global für alle Proxy-Hosts hinter NPM (also auch `keycloak.iri-base.org`) — Keycloak-Outages sind selten und werden nicht von `deploy.sh` ausgelöst, deshalb ist der Wortlaut bewusst generisch („System Maintenance"). **Doku**: Neuer Abschnitt „Maintenance page" in [`docs/deployment.md`](docs/deployment.md) (Mechanik, Verifikations-Kommandos via `docker compose stop frontend` + `curl -i`). Ein Browser-Test der HTML-Seite ist möglich, indem die Datei lokal geöffnet wird; die Nginx-Verdrahtung selbst muss serverseitig mit `docker compose exec npm nginx -t` validiert werden, da Docker auf der lokalen Dev-Maschine nicht verfügbar ist.

## [v0.1.3](https://github.com/krt-profit/basetool/releases/tag/v0.1.3) - 2026-05-16

### Changed

- **`README.md` neu strukturiert in fuenf klare Top-Level-Abschnitte ("Overview", "Project Documentation", "Deployment", "Development & Testing", "Technical Details") plus abschliessenden Lizenz-Hinweis**: Vorher waren Deployment-, Dev-Setup-, Test-Stack- und Tech-Details-Abschnitte ueber die ganze Datei verstreut und teilweise unter zwei verschiedenen Top-Level-Headlines (`## Getting Started` vs. `## Tech Stack`/`## Configuration`) zu finden — neue Leser mussten die README zweimal lesen, bevor klar war, was wo steht. **Reihenfolge**: (1) *Overview* mit einer Feature-Liste (Mission Planning, Hangar/Inventory, Refinery/Materials, Terminals, User Admin, Personal Inventory, i18n, Keycloak-Theme) und einem ASCII-Architektur-Diagramm Browser → Keycloak → Backend → Postgres/Redis, das die Modulgrenzen (Backend = REST only, Frontend = Thymeleaf + WebClient, kein direkter DB-/Keycloak-Admin-Zugriff) auf einen Blick sichtbar macht. (2) *Project Documentation* — neue Tabelle mit allen Community-Health-Files und Sub-Doc-Dateien (`CHANGELOG`, `CONTRIBUTING`, `CODE_OF_CONDUCT`, `.github/SECURITY.md`, `LICENSE`, `ROLES_AND_PERMISSIONS`, `Styleguide`, `docs/deployment.md`, `backend/.../db/migration/README.md`, `CLAUDE.md`, PR-Template, `impressum.md`), jeweils mit einer Zeile, was drinsteht. Bisher tauchte z. B. die Security-Policy nirgends im README auf, obwohl GitHub sie als Tab anzeigt. (3) *Deployment* — `.env`-Block (mit dem `IMPORTANT`-Hinweis zu Placeholder-Credentials von oben hochgezogen), Produktions-Pipeline (GHCR + systemd-Timer + Promote-Workflow) als Kurz-Abriss mit Verweis auf `docs/deployment.md`, und der lokale `docker compose --profile prod`-Pull-Mode. (4) *Development & Testing* — Prerequisites + Hinweis auf Gradle-Wrapper-Pflicht und refreshVersions, Local Development Setup (Gradle bootRun gegen Container-Dependencies), Full Dev Stack (GHCR-pull und Local-rebuild-Variante), kompletter Local Test Stack (`.env.test`, keytool-Rezept, `build-test-realm.py`, `docker-compose.test.yml`-Override, One-time-cleanup, Tear-down mit `--volumes`), Test-Befehle inkl. `--tests` Filter und ArchUnit-Hinweis, Lint-Befehle (`checkstyleMain`, `spotbugsMain`, `spotlessApply`, `cyclonedxBom`). (5) *Technical Details* — Tech Stack, Project Structure (alle Top-Level-Verzeichnisse: `backend`, `frontend`, `keycloak-theme`, `design`, `scripts`, `docs`), Configuration-Tabelle (um `IRI_BASETOOL_VERSION`, `IRI_IMAGE_NAMESPACE`, `IRI_KEYSTORE_HOST_PATH`, `REDIS_PASSWORD` erweitert), Session Persistence, Request Correlation & Structured Logging, API-Konventionen (RFC-7807-Error-Format + API-Versioning/Deprecation + Pagination/Sorting + UTC-Time-Handling konsolidiert), Keycloak-Theme. **Sprache vereinheitlicht**: Der bislang in Deutsch verfasste Abschnitt "Session Persistence (Redis)" (inkl. der `SsoReAuthenticationEntryPoint`-Erklaerung) wurde auf Englisch uebersetzt, damit die README sprachlich konsistent ist — passt zur projektweiten Regel, dass GitHub-Inhalte in Englisch verfasst werden, und entfernt einen sprachlichen Stolperstein direkt unter einer englischen Headline. **Entfernt**: Die dedizierte Top-Level-Sektion `## Personal Inventory` mit ihrer ~20-Zeilen-Feature-Tiefenbeschreibung (Pfade, Endpoints, Optimistic Locking, UEX-Datenquelle, i18n-Keys) — der Feature-Detailgrad gehoert in den Code/Changelog, nicht in eine Top-Level-README-Sektion neben "Tech Stack". Die Funktion ist jetzt eine Zeile in der Features-Liste der Overview-Sektion, die wichtigen Implementierungs-Details (Pfade, Endpoints) sind erhalten geblieben, aber in einer angemessenen Detailtiefe. **Verifikation**: README rendert sauber in GitHub's Markdown-Preview (Tabellen, Codeblocks, Links zwischen Sektionen ueber `#section-id`-Anker), alle relativen Links auf andere Dateien des Repos sind validiert, und der Anchor-Verweis von §3.3 auf §4.4 (Test-Keystore-Recipe) funktioniert ueber die neue Anchor-Konvention `#44-running-the-local-test-stack`. Reine Doku-Aenderung — kein Code-, Build-, Test- oder Config-Touch.

- **`/admin/terminals` und `/admin/uex-locations` zu einer hierarchischen Seite `/admin/uex-data` zusammengeführt**: Die beiden flachen Tabellen — eine fuer Terminals, eine fuer Cities/Stations/Outposts/POIs — pinten denselben `hasLoadingDock`-Override-Mechanismus auf unterschiedlichen Entity-Typen, sodass ein Admin beim Pruefen einer einzelnen Location ("hat Lorville einen Loading Dock und welche Terminals dort auch?") zwischen zwei Seiten und zwei Suchfeldern alt-tabben musste. **Neue Struktur:** Eine konsolidierte Seite gruppiert alle UEX-gespiegelten Entitaeten nach Sternsystem. Pro Sternsystem ein natives `<details>`-Disclosure (default `open`) mit vier Subsektionen — `Staedte`, `Raumstationen`, `Aussenposten`, `POIs` — sowie einer optionalen `Sonstige Terminals`-Subsektion fuer Terminals ohne City/Station-Zuordnung. Cities und Stations selbst sind wiederum `<details>` (default `closed`): das Summary traegt den Loading-Dock-Override-Cluster (UEX/Ja/Nein), das Body listet die zugehoerigen Terminals in einer Sub-Tabelle mit beiden Overrides (Loading-Dock und Auto-Load) plus dem Hidden-Toggle. Outposts und POIs bleiben Flach-Tabellen, weil bei ihnen keine Terminals haengen. **Terminal-Bucketing:** [`AdminUexPageController.buildHierarchy`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/controller/AdminUexPageController.java) matcht Terminals per `(starSystemName, cityName)` bzw. `(starSystemName, spaceStationName)` case-insensitive auf den jeweiligen Parent — die UEX-Daten zeigen hin und wieder Casing-Drift zwischen Terminal-Eintrag und Parent-Eintrag, sodass case-sensitives Matching ganze Terminal-Pulks in die Orphan-Liste fallen lassen wuerde. Terminals ohne `cityName` und ohne `spaceStationName` (free-floating, z.B. Orbit-Trade-Stationen) landen im `orphanTerminals`-Bucket pro System. Auch Terminals, die einen Parent-Namen tragen, aber kein passender City-/Station-Record existiert (UEX-Sweep-Reihenfolge: Terminals vor Parents auf frischer Installation), fallen sicherheitshalber in denselben Bucket — der Admin kann ihre Overrides damit weiterhin flippen, statt dass die Zeile von der Seite verschwindet. **Header-Chips + Sync-Stamp:** Im Page-Header sitzt der bisherige "Letzter UEX-Sync"-Zeitstempel (max ueber alle Terminal-Rows, weil ein Sweep alle Zeilen mit demselben Instant schreibt) plus sechs Summary-Chips (Sternsysteme, Cities, Stations, Outposts, POIs, Terminals) als Auf-einen-Blick-Ueberblick — der Admin sieht ohne Aufklappen, wieviel Stoff auf der Seite ist. Pro Sternsystem-Summary steht zusaetzlich `<location_count> Locations, <terminal_count> Terminals`, pro City/Station-Summary die Anzahl ihrer Terminals — Klicken lohnt sich, wenn die Zahl > 0 ist. **Filter:** Das globale Filter-Input matcht gegen den Sternsystem-Namen *und* gegen alle verschachtelten Location-/Terminal-Namen (`data-name`-Attribute); ein Sternsystem mit ≥ 1 passendem Kind wird automatisch aufgeklappt, sodass der Admin sofort sieht, was matcht, ohne manuell expandieren zu muessen. Leere Sternsysteme werden bei aktivem Filter ausgeblendet, ein leerer Filter resettet alles auf den Default-Zustand. **Disclosure-State-Persistierung:** Da die Override-Forms POST→302-redirect→GET fahren und `<details>` beim Reload den Default-Zustand wieder einnimmt, snapshotted ein kleines `sessionStorage`-Helfer-Pattern (`admin-uex-open-systems`/`admin-uex-open-parents`) die expandierten Disclosures vor jedem Submit und stellt sie nach dem Redirect wieder her (Default-open-Sternsysteme bleiben offen sofern der User sie nicht geschlossen hat; Default-closed-Parents bleiben geschlossen sofern der User sie nicht expandiert hat). Scroll-Position wird wie zuvor in `admin-uex-scroll-y` gespeichert. Die Override-Forms im Parent-Summary stoppen ihre Klick-/Submit-Bubble explizit, sonst wuerde jeder Button-Klick den umliegenden `<summary>` mit-toggle und die UI fuehlte sich kaputt an. **URL- und Sidebar-Umbau:** Die zwei alten Sidebar-Eintraege "Terminals" + "UEX-Orte" werden durch einen Eintrag "UEX-Daten" ersetzt, die alten URLs `/admin/terminals` und `/admin/uex-locations` sind weg (kein Redirect — die Seiten sind neu seit kurzem und nicht ausserhalb des Tools verlinkt). Der konsolidierte Dispatcher serviert `POST /admin/uex-data/{kind}/{id}/loading-dock?action=...` fuer alle fuenf Kinds (`cities`, `space-stations`, `outposts`, `pois`, `terminals`) plus `POST /admin/uex-data/terminals/{id}/auto-load?action=...` und `POST /admin/uex-data/terminals/{id}/toggle-visibility?hidden=...`; das Backend wird **nicht** angefasst, die bestehenden `/api/v1/.../loading-dock`- und `/loading-dock-override`-Endpoints bleiben 1:1 weiterverwendet. **Code-Aenderungen:** Neuer [`AdminUexPageController.java`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/controller/AdminUexPageController.java) mit den drei internen Records `StarSystemGroup`/`CityNode`/`SpaceStationNode` als View-Model fuer den Thymeleaf-Render-Pass; neues Template [`templates/admin/uex.html`](frontend/src/main/resources/templates/admin/uex.html) mit einem wiederverwendbaren `terminalRow(term)`-Thymeleaf-Fragment, das in den drei Stellen ausgegeben wird (City-Terminals, Station-Terminals, Orphan-Terminals), damit die Spaltenstruktur konsistent ist. Geloeschte Files: `AdminTerminalsPageController.java`, `AdminUexLocationsPageController.java`, `templates/admin/terminals.html`, `templates/admin/uex-locations.html`, beide zugehoerigen Tests. **Tests:** Neuer [`AdminUexPageControllerTest.java`](frontend/src/test/java/de/greluc/krt/profit/basetool/frontend/controller/AdminUexPageControllerTest.java) deckt alle Override-Dispatcher (5 Kinds x 3 Actions inklusive der Unknown-Kind/Unknown-Action-Rejects), `listData`-Parsing der UEX-Mirror-Felder + globalen `latestUexSync`, sowie die Hierarchie-Builder-Branches (City/Station-Match per Name, Case-Insensitivity, Free-Floating-Terminals -> Orphans, Terminals mit referenzlosem Parent -> Orphans, Multi-System-Gruppierung). **i18n-Schluessel-Diff:** Entfernt — `admin.terminals.terminals`, `admin.terminals.title`, `admin.uex_locations.title`, `error.admin.terminals.load`, `error.admin.uex_locations.load`, `nav.admin.terminals`, `nav.admin.uexLocations`. Hinzugefuegt — `admin.uex.title`, `admin.uex.systems.heading`, `admin.uex.system.unknown`, `admin.uex.orphanTerminals`, `admin.uex.empty.{systems,cities,stations,outposts,pois,terminals}`, `admin.uex.summary.{systems,locations,cities,stations,outposts,pois,terminals}`, `error.admin.uex.load`, `nav.admin.uex`, in allen drei Properties-Dateien (`messages.properties`/`messages_de.properties`/`messages_en.properties`, Umlaute im DE-/Default-File als `\uXXXX`).

### Removed

- **Repo-Root-Audit-Dokumente `ANALYSIS.md` (2026-05-11) und `PROJECT_REVIEW.md` (2026-05-12) entfernt**: Beide Dateien waren punktuelle Snapshot-Audits, deren Findings inzwischen entweder behoben oder in offene Follow-ups in `CLAUDE.md` / `CHANGELOG.md` ueberfuehrt sind; sie wurden zuletzt nicht mehr aktualisiert und drohten als veraltete "Single Source of Truth" zu wirken. Vorhandene Verweise auf die geloeschten Dateien wurden in `build.gradle.kts`, `backend/src/main/resources/application-test.yml`, `backend/src/main/resources/db/migration/README.md`, `frontend/.../RedisSessionConfigTest.java` und `backend/.../OperationServiceTest.java` so umformuliert, dass die jeweilige Begruendung im Kontext stehen bleibt, aber kein toter Link mehr auf die geloeschte Datei zeigt. Historische CHANGELOG-Eintraege, die auf `ANALYSIS.md` Abschnitte referenzieren, bleiben unveraendert (historisches Protokoll).

### Fixed

- **`/materials`: Kategorie-Header liessen sich nicht mehr ausklappen — der `+`-Button reagierte nicht auf Klicks, obwohl die Header sichtbar als anklickbar gerendert wurden**: Die Inline-Expression `const materialNames = /*[[${materials.![name]}]]*/ [];` am Anfang des `<script>`-Blocks von `frontend/src/main/resources/templates/materials.html` brach die Thymeleaf-3.1-Verarbeitung des gesamten umgebenden Skript-Blocks ab, sobald die `List<String>` aus `materials.![name]` in den JS-Kontext serialisiert wurde — der substituierte Wert `["Aluminum",…]` schluckte jedes weitere Template-Event bis zum schliessenden `</script>`, inklusive der `function toggleKindGroup(...)`-Definition und der `window.krtEvents.on('click', 'materials-toggle-kind', …)`-Registrierung. Die gerenderte HTML-Antwort endete mitten in der Zeile beim substituierten Array-Literal (`const materialNames = ["Aluminum"]` ohne abschliessendes `;`, ohne `</script>`, ohne `</body></html>`), so dass der Browser den Toggle-Click-Handler nie an die Kategorie-Header bindet. Empirisch verifiziert: die Trunkierung tritt auch bei der bare `[[${materials.![name]}]]`-Form (ohne Comment-Fallback) auf, der projektinterne `JavaTimeAwareJavaScriptSerializer` und Thymeleafs eigener `StandardJavaScriptSerializer` produzieren byte-identischen Output `["Aluminum"]` für `List<String>` — der Fehler liegt also in Thymeleafs nachgelagerter Pipeline, nicht im Serializer-Escaping. **Fix:** Material-Namen für das `krtAutocomplete`-Dropdown wandern in ein `<datalist id="materialNames-data">`-Element neben dem Filter-Input, das via `<option th:each="mat : ${materials}" th:value="${mat.name}">` befuellt wird. Das umgebende `<script>` verzichtet auf `th:inline="javascript"` (es brauchte den Modus nur fuer die jetzt entfernte List-Inlining-Stelle) und liest die Namen via `Array.from(document.getElementById("materialNames-data").options).map(opt => opt.value)`, wodurch Thymeleaf keinerlei `List`/`Collection` mehr in JS-Kontext serialisieren muss und das Skript komplett bis zur Bindung der Toggle- und Filter-Handler durchläuft. Neuer MockMvc-Regressionstest `MaterialsPageControllerMvcTest.listMaterials_rendersCategoryToggleBindingAndCompletesScript` pinnt sowohl die Praesenz der `materials-toggle-kind`-Registrierung als auch das Vorhandensein von `</body>` und `</html>` im Response, damit eine Reintroduktion des broken Inline-Patterns sofort am Build scheitert. Andere Frontend-Templates mit dem gleichen Pattern (`admin/locations`, `admin/materials`, `admin/terminals`, `admin/uex-locations`, `material-detail`, `ship-data`) sind als Cowork-Follow-up-Task gekennzeichnet — sie sind seit Einfuehrung des `JavaTimeAwareJavaScriptSerializer` ebenfalls von der gleichen Trunkierung betroffen (autocomplete und teils nachfolgende Handler wie `filterTable`, KRT-Confirm-Wrappers und Scroll-Restore broken), wurden aber bisher nicht gemeldet und sind ausserhalb des direkten Bug-Reports.

- **Follow-up zum `/materials`-Datalist-Fix oben: gleiche Thymeleaf-3.1-JS-inline-Trunkierung auf `/admin/locations`, `/admin/materials`, `/materials/{id}` und `/ship-data` behoben**: Die im vorigen Eintrag als Follow-up benannten vier Templates trugen alle das gleiche `/*[[${xxx.![name]}]]*/ []`-Pattern und damit die identische Trunkierung des umgebenden Script-Blocks. Konsequenz pro Seite: `/admin/locations` — `function filterTable(tableId, query)` nie definiert, der Tabellenfilter-Input ueber der Locations-Tabelle reagierte gar nicht. `/admin/materials` — die `data-krt-confirm`-Loeschbestaetigung der Kategorien-Tabelle wurde nie an die Form-Submits gebunden (Bestaetigung fiel auf native Browser-Dialoge zurueck, was die "no-native-dialogs"-UI-Regel verletzt) und der `'admin-materials-update'`-Delegated-Event-Handler fuer Quantity-/Category-/Refined-/Manual-Raw-/Job-Order-Aenderungen pro Materialzeile wurde nie registriert. `/materials/{id}` — das Terminal-Autocomplete oben rechts und die Click-Bindungen `'material-detail-sort'` (sortierbare Spalten) sowie `'material-detail-filter-terminals'` (Live-Filter) blieben aus. `/ship-data` — der zweite Autocomplete (Manufacturers) und `filterTable` blieben aus, dazu wurde der `'ship-close-modal'`-Handler des "Alle unfitted"-Bestaetigungsmodals nicht registriert, so dass das Modal nur ueber den Outside-Klick und nicht ueber den Abbrechen-Button schliessbar war. **Fix-Pattern (identisch zum `/materials`-Eintrag oben):** Jede `xxx.![name]`-Inline-Expression durch eine geschwister `<datalist id="xxxNames-data">` ersetzt, die per `<option th:each="...">` gefuellt wird; das Script liest sie ueber `Array.from(...options).map(o => o.value)`. `th:inline="javascript"` faellt am Script-Tag weg — Ausnahme: `admin/materials.html` behaelt es, weil die Single-Primitive-Inline-Expressions fuer `notification.success.save` / `notification.error.save` weiter im Toast-Flow gebraucht werden (Single-Primitives sind nicht von der Trunkierung betroffen). Drei neue MockMvc-Regressionstests ([AdminLocationsPageControllerMvcTest](frontend/src/test/java/de/greluc/krt/profit/basetool/frontend/controller/AdminLocationsPageControllerMvcTest.java), [AdminMaterialsPageControllerMvcTest](frontend/src/test/java/de/greluc/krt/profit/basetool/frontend/controller/AdminMaterialsPageControllerMvcTest.java), [ShipDataPageControllerMvcTest](frontend/src/test/java/de/greluc/krt/profit/basetool/frontend/controller/ShipDataPageControllerMvcTest.java)) und ein zweiter Testfall in [MaterialsPageControllerMvcTest](frontend/src/test/java/de/greluc/krt/profit/basetool/frontend/controller/MaterialsPageControllerMvcTest.java) (`getMaterialDetail_ShouldRenderFilterBinding_AfterDatalist`) pruefen je Template, dass ein Handler/Funktionsname NACH der Datalist-Stelle (`function filterTable(tableId, query)`, `'admin-materials-update'`, `'material-detail-filter-terminals'`, `'ship-close-modal'`) im gerenderten HTML ueberlebt und die Response `</html>` enthaelt — beides war im Pre-Fix-Zustand abgeschnitten. Die ebenfalls im Follow-up-Vermerk genannten `admin/terminals.html` und `admin/uex-locations.html` sind hier nicht mehr Teil des Batches: sie wurden in Commit `ebb2cb8 feat(admin/uex): consolidate terminals + uex-locations into hierarchical /admin/uex-data` in das neue Template `admin/uex.html` zusammengefuehrt, das aktuell keine List-Inline-Expression mehr enthaelt — kein zusaetzlicher Datalist-Block dort noetig.

- **SpotBugs `NP_NULL_PARAM_DEREF` in `BackendServiceException.fromProblem` aufgeloest**: Die statische Factory-Methode konstruierte das Exception-Message-Praefix per `"Backend returned " + status + (code != null ? " [" + code + "]" : "")` und reichte `code` anschliessend an den Konstruktor-Parameter `@NotNull String problemCode` weiter. Die defensive `code != null`-Pruefung war ueberfluessig — `deriveCodeFromStatus(...)` ist `@NotNull` und Jacksons `asText(default)` faellt nie auf `null` zurueck —, signalisierte SpotBugs aber, dass der Autor `null` fuer erreichbar haelt, was zur `@NotNull`-Annotation am Konstruktor in Konflikt stand. Im CHANGELOG wurde dieses Finding seit mehreren Iterationen als "bekannt, pre-existing" akzeptiert. Fix: die `!= null`-Branch ersatzlos entfernt und der gesamte SpotBugs-Lauf auf dem Frontend-Modul auf **0 Findings** zurueckgesetzt (`./gradlew :frontend:spotbugsMain` -> sauberer Report, vorher 1 Finding seit `2426983 docs: add Javadoc to frontend config, filter, logging, exception, service`). Reine 1-Zeilen-Aenderung in [BackendServiceException.java:160](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/service/BackendServiceException.java:160) plus erklaerender Kommentar; kein Verhaltenswechsel, keine Test-Aenderung.

### Security

- **Einsatz-Detailseite (`/missions/{id}`): Presence/Awareness in Echtzeit (Stufe 3).** Wer gerade welche Sektion eines Einsatzes bearbeitet, ist jetzt für alle anderen authentifizierten Betrachter desselben Einsatzes sichtbar — ein dezenter pulsierender Orange-Indikator mit Editor-Anzahl erscheint neben dem entsprechenden Panel-Header und führt im Tooltip die Namen der aktiven Editor:innen auf. Reine Awareness, keine Locks: das Verhalten der Optimistic-Lock-Counter aus Stufe 1 und der Section-Patch-Routes bleibt unverändert; Saves werden niemals blockiert. **Backend (Frontend-Modul):** Neuer nativer WebSocket-Endpoint unter [`/ws/missions/{id}/presence`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/config/MissionPresenceWebSocketConfig.java) (`spring-boot-starter-websocket`; kein STOMP, keine SockJS). Der Endpoint ist in [`SecurityConfig`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/config/SecurityConfig.java) als `authenticated()` markiert — anonyme Gäste können weder als Editor sichtbar werden noch sehen, wer editiert. Stateful: [`MissionPresenceService`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/service/MissionPresenceService.java) hält pro Einsatz und Sektion eine Map `userId → (displayName, lastHeartbeat)`, TTL 30 Sekunden, Entries werden vom Reaper (`MissionPresenceWebSocketHandler.tickReaper`, 10-Sekunden-Tick) gepflegt. **Single-Instance-only** für jetzt — das Javadoc des Services dokumentiert den Umstieg auf Redis-Pub/Sub als Folge-Schritt, sobald die Frontend-Instanz horizontal skaliert. **Wire-Format:** Client sendet `{type:"focus"|"blur"|"heartbeat", sectionKey}`, Server broadcastet `{type:"presence", sections:{<key>:[{userId,displayName}]}}`. Multiple Tabs eines Users auf demselben Einsatz wischen sich nicht gegenseitig: ein `afterConnectionClosed` clear-t nur, wenn keine andere Session desselben Users mehr aktiv ist. **Frontend-Client:** [`mission-presence.js`](frontend/src/main/resources/static/js/mission-presence.js) öffnet die Socket beim Page-Load (nur für angemeldete Nutzer), trackt `focusin`/`focusout` auf `[data-panel-key]`-Containern, sendet Heartbeats alle 10 s, blurt auf `visibilitychange` (Tab wechseln), reconnected mit exponentiellem Backoff bis 30 s und filtert den lokalen User aus der Anzeige. **UI:** Pulsierender Orange-Punkt im KRT-Brand `#E77E23` mit Box-Shadow-Animation `@keyframes krt-presence-pulse` plus Editor-Count-Chip; CSS-Klassen `.krt-presence-indicator`, `.krt-presence-dot`, `.krt-presence-count` in [styles.css](frontend/src/main/resources/static/css/styles.css). **i18n:** Zwei neue Keys `mission.presence.editing.solo` und `mission.presence.editing.multi` in `messages.properties`/`_de`/`_en`. **Tests:** [`MissionPresenceServiceTest`](frontend/src/test/java/de/greluc/krt/profit/basetool/frontend/service/MissionPresenceServiceTest.java) deckt Touch/Clear/ClearAll/Reaper/Snapshot-Immutability ab; [`MissionPresenceWebSocketHandlerTest`](frontend/src/test/java/de/greluc/krt/profit/basetool/frontend/websocket/MissionPresenceWebSocketHandlerTest.java) treibt den Handler über `TestWebSocketSession` durch alle Message-Typen plus den Multi-Tab-Same-User-Edge-Case.

- **Einsatz-Detailseite (`/missions/{id}`): konsistente Per-Sub-Resource Version-Synchronisierung (Stufe 2).** Folge-Klicks auf eine Sub-Resource (Teilnehmer-Zeile, Frequenz-Reihe) nach einer AJAX-Änderung an derselben Sub-Resource lösen jetzt keinen spurious 409 mehr aus. Bisher führte z. B. ein Wechsel des Payout-Preference-Dropdowns dazu, dass der `data-version`-Stempel nur auf dem Edit-Button der Zeile aktualisiert wurde — andere `data-version`-tragende Elemente in derselben `<tr>` (insbesondere das Edit-Modal beim nächsten Öffnen) behielten den Pre-Save-Wert und führten beim nächsten Save in einen 409, obwohl niemand anderes editiert hatte. **Template:** Die Teilnehmer-Zeile `<tr th:each="p : ${participants}">` trägt jetzt `data-participant-id` + `data-version` als Container-Stempel; der Frequenz-Reihen-Container `<div class="flex-gap-sm" data-freq-type-id="…">` trägt zusätzlich ein `data-version`. **JS:** Die Funktion `updatePayoutPreference` ([mission-detail.html](frontend/src/main/resources/templates/mission-detail.html)) ruft nach erfolgreichem Save jetzt `MissionSubresource.syncVersion('tr[data-participant-id="…"]', updatedParticipant.version)` auf und propagiert damit die frische Version atomar auf jedes `data-version`-Attribut in der Row (Container, Edit-Button, Modal-Triggers). Die Frequenz-Update-Logik synchronisiert ebenfalls das `data-version` des Frequenz-Containers aus dem Slim-Response. **Backend:** Keine Änderungen — die Slim-Endpoints liefern bereits die jeweiligen Sub-Resource-Versionen mit, das war reine Frontend-DOM-Konsolidierung.

- **Einsatz-Detailseite (`/missions/{id}`): feingranulares Optimistic Locking pro Sektion (Stufe 1).** Zwei User können jetzt gleichzeitig disjunkte Bereiche derselben Mission bearbeiten — z. B. Person A passt die Stammdaten (Name/Beschreibung/Status/Operation) an, Person B trägt parallel die Ist-Zeiten ein —, ohne dass einer der beiden Saves mit HTTP 409 abgewiesen wird. Bisher prüften die Section-PATCH-Endpoints `/api/v1/missions/{id}/core`, `/schedule` und `/flags` alle gegen denselben globalen `Mission.@Version`-Zähler, sodass jede schreibende Aktion an einer Sektion *alle* offenen Editor-Formulare auf der Mission invalidierte. **Datenmodell:** Drei zusätzliche `BIGINT NOT NULL DEFAULT 0` Spalten auf `mission` via [`V77__add_mission_section_versions.sql`](backend/src/main/resources/db/migration/V77__add_mission_section_versions.sql) — `core_version`, `schedule_version`, `flags_version`. Die `Mission.@Version` bleibt als technisches Sicherheitsnetz für die restlichen, nicht in einer Sektion gespiegelten Felder erhalten; alle drei neuen Counter sind via `@OptimisticLock(excluded = true)` markiert, damit ihr Hochzählen die globale Version nicht mit-bumpt. Zusätzlich ist `Mission.operation` jetzt ebenfalls `@OptimisticLock(excluded = true)`, weil eine Operation-Änderung jetzt zur Core-Section gehört. **Service:** `MissionService.updateCoreSection`, `updateScheduleSection`, `updateFlagsSection` prüfen nun den jeweils dedizierten Sektions-Counter (`assertCoreVersion`, `assertScheduleVersion`, `assertFlagsVersion`) und inkrementieren ihn nach erfolgreichem Schreibzugriff. Der Full-PUT (`updateMission`) bumpt weiterhin alle drei (Full-Replace-Semantik). **Status-Auto-Transition:** Beim Statuswechsel `PLANNED → ACTIVE` über den Core-Patch wird `actualStartTime` weiterhin automatisch gestempelt — neu ist, dass dies über die private `bumpActualStartTimeOnActivationWithinTransaction(...)`-Methode geht, die zusätzlich den `scheduleVersion`-Counter bumpt (CLAUDE.md `…WithinTransaction`-Pattern). Damit bekommen offene Schedule-Editoren beim nächsten Save ein 409 statt einer leisen Überschreibung des Auto-Stamps. **API:** `PatchMissionCoreRequest` trägt zusätzlich ein optionales `operationId`; die `version`-Felder der drei Patch-DTOs entsprechen jetzt semantisch dem jeweiligen Section-Counter (Wire-Format unverändert). `MissionDto` liefert die drei Section-Versionen zusätzlich zum globalen `version`. **Frontend:** [`mission-detail.html`](frontend/src/main/resources/templates/mission-detail.html) hat statt eines einzigen `#mission-version` jetzt zusätzlich drei Hidden-Inputs `#mission-core-version`, `#mission-schedule-version`, `#mission-flags-version`. Der Form-POST `/missions/{id}` in [`MissionPageController`](frontend/src/main/java/de/greluc/krt/profit/basetool/frontend/controller/MissionPageController.java) splittet die Eingabe in drei sequentielle PATCHes (Schedule → Core → Flags), jeder mit dem zugehörigen Counter. Die JS-Funktion `saveActualTimeAndReload` (Knopf „Jetzt") liest jetzt aus `#mission-schedule-version` und ruft den Schedule-PATCH direkt auf — kein Full-PUT mehr und keine versehentliche Mission-`@Version`-Bewegung. **Tests:** Erweiterung von `MissionServiceSectionPatchTest` u. a. um den Lackmus-Test `sectionPatches_acrossDisjointSections_doNotInvalidateEachOther` (zeigt, dass nach einem Core-PATCH eine mit alter `flagsVersion` geladene Flags-Form noch erfolgreich submitten kann), Stamp-Test für die Auto-Transition `updateCoreSection_shouldAlsoBumpScheduleAndStampActualStart_whenStatusTransitionsToActive`, und je 409-Test pro Section.

## [v0.1.2](https://github.com/krt-profit/basetool/releases/tag/v0.1.2) - 2026-05-16

### Fixed

- **`/promotion/manage`: Eingeklappte Themenbereich-Spalten waren nicht mehr erreichbar — das `+`-Symbol stand sichtbar in der Header-Zeile, ein Klick darauf hatte aber keine Wirkung mehr, weil dort visuell bereits die Kategorie-Spalte des nächsten Themenbereichs lag**: Die bestehende Collapse-Logik (JS `pmSetTopicCollapsed`) reduzierte beim Einklappen den `colspan` des Themenbereich-Headers von N (Anzahl seiner Kategorien) auf 1 UND setzte die N Kategorie-Zellen sowie die N Datenzellen pro Body-Zeile auf `display: none`. `display: none` entfernt eine Zelle komplett aus dem Tabellen-Layout, inklusive ihres Spaltenslots — die Themenbereich-Zeile verliert dadurch nur `N-1` Slots (von colspan-Reduktion), die Kategorie- und Body-Zeilen aber `N` Slots, also genau einen Slot Versatz pro eingeklapptem Themenbereich. Der Browser kompensiert das, indem er die Zellen des nächsten Themenbereichs in der Kategorie-/Body-Zeile um einen Slot nach links zieht — visuell landet damit "Kategorie B1" unterhalb des `+`-Triggers von "Themenbereich A", und der Klick-Target des A-Headers liegt nicht mehr dort, wo das `+`-Symbol gerendert wird. Zusätzlich war der nominell vorhandene Klick-Target zu schmal: der eingeklappte Header ist 3rem (48px) breit, das `<th>` behielt aber sein default `padding: 0.5rem 0.75rem` (24px horizontal), sodass die Schaltfläche darin (`width: 100%` der TH-Content-Area) nur ~24px breit war und mittig im Cell sass — das umliegende TH-Padding sah klickbar aus, war es aber nicht. **Fix in drei Teilen:** **(1)** Das Hide-Verhalten der Kategorie-/Body-Zellen wechselt von `display: none` zu `visibility: hidden` plus `width: 0; min-width: 0; max-width: 0; padding: 0; border-right: 0`. Die Zellen bleiben damit Teil des Tabellen-Grids — ihre Spaltenslots existieren weiterhin, nur das Rendering hat Nullgrösse —, der Browser kann nichts mehr verschieben. **(2)** Die `pmSetTopicCollapsed`-JS-Funktion ändert den `colspan` des Themenbereich-Headers nicht mehr (das gesamte `originalColspan`-Backup-Block und `setAttribute('colspan', ...)` ist entfernt). Damit hat die Themenbereich-Zeile beim Einklappen die gleiche Anzahl Spaltenslots wie zuvor — der Themenbereich-Header spannt nominell weiterhin N Spalten, ist aber visuell nur 3rem breit (CSS-Force über `data-pm-collapsed="true"`, der Browser verteilt die 3rem proportional auf die N Spalten, weil die einzelnen Kategorie-Zellen darin auf width 0 gezwungen sind). **(3)** Klick-Target des `+`-Triggers vergrössert: TH-Padding im eingeklappten Zustand auf `0` gesetzt (war default `0.5rem 0.75rem`), Toggle-Button `width: 100%; min-height: 44px` (matched die Touch-Target-Vorgabe aus CLAUDE.md "minimum click target 44px"), das `+`/`−`-Glyph auf `font-size: 1.1rem` (war default 0.8rem aus dem Topic-Row-Font). Die volle 3rem-Spalte ist damit klickbar, statt nur dem inneren 24px-Button. Reine Template-/CSS-/JS-Änderung in `frontend/src/main/resources/templates/promotion-manage.html`, kein Backend-/Controller-/i18n-Touch; keine Tests betroffen, da kein Test die Collapse-DOM-Manipulationen dieser Seite asserted.

- **`/promotion/manage`: Header-Titelzeilen waren bei den angepinnten Spalten (Checkbox / Mitglied / Beförderbar) anders hoch als bei den scrollbaren Spalten (Themenbereich + Kategorie)**: Die drei links-gepinnten `<th>`-Zellen waren mit `rowspan="2"` UND `position: sticky` gleichzeitig deklariert. Browser (insbesondere Chromium) reconcile die Höhe einer sticky+rowspan-Zelle nicht zuverlässig mit der Summe der beiden Zeilen, die sie eigentlich überspannt — Folge: die untere Kante der angepinnten Kopf-Spalten lag sichtbar über bzw. unter der unteren Kante der Kategorie-Zeile in den beweglichen Spalten, der Datensatz darunter wirkte gegen die Pin-Zelle ausgerichtet, gegen die Kategorie-Zelle aber versetzt. **Fix**: In `frontend/src/main/resources/templates/promotion-manage.html` das `rowspan="2"` von den drei Pin-Zellen entfernt und jeweils eine leere `<th class="… aria-hidden="true">`-Zelle in `tr.pm-row-category` ergänzt — jede Header-Zeile hat damit ihre natürliche, vom Browser über alle Spalten hinweg synchronisierte Höhe, und das Sticky-Layout muss keine rowspan-Geometrie mehr auflösen. Damit die Pin-Spalte trotz der zwei separaten Zellen weiterhin als eine durchgehende Zelle gelesen wird, bekommen die Topic-Zeilen-Pin-Zellen `border-bottom: none` (sonst läge mittig in der Pin-Spalte ein 1px-Primärstrich, der die optische Einheit zerschneiden würde) und die Kategorie-Zeilen-Pin-Spacer `padding: 0` (damit die leere untere Hälfte nicht durch das Standard-`th`-Padding künstlich wächst und die Kategorie-Zeile darüber hinaus dehnt). Bottom-Border (2px Primärfarbe) und Right-Border der Eligibility-Zelle (2px Primärfarbe als Trenner zur scrollbaren Region) bleiben unberührt; sticky-`left`-Offsets und z-indexes (41/40/39 vs. 20 in der scrollbaren Region) gelten weiterhin für beide Pin-Zellen-Paare, weil die zugehörigen Selektoren auf `.pm-checkbox-cell`/`.pm-member-cell`/`.pm-eligibility-cell` matchen — unabhängig davon, in welcher Header-Zeile die Zelle steht. Reine Template-/CSS-Änderung, kein Backend-/Controller-/i18n-/JS-Touch; keine Tests betroffen, da kein Test die Header-DOM-Struktur dieser Seite asserted.

### Security

- **Security-Policy als `.github/SECURITY.md` ergaenzt**: GitHub erkennt die Datei und blendet damit den "Security policy"-Tab im Repo-Header sowie den "Report a vulnerability"-Button im Security-Tab ein. Inhalt: praeferierter Report-Kanal ueber GitHub Private Vulnerability Reporting (passt zum bereits in `.github/ISSUE_TEMPLATE/config.yml` verlinkten `/security/advisories/new`), Response-Targets (Acknowledge 7 Tage, Triage 14 Tage, Fix High/Critical 90 Tage, koordinierte Disclosure 14 Tage nach Release), Supported-Versions-Tabelle fuer die `0.x`-Reihe und die GHCR-Container-Tags (`:edge`, `:sha-<short>`, `:stable`), explizite Scope-Liste (in: Quellcode, Workflows, GHCR-Images `ghcr.io/krt-profit/basetool-{backend,frontend}`, Compose-/Deploy-Scripts, mitgelieferter Keycloak-Realm/-Theme; out: Upstream-CVEs ohne Fix-Pfad, Fehlkonfig fremder Self-Hosted-Instances, Scanner-Spam ohne reproduzierbaren Impact, Single-Tenant-DoS), Aufzaehlung der architekturspezifischen Hotspots (Cross-Tenant-Leaks via JWT-`sub`-Filter, `cleanupForGuest`-Bypass, fehlende `@PreAuthorize`, Optimistic-Locking-Bypass, Injection, SSRF ueber UEX/WebClient, sensible Daten in Logs, Supply-Chain-Eingriffe in `release-images.yml`/`promote.yml`), Safe-Harbor-Klausel fuer Good-Faith-Research, und ein Verifikations-Abschnitt zur Cosign-Keyless-Signatur + SLSA-Provenance + SPDX-SBOM, die an jedes GHCR-Image als OCI-Attestation gehaengt werden (Identity-Regex aus `promote.yml`: `release-images.yml@refs/.+` ueber `token.actions.githubusercontent.com`). Datei liegt unter `.github/` passend zur bestehenden Konvention der Community-Health-Files (PR-Template, Issue-Templates, `dependabot.yml`); GitHub findet sie dort genauso wie im Repo-Root oder unter `docs/`. Reine Doku-Aenderung, kein Code-/Build-/Test-Touch.

## [v0.1.1](https://github.com/krt-profit/basetool/releases/tag/v0.1.1) - 2026-05-16

### Fixed

- **Eligibility-Auswertung zaehlte Kategorien doppelt: zwei topic-scoped Rangvoraussetzungen im selben Topic teilten sich ihren Kategorie-Pool, statt ihn disjunkt aufzuteilen**: Das Beförderungskonzept der Staffel verlangt z.B. "3x Stufe B in Grundlagen-Kategorien plus 1x Stufe A in einer ANDEREN Grundlagen-Kategorie" — die A-Anforderung muss sich also auf eine vierte, von den drei B-Kategorien verschiedene Kategorie stuetzen. Die alte `PromotionEligibilityService.evaluateTopicRequirement(...)` hat aber jede Anforderung unabhaengig ueber `levelByCategory.entrySet()` iteriert und einfach gezaehlt, wieviele Kategorien das Mindestlevel erreichen — ohne Buchhaltung darueber, welche schon einer anderen Regel zugeordnet sind. Folge: drei B-bewertete Grundlagen-Kategorien haben sowohl "3x B" (achieved=3) ALS auch "1x A" (achieved=3, weil B in der A < B < C-Ordnung trivial A erfuellt) befriedigt — der Nutzer war faelschlich beförderbar, obwohl es gar keine vierte Kategorie gibt. Die Doppelzaehlung war im UI sichtbar als Zeilen wie "Grundlagen | A | 2 / 1" auf `/promotion/my-evaluations`, wo die "2" alle B/C-Bewertungen mitgezählt hat, die eigentlich von der strengeren B-Regel beansprucht werden. **Fix**: Neue Pre-Pass-Methode `evaluateTopicScopedDisjoint(...)` in `PromotionEligibilityService`, die alle topic-scoped Anforderungen pro Topic gruppiert, absteigend nach `minimumLevel` sortiert (strikteste zuerst — `LEVEL_C > LEVEL_B > LEVEL_A`) und dann greedy `requiredCount` Kategorien pro Regel reserviert. Eine Kategorie zaehlt damit fuer höchstens eine topic-scoped Regel desselben Topics. Strictest-first ist beweisbar optimal, weil eine höherstufige Kategorie sowohl strikte als auch lockere Slots fuellen kann — gibt man dem strikteren Slot first pick, verschwendet man keine hohe Kategorie an einen Slot, den eine niedrige genauso gut fuellen koennte. `evaluateForRanks(...)` ruft den Pre-Pass auf, mischt das Ergebnis fuer topic-scoped Regeln in den Ergebnis-Pfad, und behandelt category-scoped sowie globale Regeln weiterhin unabhaengig per `evaluateRequirement(...)` (welcher topic-scoped Eingaben jetzt mit `IllegalStateException` ablehnt, damit eine künftige "Vereinfachung" die Disjointness nicht versehentlich aushebelt). **Bewusste Ueberlappung bleibt erhalten**: category-scoped Regeln (z.B. "Anwesenheit muss B sein") reservieren ihre Kategorie NICHT — die Anwesenheit-Kategorie zaehlt also weiterhin sowohl fuer die category-scoped "Anwesenheit B"-Regel als auch als eine der drei B-Kategorien fuer das Topic-Aggregat, exakt wie der bestehende Test `evaluateForRanks_shouldCountAnwesenheitAIntoTopicAggregate` pinnt und das Beförderungskonzept ("Anwesenheit darf in den vier vorgenannten Kategorien sein") explizit erlaubt. **UI-Auswirkung**: die `achievedCount`/`requiredCount`-Anzeige pro Topic-Regel wird semantisch ehrlicher — statt "2 / 1" steht jetzt "1 / 1", weil die zweite Kategorie schon der strikteren Regel zugeordnet ist und nicht mehr doppelt zaehlt. Der Wert ist auf `requiredCount` gedeckelt, sobald die Regel erfuellt ist (Reservierung stoppt sofort, kein Over-counting). **Tests**: ein bestehender Test (`evaluateForRanks_shouldCountCategoriesInTopic_whenTopicScopedRequirement`) wurde angepasst, weil sein 3/2-Assert die alte Over-counting-Semantik gepinnt hat — neue Erwartung: 2/2. Sechs neue Mockito-only Tests pinnen die Disjoint-Logik: (1) `shouldEnforceDisjointMatching_betweenTopicScopedRulesInSameTopic` — der konkrete User-Spec-Fall (3 B-Kategorien + Regeln "3xB" + "1xA" → A-Regel scheitert, weil keine vierte Kategorie da ist); (2) `shouldSatisfyDisjointTopicRules_whenFourDistinctCategoriesProvided` — vier Kategorien (3 B + 1 A) → beide Regeln satisfied; (3) `shouldReserveHigherLevelForStricterRule_inDisjointMatching` — strict-first-Ordering, auch wenn Repository-Order umgekehrt ist; (4) `shouldStillSatisfyLooserRule_whenStricterRuleNotFullyMet` — lockere Regel ist nicht "either-or" mit der strengeren, sie kann mit den uebrigen Kategorien noch satisfied sein; (5) `shouldAllowCategoryRuleToOverlapWithTopicAggregate_underDisjointMatching` — Anwesenheit-B zaehlt fuer beide Regeln; (6) `shouldKeepDisjointMatchingPerTopic_notAcrossTopics` — Disjointness ist Within-Topic, "1xB Grundlagen" + "1xB Spezialisierungen" konkurrieren nicht. Plus eine Helper-Methode `checkForRequirement(result, req)` die per `requirementId` filtert, damit die Tests nicht auf List-Index-Ordering verlassen muessen. Verifiziert per `./gradlew :backend:test --tests "PromotionEligibilityServiceTest"` (alle Tests gruen, inkl. der bisherigen acht) und `./gradlew :backend:spotlessApply :backend:checkstyleMain :backend:spotbugsMain` (BUILD SUCCESSFUL, keine neuen Findings).

- **Fortschrittsanzeige auf `/promotion/my-evaluations` blieb pro Eligibility-Karte immer bei `0 / 0`, auch wenn einzelne Kategorien oder ganze Rangvoraussetzungen schon erfuellt waren**: In `frontend/src/main/resources/templates/promotion-my-evaluations.html` wird das Aggregat oberhalb der Checks-Tabelle (`<span class="me-progress-text">0 / 0</span>` auf Zeile 265) nur hardcoded geseedet und client-seitig durch `meFillProgressBars()` (Zeile 409-433) gesetzt — die Funktion summiert die "Erfuellt"-Spans aus jeder Tabellenzeile auf. Der Spans-Selector war aber `[class^="check-status-"]` (Attribut-Prefix-Selector). Die Spans selbst auf Zeile 318-319 sind per `<span th:classappend="${chk.satisfied} ? ' check-status-ok' : ' check-status-missing'">` definiert ohne initiales `class`-Attribut. Thymeleafs `th:classappend` legt das `class`-Attribut in diesem Fall direkt mit dem unveraenderten Ausdruck an — also `class=" check-status-ok"` mit fuehrendem Leerzeichen, weil beide Ternaer-Aeste das Leerzeichen mitfuehren (analog zu den anderen `th:classappend`-Aufrufen im selben Template, die alle mit `' is-foo'` arbeiten weil sie an ein existierendes `class` anhaengen). Der Attribut-Prefix-Selector vergleicht buchstaeblich gegen den Attribut-Wert und matcht daher nicht — `.classList`-basiertes CSS (`.check-status-ok { color: ... }` auf Zeile 148-149) funktioniert weiter, weil die Klassenliste tokenisiert ist, das visuelle Rot/Gruen pro Zeile war also nie kaputt; nur das Summieren oben fiel auf 0/0 zurueck (`achieved=0`, `required=0`, JS schreibt das Ergebnis dann ueber den Seed-Wert). **Fix**: Einzeiler im JavaScript — `[class^="check-status-"]` durch zwei Klassen-Selektoren ersetzt (`.checks-table tbody tr .check-status-ok, .checks-table tbody tr .check-status-missing`), zusammen mit einem Block-Kommentar oberhalb der Zeile der den Mechanismus dokumentiert, damit die naechste Person die das auf `[class^=...]` zurueckdrehen will weiss warum es load-bearing ist. Kein Backend-/DTO-/i18n-/Service-Touch noetig; das Aggregat bleibt aus historischen Gruenden im Client (siehe Block-Kommentar auf Zeile 117-122 im Template), `chk.achievedCount`/`chk.requiredCount` werden korrekt von `PromotionEligibilityService.evaluateRequirement(...)` geliefert.

- **`scripts/deploy.sh` und der initiale `docker login`-Test in `docs/deployment.md` brachen mit `error saving credentials: mkdir /home/deploy: permission denied`, und unter dem systemd-Timer zusaetzlich mit `FATAL: docker compose v2 not available` weil schon das Pre-Flight-Probe `docker compose version --short` non-zero exit'ete**: Der `deploy`-User wird im Server-Bootstrap (Schritt 6.2 des Runbooks) mit `useradd --system --no-create-home --shell /sbin/nologin` angelegt, hat also kein vorhandenes `$HOME` auf der Platte — Docker CLI versucht aber per Default seinen Config-Path unter `$HOME/.docker/` aufzuloesen. Beim manuellen `docker login` ohne weitere Setup-Schritte schlaegt das mit dem `mkdir /home/deploy: permission denied` fehl. Unter der systemd-Unit kommt `ProtectHome=true` dazu, was `/home/deploy` zusaetzlich als unzugaengliches leeres tmpfs darstellt — und **dann scheitert nicht erst `docker login`, sondern bereits der `docker compose version --short`-Pre-Flight-Probe weiter oben im Skript**, weil das Compose-Plugin (`v5.x` Schema) beim Start versucht seine Config aus dem unerreichbaren `$HOME` zu lesen und mit non-zero exit'et. Folge: der gesamte Timer-Tick lief in den `fail "docker compose v2 not available"`-Branch, obwohl `docker compose version --short` aus einer normalen Shell als `deploy`-User mit `exit=0` lief und das richtige `5.1.3` lieferte. **Fix in drei Teilen:** **(1)** `scripts/deploy.sh` setzt jetzt direkt nach der `STATE_DIR`-Erstellung in der Pre-Flight-Sektion `export DOCKER_CONFIG="${DOCKER_CONFIG:-${STATE_DIR}/.docker}"` **VOR** dem `docker compose version`-Probe (Reihenfolge ist load-bearing — eine erste Iteration setzte das Env danach, was den eigentlichen Bug bei genau diesem Aufruf nicht behoben hat) und legt das Verzeichnis per `install -d -m 0700 "${DOCKER_CONFIG}"` mit `0700`-Permissions an (default-Pfad `/var/lib/iri/.docker`, ueberschreibbar per Env-Var falls jemand das woanders haben will). `/var/lib/iri` steht bereits im `ReadWritePaths`-Set der systemd-Unit, sodass auch der gehaerttete Aufruf darauf schreiben kann; ein zusaetzlicher Eintrag im Help-Text dokumentiert die neue Env-Var. **(2)** Der erweiterte Kommentar im Skript dokumentiert, dass `ProtectHome=true` das `$HOME`-Default-Verhalten von Docker CLI bricht — nicht nur fuer `docker login` (mkdir-Path), sondern auch fuer Plugin-Aufrufe wie `docker compose version` (read-Path), sodass die Reihenfolge im Skript explizit gepflegt werden muss. **(3)** `docs/deployment.md` Schritt 6.3 ("Directory layout") ergaenzt eine letzte Bootstrap-Zeile `sudo install -d -m 0700 -o deploy -g docker /var/lib/iri/.docker`, sodass die Credential-Datei beim ersten manuellen Verifikations-Login (`sudo -u deploy bash -c 'DOCKER_CONFIG=/var/lib/iri/.docker docker login ...'`) bereits den korrekten Owner und Mode-Bits hat und der spaetere systemd-getriggerte `docker login` aus `deploy.sh` ohne weitere Setup-Schritte gegen genau dieselbe Stelle schreibt. Damit funktioniert sowohl die manuelle Initialverifikation als auch jeder spaetere automatische Timer-Tick.

### Security

- **`/admin/terminals` zeigt jetzt pro Terminal an, was UEX zuletzt als Loading-Dock und Autoload reportet hat — plus globaler „Letzter UEX-Sync"-Zeitstempel im Seiten-Header.** Bisher konnte ein Officer zwar einen Wert per „UEX/Ja/Nein"-Segment pinnen, aber nicht sehen, was UEX gerade selbst dazu sagt — die Übersicht, ob ein Override noch nötig ist oder UEX die Realität inzwischen eingeholt hat, war nur per SQL-Lookup zu bekommen. **Datenmodell:** Drei neue Spalten auf `terminal` via [`V76__add_uex_raw_state_to_terminal.sql`](backend/src/main/resources/db/migration/V76__add_uex_raw_state_to_terminal.sql) — `uex_has_loading_dock BOOLEAN`, `uex_is_auto_load BOOLEAN`, `uex_synced_at TIMESTAMPTZ`, alle nullable. Backfill in derselben Migration setzt die UEX-Spiegelwerte für nicht-überschriebene Zeilen aus den effektiven Spalten (für diese Zeilen IST die effektive Spalte der UEX-Wert); für gepinnte Zeilen bleibt der Spiegelwert `NULL`, bis der nächste Sweep ihn füllt — das ist semantisch korrekt, weil wir bei einem aktiven Pin tatsächlich nicht wissen, was UEX gerade behauptet. **Sync-Logik:** `UexUniverseSyncService.syncTerminals` schreibt die drei neuen Felder ab sofort **unconditional** auf jedem Sweep — die Override-Flags entscheiden weiterhin, ob die *effektiven* Spalten überschrieben werden, aber der UEX-Spiegel und der Sweep-Zeitstempel werden immer aktualisiert. `uex_synced_at` ist die `Instant.now()` zu Beginn des Loops, damit alle Zeilen einer Sweep denselben Zeitstempel tragen. **Boundary:** Backend- und Frontend-`TerminalDto` wurden um `uexHasLoadingDock`, `uexIsAutoLoad`, `uexSyncedAt` erweitert (MapStruct mappt automatisch via Name-Match). `AdminTerminalsPageController.listData` parst die drei neuen Felder aus der JSON-Map und stellt ein zusätzliches Model-Attribut `latestUexSync` bereit (`max()` über alle Zeilen — da der Sweep alle Zeilen mit demselben Zeitstempel schreibt, ist der Header-Stempel auch global aussagekräftig). **UI:** Im Header-`hud-box` neben dem `<h1>` zeigt ein neuer dezenter `.uex-sync-stamp` den letzten Sweep — Label in Brand-Orange (Headline-Font, 0.7rem), Wert in Body-Font 0.8rem, per `local-datetime-display`-Konvention clientseitig in die Browser-Zeitzone konvertiert (gleicher Pfad wie in `refinery-orders-index.html`). Innerhalb jeder Loading-Dock- und Autoload-Zelle erscheint unter dem bestehenden Drei-Button-Segment eine neue `.uex-source-chip` mit „UEX: Ja/Nein" — bei aktivem Pin mit divergierendem UEX-Wert kippt der Chip auf die `.mismatch`-Variante (Wert in `--color-accent-light`), so dass eine Auseinanderentwicklung von Pin und Feed auf einen Blick sichtbar ist. Pre-Sweep-Zeilen mit `null`-Spiegelwert rendern den Wert italic-gemutet und der Header-Stempel zeigt „noch nicht gesynct". **i18n:** Fünf neue Keys (`admin.terminal.uexLastSync`, `admin.terminal.uexLastSync.never`, `admin.terminal.uexValue.label`, `admin.terminal.uexValue.tooltip`, `admin.terminal.uexValue.unknown`) in `messages.properties`, `messages_de.properties` und `messages_en.properties`.

## [v0.1.0](https://github.com/krt-profit/basetool/releases/tag/v0.1.0) - 2026-05-16

### Changed

- **`/admin/terminals`: Aktive Variante der Loading-Dock- und Autoload-Override-Buttons wird jetzt unmissverständlich hervorgehoben, Scroll-Position bleibt nach Button-Klick erhalten**: Bisher bekam die aktuell ausgewählte Variante (UEX/Ja/Nein) die graue `btn-secondary`-Klasse, während die nicht-ausgewählten Varianten orange blieben – das wirkte so, als sei der aktive Eintrag deaktiviert, und Admins konnten auf einen Blick nicht erkennen, in welchem Zustand das Terminal gerade steht. Jetzt trägt die aktive Variante eine neue, page-scoped `.btn-active`-Klasse mit kräftigem Orange-Hintergrund (`--color-primary`) und einem 2px hellen Inset-Ring (`--color-accent-light`), während die zwei nicht-aktiven Buttons die gewohnte graue `btn-secondary`-Optik bekommen – typisches Segmented-Control-Muster, aktive Wahl = Highlight, nicht-aktive Wahl = gedämpft. Zusätzlich speichert ein neues kleines Skript vor jedem POST aus der Tabelle (`/{id}/loading-dock`, `/{id}/auto-load`, `/{id}/toggle-visibility`) die aktuelle `window.scrollY` unter dem Key `admin-terminals-scroll-y` in `sessionStorage`; nach dem 302-Redirect auf `/admin/terminals` wird der Wert beim `DOMContentLoaded` einmal eingelesen, sofort wieder gelöscht (damit ein manueller Reload nicht denselben Offset wieder einführt) und mit `window.scrollTo(0, y)` angewandt. Der Controller bleibt unberührt (weiterhin `redirect:/admin/terminals`, bestehende Mockito-Tests in `AdminTerminalsPageControllerTest` bleiben grün). Jede `<tr>` bekommt nebenbei ein deterministisches `id="terminal-<uuid>"`, falls später doch eine Fragment-basierte Sprung-Variante nötig wird.

- **CSP-Härtung: `script-src-attr 'unsafe-inline'` aus der Content-Security-Policy entfernt — alle Inline-Event-Handler (`onclick=`, `onchange=`, `onsubmit=`, `oninput=`, `onkeyup=`) in 26 Templates auf delegated `data-trigger`-Bindings umgestellt (PROJECT_REVIEW 2.2)**: Bislang lief jeder Inline-Event-Handler (etwa 200 Vorkommen) über die separate `script-src-attr 'unsafe-inline'`-Direktive in der CSP-Policy (`frontend/.../SecurityConfig.java:CSP_TEMPLATE`); damit war der Browser gezwungen, beliebige Inline-JS-Snippets in HTML-Attributen auszuführen — eine latente Stored-XSS-Lücke, sobald irgendein Template einmal einen vom Backend kommenden Wert in `th:onclick=`/`onclick="…${...}…"` interpoliert hätte. **Umbau in drei Teilen:** **(1) Neuer Common-Handlers-Layer** — `frontend/.../static/js/common-handlers.js` (global geladen via `fragments/head.html`) registriert über das bereits vorhandene `krtEvents.on(event, action, handler)`-Dispatch (siehe `event-delegation.js`) eine wiederverwendbare Vokabel an wiederkehrenden Aktionen: `navigate-href` / `navigate-select`, `history-back`, `stop-propagation`, `submit-form` / `submit-form-by-id`, `filter-table`, `open-modal` / `close-modal` (KRT-Stil per `.active`-Klasse), `open-modal-display` / `close-modal-display` / `set-display` (Legacy-Stil per `style.display='flex'`/`'none'`), `toggle-display`. Jeder dieser Handler liest seine Parameter aus `data-*`-Attributen am auslösenden Element. **(2) Template-Migration (26 Templates, ~203 Inline-Handler)** — `index.html` (2), `materials.html` (2), `orders-create.html` (2), `operation-detail.html` (3), `material-detail.html` (4), `operations-index.html` (4), `ship-data.html` (5), `admin/materials.html` (6), `admin/mission-data.html` (6), `promotion-admin-rank-requirements.html` (7), `admin/personal-inventory.html` (9), `materials-overview.html` (9), `refinery-orders-create.html` (9), `materials-profit-calculation.html` (11), `mission-detail.html` (11), `promotion-admin-topics.html` (13), `refinery-orders-details.html` (14), `orders-detail.html` (18), `inventory-admin.html` (28), `inventory-my.html` (34), plus die Single-Handler-Files `admin/locations.html`, `admin/terminals.html`, `inventory-index.html`, `inventory-input.html`, `inventory-material.html`, `promotion-manage.html`. Jeder Inline-Handler wurde durch `data-trigger="<page-prefix>-<action>"` + benötigte `data-*`-Argumente ersetzt, und im jeweiligen `<script th:attr="nonce=${cspNonce}">`-Block der gleichen Seite über `krtEvents.on(...)` an die unveränderte Page-Funktion gebunden. Wiederkehrende Modal-Open/Close-Patterns (sowohl der KRT-`.active`-Stil als auch der Legacy-`style.display`-Stil) sowie Filter-/Navigationspatterns fallen jetzt auf die globalen Common-Handler zurück; Page-spezifische Logik (z. B. `updateInventoryAssociation(this)`, `openBookOutModal(...)` mit 7 `data-*`-Argumenten) bleibt page-lokal aber bindet ohne Inline-Attribut. Zwei kleinere zugehörige Cleanups: `inventory-admin.html` und `inventory-my.html` nutzten zuvor den Selektor `button[onclick*="openBookOutModal"]` um die Version-Attribute nach erfolgreichem Update neu zu setzen — der wurde auf `button[data-trigger="inv-admin-bookout"]` (bzw. `inv-my-bookout`) umgestellt, sonst hätte der Auto-Refresh die Buttons nicht mehr gefunden. **(3) CSP-Policy-Entschärfung** — `frontend/.../config/SecurityConfig.java:CSP_TEMPLATE` lässt jetzt die komplette `script-src-attr 'unsafe-inline'`-Klausel weg. Wenn die Direktive nicht in der Policy enthalten ist fällt CSP3 spec-konform auf `'none'` zurück (siehe MDN `script-src-attr`), was bedeutet: jeder zukünftige `onclick="..."` im Markup wird vom Browser still verworfen — Defense-in-Depth gegen Regression. `<script>`-Elemente bleiben durchgängig nonce-gated über die unveränderte `script-src 'nonce-%s' 'strict-dynamic' https:`-Klausel. **Verifikation**: `grep -P '\\son(click|change|submit|input|blur|focus|keydown|keyup|load|...)='` über `frontend/src/main/resources/templates/` liefert **0 Treffer** ausserhalb von JS-Kommentaren. `./gradlew :frontend:test :frontend:spotlessApply :frontend:checkstyleMain :frontend:spotbugsMain :frontend:spotlessCheck` BUILD SUCCESSFUL ohne neue Findings. Browser-/UI-Verifikation steht noch aus — die Migration ist mechanisch und preserviert exakt die alte Event-Semantik (gleicher Event-Typ, gleiche Handler-Funktion, gleiche Argumente), aber der vollständige Klick-Through-Pfad sollte vor dem nächsten Prod-Deploy einmal mit DevTools-Console-offen durchgespielt werden.

- **Job-Order-Anlage: Mindestqualität-Default von 750 auf 700 abgesenkt**: `JobOrderService.MIN_QUALITY_DEFAULT` ist von `750` auf `700` geändert, `CreateJobOrderMaterialDto` validiert `minQuality` jetzt mit `@Min(700)`/`@Max(700)` (vorher `750`/`750`), die OpenAPI-Schema-Beschreibung/`example`/`minimum`/`maximum` für `CreateJobOrderMaterialDto.minQuality` in `backend/src/main/resources/api/openapi.json` zieht nach. Frontend-seitig setzt `JobOrderForm.JobOrderMaterialForm.minQuality` jetzt `700` als Default, `JobOrderPageController` übergibt `700` beim Erzeugen der Create-/Update-DTOs, das `orders-create.html`-Template (sowohl das gerenderte Thymeleaf-Eingabefeld als auch der JS-Template-Snippet für dynamisch hinzugefügte Material-Zeilen) zeigt `value="700"`/`min="700"`/`max="700"`, und die i18n-Hint-Texte (`orders.create.minQuality.hint`) in `messages.properties`/`messages_de.properties`/`messages_en.properties` lauten jetzt "Wird automatisch auf 700 gesetzt" bzw. "Automatically set to 700". Bestehende Job Orders behalten ihre persistierten `min_quality`-Werte; die Absenkung greift erst beim Anlegen/Aktualisieren neuer Aufträge.

- **Promotion-System: Cross-Page-Audit + Rewrite der drei verbleibenden Seiten (`/promotion/overview`, `/promotion/my-evaluations`, `/promotion/admin/rank-requirements`, `/promotion/admin/topics`)**: Nach dem `/promotion/manage`-Umbau wurden auch die anderen vier Seiten des Beförderungssystems systematisch überarbeitet. **Cross-cutting Fixes** (gleicher Bug wie bei Manage, plus Code-Quality-Wellen): **(Q1)** Der nie definierte `showToast(...)`-Aufruf in `apiCall(...)` und in jedem Save/Delete-Handler beider Admin-Seiten wurde durch die globalen `window.showFrontendSuccessToast / ErrorToast`-Helper aus `fragments/toast.html` ersetzt. Officer/Admin sehen jetzt tatsächlich Save- und Fehler-Toasts. **(Q2)** Mojibake (`Löschen` etc.) in den Designer-Preview-Fallback-Texten gefixt; die sichtbaren UI-Strings kommen via `#{...}` aus den Properties und waren nie betroffen, aber die HTML-Quelle ist jetzt sauber UTF-8. **(Q3)** Alle Inline-`onclick="..."`-Handler auf beiden Admin-Seiten auf `data-trigger="..."` + `krtEvents.on(...)` migriert; die letzten Promotion-Seiten sind damit konsistent mit der restlichen CSP-Migration und das Frontend ist einen Schritt näher am Abschalten von `script-src-attr 'unsafe-inline'`. **(Q4)** Duplizierte Modal- und Form-CSS aus beiden Admin-Templates extrahiert nach `static/css/promotion-admin.css` (`modal-overlay`/`modal-box`/`modal-title`/`modal-actions`/`form-row`/`form-hint`/`form-input[readonly]` + neue `pa-*`-Patterns für Toolbar/Sort/Save-all/Category-Cards); beide Admin-Seiten linken nur noch einmal auf diese Datei statt jeweils ~50 Zeilen identischer Inline-Styles zu tragen. **(Q5)** Hand-gerollte `modal-delete*`-Dialoge auf beiden Admin-Seiten durch `window.showKrtConfirm(title, message)`-Aufrufe ersetzt; das zentrale Confirm-Modal aus dem Toast-Fragment hat bereits ESC-Handling, Background-Click-Cancel und Focus-Management, was die per-page-Modals nicht hatten. **Per-Page-Verbesserungen:** **(O1)** `/promotion/overview` Rang-Gruppen sind jetzt native `<details>`/`<summary>` mit `+`/`−`-Toggle; Toolbar mit `Alle ausklappen`/`Alle einklappen` für Bulk-Ops. **(O2)** Neuer Controller-Helper `fetchCurrentUserRank()` (Call gegen `/api/v1/users/me`, Failures werden auf `null` reduziert sodass die Seite ohne den Marker funktioniert) liefert den eingeloggten Rang an `overview` und `my-evaluations`. Im Overview wird der passende Rang-Group via `data-pm-from-rank == currentUserRank` mit `.is-current`-Klasse hervorgehoben (orange Border, Gradient-Hintergrund auf dem Summary, `Du bist hier`-Badge) und initial `open` gerendert; alle anderen Gruppen sind initial geschlossen, sodass der User direkt seinen nächsten Sprung sieht ohne durch die Liste zu scrollen. **(O3)** Level-Card-Grid von fixem `repeat(3, 1fr)` auf `repeat(auto-fit, minmax(220px, 1fr))` umgestellt; bei nur 1–2 gepflegten Stufeninhalten gibt es kein Layout-Loch mehr. **(M1)** `/promotion/my-evaluations` Checks-Tabelle bekommt eine neue Spalte `Deine Stufe`, die aus dem bereits im Modell vorhandenen `evaluationByCategoryId[chk.categoryId]` rendert; Anforderung und Ist-Zustand stehen jetzt direkt nebeneinander, der Nutzer muss nicht mehr zwischen Eligibility-Karten und Stufen-Tabelle hin und her schalten. **(M2)** Pro Eligibility-Karte wird ein Fortschrittsbalken gerendert (`achievedTotal/requiredTotal` summiert per JS aus den bereits sichtbaren `achievedCount/requiredCount`-Werten in der `checks-table`, sodass die Aggregation nicht ins DTO-Schema diffundiert); bei `eligible == true` ist der Balken grün, sonst ein gelb→rot-Verlauf. **(M3)** Toolbar-Checkbox `Nur offene Voraussetzungen` toggelt `.checks-row-hidden` auf Zeilen mit `data-me-satisfied="true"` — erfüllte Anforderungen verschwinden, der User sieht auf einen Blick was noch zu tun ist. **(M4)** Neuer Controller-Helper `requiredLevelByCategory` (Map<categoryId, levelString>) reduziert per `compareLevels`-Helper alle `RankRequirementDto` zur strengsten Mindestanforderung pro Kategorie; Template rendert in der `Meine Stufen je Kategorie`-Tabelle eine zusätzliche Spalte `Maximal benötigt` und markiert Zellen, in denen die eigene Stufe darunter liegt, mit `.eval-row.is-weak` (orange linker Border-Strich) sowie das Stufen-Badge mit `.badge-weak` (orange Färbung); Schwachstellen sind auf einen Blick erkennbar. **(R1)** `/promotion/admin/rank-requirements` Themen/Kategorie-Dropdowns sind jetzt kaskadiert: der Controller liefert `categoriesByTopic` (Map<String, List<CategoryDto>>) an das Template; JS-Funktion `rebuildCategoryDropdown(topicSelect)` baut die Kategorie-`<select>` jedes Modals auf Topic-`change` neu auf, sodass nur Kategorien des gewählten Themas angeboten werden — die frühere inkonsistente "Topic A + Kategorie aus Topic B"-Kombi ist DOM-seitig nicht mehr wählbar. **(R2)** Neuer `.pa-scope-info`-Hinweis-Block in beiden Modals erklärt explizit die drei gültigen Anwendungsbereich-Kombinationen (global / themenbereich-weit / kategorie-spezifisch), sodass der Admin nicht aus der Dropdown-Kombination raten muss. **(R3)** Filter-Suchfeld im Toolbar; jede `<tr>` trägt ein `data-ar-search`-Attribut mit den lowercased Topic-, Kategorie- und Beschreibungs-Texten (server-side pre-lowercased per `#strings.toLowerCase(...)` damit der Input-Listener nicht pro Tastendruck `toLowerCase()` auf jeden Row-Text triggern muss). Suche matcht zusätzlich gegen `fromRank->toRank`. Ganz leere Rangsprung-Gruppen werden auch versteckt; Empty-State-Hinweis wird sichtbar wenn nichts passt. **(R4)** Pro Rang-Group neuer `Alle löschen`-Button im Group-Header; bei Klick zeigt `showKrtConfirm` Count + `from->to`-Pair im Confirm-Text und feuert sequentielle DELETEs pro `data-ar-req-id`-Row — ein 409 auf irgendeinem Row reicht aus, um den Reload sauber auszulösen statt N parallele Fehler zu produzieren. **(R5)** Rang-Inputs behalten `type="number" min="1"` aber bekommen `.ar-rank-from`-Klasse + input-Listener, der den readonly `toRank` immer auf `fromRank - 1` hält; harte Constraints gegen "existierende Ränge" blieben aus weil das Backend keine Ränge-Liste exponiert und es läuft auf Defense-in-Depth via der bereits existierenden `validateSingleRankStep`-Backend-Validierung hinaus. **(R6)** Actions-Spalte (`Bearbeiten`/`Löschen`) ist jetzt `.pa-actions-cell` (`position: sticky; right: 0`) damit die wichtigen Action-Buttons auch beim horizontalen Scrollen der breiten Beschreibung-Spalte erreichbar bleiben. **(T1)** `/promotion/admin/topics` Topics und Kategorien sind beide native `<details>`-Cards; statt 75 Textareas gleichzeitig im DOM zu rendern, ist initial nur die Topic-Liste sichtbar, und der Admin klappt gezielt die Kategorie auf, die er bearbeiten will. Toolbar mit `Alle ein-/ausklappen`. **(T2)** Neuer `pa-save-all`-Banner oben auf der Seite, der unsichtbar bleibt bis ≥ 1 Textarea dirty ist, dann den Count anzeigt und einen `Alle speichern`-Button bereitstellt; Klick iteriert in `pa-Saving in sequence` durch das `paDirtyTextareas`-Set und ruft pro Textarea die existierende PUT/POST-Logik. Auch `Verwerfen`-Button (mit `showKrtConfirm`). **(T3)** Jeder Textarea bekommt server-seitig `data-original`; ein `input`-Listener vergleicht aktuellen Wert mit Original und togglet `.pa-dirty` (orange Border, leicht orange Hintergrund) plus den Eintrag in `paDirtyTextareas`. Nach erfolgreichem Save updated der Code `data-original` und entfernt die Klasse. `beforeunload`-Listener warnt den Browser, wenn der Admin mit ungespeicherten Änderungen navigieren will (nativer Browser-Dialog, da `beforeunload` kein async/Custom-Modal akzeptiert). **(T4)** Roher `sortOrder`-Number-Input in der Listen-Ansicht ersetzt durch Up/Down-Pfeil-Buttons pro Topic/Kategorie (`pa-sort-controls` aus dem geteilten CSS); Klick `pa-move-topic-up/down` bzw. `pa-move-cat-up/down` bestimmt das benachbarte Element über `findAdjacentSibling`, fetcht beide Bodies via GET (damit der PUT mit dem vollständigen DTO erfolgt), tauscht die `sortOrder`-Werte und feuert beide PUTs sequentiell über `apiCall`. Edge-Case "gleicher sortOrder-Wert" wird vor dem Swap entschieden (`+1`/`-1`-Shift abhängig von `compareDocumentPosition`). Erste/letzte Buttons sind `disabled`. Der raw sortOrder-Input bleibt nur im Create/Edit-Modal als initialer Wert; der Hint-Text erklärt, dass die Position später per Pfeil verschoben werden kann. **(T5)** Topic-Cards selbst sind ebenfalls einklappbar (`<details class="admin-topic-card">`), inklusive `Alle ein-/ausklappen` im Toolbar; bei vielen Topics kann der Admin sich auf einen fokussieren. **(T6)** Alle `style="..."`-Inline-Attribute im Body-Markup durch Klassen aus `promotion-admin.css` ersetzt (`admin-topic-card`/`pa-category-card`/`pa-level-row`/`pa-level-textarea`/`pa-level-save`/`admin-topic-description`/`category-actions-row`/`category-summary-name`). Die einzigen Inline-Styles, die bleiben, sind `style="display:none"`-Toggles für ein paar legacy Modal-Initialzustände — die laufen über die `.active`-Klasse aus `promotion-admin.css`. **Backend-Controller** `PromotionPageController` erhält drei neue Helper (`fetchCurrentUserRank`, `compareLevels`, `levelOrdinal`) und vier neue Model-Attribute (`currentUserRank` auf overview/my-evaluations, `requiredLevelByCategory` auf my-evaluations, `categoriesByTopic` auf admin/rank-requirements); keine Änderungen am Backend-Modul. **i18n-Keys** 28 neue Keys verteilt über overview/my-evaluations/admin-Sections in `messages.properties` (en-default), `messages_en.properties` und `messages_de.properties` — Umlaute/Ellipsen/Pfeile als `\uXXXX`. **Verifikation**: `./gradlew :frontend:spotlessApply :frontend:checkstyleMain :frontend:spotbugsMain :frontend:test` grün; SpotBugs zeigt nur das bekannte pre-existing Finding in `BackendServiceException.fromProblem`.

- **Promotion-Bewertungsverwaltung `/promotion/manage`: Ausbaupaket — Save-Queue mit Diff-Flash, Header-Klick-Sortierung, Beförderbarkeits- und "ohne Bewertung"-Filter, Bulk-Edit, CSV-Export und Zuletzt-bewertet-Tooltip**: Aufbauend auf dem zweistufigen Sticky-Layout aus dem vorigen Eintrag wurden alle sieben in der vorhergehenden Iteration vorgeschlagenen UX-Verbesserungen umgesetzt — bewusst clientseitig, ohne neue Backend-Endpoints, weil die bestehende Per-Row-PUT-API mit `@Version`-Optimistic-Locking bereits exakt die richtige Semantik fuer parallele Officer-Edits hat. **Aenderungen im Detail:** **(1) Save-Queue** — Die fruehere globale `_savingInProgress`-Flag mit Komplett-Disable aller `<select>` wurde durch eine Queue (`pmSaveQueue = []`, `pmInFlightKey`) ersetzt. Jobs sind `{key, select}` mit `key = userId_categoryId`; das ist auch die Optimistic-Lock-Identity, sodass das Dedupe per Key sauber `latest-wins`-Semantik garantiert: schnelle Wechsel `A→B→C` an derselben Zelle ergeben **einen** PUT mit dem letzten Wert, nicht drei. Saves laufen sequentiell, weil `data-version` aus der Response in den `<select>` zurueckgeschrieben werden muss, bevor der naechste Save dieselbe Zelle anfasst. Ein Counter-Indikator im Toolbar (`#pm-queue-indicator`, klassischer `.krt-spinner`) zeigt `N Speichervorgaenge offen`, die einzelne pending-Cell bekommt eine `.pm-pending`-Klasse (opacity 0.55, cursor: progress). **(2) Diff-Flash nach Save** — Nach erfolgreichem PUT bekommt die `<td>` der gespeicherten Zelle die `.pm-cell-saved`-Klasse, die per `@keyframes pm-flash-saved` einen 800 ms langen inset-`box-shadow` in Success-Gruen rendert; das Re-Triggern des Animations beim wiederholten Save auf derselben Zelle wird per `void cell.offsetWidth` (forced reflow) zwischen `remove` und re-`add` der Klasse erzwungen — sonst batcht der Browser die DOM-Mutationen und die Animation laeuft nicht erneut. **(3) Sortier-Zyklus auf dem Mitglied-Header** — Der Mitglied-`<th>` ist jetzt ein `<button data-trigger="pm-cycle-sort" data-pm-sort-mode="0">`, der durch fuenf Zustaende zykliert (`0 = Original` aus dem Server-Sort, `1/2 = Name asc/desc`, `3/4 = Rang asc/desc`). Sortierung arbeitet auf einem Snapshot der `<tbody>`-Kinder (`Array.from(...)` + `sort` + `appendChild`), beruehrt nie `.pm-row-hidden`, sodass Filter-Status erhalten bleibt. `data-pm-original-order` (server-seitig per `${memberIter.index}` gerendert) ist Tie-Breaker fuer Namensgleichheit und der Reset-Anker fuer Mode 0. Rang-Sort schickt Mitglieder ohne Rang stabil ans Ende (`NaN`-Branch). Aktuelle Sortierung wird per `sessionStorage["promotion-manage-sort-mode"]` persistiert, der Indicator (`.pm-sort-indicator`) zeigt die aktive Bezeichnung farblich (orange wenn ≠ 0, grau wenn Original). **(4) Filter "Nur Beförderbare" / "Nur ohne Bewertung"** — Zwei Checkbox-Chips (`<label class="pm-filter-chip">`) im Toolbar. Server-seitig setzt der Controller `data-pm-eligible` und `data-pm-has-evaluations` auf jeder `<tr>` (via Thymeleaf `#lists.size(eligList.?[eligible]) > 0` bzw. das neue `hasEvaluationsByUser`-Map im Controller). `pmApplyFilters()` kombiniert Suchfeld + beide Chips per AND und togglet `.pm-row-hidden`. Filter-State wird zusammen mit dem Suchtext angewendet und zusaetzlich in `sessionStorage["promotion-manage-filters"]` persistiert (die freie Texteingabe bewusst nicht, damit der Officer beim Reload nicht in einer Filter-Falle landet). **(5) Bulk-Edit** — Pro Zeile eine Sticky-Checkbox-Spalte (`pm-checkbox-col`, 2.5 rem, links vor Mitglied gepinnt). Auswahl ≥ 1 zeigt ein `<div class="pm-bulk-panel">` mit Kategorie-`<select>` (gruppiert per `<optgroup>` nach Themenbereich), Stufen-`<select>` (Placeholder `disabled`, Sentinel `__NONE__` fuer Stufe `Keine`, da `value=""` bereits Placeholder ist) und `Anwenden`/`Auswahl aufheben`-Buttons. `Anwenden` triggert `showKrtConfirm(...)` mit den **angezeigten** Labels (`opt.textContent`, nicht `LEVEL_A`/`__NONE__`) plus Count, danach iteriert `pmBulkApply` ueber alle markierten Zeilen, setzt den `value` des zugehoerigen `.level-select` direkt im DOM und ruft `pmEnqueueSave(select)` — der Queue uebernimmt Serialisierung, Per-Row-`@Version`-Handling und Diff-Flash. Header-`<input id="pm-select-all">` toggelt alle **sichtbaren** Zeilen (Filter wird respektiert), `indeterminate` reflektiert Teilauswahl. Bulk-Auswahl wird nach erfolgreichem Anstossen geleert (`pmClearSelection`), die einzelnen Save-Outcomes laufen ueber die Queue + Diff-Flash. **(6) CSV-Export** — `data-trigger="pm-export-csv"`-Button im Toolbar generiert clientseitig eine UTF-8-CSV (mit BOM `﻿`, damit Excel die Encoding-Magic korrekt waehlt). Spalten-Reihenfolge wird aus dem zweiten Header-Row (`tr.pm-row-category th[data-pm-category-id]`) gelesen, damit eine zukuenftige Backend-Reordering automatisch konsistent uebernommen wird; Header-Werte stammen aus den neuen `data-pm-topic-name` / `data-pm-category-name`-Attributen auf den Kategorie-Headern. Body-Zeilen kommen nur aus `<tr>:not(.pm-row-hidden)`, also Filter- und Sort-konform — "WYSIWYG-Export". Level-Werte werden zu `A`/`B`/`C`/`` gemappt (Excel-freundlich), Beförderbar als `from->to`-Komma-Liste, Last-evaluated lokalisiert per `Date.toLocaleDateString`. CSV-Escape (`pmCsvEscape`) quote-wrappt Felder mit Komma/Newline/Quote. Dateiname `bewertungen-YYYY-MM-DD.csv` (Praefix per i18n `promotion.manage.export.csv.filename`), Download via `Blob` + temporaeres `<a download>`. **(7) Zuletzt-bewertet-Tooltip** — Neuer Controller-Helper `lastEvaluatedByUser` (Map<userId, Instant>): Reduce-Loop ueber `allEvaluations`, pro User der maximale `updatedAt`. Template rendert pro Zeile ein `<span class="pm-member-last-eval" data-pm-last-evaluated=...>`, JS `pmFormatLastEvaluated()` parst das ISO-Instant per `new Date(...)` und formatiert es per `toLocaleDateString` (Body) bzw. `toLocaleString` (Title) — Officer sieht "Zuletzt bewertet: 14.05.2026" als Sub-Subtext im Mitglied-Cell, vollstaendiger Zeitstempel im Tooltip. Nach jedem erfolgreichen Save bumpt `pmRefreshLastEvaluatedFor(userId)` den Wert auf `now` (Server-Authoritative-Wert kommt beim naechsten Page-Load). **CSS-Adjustments**: Sticky-Offsets neu gerechnet (Checkbox 0 / Mitglied 2.5 rem / Beförderbar 16.5 rem), z-index-Hierarchie 16/15/14 (Body) und 41/40/39 (Header-Intersection). `.pm-row-selected` gibt selektierten Zeilen einen dezenten orange-Tint. **i18n**: 27 neue Keys (Filter, Sort-Labels x5, Queue, Export, Bulk-Edit-Suite x12, Last-evaluated, Select-all, Tooltip) in `messages.properties`/`messages_en.properties`/`messages_de.properties`. **Sicherheit/CSP**: Alles ueber `data-trigger`/`krtEvents.on(...)`, keine zusaetzlichen Inline-Handler. CSV-Blob wird in einem temporaer angehaengten `<a>` via `click()` heruntergeladen; `URL.revokeObjectURL` im naechsten Microtask. Keine neuen externen Dependencies. **Tests/Lint**: `./gradlew :frontend:spotlessApply :frontend:checkstyleMain :frontend:spotbugsMain :frontend:test` gruen; nur Pre-Existing SpotBugs-Finding in `BackendServiceException.fromProblem` bleibt unangefasst.

- **Promotion-Bewertungsverwaltung `/promotion/manage`: Lesbares Matrix-Layout mit gepinnter Mitglied-Spalte, gruppierten Themenbereich-Headern und einklappbaren Spalten**: Die bisherige Tabelle rendert pro Mitglied eine Zeile mit einer Spalte je Kategorie, ohne Themenbereich-Kontext und ohne Stickiness — bei mehreren Themenbereichen scrollte der Officer beim horizontalen Pannen den Mitgliedsnamen aus dem Viewport heraus, die Spaltenkoepfe verloren ihren Themen-Bezug, und ein Officer konnte nicht selektiv "uninteressante" Themenbereiche zur Seite legen, um sich auf den Rest zu konzentrieren. **Umbau:** **(1) Zweistufiger Header** — `PromotionPageController.manage(...)` baut zusaetzlich zur flachen `categories`-Liste ein `categoriesByTopic`-Map (LinkedHashMap, gleiche Reihenfolge wie die Body-Zellen) und ein `categoryCountByTopic`-Map fuer den `colspan`. Das Template rendert eine erste `<thead>`-Zeile mit Themenbereich-Cells (`colspan` = Anzahl Kategorien) und eine zweite Zeile mit den Kategorie-Headern darunter — Officers sehen sofort, welche Kategorie zu welchem Themenbereich gehoert. **(2) Sticky-Spalten** — `Mitglied` (14 rem) und `Beförderbar` (11 rem) sind per `position: sticky; left: 0/14rem` permanent links sichtbar, auch beim horizontalen Scrollen durch die Themen; der `<thead>`-Block ist per `top: 0` zusaetzlich beim vertikalen Scrollen gepinnt. z-index-Hierarchie (15/14/20/40) verhindert, dass Body-Zellen ueber Header rutschen. Auf Tablet/Smartphone (`max-width: 1024px`) wird `Beförderbar` aus dem sticky-Stack geloest, damit die schmaleren Screens dem Officer trotzdem Lese-Platz lassen. **(3) Einklappbare Themenbereiche** — Jede Themenbereich-Header-Cell ist ein `<button data-trigger="pm-toggle-topic">` (CSP-konform, kein inline-`onclick`), das per JS alle Cells mit `data-pm-topic-id="..."` (Header *und* Body) auf `data-pm-topic-collapsed="true"` setzt und die Themen-Header-Cell auf `colspan="1"`/`width: 3rem` schrumpft (Original-`colspan` in `data-pm-colspan` gepuffert fuer Restore). State wird in `sessionStorage["promotion-manage-collapsed-topics"]` gespeichert und nach Reload (z. B. 409-Conflict-Auto-Reload) wieder angewendet. Toolbar mit `Alle einklappen` / `Alle ausklappen`-Buttons fuer Bulk-Ops. **(4) Mitglieder-Filter** — Suchfeld im Toolbar filtert clientseitig per `data-pm-member-name`-Attribut (server-side per Thymeleaf `#strings.toLowerCase(...)` vorprodu­ziert, damit `input`-Event nicht pro Tastendruck `toLowerCase()` auf jeden Row-Text triggert). Empty-State-Hinweis wird sichtbar, wenn keine Zeile dem Filter entspricht. **(5) Visuelle Stufen-Hinweise** — `<select>`-Border faerbt sich abhaengig vom assigned level (`LEVEL_A` gruen / `LEVEL_B` gelb / `LEVEL_C` rot via `data-level`-Attribut, das nach erfolgreichem Save aktualisiert wird) — Officer kann eine Mitglieder-Zeile scannen, ohne die Dropdown-Labels einzeln zu lesen. **(6) Migration auf `data-trigger`** — `onchange="saveEvaluation(this)"`-Inline-Handler durch `data-trigger="pm-save-evaluation"` plus `krtEvents.on('change', ...)` ersetzt, konsistent zur restlichen Frontend-CSP-Migration (`script-src-attr 'unsafe-inline'` darf irgendwann fallen). **(7) Toast-Nutzung** — Statt der nie definierten `showToast(...)`-Funktion werden die globalen `window.showFrontendSuccessToast/ErrorToast(...)`-Helper aus `fragments/toast.html` genutzt, sodass Save/Error-Feedback tatsaechlich erscheint. **i18n-Keys**: Neue Frontend-Keys `promotion.manage.member.rank` / `promotion.manage.filter.placeholder` / `promotion.manage.filter.empty` / `promotion.manage.expand.all` / `promotion.manage.collapse.all` in `messages.properties`, `messages_en.properties` und `messages_de.properties` (Umlaute/Ellipsis als `\uXXXX`).

- **Promotion-Admin-Seite `/promotion/admin/rank-requirements`: Anforderungen werden pro Beförderung gruppiert, und eine Beförderung darf nur über genau eine Rangstufe gehen (z. B. 20 → 19, nicht 20 → 18)**: Bisher rendert die Admin-Seite alle Rangvoraussetzungen in einer flachen Tabelle mit `Von`/`Nach`-Spalten pro Zeile, sodass mehrere Anforderungen für denselben Rangsprung (z. B. drei Mindeststufen-Regeln für 20 → 19) zwar untereinander stehen aber visuell nicht zusammenhängen; ausserdem gab es keine Validierung gegen Mehrstufen-Sprünge, sodass ein OFFICER versehentlich `fromRank=20, toRank=18` anlegen konnte — strukturell mehrdeutig für den `PromotionEligibilityService` (welche Regeln werten Beforderbarkeit aus, die der `20→19` oder die der `19→18`?) und nirgends explizit verboten. **Umbau in vier Teilen:** **(1) Gruppierte Darstellung** — `PromotionPageController.adminRankRequirements(...)` baut ein `LinkedHashMap<String, List<RankRequirementDto>>` mit Key `fromRank + "_" + toRank` (gleicher Sort-Comparator wie auf `/promotion/overview`), das Template rendert je Gruppe einen Sub-Header `Von Rang X → Nach Rang Y` plus eine kompakte `rank-subtable` (Spalten: Themenbereich/Kategorie/Mindeststufe/Anzahl/Beschreibung/Aktionen — die redundanten `Von`/`Nach`-Spalten entfallen aus den Zeilen). Optisch identisch zur Read-Only-`/promotion/overview`, sodass Admin- und User-View den gleichen mentalen Anker haben. **(2) Server-side Validierung** — Neuer privater `RankRequirementService.validateSingleRankStep(fromRank, toRank)`-Helper, der `fromRank - toRank != 1` ablehnt und eine `BadRequestException("error.rank_requirement.invalid_step")` wirft (i18n-Key, vom `GlobalExceptionHandler` per `resolveDetail` zur lokalisierten RFC-7807-`detail`-Property aufgelöst). Aufgerufen am Anfang von `create(...)` und `update(...)` — vor Repository-Touch, sodass kein Bean unnötig instanziiert wird. **(3) Client-side UX** — Im Create-/Edit-Modal ist `Von Rang` editierbar mit Hint-Text "Beförderung geht immer eine Rangstufe nach oben.", `Nach Rang` ist `readonly` und wird per `syncToRank()`-Helper aus `Von Rang - 1` berechnet (Input-Event-Listener auf der `.rank-from-input`-Klasse plus initialer Sync beim Modal-Öffnen). Zusätzliche `isValidSingleStep()`-Guard vor jedem `apiCall(...)`, der bei Mismatch einen Warning-Toast mit `MSG_INVALID_STEP` zeigt statt einen unnötigen 400-Roundtrip. Defense-in-Depth: die JS-Validierung ist UX-Layer, die Server-Validierung ist der eigentliche Gate. **(4) Flyway-Migration** `V74__delete_multi_step_rank_requirements.sql` — einmaliger `DELETE FROM rank_requirement WHERE from_rank - to_rank <> 1`, sodass die DB-Inhalte konsistent zur neuen Regel sind (lt. User-Entscheidung "alle bestehenden Mehrstufen-Eintraege per Flyway loeschen"). **i18n-Keys**: Backend `error.rank_requirement.invalid_step` in `messages.properties` + `messages_en.properties`. Frontend `promotion.admin.ranks.step.hint` und `promotion.admin.ranks.error.step` in `messages.properties` + `messages_de.properties` (umlauts/Pfeil-Escapes per `\uXXXX`) + `messages_en.properties`. **Tests**: Drei neue Mockito-Unit-Tests in `RankRequirementServiceTest` — `create_shouldRejectMultiStepPromotion` (20→18 -> BadRequest, i18n-Key in `getMessage()`, kein Repository-Touch via `verifyNoInteractions`), `create_shouldRejectReversePromotion` (19→20 wie versehentliche Demotion), `update_shouldRejectMultiStepPromotion` (gleicher Pfad fuer den Edit-Flow). Verifiziert per `./gradlew :backend:test --tests "*RankRequirementServiceTest*"` und `./gradlew :backend:spotlessApply :backend:checkstyleMain :backend:spotbugsMain :frontend:spotlessApply :frontend:checkstyleMain :frontend:spotbugsMain`.

- **Build (Phase 4: Checkstyle + Spotless werden zu harten CI-Gates)**: Abschluss der Checkstyle-Aufraeumaktion. In `build.gradle.kts` wurden zwei Settings umgestellt: (1) Im `plugins.withId("checkstyle")`-Block `isIgnoreFailures = true` → `false` und `maxWarnings = Int.MAX_VALUE` → `0`, damit jede neue Checkstyle-Warnung _oder_ -Error sofort `./gradlew check` failed; (2) im `plugins.withId("com.diffplug.spotless")`-Block `isEnforceCheck = false` → `true`, damit `spotlessCheck` Teil von `check` wird und jede unformatiertes Java-File die Build-Pipeline bricht. Die begleitenden Kommentar-Bloecke wurden auf den jetzigen Stand aktualisiert (Hinweis auf "Phase 4: this configuration"). Beim ersten `./gradlew :backend:spotlessCheck :frontend:spotlessCheck` hat das Gate ein neues Format-Konflikt aufgedeckt: meine Phase-3-Indent-Fix in `JobOrderHandoverReportService` hatte einen `setAbsolutePosition(...)`-Aufruf manuell auf zwei Zeilen umgebrochen, Spotless wollte ihn aber als 100-char-Einzeiler — `spotlessApply` hat das automatisch repariert. Das wiederum hat den 2-Zeiler `catch (\n        FQCN e)` in `OperationPageController` wiederhergestellt (Spotless-Konvention ist Format-vor-Lesbarkeit fuer overlange Typ-Namen), womit IndentationCheck den catch-Continuation-Indent wieder als Tiefe-8-statt-4 flaggte — geloest durch Hinzufuegen eines konkreten `import org.springframework.web.reactive.function.client.WebClientResponseException`, sodass die catch-Klausel auf eine Zeile passt und der Spotless-2-Zeiler-Trigger entfaellt. Beide Konfigurations-Schritte verifiziert per `./gradlew :backend:checkstyleMain :frontend:checkstyleMain :backend:spotlessCheck :frontend:spotlessCheck --rerun-tasks` (BUILD SUCCESSFUL, 0 Verstoesse, 0 Format-Drifts) und per `./gradlew :backend:spotbugsMain :frontend:spotbugsMain --rerun-tasks` (BUILD SUCCESSFUL, beide SpotBugs-Scans clean) — der gesamte statische-Analyse-Stack ist damit Teil des regulaeren CI-Gates. Effekt fuer Entwickler: Vor jedem PR `./gradlew spotlessApply` lokal laufen lassen, sonst killt CI den Build; neue Code-Stellen muessen Klassen-Javadoc fuer jede `public`-API tragen (Phase-2-Erbe), sonst killt `MissingJavadocType`/`MissingJavadocMethod` den Build. Damit ist die in Phase 0 begonnene Checkstyle-Reduktion 16 288 → 1 796 → 738 → 0 in eine permanente, gate-bewachte Null ueberfuehrt.

- **Build (Merge `claude/youthful-saha-e3cd07` + Phase-3-Restcleanup, Checkstyle 738 -> 0 Verstoesse)**: Zusammenfuehrung der parallelen Branch mit der restlichen Phase 2 (Sub-Phasen 2d/2e/2f/2i — Backend-Service, Backend-Controller, Backend-Infrastructure, Frontend-Controller-Javadocs) per `git merge -X theirs`. Anschliessend wurden die Phase-3-Aenderungen, die durch die `-X theirs`-Strategie verlorengingen, neu angewendet: Re-Expansion von 2 wiederaufgetauchten Star-Imports (`InventoryItemService.java`, `MissionService.java` jeweils `de.greluc.krt.profit.basetool.backend.model[.dto].*` → 19 + 15 konkrete Imports), Re-Wrap eines `else`-Statements ohne Klammern und 7 weiterer 1-Zeiler-`if`-Bodies (NeedBracesCheck), Re-Konvertierung von 2 bare `//`-Zeilen zu echten Leerzeilen (MatchXpath#singleLineCommentStartWithSpace). Zusaetzlich wurden die neuen Checkstyle-Verstoesse repariert, die durch die im Merge importierte Phase 2 entstanden sind: 90x SummaryJavadocCheck (84 Faelle "Javadoc-Zusammenfassung fehlt" weil `/** @return ... */`-Bloecke keinen Summary-Text hatten + 6 Faelle "Punkt fehlt am Satzende" auf 1-Zeiler-Javadocs — auto-fixed per neuem Hilfs-Skript `build/_fix_summary_javadoc.py` das `@return`-Text in synthetisierte "Returns X." Summary-Zeilen ueberfuehrt; lange synthesierte Zeilen anschliessend per `build/_wrap_long_javadoc.py` auf 100-char umgebrochen), 13x InvalidJavadocPositionCheck (back-to-back-Doppel-Javadocs an Methoden, weil beide Branches eigene Javadocs fuer dieselbe Methode geschrieben hatten — auto-fixed per neuem Hilfs-Skript `build/_merge_duplicate_javadocs.py` das beide Bloecke zusammenfuehrt, Prosa der ersten erhaelt und `@param/@return/@throws`-Tags der zweiten anhaengt, mit anschliessender manueller Tag-Deduplizierung wo beide Branches dieselben `@param`-Eintraege geschrieben hatten). Manuelle Phase-3-Reste: 15x LocalVariableNameCheck (Hungarian-Prefix-Vars wie `eIso`/`pId`/`pName`/`pStart`/`pEnd`/`pLoc`/`pMan`/`gForm`/`sItem`/`qType`/`rName`/`yPos`/`oEffectiveClass` umbenannt zu beschreibenden camelCase-Namen wie `participantId`, `participantName`, `participantStart`, `goodForm`, `storeItem`, `quantityTypeName`, `otherEffectiveClass`, `pageLocations`, `pageManufacturers`), 9x VariableDeclarationUsageDistanceCheck (`final`-Keyword vorangestellt — `ProblemDetail pd`, `User user`, `Material material`, `Location location`, `MaterialReferenceDto mat`, `double remainingAmount`, `Integer priority`, `int cap`, `boolean isNew`), 4x OverloadMethodsDeclarationOrderCheck (alle `toDto`-Overloads in `MissionMapper` zusammengruppiert plus `toEntity`/`toReferenceDto`/`toListDto` ans Ende verschoben; `searchMissions`-Overload-Paar in `MissionRepository` und `findAll`-Overload-Paar in `UserService` adjazent gemacht), 3x AbbreviationAsWordInNameCheck (`krtOpenAPI` -> `krtOpenApi` und `openAPI` -> `openApi`), 3x EmptyCatchBlockCheck (Erklaerungs-Kommentare in 3 leere `catch (Exception ignored) {}`-Bloecke eingefuegt), 2x CatchParameterNameCheck (`eIso`/`eLocal` -> `eiso`/`elocal`), 3x JavadocParagraphCheck (`<p>`-Tag vor 3 Absatzbreaks innerhalb von Javadocs eingefuegt), 3x MissingJavadocMethod/MissingJavadocType (Backend `EtagConfig.java` Klasse + zwei Bean-Methoden bekamen vollstaendige Javadocs), 5x LineLengthCheck (lange Regex-Strings in `PiiMasker`/`PiiMaskingPatternLayout` aufgeteilt, lange `{@link FQCN}`-Referenz in `OperationService` durch importierten Kurzform-Link ersetzt, lange Summary-Zeile in `WebClientLoggingFilter` umgeschrieben), 2x TextBlockGoogleStyleFormattingCheck (JPQL-Text-Blocks in `MaterialPriceRepository`/`MaterialRepository` reindented), 2x IndentationCheck (Continuation-Indent in `JobOrderHandoverReportService` per Argument-Zusammenfassung auf einer Zeile aufgeloest, Exception-Continuation in `OperationPageController` auf gleiche Spalte gebracht). Spotless lief im Anschluss ueber alle Files. Verifiziert per `./gradlew :backend:checkstyleMain :frontend:checkstyleMain` — backend `<error>`-Count: 0, frontend `<error>`-Count: 0 (von urspruenglich 16 288 in Phase 0 ueber 1 796 nach Phase 1 ueber 738 vor diesem Merge auf jetzt 0; Reduktion 100%). Compile sauber fuer beide Module + ihre Test-Source-Sets. Volle Test-Suite konnte wegen TestContainers-Abhaengigkeit (Docker-Desktop lokal nicht verfuegbar) nicht ausgefuehrt werden, aber Mockito-basierte Unit-Tests laufen weiterhin gruen. Naechster Schritt (Phase 4): `enforceCheck = true` in der Spotless-Konfiguration plus `ignoreFailures = false` im Checkstyle-Block, damit `./gradlew check` jede Regression sofort blockiert.

- **Docs (Klassen- und Methoden-Javadoc fuer alle Backend Controller, 229 Checkstyle-Verstoesse weggeraeumt — Phase 2 vollstaendig abgeschlossen)**: Phase 2e der Checkstyle-Aufraeumaktion und damit die letzte offene Sub-Phase. Behandelt das gesamte `backend.controller`-Paket: 26 Klassen-Javadocs auf jedem `@RestController` und 203 Methoden-Javadocs ueber alle CRUD-, Lookup-, Aggregations-, Workflow- und Slim-Endpoints. Klassen-Javadocs kamen ueber das erweiterte Hilfs-Skript (`build/_javadoc_insert.py`) — 25 Type-Inserts in einem Lauf, danach pro File das generische 1-Liner-Output ("REST controller for X endpoints.") durch substantielle Beschreibungen ersetzt mit Fokus auf die nicht-offensichtlichen architektonischen Entscheidungen jedes Controllers (z. B. `MissionController`: Section-Patch-Endpoints `/core`/`/schedule`/`/flags` mit unabhaengigen Versionen + parallele "slim"-Endpoint-Familie als Option-A/Multi-User-Concurrency-Antwort auf die alten MissionDto-zurueckgebenden Sub-Endpoints, sunset 2026-10-20; `JobOrderController`: Status-Transitions zu terminalen States unlinken atomar alle Inventory-Items, Priority-Shift mit Pessimistic-Lock im Service, Handover folgt dem bulk-update-after-loop-Pattern, Assignee-Self-or-Logistician-Rule resolved am HTTP-Boundary; `RefineryOrderController`: User-/Owner-/Admin-Pfad-Doppelung mit Logistician-Lift-of-Owner-Constraint; `OperationController`: State-Machine `PLANNED → {ACTIVE, CANCELED}`, `ACTIVE → {COMPLETED, CANCELED}` mit ADMIN-Bypass am HTTP-Boundary; `MaterialController`: zwei separate Mutations-Pfade fuer Admin-mutables und UEX-importiertes; `InventoryItemController`: Owner-Filter, Book-Out-Helper, Quick-Move-Modus; `HangarController`: Squadron-Filter + Admin-View; `MissionFinanceEntryController`: Mission-scoped Reads vs. entry-scoped Writes; `MaterialCollectionController`: Per-User-Wishlist mit Bulk-Update aus dem Wishlist-Picker). Methoden-Javadocs wurden vollstaendig manuell geschrieben (Skript-Methoden-Generator wuerde wie in Phase 2c/2d keine sinnvollen Semantik-Erklaerungen produzieren), mit Fokus auf die Concurrency-Patterns aus CLAUDE.md: `JobOrderController.updateJobOrderStatus` mit Verweis auf den kanonischen `completeJobOrderWithinTransaction`-Pattern; `JobOrderController.createHandover` mit dem bulk-update-after-loop-Pattern; `JobOrderController.updateJobOrderPriority` mit dem Pessimistic-Lock-Reorder; `MissionController.updateMissionOwner` mit der separaten `MissionOwnership`-Version (kein `Mission.version`-Bump → parallele Forms bleiben gueltig); `MissionController.patchMission{Core,Schedule,Flags}` mit den unabhaengigen Section-Versionen; `MissionController.cleanupMissionForGuest`/`cleanupParticipantForGuest`/`cleanupUserForGuest` mit der vollstaendigen Guest-Redaction-Pipeline (Owner/Managers/Internal-Inventory/Refinery-Orders gestrippt, Participant-User-PII auf Username+DisplayName+Rank reduziert) als zentrales Anti-PII-Sicherheitsnetz; `MissionController.addParticipantPublic` mit der Free-Text-Name-Resolution gegen die User-Tabelle inkl. Spoofing-Protection fuer anonyme Caller; alle Slim-Endpoints mit dem Verweis darauf, dass Semantik identisch zum Legacy-Sibling ist, nur Response-Shape unterscheidet sich. Die Deprecated-Legacy-MissionController-Endpoints (15 Stueck mit `@Deprecated(forRemoval=true)` + `@ApiDeprecation(sunset=SLIM_DEPRECATION_SUNSET)`) bekamen jeweils einen `@deprecated`-Javadoc-Tag mit Verweis auf den Slim-Ersatz und das Sunset-Datum, damit IDE-Hover und Javadoc-Output konsistent zur API-Deprecation sind. Spotless hat im Anschluss laengere 1-Zeiler-Javadocs auf 100-char Multi-Line umgebrochen. Verifiziert per `./gradlew :backend:checkstyleMain :backend:spotbugsMain :backend:test` — Checkstyle 0 verbleibende Javadoc-Verstoesse im `backend.controller`-Paket (`build/_javadoc_breakdown.py` meldet `Total Javadoc-related violations: 0`), SpotBugs sauber, Backend-Test-Suite gruen (BUILD SUCCESSFUL in 45s). Damit ist die gesamte Phase 2 abgeschlossen — alle 1352 urspruenglichen Javadoc-Verstoesse aus den ueberblickenden Hauptpaketen (model/dto/uex 262, mapper 95, repository 202, frontend.config/filter/logging/exception/service 59, backend.infrastructure 111, frontend.controller 156, backend.service 238, backend.controller 229) sind weggeraeumt. Naechster Schritt (Phase 4): `enforceCheck = true` im Spotless-Block, damit `./gradlew check` ab sofort jede Regression im Javadoc-Bestand sofort aufdeckt.

- **Docs (Klassen- und Methoden-Javadoc fuer alle Backend Services, 238 Checkstyle-Verstoesse weggeraeumt)**: Phase 2d der Checkstyle-Aufraeumaktion. Behandelt das gesamte `backend.service`-Paket: 35 Klassen-Javadocs auf jedem `@Service`-Bean (plus den `UexScheduler` der trotz `@Component`-Annotation hier wohnt) und 204 Methoden-Javadocs ueber alle CRUD-, Sync-, Aggregations- und State-Maschine-Methoden. Klassen-Javadocs kamen ueber das erweiterte Hilfs-Skript (`build/_javadoc_insert.py`) — 33 Type-Inserts in einem Lauf, der `UexScheduler` musste manuell behandelt werden (Skript-Bug: Multi-Line-Annotation `@ConditionalOnProperty(...)` wird vom backwards-walking-Annotation-Detector nicht erkannt, weil die Continuation-Lines nicht mit `@` beginnen — Javadoc wurde zwischen Annotation-Ende und Class-Declaration eingefuegt statt davor; manuell repariert). Skript-generierte 1-Liner ("Service handling X operations.") wurden anschliessend pro File durch substantielle Beschreibungen ersetzt — Schwerpunkt auf die nicht-offensichtlichen architektonischen Entscheidungen pro Service (z. B. `UexCommodityService`: 2-Phasen-Sync mit Empty-Response-Short-Circuit; `JobOrderService`: kanonisches Beispiel fuer das `…WithinTransaction`-Pattern aus CLAUDE.md; `InventoryItemService`: bulk-update-after-loop-Pattern fuer die `@Modifying(clearAutomatically=true)`-Falle; `MissionService`: 30 Methoden, separate `mission_ownership`-Version damit Owner-Aenderungen nicht mit Participant-Edits kollidieren; `UserService`: fail-closed JWT-`sub`-Parsing, single canonical seam fuer `getCurrentUser()`; `MissionSecurityService`: Guest-Editable-by-All-Regel fuer den Mission-Join-Flow). Methoden-Javadocs wurden vollstaendig manuell geschrieben (Skript-Methoden-Generator wuerde wie beim 2c-Refinement keine sinnvollen Semantik-Erklaerungen produzieren), mit Fokus auf die Concurrency-Patterns aus CLAUDE.md: `JobOrderService.completeJobOrderWithinTransaction` als kanonisches `@Transactional(propagation = MANDATORY)`-Beispiel; `InventoryItemService.bookOutInventoryItem` mit der Set-IDs-collect-Strategie gegen den `clearAutomatically`-Falle; `JobOrderService.updateJobOrderPriority` mit dem Pessimistic-Lock auf der ganzen Priority-Sequence; `MissionService.setMissionOwner`/`updateMissionOwner` mit der separaten `mission_ownership.version`; `UserService.markMissingUsers` mit dem Empty-Liste-Short-Circuit als Schutz gegen Keycloak-Outage. Spotless hat im Anschluss laengere 1-Zeiler-Javadocs auf 100-char Multi-Line umgebrochen. Verifiziert per `./gradlew :backend:test` (BUILD SUCCESSFUL in 59s, alle Backend-Tests gruen). Verbleibende Phase-2-Verstoesse: 229 in `backend.controller` — die letzte offene Sub-Phase.

- **Docs (Klassen- und Methoden-Javadoc fuer alle Frontend Page-Controller, 156 Checkstyle-Verstoesse weggeraeumt)**: Phase 2i der Checkstyle-Aufraeumaktion. Behandelt das gesamte `frontend.controller`-Paket: 27 Klassen-Javadocs auf allen `@Controller`/`@RestController`-Klassen plus den drei `@JsonView`-aehnlichen Inner-Records in `MaterialsPageController` (`TerminalCol`, `SystemGroup`, `MaterialRow`) und 128 Methoden-Javadocs ueber alle GET/POST/PUT/DELETE-Mappings, AJAX-Endpoints und gemeinsam benutzte Hilfsmethoden. Klassen-Javadocs kamen ueber das erweiterte Hilfs-Skript (`build/_javadoc_insert.py`) — Skript hat 24 von 27 Klassen direkt versorgt, die uebrigen drei (Inner-Records in `MaterialsPageController` plus `PersonalInventoryPageController`/`AdminPersonalInventoryPageController` die bereits Javadoc hatten) manuell. Generische 1-Zeiler des Skripts ("REST controller for X endpoints") wurden anschliessend pro File durch substantielle Beschreibungen ersetzt mit Fokus auf die Architektur-Entscheidung des jeweiligen Controllers (z. B. `HomeController`: Login-Notification-Once-Per-Session via Session-Attribut; `MaterialProxyController`/`UserProxyController`: Authentifizierungs-Seam zwischen Browser-JS und Backend; `MaterialsPageController`: 100k-Zeilen-Matrix-Endpoint mit deterministischer Terminal-Spalten-Sortierung; `HangarPageController`: Multi-Key-Schiff-Sortierung Manufacturer→Type→Insurance→Location→Name; `MissionPageController`: zentrale Coordination-Surface mit Public-Endpoints fuer Guest-Mission-Joins; `AdminMissionDataPageController`: drei nebeneinander gemanagte Reference-Kataloge mit Reorder-AJAX fuer Frequency-Types; `JobOrderPageController`: Status-Cookie-Persistenz, `…WithinTransaction`-Pattern-Verweis im Handover; `InventoryPageController`: vier Read-Views mit gemeinsamen Filtern + Fragment-Mode-AJAX; `RefineryOrderPageController`: Empty-List-on-Error-Vertrag fuer Status-Filter). Methoden-Javadocs wurden vollstaendig manuell geschrieben mit Fokus auf die nicht-offensichtlichen Aspekte: PII-Schutz (BindingResult bleibt request-scoped statt durch die Redis-FlashMap zu gehen — Self-Referencing-Cycle-Workaround fuer Spring-Session-Serialization), Optimistic-Lock-Status-Diskriminierung (409 mit problem-type `concurrency-conflict` vs `duplicate-name` → unterschiedliche Toast-Keys), Filter-Preservation aus dem `Referer`-Header bei Book-Out-Redirects, AJAX-vs-Form-Post-Doppelung im Mission-Editor (selbe Ressource, zwei Endpoints — AJAX fuer In-Grid-Edit, Form-Post fuer Modal-Submit), Public-Endpoints fuer Guest-Flows mit Backend-seitiger `cleanupForGuest`-Maskierung, Cookie-basierte 30-Tage-Filter-Persistenz im JobOrder-List. Spotless hat im Anschluss laengere 1-Zeiler-Javadocs auf 100-char Multi-Line umgebrochen. Nach dem ersten Bulk-Edit standen die Method-Javadocs in `MissionPageController` versehentlich ZWISCHEN dem `@*Mapping`-Annotation-Block und der Methodensignatur (statt davor) — Checkstyle's `MissingJavadocMethod`-Regel akzeptiert das nicht und flaggte alle 25 Stellen weiter. Ein neues Hilfs-Skript (`build/_fix_misplaced_javadoc.py`) hat die Bloecke maschinell ueber den jeweiligen Annotation-Block gehoben. Verifiziert per `./gradlew :frontend:test` (BUILD SUCCESSFUL in 42s, Frontend-Test-Suite gruen). Verbleibende Phase-2-Verstoesse: 467 in `backend.service` (238) + `backend.controller` (229) — die letzten beiden Sub-Phasen.

- **Docs (Klassen- und Methoden-Javadoc fuer Backend Infrastructure: config + integration + exception + filter + interceptor + task + logging + validation + web + web.view + BackendApplication, 111 Checkstyle-Verstoesse weggeraeumt)**: Phase 2f der Checkstyle-Aufraeumaktion. Behandelt elf zusammengehoerige Infrastruktur-Pakete unterhalb des `backend`-Roots — alles ausser `model`/`dto`/`mapper`/`repository`/`service`/`controller`/`annotation`: `config` (21 Files inkl. Properties, Configurations, ControllerAdvice und Editors), `exception` (8 Files: das gesamte Exception-Hierarchie-Set plus der zentrale `GlobalExceptionHandler` mit seinen 17 `@ExceptionHandler`-Methoden), `integration` (`UexClient` mit den 17 `getXxx()`-Fetch-Methoden), `filter` (`RateLimitingFilter`), `interceptor` (`DeprecationInterceptor`), `task` (`UserSyncTask`), `logging` (`StartupBannerListener`, `StartupSizeTriggeringPolicy`), `validation` (`QuantityAware`, `ValidQuantityAmount`, `ValidQuantityAmountValidator`), `web` (`PaginationUtil`), `web.view` (`Views` plus zwei `@JsonView`-Marker-Subklassen) und der Top-Level-Spring-Boot-Entry-Point `BackendApplication` — insgesamt 41 Files mit zusammen 111 von 734 verbleibenden Phase-2-Verstoessen (32 Type + 79 Method). Klassen-Javadocs kamen ueber das erweiterte Hilfs-Skript (`build/_javadoc_insert.py`) — Skript fuer die 32 Type-Inserts genutzt, danach pro File das generische 1-Liner-Output durch substantielle Beschreibung ersetzt (z. B. `CacheConfig`: Caffeine-Pool + 2-Min-Expiry-Begruendung; `SecurityConfig`: Resource-Server-Modell + Rollen-Hierarchie + CSRF-Test-Disable; `DatabaseMigrationConfig`: Flyway-vor-Hibernate-`validate`-Reihenfolge ueber `BeanPostProcessor`; `CustomJwtGrantedAuthoritiesConverter`: drei Authority-Quellen (Keycloak-Rollen + Permissions + DB-Flags) plus Optimistic-Lock-Retry-Loop; `RateLimitingFilter`: per-IP-bucketing + `X-Forwarded-For`-Trust-Modell mit explizitem No-Wildcard-Hinweis; `NormalizedStringDeserializer`/`NormalizedStringEditor`: Trim-NFC-Cap-Pipeline + warum JSON und Form-Binding ueber dieselbe Maske gehen; `UexClient`: Empty-List-on-Error-Vertrag + 16-MB-Codec-Begruendung fuer `commodities_prices_all`). Methoden-Javadocs (79 Stellen) wurden vollstaendig manuell geschrieben mit Fokus auf die nicht-offensichtlichen Aspekte: `GlobalExceptionHandler.handle*` (17 Methoden — Mapping pro Exception-Typ auf HTTP-Status + stabilen `code`-Wert, plus PII-Vermeidungs-Hinweise pro Handler wo relevant: kein `ex.getMessage()`-Echo in 403/401 wegen Information-Disclosure, kein Echo des rejected Values in 400 TypeMismatch wegen PII, Cause-Message-Cleanup auf erste Zeile bei `DataIntegrityViolation`); `UexClient.get*` (17 Methoden — verweisen auf das im Klassen-Javadoc beschriebene Fetch-Pattern statt das Pattern 17 mal zu wiederholen, Sondernotizen nur bei den Methoden mit echtem Spezifikum wie `getCommoditiesPricesAll` mit der 1-MB-Payload-Bemerkung); `SecurityConfig.filterChain/roleHierarchy/jwtAuthenticationConverter/corsConfigurationSource` (zentrale Auth-Konfig — Filter-Reihenfolge, Rollenhierarchie aus CLAUDE.md, JWT-Converter-Wiring, `allowCredentials=false` als load-bearing CSRF-Schutz-Note); Config-Bean-Methoden in `CacheConfig`/`DatabaseMigrationConfig`/`ETagConfig`/`JacksonConfig`/`OpenApi*Config`/`RateLimitingConfig`/`WebClientConfig`/`WebMvcConfig` (jeweils kontextbezogen mit Begruendung warum der Bean so konfiguriert ist). Generische Type-Level-Javadocs in `Views.Public`/`Views.Internal`, `StartupBannerListener.sanitiseJdbcUrl`-Schwesterklasse `StartupSizeTriggeringPolicy` und allen Properties-Klassen (`AppProblemProperties`, `KeycloakSyncProperties`, `RateLimitProperties`, `UexProperties`) wurden vom Skript-Default auf konkrete Beschreibungen aufgewertet. Eine versehentliche Doppel-Javadoc-Insertion auf `GlobalExceptionHandler.handleAccessDenied` (war bereits dokumentiert, der Pre-Pass hat trotzdem einen `@param`-Block davor gesetzt) wurde unmittelbar in den bestehenden Javadoc-Block konsolidiert. Spotless hat im Anschluss laengere 1-Zeiler-Javadocs auf 100-char Multi-Line umgebrochen. Verifiziert per `./gradlew :backend:test` (BUILD SUCCESSFUL in 54s, alle Backend-Tests gruen). Verbleibende Phase-2-Verstoesse: 623 in `backend.service` (238) + `backend.controller` (229) + `frontend.controller` (156).

- **Docs (Klassen- und Methoden-Javadoc fuer alle Backend-Repositories, 202 Checkstyle-Verstoesse weggeraeumt)**: Phase 2c der Checkstyle-Aufraeumaktion. Behandelt das gesamte `backend.repository`-Paket: 41 Klassen-Javadocs auf allen Spring-Data-Interfaces und 161 Methoden-Javadocs auf allen derived queries und Custom-`@Query`-Methoden. Klassen-Javadocs kamen ueber das erweiterte Hilfs-Skript (`build/_javadoc_insert.py`) mit neuer Suffix-Regel `*Repository` → `Spring Data repository for X`. Methoden-Javadocs wurden ueber ein zweites, dediziertes Hilfs-Skript (`build/_javadoc_methods_insert.py`) generiert, das die Spring-Data-Method-Name-Praefixe parst und passende 1-Zeiler emittiert: `findBy*` → "Derived Spring-Data query - returns entities matching X", `existsBy*` → "Returns {@code true} iff at least one row matches X", `countBy*` → "Returns the count of rows matching X", `deleteBy*` → "Deletes every row matching X", `findFirstBy*`/`findTopBy*` → "Returns the first matching X (limit 1)", `findAll*` → "Lists every entity. Overridden here to attach an @EntityGraph". Annotation-Detection ergaenzt den 1-Zeiler: `@EntityGraph` haengt "Eagerly fetches the configured relations via @EntityGraph" an, `@Lock` haengt "Acquires a pessimistic write lock" an. Die ~38 Custom-`@Query`-Methoden, die das Skript nur mit einem generischen Platzhalter bedienen konnte (Method-Name allein verraet die Semantik nicht), wurden unmittelbar im Anschluss manuell durch substanzielle Javadocs ersetzt — pro Methode wird konkret beschrieben, was die JPQL-/Native-Query tatsaechlich tut (Filter-Kombinationen, Projektions-Ziele, Bulk-Update-Semantik inkl. der `clearAutomatically/flushAutomatically`-Falle aus CLAUDE.md, Pessimistic-Lock-Vertrag), insgesamt ueber alle 10 betroffenen Files (InventoryItemRepository, MissionRepository, MaterialPriceRepository, JobOrderRepository, UserRepository, MaterialRepository, ShipRepository, RefineryOrderRepository, LocationRepository, MissionParticipantRepository). Verifiziert per `./gradlew :backend:test` (BUILD SUCCESSFUL, alle 1300+ Backend-Tests gruen).

- **Docs (Klassen- und Methoden-Javadoc fuer alle Backend-Mapper, 95 Checkstyle-Verstoesse weggeraeumt)**: Phase 2b der Checkstyle-Aufraeumaktion. Behandelt das gesamte `backend.mapper`-Paket: 22 Klassen-Javadocs (alle MapStruct-Interfaces plus die manuelle `EntityMappers`-Utility-Klasse) und 73 Methoden-Javadocs (`toDto`, `toEntity`, `toListDto`, `toReferenceDto`, nested mappers, `@AfterMapping`-Callbacks, MapStruct-`default`-Helfer). Klassen-Javadocs kamen ueber das erweiterte Hilfs-Skript (`build/_javadoc_insert.py`) mit neuer Suffix-Regel `*Mapper` → `MapStruct mapper between X entities and DTOs`. Methoden-Javadocs wurden alle manuell geschrieben mit Fokus auf die nicht-offensichtlichen Aspekte: ignorierte Server-Felder bei `toEntity`-Methoden (id, version, Timestamps, Aggregat-Kollektionen — werden vom Service rewired), Boolean-Konvertierung von UEX-`Integer` 0/1 in `MaterialMapper`, deterministische Sortier-Strategie in `JobOrderMapper.mapAndSortMaterials` (SCU-typed first, dann alphabetisch), Description-Redaction fuer Gaeste in `MissionMapper.resolveDescription`, Leader-First-Sortierung in `MissionMapper.resolveCrew`. Die `EntityMappers`-Utility-Klasse (5 statische Helfer fuer Faelle, in denen MapStruct nicht funktioniert) bekam ein erklaerendes Klassen-Javadoc statt des generischen Skript-Outputs. Spotless hat im Anschluss laengere 1-Zeiler auf Multi-Line umgebrochen. Verifiziert per `./gradlew :backend:test` (BUILD SUCCESSFUL in 59s, 1300+ Backend-Tests gruen).

- **Docs (Klassen- und Methoden-Javadoc fuer Frontend Config, Filter, Logging, Exception und Service-Layer, 59 Checkstyle-Verstoesse weggeraeumt)**: Phase 2h + 2j der Checkstyle-Aufraeumaktion. Behandelt alle Frontend-Pakete _ausser_ `controller` (das bleibt fuer Phase 2i): `frontend/config` (12 Klassen-Javadocs + 11 Methoden-Javadocs auf Beans), `frontend/filter` (1 Klassen-Javadoc), `frontend/logging` (2 Klassen-Javadocs + 8 Methoden-Javadocs auf `CorrelationContext` und `LogMasker`), `frontend/exception` (4 Methoden-Javadocs auf `GlobalExceptionHandler.handle*`), `frontend/service` (1 Klassen-Javadoc + 19 Methoden-Javadocs auf `BackendApiClient` und `BackendServiceException`) sowie `FrontendApplication` selbst — insgesamt 59 von 1 090 verbleibenden Phase-2-Verstoessen. Skripterweiterung (`build/_javadoc_insert.py`): zusaetzliche Suffix-Heuristiken (`Properties`, `Config`, `Filter`, `Editor`, `Handler`, `Policy`, `Layout`, `Context`, `Application`) plus Annotation-Detection fuer `@Configuration`, `@ConfigurationProperties`, `@ControllerAdvice` (cross-cutting Advice statt Controller-Endpoints), `@RestController` und `@Service`. Suffix-Wort wird vor der Vorlage gestrippt, damit aus `BackendRoleSyncFilter` "Servlet filter handling Backend Role Sync" wird statt "Servlet filter handling Backend Role Sync Filter". Methoden-Javadoc-Schwerpunkte: `SecurityConfig.filterChain/roleHierarchy/userAuthoritiesMapper` (zentrale Auth-Konfig — Filter-Reihenfolge, Rollenhierarchie, OIDC-Claim-Mapping); `WebClientConfig.webClient/publicWebClient/authorizedClientManager` (Resilience4j-Chain, OAuth2-Bearer-Relay, 16-MB-Codec); `BackendApiClient` Klassen-Javadoc neu geschrieben (WebClient-Wrapper, Resilience4j, RFC-7807-Decode) plus 17 HTTP-Verb-Wrapper-Methoden (GET/POST/PUT/DELETE/PATCH mit `Class<T>`/`ParameterizedTypeReference`/`isPublic`-Overloads, plus `getCached*` und `clearStaticDataCache`); `LogMasker.mask*` (Maskierungsstrategie pro PII-Kategorie); `GlobalExceptionHandler.handle*` (RFC-7807-Aufloesung, 404/400/500-Pfade, PII-Vermeidung in den Type-Mismatch-Logs). Verifiziert per `./gradlew :frontend:test` (BUILD SUCCESSFUL in 43s, Frontend-Test-Suite gruen).

- **Docs (Klassen- und Methoden-Javadoc fuer alle Backend- und Frontend-Model-/DTO-/Form-/UEX-Schichten, 262 Checkstyle-Verstoesse weggeraeumt)**: Phase 2a + 2g der Checkstyle-Aufraeumaktion. Behandelt drei zusammengehoerige "triviale" Schichten: `backend/dto/uex` (UEX-Inbound-Records, 31), `backend/model` + `backend/model/dto` + `backend/model/dto/request` (Entities, Enums, DTO-Records, 133), `frontend/model` + `frontend/model/dto` + `frontend/model/form` (Frontend-DTOs, Forms, Enums, 98) — insgesamt 262 von 1 352 verbleibenden Phase-2-Verstoessen. Klassen-Javadoc wurde grossteils ueber ein dediziertes Hilfs-Skript (`build/_javadoc_insert.py`, gitignored) eingefuehrt, das den Checkstyle-Report einliest und pro Verstoss eine knappe, kontextbezogene 1-Zeiler-Klassen-Beschreibung generiert (z. B. `Inbound JSON record for UEX Corp's /factions endpoint`, `City JPA entity`, `Form-binding object for Job Order input`, `Data transfer record carrying Mission payload`). Die UEX-Records wurden alle handgeschrieben (Verweis auf den synchronisierenden Service als Mapper-Zielentitaet) und sind die Vorlage fuer das Skript-Template. Methoden-Javadoc (13 Stellen) wurde vollstaendig manuell geschrieben — JPA-Lifecycle-Hooks (`@PrePersist/@PreUpdate`), Aggregat-Helfer (`addMaterial`, `addItem` mit bidirektionaler Back-Reference), abgeleitete Felder (`getEndsAt`, `frequencyTypeId`, `getJobSummary`) und Tuple-Mapping-Konvertierungs-Konstruktoren. Spotless laeuft im Anschluss drueber, sodass eingefuegte ein-zeilige Javadoc-Bloecke ggf. auf 100-char Multi-Line umgebrochen werden. Verifiziert per `./gradlew :backend:test :frontend:test` (BUILD SUCCESSFUL in 1m 40s, 1300+ Backend- und 491+ Frontend-Tests gruen). Die schwereren Schichten (Services, Controllers, Repositories, Mapper, Configs, Integration, Exception, Logging) bleiben fuer Folge-Sub-Phasen, weil sie inhaltliches Javadoc benoetigen, das nicht maschinell erzeugt werden kann; `enforceCheck` bleibt entsprechend weiterhin auf `false`.

- **Build (Massen-Reformat per `./gradlew spotlessApply`, 767 Java-Dateien angefasst — Checkstyle-Warnungen von 16 288 auf 1 796 reduziert)**: Phase 1 der Checkstyle-Aufraeumaktion. Der in Phase 0 eingebaute Spotless-Lauf (`googleJavaFormat()` + `removeUnusedImports()` + `formatAnnotations()`) wurde einmalig ueber den gesamten `backend/src/` und `frontend/src/` Baum gefahren. Diff-Profil: 767 modifizierte Java-Dateien, +64 325 / -58 146 Zeilen (Netto ~-6 000 Zeilen, weil 4-Space-Indent durch 2-Space ersetzt wurde). Inhaltliche Wirkung gegen Checkstyle: `IndentationCheck` 11 806 → 8, `LineLengthCheck` 1 507 → 82, `CustomImportOrderCheck` 1 074 → 0, `EmptyLineSeparatorCheck` 87 → 0, `OperatorWrapCheck` 57 → 0, `WhitespaceAroundCheck` 17 → 0. Was unveraendert blieb und in spaeteren Phasen separat behandelt wird: `MissingJavadocMethodCheck` (913) und `MissingJavadocTypeCheck` (439) — beide inhaltlich, brauchen eine Javadoc-Politik-Entscheidung; `AvoidStarImportCheck` (151) — google-java-format expandiert Star-Imports bewusst nicht, weil die Aufloesung mehrdeutig sein kann; `NeedBracesCheck` (131) — gehoert nicht zum google-java-format-Scope; restliche ~150 Warnungen Kleinkram. Reformat ist rein mechanisch — keine logischen Aenderungen — und wird durch eine begleitende `.git-blame-ignore-revs`-Datei aus `git blame`-Ausgaben gefiltert, damit Autorenschaft nicht maskiert wird (Devs muessen einmalig `git config blame.ignoreRevsFile .git-blame-ignore-revs` setzen). Volle Backend- (1300+) und Frontend-Test-Suiten bleiben gruen (`./gradlew :backend:test :frontend:test`, 1m 48s, 0 Failures). `enforceCheck` bleibt vorerst auf `false`; der Flip auf `true` erfolgt in Phase 4 nach Abschluss der Restthemen.

- **CI (`.github/workflows/codeql.yml` — Matrix um `language: actions` und `language: python` erweitert)**: Der ursprüngliche CodeQL-Workflow scannte nur `java-kotlin` und `javascript-typescript`. CodeQL hat seitdem einen eigenen Analyzer für GitHub-Actions-Workflow-Dateien (`language: actions`) bekommen, und die sieben unter `scripts/` getrackten Python-Helfer (`escape_umlauts.py`, `find_untranslated.py`, `fix_imports.py`, `fix_test_assertions.py`, `replace_rse.py`, `sync_messages.py`, `translate_en.py`) liefen bisher komplett ohne SAST-Abdeckung. Beide Lücken werden geschlossen. (1) `language: actions, build-mode: none` — der Actions-Analyzer findet die Klasse Bugs, die spezifisch für GitHub-Workflows ist: Script-Injection durch tainted `github.event.pull_request.title`/`body` in `run:`-Steps, `pull_request_target`-Misuse (Fork-PR läuft mit Repo-Secrets), Secret-Exfiltration über `echo $SECRET` in `run:`-Blöcken, gefährliche Kombinationen aus `contents: write` plus tainted Input. Konkret relevant für `refresh-versions.yml` (hat `contents: write` plus `pull-requests: write` und ruft `peter-evans/create-pull-request@v7` mit gerendertem Body auf), `dependency-check.yml` (lädt NVD-Feeds und uploaded SARIF mit `security-events: write`) und `ci.yml` (uploaded Test-/JaCoCo-/Checkstyle-/SpotBugs-Reports). Build-Mode `none`, weil Workflow-Dateien direkt aus YAML extrahiert werden — kein Setup-Step nötig, Laufzeit unter einer Minute. (2) `language: python, build-mode: none` — die sieben Dev-Helfer-Scripts sind reine Tooling-Komponenten, die der Maintainer lokal ausführt (Threat-Model entsprechend dünn — Angreifer wäre der Entwickler selbst), aber CodeQL findet trotzdem Path-Traversal-Patterns in den File-Operations (`open(arg)` auf User-Input-Pfaden in `escape_umlauts.py` und `sync_messages.py`), unsichere Subprocess-Aufrufe in `translate_en.py`, sowie Regex-Injection- oder Race-Condition-Patterns, die in den Scripts plausibel sind. Buildless-Python-Extraction ist der CodeQL-Standard; keine `setup-python`-Step nötig. Die existierende `paths-ignore`-Config (`**/build/**`, `**/node_modules/**`, `frontend/src/main/resources/static/js/vendor/**`, `**/*.min.js`) gilt automatisch auch für die zwei neuen Sprachen — für Python und Actions sind die Pfade ohnehin irrelevant, aber die Config ist sprachübergreifend. Die GitHub-generierte CodeQL-Advanced-Template (die das UI beim "Set up advanced configuration"-Klick anbietet) wurde bewusst NICHT übernommen, sie würde gegenüber dem existierenden Workflow auf zehn Achsen regressieren: Query-Suite zurück auf Default statt `security-extended`, `paths-ignore` weg, expliziter Gradle-Compile-Step durch einen Placeholder-Step mit `exit 1` ersetzt, `concurrency` und `timeout-minutes: 60` weg, `workflow_dispatch` weg, Schedule-Cron auf GitHub-Default `'27 17 * * 5'` statt der bewussten Versetzung gegen den Montag-OWASP-Run (`'17 6 * * 2'`), JDK-25-Setup und Gradle-Cache komplett weg, `actions/checkout` auf v4 statt v6 downgegradet, und `build-mode: none` für `java-kotlin`, der laut Template-Kommentar selbst "only analyzes Java" — Kotlin wäre damit ungescannt. Übernommen wurden ausschließlich die zwei Matrix-Sprachen, die echten Mehrwert liefern. **Voraussetzung zum Wirken**: Das im ursprünglichen Add-Entry beschriebene Config-Conflict-Problem bleibt bestehen, solange der Org-Admin das GitHub-Default-Setup-CodeQL nicht im UI deaktiviert (Settings → Code security → Code scanning → CodeQL analysis → von "Default" auf "Advanced" umstellen). Ohne diesen Schritt scheitert auch der erweiterte Workflow weiterhin mit "advanced configurations cannot be processed when the default setup is enabled". **Branch-Protection-Konsequenz**: Die Check-Namen-Matrix wächst von zwei auf vier ("CodeQL / Analyze (actions)", "CodeQL / Analyze (java-kotlin)", "CodeQL / Analyze (javascript-typescript)", "CodeQL / Analyze (python)"). Falls die zwei ursprünglichen Check-Namen bereits als Required Status Checks in der `main`-Branch-Protection gepinnt sein sollten, müssen die zwei neuen erst einmal durchlaufen, bevor sie ebenfalls pin-bar werden — GitHub kennt einen Check-Namen erst nach dem ersten erfolgreichen Run auf dem Target-Branch.

- **Backend (`@Transactional`-Konvention auf alle Services vereinheitlicht: Klassen-Level `readOnly = true` als Default, Methoden-Level nur noch für Writes)**: Bislang war die `@Transactional`-Verteilung im Service-Layer inkonsistent. Etwa die Hälfte der 38 Services trug bereits `@Transactional(readOnly = true)` auf Klassenebene und überschrieb nur die Write-Methoden mit einer eigenen Annotation (`UserService`, `AnnouncementService`, `JobTypeService`, `StarSystemService`, `MissionService`, `MaterialService`, `SquadronService`, `ShipTypeService`, `HangarService`, `LocationService`, `RefineryOrderService`, `RefiningMethodService`, `ManufacturerService`, `RoleService`, `PersonalInventoryItemService`, `FrequencyTypeService` — alle mit dem Pattern). Die andere Hälfte hatte kein Klassen-Level-Default und annotierte jede Methode einzeln (`OperationService`, `OperationFinanceService`, `SystemSettingService`, `MissionSecurityService`, `JobOrderService`, `JobOrderHandoverService`, `JobOrderHandoverReportService`, `InventoryItemService`, `MissionFinanceEntryService`, `ProfitCalculationService`, `HangarImportService`, plus alle sechs `Uex*Service`-Sync-Klassen). Die zweite Variante war fehleranfällig: drei konkrete Read-Methoden waren komplett ohne `@Transactional` — `MaterialCategoryService.findAll`/`findById`, `TerminalService.getAllTerminals`/`getTerminal`, `JobOrderService.findAllActiveReference`. Ohne Annotation öffnet Hibernate für jede `findById`/`findAll`-Query eine eigene Mini-Transaktion (Auto-Commit-Modus) und die LazyInitializationException-Falle bei verschachtelten Entity-Zugriffen wird zur Zeitbombe — sichtbar nur, wenn der Aufrufer (Controller) keine eigene Transaktion eröffnet. (Im aktuellen Projekt hatten viele Controller selbst `@Transactional`, was den Bug verdeckte — das ist aber Anti-Pattern und sollte nicht der Grund sein, warum es funktioniert.) Fix in zwei Phasen: (1) **Lücken schließen**: Die drei oben genannten Methoden bekommen eine `@Transactional(readOnly = true)`-Wirkung via Klassen-Level-Default. (2) **Konvention vereinheitlichen**: Auf allen Services mit DB-Operationen wird das im Projekt schon etablierte "Klassen-Level `@Transactional(readOnly = true)` + Methoden-Level `@Transactional` nur für Writes"-Pattern angewendet. Dadurch entfallen ca. 25 redundante `@Transactional(readOnly = true)`-Methoden-Annotationen, der Code wird prägnanter, und neu hinzugefügte Read-Methoden bekommen automatisch das richtige Default-Verhalten. Write-Methoden behalten ihre explizite `@Transactional`-Annotation (überschreibt den Class-Level-`readOnly`), `@Transactional(propagation = MANDATORY)` (z. B. `JobOrderService.completeJobOrderWithinTransaction`) bleibt erhalten — die Annotation ist hier ein Vertrag, der dokumentiert dass die Methode nur innerhalb einer bestehenden Transaktion aufgerufen werden darf. Drei Services bleiben ohne `@Transactional`, weil sie keine direkten DB-Operationen ausführen: `AuthHelperService` (reiner `SecurityContextHolder`-Wrapper), `KeycloakService` (HTTP-Calls an Keycloak Admin API), `UexScheduler` (delegiert nur an andere `Uex*Service`-Methoden, die ihre eigene Transaktion mitbringen — eigene Klassen-Level-Transaktion würde alle Sync-Schritte in eine langlebige Mega-Transaktion einbetten und ist explizit unerwünscht).

- **Frontend (Operation-Details inline-editierbar, Edit-Modal entfernt — Pattern angeglichen an `mission-detail.html`)**: Bisher war das Bearbeiten einer Operation eine Modal-basierte Choreographie: ein "Edit"-Button im Details-Header öffnete via JavaScript ein vollwertiges Edit-Modal (eigene `<form id="edit-operation-form">` mit Hidden-Version-Feld, eigene `openEditModal(...)`-JS-Funktion, die die Daten via `data-*`-Attributen reinkopiert), das beim Save zur Operations-Liste weiterleitete. Mission-Details haben dieses Pattern vor längerem aufgegeben zugunsten von Inline-Form-Feldern, die per `th:disabled="${!canEdit}"` rollen-gated sind, und einem Save-Button im Buttons-Box am Panel-Ende — Operation-Details ziehen jetzt nach. Implementierung: (1) `OperationPageController.operationDetails` bekommt einen `Authentication`-Parameter und legt `canEdit = hasMissionManagerRole(authentication)` aufs Model — true für ROLE_ADMIN, ROLE_OFFICER, ROLE_MISSION_MANAGER (auch die DB-Flag-Variante, weil der JWT-Converter den Flag in dieselbe Authority übersetzt). Die Berechnung läuft am HTTP-Boundary, kein `SecurityContextHolder` im Service — selber Pattern wie bei der OperationController-`canOverrideStatus`-Logik. (2) Im Template wird die alte "Read-Only-Darstellung mit Edit-Button"-Box ersetzt durch eine `<form id="operation-form" method="post">`-Box, die direkt die drei Felder `name`/`status`/`description` als reguläre Inputs/Select/Textarea enthält, jeweils mit `th:disabled="${!canEdit}"`. Das `version`-Feld ist ein Hidden-Input für die Optimistic-Lock-Validierung. Direkt unter der Form, im selben `mission-column`-Container, steht jetzt eine Buttons-Box mit Delete (nur für ADMIN), Save (nur wenn `canEdit`, `<button form="operation-form">`-Attribut bindet den Submit über die Form-ID) und einem zusätzlichen Back-Button — identisch zur mission-detail Anordnung. (3) Edit-Modal-HTML komplett entfernt (~30 Zeilen weniger Template-Code) inklusive der `openEditModal(...)`-JS-Funktion. Die Delete-Modal-Mechanik bleibt erhalten — Delete ist destruktiv und benötigt weiterhin eine Bestätigung. Tests: zwei neue Cases in `OperationPageControllerMvcTest` pinnen das `canEdit`-Verhalten. `operationDetail_readOnlyUser_seesDisabledFormAndNoSaveButton` (`@WithMockUser(roles = "KRT_MEMBER")`) asserted dass `<form id="operation-form">` und `<input id="op-name">` zwar gerendert werden, aber das `disabled`-Attribut im HTML steht und kein `form="operation-form"`-Submit-Button existiert. `operationDetail_missionManager_seesEnabledFormAndSaveButton` (`@WithMockUser(roles = "MISSION_MANAGER")`) asserted das Gegenteil. Beide nutzen den neuen `stubDetailEndpoints`-Helper, der alle vier Backend-Calls (Operation, Missions, Finances, Payouts) auf leere/dummy-Daten stubbt. Frontend-Test-Suite bleibt gruen.

- **Backend (`OperationService.updateOperation` nimmt jetzt die `OperationUpdateDto` direkt entgegen, `OperationMapper.updateEntity` entfernt)**: Bisheriger Flow im Controller war eine fragile Choreographie: (1) `operationService.getOperationById(id)` lieferte die Entity zurück, (2) `operationMapper.updateEntity(updateDto, operation)` patchte die DTO-Felder via MapStruct in die Entity, (3) der Service rief erneut `findById` auf und prüfte die Version. Problem: Der Mapper hatte `unmappedTargetPolicy = ReportingPolicy.IGNORE` und keinen expliziten `@Mapping(target = "version", ignore = true)` — MapStruct mappte deshalb implizit auch die `version` der DTO auf die geladene (potenziell gemanagte) Entity. Der nachfolgende manuelle Versions-Check im Service nutzte diese mapper-übertragene Version. Das funktionierte zufällig richtig, aber war komplett unsichtbar im Code und durch keinen Test pinned — eine MapStruct-Refaktorisierung, ein neues Mapping-Profil oder das Hinzufügen weiterer `@Mapping(target = X, ignore = true)`-Direktiven (mit Copy-Paste-Fehler) hätte die Optimistic-Locking-Garantie still gekippt. Fix: das Pattern an `JobOrderService.updateJobOrderStatus` angeglichen, das im Projekt schon seit langem das Vorbild ist. (1) `OperationService.updateOperation(UUID, Operation)` umgestellt auf `updateOperation(UUID, OperationUpdateDto)` — Service liest die Versions-Erwartung direkt aus `updateDto.version()`, kein Umweg mehr über eine gemanagte Entity. (2) `OperationMapper.updateEntity(OperationUpdateDto, @MappingTarget Operation)` ersatzlos gestrichen — keine Call-Site mehr, weil der Service die DTO-Felder per Setter direkt anwendet. Mapper-Interface enthält nur noch `toDto(Operation)` für Outbound und `toEntity(OperationCreateDto)` für Create. (3) `OperationController.updateOperation` reduziert sich auf eine einzige Zeile: `return operationMapper.toDto(operationService.updateOperation(id, updateDto));` — die doppelte `getOperationById`+`mapper.updateEntity`-Choreographie ist weg. Tests entsprechend angepasst: `OperationServiceTest.UpdateOperationTests` (4 Tests) baut die incoming-Daten jetzt als `OperationUpdateDto` statt als nackte `Operation`-Instanz, der `acceptsNullVersionInIncoming_asBypassToken`-Test dokumentiert explizit, dass der Bypass-Pfad an der DTO-Boundary nur noch durch Service-interne Aufrufer erreichbar ist (Controller-Boundary erzwingt `@NotNull` auf `version`). `OperationMapperTest` verliert den `updateEntity_shouldOverwriteScalarFieldsButNotIdTimestampsOrMissions`-Test (Methode existiert nicht mehr). Volle Backend-Suite bleibt gruen.

- **Docs (`Running the Local Test Stack`-Section im README + harte Test-Credential-Regel in CLAUDE.md)**: Bisher gab es zwei dokumentierte Wege, den Stack lokal hochzuziehen — `--profile dev up -d ...` mit nur den Deps (Apps via Gradle bootRun) und `--profile dev up -d` als voller Dev-Stack. Beide ziehen die produktive `.env` mit echten Postgres-Passwoertern, Keycloak-Admin-Secrets und SSL-Keystore-Passwoertern. Beim UI-Verifikations-Spinup im Worktree war damit Production-Credential-Drift unvermeidlich (Container-Volumes persistieren ueber Worktrees, MCP-Preview-Snapshots haengen an, `docker volume prune` traegt Spuren mit). Neue dritte Variante: isolierter Test-Stack auf Basis von `.env.test` + dediziertem Test-`keystore.p12` + gestripptem Test-`realm-export.json` + `docker-compose.test.yml`-Override. Der README-Abschnitt `Running the Local Test Stack` (zwischen dem Full-Dev-Stack-Abschnitt und Local Development Setup) listet die acht Schritte explizit: `.env.test` mit Throwaway-Credentials, `keytool`-Befehl zum Keystore-Generieren (mit SAN `localhost`/`127.0.0.1`/`backend`), Python-Skript-Vorlage zum Ableiten des Test-Realms aus dem Produktiv-Export (SMTP geleert, Password-Policy weg, `backend-service`-Secret ausgetauscht, `users` durch single `test-admin`/`test-admin-pw` ersetzt mit Roles `Admin`/`Officer`/`KRT Member`), Erklaerung der `KC_HOSTNAME=host.docker.internal`+`extra_hosts: host-gateway`-Loesung im Override-Compose (gleicher OIDC-Issuer aus dem Host-Browser und aus den Containern), Cleanup-Workaround fuer die `/var/iri/db-{backend,keycloak}`-Bind-Mount-Datadirs bei Postgres-Auth-Failures (`MSYS_NO_PATHCONV=1`-Prefix fuer Git-Bash auf Windows), `up`/`down --volumes`-Befehle. CLAUDE.md bekommt zwei Anpassungen: (1) der `Local stack`-Abschnitt listet die Test-Stack-Variante als vierten Compose-Befehl neben dev/prod, mit Verweis auf die README; (2) der `Testing`-Abschnitt enthaelt jetzt die harte Regel "Never use production / real credentials in tests or local test stacks" mit Aufzaehlung der verbotenen Inputs (produktive `.env`, geteilter `keystore.p12`, geteiltes `realm-export.json`, echte Client-Secrets/DB-Passwoerter/SMTP-Credentials/Admin-Passwoerter/Signing-Keys) und der Begruendung (alles was in Worktree/CI-Log/Container-Volume/Screenshot landet, gilt als geleakt und muss rotiert werden — und Recovery ist teurer als von vornherein nicht zu leaken).

- **Frontend (`OperationPageController` von `Map<String, Object>` auf typisierte DTOs umgestellt, `OperationDto` auf Backend-Shape aligned)**: Der `OperationPageController` war der einzige Page-Controller, der noch durchgängig mit `Map<String, Object>` und `ParameterizedTypeReference<PageResponse<Map<String, Object>>>` arbeitete — Stringly-typed, kein Compile-Check bei Backend-Schema-Drift, keine IDE-Auto-Complete. Gleichzeitig deklarierte `OperationDto` fünf Felder (`description, startTime, endTime, status, version`), die nirgends gelesen wurden, plus `startTime`/`endTime`, die nicht einmal im Backend-`OperationDto` existierten (Jackson hat sie auf Read auf `null` gesetzt und beim Write zwei tote Felder ins JSON serialisiert). Fix in zwei Schritten: (1) `OperationDto` auf die tatsächlich vom Backend gelieferte und vom Frontend gelesene Shape gebracht: `record OperationDto(UUID id, String name, String description, String status, Long version)` — also `id, name` (für das Operations-Dropdown im Mission-Editor und das Form-Building in `MissionPageController`) plus `description, status, version` (für die Operations-Tabelle und Detail-Box). `createdAt`/`updatedAt` aus der Backend-Shape sind weiterhin nicht im Frontend-DTO, weil nichts sie liest — Jackson schluckt sie via Default-`FAIL_ON_UNKNOWN_PROPERTIES=false`. `status` als `String` und nicht als `OperationStatus`-Enum-Mirror, weil die Status-Werte ohnehin Strings sind, sobald sie i18n-aufgeloest werden. (2) `OperationPageController` komplett auf typisierte DTOs umgestellt: `PageResponse<OperationDto>` statt `PageResponse<Map<String, Object>>` fuer die Liste, `OperationDto` statt `Map<String, Object>` fuer das Detail, `PageResponse<MissionListDto>` statt `PageResponse<Map<String, Object>>` fuer die eingebettete Missions-Tabelle (MissionListDto existierte bereits und wurde nur eingehaengt). `Map`-Import entfaellt, das `java.util.List<de.greluc.krt.profit.basetool.frontend.model.dto.OperationPayoutDto>`-Inline-Generic-Monster aus dem Payout-Call wurde auf den normalen `List<OperationPayoutDto>`-Import zurueckgesetzt. Die zwei Konstruktor-Aufrufstellen in `MissionPageController` (Zeile 795-796, 855-856) verlaengern sich wieder von `new OperationDto(uuid, null)` auf `new OperationDto(uuid, null, null, null, null)` — netto ist das aber gewuenscht, weil jedes der fuenf DTO-Felder jetzt nachweisbar von mindestens einem Konsumenten gelesen wird (id/name vom Mission-Dropdown, description/status/version von den Operations-Templates). `OperationPageControllerMvcTest` entsprechend angepasst: keine `Map.of(...)`-Stubs mehr, stattdessen `new OperationDto(...)` und `new MissionListDto(...)`. Frontend-Test-Suite bleibt gruen.

- **Backend (`OperationService.deleteOperation` löscht Missionen nicht mehr mit)**: Beim Löschen einer Operation wurde bislang die komplette Aggregat-Kette mit aufgeräumt — alle verknüpften Missionen wurden via `cascade = CascadeType.ALL` mitgelöscht, `InventoryItem.mission` und `RefineryOrder.mission` per Bulk-Update auf `null` gesetzt, und sämtliche `MissionFinanceEntry`-Zeilen der betroffenen Missionen per `deleteByMissionIdIn` weggeräumt. Damit ging beim Entfernen einer (oft nur organisatorischen) Operation der gesamte operative Verlauf der zugehörigen Missionen verloren. Neues Verhalten: die Operation wird gelöscht, jede Mission behält ihre Identität und alle eigenen Verknüpfungen (Participants, Finance-Einträge, Inventory-Items, Refinery-Orders) — nur die Rück-Referenz `Mission.operation` wird auf `null` gesetzt, sodass die Mission als operations-lose Mission weiterläuft. Umsetzung: (1) `cascade = CascadeType.ALL` aus `Operation.missions` entfernt, sodass JPA den Delete nicht mehr nach unten propagiert; das DB-FK-Constraint `ON DELETE SET NULL` auf `mission.operation_id` bleibt als Sicherheitsnetz erhalten. (2) `OperationService.deleteOperation` iteriert die geladenen Missionen, setzt `mission.setOperation(null)` (Dirty-Checking emittiert die `UPDATE mission SET operation_id = NULL`-Statements), leert die In-Memory-Collection für konsistente Bidirektionalität und ruft dann `operationRepository.delete(operation)`. (3) Die nicht mehr benötigten Service-Abhängigkeiten `InventoryItemRepository`, `RefineryOrderRepository`, `MissionFinanceEntryRepository` und der direkte `EntityManager` (samt manuellem `flush/clear` für den `@Modifying`-Bulk-Update-Pfad) fliegen aus dem Konstruktor. Tests entsprechend angepasst: neuer Mockito-Unit-Test `deleteOperation_unlinksMissions_butDoesNotDeleteThem` in `OperationServiceTest` prüft, dass `Mission.operation` nach dem Delete `null` ist und die `Operation.missions`-Collection geleert wurde; der bestehende `OperationDeletionIntegrationTest` wurde in `testDeleteOperationKeepsMissionAndAllItsReferences` umgeschrieben und verifiziert per `@SpringBootTest`, dass nach `DELETE /api/v1/operations/{id}` Mission, InventoryItem, RefineryOrder, MissionFinanceEntry und MissionParticipant alle noch existieren und InventoryItem/RefineryOrder/FinanceEntry weiterhin auf die (jetzt operations-lose) Mission zeigen.

- **Tests (`UexRefinerySyncService`-Coverage 0 % -> 100 %)**: Neuer `UexRefinerySyncServiceTest` (14 Mockito-Tests) deckt beide Sync-Methoden (`syncRefiningMethods`, `syncRefineryYields`) vollstaendig ab. Spezifisch getestet: Empty-Response-Abort wischt nicht die lokale Tabelle weg, In-Place-Update existierender Rows behaelt `id`/`@Version` (Optimistic-Locking-Kontrakt), Skip-Pfade bei fehlendem Namen oder leerem/blanken Namen (Refining Methods) sowie bei `null` `idCommodity`/`idTerminal`/`value` (Yields), Skip-Pfad fuer unbekannte Materials oder Terminals beim Yield-Sync (Yields duerfen NIE Placeholder-Materials erzeugen — der Commodity-Catalog ist die einzige Wahrheitsquelle), Mixed-Batch-Verarbeitung mit ArgumentCaptor-Verifikation.

- **Tests (Frontend `GlobalExceptionHandler`-Coverage 59 % -> hoeher)**: 13 neue Tests fuer die bislang nicht ueberdeckten Handler-Methoden und Edge-Cases in `GlobalExceptionHandlerTest`. Neue Tests: `handleNoResourceFoundException` (404 + Modellattribute), `handleTypeMismatch` mit JSON- und HTML-Branch inkl. defensive Null-Check fuer `getRequiredType()` (rare aber valide), `handleAccessDenied` fuer beide Spring-Security-Exception-Typen (`AccessDeniedException` legacy plus `AuthorizationDeniedException` Spring-Security-6.x — beide muessen erfasst werden, da `@PreAuthorize` heute die neue Variante wirft, alte Code-Pfade die alte), `handleException` generischer Catch-All inkl. WebClient-Cause-Unwrap (eine 503-Antwort vom Backend muss als 503 zur Error-Page propagiert werden, nicht als blankes 500), JSON-via-`X-Requested-With`-Detection (legacy XHR-Aufrufe ohne `Accept: application/json`), korrekter Verzicht auf `correlationId`-Key wenn nicht gesetzt (sonst rendert das Toast-Snippet "null"), `reloadHint`-Flag fuer `CONFLICT` und `OPTIMISTIC_LOCK` (Frontend nutzt das fuer Auto-Reload nach Toast-Dismiss), Fallback auf 500 bei nicht-resolvbarem HTTP-Status.

- **Tests (`ProfileController`-Coverage drastisch erhoeht via Pure-Mockito-Unit-Test)**: Neuer `ProfileControllerTest` (17 Tests) ergaenzt den bestehenden MVC-Integrationstest mit Detail-Asserts ueber die OIDC-Claim-vs-Backend-Praezedenz, die in den MVC-Tests nicht greifbar waren. Konkret: Unauthenticated → Redirect home (kein Backend-Call), Backend-Werte ueberschreiben Token-Claims, Backend-Unavailable behaelt Token-Daten, Multi-Value-OIDC-Claims (Keycloak listet `description=[a,b,c]`) werden zum ersten Element entrollt, leere Liste bleibt Liste (sonst NPE im Template), Backend-Response mit `null` ueberschreibt nicht versehentlich (`description`-Key-Anwesenheit vs. Wert), unparsbares `joinDate` wird stumm verworfen, `version` als `Number`/`String`/Unparsbar wird zu Long-or-0. POST `/profile/description` mit allen vier Branches: Happy-Path mit `successToast`, BindingResult-Errors rendern Profil ohne Backend-Call, Optimistic-Lock-Conflict (mit Mockito-Spy auf `getProblemType()` da der echte Pfad einen ProblemDetail-Decoder braucht) → `concurrency.conflict`-Toast, generische Backend- oder Runtime-Exception → `profile.update.failed`-Toast. Null-Felder im Form-Submit werden als leerer String an die Backend-API geschickt (sonst landet der Literal-String "null" in der DB).

- **Tests (Backend-Controller-Coverage 56 % -> 69 % im `..backend.controller..`-Paket)**: Zwoelf neue Pure-Mockito-Unit-Tests fuer die Delegations-Controller, die bislang gar keinen dedizierten Test hatten. Alle als `@ExtendWith(MockitoExtension.class)` ohne Spring-Context. (1) `SquadronControllerTest` (7 Tests): Pagination-Wrapping inkl. `includeInactive`-Flag-Forwarding, `update`-Endpoint forwardet die DTO direkt (kein Mapper-Roundtrip — Service entscheidet, welche Felder mutable sind), Mapper-Roundtrip nur bei `create`. (2) `MaterialCategoryControllerTest` (5 Tests): Listen-Reihenfolge vom Service durchgereicht, CRUD-Mapper-Roundtrip. (3) `AnnouncementControllerTest` (4 Tests): kritischer `getPublicAnnouncement`-Pfad mit `Optional.empty → 204 No Content` (kein Fake-DTO), Admin-Endpoint immer mit Body. (4) `TerminalControllerTest` (4 Tests): PUT-Endpoint propagiert ausschliesslich `hidden`-Flag (UEX-importierte Felder bleiben unangetastet — Regression haette Admins Renaming via PUT erlaubt). (5) `LocationControllerTest` (8 Tests): `stripServerManaged()` strippt `id`/`version` aus dem POST-Body (Mass-Assignment-Schutz mit `ArgumentCaptor`-Beweis), `update` forwardet die DTO direkt, `lookup` umgeht den Mapper komplett (sensible Felder leaken sonst). (6) `UserControllerTest` (14 Tests): `/me`-Endpoints leiten die Id ausschliesslich aus dem JWT ab (nie URL — Impersonation-Schutz), Logistician/Mission-Manager-Patches forwarden `true`/`false` verbatim, `lookupUsers` umgeht den Mapper. (7) `ManufacturerControllerTest`, `StarSystemControllerTest`, `RefiningMethodControllerTest`, `ShipTypeControllerTest`, `JobTypeControllerTest`, `MaterialCollectionControllerTest` (5+5+5+5+7+2 Tests): die kleinen UEX-/Master-Data-Controller mit Pagination + CRUD-Delegations, jeweils mit Mapper-Roundtrip-Assertions und Verifizierung der `includeHidden`/`includeInactive`-Flag-Weiterleitung; `ShipTypeController` testet zusaetzlich, dass die List-Methode `shipTypeToDto` (nicht die grosse `toDto`-Variante) nutzt — eine Regression haette zu viel ueber den Endpoint geleakt. 73 neue Tests gesamt; `..backend.controller..` 56 % -> 69 % Instruction-Coverage, Backend gesamt 81 % -> 83 %; 1297 Backend-Tests, 0 Failures.

- **Build (Gradle Wrapper auf 9.5.1 angehoben)**: `gradle/wrapper/gradle-wrapper.properties` zieht jetzt `gradle-9.5.1-bin.zip` (vorher 9.4.1). Die Dokumentationsreferenzen in `README.md` und `backend/.junie/AGENTS.md` wurden auf 9.5.1 nachgezogen (letztere stand noch auf 9.4.0). Reiner Wrapper-Bump — Build-Skripte, `versions.properties` und Plugin-IDs bleiben unangetastet; beim naechsten `./gradlew`-Aufruf laedt der Wrapper die neue Distribution automatisch nach.

- **Build (Lombok / MapStruct / Spring-Boot-Application-Boilerplate aus JaCoCo-Coverage ausgeschlossen)**: Damit der JaCoCo-Report nur ueber Code reportet, der ueberhaupt einen sinnvollen Test verdient hat, sind drei Klassen von Generated-Code jetzt aus der Coverage-Berechnung herausgenommen. (1) **Lombok**: neue Datei `lombok.config` am Repository-Root setzt `lombok.addLombokGeneratedAnnotation = true` und `config.stopBubbling = true`. JaCoCo 0.8.2+ honoriert jede Annotation, die `Generated` heisst und CLASS- oder RUNTIME-Retention hat — `@lombok.Generated` ist RUNTIME — also fallen Lombok-generierte Getters/Setters/`@Builder`-Boilerplate/`toString`/`equals`/`hashCode` aus dem Bericht ohne weitere JaCoCo-Konfiguration. (2) **MapStruct**: dessen `@Generated` ist `javax.annotation.processing.Generated` mit SOURCE-Retention, also fuer JaCoCo zur Bytecode-Zeit unsichtbar. Loesung: zentral im Root-`build.gradle.kts` unter `plugins.withId("jacoco")` `classDirectories.setFrom(...)` mit Filterung `**/*MapperImpl.class` und `**/*MapperImpl$*.class`. Wichtig: die MapStruct-Interfaces (`*Mapper`) bleiben drin — die haben `default`-Methoden, Comparators und Business-Helper wie `computeProfit(...)`, die testbar sind und auch getestet werden. (3) **Spring-Boot-Main**: das jeweilige `*Application.class` (One-Line-`SpringApplication.run(...)`-Stubs in jedem Modul) wird ebenfalls per Pattern ausgenommen. Resultat: `..backend.dto.uex..` springt von 30 % auf 95 %, `..backend.mapper..` von 90 % auf 96 % — beides waere ohne den Exclude weiter Lombok- bzw. MapStruct-Boilerplate-Rauschen gewesen.

- **Tests (Frontend Proxy-Controller und Backend Small-Controller — Coverage-Nachzug nach dem Generated-Exclude)**: Fuenf neue Test-Klassen schliessen die nach Lombok-/MapStruct-Exclude jetzt sichtbaren Luecken in der Controller-Schicht. (1) Neuer `MaterialProxyControllerTest` (Frontend, 6 Mockito-Tests): URI-Aufbau fuer `getMaterialTerminals(id)` und `getProfitCalculation(shipId, starSystemNames)` — letzteres incl. Multi-Value-Query-Param-Anhang (NICHT CSV-encoded), Null-Backend-Response wird zu leerer Liste normalisiert damit Thymeleaf nicht NPEt. (2) Neuer `HangarDeleteAllProxyControllerTest` (Frontend, 4 Tests mit MockWebServer): 204-Happy-Path, 4xx/5xx-Pass-Through via `ResponseStatusException` mit gleichem Status, Verbindungsabbruch wird zu sanitisiertem 500. (3) Neuer `HangarImportProxyControllerTest` (Frontend, 6 Tests mit MockWebServer): Multipart-Upload-Forwarding mit korrektem Content-Type / Name="file" / Filename, Fallback-Filename `fleetview.json` wenn `getOriginalFilename()` `null` ist (MockMultipartFile normalisiert das zu `""` — daher Inline-Anonymous-Subclass-Override), 4xx/5xx-Backend-Passthrough, IOException beim `getBytes()` wird zu 500 gewrappt, Connection-Failure ebenfalls 500. (4) Neuer `SystemControllerTest` (Backend, 2 Tests): v1-Ping liefert Legacy-`Map<String,String>` verbatim (Monitor-Kompat), v2-Ping liefert `PingResponse` mit UTC-Instant zwischen Call-Start und Call-Ende — Regression-Schutz fuer die CLAUDE.md-„All times in UTC"-Regel an der Boundary. (5) Neuer `SystemSettingControllerTest` (Backend, 3 Tests): Controller-zu-Service-Delegation pro Endpoint, Key-Verbatim-Forward (Regression haette zwei Rows fuer denselben Setting-Key produziert). (6) Neuer `FrequencyTypeControllerTest` (Backend, 8 Tests): Pagination-Wrapping (`PageResponse` aus Spring-`Page` korrekt zusammengebaut), CRUD-Delegation mit Mapper-Round-Trip, Reorder-Endpoint forwardet ID-Liste in Insertion-Order (Service nutzt sie zur sortIndex-Berechnung — Reordering haette die Sortierung gekillt). 31 neue Tests insgesamt; Backend-Controller-Paket 53 % -> 56 %, Frontend-Controller-Paket 49 % -> 50 %.

- **Tests (UEX-Sync-Service-Coverage: gesamtes `..backend.service..`-Paket 69 % -> 82 %)**: Vier neue / erweiterte Test-Klassen decken die bisher fast vollstaendig untestete UEX-Synchronisations-Ebene ab. (1) Neuer `UexVehicleServiceTest` (8 Mockito-Tests, keine SpringContext): leere UEX-Antwort wischt nicht die lokale Tabelle weg, Vehicles ohne Name werden uebersprungen, Manufacturer-Lookup case-insensitive mit nicht-fataler Fallback-Klausel, In-Place-Update auf existierenden ShipType mit konservierter `id`/`version`/`@Version`, Description-Builder schreibt nur Felder die tatsaechlich gesetzt sind (kein „Crew: null"-Geraffel), Wiki-URL hat Vorrang vor Store-URL, Batch mit mixed-validity-Eintraegen wird vollstaendig verarbeitet. (2) Erweiterter `UexCommodityServiceTest` (vorher 1 Test, jetzt 15): beide Synchronisations-Pfade (Commodity-Katalog plus Price-Sync), `determineMaterialType`-Branches (REFINED / RAW / NO_REFINE), Reuse-by-id_commodity vs. backfill-by-name, Placeholder-Material mit `NO_REFINE`-Default fuer unbekannte Commodity-IDs im Price-Sync, Per-Row-Exception-Swallowing damit ein verkorkster UEX-Datensatz nicht den ganzen Batch killt, korrekte Status-Buy/Sell-Konvertierung Integer-1/0 <-> Boolean, dateModified Unix-Epoch-Sekunden zu `Instant`. (3) Neuer `UexSchedulerTest` (4 Tests): exakt-einmalige Invocation jeder Sync-Methode, dokumentierte Phasen-Ordnung (Universe-Basics → Catalogue → Refineries) per `InOrder.verify(...)`, Exceptions aus dem inneren Service werden vom outer `try/catch` geschluckt sodass Spring `@Scheduled` die naechste Tick-Invocation nicht suppressed. (4) Erweiterter `UexUniverseSyncServiceTest` (vorher 2 Tests, jetzt 24): pro Sync-Methode (`syncCities`, `syncFactions`, `syncJurisdictions`, `syncMoons`, `syncOrbits`, `syncOutposts`, `syncPlanets`, `syncPois`, `syncSpaceStations`, `syncTerminals`) je ein Happy-Path-Upsert-Test plus die Sonderfaelle "leere Response = Abort", "DTO ohne id = Skip", und fuer Cities/SpaceStations zusaetzlich das Materialisieren der zugehoerigen `Location` nur fuer `isAvailableLive=true`. JaCoCo nach diesem Patch: `..backend.service..` 69 % -> 82 %, Backend gesamt 74 % -> 82 % Instruction-Coverage; 1210 Backend- und 491 Frontend-Tests laufen gruen.

- **Tests (Frontend `BackendApiClient` Happy-Path-Coverage via MockWebServer)**: Neuer `BackendApiClientHappyPathTest` (22 Tests) deckt die bisher untestrierten Erfolgs-Pfade aller HTTP-Verben auf dem Frontend-WebClient-Wrapper ab. Die existierenden Sibling-Tests (`BackendApiClientResilienceTest` fuer Resilience4j-Fehlerklassifikation, `BackendApiClientProblemJsonTest` fuer RFC-7807-Decoding) haben den Service-Layer-Fehlerpfad bereits gut abgedeckt; was fehlte war: GET mit `Class<T>` und mit `ParameterizedTypeReference<T>`, dasselbe in den `getCached(...)`-Overloads, jeweils inkl. `isPublic=true`/`false`-Variante (verifizert via Default-Header welcher WebClient genommen wurde), POST/PUT/PATCH mit und ohne Body (der `body != null`-Branch in den `execute*`-Methoden), und DELETE inkl. `Void.class`-204-Pfad. Frontend-Service-Paket: 63 % -> 84 %; Total-Coverage Frontend 56 % -> 57 % Instruction-Coverage. Keine neuen Dependencies — `okhttp3.mockwebserver` war im Frontend schon im Catalog.

- **Tests (`UexClient`-Coverage 3 % -> 83 % via MockWebServer)**: Neuer `UexClientTest` mit OkHttp-`MockWebServer` deckt jetzt alle 17 GET-Methoden des UEX-API-Clients ab. Drei systematische Verhaltensmuster pro Endpoint: (1) Happy-Path JSON `{"status":"ok","data":[...]}` wird geparst zurueckgegeben, (2) 5xx-/Netzwerk-Fehler -> `onErrorResume`-Fallback liefert eine leere Liste statt eine Exception nach oben durchzureichen, (3) leerer `data`-Array -> leere Liste. Drei „Schwergewichts"-Endpoints (Commodities, CommoditiesPrices, StarSystems) bekommen volle Schema-Parsing-Assertions; die restlichen 14 Endpoints werden ueber den `assertHitsEndpoint(...)`-Helper als "trifft den richtigen URI-Pfad mit GET"-Smoke-Test abgedeckt — damit faellt ein versehentliches Endpunkt-Vertauschen in `UexProperties` (z. B. `terminalsEndpoint` <-> `outpostsEndpoint`) sofort auf. `DISCONNECT_DURING_REQUEST_BODY`-Socket-Policy fuer Connection-Drop-Test. Neue Test-Dependency `testImplementation(libs.okhttp3.mockwebserver)` in `backend/build.gradle.kts` (Version 5.3.2 war bereits im Catalog vom Frontend). 19 neue Tests; Paket `..backend.integration..` von 3 % auf 83 % Instruction-Coverage.

- **Tests (`JobOrderMapperImpl`-Coverage 1 % -> 100 %)**: Der bisherige `JobOrderMapperTest` hat nur die `mapAndSortMaterials`-Default-Methode ueber anonymes Subclassing exerziert und liess die MapStruct-generierte `*Impl` quasi unberuehrt. Neu: 8 Tests gegen die echte `Mappers.getMapper(JobOrderMapper.class)`-Instanz mit `ReflectionTestUtils.setField(...)`-Verkabelung der drei `@Autowired`-Sub-Mapper (`UserMapper`, `MaterialMapper`, `JobOrderHandoverMapper`). Abgedeckt: kompletter `JobOrder` -> `JobOrderDto`-Pfad inkl. Materials-Sortierung (SCU-Gruppe zuerst, dann case-insensitive alphabetisch), Assignees-Liste ueber `UserMapper`, Handovers-Liste mit FK-Flatten von `jobOrder.id` zu `jobOrderId`; `currentStock` bleibt explizit `null` (Service-Verantwortung); null-Material-Verteidigung im Comparator; null-Sets liefern null-Listen (MapStruct-Default), null-Source liefert null fuer beide Overloads.

- **Memory-Hinweis (`*MapperImpl`-Wiring im Unit-Test ohne Spring-Context)**: Die generierten MapStruct-`*Impl`-Klassen sind `@Component` mit `@Autowired`-Feldern. In reinen Unit-Tests muss daher nach `Mappers.getMapper(...)` jedes Sub-Mapper-Feld per `ReflectionTestUtils.setField(mapper, "fieldName", subMapper)` injiziert werden. Pattern jetzt konsistent in `InventoryItemMapperTest`, `ShipMapperTest`, `JobOrderHandoverMapperTest`, `JobOrderMapperTest`, `RefineryOrderMapperTest`.

- **Tests (Mapper-Coverage massiv ausgebaut — 17 % -> 90 % im `..backend.mapper..`-Paket)**: 17 neue Mapper-Test-Klassen und ein neuer Test fuer den `DeprecationInterceptor` decken die bislang weisse Flaeche zwischen MapStruct-Codegen und Entity/DTO-Boundary ab. Alle Tests sind reine JUnit/Mockito-Unit-Tests ohne Spring-Context (`Mappers.getMapper(...)` plus `ReflectionTestUtils.setField(...)` fuer das manuelle Verkabeln der `@Autowired`-Sub-Mapper in `InventoryItemMapperImpl`, `ShipMapperImpl`, `JobOrderHandoverMapperImpl` und `RefineryOrderMapperImpl` — TestContainers wird hier bewusst nicht angefasst, das Coverage-Ziel sind die Mappings selbst, nicht der Spring-Lifecycle). Konkret neu: `AnnouncementMapperTest`, `FrequencyTypeMapperTest`, `ManufacturerMapperTest`, `MaterialCategoryMapperTest`, `MaterialMapperTest` (inklusive Integer-1/0 <-> Boolean-Konvertierung der `isIllegal/isVolatileQt/isVolatileTime`-Flags und `stripServerManaged`-Mass-Assignment-Schutz), `RoleMapperTest`, `RefiningMethodMapperTest`, `LocationMapperTest` (inklusive `stripServerManaged`), `OperationMapperTest` (Create/Update/Read-Trennung, Server-managed-Felder bleiben unangetastet), `ShipMapperTest`, `StarSystemMapperTest`, `TerminalMapperTest`, `SystemSettingMapperTest`, `InventoryItemMapperTest` (FK-Flattening fuer `jobOrderId`/`missionId`/`missionName`), `JobOrderHandoverMapperTest`, `RefineryOrderMapperTest` (Schwerpunkt `computeProfit = oreSales - expenses - otherExpenses` inklusive Null-Toleranz und Loss-Fall) sowie `EntityMappersTest` (statische Utility-Klasse fuer User/JobType/Squadron, inkl. `effectiveName`-Fallback und Private-Constructor-Garantie). Plus `DeprecationInterceptorTest` (8 Tests, kein `@SpringBootTest`): Method-Level vs. Class-Level `@ApiDeprecation` und `@Deprecated`, Sunset-Header-Format `EEE, dd MMM yyyy HH:mm:ss 'GMT'`, Link-Header `</api/v.../>; rel="alternate"`, malformaler Sunset wird geloggt und uebersprungen statt zu werfen. Gesamtbild Backend nach diesem und dem `UexClientTest`/`JobOrderMapperTest`-Patch oben: 74 % Instruction-Coverage (vorher 44 %), 60 % Branch-Coverage; 1161 Backend- und 469 Frontend-Tests laufen gruen.

- **Backend (`HangarService.getSquadronOverview` ohne `SecurityContextHolder`)**: Die Methode entschied bisher selbst per `SecurityContextHolder.getContext().getAuthentication()`, ob die Antwort die per-Schiff-Owner-Details enthaelt (ADMIN/OFFICER) oder nur die aggregierten Counts (alle anderen). Damit war die Authentifizierung in die Business-Logik gemischt und der Service nicht thread-/scheduling-sicher (siehe Anmerkung im ArchUnit-Kommentar). Refactor: die Methode bekommt einen expliziten `boolean includeOwnerDetails`-Parameter, der Rollen-Check liegt jetzt in `HangarController.getSquadronOverview` und nutzt das von Spring Security injizierte `Authentication`-Objekt (`Authentication`-Parameter wird automatisch befuellt) plus `GrantedAuthority`-Stream. Damit erfuellt der Service die Architektur-Regel "Service-Layer faesst nicht direkt auf SecurityContextHolder", die jetzt strikt ist — `HangarService` ist nicht mehr in der Allow-List in `ArchitectureTest.SECURITY_CONTEXT_HOLDER_EXCEPTIONS`; nur `UserService` (legitimer Auth-Helper) bleibt drin. Die HTTP-API-Semantik des Endpunkts bleibt unveraendert: nicht-authentifizierte Aufrufer (oder Squadron-Members) sehen weiterhin nur Counts, ADMIN/OFFICER sehen den Details-Breakdown.

- **Tests (ArchUnit-Architekturtests fuer CLAUDE.md-Regeln)**: Neue Test-Klassen `backend/.../ArchitectureTest.java` und `frontend/.../ArchitectureTest.java` setzen die vier in CLAUDE.md formulierten Architektur-Invarianten mechanisch durch und scheitern den Build, sobald jemand eine neue Verletzung committed: (1) **Service-Layer faesst nicht direkt auf `SecurityContextHolder`** — nur `UserService` (legitimer Auth-Helper) und `HangarService` (bekannte bestehende Ausnahme, dokumentiert mit Follow-up zur Refactoring-Aufgabe) sind in der Allow-List; jede neue Klasse im `..backend.service..`-Paket, die SecurityContextHolder benutzt, faellt durch. (2) **Controller geben keine JPA-Entities zurueck** — `noMethods()` in `..backend.controller..` darf keinen Raw-Return-Type haben, der mit `@jakarta.persistence.Entity` annotiert ist; aktuell sauber. (3) **Jeder `@RestController` hat mindestens irgendwo `@PreAuthorize`** — entweder auf Klassen-Ebene oder mindestens an einer Methode; `SystemController` hat bei dieser Gelegenheit explizite `@PreAuthorize("permitAll()")`-Annotationen an den beiden Public-Ping-Endpoints bekommen. Die strengere Variante "jede Handler-Methode hat eigenes `@PreAuthorize`" waere ein groesserer Refactoring-Schritt (~57 Methoden ohne Methoden-Annotation, die bisher ueber `SecurityConfig.requestMatchers().permitAll()` laufen) und ist als Follow-up dokumentiert. (4) **Frontend haengt nicht direkt am Spring Data JPA / JDBC** — `noClasses()` in `..frontend..` darf nichts aus `org.springframework.data.jpa..` oder `java.sql.Connection/Statement/PreparedStatement/DriverManager` referenzieren. Dependency: `com.tngtech.archunit:archunit:1.3.2` (Core, nicht `archunit-junit5` — letzteres bringt eine kollidierende JUnit-Platform-Version mit; wir rufen `rule.check(CLASSES)` aus Plain-`@Test`-Methoden auf). CLAUDE.md verlinkt jetzt aus dem "Security model"-Abschnitt auf die neuen Tests.

- **Docs (Flyway-Migrations-Konventionen formalisiert)**: Neue Datei `backend/src/main/resources/db/migration/README.md` fasst die bisher nur muendlich tradierten Spielregeln fuer Schema-Migrationen zusammen: Namensschema `V<n>__<snake_case>.sql`, "one logical change per migration"-Regel, ddl-auto-validate-Pflicht, Zweiphasen-Drop fuer `DROP TABLE`/`DROP COLUMN` (Phase 1 stop-writing + Entity-Anpassung in Release N, Phase 2 `DROP COLUMN` erst in Release N+1 nach Production-Deploy + Grace-Period), Idempotenz-Pflicht fuer Backfill-`UPDATE`-Statements, Performance-/Lock-Hinweise zu `ALTER TABLE` auf grossen Tabellen, Verweis auf `${flyway:noTransaction}` fuer `CREATE INDEX CONCURRENTLY`, Test-Caveat (Test-Profile setzt `flyway.enabled=false`, dev-Stack vor Merge starten), File-Header-Konvention (Warum, nicht Was), und eine Pre-Merge-Checkliste. CLAUDE.md verlinkt jetzt auf das neue Dokument, damit der Verweis aus dem Hauptkontext nicht verlorengeht.

- **Docs (README erweitert um Keycloak-Theme-Sektion)**: Die `keycloak-theme/krt-theme`-Custom-UI (FreeMarker-Templates + CSS + Schriften fuer Keycloak-Login und -Account-Console) war in der README bisher nicht beschrieben. Neue `## Keycloak Theme`-Sektion erlaeutert die Verzeichnisstruktur (`login/` mit `keycloak`-Parent, `account/` mit `keycloak.v3`-Parent, jeweils mit `theme.properties`, `krt-*-v3.css` und `Ethnocentric`/`Lato`-Schriften), wie der Theme ueber den `./keycloak-theme/krt-theme:/opt/keycloak/themes/krt-theme`-Bind-Mount in den Keycloak-Container kommt, dass der `loginTheme`/`accountTheme` ueber das realm-export-Profil auf `krt-theme` gesetzt wird und welche Schritte fuer Theme-Aenderungen noetig sind (Edit -> `docker compose restart keycloak` -> Browser-Hard-Reload, kein Backend-Rebuild noetig). Zusaetzlich kurz die anderen Top-Level-Verzeichnisse erwaehnt, die bisher in der `## Project Structure`-Sektion fehlten: `design/` (Brand-Assets + Styleguide-Quelle) und `scripts/` (Wartungs-Skripte fuer i18n-Sync etc.).

- **Build (Pitest 1.19.0 als Mutation-Testing-Tool, On-Demand)**: Gradle-Plugin `info.solidsoft.pitest` 1.19.0 ist jetzt in beiden Modulen aktiv und am Root zentral konfiguriert. Aufruf via `./gradlew :backend:pitest` bzw. `:frontend:pitest`. Der Task ist bewusst NICHT in `check` eingehaengt — ein voller Pitest-Lauf dauert pro Service in der Groessenordnung von Minuten und wuerde den Standard-PR-Build sprengen; Mutation-Testing soll on-demand laufen, etwa als wochentliches CI-Job oder bevor groessere Refactorings starten. Konfiguration im `subprojects { plugins.withId("info.solidsoft.pitest") { ... } }`-Block am Root: `targetClasses = "...basetool.<module>.service.*"` (nur Business-Logik mutieren, keine DTOs/Mapper/Repositories/Config), `targetTests = "...basetool.<module>.service.*Test"`, `threads = 4`, `outputFormats = ["HTML", "XML"]`, `timestampedReports = false`. JVM-Args werden lazily aus der `testRuntimeClasspath` aufgebaut, damit Mockito 5+ in PIT's Minion-Prozessen ueber `-javaagent:<mockito-core>` self-attachen kann und `--enable-native-access=ALL-UNNAMED` greift. Hinweis: erster Pitest-Lauf bricht voraussichtlich ab, weil einige Service-Tests einen Spring-Kontext erwarten, den PIT's isolierte Minion-Prozesse nicht starten. Folge-Schritte: betroffene Tests entweder in `excludedTestClasses` aufnehmen oder als `*IntegrationTest` umbenennen, danach Baseline messen und `mutationThreshold` / `coverageThreshold` aktivieren. Reports landen unter `<module>/build/reports/pitest/index.html`.

- **Build (JaCoCo-Coverage auch im Backend)**: Das `jacoco`-Gradle-Plugin war bisher nur im Frontend aktiv. Backend bekommt es jetzt analog (`plugins { id("jacoco") }`), und die gemeinsame Konfiguration (XML + CSV + HTML Reports, `Test`-Task finalisiert `jacocoTestReport`) wandert in einen `subprojects { plugins.withId("jacoco") { ... } }`-Block im Root — die bisherige Frontend-spezifische Wiring (`tasks.withType<Test>().configureEach { finalizedBy(tasks.jacocoTestReport) }` plus `tasks.jacocoTestReport { reports { ... } }`) entfaellt entsprechend. Reports landen bei jedem Test-Lauf unter `<module>/build/reports/jacoco/test/jacocoTestReport.{xml,csv}` und `<module>/build/reports/jacoco/test/html/index.html`. Erstmessung (informativ, kein Gate):
  - Backend: INSTRUCTION 60.1 %, LINE 64.2 %, BRANCH 43.7 %, METHOD 56.0 %, CLASS 76.8 %.
  - Frontend: INSTRUCTION 53.6 %, LINE 52.1 %, BRANCH 42.0 %, METHOD 63.1 %, CLASS 69.9 %.

- **Build (Checkstyle 13.4.2 mit Google Java Style)**: Drittes statisches Analyse-Tool neben SpotBugs und OWASP Dependency-Check. Plugin `checkstyle` (Gradle-Core, kein Drittanbieter — `plugins { checkstyle }` reicht), `toolVersion = "13.4.2"` (aktuelle Release zum Zeitpunkt des Imports, siehe https://github.com/checkstyle/checkstyle/releases/tag/checkstyle-13.4.2). Konfiguration aus dem offiziellen Google Java Style Guide (`google_checks.xml`), heruntergeladen vom Checkstyle-13.4.2-Release-Tag und unter `config/checkstyle/google_checks.xml` eingecheckt — damit der Build offline reproduzierbar bleibt und ein gepinntes Profil hat. Zentrale Plugin-Konfiguration im `subprojects { plugins.withId("checkstyle") { ... } }`-Block am Root: `configFile = rootProject.file("config/checkstyle/google_checks.xml")`, `isIgnoreFailures = true` und `maxWarnings = Int.MAX_VALUE` (Build-Gate ist initial nicht-blockierend, weil der existierende Code-Stil 4er-Indent + nicht zwingend Javadoc-fuer-jedes-public benutzt und Google's Style entsprechend ~10.000 Warnings im Backend und ~6.300 im Frontend findet); Test-Source-Set deaktiviert (`tasks.matching { it.name == "checkstyleTest" }.configureEach { enabled = false }`), weil Test-Code Google-Style-Verstoesse wie laengere Methodennamen mit Unterstrich (`should_returnX_when_Y`) und laengere BDD-Assertion-Zeilen produziert, die als Rauschen ueberwiegen wuerden. Reports landen unter `<module>/build/reports/checkstyle/main.html`; der HTML-Report ist fuer Code-Review optimiert. `:<module>:checkstyleMain` haengt automatisch an `:<module>:check` (Gradle-Core-Verhalten). Naechste Schritte: nach Triage der Initial-Findings Indent auf 2 normalisieren ODER ein Suppression-Profil ueberlegen.

- **Build (statische Analyse: SpotBugs + OWASP Dependency-Check)**: Beide vorgesehenen Tools aus ANALYSIS.md 5.1 sind jetzt im Build verkabelt. (1) **SpotBugs** — Plugin `com.github.spotbugs-base` 6.5.4 (Direkt-DSL pro Modul, nicht ueber den Version-Katalog), `-base`-Variante damit keine Source-Set-Tasks automatisch erzeugt werden; pro Modul wird genau ein `SpotBugsTask` fuer den `main`-Source-Set registriert und an `:<module>:check` gehaengt. Konfiguration: `effort=DEFAULT`, `reportLevel=HIGH` (nur high-confidence-Findings), `ignoreFailures=true` (Build-Gate ist initial nicht-blockierend, bis die Baseline trianguliert ist); Reports unter `<module>/build/reports/spotbugs/main.{html,xml}`. Initialer Lauf: Backend 0 Findings, Frontend 1 HIGH-Confidence-Finding (`NP_NULL_PARAM_DEREF` in `BackendServiceException.fromProblem` — null fuer Non-Null-Parameter im 7-Argument-Konstruktor; separater Follow-up). (2) **OWASP Dependency-Check** — Plugin `org.owasp.dependencycheck` 12.2.0 (Direkt-DSL am Root-Projekt) inkl. NVD-CVE-Datenbank-Lookup; aggregiert ueber alle Subprojekte via `./gradlew dependencyCheckAggregate`. Konfiguration: `failBuildOnCVSS=11.0f` initial (de-facto kein Gate-Fail), `formats=["HTML","SARIF"]` (HTML fuer Code-Review, SARIF fuer GitHub-Code-Scanning-/CI-Importer), Output unter `build/reports/dependency-check/`. NVD-Api-Key wird ueber Project-Property `nvdApiKey` aufgenommen, um Public-Rate-Limits zu umgehen — auf CI via `-PnvdApiKey=<key>`. Erstinvocation laedt den NVD-Feed (~500 MB, ~5-15 min, gecacht unter `~/.gradle/dependency-check-data`); spaetere Laeufe sind Sekunden. Beilaeufig CycloneDX-Plugin von 3.2.3 auf 3.2.4 angehoben (refreshVersions-Update). Verfuegbare Tasks: `dependencyCheckAggregate`, `dependencyCheckAnalyze` (pro-Subprojekt), `dependencyCheckUpdate` (NVD-Cache erneuern), `dependencyCheckPurge`.

- **Backend (PDF-Generator wirft kein `ResponseStatusException` mehr)**: Der letzte verbliebene `ResponseStatusException`-Aufruf im Service-Layer (`JobOrderHandoverReportService` bei IOException waehrend PDF-Generierung) wurde durch eine generische `RuntimeException("PDF generation failed", e)` ersetzt. Damit greift `GlobalExceptionHandler.handleAllExceptions`, das die i18n-Variante von `problem.internal_error.title/detail` zurueckgibt, die Correlation-ID setzt und den vollen Stacktrace mit MDC-Kontext loggt — anstatt die englische Reason-Phrase ungefiltert an den Client zu reichen. `JobOrderHandoverReportControllerTest.previewHandoverReport_shouldPropagateServiceException` entsprechend auf `assertThrows(RuntimeException.class, ...)` umgestellt; die nicht mehr genutzten Imports von `HttpStatus` / `ResponseStatusException` im Service entfernt.

- **Backend (Timezone-Serialisierungstests deutlich erweitert)**: Der bisher vorhandene `InstantSerializationTest` (eine einzige Happy-Path-Assertion) wurde durch `TimezoneSerializationTest` mit zehn Tests ersetzt, die den CLAUDE.md-Kontrakt "Times in UTC — store/process as Instant or OffsetDateTime" voll abdecken: (1) `Instant` serialisiert immer mit `Z`-Suffix, (2) `Instant` Round-Trip ueber Epoche, Jetzt und ferne Zukunft, (3) `Instant`-Deserialisierung mit Nanosekunden-Genauigkeit, (4) `Instant` aus Offset-Suffix `+02:00` wird auf UTC normalisiert, (5)-(7) `OffsetDateTime`-Serialisierung mit positiven, negativen und `Z`-Offsets verbatim, (8)-(9) `OffsetDateTime`-Deserialisierung normalisiert auf UTC (Jackson-Default `ADJUST_DATES_TO_CONTEXT_TIME_ZONE=true` — konsistent mit der CLAUDE.md-Regel), (10) `LocalDateTime` in `HandoverReportPreviewRequestDto` ohne Offset durchgereicht (kein impliziter Zeitzonen-Roundtrip), und (11) DST-Spring-Forward-Boundary `2026-03-29` Europe/Berlin: Wert vor und nach 02:00 sind nicht 2 sondern 1 absolute Stunde auseinander, was den fehlenden 02:00-03:00-Slot validiert. Damit ist die in ANALYSIS.md 3.5 angesprochene Luecke geschlossen.

- **Docker (HEALTHCHECK fuer Backend und Frontend)**: Beide Dockerfiles erhalten eine `HEALTHCHECK`-Direktive, die den Spring-Boot-Actuator-Health-Endpoint anpingt (`https://localhost:11261/actuator/health` bzw. `https://localhost:18081/actuator/health`, jeweils mit `curl -k`, da die Container HTTPS mit Self-Signed-Cert servieren). Damit kann `docker compose` Container per `condition: service_healthy` orchestrieren; das `frontend-(dev)`-Service nutzt das jetzt fuer `backend`/`backend-dev` (vorher `condition: service_started`). Konkret: (1) `backend/src/main/resources/application.yml` ergaenzt einen `management:`-Block, der `endpoints.web.exposure.include=health` setzt und `endpoint.health.show-details=never`/`show-components=never` erzwingt — die Antwort ist auf `{"status":"UP"}` reduziert, sodass unauthentifizierte Clients keine internen Komponenten enumerieren koennen; `endpoint.health.probes.enabled=true` aktiviert die Liveness/Readiness-Sub-Endpoints fuer Kubernetes-aehnliche Umgebungen. (2) `backend/.../SecurityConfig.java` `permitAll`-t `/actuator/health` und `/actuator/health/**`; alle anderen Actuator-Endpoints fallen weiterhin unter `anyRequest().authenticated()`. (3) Frontend bekommt erstmals `org.springframework.boot:spring-boot-starter-actuator` als Dependency (siehe `frontend/build.gradle.kts`); analoger `management:`-Block plus `permitAll` im SecurityConfig. (4) `BotProtectionFilter` blockte bisher `/actuator/*` komplett als Scanner-Pfad; eine neue `LEGITIMATE_PATHS`-Whitelist (aktuell nur `/actuator/health`) wird in `isBotPath(...)` _vor_ der Praefix-Pruefung konsultiert, sodass `/actuator/health[/...]` durchgereicht wird, alle anderen `/actuator/...`-Pfade aber weiterhin als Bot-Pfad mit HTTP 404 abgewiesen werden. Korrespondierende Updates in `BotProtectionFilterTest` (Whitelist-Verhalten als eigener Test, das alte `/actuator/health`-Bot-Path-Assertion-Beispiel durch `/actuator/env` ersetzt). (5) Beide Dockerfiles installieren `curl` + `ca-certificates` im finalen Image (Eclipse-Temurin-JRE-Jammy enthaelt es nicht standardmaessig, etwa +5 MB pro Image) und konfigurieren `HEALTHCHECK --interval=30s --timeout=5s --start-period=60s --retries=3 CMD curl -fsSk <url>/actuator/health || exit 1`. Die `start-period=60s` deckt JVM-Warm-up + Tomcat-Bind + Flyway ab, sodass Compose den Container waehrend des Boot-Vorgangs nicht in `unhealthy` flippt. Frontend-SBOM (`frontend/docs/frontend-bom.{json,xml}`) regeneriert mit der neuen Actuator-Dependency.

- **Build (gemeinsame Test/JavaCompile/BootRun-Konfiguration zentralisiert)**: `tasks.withType<Test>`, `tasks.withType<JavaCompile>` und `tasks.withType<org.springframework.boot.gradle.tasks.run.BootRun>` waren bisher in `backend/build.gradle.kts` und `frontend/build.gradle.kts` praktisch identisch dupliziert (JUnit-Platform, native-access-JVM-Arg, Spring-Profile, Mockito-Java-Agent-Anhängen, UTF-8-Encoding, `-Xlint:unchecked`/`-Xlint:deprecation`). Die gemeinsame Konfiguration wandert in einen `subprojects { plugins.withId("java") { ... } plugins.withId("org.springframework.boot") { ... } }`-Block im Root-`build.gradle.kts` — DRY und reagiert nur dort, wo das Java- bzw. Spring-Boot-Plugin tatsächlich angewendet ist. Frontend behält die JaCoCo-Erweiterung (`tasks.withType<Test>().configureEach { finalizedBy(tasks.jacocoTestReport) }`) als modulspezifischen Zusatz. Beiläufig vereinheitlicht: Backend bekommt jetzt ebenfalls den `-parameters`-Compiler-Flag (war vorher nur Frontend), was Spring/Jackson-Reflection auf Method-Parameter-Namen zuverlässiger macht und CLAUDE.md-Konventionen folgt. BootRun-Konfiguration nutzt `tasks.named<JavaExec>("bootRun")` (BootRun erbt von JavaExec), damit der Root-Build keine direkte Abhängigkeit zur Spring-Boot-Plugin-Klasse braucht. Verifiziert via `./gradlew :{backend,frontend}:compileJava --debug` (zeigt `-parameters`, `-Xlint:*`) und Test-Runner-Kommandozeile (zeigt `-javaagent:.../mockito-core-5.20.0.jar` und `-Dspring.profiles.active=test` für beide Module).

- **Backend/Frontend (Raffinerieauftrag — Kostenfelder optional)**: Die drei Geldfelder eines Raffinerieauftrags (`expenses`, `otherExpenses`, `oreSales`) sind nun explizit optional und werden beim Speichern als `null` persistiert, falls der Wert leer oder `0` ist. Hintergrund: die Felder sind im UI mit `0` vorbelegt, sodass Nutzer beim "nichts eintragen" semantisch keine `0` setzen wollen — die bisherige Pflichtfeld-Markierung war daher inkonsistent. Konkret: (1) `RefineryOrderForm` (Frontend): `@NotNull` an `expenses`, `otherExpenses` und `oreSales` entfernt, `@Min(0)` bleibt; (2) Thymeleaf-Templates `refinery-orders-create.html` und `refinery-orders-details.html`: `required`-Attribut von den Inputs `otherExpenses` und `oreSales` entfernt; (3) `RefineryOrderPageController` (Frontend): neue Helper-Methode `zeroToNull(Double)` mappt `null` und `0.0` einheitlich auf `null`, wird in beiden DTO-Bauten (Create- und Update-Pfad) für `expenses`, `otherExpenses`, `oreSales` verwendet; (4) `RefineryOrderService` (Backend): analoge `zeroToNull`-Logik ersetzt die bisherigen `... != null ? ... : 0d`- und Default-`0`-Zuweisungen — `0` wird jetzt konsistent als `null` gespeichert; (5) Entity `RefineryOrder`: `@Column(nullable = false)` an `otherExpenses`/`oreSales` entfernt und Java-Default `= 0d` an allen drei Feldern entfernt, sodass die Spalten nullable sind. Die Profit-Berechnung (`RefineryOrder#getProfit`, `RefineryOrderMapper#computeProfit`, `OperationFinanceService`, `MissionFinanceEntryService#calculateTotalSum`) behandelt `null` bereits als `0`, sodass Gewinn/Verlust und Einsatz-Bilanz unverändert korrekt bleiben.

- **Frontend (i18n)**: Das Label des Kostenfelds im Raffinerieauftrag wurde von "Kosten (aUEC)" auf "Auftragskosten (aUEC)" umbenannt, um es klarer vom benachbarten Feld "Sonstige Kosten" abzugrenzen. Geändert wurde ausschließlich der i18n-Schlüssel `refineryorder.form.expenses` in `messages.properties` und `messages_de.properties` (sowie konsistent "Order Expenses (aUEC)" in `messages_en.properties`); andere Vorkommen von "Kosten" im Projekt (z. B. `profit.col.load_cost`, `refineryorder.rating.cost`) wurden bewusst nicht angefasst. Die Fallback-Texte in den Thymeleaf-Templates `refinery-orders-create.html` und `refinery-orders-details.html` wurden ebenfalls angeglichen.

- **Infrastruktur (docker-compose.yml)**: Heap-Dump-Konfiguration und persistente `/tmp`-Volumes für `backend` und `frontend` entfernt (Option B). Konkret entfallen die JVM-Flags `-XX:+HeapDumpOnOutOfMemoryError -XX:HeapDumpPath=/tmp/heapdump.hprof` aus `JAVA_TOOL_OPTIONS` sowie die Bind-Mounts `/var/iri/backend:/tmp` bzw. `/var/iri/frontend:/tmp`. Damit bleibt `/tmp` containerintern und flüchtig (kein Aufblähen des Host-Volumes durch Tomcat-Tempfiles, keine sensiblen Heap-Dumps mit Tokens/JWTs auf dem Host). Konsequenz: Bei einem OutOfMemoryError wird kein Heap-Dump mehr persistiert; falls Crash-Forensik wieder benötigt wird, kann später ein dediziertes Dump-Verzeichnis (Option A) eingeführt werden.

- **Frontend (Auftragsdetails)**: In der Auftragsdetail-Ansicht (`orders-detail.html`) wurde die Überschrift „Übergaben" zwischen den Bearbeiter-Bereich und die Buttons „Übergabe protokollieren" / „Materialsammelübersicht" verschoben. Die Überschrift ist nun immer sichtbar (nicht mehr nur wenn Übergaben vorhanden sind) und verwendet dasselbe Styling wie die „Bearbeiter"-Überschrift (`color: var(--color-primary)`, Lato Bold 700). Die doppelte, versteckte `<h3>`-Überschrift innerhalb des bedingten Übergaben-Blocks wurde entfernt.

- **AGENTS.md (Testregel verschärft)**: Die CRITICAL JUNIE RULE „GRADLE FOR TESTS ONLY" wurde um ein explizites Verbot des `run_test`-Agent-Tools ergänzt. Das Tool `run_test` ist ab sofort ausdrücklich untersagt; Tests müssen ausschließlich über das `bash`-Tool mit dem entsprechenden Gradle-Befehl ausgeführt werden.

- **Frontend (Einsatz-Detailseite)**: Die verbleibenden Roh-`fetch`-Flows `addMissionManager`, `removeMissionManager` und `changeMissionOwner` in `mission-detail.html` laufen jetzt über den gemeinsamen `window.MissionSubresource.patchSubResource`-Helper (Section-Keys `manager` bzw. `owner`). Dadurch erhalten alle drei Aktionen automatisch das einheitliche 409-Handling mit KRT-Styled Toast + Reload-Angebot (statt generischem `showFrontendErrorToast`), den standardisierten Success-Toast via i18n-Keys `mission.save.section.manager`/`.owner` und konsistentes CSRF-Header-Handling. Die Backend-Proxy-Pfade (`POST|DELETE /missions/{id}/managers/{userId}` und `PUT /missions/{id}/owner/{userId}`) sind unverändert — sie zeigen bereits seit Paket 3A auf die schlanken Backend-`/slim`-Varianten. Damit ist die Einsatz-Detailseite frei von rohen `fetch`-Flows für Sub-Sections; Konflikte anderer Nutzer in Manager-/Owner-Änderungen führen nie mehr zu einem stillen Fehlschlag, sondern zu einem expliziten KRT-Toast mit Reload-Option (AGENTS.md CRITICAL JUNIE RULE: FRONTEND DOM VERSION SYNC + CONCURRENCY).

- **Tests**: `./gradlew :frontend:test` weiterhin grün (158/158); kein MissionSubresource-spezifischer Pfad testbar in MVC-Tests, die Änderungen betreffen ausschließlich die Client-JS-Aufrufseite; die bestehenden Backend-Proxy-Endpoints sind unverändert und bleiben durch die vorhandenen `MissionPageControllerMvcTest`-Fälle abgedeckt.

- **Frontend (Einsatz-Detailseite, Paket 3C Option c)**: Das Crew-Sub-Panel (Crew-Slots innerhalb der Units) in `mission-detail.html` nutzt jetzt AJAX über `window.MissionSubresource.patchSubResource`. Hinzufügen (Crew zuweisen), Bearbeiten und Löschen eines Crew-Mitglieds laufen über drei neue Frontend-Proxy-Endpoints (`POST /missions/{id}/units/{unitId}/crew/ajax`, `PUT /missions/{id}/units/{unitId}/crew/{crewId}/ajax`, `DELETE /missions/{id}/units/{unitId}/crew/{crewId}/ajax` — alle `@ResponseBody`, JSON, `@PreAuthorize("isAuthenticated()")`), die direkt auf die schlanken Backend-`/slim`-Pfade zeigen. Damit bumpt keine Crew-Änderung mehr die Parent-`Mission.version` — gleichzeitig in anderen Sub-Panels (Stammdaten, Teilnehmer, Finanzen, Frequenzen, Units-Meta …) geöffnete Formulare behalten ihre Eingaben und werden nicht mehr durch spurious 409-Conflicts invalidiert (Option A, AGENTS.md CRITICAL JUNIE RULE: CONCURRENCY). Die Add-Buttons `.assign-crew-btn` und Edit-Buttons `.edit-crew-btn` propagieren neu `data-action-ajax` + `data-unit-id` (+ `data-crew-id` bei Edit) auf die gemeinsamen Modals `#assign-crew-form`/`#edit-crew-form`; das gemeinsame Delete-Confirm-Modal wurde um eine crew-spezifische Routing-Logik erweitert, die zusätzlich `data-sub-unit-id` auf dem Form propagiert, damit der AJAX-Handler die verschachtelte URL `/units/{u}/crew/{c}/ajax` bauen kann. Der neue IIFE-Block `wireCrewAjax` bindet Submit-Listener an Add/Edit und den Delete-Confirm-Flow. Bei einem echten 409 zeigt der `MissionSubresource`-Handler den KRT-Styled Toast mit Reload-Angebot (keine nativen `confirm()`/`alert()`); bei Erfolg wird die Seite neu geladen, um die verschachtelte Units-/Crew-Darstellung konsistent aus dem Server-Rendering zu beziehen. Die klassischen POST-Routen (`/units/{u}/crew`, `/units/{u}/crew/{c}/update`, `/units/{u}/crew/{c}/delete`) bleiben als Non-JS-Fallback unverändert erhalten.

- **Tests**: Drei neue MockMvc-Testfälle in `MissionPageControllerMvcTest` (`addCrewAjax_WithValidBody_ShouldReturn200`, `updateCrewAjax_WithBackendConflict_ShouldReturn409`, `deleteCrewAjax_Success_ShouldReturn204`) sichern die neuen AJAX-Endpoints ab; Gesamt `./gradlew :frontend:test` weiterhin grün (158/158).

- **Frontend (Einsatz-Detailseite, Paket 3C Option b)**: Das Teilnehmer-Sub-Panel in `mission-detail.html` nutzt jetzt durchgängig AJAX über `window.MissionSubresource.patchSubResource`. Hinzufügen, Bearbeiten, Löschen, Check-in und Check-out eines Teilnehmers laufen über fünf neue Frontend-Proxy-Endpoints (`POST /missions/{id}/participants/ajax`, `PUT /missions/{id}/participants/{participantId}/ajax`, `DELETE /missions/{id}/participants/{participantId}/ajax`, `POST /missions/{id}/participants/{participantId}/check-in/ajax`, `POST /missions/{id}/participants/{participantId}/check-out/ajax` — alle `@ResponseBody`, JSON, `@PreAuthorize("isAuthenticated()")`), die direkt auf die schlanken Backend-`/slim`-Pfade zeigen. Dadurch bumpt keine Teilnehmer-Änderung mehr die Parent-`Mission.version` — gleichzeitig in anderen Sub-Panels (Einheiten, Finanzen, Stammdaten, Frequenzen …) geöffnete Formulare behalten ihre Eingaben und werden nicht mehr durch spurious 409-Conflicts invalidiert (Option A, AGENTS.md CRITICAL JUNIE RULE: CONCURRENCY). Das gemeinsame Delete-Confirm-Modal wurde um die Sub-Section-Routing-Logik erweitert: Delete-Buttons tragen jetzt `data-sub-section="participant"` + `data-participant-id`, der Submit-Handler filtert darauf und ruft den AJAX-Endpoint statt des klassischen POST-Redirects. Der neue IIFE-Block `wireParticipantAjax` bindet Submit-Listener an das Add-Formular (`id="add-participant-form"`), das Edit-Formular (`id="edit-participant-form"` mit propagiertem `data-participant-id`), alle Check-in/Check-out-Forms (Klasse `participant-action-form` mit `data-participant-action`) und den gemeinsamen Delete-Confirm-Flow. Bei einem echten 409 zeigt der `MissionSubresource`-Handler einen KRT-Styled Toast mit Reload-Angebot (keine nativen `confirm()`/`alert()`); bei Erfolg wird die Seite neu geladen, um die komplexe Teilnehmer-/Crew-Darstellung konsistent aus dem Server-Rendering zu beziehen. Die klassischen POST-Routen (`/participant`, `/participants/{pId}/check-in`, `/participants/{pId}/check-out`, `/participants/{pId}/update`, `/participants/{pId}/delete`) bleiben als Non-JS-Fallback unverändert erhalten.

- **Tests**: Fünf neue MockMvc-Testfälle in `MissionPageControllerMvcTest` (`addParticipantAjax_WithValidBody_ShouldReturn200`, `updateParticipantAjax_WithBackendConflict_ShouldReturn409`, `deleteParticipantAjax_Success_ShouldReturn204`, `checkInParticipantAjax_Success_ShouldReturn200`, `checkOutParticipantAjax_WithBackendError_ShouldPropagateStatus`) sichern die neuen AJAX-Endpoints ab; Gesamt `./gradlew :frontend:test` weiterhin grün (155/155).

- **Frontend (Einsatz-Detailseite, Paket 3C)**: Das Units-Sub-Panel in `mission-detail.html` speichert Hinzufügen/Bearbeiten/Löschen von Einheiten jetzt via AJAX über `window.MissionSubresource.patchSubResource` gegen die neuen Frontend-Proxy-Endpoints `POST /missions/{id}/units/ajax`, `PUT /missions/{id}/units/{unitId}/ajax` und `DELETE /missions/{id}/units/{unitId}/ajax` (alle `@ResponseBody`, JSON, `@PreAuthorize("isAuthenticated()")`), die ihrerseits direkt auf die schlanken Backend-`/slim`-Endpoints (`POST/PUT/DELETE /api/v1/missions/{id}/units[/.../slim]`) zeigen. Dadurch bumpt keine Unit-Änderung mehr die Parent-`Mission.version` — gleichzeitig in anderen Sub-Panels geöffnete Formulare (Teilnehmer, Finanzen, Stammdaten …) behalten ihre Eingaben und werden nicht mehr durch spurious 409-Conflicts invalidiert (Option A, AGENTS.md CRITICAL JUNIE RULE: CONCURRENCY). Bei einem echten 409 zeigt der gemeinsame `MissionSubresource`-Handler einen KRT-Styled Toast mit Reload-Angebot (keine nativen `confirm()`/`alert()`); bei Erfolg wird die Seite neu geladen, um die komplexe Units-/Crew-Darstellung konsistent aus dem Server-Rendering zu beziehen. Der Delete-Confirm-Modal-Flow wurde erweitert: Delete-Buttons tragen nun `data-sub-section="unit"` + `data-unit-id`, der gemeinsame Submit-Handler erkennt dies und ruft den AJAX-Delete-Endpoint statt des klassischen POST-Redirects. Die klassischen POST-Routen (`/units`, `/units/{u}/update`, `/units/{u}/delete`) bleiben als Non-JS-Fallback unverändert erhalten.

- **Tests**: Drei neue MockMvc-Testfälle in `MissionPageControllerMvcTest` (`addUnitAjax_WithValidBody_ShouldReturn200`, `updateUnitAjax_WithBackendConflict_ShouldReturn409`, `deleteUnitAjax_Success_ShouldReturn204`) sichern die neuen AJAX-Endpoints ab; Gesamt `./gradlew :frontend:test` weiterhin grün (150/150).

- **Frontend (Einsatz-Detailseite, Paket 3B)**: Das Frequenzen-Sub-Panel in `mission-detail.html` speichert Änderungen jetzt via AJAX über `window.MissionSubresource.patchSubResource` gegen die neuen Frontend-Proxy-Endpoints `PUT /missions/{id}/frequencies/ajax` und `DELETE /missions/{id}/frequencies/{freqId}/ajax` (beide `@ResponseBody`, JSON, `@PreAuthorize("hasRole('MISSION_MANAGER')")`), die ihrerseits direkt auf die schlanken Backend-`/slim`-Endpoints zeigen. Der angezeigte Frequenzwert wird in-place im DOM aktualisiert (`<span class="freq-value-display" data-freq-type-id="...">`), das Modal schliesst ohne Full-Page-Reload, und der "Edit"-Button führt dank `data-current-value` die bestehende Frequenz zur Bearbeitung vor. Bei 409-Konflikten zeigt der bestehende `MissionSubresource`-Handler einen KRT-Toast mit Reload-Angebot (keine nativen `confirm()`/`alert()`), Eingaben in anderen Sub-Panels bleiben erhalten. Der klassische Redirect-Endpoint bleibt als Non-JS-Fallback unverändert erhalten.

- **Tests**: Drei neue MockMvc-Testfälle in `MissionPageControllerMvcTest` (Happy-Path mit Slim-List-Response, 409-Durchreichung, Delete-AJAX) sichern die neuen AJAX-Endpoints ab; Gesamt `./gradlew :frontend:test` weiterhin grün (147/147).

- **Frontend (`MissionPageController`, Paket 3A — Proxy auf Slim-Endpoints)**: Die vier Frontend-Proxy-Endpoints der Einsatz-Detailseite für Manager und Frequenzen rufen jetzt die neuen Backend-`/slim`-Varianten auf und nicht mehr die in Paket 2 deprecated markierten Legacy-Pfade: `POST /api/v1/missions/{id}/managers/{userId}` ruft backendseitig `.../slim` auf, `DELETE /api/v1/missions/{id}/managers/{userId}` ruft `.../slim` auf, `POST /api/v1/missions/{id}/frequencies` ruft `/frequencies/slim` auf, `DELETE /api/v1/missions/{id}/frequencies/{frequencyId}` ruft `.../slim` auf. Damit umgeht das Frontend die `Deprecation`/`Sunset`-Header und profitiert direkt von schlanken Sub-Response-DTOs ohne Parent-`MissionDto`-Kopplung. Verhalten, Sicherheitsregeln (`@PreAuthorize`) und CSRF-Integration bleiben unverändert; der bestehende MVC-Test `MissionPageControllerMvcTest#addManager_WithBackendError_ShouldPropagateStatus` wurde auf den neuen Backend-Pfad nachgezogen. Template-Review von `mission-detail.html` bestätigt zusätzlich, dass Notes/Logs/SubMissions dort nicht inline editiert werden (Notes/Logs nur Anzeige/Listen auf separaten Seiten, SubMissions nur Create via `POST /subMissions`) — für diese drei Bereiche besteht im Rahmen der Option-A-Migration kein weiterer Handlungsbedarf.

- **Infrastruktur (Keycloak)**: Keycloak-Docker-Image in `docker-compose.yml` von `quay.io/keycloak/keycloak:26.5` auf `quay.io/keycloak/keycloak:26.6` angehoben. Review der Keycloak-Release-Notes 26.6 gegen das Projekt ergab keinen Anpassungsbedarf am Code: (1) Keine Keycloak-Client-/Admin-Client-/Adapter-Dependencies in `versions.properties` vorhanden – der Backend/Frontend-Security-Stack basiert ausschließlich auf Spring Security OAuth2 Resource Server (JWT via `KEYCLOAK_ISSUER_URI`), der über den standardisierten OIDC-Discovery-Endpoint versionsunabhängig angebunden ist; Role-Mapping (JWT → `ROLE_*`, Uppercase) bleibt unverändert. (2) Start-Kommando `start --proxy-headers xforwarded`, `KC_HOSTNAME*`, `KC_HEALTH_ENABLED`, `KC_HTTP_ENABLED`/`KC_HTTP_PORT` und die DB-Parameter sind in 26.6 unverändert gültig – keine Änderungen am Compose-File nötig. (3) `realm-export.json` ist weiterhin importkompatibel (keine Breaking Changes am Realm-Import-Schema in 26.6). (4) KRT-Theme (`keycloak-theme/krt-theme/login`, `.../account`) nutzt nur stabile FreeMarker-Template-APIs (`login.ftl`, `login-reset-password.ftl`, CSS/Resources); keine entfernten Template-Variablen betroffen. (5) `log.txt` war leer, daher keine laufzeitbasierten Findings – nach dem nächsten Produktions-Start sollte `log.txt` erneut gegen Deprecations geprüft werden. Empfehlung für den Rollout: Keycloak-Container mit frischem Image neu starten (`docker compose pull keycloak && docker compose up -d keycloak`) und Health-Check (`/health/ready` auf Port 9000) sowie einen Test-Login gegen `https://keycloak.iri-base.org/realms/iri` verifizieren.

- **Frontend (Einsatz-Detailseite Responsive Layout)**: Die Reiter/Panels der Einsatz-Detailseite (`mission-detail.html`) passen ihre Breite nun inhaltsabhängig an, damit Tabellen und rechts angeordnete Action-Buttons (z. B. "Bearbeiten", "Löschen", "Check-in") nicht mehr rechts aus dem Panel herauslaufen und kein horizontales Scrollen innerhalb eines Panels mehr erzwingen. Jedes Panel deklariert über eine CSS-Custom-Property `--panel-min-width` seine realistische Mindestbreite (schlanke Panels `details` 420px, `organisation` 480px; breite Tabellen-Panels `participants`/`units`/`economy` 720px, `payout`/`finance` 680px); expandierte Panels erzwingen diese Mindestbreite per `min-width: min(100%, var(--panel-min-width))`, während eingeklappte Panels weiterhin kompakt bleiben (`min-width: 0`). Das `auto-fit`-Grid verwendet jetzt den größten Panel-Min-Wert (720px) als globales `minmax()`, sodass eine zweite Spalte nur entsteht, wenn wirklich jedes Panel nebeneinander voll darstellbar ist – ansonsten wird automatisch auf eine Spalte pro Reihe umgebrochen (inkl. Tablet 768–1024px und Smartphone ≤768px). Action-Button-Zellen (`td:last-child`) sind jetzt `nowrap` und rechtsbündig, wodurch sie als Mindestbreiten-Anker des Panels wirken. `.table-responsive` bleibt als letzte Rückfallebene für extreme Ausnahmen. Abgesichert durch erweiterten MVC-Test `MissionPageControllerMvcTest#missionDetail_ShouldRenderWithoutErrors` (prüft die `--panel-min-width`-Regeln und die per-Panel-Werte für `participants`/`units`/`payout`).

- **Frontend (UI/UX, Responsive, a11y)**: Refactoring der Einsatz-Detailseite (`mission-detail.html`). Die bisherige horizontale Reiter-/Spalten-Ansicht mit erzwungenem horizontalem Scrollen (`.mission-columns-container` mit `flex-wrap: nowrap; width: max-content`) wurde durch ein responsives CSS-Grid mit `repeat(auto-fit, minmax(min(100%, var(--mission-col-min-width)), 1fr))` ersetzt. Die Panels (Details, Organisation, Teilnehmer, Einheiten, Auszahlung, Finanzen, Wirtschaft) brechen jetzt automatisch auf schmalen Viewports nach unten um; auf Smartphones (<= 768px) gibt es keinen horizontalen Scrollbalken mehr (`overflow-x: hidden` auf `html`/`body` und `.page-wrapper`, einspaltiges Grid), auf Tablets (768-1024px) passen sich 1-2 Spalten an, auf Desktop (1024-1600px) mehrere Spalten via `auto-fit`, und auf Ultra-Wide-Desktops (>1600px) wird der verfügbare Platz effizient genutzt, während Fließtext in Panels über `max-width: 80ch` lesbar bleibt. Die Einklapp-Funktion wurde neu gestaltet: Der Panel-Header bleibt im eingeklappten Zustand als kompakter Balken mit Titel, optionalem Status-Badge (z. B. Teilnehmer-Zähler) und animiertem Chevron-Indikator (CSS-Transition, Rotation) im KRT-HUD-Design sichtbar (Orange `#E77E23`, Dark BG, Ethnocentric Uppercase); nur der Inhalt wird ausgeblendet, und das kollabierte Panel belegt im Grid nur den Platz seines Headers, sodass Nachbar-Panels aufrücken. Der Header ist nun ein echter `<button type="button">` mit `aria-expanded`/`aria-controls`, sichtbarem Fokus-Ring in KRT-Orange, Tastatursteuerung (Enter/Space toggelt) und mindestens 44px Klickfläche für Touch. Der Collapse-Zustand wird pro Nutzer und pro Einsatz im `localStorage` unter dem Key-Namespace `krt.einsatz.<missionId>.panel.<panelKey>.collapsed` persistiert und beim Reload wiederhergestellt; der alte Key `mission-<colId>-collapsed` wird transparent migriert und parallel weiter synchronisiert (Rückwärtskompatibilitaet). Alle sichtbaren Texte (neue aria-Labels und Tooltips) sind ausschließlich über `messages*.properties` (DE/EN) gepflegt (neue Schluessel `mission.panel.toggle.aria`, `mission.panel.toggle.expand`, `mission.panel.toggle.collapse`, Umlaute als `\uXXXX`); keine neuen Hardcoded-Strings, keine neuen Dependencies, keine Backend-Änderungen. Der bestehende MVC-Test `MissionPageControllerMvcTest#missionDetail_ShouldRenderWithoutErrors` wurde um Assertions für das neue Markup erweitert (`mission-columns-container`, `data-panel-key`, `data-panel-toggle`, `aria-expanded`, `aria-controls`) und stellt zusätzlich sicher, dass die alten `vertical-title`-Marker entfernt sind.

- **Frontend**: Raffinerieaufträge im Status ABGESCHLOSSEN oder ABGEBROCHEN können durch den Besitzer bzw. Logistiker/Offiziere/Admins wieder auf OFFEN oder IN BEARBEITUNG zurückgesetzt werden. Der Speichern-Button in der Detailansicht (`refinery-orders-details.html`) ist dafür jetzt auch in diesen Endzuständen sichtbar und funktionsfähig (zuvor wurde er gemeinsam mit "Einlagern" und "Auftrag Abbrechen" nur für OPEN/IN_PROGRESS gerendert). Die Aktionen "Einlagern" und "Auftrag Abbrechen" bleiben weiterhin auf OPEN/IN_PROGRESS beschränkt. Das Backend (`RefineryOrderService#updateRefineryOrder`) erlaubte die Statuswechsel bereits (inkl. Optimistic Locking via `version` und Owner-/Logistician-Check); es waren keine Backend- oder DB-Änderungen nötig.

- **Frontend/Backend**: Alle Dropdown-/Select-Felder zur Auswahl eines Einsatzes (Mission) zeigen die Optionen nun im Format "Name - Datum" an. Das Datum entspricht dem geplanten Start (`plannedStartTime`), wird aus UTC in die Zeitzone des Nutzers konvertiert, ohne Uhrzeit und gemäß Locale formatiert (Key `format.date`: `dd.MM.yyyy` für DE, `MM/dd/yyyy` für EN). Fehlt der geplante Start, wird nur der Name angezeigt. Betroffen sind die Thymeleaf-Templates `inventory-input.html`, `inventory-admin.html` (Filter und Association-Dropdown), `inventory-my.html` (Filter und Association-Dropdown), `refinery-orders-create.html` und `refinery-orders-details.html`. Dafür wurde `MissionReferenceDto` (Backend und Frontend) um das Feld `plannedStartTime` (`Instant`, UTC) erweitert und die Query `MissionRepository#findAllActiveReference` angepasst; keine Schemaänderungen.

- **Backend/Frontend**: `JobType` kann nun auch mit Archetyp `CREW` als Führungsposition (`isLeadershipRole`) markiert werden. Die bisherige Einschränkung war rein im Frontend-Formular des Admin-Bereichs (`admin/mission-data.html`, `updateLeadershipVisibility`) auf den Archetyp `MISSION` beschränkt; dieses UI-Gate wurde so erweitert, dass die Checkbox "Führungsposition" auch bei Auswahl von Archetyp `CREW` sichtbar und setzbar ist. Die bestehende Führungspositions-Funktionalität für Archetyp `MISSION` bleibt unverändert (Mission-Lead-Gruppierung in der Einsatz-Detailansicht via `plannedMissionJobType.isLeadershipRole`). Zusätzlich werden Crewmitglieder einer `MissionUnit`, deren zugewiesener `JobType` als Führungsposition markiert ist, in der Crew-Liste nun serverseitig vor allen übrigen Crewmitgliedern einsortiert; die Sekundärsortierung bleibt stabil alphabetisch nach Teilnehmername. Dazu wurde `MissionUnitDto.crew` (Backend und Frontend) von `Set<MissionCrewDto>` auf `List<MissionCrewDto>` umgestellt und im zentralen `MissionMapper` eine neue Resolver-Methode `resolveCrew(MissionUnit)` mit passendem `Comparator` ergänzt. Neue Tests in `MissionMapperCrewLeaderSortingTest` decken sechs Edge-Cases ab (null Unit, leere Crew, rein alphabetisch ohne Leader, ein Leader zuerst, mehrere Leader mit stabiler Sekundärsortierung, MISSION-Archetyp-Leader als Regressionsschutz). Es war keine Datenbank-Migration nötig (Flag existiert bereits als `is_leadership_role` seit `V43`).

- **Frontend/Backend**: In der Einsatz-Detailansicht (`mission-detail.html`) wird neben dem Spalten-Titel "Teilnehmer" nun zusätzlich der Zähler `X/Y` angezeigt, wobei `X` der Anzahl der bereits eingecheckten Teilnehmer (Participant mit gesetztem `startTime`) und `Y` der Anzahl aller angemeldeten/eingetragenen Teilnehmer entspricht. Die beiden Werte werden im Backend zentral im `MissionMapper` (MapStruct, Spring-Component) berechnet und als neue `int`-Felder `checkedInParticipants` und `registeredParticipants` über das bestehende `MissionDto` (Backend und Frontend) transportiert – es war keine neue API-Route nötig. Das Thymeleaf-Template rendert den Zähler sowohl im vertikalen Spaltentitel (Smartphone/Tablet) als auch im aufgeklappten `col-header` (Desktop/Ultra Wide) im KRT-Corporate-Design (Orange `#E77E23`, Lato Bold) – alle sichtbaren Texte laufen weiterhin ausschließlich über `messages.properties` (neue Schlüssel `mission.participants.count`, `mission.participants.count.tooltip` in DE/EN, Umlaute als `\uXXXX`). Neue Tests sichern die Zählerlogik ab: `MissionMapperParticipantCountTest` (Edge Cases: 0 Teilnehmer, niemand eingecheckt, alle eingecheckt, gemischt, Null-Mission) und bestehende `MissionPageControllerMvcTest`-Fälle wurden auf das erweiterte `MissionDto`-Record angepasst.

- **Frontend/Backend**: Die "Jetzt"-Buttons in den Einsatz-Details (`mission-detail.html`) füllen nicht mehr nur das lokale Formularfeld, sondern speichern den gesetzten UTC-Zeitpunkt (tatsächlicher Start/tatsächliches Ende) sofort über einen neuen AJAX-Endpoint (`POST /missions/{id}/actual-time`) und lösen anschließend einen Full-Page-Reload aus. Der Endpoint erwartet ein neues Request-DTO `MissionActualTimeUpdateRequest` (Jakarta-Constraints: `@NotBlank` + `@Pattern` für `field`, `@NotNull` für `version`) und leitet die aktuelle `version` aus dem DOM (`#mission-version`) an das Backend weiter. Dadurch greift Spring Data JPAs Optimistic Locking: bei parallelen Änderungen antwortet der Endpoint mit `409 Conflict`, das Frontend zeigt einen KRT-konformen Fehlertoast (i18n-Keys `mission.actual_time.error` / `mission.actual_time.conflict`) und lädt die Seite anschließend neu, um eine frische Version zu holen (siehe AGENTS.md: CRITICAL JUNIE RULE – CONCURRENCY AND OPTIMISTIC LOCKING + FRONTEND DOM VERSION SYNC). Neue MVC-Tests in `MissionPageControllerMvcTest` decken Happy-Path (200), Optimistic-Lock-Konflikt (409) sowie Validierungsfehler bei unbekanntem Feld (400) ab.

- **Frontend/Backend**: Das Kostenfeld (`expenses`) im Raffinerieauftrag-Formular (Create und Details) ist nun mit `0` vorbelegt und akzeptiert `0` als gültigen Wert (`min="0"`, `@PositiveOrZero` in der Entity, `@NotNull @Min(0)` im Frontend-Form). Zuvor war das Feld leer und erforderte einen Wert ≥ 1, was beim Anlegen unnötige Validierungsfehler verursachte.

- **Backend/Frontend**: Raffinerieauftrag erfasst nun zusätzlich die Einnahmen durch den Verkauf roher Erze ("Ore Sales"). Neues Pflichtfeld `oreSales` (Ganzzahl ≥ 0, Default 0) im Create- und Bearbeiten-Formular (`refinery-orders-create.html`, `refinery-orders-details.html`) sowie ein read-only "Gewinn/Verlust"-Feld, das live aus `oreSales - expenses` berechnet und im KRT-HUD-Stil (orangefarbener Border-Left) dargestellt wird. Neue i18n-Schlüssel: `refineryorder.form.oreSales`, `refineryorder.form.oreSales.help`, `refineryorder.form.profit`, `refineryorder.form.profit.help` in `messages.properties`, `messages_de.properties` und `messages_en.properties` (Umlaute als `\uXXXX`). Entity `RefineryOrder` erhält das persistierte Feld `oreSales` (Double, NOT NULL, DEFAULT 0) sowie einen transienten `getProfit()`. DTOs (`RefineryOrderDto`, `RefineryOrderListDto` im Backend und Frontend) wurden um `oreSales` und das serverseitig via MapStruct-Expression abgeleitete `profit` erweitert; Request-Validierung via `@PositiveOrZero` / `@Min(0)`.

- **Backend**: Die Einsatz-Finanzauswertung (`MissionFinanceEntryService#calculateFinanceSum` und `OperationFinanceService`) verwendet für Raffinerieaufträge nun den Gewinn/Verlust (`oreSales - expenses`) statt der reinen Kosten. Dadurch fließen sowohl Gewinne (positiv) als auch Verluste (negativ) korrekt in die Einsatz-Bilanz ein. Null-Werte werden als 0 behandelt, damit Altdaten keine Rechenfehler verursachen.

- **Frontend (UI/UX)**: Die Notiz-Vorschau eines Lagereintrags im persönlichen Lager (`/inventory/my`) und im globalen Lager (`/inventory/all`) wird nun deutlich besser lesbar und auffälliger dargestellt. Statt einer kleinen, einzeiligen Ellipsis-Zeile unter dem Button erscheint die Notiz jetzt als eigenständiger KRT-HUD-Block mit dunklem Hintergrund (`#141414`), orangefarbenem Border-Left (`#E77E23`), größerer Lato-Schrift, mehrzeiliger Darstellung (`white-space: pre-wrap`, `word-break: break-word`) und einem uppercase "NOTIZ"-Label in Ethnocentric. Auf Desktop/Ultra Wide bleibt der Block auf `max-width: 80ch` begrenzt, auf Tablet/Smartphone nimmt er die volle Breite ein. Alle Texte werden weiterhin über i18n geladen (`inventory.note.label`); die bestehende DOM-Version-Sync-Logik und das Verhalten beim Bearbeiten/Entfernen der Notiz wurden beibehalten.

- **UI/UX**: Colored the "Check-In" button in the Missions detail view green (`btn-success`) to improve visibility while matching the styleguide.

- **UI/UX**: Renamed user-facing names in the Materials section: "Materialien" to "Handel", "Material-Liste" to "Material-Übersicht", and "Material-Übersicht" to "Preis-Übersicht" (and their English equivalents).

- **Backend/Frontend**: In der Auftragsübersicht (JobOrders) werden die Materialien nun nach Mengentyp (zuerst "SCU", dann "Stück") und anschließend alphabetisch sortiert angezeigt.

- **UI/UX**: Fixed a layout overflow issue in the mission details tab (`mission-detail.html`). The "Actual Start" and "Actual End" fields (including their "Now" buttons) now stack vertically on smaller desktop screens instead of trying to fit into a single row, ensuring all elements fit neatly into the surrounding box.

- **Frontend**: Moved the "Impressum" link from the "Rechtliches" dropdown to the top level of the sidebar, directly after the "Rechtliches" menu.

- **Backend**: Added database tables `job_order_handover` and `job_order_handover_item` for tracking material handovers.

- **Backend**: Implemented `JobOrderHandoverService` to process handovers and correctly reduce `InventoryItem` amounts using pessimistic locking.

- **Backend**: Added POST endpoint `/api/v1/orders/{id}/handovers` in `JobOrderController` to handle the handover creation.

- **Frontend**: Added DTOs and API client methods for handover creation.

- **Frontend**: Implemented handover modal in `orders-detail.html` with dynamic selection of available inventory items.

- **i18n**: Added translation keys for the new handover UI (`orders.detail.handover`, `orders.handover.*` etc.).

- **Frontend**: Die auf-/zugeklappten Zeilen in den Lagerübersichten (`inventory-my`, `inventory-admin`) werden nun benutzerspezifisch (per LocalStorage) gespeichert und beim Neuladen der Seite wiederhergestellt.

- **Frontend**: Added a modal to edit existing job orders (squadron, handle, materials) directly from the details view for authorized users (Logisticians, Officers, Admins).

- **Frontend**: Job order status is now an asynchronous dropdown menu that updates immediately without requiring a full form submission or a separate "Update" button.

- **Backend**: Implemented `unlinkJobOrderMaterial` in `InventoryItemRepository` and `JobOrderService` to properly decouple inventory items when a specific material is removed from an active job order during an update.

- **Backend**: Modified `JobOrderController` and `JobOrderPageController` to process job order details updates via DTOs.

- **Frontend**: Added dynamic unit labels (e.g. "SCU" or "Stück") behind all quantity numbers across the inventory views (`inventory-admin`, `inventory-my`, `inventory-index`, `inventory-material`) and mission details, formatting decimals based on the material's quantity type.

- **Backend & Frontend**: Added distinct handling for quantity types (`SCU` and `Stück/PIECE`) across inventory and refinery order storage dialogs.

- **Backend**: Implemented `@ValidQuantityAmount` Jakarta Constraint for `InventoryItemCreateDto`, `InventoryItemUpdateDto`, and `RefineryOrderStoreItemDto` to ensure PIECE quantities are integers and SCU quantities have max 3 decimal places.

- **Database**: Added Flyway migration script `V56__update_inventory_quantities.sql` to floor legacy `PIECE` amounts to whole integers.

- **Frontend**: `RefineryOrder` storage dialog now intelligently prefills amounts: SCU quantities are calculated and rounded to 3 decimals based on system settings, while PIECE quantities receive the exact integer units.

- **Frontend**: Added dynamic HTML5 constraints (`step="1"` vs `step="0.001"`) and visual quantity type indicators for `SCU` / `Stück` to all inventory input and book-out modals.

### Removed

- **Frontend (verwaister i18n-Key `operation.edit` aus allen drei Locales geloescht)**: Nach dem Umbau auf inline-editierbare Operation-Details (Edit-Modal raus) referenziert kein Template / kein Java-Code mehr `#{operation.edit}` — die Modal-Heading "Operation bearbeiten" / "Edit Operation" war die einzige Verwendung. Audit per Skript (alle `operation.*`- und `operations.*`-Keys aus `messages.properties` extrahiert, in `frontend/src/main/`, `backend/src/main/` nach Referenzen in `*.html`/`*.java`/`*.js` gegrept) bestaetigt: genau ein Key uebrig — `operation.edit`. Eintrag aus allen drei Locales entfernt (`messages.properties`, `messages_de.properties`, `messages_en.properties`). Verbleibende Operation-i18n-Surface unveraendert (23 weitere `operation.*`-Keys + 2 `operations.*`-Keys, alle weiterhin referenziert).

### Fixed

- **`/promotion/admin/rank-requirements` lieferte beim Aufruf einen 500: `InvalidDefinitionException: Java 8 date/time type java.time.Instant not supported by default`**: Beim Rendern des Templates `promotion-admin-rank-requirements` versuchte Thymeleaf eine `[[${categoriesByTopic}]]`-Inline-JS-Expression auf eine `LinkedHashMap<UUID, List<PromotionCategoryDto>>` anzuwenden, deren DTO-Eintraege `createdAt`/`updatedAt` als `java.time.Instant` tragen. Ursache: Thymeleafs interner `org.thymeleaf.standard.serializer.StandardJavaScriptSerializer$JacksonStandardJavaScriptSerializer` instanziiert einen nackten `new ObjectMapper()` ohne `JavaTimeModule`-Registrierung; Spring Boot 4 hat die eigene primaere `ObjectMapper`-Bean auf Jackson 3 (`tools.jackson.core`) umgestellt, sodass das JSR-310-Modul auch nicht mehr transitiv auf der Jackson-2-Instanz (`com.fasterxml.jackson.core`) landet, die Thymeleaf intern verwendet. Folge: jede `[[${dto}]]`-Inline-Expression auf einem Objekt, das `java.time.*`-Werte enthaelt, sprengt das Rendering mid-output (Status 200 wurde vom Pre-Filter bereits gesetzt, Spring kann die Render-Exception danach nicht mehr in eine saubere Error-Page umlenken — der Browser sieht eine abgeschnittene HTML-Antwort, der Reverse-Proxy logged 500). **Fix in drei Teilen**: **(1)** Neue `frontend/.../config/ThymeleafJavaScriptSerializerConfig` mit einem hybriden `JavaTimeAwareJavaScriptSerializer`: fuer **primitive Typen** (`CharSequence`, `Number`, `Boolean`, `Character`, `Enum`, `null`) delegiert er an Thymeleafs Stock-`StandardJavaScriptSerializer(true)` — diese Werte koennen nie transitiv `java.time` referenzieren, und die Delegation garantiert Byte-fuer-Byte-identische Ausgabe zu dem, was die Engine seit Jahren produziert (eine erste Version, die _alles_ ueber unseren Mapper geschickt hat, hat 36 MVC-Tests gebrochen weil sich Escape-Detail-Unterschiede in JS-Inline-Comment-Stripping niedergeschlagen haben — siehe `OperationPageControllerMvcTest`). Fuer **komplexe Objekte** (Maps, Lists, DTOs) faellt der Serializer zurueck auf einen eigenen Jackson-2-`ObjectMapper`, der `JavaTimeModule` registriert hat, `WRITE_DATES_AS_TIMESTAMPS` deaktiviert (ISO-8601 statt Epoch-Millis — passt zur CLAUDE.md-Regel "All times in UTC" und zur bereits existierenden `TimezoneSerializationTest`-Contract) und Thymeleafs JS-/HTML-Escapes uebernimmt (`&`, `'`, `"`, `/` via `\\u00XX`, `U+2028`/`U+2029` via `\\u2028`/`\\u2029`, uppercase Hex byte-fuer-byte deckungsgleich mit Stock-`JacksonThymeleafEscapes`). Ein `@PostConstruct`-Hook iteriert ueber `SpringTemplateEngine#getDialects()`, castet jeden `StandardDialect` und ruft `setJavaScriptSerializer(...)` auf — laeuft erst nach der Thymeleaf-Auto-Config, sodass der Default-Serializer regulaer ueberschrieben wird. **(2)** `frontend/build.gradle.kts` ergaenzt `implementation("com.fasterxml.jackson.datatype:jackson-datatype-jsr310")` (ohne explizite Version — Spring-Boot-BOM pinnt es auf die gleiche `2.21.x` wie das bereits explizit eingebundene `jackson-databind`) plus Block-Kommentar, warum die Dependency hier explizit aufgefuehrt werden muss obwohl Spring Boot frueher das JSR-310-Modul transitiv mitlieferte (Spring Boot 4 ist auf Jackson 3 umgezogen, Thymeleaf 3.1.x haengt an Jackson 2). **(3)** Acht Mockito-freie Unit-Tests in `ThymeleafJavaScriptSerializerConfigTest`: `Instant`/`OffsetDateTime`/`LocalDateTime` zu ISO-8601, ein `PromotionCategoryDto` mit Instant-Feldern, der exakte `Map<String, List<PromotionCategoryDto>>`-Reference-Chain aus dem Original-Stacktrace, plus XSS-Safety-Asserts auf dem Stock-Pfad (`</script>` darf nicht ueberleben — `\/` ist die Sicherheitsgarantie, weil der HTML5-Parser nach `</` und nicht nach `<\/` sucht; `&` → `\\u0026`, `"` → `\"`). Damit sind sowohl der Render-Fix als auch die XSS-Schutz-Eigenschaft gegen Regression abgesichert. Verifiziert per `./gradlew :frontend:test` (alle Tests gruen, einschliesslich der 36 zuvor regressionsverdaechtigen MVC-Tests im `controller`-Paket) und `./gradlew :frontend:spotlessApply :frontend:checkstyleMain :frontend:spotbugsMain` (BUILD SUCCESSFUL).

- **Typ-Spalte in der Finanzen-Tabelle der Missionsdetailseite (`/missions/{id}`) blieb fuer manuell angelegte Eintraege leer**: Eine vom Nutzer eingetragene Ausgabe (oder Einnahme) erschien in der "Typ"-Spalte ohne sichtbares Label, waehrend die automatisch aus Raffinerieauftraegen erzeugten Zeilen weiterhin "Ausgabe" in rot anzeigten. Ursache in `frontend/src/main/resources/templates/mission-detail.html:786-787`: Die Thymeleaf-Bedingungen `th:if="${entry.type == 'INCOME'}"` bzw. `... == 'EXPENSE'` verglichen den `FinanceType`-Enum aus `MissionFinanceEntryDto` direkt mit einem String-Literal. Thymeleaf/SpEL setzt `==` zu `equals()` um, und `Enum.equals(...)` ist final-identity-based — ein Enum ist niemals gleich einem String, beide Branches evaluierten daher immer zu `false`, und das `<td>` blieb leer. Die Raffinerie-Zeilen (Zeile 811) sind davon nicht betroffen, weil sie den `mission.finance.type.expense`-i18n-Key bedingungslos rendern. **Fix**: `entry.type == 'INCOME'` / `... == 'EXPENSE'` durch `entry.type.name() == 'INCOME'` / `... == 'EXPENSE'` ersetzt, sodass der Vergleich auf String-Ebene laeuft. Keine weiteren Vorkommen dieses Patterns im Frontend (`grep`-Sweep ueber alle Templates: die uebrigen `.type == 'STRING'`-Vergleiche betreffen `MaterialDto.type`, das ein `String` ist, nicht ein Enum). Reine Template-Aenderung, kein Backend-/i18n-/JS-Touch noetig.

- **`Dependency Check`-Workflow schlug auf `workflow_dispatch` und auf der Wochen-Cron mit `NVD Returned Status Code: 429` und korruptem H2-OnDisk-DB fehl** (Run [`25933803540`](https://github.com/krt-profit/basetool/actions/runs/25933803540) und der vorhergehende Manual-Run): Der OWASP-Dependency-Check-Plugin laeuft auf jedem Workflow-Run das Default-`nvd.validForHours = 4` durch, also versuchte er bei jedem Trigger erneut, das NVD-2.0-API anzusprechen. Auf github-hosted Runners (`ubuntu-latest` teilt sich den Source-IP-Pool mit jedem anderen CI-Job auf github.com, der Montagmorgens seine Wochen-Scans neu zieht) liefert das oeffentliche NVD-Endpoint `429 Too Many Requests`. Der Plugin retried per Default 10 Mal mit 8 s Delay; das ist gegen den dokumentierten 5-req/30-s-Public-Rate-Limit auf der Kante und reichte in `25933803540` nicht aus — nach `Retrying request for the 31st time` wurde die H2-Update-Transaktion abgebrochen, `org.h2.mvstore.MVStoreException: Reading from file ... failed at 69781360` markierte den On-Disk-Store als korrupt, jeder Folge-`updateOrInsertVulnerability(...)` warf `NullPointerException: ... connectionPool is null`, die SARIF-Generierung lief nie und der nachgelagerte `Upload SARIF report to GitHub Security`-Step scheiterte zusaetzlich mit `Path does not exist: build/reports/dependency-check/dependency-check-report.sarif`. **Fix in drei Teilen:** **(1)** `build.gradle.kts`-`dependencyCheck { ... }`-Block setzt `nvd.validForHours = 168` (sieben Tage), sodass innerhalb einer Woche der lokale Cache als frisch gilt und kein einziger NVD-API-Call faellt; nur der erste Run einer neuen ISO-Woche kontaktiert NVD wirklich. **(2)** Gleicher Block bumpt `nvd.maxRetryCount` von 10 auf 20 und setzt `nvd.delay` abhaengig vom `-PnvdApiKey`-Property: 2 s mit API-Key (~30 req/min, sicher unter dem authentifizierten 50-req/30-s-Limit), 16 s ohne (~3.75 req/30 s, sicher unter dem oeffentlichen 5-req/30-s-Limit) — der Default von 8 s war zu nah am Limit. **(3)** `.github/workflows/dependency-check.yml` rotiert den `actions/cache`-Key woechentlich (`nvd-feed-v1-${{ runner.os }}-${{ ISO-Woche }}` mit Fallback-`restore-keys: nvd-feed-v1-${{ runner.os }}-`) statt vorher `nvd-${{ runner.os }}-${{ github.run_id }}` — der `run_id` kollidierte nie und liess den Plugin auf jedem Workflow-Dispatch die DB von Grund auf neu aufbauen, was bei fehlendem API-Key garantiert in 429-Land enden musste. Zusaetzlicher `Check SARIF report exists`-Step gateet den SARIF-Upload, damit ein abgebrochener Plugin-Lauf nicht zusaetzlich noch den `upload-sarif`-Schritt rot wirft (der eigentliche Gradle-Fehler ist bereits rot, der zweite Fehler war Noise). Header-Kommentar im Workflow-File und im `build.gradle.kts`-Block linkt explizit auf https://nvd.nist.gov/developers/request-an-api-key und beschreibt, wie der `NVD_API_KEY`-Repo-Secret gesetzt wird, damit der First-of-Week-Run von 15+ Minuten auf wenige Minuten faellt.

- **"Globales Lager leeren"-Button auf `/inventory/all` reagierte nicht (Browser-Konsole: `Uncaught TypeError: (intermediate value)(...) is not a function`)**: Der weiter oben in diesem Unreleased-Block neu hinzugefuegte ADMIN-Bulk-Delete-Button blieb beim Klick stumm. Ursache: In `frontend/src/main/resources/templates/inventory-admin.html` endet die unmittelbar vorausgehende `window.onclick = function(event) { ... }`-Zuweisung ohne Semikolon, und die naechste nicht-leere Zeile beginnt mit `(function() { ... })();` — dem IIFE, der `delete-all-global-inventory-btn` an seinen Modal-/Fetch-Handler bindet. JavaScripts Automatic Semicolon Insertion fuegt **keine** Semikolons vor `(` ein, also parst der Browser die beiden Statements als `window.onclick = function(event){...}(function(){...})()`: die `function(event){...}` wird mit der IIFE-Definition als Argument **aufgerufen**, liefert `undefined` zurueck, und das nachfolgende `()` versucht dieses `undefined` ein zweites Mal als Funktion aufzurufen — exakt der `TypeError: (intermediate value)(...) is not a function`, den die DevTools-Konsole zeigt. Da das gesamte Skript an dieser Stelle abbricht, wurden die `addEventListener('click', ...)`-Calls fuer Delete-Button/Confirm-Button/Cancel-Button nie registriert; der Click-Handler des Buttons existierte zur Laufzeit schlicht nicht. **Fix**: Ein einzelnes Semikolon am Ende der `window.onclick`-Funktionsdefinition (`}` -> `};`). Damit wird die Zuweisung sauber terminiert, die IIFE laeuft beim DOMContentLoaded-Pfad regulaer durch und der Button oeffnet wieder das Confirm-Modal. Kein Backend-/Build-Change noetig, keine i18n-Aenderung — reine 1-Zeichen-JS-Korrektur in der View-Template. Manuelle Verifikation: Browser-Reload mit DevTools-Konsole offen, der TypeError verschwindet und der Klick auf "Globales Lager leeren" oeffnet das KRT-styled Modal, das `DELETE /inventory/all` feuert und nach Erfolg per Toast + `window.location.reload()` schliesst.

- **Backend startete im Prod-Container nicht mehr: `BeanInstantiationException: ... KeycloakHealthIndicator: No default constructor found`**: Die in PR #73 (Readiness-Probes) hinzugefuegten Custom-HealthIndicators (`backend/.../health/KeycloakHealthIndicator`, `frontend/.../health/KeycloakHealthIndicator`, `frontend/.../health/BackendHealthIndicator`) deklarieren jeweils **zwei** Konstruktoren: einen Production-Constructor mit `@Value`-/`AppBackendProperties`-Parametern und einen paketsichtbaren Visible-for-Testing-Constructor mit expliziten `Duration`-Parametern. Spring Framework 7's `ConstructorResolver` macht bei mehreren Konstruktoren ohne explizites `@Autowired`-Hinweis **keinen** automatischen Pick mehr (anders als Spring 4.3 Single-Ctor-Heuristik) und faellt auf den Default-No-Arg-Constructor zurueck — der existiert nicht, also `NoSuchMethodException: <init>()` waehrend `preInstantiateSingletons()`, `APPLICATION FAILED TO START`, Docker-`HEALTHCHECK` schlaegt nach drei Retries fehl, Frontend-Container bleibt im `Error dependency backend failed to start`-Status. **Warum es im Test-Profil keinen Crash gab**: das `application-test.yml` setzt `management.health.keycloak.enabled: false` und `management.health.backend.enabled: false` (genau aus Performance-Gruenden um die nicht aufloesbaren Test-Issuer-URIs nicht jedes Mal anzulaufen), wodurch der `@ConditionalOnEnabledHealthIndicator`-Annotation das Bean von der Auto-Registrierung ausschliesst — der defekte Constructor-Pfad wird im Test-Profil **nicht** durchlaufen, deswegen waren alle 554+1411 Tests gruen. In der Prod-Konfiguration (`SPRING_PROFILES_ACTIVE: prod`, keine Disable-Overrides) sind die Indicators aktiv und der Constructor-Pick faellt um. **Fix**: Production-Constructor in allen drei Indicators mit `@Autowired` annotiert plus Block-Kommentar oberhalb der Annotation der genau die obige Failure-Mode dokumentiert, damit niemand den Marker spaeter wieder rausnimmt mit "scheint redundant" als Begruendung. **Regression-Guard**: pro Indicator-Test einen neuen `productionConstructor_isAnnotatedAutowired_soSpringCanInstantiate()`-Test (3 Tests insgesamt) der reflektiv prueft, dass genau **ein** Konstruktor `@Autowired` traegt und es **exakt zwei** Konstruktoren gibt — der zweite Assert ist defensiv, falls jemand spaeter einen dritten Konstruktor hinzufuegt soll das Test mit Hinweis "revisit the @Autowired contract" rot werden statt still nochmal in denselben Trap zu laufen. Verifiziert per `./gradlew :backend:test --tests "*KeycloakHealthIndicator*" :frontend:test --tests "*HealthIndicator*"` (alle 9+1 Tests gruen), `./gradlew :backend:spotlessApply :backend:checkstyleMain :backend:spotbugsMain :frontend:spotlessApply :frontend:checkstyleMain :frontend:spotbugsMain` (BUILD SUCCESSFUL, keine neuen Findings). Lessons-Learned dokumentiert in der `@Autowired`-Javadoc und im Regression-Test-Kommentar — das in PR #73-Test-Plan offen gelassene "Spin up the local test stack ... and confirm container transitions" haette das gefangen, kommt jetzt auch hier wieder als unchecked Item in den PR rein.

- **`docker compose --profile prod up --build` schlug auf Linux-Hosts mit `:frontend:spotlessJavaCheck FAILED` fehl (CRLF im Build-Context)**: Beim Re-Build des Prod-Stacks auf dem Ubuntu-Server brach `RUN ./gradlew :frontend:build -x test -x checkstyleMain -x spotbugsMain --no-daemon` in `frontend/Dockerfile:26` ab mit `The following files had format violations:` und einem Diff aller 252 Java-Files in `frontend/src/main/java/` plus ein paar weitere die Spotless als formatiert markiert (`@@ -1,18 +1,18 @@ -...\r\n +...\n`). Ursache: die Files sind in git mit LF gespeichert (per `git ls-files --eol` verifiziert: `i/lf`), aber der Working-Tree des Build-Hosts hat CRLF — typische Folge eines Windows-Devs der mit `core.autocrlf=true` clont, lokal Files anschaut/editiert und dann ueber `rsync`/`scp`/Docker-Build-Context-Transfer die Working-Tree-Version (nicht die Git-Index-Version) an den Linux-Build-Host weitergibt. Spotless erzwingt LF und scheitert bevor `bootJar` ueberhaupt laeuft. **Fix in zwei Teilen**: **(1)** `backend/Dockerfile` und `frontend/Dockerfile` Gradle-Build-Invokationen erweitert um `-x spotlessCheck -x spotlessJavaCheck` (zusaetzlich zu den bereits ausgeschlossenen `-x checkstyleMain -x spotbugsMain`), genau aus dem im Block-Kommentar darueber dokumentierten Grund: Spotless ist ein Dev-/CI-Werkzeug und braucht im Produktion-Image-Build nichts zu suchen — die `check`-Garantie kommt aus dem `ci.yml`-Workflow auf einem kontrollierten Linux-Runner, nicht aus dem Container-Image. Der Block-Kommentar wurde erweitert um die CRLF-Drift-Erklaerung, damit der naechste Reviewer der den Exclusion-Vector sieht weiss warum genau Spotless dazugehoert. **(2)** `.gitattributes` um Source-File-Patterns erweitert (`*.java`, `*.kt`, `*.kts`, `*.yml`, `*.yaml`, `*.properties`, `*.html`, `*.css`, `*.js`, `*.json`, `*.xml`, `*.sql`, `*.md`, `Dockerfile`, `*.sh` jeweils `text eol=lf`), sodass ein Windows-Clone mit `core.autocrlf=true` die Source-Files trotzdem mit LF im Working-Tree haelt und damit nicht mehr in dieselbe Drift laufen kann. Existierende Files brauchen kein `git add --renormalize .` weil der Git-Index ohnehin schon LF haelt; bei naechstem `git checkout`/`git pull` auf einem Windows-Worktree mit autocrlf wandelt git die Working-Tree-Version zu LF um (Index-Inhalt ist die Source-of-Truth). Verifiziert lokal: `./gradlew :frontend:build :backend:build -x test -x checkstyleMain -x spotbugsMain -x spotlessCheck -x spotlessJavaCheck --no-daemon` baut beide Module durch (Spotless wird in der Task-Graph nicht mehr ausgefuehrt); Docker-Re-Build muss der User auf dem Ubuntu-Host nachstellen.

- **CI (`Analyze (java-kotlin)` schlug auf jedem PR ohne Java-Aenderung fehl)**: Der CodeQL-Workflow lief in `Compile Java sources for extraction` mit `./gradlew compileJava compileTestJava --stacktrace`. Auf PRs, die nur HTML-Templates oder Dokumentation aenderten (wie der vorherige Fix in diesem Branch), wurden alle vier Compile-Tasks aus dem von `gradle/actions/setup-gradle` bereitgestellten Build-Cache als `FROM-CACHE` aufgeloest — der Cache wird vom letzten `main`-Run geschrieben und ist auf Feature-Branches read-only (`cache-read-only: ${{ github.ref != 'refs/heads/main' }}`). Dadurch wurde `javac` nie tatsaechlich invoked; CodeQLs Java-Extractor traced `javac`-Calls via `LD_PRELOAD`, hatte aber keine Events zu erfassen und brach den anschliessenden `database finalize`-Step mit `CodeQL could not process any code written in Java/Kotlin. Exit code 32` ab (siehe Run [`25902079377`](https://github.com/krt-profit/basetool/actions/runs/25902079377)). Fix: `--no-build-cache` an den Compile-Aufruf in `.github/workflows/codeql.yml:142` angehangt, plus ausfuehrlichen Block-Kommentar oberhalb der Zeile der erklaert warum das Flag load-bearing ist (sonst gibt's Drift sobald die naechste Person den Workflow vereinfachen will). Effekt: Gradle umgeht den Shared-Cache und kompiliert die Sources jedes mal frisch, sodass der CodeQL-Tracer immer Compile-Events sieht. Per-Workspace-Up-to-date-Check bleibt aktiv, aber CI-Runner sind ephemer — jeder Run startet ohnehin clean. Trade-Off: Compile-Schritt dauert jetzt ~30s statt ~5s-from-cache, dafuer wird CodeQL-Analyse wieder zuverlaessig. Build-Cache fuer das regulaere `ci.yml` (Test-Workflow) bleibt unangetastet, dort gibt's keinen `LD_PRELOAD`-Tracer-Constraint.

- **Promotion-Admin-Seiten: "Neue Anforderung"/"Neuer Themenbereich"/"Bewertung speichern" Buttons funktionierten nicht (CSP blockierte Inline-Skript)**: Auf `/promotion/admin/rank-requirements` schlug ein Klick auf "Neue Anforderung" stillschweigend fehl; Browser-Konsole zeigte zwei Fehler: (1) `Content-Security-Policy: Die Einstellungen der Seite haben die Ausfuehrung eines Inline-Skripts (script-src-elem) blockiert, da es gegen folgende Direktive verstoesst: "script-src 'nonce-...' 'strict-dynamic' https:"` und (2) folgend `Uncaught ReferenceError: openCreateModal is not defined` am `onclick`-Handler. Ursache: Die in `SecurityConfig.java:45-56` definierte CSP nutzt `script-src 'nonce-%s' 'strict-dynamic' https:` und der `CspNonceFilter` setzt pro Request ein `cspNonce` Request-Attribut, das **jedes** `<script>`-Element via `th:attr="nonce=${cspNonce}"` mitnehmen muss (so machen es alle anderen Templates: `materials.html:133`, `inventory-admin.html:456`, `orders-detail.html:341`, etc.). Drei Promotion-Templates wurden seit ihrem Branch-Merge ohne diese Nonce-Anbindung ausgeliefert: `frontend/.../templates/promotion-admin-rank-requirements.html:259` (verantwortlich fuer die ReferenceError, weil das Inline-Skript mit `openCreateModal`/`openEditModal`/`openDeleteModal`/`createRequirement`/`updateRequirement`/`deleteRequirement` komplett blockiert wurde), `promotion-admin-topics.html:325` (gleicher Effekt: alle Topic-/Kategorie-/Lernziel-Modals stumm) und `promotion-manage.html:117` (verhinderte das Speichern manuell editierter Bewertungen). Fix: An jedem der drei `<script th:inline="javascript">`-Tags ein `th:attr="nonce=${cspNonce}"` ergaenzt, sodass Thymeleaf das Per-Request-Nonce-Attribut renderet und der Browser das Skript gegen die CSP-Header-Nonce validieren kann. Die in der CSP-Policy ebenfalls vorhandene `script-src-attr 'unsafe-inline'`-Klausel (siehe Kommentar in `SecurityConfig.java:50-56`) deckt die Inline-`onclick="openCreateModal()"`-Handler ab — die funktionieren also korrekt, sobald die zugehoerigen Funktionsdefinitionen geladen sind. Kein Backend-Code geaendert; kein Java-Test betroffen, da Checkstyle/SpotBugs auf HTML-Templates nicht laufen. Manuelle Verifikation per Browser-Reload mit DevTools-Konsole offen: Inline-Skripte werden jetzt mit `nonce`-Attribut serviert und vom CSP-Engine zugelassen, alle drei Promotion-Admin-Seiten reagieren wieder auf "Neue X"-Buttons sowie Edit-/Delete-Modals.

- **Controller-Tests fuer die vier Promotion-System-Controller (ANALYSIS 1.2): `PromotionTopicController`, `PromotionCategoryController`, `PromotionLevelContentController`, `PromotionEligibilityController` jetzt mit 28 Mockito-only Unit-Tests und 100% Line/Branch/Method-Coverage**: Die V72-Promotion-System-Migration hatte die vier `@RestController`-Klassen plus dazugehoerige Services eingefuehrt, aber CLAUDE.md's Regel "Every new feature ships with tests. No exceptions." lag fuer die Controller-Ebene auf der TODO-Liste — Service-Tests existierten zwar (vier `Promotion*ServiceTest` plus ein `PromotionEntityTest`), aber kein `*ControllerTest` deckte die HTTP-Boundary ab. Damit waren Bugs wie eine versehentliche Vertauschung der `update(id, request)`-Argumente, ein Drop des `PromotionLevel`-Enums beim DTO-Mapping oder ein durchgereichter `null`-Subject-Claim auf `/eligibility/my` (Daten-Isolation-Verletzung!) ohne Regression-Guard. **Pattern**: `@ExtendWith(MockitoExtension.class)` + `@Mock`-Service + `@InjectMocks`-Controller — keine Spring-Context, keine Testcontainers (Project-Memory: Docker laeuft lokal nicht), pure Pure-Function-Tests. **Abgedeckte Kontrakte (pro Controller)**: list-Endpoint translatiert Spring-`Page` -> projekt-eigenen `PageResponse`-Record content/page/size/totalElements/totalPages/sort-fuer-feld, list ohne Params triggert PaginationUtil-Defaults, listAll/listByTopic/listByCategory leiten Path-Variablen unveraendert weiter, get/create/update/delete machen Verbatim-Pass-Through (`isSameAs`-Asserts auf das Service-Mock-Return). **Eligibility-Spezialfall**: `PromotionEligibilityController.requireSub(JwtAuthenticationToken)` ist die einzige nicht-triviale Logik im ganzen Promotion-Stack und tritt als Single-Point-Of-Failure fuer Daten-Isolation auf — alle vier Fehlermodi (`null`-Auth, `null`-Token, `null`-Sub, blank-Sub) pinned mit `assertThatThrownBy(AccessDeniedException)` + `verifyNoInteractions(service)`, damit eine zukuenftige "kleine Vereinfachung" (z. B. `Optional.ofNullable(jwt.getSubject()).orElse(callerUuid)`) nicht still die `/my`-Daten-Isolation aushebeln kann. Plus Pass-Through-Test fuer `eligibilityForUser` (Admin/Officer-View, `@PreAuthorize` lebt in SecurityConfig und ist hier nicht Unit-Test-Concern). **PromotionLevelContent-Spezialfall**: zusaetzliche `assertThat(result.level()).isEqualTo(LEVEL_X)`-Asserts auf create/update verifizieren dass der `PromotionLevel`-Enum durch Create/Update-DTO -> Response-DTO byte-fuer-byte erhalten bleibt, falls jemand spaeter mappt nach String (Daten-Verlust bei Round-Trip waere sonst stumm). **Coverage** verifiziert per `backend/build/reports/jacoco/test/jacocoTestReport.csv`: 0 instructions missed bei 55 / 70 / 56 / 52 instructions, 0 branches missed bei 8 covered (Eligibility), 0 methods missed bei 7 / 8 / 6 / 9 methods — d. h. 100&nbsp;% line / branch / method coverage auf allen vier Controller-Klassen. Verifiziert per `./gradlew :backend:test --tests "*Promotion*ControllerTest"` (alle 28 Tests gruen) und `./gradlew :backend:spotlessApply :backend:checkstyleMain :backend:spotbugsMain :backend:spotlessCheck` (BUILD SUCCESSFUL, keine neuen Findings).

- **Frontend-Readiness-Check spiegelt das Backend: zwei eigene HealthIndicators (Keycloak + Backend) plus Compose-Healthcheck auf `/actuator/health/readiness`**: Bisher zeigte das Frontend per Docker-`HEALTHCHECK` nur `/actuator/health`, das aus dem auto-konfigurierten Spring-Set (`PingHealthIndicator`, `DiskSpaceHealthIndicator`, `SslHealthIndicator` via `server.ssl.enabled=true`, `RedisHealthIndicator` via `spring-boot-starter-data-redis`, sowie der Resilience4j-`CircuitBreakerHealthIndicator` aus `resilience4j-spring-boot3`) aggregierte. Zwei Probleme damit fuer Startup-Gating: (a) Ein **Backend-Ausfall** schlaegt nicht ins Frontend-Health durch — der `CircuitBreakerHealthIndicator` geht erst nach `slidingWindowSize=10`/`minimumNumberOfCalls=5` echten Failures auf OPEN, also frueh in der Startup-Phase noch nicht; (b) Ein **Keycloak-Ausfall** schlaegt ebenfalls nicht durch, weil das `oauth2-client`-Starter keinen Issuer-Indicator beisteuert (analog zur Backend-Situation vorher). Frontend haette also `service_healthy` gemeldet, obwohl jede Daten-tragende Seite SSO-Login oder Backend-Roundtrip braucht. Loesung in 2 Schritten: **(1) Neuer `frontend/src/main/java/.../health/KeycloakHealthIndicator.java`** (direkter Spiegel des Backend-Indicators aus dem vorhergehenden Schritt, aber gebunden an `spring.security.oauth2.client.provider.keycloak.issuer-uri` statt der Resource-Server-Property — die beiden Module erreichen Keycloak ueber unterschiedliche Netzwerk-Pfade in der Prod-Compose-Topologie: Frontend ueber NPM in Richtung Public-DNS, Backend ueber die interne `net-backend-keycloak`-Bridge, jedes Modul brauch seinen eigenen Reachability-Check). Hinter `@ConditionalOnEnabledHealthIndicator("keycloak")`, identische Probe-Mechanik (`RestClient` + `JdkClientHttpRequestFactory`, 2&nbsp;s Connect / 3&nbsp;s Read, drei Outcomes UP/`status`-Detail/`error`-Detail). **(2) Neuer `frontend/src/main/java/.../health/BackendHealthIndicator.java`**, probt explizit das `/actuator/health/readiness` des Backends — also genau den Endpoint, an dem Compose das Backend-Service-Healthy gating macht, sodass die Frontend-Readiness end-to-end-`UP` erst dann ist, wenn das Backend selbst readyl ist. Hinter `@ConditionalOnEnabledHealthIndicator("backend")` (key wird aus dem Bean-Namen `backendHealthIndicator` minus `HealthIndicator`-Suffix abgeleitet), zieht die Backend-URL aus `AppBackendProperties.getBackendUrl()` (`@ConfigurationProperties("app")` -> `app.backend-url`, in Prod `https://backend:11261`, in Test `http://backend:11261`). Trailing-Slash-Trimming im Konstruktor verhindert doppelte Slashes wenn der Operator `BACKEND_URL=https://backend:11261/` setzt. **SSL-Behandlung**: Backend serviert mit Self-Signed-Cert auf der internen Compose-Bridge — der bestehende `WebClient` aus `WebClientConfig` skipped Validation via Netty's `InsecureTrustManagerFactory.INSTANCE`, der neue Indicator routet die JDK-`SSLContext`-Init durch genau dieselbe Factory (`SSLContext.init(null, InsecureTrustManagerFactory.INSTANCE.getTrustManagers(), null)`) statt einen eigenen anonymen `X509TrustManager` zu deklarieren. Der Refactor-Grund ist CodeQL's `java/insecure-trustmanager`-Query: die targets hand-rolled empty-method-TrustManager-Klassen, nicht etablierte Drittanbieter-Factories wie Netty's, sodass die Probe-Klasse dieselbe Trust-Semantik wie der bestehende WebClient bekommt, ohne ein zweites CodeQL-Finding aufzumachen. Trust-Boundary-Begruendung steht im `trustAllSslContext()`-Javadoc plus Klassen-Javadoc: Compose-internes Netzwerk, public-TLS terminiert NPM, Probe-URL server-controlled (keine User-Inputs). **Readiness-Group erweitert** in `frontend/src/main/resources/application.yml`: `management.endpoint.health.group.readiness.include: readinessState, backend, keycloak, redis, diskSpace, ssl`. Liveness bleibt absichtlich getrennt — ein transienter Backend/Keycloak-Blip soll diese Frontend-Instanz aus der Rotation nehmen, **nicht** neustarten. **Dockerfile** in `frontend/Dockerfile` umgestellt von `curl https://localhost:18081/actuator/health` auf `curl https://localhost:18081/actuator/health/readiness` (Timing unveraendert: `--interval=30s --timeout=5s --start-period=60s --retries=3`). **Security-Whitelist**: keine Aenderung — `SecurityConfig.java:145-146` matcht bereits `/actuator/health/**`, deckt also Liveness und Readiness mit ab; `show-details: never` bleibt global. **Test-Profil** in `frontend/src/main/resources/application-test.yml`: beide Indicators per `management.health.backend.enabled: false` + `management.health.keycloak.enabled: false` deaktiviert (Test-URLs sind nicht aufloesbare Platzhalter), Readiness-Group auf `readinessState, diskSpace` reduziert (mirror des Backend-Patterns fuer den Spring-Boot-4-`HealthEndpointGroupMembershipValidator`, der nicht-existente Contributor in Group-Includes als Startup-Fehler wirft — `redis` ist im Test-Profil absent wegen `spring.session.store-type: none`, `ssl` wegen `server.ssl.enabled: false`). **Tests**: `frontend/src/test/java/.../health/KeycloakHealthIndicatorTest.java` mit 4 Mockito-freien MockWebServer-Tests (analog zum Backend: 200-Happy-Path mit Path-Assertion `/realms/iri/.well-known/openid-configuration`, 503, 404, Server-down via `server.shutdown()` mit TCP-RST-Fast-Fail). `frontend/src/test/java/.../health/BackendHealthIndicatorTest.java` mit 5 Tests: 200-UP, 503-DOWN-mit-`status`-Detail (modelliert die realistische Kette "Backend reachable aber DOWN weil sein Keycloak nicht erreichbar"), 404-DOWN (Misconfiguration `BACKEND_URL` falsch), Server-down (Transport-Failure mit `error`-Detail), und Trailing-Slash-Canonicalization-Test der verhindert dass `BACKEND_URL=…/` zu einem `//actuator/health/readiness`-Pfad fuehrt. Alle 9 Tests laufen in 500&nbsp;ms-Timeouts millisekundenschnell. Verifiziert per `./gradlew :frontend:test --tests "*HealthIndicator*"` und `./gradlew :frontend:spotlessApply :frontend:checkstyleMain :frontend:spotbugsMain`.

- **Keycloak-Reachability als zusaetzlicher Backend-HealthIndicator und Compose-Healthcheck auf `/actuator/health/readiness` umgestellt**: Vorher war der Backend-Health auf den Standardsatz Spring-Boot-Auto-Indikatoren beschraenkt (`PingHealthIndicator`, `DataSourceHealthIndicator` via `spring-boot-starter-data-jpa` + Hikari + Postgres-JDBC mit `SELECT 1`, `DiskSpaceHealthIndicator` mit 10-MB-Mindestschwelle, `SslHealthIndicator` aktiv wegen `server.ssl.enabled=true` mit Default-14-Tage-Expiry-Warnung); ein Keycloak-Ausfall haette `/actuator/health` weiter auf `{"status":"UP"}` belassen, weil weder das `oauth2-resource-server`-Starter noch der `KeycloakService` einen eigenen Indicator beisteuert — d.h. `docker-compose depends_on: backend: condition: service_healthy` haette das Frontend freigegeben, obwohl jeder authentifizierte Request danach am JWT-Validation gescheitert waere. Loesung: **neuer custom `KeycloakHealthIndicator`** in `backend/src/main/java/.../health/KeycloakHealthIndicator.java`, hinter `@ConditionalOnProperty(prefix = "management.health.keycloak", name = "enabled", havingValue = "true", matchIfMissing = true)` (default ON, im `test`-Profil per `application-test.yml` auf `false` weil das Test-Issuer-URI `http://keycloak.example.com/realms/test` nicht aufloesbar ist und sonst jeden `@SpringBootTest`-Kontextstart um die Connect-Timeout-Dauer verlangsamen wuerde). Der Indicator probt synchron `${spring.security.oauth2.resourceserver.jwt.issuer-uri}/.well-known/openid-configuration` per `RestClient` ueber `JdkClientHttpRequestFactory` (JDK 25 `HttpClient`) mit **2&nbsp;s Connect-Timeout und 3&nbsp;s Read-Timeout** — bewusst innerhalb des Docker-`HEALTHCHECK --timeout=5s`-Budgets, damit ein langsames Keycloak als `DOWN` durchschlaegt bevor `curl` selbst aufgibt. Drei Outcome-Klassen: (1) 2xx-Response -> `Health.up().withDetail("endpoint","openid-configuration")`. (2) 4xx/5xx-Response -> `RestClientResponseException` -> `Health.down()` mit Detail `status=<code>` (WARN-Log mit Status + URL). (3) I/O-Fehler (DNS, Connection-Refused, Timeout, TLS-Handshake) -> `RestClientException` -> `Health.down()` mit Detail `error=<ExceptionClassSimpleName>` (WARN-Log). Bean-Name `keycloakHealthIndicator` -> Spring strippt das `HealthIndicator`-Suffix -> Indikator-Key in Group-Includes ist `keycloak`. Zwei Konstruktoren: Spring nutzt den `@Value`-Konstruktor mit den Production-Konstanten; ein paketsichtbarer Konstruktor mit expliziten `Duration`-Parametern erlaubt Unit-Tests gegen `MockWebServer` mit Millisekunden-Timeouts. **Compose-Healthcheck umgestellt** in `backend/Dockerfile`: vorher `curl -fsSk https://localhost:11261/actuator/health`, jetzt `curl -fsSk https://localhost:11261/actuator/health/readiness` (`HEALTHCHECK --interval=30s --timeout=5s --start-period=60s --retries=3` unveraendert). Damit kommen zwei Verschaerfungen rein: (a) Spring's `readinessState` geht erst nach `ApplicationReadyEvent` auf `UP`, d.h. der 200er hier garantiert vollstaendige Context-Initialisierung (Flyway, Caches, Scheduler) — nicht nur "Tomcat bindet"; (b) die in `application.yml` neu konfigurierte Readiness-Group (`management.endpoint.health.group.readiness.include: readinessState, db, keycloak, diskSpace, ssl`) erweitert den Default `readinessState`-only-Set um genau die Indikatoren deren `DOWN` `"stoppe Traffic"` bedeutet — Keycloak-Ausfall, DB unerreichbar, Disk voll, SSL-Cert laeuft ab. `livenessState` ist absichtlich NICHT in der Readiness-Group: ein vorruebergehender Keycloak-Blip soll den Container aus der `service_healthy`-Rotation nehmen, **nicht** neustarten — Liveness und Readiness werden bewusst getrennt gehalten fuer eine spaetere K8s-Migration. **Security-Whitelist**: keine Aenderung — `SecurityConfig.java:174` matcht bereits `/actuator/health/**`, deckt also `/actuator/health/readiness` und `/actuator/health/liveness` mit ab; `show-details: never` bleibt global, der Response-Body bleibt auf `{"status":"UP"|"DOWN"}` beschraenkt (keine Component-Enumeration nach aussen). **Tests**: `KeycloakHealthIndicatorTest` mit 4 Mockito-freien Tests gegen `okhttp3.mockwebserver.MockWebServer` (analoge Test-Patterns wie `UexClientTest`): `health_returns_up_when_discovery_endpoint_responds_200` (Happy-Path mit realistischem OIDC-Discovery-Body, prueft Status + Details + `RecordedRequest`-Path = `/realms/iri/.well-known/openid-configuration` und Methode `GET`), `health_returns_down_when_discovery_endpoint_responds_503` (Upstream-5xx, prueft `status=503`-Detail), `health_returns_down_when_discovery_endpoint_responds_404` (Realm existiert nicht, prueft `status=404`-Detail), `health_returns_down_when_server_is_unreachable` (Server-Port nach `server.shutdown()` geschlossen -> TCP-RST sofort, prueft `error`-Detail nicht leer). Tests laufen mit 500&nbsp;ms Test-Timeouts millisekundenschnell. Verifiziert per `./gradlew :backend:test --tests "*KeycloakHealthIndicatorTest*"` und `./gradlew :backend:spotlessApply :backend:checkstyleMain :backend:spotbugsMain`.

- **Globales Lager leeren — neuer ADMIN-only Bulk-Delete-Button auf `/inventory/all`**: Im globalen Lager (`inventory-admin.html`) erscheint fuer Nutzer mit `ROLE_ADMIN` ein zusaetzlicher roter Button "Globales Lager leeren" in der Actions-Bar. Per Klick oeffnet sich ein KRT-styled Confirm-Modal mit der Frage "Alle globalen Lagereintraege wirklich loeschen?" — ausdruecklich mit der Klarstellung "Persoenliche Lagereintraege bleiben erhalten" — und einem prominenten "Ja, alle loeschen"-Confirm-Button. Bestaetigt der Admin, feuert der JS-Handler einen `fetch('/inventory/all', { method: 'DELETE' })` ab; bei 204 wird ein Erfolgs-Toast gezeigt und nach 600ms `window.location.reload()` aufgerufen, bei Fehler ein Error-Toast mit dem HTTP-Status. **Backend** (`backend/.../controller/InventoryItemController.java`): neuer `DELETE /api/v1/inventory/all`-Endpoint mit `@PreAuthorize("hasRole('ADMIN')")`, OpenAPI-`@Operation`/`@ApiResponses` (204/401/403), delegiert an `InventoryItemService.deleteAllGlobalInventory()`. Im `InventoryItemService` neue `@Transactional`-Methode, die per neuer `InventoryItemRepository.deleteAllNonPersonal()`-`@Modifying`-Query (`DELETE FROM InventoryItem i WHERE i.personal = false`) das gesamte globale Lager in einem einzigen Bulk-Statement leert. Personal-Items (`personal = true`) bleiben absichtlich erhalten — das globale Lager schliesst persoenliche Eintraege ohnehin aus. Kein Pre-Cleanup-Loop fuer Fremdschluessel noetig: der einzige bisherige FK `job_order_handover_item.inventory_item_id REFERENCES inventory_item(id)` wurde bereits in `V64__remove_inventory_item_from_job_order_handover_item.sql` entfernt (Handover-Zeilen snapshotten die Material-Daten direkt), also kollidiert das Bulk-Delete mit nichts. SecurityConfig braucht keine Anpassung — `/api/v1/inventory/**` ist bereits auf `hasAnyRole('ADMIN', 'OFFICER', 'LOGISTICIAN', 'KRT_MEMBER')` gegated und das `@PreAuthorize` engt das auf ADMIN ein. **Frontend** (`frontend/.../controller/InventoryDeleteAllProxyController.java` — neu, analog zum bewaehrten `HangarDeleteAllProxyController`-Pattern): `@RestController` mit `@RequestMapping("/inventory")`, `@DeleteMapping("/all")` mit `@PreAuthorize("hasRole('ADMIN')")` als Defence-in-Depth-Vorab-Check, proxied per authentifiziertem `WebClient` an `/api/v1/inventory/all` und propagiert Backend-Status-Codes (4xx/5xx) verbatim per `ResponseStatusException`; nicht-HTTP-Failures (Connect/Read) werden auf 500 mit sanitised Message gewrappt, um keinen Upstream-Stacktrace zu leaken. Der Pfad `/inventory/all` lebt parallel zum bereits bestehenden `GET /inventory/all` im `InventoryPageController` (squadron view) — Spring routet nach HTTP-Methode, also kollidieren die beiden Mappings nicht. CSRF ist fuer `/inventory/**` bereits in der `SecurityConfig.csrf().ignoringRequestMatchers(...)`-Liste, also kein zusaetzliches CSRF-Token-Mitsenden im JS noetig (selber Vertrag wie der Hangar-Pendant). **i18n**: Sechs neue Keys (`inventory.deleteAll.btn`, `.modal.title`, `.modal.body`, `.modal.confirm`, `.success`, `.error.failed`) parallel in `messages.properties`, `messages_de.properties` (deutsche Umlaute als `\uXXXX`-Escapes nach CLAUDE.md-Regel) und `messages_en.properties` gepflegt. **Tests**: Service-Tests (`InventoryItemServiceTest`) decken Happy-Path (42 Rows geloescht -> 42 zurueck) und Empty-Path (0 Rows geloescht -> 0 zurueck) ab, jeweils mit `verify(inventoryItemRepository).deleteAllNonPersonal()`. Frontend-Proxy-Test (`InventoryDeleteAllProxyControllerTest`) deckt analog zum `HangarDeleteAllProxyControllerTest` per `MockWebServer` alle vier Branches ab: 204-Passthrough mit Pfad-Assertion auf `/api/v1/inventory/all`, 403-Propagation (Backend-403 fuer Non-Admin-Caller), 500-Propagation, und Connection-Failure-Wrap-as-500 mit "unexpected"-Reason-Assertion (kein Upstream-Stacktrace-Leak). **UX-Eigenheiten**: Der Button-Click oeffnet zuerst das Modal — keine native `confirm()`-Verwendung (CLAUDE.md-Regel "Never confirm/alert"), stattdessen ein eigenes KRT-styled HUD-Modal mit `flex-center-gap`-Action-Bar und `btn-danger`-Confirm-Button. Waehrend des laufenden DELETE-Requests werden beide Modal-Buttons disabled, beim Submit-Ende wieder freigegeben — verhindert Doppel-Submits bei langsamen Verbindungen. Klick ausserhalb des Modal schliesst es; ESC-Handling kommt automatisch durch den existierenden `window.onclick`-Pattern.

- **Hangar-Import akzeptiert jetzt auch HangarXPLOR-Shiplist-JSON (zweites Format, Auto-Detection, LTI-Mapping aus dem Export, Endpunkt umbenannt zu `/api/v1/hangar/import/ships`)**: Der bisherige Hangar-Import konnte nur das CCU-Game-Fleetview-Format (`[{"name","shipname","type"}]`) lesen. Der User hat zusaetzlich Exporte aus dem [HangarXPLOR-Plugin](https://github.com/dolkensp/HangarXPLOR) (RSI Pledge Store Inspector, `shiplist.json` im `input/`-Ordner: 64 Eintraege mit ~13 Feldern pro Record inkl. `ship_code`/`ship_name`/`manufacturer_code`/`manufacturer_name`/`lti`/`name`/`warbond`/`entity_type`/`pledge_id`/`pledge_name`/`pledge_date`/`pledge_cost` plus optional `unidentified`/`lookup` bei nicht-zuordenbaren Eintraegen). **Empirische Vorauswertung gegen den Live-UEX-Dump (275 Schiffe)**: von 62 unique Modellnamen im shiplist.json matchen mit der bestehenden 4-Stage-Pipeline bereits Stage 1=54 (exact), Stage 2=1 (`A.T.L.S.` -> `ATLS`), Stage 3=3 (`Ares Inferno/Ion` -> `…Starfighter`, `Hercules Starlifter A2` -> `A2 Hercules Starlifter` durch Token-Reordering), Stage 4=2 (`325a Fighter` -> `325a`, `Ursa Rover` -> `Ursa`); nur 2 unique Namen bleiben unmatched (`600i Exploration Module` und `Dragonfly Star Kitten Edition`), beide sind echte Daten-Drift ohne UEX-Pendant und landen wie gewohnt im `skippedShips`-Modal — die Resolution-Logik braucht also **keine** Erweiterung, der Hangar-Service kommt mit dem neuen Format direkt klar, sobald das Parsing es liefert. **Service-Aenderungen** in `backend/.../service/HangarImportService.java`: (1) Neues Input-DTO `ShiplistEntryDto` (`@JsonIgnoreProperties(ignoreUnknown = true)`) mit nur den 4 fuer den Import relevanten Feldern `name`, `ship_name`, `entity_type`, `lti` — der Rest der HangarXPLOR-Payload wird stillschweigend ignoriert. (2) Format-Detection in `parseEntries(MultipartFile)`: liest das Root-Array als `JsonNode`, nimmt das erste Object-Element und probt auf `pledge_id`/`ship_code` (-> HangarXPLOR Shiplist) versus `shipname`/`type` (-> CCU Game Fleetview) versus keines (-> `BadRequestException` mit klarer Fehlermeldung). Root nicht-Array oder leeres-Array werden ebenfalls sauber gehandhabt (BadRequest bzw. leere Result-Liste). (3) Internes format-agnostisches Record `FleetImportEntry(name, individualName, insurance)` als gemeinsamer Vertrag zwischen beiden Parsern und der unveraenderten Stage-1-bis-4-Resolution. (4) Mappings: `mapFleetviewEntry` liefert wie bisher `insurance=null` (-> Service-Default `"0"`); `mapShiplistEntry` mappt `lti=true` auf `LTI_INSURANCE = "LTI"` und `lti=false`/absent auf `null` (-> Default `"0"`, weil wir wissen es ist NICHT Lifetime aber nicht wie viele Monate). (5) HangarXPLOR-spezifische Heuristik `computeIndividualNameFromShiplist(modelName, shipName)`: ein `ship_name` wird nur als individueller Schiffsname uebernommen, wenn er nicht eine Abkuerzung des `name`-Felds ist (formale Bedingung: `normalize(name)` enthaelt `normalize(shipName)` nicht als Substring). Damit werden die 3 echten Custom-Namen aus dem Beispiel-File (`KRT Olymp`, `KRT Falcon`, `KRT Franklin`) als individueller Schiffsname uebernommen und die 12 Echo-Faelle wie `ship_name="325a"`/`name="325a Fighter"` oder `ship_name="ATLS"`/`name="ATLS GEO"` als individueller Name weggefiltert (sonst haetten alle importierten Schiffe einen sinnlosen Pseudo-Namen). (6) `entity_type != "ship"` wird im Parser-Step weggeworfen, nicht als skipped gemeldet — defensiv gegen HangarXPLOR-Exporte, die auch Module/Pakete/Paint-Bundles enthalten koennten (im aktuellen File alle `"ship"`, aber CIG verkauft im Pledge Store auch andere Items). **Endpunkt-Rename mit Sunset** (analog zur API-Versioning-Konvention in CLAUDE.md): Der neue kanonische Pfad ist `POST /api/v1/hangar/import/ships` — format-neutral, akzeptiert beide Payloads. Der alte `POST /api/v1/hangar/import/fleetview` bleibt **bis zum 2027-05-14** als deprecated Alias, der intern an dieselbe Service-Methode delegiert; in `HangarController.java` mit `@ApiDeprecation(sunset = "2027-05-14", replacement = "/api/v1/hangar/import/ships")` plus Java-`@Deprecated(since = "2026-05-14", forRemoval = true)` annotiert, sodass der `DeprecationInterceptor` automatisch `Sunset: 2027-05-14`- und `Link: …; rel="alternate"`-Response-Header setzt und OpenAPI das Endpunkt-Schema entsprechend ausweist. Der Frontend-Proxy `HangarImportProxyController` wurde analog gespiegelt: neuer `/hangar/import/ships` -> Backend `/api/v1/hangar/import/ships`, deprecated `/hangar/import/fleetview` -> Backend `/api/v1/hangar/import/fleetview`. Beide nutzen einen gemeinsamen `forwardImport`-Helper (kein Duplicate-Code). Browser-bookmarked alte Pfade funktionieren unveraendert weiter. **SecurityConfig-Updates** in Backend (`SecurityConfig.java:257-261`, neuer `requestMatchers(POST, "/api/v1/hangar/import/ships").authenticated()`) und Frontend (`SecurityConfig.java:113-118`, neuer Pfad in der `csrf().ignoringRequestMatchers(...)`-Liste); beide alten Pfade bleiben in den Whitelists, sodass Auto-Detection und Deprecation-Pfad gleichermassen erreichbar sind. **Frontend-UI**: hangar.html-JS-fetch zeigt jetzt auf `/hangar/import/ships`; `messages_de.properties` und `messages_en.properties` haben die drei Fleetview-spezifischen Strings (`hangar.import.title`, `hangar.import.description`, `hangar.import.label`) zu format-neutralen Texten verallgemeinert (`Schiffs-Import` / `Ship import` als Section-Titel; Beschreibung erwaehnt jetzt explizit beide Formate plus "Das Format wird automatisch erkannt"; Label `Schiffsliste (Fleetview / HangarXPLOR JSON)` / `Ship list (Fleetview / HangarXPLOR JSON)`). Das Result-Modal mit den drei Stats (Importiert / Nicht erkannt / Duplikate uebersprungen) plus der `skippedShips`-Liste bleibt unveraendert — die `FleetviewImportResponseDto` ist bewusst format-agnostisch gehalten (Name nur historisch). **Funktionserhalt (alles wie zuvor)**: `max(0, jsonCount - hangarCount)`-Duplikat-Schutz, `skippedShips` mit Original-Casing + case-insensitivem Dedupe, `fitted=false`-Default auf neu erzeugten Schiffen, JWT-`sub` als Owner-ID, Stage-1-bis-4-Resolution-Pipeline mit der Hyphen-/Whitespace-Normalisierung und der Token-Subset-Logik aus dem vorhergehenden Schritt — nichts davon wurde angefasst. **Tests**: `HangarImportServiceTest` um **11 weitere Mockito-Tests** erweitert (insgesamt jetzt 35 Tests, vorher 23 + 12 = 35 - 1 Rename, sowie 0 entfernte ergibt 35 — alle gruen): `importShips_shiplistFormat_isAutoDetectedAndImported` (basic happy path mit voller HangarXPLOR-Payload inkl. allen Metadatenfeldern), `importShips_shiplistLtiTrue_setsLtiInsurance` (LTI -> "LTI"), `importShips_shiplistLtiFalse_fallsBackToDefaultInsurance` (Standard -> "0"), `importShips_shiplistCustomShipName_isSetAsIndividualName` (KRT Olymp / 600i Explorer -> individueller Name wird gesetzt), `importShips_shiplistShipNameEchoesModelName_individualNameStaysNull` (325a / 325a Fighter -> Echo wird gefiltert), `importShips_shiplistNonShipEntityType_isDroppedAtParseStep` (paint-Eintrag verschwindet still), `importShips_fleetviewFormatStillRecognisedAfterShiplistAddition` (Regression: Fleetview-Detection bricht nicht durch die neue shiplist-Branch), `importShips_unknownFormat_throws400`, `importShips_rootIsObjectNotArray_throws400`, `importShips_emptyArray_returnsAllZero`, `importShips_shiplistMixedResolution` (End-to-End: Polaris exact + Hercules Stage-3-Reorder + Ursa-Rover Stage-4 + 600i Exploration Module skipped — alles in einer Payload). Bestehende 24 Fleetview-Tests wurden auf den neuen Service-Methodennamen `importShips()` umbenannt und laufen weiter gruen. Plus `HangarImportProxyControllerTest`: ein neuer Smoke-Test `importShips_happyPath_proxiesMultipartToCanonicalBackendPath` verifiziert dass der neue Proxy-Endpunkt zum richtigen Backend-Pfad weiterleitet; alle 6 bestehenden Tests des deprecated Aliases laufen weiter; ein Test wurde umbenannt von `fallsBackToFleetviewJson` zu `fallsBackToShiplistJson`, weil der Fallback-Filename im jetzt format-neutralen Proxy generisch `"shiplist.json"` ist. Verifiziert per `./gradlew :backend:test --tests "*HangarImportServiceTest*" :frontend:test --tests "*HangarImportProxyControllerTest*"` (BUILD SUCCESSFUL) und `./gradlew :backend:spotlessApply :backend:checkstyleMain :backend:spotbugsMain :frontend:spotlessApply :frontend:checkstyleMain` (BUILD SUCCESSFUL, 0 Verstoesse). Der HangarImportIntegrationTest sowie Fleetview-Pfad-Tests im Frontend-Proxy decken den deprecated Pfad weiter ab, sodass kein Drift zwischen den beiden Endpunkten entsteht.

- **Hangar-Fleetview-Import (vierstufige tolerante Namensaufloesung, 14 von 14 nicht-ambigen Drift-Faellen werden jetzt importiert)**: Der Fleetview-JSON-Import im Hangar matchte bisher jeden Eintrag nur ueber `ShipTypeRepository.findByNameIgnoreCase(name)` — also strikt case-insensitive, aber sonst Zeichen-fuer-Zeichen exakt. Damit fielen alle Eintraege durchs Raster, deren CCU-Game-Export-Schreibweise sich auch nur minimal von UEX' kanonischem `ShipType.name` (gespeist aus `https://api.uexcorp.space/2.0/vehicles` via `UexVehicleService`) unterscheidet. Empirische Auswertung der vier `input/fleetview*.json`-Dateien gegen den Live-UEX-Dump (275 Schiffe): von 186 unique Fleetview-Namen matchten 166 exact (Stage 1), 20 wurden als "unbekannt" verworfen. Davon: 6 nur durch Bindestrich/Whitespace-Drift (`L21 Wolf` <-> `L-21 Wolf`, alle vier `Cyclone-AA/RC/RN/TR` <-> `Cyclone AA/RC/RN/TR`), 13 durch strukturelle Token-Unterschiede (UEX-Suffix-Drift wie `A2 Hercules` <-> `A2 Hercules Starlifter`, Aurora-Mk-I-Standardvarianten wie `Aurora MR` <-> `Aurora Mk I MR`, Wortumstellung `Pirate Gladius` <-> `Gladius Pirate`, abgekuerzte Marketing-Namen `Ursa Rover` <-> `Ursa`) und 1 echter Ambig-Fall (`F7C-M Super Hornet` koennte Mk I, Heartseeker Mk I oder Mk II sein). Loesung in `backend/.../service/HangarImportService.java`: **vier-stufige Resolution-Pipeline** auf einem einmal pro Import aus `shipTypeRepository.findAll()` aufgebauten Index. **Stage 1** (exact case-insensitive, `name.trim().toLowerCase(Locale.ROOT)`-Key auf einer `HashMap<String, ShipType>`) — deckt den bisherigen `findByNameIgnoreCase`-Pfad ab. **Stage 2** (normalised, `name.toLowerCase(Locale.ROOT).replaceAll("[^a-z0-9]", "")`-Key auf einer zweiten `HashMap`) — absorbiert Hyphen-/Whitespace-Drift wie `L21 Wolf` <-> `L-21 Wolf`. **Stage 3** (token-set `fv ⊆ uex` mit Eindeutigkeit) — tokenisiert beide Namen via `Pattern.compile("[^a-z0-9]+")` zu Lowercase-Token-Sets und matcht eindeutig genau dann, wenn die fv-Tokens vollstaendig in den UEX-Tokens enthalten sind und dies nur fuer **genau ein** UEX-Schiff gilt; ambige Faelle (z.B. `F7C-M Super Hornet`) werden bewusst skipped, statt zu raten. **Stage 4** (token-set `uex ⊆ fv` mit Eindeutigkeit) — die Reverse-Richtung fuer Faelle wie `Ursa Rover` (fv-Name laenger als der UEX-kanonische Kurzname `Ursa`); ueber alle Ursa-Varianten ist nur `Ursa` eine Teilmenge von `{ursa, rover}` (`{ursa, fortuna}` und `{ursa, medivac}` enthalten Tokens die nicht in fv sind), Match also eindeutig. **Stage-Vorrang**: Stage 1 dominiert immer (regression-getestet) — wenn fv `Gladius` exakt einem `ShipType "Gladius"` matcht, gewinnt Stage 1 auch wenn `Gladius Pirate`/`Gladius Valiant` parallel existieren. Stage 3 short-circuited nicht in Stage 4 hinein wenn Stage 3 mehrere Kandidaten fand (sonst koennte ein bare `Foo` durch uex⊆fv-Subset-Match einen ambigen `Foo X`/`Foo Y`/`Foo Z`-Fall hijacken). **Empirisches Endergebnis** (geprueft per Python-Simulator gegen den Live-UEX-Dump): Stage 1 = 166, Stage 2 = 6, Stage 3 = 12, Stage 4 = 1, **Total automatisch importiert = 185 von 186 unique Schiffsnamen**, nur 1 echter Ambig-Fall (F7C-M Super Hornet) bleibt skipped — und das absichtlich, weil ohne Mk-Suffix vom User keine deterministische Variante ableitbar ist. **Funktionserhalt (alles unveraendert)**: (1) Duplikat-Schutz bleibt — `toCreate = max(0, jsonCount - hangarCount)`, d.h. ueberzaehlige Schiffe im Hangar werden NIE geloescht, und neu erzeugt wird hoechstens so viele wie der JSON-Count erfordert, sodass der Hangar nach dem Import nie mehr Schiffe eines Typs enthaelt als die JSON spezifiziert (es sei denn er hatte vorher schon mehr). (2) Schiffe, die ueberhaupt nicht aufgeloest werden konnten, landen weiterhin in `skippedShips` der `FleetviewImportResponseDto`, die das bestehende Frontend-Modal (`hangar.html` `#import-skip-section`) als Liste anzeigt; Dedupe ist jetzt ueber ein `Set<String>` mit lowercased Key statt der bisherigen O(n^2)-`stream().noneMatch(equalsIgnoreCase)`-Suche, originale Schreibweise des ersten Vorkommens bleibt erhalten. (3) Insurance default `"0"`, Individual-Shipname auf erstem neu erzeugten Schiff, `fitted=false` — alles wie vorher. **Performance-Nebeneffekt**: Ein typisches Fleetview mit 200 Eintraegen produzierte vorher 200x `findByNameIgnoreCase`-Queries; jetzt genau **eine** `findAll()`-Query plus In-Memory-Map-Probes und Token-Set-Comparisons (O(n*m) Set-Inklusionschecks, wobei n = fleetview-entries ~ 200 und m = ShipTypes ~ 275, also ~55k Set-Operationen ueber sehr kleine Sets — Millisekunden-Bereich), plus pro distinct `ShipType` einen `countByOwnerIdAndShipTypeId`. Begleitend `HangarImportServiceTest` auf das neue Mock-Pattern (`findAll().thenReturn(List.of(...))` statt N `findByNameIgnoreCase(...)`-Stubs) umgestellt und um **12 neue Test-Cases** erweitert: `caseOnlyDifference_stillMatches` + `hyphenInDbNotInJson_stillMatchesViaNormalisation` + `hyphenInJsonNotInDb_stillMatchesViaNormalisation` + `twoSpellingsSameShip_aggregateToSameType` + `extraWhitespaceInJson_stillMatches` + `skippedList_preservesOriginalCasingAndDedupes` + `shipTypeWithBlankName_isIgnoredInIndex` (Stage 1+2 + Defensive); `fvAbbreviatesUexCanonicalSuffix_resolvesUniquely` (A2 Hercules Starlifter mit C2-Decoy) + `fvAbbreviatesAuroraVariant_resolvesToMkI` (alle sechs Aurora-Mk-I-Varianten plus Mk-II-Decoy, `Aurora MR` muss genau `Aurora Mk I MR` waehlen) + `tokenReorderingMatches` (Pirate Gladius → Gladius Pirate mit Gladius+Valiant-Decoys) + `uexShorterThanFv_resolvesViaStage4` (Ursa Rover → Ursa mit Fortuna+Medivac-Decoys) + `stage3Ambiguous_skipsInsteadOfGuessing` (F7C-M Super Hornet zwischen 3 Mk-Varianten) + `stage4Ambiguous_skipsInsteadOfGuessing` (Foo Bar zwischen 2 Subset-Kandidaten) + `exactMatchWinsOverTokenSubset` (Stage-Vorrang-Regression: fv `Gladius` matcht plain `Gladius`, nicht `Gladius Pirate`). Verifiziert per `./gradlew :backend:test --tests "*HangarImportServiceTest*"` (BUILD SUCCESSFUL, alle 23 Tests gruen) und `./gradlew :backend:spotlessApply :backend:checkstyleMain :backend:spotbugsMain` (BUILD SUCCESSFUL, 0 Verstoesse). Der bestehende `HangarImportIntegrationTest` profitiert automatisch (er bestueckt die DB mit echten `ShipType`-Rows, die jetzt vom `findAll()`-Pfad mitgenommen werden) — kann lokal aber nur in Docker-faehigen Umgebungen ausgefuehrt werden.

- **Logging (CodeQL Log-Cleanup: 9x sensitive-log + 1x tainted-arithmetic + 56x log-injection adressiert)**: Schliesst die zweite Welle CodeQL-Findings, die das Re-Scan nach dem XSS-Cleanup auf PR #65 surfaced hat — 66 Alerts in Java-Logging-Code, alle vor PR #65 existierend, aber durch die Phase-1-Reformatierung und Phase-2-Javadoc-Annotation in den PR-Diff-Surface gewandert. **(1) `clear-text-logging-of-sensitive-information` / `java/sensitive-log` (9 high in `BackendRoleSyncFilter.java`)**: Der Filter loggte `token.getName()` (OIDC-Principal-Name = Username) in `log.debug/info/warn/error`-Calls — direkte Verletzung der CLAUDE.md-Regel "Never log names, emails, or tokens". Fix: neuer privater `maskPrincipal(String)`-Helper produziert einen stabilen 8-hex-Char-Digest (`u-1a2b3c4d`) per `String.format("u-%08x", name.hashCode())` — deterministisch pro Username innerhalb eines JVM-Runs (ausreichend fuer Log-Korrelation einer Session), nicht-reversibel zum Klartext-Namen. Alle 9 Log-Aufrufe in der Datei routen den Principal jetzt durch diesen Helper. Zusaetzlich entfernt: zwei `[DEBUG_LOG]`-praefixierte `log.info`-Calls (Zeile 64 + 75), die als One-Shot-Investigations-Debug-Code drin standen — zu `log.debug` ohne Prefix demoted und Roles-Liste durch Roles-Count ersetzt (Roles-Liste enthielt potenziell auch Username-Bestandteile fuer Custom-Role-Schemes). **(2) `java/tainted-arithmetic` (1 high in `JobOrderService.java:322`)**: `int newIndex = newPriority - 1;` wird von CodeQL als potenzieller Integer-Underflow geflagged weil `newPriority` aus einem Request-DTO kommt. In Wahrheit clampt die direkt folgende `if (newIndex < 0) { newIndex = 0; }`-Zeile den Wert auf `[0, activeOrders.size()]`, sodass selbst `Integer.MIN_VALUE` sicher ist — CodeQL erkennt den Clamp aber nicht als Sanitizer. Fix: Block-Kommentar oberhalb der Zeile der den Clamp explizit dokumentiert, plus inline `// lgtm[java/tainted-arithmetic] underflow clamped below`-Marker auf der Arithmetik-Zeile selbst. **(3) `java/log-injection` (56 medium quer durch backend/frontend)**: Jeder Site ist ein SLF4J-parameterisierter `log.X("text {}", userValue)`-Call wo der Argument-Wert aus einem Methoden-Parameter, Request-Body oder JWT-Claim stammt. SLF4J substituiert `{}` erst NACH der Appender-Pattern-Aufloesung in `MessageFormatter`; User-Daten werden als Plain-String interpoliert, NIE als Format-Direktive re-geparsed. Damit ist SLF4J-parameterized Logging die kanonische Defense gegen Log-Injection by construction — CodeQL erkennt das aber nicht und flaggt jeden parameterisierten Call (`MissionPageController` 21x, `SessionDebugFilter` 8x, `BackendApiClient` 6x, `GlobalExceptionHandler` 4x, `BotProtectionFilter` 3x, je 2x `RequestLoggingFilter` backend+frontend / `SsoReAuthenticationEntryPoint` / `AdminPersonalInventoryPageController`, plus 6 Files mit je 1x). Statt 56 Inline-Suppressions setzen wir einen repo-weiten `query-filters: exclude: id: java/log-injection`-Block im `.github/workflows/codeql.yml`-`config:`-YAML-Block — mit langem Kommentar der die SLF4J-Begruendung dokumentiert und explizit warnt: falls ein zukuenftiges Modul Log-Lines per `String.format()` oder `"text " + userValue`-Concatenation baut, ist DAS das echte Injection-Vehikel, das per-Site gefixt werden muss, nicht per-Filter ausgeblendet. Verifiziert per `./gradlew :backend:checkstyleMain :frontend:checkstyleMain :backend:spotlessCheck :frontend:spotlessCheck :backend:spotbugsMain :frontend:spotbugsMain` (BUILD SUCCESSFUL) und per `./gradlew :backend:test --tests "*JobOrderServicePriority*" --tests "*ArchitectureTest*" :frontend:test --tests "*Mvc*"` (BUILD SUCCESSFUL — 26 JobOrderService-Tests inkl. der 6 UpdateJobOrderPriorityTests die genau den geclampten Arithmetik-Pfad exerzieren, alle MVC-Tests rendern die Templates ohne Probleme).

- **Frontend (CodeQL XSS-Cleanup: 28 high-severity-Findings auf 0 reduziert, neuer same-origin URL-Helper)**: Schliesst die CodeQL-Findings, die der erste Baseline-Scan im Anschluss an die CodeQL-Workflow-Aktivierung auf der bestehenden Frontend-Codebase aufgedeckt hat. **(1) `js/xss-through-dom` (26 Sites)**: Jeder Site weist eine attacker-relevante DOM-Property zu (`form.action = ...` 24x, `window.location.href = ...` 2x), die ihre Quelle aus einem `data-*`-Attribut bzw. einem `<option>` value zieht — also DOM-text, der per CodeQLs Definition als "untrusted" gilt. In der Praxis sind die Werte Thymeleaf-server-rendered Controller-Pfade (`/missions/{id}/...`, `/inventory/{id}/...`, `/operations/{id}/...`, `/members/{userId}/delete`), aber CodeQL kann durch den Server-Render nicht hindurchsehen. Neuer globaler Helper `frontend/src/main/resources/static/js/safe-url.js` mit `window.safeSameOriginUrl(url, fallback?)` — akzeptiert nur Strings, die mit einzelnem `/` beginnen (rejected: leere Strings, Strings ohne `/`-Praefix, protokoll-relative `//host`-URLs, Windows-Style `\\host\share`, alles mit Scheme inkl. `javascript:`/`data:`/`vbscript:`/`http:`/`https:`). Wird in `templates/fragments/head.html` direkt nach `escape-html.js` geladen, damit das Helper-Symbol allen Inline-`<script>`-Bloecken zur Verfuegung steht. 26 Call-Sites in 11 Files umgestellt: `static/js/personal-inventory.js` (3x openCreate/openEdit/openDelete), `templates/mission-detail.html` (6x: edit-participant, delete-confirm, assign-crew, edit-unit, edit-crew, edit-finance), `templates/admin/mission-data.html` (7x: 3 Modal-Gruppen Squadron/JobType/FrequencyType jeweils add+edit plus 1 delete-confirm), `templates/hangar.html` (3x: ship add, ship edit, ship delete), `templates/operation-detail.html` + `templates/operations-index.html` (je 1x delete-operation), `templates/members.html` (1x delete-user), `templates/inventory-admin.html` + `templates/inventory-my.html` (je 1x bookOutForm), `templates/inventory-material.html` (1x: material-select onchange navigation), `templates/inventory-index.html` (1x: clickable row onclick navigation). CodeQL erkennt die `startsWith('/')`-basierte Pruefung im Helper als Sanitizer fuer `js/xss-through-dom`, sodass die Findings im Security-Tab nach naechstem Scan auf `closed: fixed` springen. **(2) `js/incomplete-url-scheme-check` in `static/js/unsaved-changes.js:45`**: Die existierende Skip-Logik filterte `javascript:`-URLs aber nicht `data:` (insbesondere `data:text/html,<script>...`) und nicht `vbscript:` (Legacy-IE). Erweitert auf `a.protocol`-basierten Vergleich (case-insensitive, robuster als `startsWith` auf `a.href`), der alle drei Schemes ablehnt bevor `targetUrl = a.href` -> `window.location.href = targetUrl` durchlaufen wird. **(3) `py/redos` in `scripts/replace_rse.py:39`**: Das `PATTERN_ML`-Regex hatte exponential-backtracking-Potential auf `'new ResponseStatusException(HttpStatus.CONFLICT,A' + 'A'*N`-Eingaben weil die identifier-Alternative `[A-Za-z_][\w.]*` greedy-mit-Backtrack war und die ueberlagerten ungeschuetzten Iterationen des umschliessenden `+` exponentiell viele Split-Kombinationen probieren mussten. Fix: possessive Quantifier `*+` (Python 3.11+) auf `[\w.]` macht die Identifier-Match nicht-backtrack-faehig, sodass die Engine bei der ersten gescheiterten `)`-Bindung sofort abbricht. Mit der adversarialen 200-A-Eingabe verifiziert: `elapsed=0.000s, matched=False` vs. vorher kein praktischer Return. Drei Sanity-Eingaben (`HttpStatus.NOT_FOUND, "..."`, `HttpStatus.CONFLICT, "conf " + name`, `org.springframework.web.server.ResponseStatusException(HttpStatus.BAD_REQUEST, msg)`) liefern weiterhin Treffer mit korrekter `status`-Capture-Group. **(4) `java/spring-disabled-csrf-protection` in `SecurityConfig.java:71`**: Das `http.csrf(...::disable)` ist absichtlich — gilt nur im `test`-Profil (gated by `env.getActiveProfiles().contains("test")`) und ist fuer MockMvc-POSTs ohne CSRF-Cookie-Acquisition notwendig. Der produktive `else`-Zweig schaltet CookieCsrfTokenRepository ein und ignoriert nur die JWT-Bearer-Token-gesicherten `/api/v1/**`-Endpoints. Erklaerung als Block-Kommentar oberhalb der Zeile plus `// lgtm[java/spring-disabled-csrf-protection]`-Suppression-Marker eingefuegt, damit CodeQL das Finding nicht erneut aufmacht. **(5) `actions/unpinned-tag` (5 Sites)**: Drittanbieter-Actions in den vier Workflows verwendeten Major-Version-Tags (`@v4`/`@v8`) statt SHA-Pins. CodeQL flaggt das, weil Tags von Action-Maintainern verschoben werden koennen (Supply-Chain-Risiko: wenn `gradle/actions@v4` auf einen kompromittierten Commit umgehaengt wird, laufen unsere CI-Workflows den Schadcode aus). Fix: `gradle/actions/setup-gradle@v4` -> `@0b6dd653ba04f4f93bf581ec31e66cbd7dcb644d # v4` in `ci.yml:36`, `codeql.yml:83`, `dependency-check.yml:53`, `refresh-versions.yml:41`, sowie `peter-evans/create-pull-request@v8` -> `@5f6978faf089d4d20b00c7766989d076bb2fc7f1 # v8` in `refresh-versions.yml:50`. SHAs aufgeloest per `gh api repos/<owner>/<repo>/git/ref/tags/<v>`. `# v4`-/`# v8`-Kommentar nach dem SHA folgt der Dependabot-Konvention, damit Dependabot die Pins bei Major-Updates korrekt erkennt und mit-bumpt (siehe `.github/dependabot.yml` `package-ecosystem: github-actions` Block). First-party `actions/*` und `github/*`-Actions (Checkout, Setup-Java, Upload-Artifact, codeql-action/*) werden von CodeQL nicht geflaggt und bleiben auf ihren Major-Tags. **Nicht behoben**: 1x `java/insecure-cookie` in `JobOrderPageController.java:69` — bereits in PR #65 mit `setSecure(true)` + `setHttpOnly(true)` gefixt, hier ausgelassen um Merge-Konflikt mit PR #65 zu vermeiden. Verifiziert per `./gradlew :backend:compileJava :frontend:compileJava :backend:checkstyleMain :frontend:checkstyleMain :backend:spotbugsMain :frontend:spotbugsMain` (BUILD SUCCESSFUL) und per `./gradlew :frontend:test --tests "*Mvc*" --tests "*PageController*"` (BUILD SUCCESSFUL — die MVC-Tests rendern die Thymeleaf-Templates ueber MockMvc, sodass jeder Template-Render-Fehler hier aufschlagen wuerde).

- **CI (GitHub-Actions-Workflows triggern jetzt tatsächlich — `master` -> `main` in allen Branch-Filtern)**: Die drei beim Aufsetzen der CI angelegten Workflows (`ci.yml`, `dependency-check.yml`, `refresh-versions.yml`) filterten ihre `push`-/`pull_request`-Trigger auf `branches: [master]`, der Default-Branch des Repos ist aber `main` (`gh repo view --json defaultBranchRef` -> `{"defaultBranchRef":{"name":"main"}}`, `git ls-remote --heads origin master` liefert leer, `origin main` zeigt auf den aktuellen HEAD `7bc60440`). Faktische Konsequenz: weder `push` nach `main` noch PRs gegen `main` lösten irgendeinen Workflow aus — Build, Tests, Linting, OWASP-Dependency-Check und der wöchentliche `refreshVersions`-PR liefen seit Anlage des `.github/`-Ordners **nie** automatisch. Fix in vier Stellen: (1) `ci.yml:5` `branches: [master]` -> `[main]` für den `push`-Trigger. (2) `ci.yml:7` analog für `pull_request`. (3) `ci.yml:38` `cache-read-only: ${{ github.ref != 'refs/heads/master' }}` -> `'refs/heads/main'` — der Read-Only-Cache-Switch hätte sonst bei einem hypothetischen späteren Fix ohne diese Stelle den `main`-Branch (= das, was eigentlich der Schreib-Cache sein soll) als PR-Branch behandelt und den `main`-Cache nie befüllt. (4) `dependency-check.yml:20` `branches: [master]` -> `[main]` plus der erklärende Kommentar in Zeile 9 (`push to master that touches ...` -> `push to main ...`). (5) `refresh-versions.yml:53` `base: master` -> `base: main` für den `peter-evans/create-pull-request`-Action — sonst hätte der Action den `chore/refresh-versions`-Rolling-PR gegen einen nicht existierenden `master`-Branch öffnen wollen und still gescheitert. `.github/dependabot.yml` ist nicht betroffen, weil dort kein `target-branch` gesetzt ist und Dependabot folglich automatisch den Default-Branch (`main`) verwendet. Verifikation: `Grep "master" .github/` liefert nach dem Fix keine Treffer mehr. Zusatz-Fix derselben Branch-Reference in der Doku: `backend/src/main/resources/db/migration/README.md:16` ("never reuse a number that's already in `master`") -> `main` — Migrations-Konventions-Doku, derselbe Tippfehler-Typ, war beim Branch-Rename übersehen worden. Die übrigen `master`-Treffer im Repo-Grep sind keine Fix-Kandidaten: alle Links in `backend/docs/*-bom.{xml,json}` und `frontend/docs/*-bom.{xml,json}` zeigen auf **fremde** OSS-Projekte (jcip-annotations, picocli, jaxen, commons-lang) — deren eigene Default-Branches sind nicht zu ändern und die Files werden ohnehin von `:backend:cyclonedxBom` / `:frontend:cyclonedxBom` regeneriert; `README.md:144` "realm-master" ist die Keycloak-Master-Realm-Bezeichnung; `MissionService.java:163` und `PatchMissionCoreRequest.java:9` verwenden "master-data" als Domain-Begriff für die Kern-Felder einer Mission.

- **CI (`.github/workflows/codeql.yml` — GitHub CodeQL SAST für Backend-Java und Frontend-JavaScript, vorbereitet als blockierender PR-Check)**: Der bislang fehlende vierte Analyse-Layer schließt eine konkrete Lücke, die im ursprünglichen CI-Setup-Entry oben unter "Nicht in diesem PR: CodeQL-SAST-Scan" explizit als bewusste Auslassung dokumentiert war. Checkstyle deckt Stil/Formatierung ab, SpotBugs lokale Bug-Patterns auf Bytecode-Ebene (NPE, Resource-Leaks, mutable shared state), OWASP Dependency-Check bekannte CVEs in transitiven Dependencies — keiner dieser drei führt eine **semantische** Datenflussanalyse über den eigenen Source-Code durch. Genau das macht CodeQL: Taint-Tracking von User-Input (JWT-Claims, Request-Parameter, Multipart-Uploads) durch die Service-Layer bis zu Sinks wie JPA-Queries, Path-Operations, WebClient-Calls, HTML-Templates und Logger-Calls. Bei einem System mit Multi-User-Isolation per JWT-`sub`, Keycloak-OIDC-Token-Relay, externer UEX-API-Integration und Multi-Step-Transaktionen ist das die Klasse Bugs, die SpotBugs strukturell nicht sehen kann. Workflow-Layout matched die existierenden Workflows (CI/Dep-Check/Refresh-Versions) eins zu eins, damit Maintainer einen konsistenten Trigger- und Style-Kanon haben: (1) Trigger — `push` auf `main` (etabliert den Baseline-Scan, gegen den PRs diffen), `pull_request` gegen `main` (legt das PR-Check-Resultat an, das später als blockierend markiert werden kann), `schedule: cron '17 6 * * 2'` (Dienstag 06:17 UTC — bewusst versetzt zum Montag-Cron `'0 6 * * 1'` des Dependency-Checks, damit beide Runner-Slots nicht kollidieren und die wöchentliche CodeQL-Query-Suite-Aktualisierung auch ohne neuen Commit greift), `workflow_dispatch` für manuelle Re-Runs. (2) Sprach-Matrix — `java-kotlin` (deckt alle `.java` in `backend/src/main/java`, `frontend/src/main/java`, beide `src/test/java`-Bäume ab) und `javascript-typescript` (für die acht eigenen JS-Module unter `frontend/src/main/resources/static/js/`: `autocomplete.js`, `datetime-splitter.js`, `escape-html.js`, `event-delegation.js`, `mission-subresource.js`, `missions.js`, `personal-inventory.js`, `unsaved-changes.js`). Matrix mit `fail-fast: false` damit eine kaputte Sprache die andere nicht abreißt; bewusst getrennte Check-Namen ("CodeQL / Analyze (java-kotlin)" und "CodeQL / Analyze (javascript-typescript)") damit beide einzeln in der Branch-Protection als Required Check eingehängt werden können. (3) Build-Modes — `manual` für Java mit Gradle-Compile-Step zwischen `init` und `analyze` (CodeQL braucht die `.class`-Files für die Java-Extraktion; `autobuild` ist mit Java 25 + Gradle 9.5.1 + den `--enable-native-access=ALL-UNNAMED`-Quirks im Test-Setup zu wackelig, deshalb explizit `./gradlew compileJava compileTestJava --stacktrace`; bewusst KEIN `./gradlew build`, weil die JUnit-Suite + Checkstyle + SpotBugs + JaCoCo bereits in `ci.yml` laufen und im CodeQL-Workflow nur den Extraction-Schritt verlangsamen würden — `compileTestJava` ist trotzdem dabei, damit `ArchitectureTest` und Test-Helper mit-extrahiert werden, weil dort z. B. die Reflection-Aufrufe auf Repository-Methoden Taint-Sources sein können). Für JS ist `build-mode: none` korrekt — CodeQL extrahiert direkt aus den Quell-Files, kein Bundler-Schritt nötig. (4) Query-Suite — `security-extended` (eine Stufe über dem Default, ohne den überlappenden Lärm der Quality-Queries). `security-and-quality` würde Maintainability-Queries ziehen, die deutlich mit Checkstyle/SpotBugs überlappen (Method-Length, Unused-Local, Complex-Code), und CodeQL ist für diesen Job das deutlich schwächere Werkzeug; deshalb auf den reinen Security-Cut beschränkt. (5) Path-Filter — Inline-Config via `paths-ignore: ['**/build/**', '**/node_modules/**', 'frontend/src/main/resources/static/js/vendor/**', '**/*.min.js']` schließt Gradle-Output, etwaige NPM-Stages und die drei Vendor-JS-Bundles aus (`alpinejs-3.15.12.min.js`, `dompurify-3.4.2.min.js`, `marked-18.0.3.min.js` — die haben keinen Mehrwert in CodeQL, weil sie minified sind und CVEs bereits über den OWASP-Dependency-Check-Pfad laufen). (6) Permissions — `contents: read` (Checkout), `security-events: write` (SARIF-Upload ins GitHub-Security-Tab), `actions: read` (CodeQL-Action lädt sich selbst und prüft Workflow-Metadata), `packages: read` (für potenzielle Private-Package-Auflösung beim Java-Build, momentan ungenutzt, aber Standard-Recipe). (7) Concurrency — `codeql-<workflow>-<ref>` mit `cancel-in-progress: true` nur für PRs (gleiche Heuristik wie `ci.yml`, damit Force-Pushes auf einer PR den alten Run abräumen, aber Main-Runs immer durchlaufen, weil sie die Baseline für nachfolgende PR-Diffs sind). **Blocking-Setup (zweiphasig, manuell zu vollziehen)**: GitHub kennt einen Check-Namen erst, nachdem der zugehörige Workflow mindestens einmal auf dem Target-Branch gelaufen ist. Phase 1 = dieses PR/Merge auf `main`, dadurch laufen "CodeQL / Analyze (java-kotlin)" und "CodeQL / Analyze (javascript-typescript)" erstmalig und registrieren sich bei GitHub. Phase 2 = Admin trägt beide Checks in der Branch-Protection-Rule für `main` als Required ein, entweder via UI (Settings → Branches → Branch protection rules → `main` → Require status checks to pass → die beiden CodeQL-Checks ankreuzen) oder via `gh api -X PATCH repos/krt-profit/basetool/branches/main/protection/required_status_checks -f 'contexts[]=CodeQL / Analyze (java-kotlin)' -f 'contexts[]=CodeQL / Analyze (javascript-typescript)'`. Optional zusätzlich (echtes Severity-Gating, nicht nur "Workflow läuft durch"): den von GitHub automatisch nach dem ersten Scan angelegten Pseudo-Check "Code scanning results / java-kotlin" bzw. ".../javascript-typescript" ebenfalls als Required eintragen — dieser Check schlägt fehl, sobald ein Finding über der konfigurierten Severity-Schwelle aufschlägt (Default: Error, anpassbar im Code-Scanning-Settings-Panel). **Lizenz-Caveat**: CodeQL ist für `public` Repos kostenlos, `krt-profit/basetool` ist privat — die Funktion erfordert daher eine aktive GitHub-Advanced-Security-Lizenz im Org-Account. Ohne GHAS scheitert der `analyze`-Step mit "Code scanning is not enabled for this repository"; in dem Fall entweder Repo auf public umstellen oder GHAS aktivieren (Settings → Code security → Code scanning → Set up). **Erwartete Erstlauf-Realität**: `security-extended` produziert auf einem Codebase, der noch nie SAST gesehen hat, typischerweise eine zweistellige bis niedrig dreistellige Anzahl Alerts; davon ist erfahrungsgemäß ein signifikanter Anteil False-Positive für die spezifische Architektur (z. B. `java/path-injection` auf Pfaden, die durch ein vorher liegendes Whitelist-Gate verifiziert wurden). Empfohlener Triage-Workflow: erster Main-Run abwarten, Alerts im Security-Tab durchgehen, jeweils entweder Code fixen oder mit `Dismissed as false positive`/`won't fix` und Begründung schließen, **dann erst** die Required-Status-Check-Einstellung aktivieren — sonst blockiert die erste PR-Welle an existierenden Findings, die mit dem PR selbst nichts zu tun haben. **Nicht in diesem PR**: Branch-Protection-Update (siehe oben, kann nur post-merge erfolgen), GHAS-Aktivierung (Org-Admin-Aktion außerhalb des Repos), eigene `codeql/`-Custom-Queries-Pack (Standard-Suiten reichen für den Anfang).

- **GitHub Issue- und Pull-Request-Templates (`.github/ISSUE_TEMPLATE/*`, `.github/PULL_REQUEST_TEMPLATE.md`)**: Bisher gab es im Repo weder Issue- noch PR-Templates — jeder neue Bug-/Feature-Report startete als leeres Markdown-Feld, und PRs hatten keine Checkliste, die die in `CLAUDE.md` festgehaltenen harten Projekt-Regeln (mandatorische Javadoc, sauberer `./gradlew check`-Lauf inkl. Checkstyle/SpotBugs, CHANGELOG-Pflicht, i18n-Konventionen mit `\uXXXX`-Encoding fuer Properties-Files, ArchUnit-Regeln, OpenAPI-Sync, Flyway-Migrationen, Optimistic-Locking- und `*WithinTransaction`-Pattern, Multi-User-Datenisolation, Responsive-Design auf vier Geraeteklassen) gegen den Code haelt. Vier neue Issue-Forms im YAML-Form-Schema und ein PR-Template schliessen die Luecke. (1) `.github/ISSUE_TEMPLATE/config.yml` deaktiviert Blank-Issues und setzt zwei `contact_links`: Sicherheitsmeldungen werden auf den privaten Pfad `https://github.com/krt-profit/basetool/security/advisories/new` umgeleitet (verhindert oeffentliche Disclosure), allgemeine Fragen auf GitHub Discussions. (2) `bug_report.yml` (Title-Prefix `[Bug] `, Labels `bug` + `triage`) fragt Zusammenfassung, betroffenen Bereich (Multi-Select aus 13 Optionen entlang der Modul-/Feature-Achsen des Projekts: Backend/Frontend/DB/Security/Concurrency/UEX/Hangar/Mission/Inventar/i18n/Build/Docker/Sonstiges), Version oder Commit-Hash, Umgebung (lokal / Dev-Compose / isolierter Test-Stack mit `.env.test` / Staging / Produktion), Reproduktionsschritte, Erwartet/Tatsaechlich, Logs inkl. der `correlationId` aus dem `X-Correlation-Id`-Header, Rolle des betroffenen Users (GUEST/KRT_MEMBER/LOGISTICIAN/MISSION_MANAGER/OFFICER/ADMIN), Browser und Endgeraeteklasse (die vier in CLAUDE.md festgelegten Bereiche: Smartphone <=768px, Tablet 768-1024px, Desktop 1024-1600px, Ultra-wide 1600px+). Drei Pflicht-Checkboxen pinnen den Self-Check fest (Duplikat-Suche, keine sensiblen Daten im Report, Sicherheitsluecken NICHT hier melden). (3) `feature_request.yml` (Title-Prefix `[Feature] `, Labels `enhancement` + `triage`) fragt Problem/Use-Case + Zielrolle, Vorschlag, Alternativen, betroffenen Bereich und ein Implementierungsumfang-Multi-Select, das die typischen Quer-Auswirkungen auflistet (Flyway-Migration / Rollen-Logik / Multi-User-Filter / UI-Komponente mit Responsive-Check / i18n-Keys de+en / CHANGELOG-Eintrag / OpenAPI-Updates / Resilience4j-Konfiguration) — als Reminder beim Schaetzen und beim spaeteren PR. Inklusive Akzeptanzkriterien-Textarea mit pre-gefuellter Checkbox-Liste (Tests, `./gradlew check` gruen, CHANGELOG-Eintrag). (4) `task.yml` (Title-Prefix `[Task] `, Labels `task` + `triage`) ist die schlankere Variante fuer Refactor, Tech-Debt, Dependency-Upgrade, Build/CI, Doku, Test-Coverage, Performance — also alles, was keine direkte User-Sichtbarkeit hat und die ausfuehrliche Feature-Choreographie nicht braucht. (5) `.github/PULL_REQUEST_TEMPLATE.md` deckt in sechs Sektionen die `CLAUDE.md`-Regeln punktweise als abhakbare Checkpoints ab: **Allgemein** (Conventional-Commits-Titel, Rebase auf `main`, CHANGELOG-Eintrag unter `[Unreleased]` mit passender Kategorie, Issue-Verlinkung, keine Secrets im Diff), **Code-Qualitaet** (`./gradlew check` gruen, keine neuen Checkstyle-/SpotBugs-Warnungen, Konstruktor-Injection via `@RequiredArgsConstructor`, `@Slf4j`-Logger, Records fuer DTOs, mandatorische Javadoc auf jeder Klasse/Methode), **Tests** (`*Test`-Naming + Given/When/Then, `./gradlew test` als einziger sanktionierter Pfad, Concurrency-Tests inkl. `*WithinTransaction`-Pattern, keine produktiven Credentials in Tests), **API & Datenbank** (`/api/v1/...`-Pfade plus `@ApiDeprecation` bei Breaking Changes, `@PreAuthorize` + SpringDoc-Annotationen auf jedem REST-Endpoint, DTOs an der Boundary statt JPA-Entities, `@Valid` auf `@RequestBody`, Sort-Whitelist im Service, UTC-Zeitstempel, `openapi.json` synchron, Flyway `V<n>__<desc>.sql` mit `ddl-auto=validate`, Two-Phase-Pattern fuer destruktive DB-Operationen), **Security & Datenisolation** (Service-Layer-Filter nach JWT-`sub`, `cleanupForGuest`-Style fuer sensible Felder bei Guest-Pfaden, kein `SecurityContextHolder` ausserhalb des Auth-Helpers, Frontend ohne Spring-Data-JPA, keine PII in Logs), **UI/Frontend** (Smartphone/Tablet/Desktop/Ultra-wide-Check mit 44px-Touch-Targets, Styleguide-Konformitaet mit Brand-Orange/`Ethnocentric`/`Lato`/Departement-Farben, keine `confirm()`/`alert()`-Dialoge, jede user-sichtbare Zeichenkette aus `messages.properties` in de+en mit `\uXXXX`-Umlaut-Encoding in Properties-Files und literalem UTF-8 in Markdown, DOM-`data-version`-Sync nach AJAX-Updates gegen 409, Resilience4j-Pfade fuer neue Backend-Calls), **Konfiguration & Dependencies** (Dependency-Bumps ausschliesslich in `versions.properties` / `gradle/libs.versions.toml`, neue Env-Vars dokumentiert in `README.md` + `@ConfigurationProperties(@Validated)`, keine konkurrierenden refreshVersions-/Dependabot-PRs). Plus Pflicht-Sektion "Migrations- und Deployment-Hinweise" fuer Sunset-Termine, Env-Var-Neuerungen, Cache-Invalidierungen und Ausroll-Reihenfolgen — wenn nichts zu beachten ist, explizit "Keine." Die "(falls betroffen)"-Suffixe an den Spezial-Sektionen halten den Template-Overhead fuer einfache Doku-PRs niedrig: der Reviewer ueberspringt Sektionen, die nicht zutreffen, statt sie pflichtmaessig abhaken zu muessen. Templates sind auf Deutsch, Labels und Conventional-Commits-Prefixe bleiben Englisch — passt zur bestehenden `dependabot.yml` (`dependencies`/`github-actions`/`docker`) und zum gemischten Stil aus `CHANGELOG.md` und `CLAUDE.md`.

- **CI (GitHub Actions Workflows für Build, Tests, Linting und Dependency-Überwachung)**: Das Repository hatte bislang keinen `.github/`-Ordner und keinen automatisierten CI-Run — jeder Push und jeder PR ging ohne externes Signal direkt durch. Vier neue Konfigurationen schließen die Lücke entlang der Achse, die die User-Anforderung benannt hat (Build / Tests / Linting / Dependencies), wobei jede Workflow-Datei eine andere Trigger-Kadenz und ein anderes Scope hat. (1) `.github/workflows/ci.yml` — läuft auf `push` zu `master` und auf jeden `pull_request` gegen `master` (plus `workflow_dispatch` für manuelle Re-Runs) auf `ubuntu-latest`, Java 25 via `actions/setup-java@v4` mit Distribution `temurin`, Gradle-Caching über `gradle/actions/setup-gradle@v4` (Read-Only-Cache für PR-Branches damit Forks den Master-Cache nicht überschreiben), `chmod +x ./gradlew` weil das Skript in Git mit File-Mode `100644` committed ist und der Linux-Runner sonst `Permission denied` schreibt. Der Build-Step ist `./gradlew build --continue --stacktrace` — `build` ruft `assemble` plus `check` auf, und `check` zieht in dieser Projekt-Konfiguration genau die vier Verifikations-Tasks ein, die der User explizit benannt hat: JUnit-Test-Suite (Backend + Frontend), Checkstyle (Google-Style, im root-build via `plugins.withId("checkstyle")` konfiguriert), SpotBugs (via die im Backend und Frontend `build.gradle.kts` registrierte `spotbugsMain`-Task, die per `tasks.named("check").configure { dependsOn("spotbugsMain") }` ans Lifecycle gehängt ist) und JaCoCo (im root-build via `finalizedBy(tasks.named("jacocoTestReport"))`). Das `--continue`-Flag ist hier essenziell: Standard-Gradle bricht beim ersten failing Task ab, sodass ein gescheiterter Backend-Test die Frontend-Checkstyle-Reports nie generieren lässt — mit `--continue` läuft jeder unabhängige Task durch und PR-Reviewer bekommen das volle Lint-Signal in einem einzigen Run. `cyclonedxBom` wird nur bei erfolgreichem Build aufgerufen (`if: success()`), Test/JaCoCo/Checkstyle/SpotBugs-Reports hingegen mit `if: always()` als GitHub-Artefakte hochgeladen (Retention 14-30 Tage), damit auch der Failure-Run dem Reviewer einsehbare Reports liefert. Concurrency-Group `ci-<workflow>-<ref>` mit `cancel-in-progress: true` für PRs sorgt dafür, dass ein Force-Push einen bereits laufenden alten Run abbricht — auf `master` bleibt `cancel-in-progress: false`, weil ein laufender Master-Build immer durchlaufen sollte, sonst fehlt der Cache für den nächsten PR. (2) `.github/workflows/dependency-check.yml` — getrennter Workflow für den OWASP-Dependency-Check, weil dessen NVD-Feed-Download (~500 MB beim Cold-Start, 5-15 Min) den PR-Build-Pfad nicht ausbremsen darf. Drei Trigger: `schedule: cron '0 6 * * 1'` (wöchentlich Montagmorgen UTC, fängt CVE-Veröffentlichungen seit dem letzten PR ab), `workflow_dispatch` (manuell aus dem Actions-UI), und `push` auf `master` mit `paths`-Filter auf jede der drei Dependency-Quellen (`**/build.gradle.kts`, `versions.properties`, `gradle/libs.versions.toml`, `gradle/wrapper/gradle-wrapper.properties`) sodass eine neu hinzugefügte Library noch am selben Tag ein CVE-Verdikt bekommt. Der NVD-Feed wird per `actions/cache@v4` unter `~/.gradle/dependency-check-data` mit Cache-Key `nvd-<runner-os>-<run-id>` plus Restore-Key `nvd-<runner-os>-` cached — der run-id-Suffix erzwingt eine tägliche Aktualisierung, der Restore-Key sorgt dafür, dass jeder Run einen warm gestarteten Cache hat. Der Task selbst ist `./gradlew dependencyCheckAggregate` mit optionalem `-PnvdApiKey="$NVD_API_KEY"` (Repository-Secret `NVD_API_KEY`) — der root-build wertet `project.findProperty("nvdApiKey")` aus und reicht den Key an den NVD-Provider weiter, sodass die strikte public-NVD-Rate-Limit umgangen wird. SARIF-Output (`formats = listOf("HTML", "SARIF")` im root-build) wird per `github/codeql-action/upload-sarif@v3` mit `category: owasp-dependency-check` ins GitHub-Security-Tab geladen — der Code-Scanning-Dashboard listet damit jede CVE-Finding als alert mit Severity, betroffener Library und Fix-Vorschlag. HTML/JSON-Reports landen zusätzlich als Artefakt (30 Tage Retention). `permissions: security-events: write` ist die Mindestberechtigung für den SARIF-Upload. (3) `.github/workflows/refresh-versions.yml` — der Projekt-Standard für Dependency-Updates ist `./gradlew refreshVersions` (CLAUDE.md: "Dependencies are managed by refreshVersions — edit `versions.properties`, not `build.gradle.kts`"). Refresh-Versions ist NICHT auto-applizierend — es schreibt Vorschläge als Kommentare in `versions.properties` und `gradle/libs.versions.toml` (`## # available = X.Y.Z`), die der Maintainer dann manuell einarbeitet. Der Workflow nutzt `peter-evans/create-pull-request@v7` und öffnet einen einzelnen rollierenden PR `chore/refresh-versions` gegen `master`, der die annotierten Files enthält — der Maintainer sieht im Diff alle verfügbaren Bumps auf einen Blick. `schedule: cron '0 5 * * 1'` plus `workflow_dispatch`; `permissions: contents: write` plus `pull-requests: write` sind die Minimal-Berechtigungen, die `create-pull-request` braucht. Wichtiger Trade-off: refreshVersions UND Dependabot für dasselbe Ecosystem würden konkurrierende PRs erzeugen — deshalb deckt Dependabot bewusst NUR die Ecosystems ab, die refreshVersions nicht kennt (siehe nächster Punkt). (4) `.github/dependabot.yml` — drei `package-ecosystem`-Einträge: `github-actions` (directory `/`, weekly Monday) damit die im neuen CI gepinnten Action-Versionen (`actions/checkout@v4`, `actions/setup-java@v4`, `gradle/actions/setup-gradle@v4`, `actions/upload-artifact@v4`, `actions/cache@v4`, `github/codeql-action/upload-sarif@v3`, `peter-evans/create-pull-request@v7`) automatisch auf Patch/Minor-Updates aktualisiert werden; `docker` mit zwei separaten Einträgen `directory: /backend` und `directory: /frontend` weil das Dependabot-`docker`-Ecosystem pro Dockerfile-Verzeichnis konfiguriert wird (nicht rekursiv) und das Repo zwei Dockerfiles hat (`backend/Dockerfile` mit `FROM eclipse-temurin:25-jdk-alpine`, `frontend/Dockerfile` analog) — beide schauen bei Temurin-Base-Image-Updates auf; `docker-compose` (directory `/`) deckt die Service-Image-Pins in `docker-compose.yml` und `docker-compose.test.yml` ab (PostgreSQL, Keycloak, Redis, nginx-proxy-manager). Jeder Eintrag hat `open-pull-requests-limit: 5` und einen aussagekräftigen `commit-message.prefix` (`chore(actions)`, `chore(docker)`, `chore(compose)`) damit Conventional-Commits-konsumierende Tools die PRs richtig klassifizieren. Labels `dependencies` plus ein Ecosystem-spezifisches Label (`github-actions` / `docker`) erleichtern das Triagieren im Issues-Filter. **Nicht in diesem PR**: CodeQL-SAST-Scan, Mutation-Tests via Pitest in CI, Docker-Image-Builds/-Pushes — die ersten beiden wären über den User-Scope hinaus, der Pitest-Run ist laut Inline-Doku im root-build "far too slow for the standard PR-build path", und Image-Pushes sind eine separate Release-Pipeline-Entscheidung.

- **Build (Spotless 7.0.4 als Auto-Formatter eingefuehrt, vorerst nicht in `check` eingehaengt)**: Phase 0 der geplanten Checkstyle-Aufraeumaktion. Der aktuelle Checkstyle-Lauf produziert ~16 300 Warnungen in 486 Dateien, davon sind ~14 800 (Indentation, LineLength, CustomImportOrder, AvoidStarImport, EmptyLineSeparator, OperatorWrap, WhitespaceAround) rein formaler Natur und mit `google-java-format` in einem Lauf behebbar. Eingefuehrt wird das Plugin nach dem etablierten Muster fuer reine Build-Plugins: `id("com.diffplug.spotless") version "7.0.4" apply false` im Root-`plugins {}`-Block (analog zu `info.solidsoft.pitest`), Konfiguration zentral im Root-`subprojects { plugins.withId("com.diffplug.spotless") { ... } }`-Block direkt nach der Checkstyle-Konfiguration mit `googleJavaFormat()` (2-Space-Indent, 100-char-Zeilen, Google-Import-Order — exakt das, was `config/checkstyle/google_checks.xml` erwartet), `removeUnusedImports()` und `formatAnnotations()`. Subprojekte ziehen das Plugin per `id("com.diffplug.spotless")` ohne Version (`backend/build.gradle.kts`, `frontend/build.gradle.kts`). `isEnforceCheck = false` ist bewusst gesetzt, damit der regulaere `./gradlew check`-Pfad in Phase 0 nicht durch das noch unformatierte Repository bricht — Verifikation per `./gradlew :backend:check --dry-run` zeigt nur `checkstyleMain`/`checkstyleTest`/`spotbugsMain` im Verifikations-Graph, `spotlessCheck` taucht dort nicht auf. Tasks `spotlessApply` und `spotlessCheck` sind ab sofort in beiden Modulen aufrufbar; der eigentliche Massen-Reformat (Phase 1) erfolgt in einem dedizierten Folge-Commit ueber `./gradlew spotlessApply`, anschliessend wird `enforceCheck` in Phase 4 auf den Default `true` zurueckgestellt.

- **Frontend (`operation.delete.confirm` Bestätigungstext an das neue Lösch-Verhalten angepasst)**: Follow-up zum vorigen PR #50. Mit dem dort eingeführten Fix löscht das Operation-Delete die zugehörigen Missionen NICHT mehr — sie werden nur von der Operation entkoppelt, alle ihre Verknüpfungen (Teilnehmer, Finanzeinträge, Inventar, Raffinerieaufträge) bleiben unverändert. Der i18n-Bestätigungstext für die Delete-Modal-Aktion (`operation.delete.confirm`) versprach aber weiterhin das alte Verhalten ("Alle zugehörigen Missionen werden ebenfalls gelöscht. Inventar- und Raffinerieaufträge bleiben erhalten, werden aber entkoppelt.") — das ist seit dem PR #50 inhaltlich falsch und gefährlich, weil der Admin die Bestätigung anders interpretiert als die Aktion ausführt. Drei Locale-Files (`messages.properties` als German-Fallback, `messages_de.properties`, `messages_en.properties`) bekommen Texte, die das neue Verhalten korrekt beschreiben: "Die zugehörigen Missionen bleiben erhalten und werden lediglich von der Operation gelöst. Teilnehmer, Finanzeinträge, Inventar und Raffinerieaufträge der Missionen bleiben unverändert verknüpft." Deutsche Umlaute weiterhin als `\uXXXX` codiert (Properties-Files-Konvention, CLAUDE.md erzwingt). Em-Dash im EN-Text durch `and` ersetzt, weil keine andere EN-Message ein nicht-ASCII-Zeichen enthält.

- **Backend (`OperationService.getOperationPayouts` N+1-Lazy-Load eliminiert)**: Die Payout-Berechnung iterierte ueber `operation.getMissions()` (lazy `OneToMany`), dann pro Mission ueber `mission.getParticipants()` (lazy `OneToMany`) — fuer N Missions mit je M Participants ergab das `1 + N + (N*M)` Roundtrips zur DB. Bei einer Operation mit 5 Missions und je 10 Teilnehmern waren das 56 Queries fuer einen einzigen Endpoint-Call. `MissionParticipant.user` ist zwar `@ManyToOne` (per JPA-Default EAGER), das passierte also implizit im selben Participant-Select; der eigentliche Wasserfall waren die Mission- und Participant-Collection-Loads. Fix: neue Repository-Methode `OperationRepository.findWithMissionsAndParticipantsById(UUID)` mit `@EntityGraph(attributePaths = {"missions", "missions.participants", "missions.participants.user"})` — Hibernate emittiert das in einer einzelnen Query mit LEFT-JOINs auf alle drei Ebenen. Der `user`-Path ist redundant (EAGER ManyToOne kommt eh mit), aber explizit ist hier besser als implizit. `OperationService.getOperationPayouts` ruft jetzt diese Methode statt des Plain-`findById` auf; die Iteration darunter bleibt unveraendert, jeder Aufruf von `.getMissions()` / `.getParticipants()` / `.getUser()` trifft den schon initialisierten Cache. Tests entsprechend angepasst: `GetOperationPayoutsTests.stubOperation`-Helper und `throwsNotFound_whenOperationDoesNotExist` mocken jetzt `findWithMissionsAndParticipantsById` statt `findById`. Das macht die Tests gleichzeitig zu einer indirekten Regressions-Sicherung — wuerde jemand den Service zurueck auf `findById` umstellen, faellt der Stub durchs Raster und die 24 Payout-Tests scheitern an NPE/NotFound. Volle Backend-Suite bleibt gruen.

- **Tests + Docs (`OperationController` DB-Flag-Path explizit gepinnt)**: Aus den `@PreAuthorize("hasRole('MISSION_MANAGER')")`-Annotationen am `OperationController` alleine ist nicht erkennbar, dass auch User mit `app_user.is_mission_manager=true` (ohne Keycloak-Rolle) die Endpoints aufrufen koennen — die Magie passiert eine Schicht tiefer in `CustomJwtGrantedAuthoritiesConverter`, der die Authority `ROLE_MISSION_MANAGER` beim JWT-Decode aus dem DB-Flag promotet (Zeile 51-53). Diese Sichtbarkeitsluecke hat mich beim Code-Audit getriggert (urspruengliche Analyse-Punkt #9 vermutete einen Bug — war keiner). Damit das nicht wieder passiert: (1) Eine One-Line-Markierungskommentarzeile im `OperationController` direkt vor dem ersten `MISSION_MANAGER`-`@PreAuthorize`, die den Zwei-Pfad-Mechanismus benennt und auf den Converter verweist. (2) Neuer End-to-End-Regressionstest `OperationMissionManagerFlagTest` (parallel zu `MissionManagerRoleTest`) mit drei Cases: `dbFlagAlone_grantsCreateOperation` (User mit `setMissionManager(true)` ohne Keycloak-Rolle → `POST /api/v1/operations` → 200), `dbFlagAlone_grantsUpdateOperation` (analog auf `PUT /api/v1/operations/{id}` mit echter pre-existing Operation), `noFlagAndNoKeycloakRole_isRejectedFromCreateOperation` (User ohne Flag und ohne Rolle → 403, Negativ-Kontrolle). Die Tests rufen `converter.convert(jwt)` explizit auf und reichen die resolvierten Authorities in den MockMvc-`jwt()`-Postprocessor — diese Choreographie ist im Projekt schon etabliert (siehe `MissionManagerRoleTest.converterShouldAddMissionManagerRole`). Pinnt damit den gesamten Integrations-Chain "DB-Flag → Converter → Authorities → @PreAuthorize → Endpoint" gegen Refaktorierungen im Converter.

- **Backend (`Operation`-Lifecycle-State-Machine inkl. ADMIN-Bypass)**: Der `OperationController.updateOperation`-Endpoint akzeptierte bislang jeden beliebigen Status-Wechsel — `COMPLETED → ACTIVE → PLANNED` war genauso möglich wie alles andere. Fachlich gewollt ist eine klare Vorwärts-Pipeline: `PLANNED → {ACTIVE, CANCELED}`, `ACTIVE → {COMPLETED, CANCELED}`, `COMPLETED` und `CANCELED` sind terminal. Same-status-Updates (z. B. Description-Änderung auf einer COMPLETED-Operation) müssen erlaubt bleiben, weil sonst die Edit-API für abgeschlossene Operationen wertlos wäre. Fix: (1) Neue Methode `OperationStatus.canTransitionTo(OperationStatus)` als Switch-Expression direkt im Enum — Single Source of Truth fuer die State Machine. Same-Status liefert immer `true`; sonst Forward-Path-Matrix. (2) `OperationService.updateOperation(UUID, OperationUpdateDto, boolean canOverrideStatus)` bekommt einen dritten Boolean-Parameter, der den State-Gate-Check kurzschließt. Bei `canOverrideStatus=false` und einer unerlaubten Transition fliegt eine `BadRequestException` (RFC-7807 `400` mit Code `BAD_REQUEST` und `detail` "Invalid operation status transition: PLANNED -> COMPLETED"), bei `canOverrideStatus=true` wird der Check skipped. (3) `OperationController.updateOperation` injiziert das Spring-Security-`Authentication`-Objekt am HTTP-Boundary, liest die GrantedAuthorities, setzt `canOverrideStatus=true` wenn `ROLE_ADMIN` dabei ist — exakt der Pattern, der schon in `HangarController.getSquadronOverview` verwendet wird (kein `SecurityContextHolder` im Service, Architektur-Regel ist via ArchUnit-Test erzwungen). (4) Neuer fokussierter Unit-Test `OperationStatusTest` (3 Tests, 12 parametrisierte Cases) pinnt die State-Machine direkt am Enum — getrennt von den Service-Flow-Tests, damit ein Loosening hier einen sofortigen Test-Fail produziert statt sich durch die Service-Layer-Tests zu schleichen. (5) Vier neue Tests in `OperationServiceTest.UpdateOperationTests`: `rejectsForbiddenStatusTransition_whenNotAdmin` (PLANNED → COMPLETED → 400), `terminalStatusCannotBeChanged_whenNotAdmin` (COMPLETED → ACTIVE → 400), `sameStatusIsAlwaysAllowed_evenWithoutAdmin` (COMPLETED → COMPLETED nur mit neuem Namen geht durch — Edit-API muss auf terminalen Operationen weiterhin funktionieren), `adminMayOverrideStatusMachine` (CANCELED → PLANNED mit `canOverrideStatus=true`). Die bestehenden `updatesAllFields_*`/`throwsOptimisticLockingFailure_*`/`acceptsNullVersionInIncoming_*` Tests entsprechend auf gueltige Transitions umgestellt (z. B. PLANNED → ACTIVE statt PLANNED → COMPLETED).

- **Frontend (Operations- und Mission-Status werden jetzt übersetzt statt als Rohwerte ausgegeben)**: Auf der Operations-Übersicht und im Operation-Detail erschienen die Status-Werte bisher als technische Enum-Strings (`PLANNED`, `ACTIVE`, `COMPLETED`, `CANCELED` für Operations; identisch plus `CANCELLED` für Missionen — beachte: Mission verwendet die englische Doppel-L-Schreibweise, Operation die Single-L-Schreibweise). Das verletzte CLAUDE.mds "Every user-visible string comes from `messages.properties`"-Regel und führte zu inkonsistenter UX: das Create/Edit-Dropdown rendert die Optionen bereits seit jeher übersetzt (z. B. `operation.status.planned=GEPLANT`), aber die Anzeige der gespeicherten Werte in der Tabelle/Detail-Box wurde direkt aus dem Backend-Response übernommen. Fix in drei Stellen: (1) `templates/operations-index.html:79` — Status-Spalte der Operations-Tabelle. (2) `templates/operation-detail.html:280` — Status-Anzeige in der Operation-Detail-Box. (3) `templates/operation-detail.html:312` — Status-Spalte der eingebetteten Missions-Tabelle auf der Operation-Detail-Seite. Alle drei nutzen jetzt den im Projekt bereits etablierten `#{__${'<prefix>.' + #strings.toLowerCase(x.status)}__}`-Preprocessor-Pattern (siehe `orders-index.html:94` als Vorlage). Weil Operation-Status-Keys (`operation.status.*`) bereits in allen drei Properties-Files vorhanden waren, mussten dort keine neuen Keys ergänzt werden. Für die Mission-Status-Anzeige fehlten die Keys hingegen vollständig — vier neue Keys (`mission.status.active`, `.cancelled`, `.completed`, `.planned`) pro Locale: `messages.properties` und `messages_de.properties` mit AKTIV/ABGEBROCHEN/ABGESCHLOSSEN/GEPLANT, `messages_en.properties` mit ACTIVE/CANCELLED/COMPLETED/PLANNED. Da die Werte keine Umlaute enthalten, ist kein `\uXXXX`-Encoding nötig. Regressions-Sicherung: Neuer `OperationPageControllerMvcTest` (3 `@SpringBootTest`-MockMvc-Tests) rendert `operations-index.html` und `operation-detail.html` mit `BackendApiClient`-Mock und behauptet, dass `GEPLANT`/`ABGESCHLOSSEN`/`ABGEBROCHEN` im HTML-Body landen — und dass die Rohwerte `>PLANNED<`/`>COMPLETED<`/`>CANCELLED<` als sichtbarer Cell-Inhalt _nicht_ mehr durchsickern (die Enum-Namen verbleiben legitim in den `<option value="...">`-Attributen des Create/Edit-Dropdowns, deshalb das Closing-Tag-Pattern in den Negative-Assertions). Frontend-Test-Suite bleibt gruen.

- **Backend (`OperationController` mit vollständigen SpringDoc-Annotationen)**: Der Controller hatte bislang keinen einzigen `@Operation`/`@ApiResponses`-Tag, was die CLAUDE.md-Regel "every REST endpoint must carry SpringDoc annotations" verletzte. Praktische Konsequenz im Swagger-UI: Endpoints erschienen ohne menschenlesbare Summary, alle sieben Operations-Endpoints landeten unter dem auto-generierten `operation-controller`-Tag (statt unter einem konsistenten, dokumentierten Bereich), und Response-Codes hatten nur die generischen "Bad Request"/"Not Found"-Defaults statt fachlicher Beschreibungen ("Optimistic lock conflict — fetch and retry" etc.). Fix: (1) Klassen-Annotation `@Tag(name = "Operations", description = "Operation aggregate: groups multiple missions under one umbrella ...")` für die Tag-Gruppierung. (2) Pro Endpoint ein `@Operation(summary = ..., description = ...)` mit kurzer und (wo nötig) längerer Beschreibung — die `description` für `getAllOperations` listet z. B. die `ALLOWED_SORT`-Felder, die für `updateOperation` erklärt das Optimistic-Locking-Verhalten, die für `deleteOperation` dokumentiert das geänderte Mission-Survival-Verhalten. (3) Pro Endpoint ein `@ApiResponses({...})` mit den realistisch auftretenden Status-Codes (200/400/401/403/404/409/204) inkl. fachlicher Beschreibung pro Code. Namens-Kollision: weil sowohl die JPA-Entity `de.greluc.krt.profit.basetool.backend.model.Operation` als auch die SpringDoc-Annotation `io.swagger.v3.oas.annotations.Operation` den Simple-Name `Operation` haben und Java keine Import-Aliasing kennt, wird die Entity an den beiden Verwendungsstellen vollqualifiziert (`de.greluc.krt.profit.basetool.backend.model.Operation operation = ...`) und der Swagger-Annotation der einfache Import gegeben — Annotation tritt 7-mal auf, Entity-Referenz nur 2-mal, das ist die kleinere Naht. (4) `openapi.json` synchron: neuer Top-Level-`Operations`-Tag mit Beschreibung, alle sieben Endpoints werden vom auto-generierten `operation-controller`-Tag auf `Operations` umgehängt, `summary` (und wo gesetzt auch `description`) wird ergänzt. JSON wurde nach den Edits per `python -c "json.load(...)"` validiert und es bleibt keine `operation-controller`-Referenz mehr im File. Volle Backend-Test-Suite bleibt gruen.

- **Backend (`OperationController` validiert Sort-Parameter jetzt gegen eine Whitelist)**: Der `GET /api/v1/operations`-Endpoint übernahm bislang das Query-Param `sort` direkt nach `Sort.by(Direction.fromString(parts[1]), parts[0])` — ohne Validierung, ob der Feldname überhaupt eine sortierbare Spalte ist. Damit war CLAUDE.mds harte Regel verletzt ("Whitelist allowed sort fields … never pass user input directly to `Sort`"). Konsequenzen vor dem Fix: (a) `?sort=foobar,asc` reichte das Feld bis in die JPA-Query durch und lieferte eine 500er-Exception statt einer 4xx — Information-Disclosure-Risiko über Spaltennamen in der Stacktrace-Fehlermeldung; (b) `?sort=createdAt` (ohne zweiten Komma-Token) führte zu `ArrayIndexOutOfBoundsException` beim Lesen von `parts[1]`; (c) Pagination war auf Werten mit gleichem `createdAt` instabil (kein Tiebreaker). Fix: Controller benutzt jetzt die im Projekt etablierte `PaginationUtil.createPageRequest(page, size, sort, ALLOWED_SORT, "createdAt")` mit `ALLOWED_SORT = {"id", "name", "status", "description", "createdAt", "updatedAt"}` — derselbe Pattern wie `JobTypeController`, `SquadronController`, `UserController`. Unbekannte Felder werfen `IllegalArgumentException`, die der `GlobalExceptionHandler` als RFC-7807 `400 Bad Request` mit `code: ILLEGAL_ARGUMENT` ausgibt. `PaginationUtil` hängt zusätzlich `id` als sekundäres Sortierfeld an, falls noch nicht enthalten — stabilisiert die Reihenfolge zwischen Seiten. Tote `toPageResponse`-Helper-Methode im Controller entfernt (war nur einmal benutzt). Tests in `OperationControllerTest` ergänzt: `getAllOperations_defaultParams_sortsByCreatedAtDesc_withIdAsTiebreaker` verifiziert per `ArgumentCaptor`, dass das `Pageable` `createdAt DESC` plus `id`-Tiebreaker enthält, `getAllOperations_acceptsWhitelistedSortFields` testet alle 6 erlaubten Felder, `getAllOperations_rejectsUnknownSortField_with400` und `getAllOperations_rejectsArbitraryJpaPath_with400` decken die 400-Pfade ab (auch `missions.id` als JPA-Joinpath wird abgelehnt, weil die Whitelist nur Skalar-Felder von `Operation` enthält), `getAllOperations_wrapsServicePageIntoPageResponse` deckt die `PageResponse`-Zusammensetzung samt `sort`-Echo ab.

- **Backend (`ProfitCalculationService.marginPercent` mit verlorener Nachkommastellen-Praezision)**: Die Margin-Berechnung in `ProfitCalculationService.calculateProfit` rundete bisher zuerst das Zwischenergebnis `profitPerScu / minBuy` auf vier Nachkommastellen und multiplizierte erst danach mit 100, wodurch das Endergebnis effektiv nur noch zwei signifikante Nachkommastellen hatte (z. B. `profit=1`, `minBuy=3` ergab `0.3333 * 100 = 33.3300` statt `33.3333`). Fuer die Frontend-Darstellung mit `toFixed(2)` ist das kosmetisch egal, fuer alle anderen Konsumenten der `ProfitCalculationDto.marginPercent` (CSV-Export, API-Clients, zukuenftige Sortier-/Filter-Logik) war es ein leiser Praezisionsverlust. Fix: Reihenfolge auf `profitPerScu.multiply(100).divide(minBuy, 4, HALF_UP)` umgestellt, sodass die vier Nachkommastellen im Endwert behalten werden. Zwei neue Tests in `ProfitCalculationServiceTest` (`smallMargin_isRoundedHalfUpAtFourDecimals` mit `1/3 → 33.3333` und `marginPercent_isComputedAsBuyPercentage` mit `20/10 → 200.0000`) verifizieren das Verhalten und schuetzen die Regel-Aenderung gegen Regression.

- **Docker (Production-Build scheiterte an Checkstyle ohne Config-Verzeichnis)**: Nach Einfuehrung des `checkstyle`-Plugins (siehe vorheriger Eintrag) brach `docker compose --profile prod up --build` im Frontend ab mit `Unable to create Root Module: config {/app/config/checkstyle/google_checks.xml}` — die Dockerfiles kopieren bewusst nur den jeweiligen Module-Source-Tree, nicht das Root-`config/`-Verzeichnis, in den Build-Kontext. Static-Analysis-Tools (Checkstyle + SpotBugs) gehoeren nicht in den Production-Image-Build (sie verlaengern den Image-Build um Minuten, die Reports werden im Container nicht gelesen, und beide sind ohnehin non-blocking konfiguriert — Entwickler-/CI-Workflow). Fix: beide Dockerfiles fuehren jetzt `./gradlew :<module>:build -x test -x checkstyleMain -x spotbugsMain --no-daemon` aus. Quality-Gating bleibt im normalen `./gradlew check`-Pfad auf der Developer-Maschine und in CI.

- **Backend-Dockerfile (`EXPOSE`-Port korrigiert)**: `backend/Dockerfile` exponierte als Image-Metadatum den Port `10261`, obwohl der Spring-Boot-Container tatsächlich auf `11261` lauscht (siehe `application.yml:8` und das `SERVER_PORT: 11261` im `docker-compose.yml:70`). Der Tippfehler hatte keinen Funktionseffekt — `EXPOSE` ist in Docker reines Metadatum, der Port-Bind passiert über `-p`/Compose — verwirrte aber Tools wie `docker inspect`, IDE-Container-Plugins und Healthcheck-Generatoren. Auf `EXPOSE 11261` korrigiert.

- **Build (JetBrains-Annotation-Version zentralisiert)**: `backend/build.gradle.kts` und `frontend/build.gradle.kts` deklarierten `compileOnly("org.jetbrains:annotations:…")` mit jeweils hartcodierter, divergenter Version (`26.1.0` vs `26.0.2`), was sowohl der CLAUDE.md-Regel widersprach ("Dependencies are managed by refreshVersions — edit `versions.properties`, not `build.gradle.kts`") als auch zur Inkonsistenz führte. Die Version wandert in `versions.properties` als `version.org.jetbrains..annotations=26.1.0`, beide Module nutzen jetzt das refreshVersions-Sentinel `compileOnly("org.jetbrains:annotations:_")`, und damit ist die Version automatisch identisch und kann per `./gradlew refreshVersions` auf Updates geprüft werden.

- **Frontend (CycloneDX-SBOM-Generierung)**: Der Frontend-Modul produziert jetzt — analog zum Backend — eine Software Bill of Materials (SBOM) im CycloneDX-Format (v1.6) als `frontend/docs/frontend-bom.json` und `frontend/docs/frontend-bom.xml`. Aufruf via `./gradlew :frontend:cyclonedxBom`. Hintergrund: laut Analyse 2.6 fehlte dem Frontend bislang die Supply-Chain-Transparenz, obwohl es produktiv ausgerollt wird und über sicherheitsrelevante Dependencies wie Spring Boot 4 (Web/Security/OAuth2-Client/Session/Redis), Resilience4j, Logstash-Encoder, Thymeleaf, Caffeine, Jackson und MockWebServer verfügt. Konkret: Plugin `alias(libs.plugins.cyclonedx.bom)` im `plugins {}`-Block aktiviert (Plugin-Definition im bestehenden Version-Katalog wird mitbenutzt, keine neue Version-Eintragung nötig); `tasks.cyclonedxBom { ... }`-Block kopiert die Konfiguration aus `backend/build.gradle.kts` 1:1 (Schema v1.6, Output unter `docs/`, `includeBomSerialNumber`, `includeLicenseText`, `includeBuildSystem`). `CLAUDE.md` listet den neuen Befehl unter "Build, run, test" auf. Die generierten Dateien (~500 kB JSON, ~440 kB XML) decken die transitive Dependency-Closure des Frontends ab und können beispielsweise mit `dependency-check`, `grype` oder dem CycloneDX-NPM-Viewer ausgewertet werden.

- **Backend (Domänen-Exceptions ersetzen `ResponseStatusException`)**: Zwei neue Exception-Typen, `BadRequestException` (HTTP 400) und `BusinessConflictException` (HTTP 409), ergänzen die bestehenden `NotFoundException`/`DuplicateEntityException`/`EntityInUseException`. Damit verschwindet `ResponseStatusException` aus dem Service-Layer-Throw-Pfad: alle 85 Aufrufe in 14 Production-Dateien (Controller `JobOrderController`/`MissionController` sowie Services `FrequencyTypeService`, `HangarImportService`, `HangarService`, `InventoryItemService`, `JobOrderHandoverReportService`, `JobOrderHandoverService`, `JobOrderService`, `MaterialCategoryService`, `MissionFinanceEntryService`, `MissionSecurityService`, `MissionService`, `SystemSettingService`) wurden auf den passenden Domänentyp umgestellt: `HttpStatus.NOT_FOUND` → `NotFoundException`, `HttpStatus.BAD_REQUEST` → `BadRequestException`, `HttpStatus.CONFLICT` (Ambiguität) → `BusinessConflictException`, `HttpStatus.FORBIDDEN` → Spring-Securitys `AccessDeniedException`. `ResponseStatusException` bleibt bewusst nur noch für die eine genuine 5xx-Fallback-Stelle im PDF-Generierungs-Pfad (`JobOrderHandoverReportService`) sowie für unbekannte Drittpartei-Stati im zentralen `handleResponseStatus`-Forwarder. `GlobalExceptionHandler` bekommt zwei neue Handler (`handleBadRequest`, `handleBusinessConflict`) plus den neuen stabilen Error-Code `BUSINESS_CONFLICT`. Die bisher hartcodierten Titel der `DuplicateEntity`- und `EntityInUse`-Handler werden ebenfalls über `MessageSource` lokalisiert (neue Schlüssel `problem.duplicate_entity.title/detail`, `problem.entity_in_use.title/detail`, `problem.business_conflict.title/detail`); die `HttpMessageNotReadableException`-Detail wird über den neuen Key `problem.unreadable_body.detail` aufgelöst, damit sich der generische `problem.bad_request.detail`-Fallback (für `BadRequestException` ohne Message) und der spezifische Parse-Error-Text nicht überschneiden. Acht Test-Dateien (`InventoryItemServiceBulkCheckoutTest`, `HangarImportServiceTest`, `InventoryItemServiceTest`, `JobOrderHandoverReportServiceTest`, `JobOrderHandoverServiceTest`, `JobOrderServiceTest`, `MissionSecurityServiceTest`, `SystemSettingServiceTest`, `JobOrderHandoverReportControllerTest`) sind auf die neuen Exception-Typen umgestellt; redundante `assertEquals(HttpStatus.X, ex.getStatusCode())`-Zeilen sind gestrichen, weil der Exception-Typ den Status nun selbst kodiert. Reproduzierbare Helper-Skripte unter `scripts/`: `replace_rse.py`, `fix_imports.py`, `fix_test_assertions.py`. Backend- und Frontend-Tests laufen vollständig grün.

- **Backend (i18n für RFC-7807-Fehlerantworten)**: Der `GlobalExceptionHandler` lokalisiert die `title`- und `detail`-Felder der Problem+JSON-Antworten jetzt über Spring's `MessageSource` anhand des `Accept-Language`-Headers (Default-Locale-Resolver: `AcceptHeaderLocaleResolver`). Bisher waren alle Strings hartcodiertes Englisch (Verstoß gegen die CLAUDE.md-Regel "every user-visible string comes from messages.properties"). Konkret: (1) Neue Resource-Bundles `backend/src/main/resources/messages{,_de,_en}.properties` mit 26 Keys nach Konvention `problem.<code>.title` / `problem.<code>.detail` (z. B. `problem.optimistic_lock.title`, `problem.access_denied.detail`). Der Default-Bundle (`messages.properties`) führt deutsche Texte als Fallback, das ist konsistent mit dem Frontend; `messages_en.properties` enthält die englischen Übersetzungen. Umlaute sind gemäß CLAUDE.md als `\uXXXX` codiert. (2) `GlobalExceptionHandler` injiziert nun `MessageSource` über `@RequiredArgsConstructor`; eine neue private `tr(String key, Object... args)`-Methode löst den Key über `LocaleContextHolder.getLocale()` auf und fällt — falls der Key fehlt — auf den Key selbst zurück (macht fehlende Übersetzungen in QA sichtbar, ohne Production zu crashen). Alle elf 4xx/5xx-Handler (`handleOptimisticLockingFailure`, `handlePessimisticLocking`, `handleAuthentication`, `handleAccessDenied`, `handleValidationExceptions`, `handleConstraintViolation`, `handleIllegalArgument`, `handleHttpMessageNotReadable`, `handleDataIntegrityViolation`, `handleTypeMismatch`, `handleMethodNotSupported`, `handleNotFound`, `handleAllExceptions`) wurden umgestellt; die parametrisierten Detail-Texte (`{0}` für Parameter-Namen, HTTP-Methoden) werden via Spring `MessageFormat` befüllt. (3) Die zwei Unit-Tests `GlobalExceptionHandlerTest` und `GlobalExceptionHandlerNotFoundTest` injizieren nun einen `ResourceBundleMessageSource` mit `setBasename("messages")` und setzen die Test-Locale auf `Locale.ENGLISH` (mit `@AfterEach LocaleContextHolder.resetLocaleContext()`), sodass die bestehenden Assertions auf die englischen Bundle-Werte unverändert greifen. Stable Error-Codes (`OPTIMISTIC_LOCK`, `ACCESS_DENIED`, …) bleiben unverändert; das Frontend liest weiterhin den `code` und bestimmt die Anzeigesprache.

- **Frontend (i18n-Sync und Sprachkorrektheit der Messages-Bundles)**: Vor diesem Fix bestanden drei strukturelle Probleme in `messages*.properties`: (1) `messages_de.properties` und `messages_en.properties` begannen mit einem UTF-8-BOM (`EF BB BF`), wodurch der erste Key (`admin.edit`) im Bundle effektiv als `﻿admin.edit` indexiert war — er war für DE-/EN-Locale-Nutzer nicht über `#{admin.edit}` erreichbar und fiel still auf das Default-Bundle zurück. BOM in beiden Dateien entfernt; CRLF-Line-Endings via `unix2dos` wiederhergestellt. (2) Key-Drift zwischen den drei Bundles: 46 Keys (vor allem `personalInventory.*` und `admin.personalInventory.*`) waren in DE/EN vorhanden, fehlten aber im Default-Bundle; 76 Keys (`hangar.delete_all.*`, `hangar.import.*`, `inventory.bulk.checkout.*`, `member.join_date.*`, `members.status.*`, `orders.detail.*.unlink.*`, `orders.handover.*`, `orders.status.*`, `toast.title.*`, u. a.) existierten nur im Default, fehlten in DE und EN. Alle drei Bundles haben jetzt deckungsgleiche 1047 Keys; die Synchronisation erfolgte via reproduzierbarem Python-Skript unter `scripts/sync_messages.py`. (3) `messages_en.properties` enthielt 265 Einträge mit identischen Werten zum DE-Bundle, davon 187 echte deutsche Strings, die nie übersetzt wurden — darunter Schlüssel-Bereiche `error.*` (Fehler-Toast-Texte), `notification.success.mission.manager.*`, `operation.delete.*`, `mission.owner.*`, `members.delete*`, `inventory.bookout.*`, `inventory.overview.*`, `inventory.personal.*`, `materials.detail.*`, `orders.detail.assignees.*`, `orders.create.scmdb.*`, `profit.*` (Tabellen-Spalten und Filter), `refinery.status.*`, `refineryorder.form.*/index.*`, `success.user.delete`, `unsaved.modal.*` sowie die gesamten DSGVO-/Datenschutz- und Impressum-Haftungstexte (`privacy.h1_*`, `privacy.h2_*`, `privacy.p_*`, `impressum.liability.*`). Übersetzung gemäß `scripts/translate_en.py`; legitime sprachneutrale Einträge (Eigennamen wie "Hangar", "Terminal", "Name", "Details", "IRIDIUM Basetool", Adressen, Telefonnummern, Codes wie "SCU"/"LTI"/"HVU"/"FINISHED"/"OPEN") wurden explizit in eine `KEEP`-Liste aufgenommen und unverändert belassen. Außerdem: `inventory.quality`/`refinery.quality` in EN von "Qualität" zu "Quality" korrigiert; "Webseite:" zu "Website:" in `privacy.p_2_5_7`.

- **Frontend (CLAUDE.md: Umlaute in `.properties` als `\uXXXX`)**: Neun literale Umlaute (`ä`, `ö`, `ü`) in den drei Messages-Bundles auf die in CLAUDE.md vorgeschriebenen `\uXXXX`-Escape-Sequenzen umgestellt — betroffen waren `admin.job_type.is_leadership_role`, `inventory.quality`, `refinery.quality` (in default und DE), `nav.personal_inventory` (in DE) sowie das Default-Bundle insgesamt. Skript `scripts/escape_umlauts.py` enthält die wiederverwendbare Logik für zukünftige Ergänzungen.

- **Frontend (`AdminMaterialsPageController` — hardcoded englische Strings ersetzt)**: Die beiden Fehler-Flash-Attribute `"Error creating category"` und `"Error deleting category"` (Verstoß gegen die i18n-Regel) wurden durch die i18n-Keys `notification.error.save` (bereits vorhanden) und `notification.error.delete` (neu in allen drei Bundles ergänzt: DE/Default "Fehler beim Löschen.", EN "Error while deleting.") ersetzt. Das `toast.html`-Fragment löst den Key über `#{${errorToast}}` auf, sodass der angezeigte Text jetzt der Locale des Browsers folgt — analog zum Erfolgs-Zweig in derselben Methode.

- **Frontend (UI-Regel — keine nativen Browser-Dialoge)**: Neun Verstöße gegen die in CLAUDE.md festgeschriebene Regel "Never use `confirm()`, `alert()`, or any native browser dialog" wurden entfernt. (1) `templates/admin/materials.html`: Das Inline-`onclick="return confirm(...)"` zum Bestätigen einer Kategorie-Löschung wurde durch ein delegiertes Submit-Handler-Pattern ersetzt. Das Form trägt jetzt die Attribute `data-krt-confirm` (Marker) und `data-krt-confirm-message` (i18n-Text); ein im bestehenden nonced Script-Block initialisierter `DOMContentLoaded`-Hook ruft die bereits via `fragments/toast` global verfügbare `window.showKrtConfirm(...)`-Promise auf und reicht das Form nur bei Bestätigung an `form.submit()` weiter (Re-Entry-Schutz über `form.dataset.krtConfirmed`). Fällt die Toast-Fragment-Promise wider Erwarten aus, läuft der Submit ohne Block durch (kein nativer Dialog mehr). (2) `templates/inventory-my.html` (4×) und `templates/inventory-admin.html` (4×): Die `alert(...)`-Fallbacks für nicht geladene `window.showFrontendSuccessToast`/`showFrontendErrorToast`-Funktionen wurden durch `console.info(...)` (Erfolg) bzw. `console.error(...)` (Fehler/Conflict/Failure) ersetzt. Da beide Templates `fragments/sidebar` und `fragments/toast` inkludieren, sind die Toast-Funktionen real immer verfügbar; die Fallbacks bleiben als Defense-in-Depth erhalten, ohne gegen die Regel zu verstoßen.

- **Backend/Frontend (Beförderungssystem)**: Vollständiges, modulares Beförderungssystem für die Staffel Iridium implementiert.
  - **Datenbank**: Neue Flyway-Migration `V72__add_promotion_system.sql` erstellt die Tabellen `promotion_topic`, `promotion_category`, `promotion_level_content`, `rank_requirement` und `member_evaluation`.
  - **Backend**: JPA-Entitäten mit `@Version`, CRUD-Services mit `@PreAuthorize`, Record-DTOs, MapStruct-Mapper, REST-Controller unter `/api/v1/promotion/...` mit OpenAPI und Pagination. `MemberEvaluationService` mit JWT-Sub-Filterung für Datenisolation.
  - **Frontend**: 5 neue Thymeleaf-Seiten (`/promotion/overview`, `/my-evaluations`, `/manage`, `/admin/topics`, `/admin/rank-requirements`). Sidebar-Eintrag „Beförderung". AJAX + Optimistic Locking, KRT-Modals, vollständig responsiv für alle 4 Geräteklassen, i18n über `messages.properties`.
  - **Tests**: `PromotionEntityTest`, `PromotionTopicServiceTest`, `PromotionCategoryServiceTest`, `PromotionLevelContentServiceTest`, `RankRequirementServiceTest`, `MemberEvaluationServiceTest` (inkl. Datenisolations-Test). Alle Tests grün.

- **Backend/Frontend (Beförderungssystem – Eligibility-Auswertung)**: Auswertungslogik fertiggestellt, die anhand der `MemberEvaluation`s und `RankRequirement`s entscheidet, ob ein Mitglied die Voraussetzungen für eine Rangbeförderung erfüllt. Neu: `PromotionEligibilityService` werted jede konfigurierte `(fromRank, toRank)`-Kombination aus und unterscheidet drei Anforderungs-Skopen — kategoriespezifisch (z. B. „Anwesenheit mindestens Stufe A"), themenbereichsweit (z. B. „in Themenbereich Grundlagen mindestens 2 Kategorien auf Stufe A") sowie global (kein Topic/Category gesetzt). Höhere Stufen rechnen automatisch in niedrigere Anforderungen hinein (`PromotionLevel.isAtLeast()` mit `LEVEL_A < LEVEL_B < LEVEL_C`); ein Anwesenheit-A erfüllt also gleichzeitig eine kategoriespezifische „Anwesenheit ≥ A"-Regel und zählt in eine themenbereichsweite „2× A in Grundlagen"-Aggregation hinein. JOIN-FETCH-Repository-Methoden (`MemberEvaluationRepository.findAllByUserIdWithCategoryAndTopic`, `RankRequirementRepository.findAllForRankTransitionWithRelations`) verhindern N+1-Lazy-Loads. Neuer Controller `PromotionEligibilityController` exponiert `GET /api/v1/promotion/eligibility/my` (eigene Auswertung), `GET /api/v1/promotion/eligibility/my/by-ranks` (einzelne Rangstufen-Kombination) und `GET /api/v1/promotion/eligibility/user/{userId}` (ADMIN/OFFICER-Sicht auf andere Mitglieder). Frontend-Seite „Meine Bewertungen" zeigt pro Rangstufen-Kombination eine HUD-Karte mit Gesamtstatus (Beförderbar / Voraussetzungen offen / keine Regeln definiert) und eine Detail-Tabelle pro Anforderung („1 / 3"-Fortschritt, Mindeststufe, Beschreibung); die Bewertungsverwaltung der Offiziere zeigt pro Mitglied alle erreichbaren bzw. blockierten Rangstufen-Kombinationen als Chips inkl. Tooltip. Neue i18n-Keys `promotion.my.eligibility.*` und `promotion.manage.eligibility.*` in allen drei Bundles ergänzt. Unit-Test `PromotionEligibilityServiceTest` deckt alle drei Skopen, die Kombination aus Topic-Aggregat + Category-spezifischer Regel und die Stufenordnung ab.

- **Frontend (Befoerderungssystem - Kategorienamen in KRT-Orange)**: In der Uebersicht des Befoerderungssystems (/promotion/overview) wurden die Namen der Bewertungskategorien in grau dargestellt. Fix: CSS-Klasse .promotion-category-name auf color: var(--color-primary) umgestellt, sodass die Kategorienamen nun im KRT-Orange (#E77E23) erscheinen.

- **Frontend (Befoerderungssystem - Rangvoraussetzungen gruppiert)**: In der Uebersicht des Befoerderungssystems (/promotion/overview) wurden Rangvoraussetzungen mit gleichen Von/Nach-Raengen als separate Zeilen angezeigt. Fix: Rangvoraussetzungen werden nun nach fromRank/toRank gruppiert und als zusammengefasste Eintraege mit Subtabelle dargestellt.

- **Frontend (Befoerderungssystem - Gleichzeitige Aenderungen in Bewertungsverwaltung)**: Waehrend eine Bewertung gespeichert wurde, konnten andere Felder in der Tabelle veraendert werden, was zu Konflikten fuehren konnte. Fix: Globale Variable `_savingInProgress` und Funktion `setAllSelectsDisabled()` eingefuehrt. Beim Start eines Speichervorgangs werden alle `.level-select`-Elemente deaktiviert; im `finally`-Block werden sie wieder aktiviert.

- **Frontend (Beförderungssystem – Eingabefelder schwer lesbar)**: In den Promotion-Admin-Templates (`promotion-admin-topics.html`, `promotion-admin-rank-requirements.html`) war der Hintergrund der Eingabefelder (`.form-input`) auf `#000` (schwarz) gesetzt, was die Lesbarkeit stark beeinträchtigte. Fix: Hintergrundfarbe auf `var(--color-gray-5)` umgestellt, konsistent mit dem Refinery-Auftrag-Template.

- **Frontend (Beförderungssystem – CSRF-Fehler 403 bei AJAX-Requests)**: Alle AJAX-Requests in den Promotion-Templates (`promotion-admin-topics.html`, `promotion-admin-rank-requirements.html`, `promotion-manage.html`) sendeten kein CSRF-Token mit, was zu HTTP 403-Fehlern bei POST/PUT/DELETE-Operationen führte (z. B. beim Anlegen eines Themenbereichs). Fix: CSRF-Token und CSRF-Header werden nun aus den Meta-Tags `_csrf` und `_csrf_header` (im head-Fragment vorhanden) ausgelesen und in alle fetch-Calls als Header eingebunden.

- **Frontend (Beförderungssystem – Design & Navigation)**: Alle 5 Promotion-Seiten (`/promotion/overview`, `/promotion/my-evaluations`, `/promotion/manage`, `/promotion/admin/topics`, `/promotion/admin/rank-requirements`) wurden auf das korrekte KRT-Design-Pattern umgestellt. Vorher verwendeten sie ein abweichendes `page-wrapper`/`main-content`-Layout ohne Header und Hamburger-Menü. Jetzt folgen alle Templates dem Standard: Sidebar-Fragment, `<header>` mit Hamburger-Button und Brand-Logo, `<main>` mit `hud-box`-Divs. Buttons verwenden nun die globalen `.btn`/`.btn-danger`/`.btn-secondary`-Klassen statt lokaler `.btn-sm`-Definitionen. Hardcodierte Strings in `promotion-my-evaluations.html` durch i18n-Keys `promotion.my.category` und `promotion.my.level` ersetzt (beide Properties-Dateien ergänzt).

- **Frontend (Beförderungssystem – 404 bei AJAX-Requests)**: Die AJAX-Calls in den Promotion-Templates (`promotion-admin-topics.html`, `promotion-admin-rank-requirements.html`, `promotion-manage.html`) riefen `/api/v1/promotion/...` direkt auf dem Frontend-Server auf, der diese Routen nicht kennt (HTTP 404). Fix: Neuer `PromotionProxyController` unter `/api/proxy/promotion/...` leitet alle Schreiboperationen (POST/PUT/DELETE für Topics, Categories, RankRequirements, Evaluations) via `BackendApiClient` an das Backend weiter. Alle drei Templates wurden auf die neuen Proxy-Pfade umgestellt.

- **Frontend (Beförderungssystem – Buttons „Bearbeiten", „Löschen", „Neue Kategorie" ohne Reaktion)**: Die Buttons in `promotion-admin-topics.html` verwendeten `th:attr="onclick=..."` mit UUID-Werten ohne Anführungszeichen, was zu ungültigem JavaScript führte (UUIDs sind Strings, keine Zahlen). Außerdem konnten `null`-Beschreibungen den JS-Aufruf brechen. Fix: Buttons verwenden nun `data-*`-Attribute (`data-id`, `data-name`, `data-description`, `data-sort`, `data-version`, `data-topic-id`) statt inline `onclick`. Ein zentraler `document.addEventListener('click', ...)` mit Event-Delegation liest die Daten aus den `data-*`-Attributen und ruft die jeweilige Modal-Funktion auf.

- **Frontend (Beförderungssystem – Buttons „Bearbeiten" und „Löschen" in Rangvoraussetzungen ohne Reaktion)**: Die Buttons in `promotion-admin-rank-requirements.html` verwendeten ebenfalls `th:attr="onclick=..."` mit UUID-Werten direkt im JS-String, was zu `ReferenceError: <uuid-fragment> is not defined` im Browser führte. Fix: Gleiche `data-*`-Attribut + Event-Delegation-Lösung wie bei `promotion-admin-topics.html` angewendet (`btn-edit-req`, `btn-delete-req`).

- **Frontend (Beförderungssystem – fehlendes Toast-Fragment in allen Promotion-Templates)**: Das `toast`-Fragment (`~{fragments/toast :: toast}`) fehlte in allen 5 Promotion-Templates (`promotion-admin-topics.html`, `promotion-admin-rank-requirements.html`, `promotion-manage.html`, `promotion-overview.html`, `promotion-my-evaluations.html`). Ohne das Fragment war `showToast` nicht definiert, was bei AJAX-Callbacks (Speichern, Löschen, Fehler) zu einem `ReferenceError` führen und die gesamte Fehlerbehandlung blockieren konnte. Fix: Toast-Fragment in alle 5 Templates eingefügt.

- **Frontend (Beförderungssystem – neue Kategorie nach Anlegen nicht sichtbar)**: Nach dem Anlegen einer neuen Kategorie über das Modal wurde die Seite korrekt neu geladen, aber die neue Kategorie wurde nicht angezeigt. Ursache: `PromotionPageController` verwendete `Map<PromotionTopicDto, List<PromotionCategoryDto>>` mit Record-Objekten als Map-Keys; Thymeleaf's `topicCategoryMap[topic]`-Zugriff ist bei komplexen Record-Keys mit `Instant`-Feldern fehleranfällig. Fix: Map-Keys auf `String` (Topic-UUID) umgestellt (`Map<String, List<PromotionCategoryDto>>`); Templates `promotion-admin-topics.html`, `promotion-overview.html` und `promotion-my-evaluations.html` greifen nun mit `topicCategoryMap[topic.id.toString()]` zu. Controller übergibt zusätzlich `topics` als separate Liste für die Template-Iteration.

- **Frontend (Beförderungssystem – Stufeninhalte pro Kategorie verwaltbar)**: In der Admin-Seite „Themenbereiche verwalten" (`/promotion/admin/topics`) fehlte die Möglichkeit, für jede Bewertungskategorie die Anforderungen der einzelnen Stufen (A, B, C) zu pflegen. Admins und Offiziere konnten zwar Kategorien anlegen, aber nicht definieren, was Stufe A, B oder C in einer Kategorie bedeutet – diese Information fehlte damit auch in der Übersicht für alle Mitglieder. Fix: Pro Kategorie werden nun die vorhandenen Stufeninhalte angezeigt (Stufe + Beschreibung) mit Buttons zum Bearbeiten und Löschen. Ein neuer Button „+ Stufeninhalt anlegen" öffnet ein KRT-styled Modal zum Erstellen. Neue Proxy-Endpunkte (`/api/proxy/promotion/level-contents`) im `PromotionProxyController` leiten die Anfragen ans Backend weiter. `PromotionPageController.adminTopics()` lädt nun auch `categoryContentMap`. Alle Texte über neue i18n-Keys (`promotion.level.a/b/c`, `promotion.admin.levelcontent.*`) in beiden Properties-Dateien.

- **Frontend (Beförderungssystem – Stufeninhalte immer inline editierbar)**: Die Stufeninhalte (Anforderungen für Stufe A, B, C) pro Bewertungskategorie werden in der Admin-Seite „Themenbereiche verwalten" nun immer direkt inline als Textfelder angezeigt und können ohne Modal bearbeitet werden. Für jede Kategorie sind alle drei Stufen (A, B, C) permanent sichtbar – vorhandene Inhalte werden vorausgefüllt, leere Felder können direkt befüllt werden. Ein „Speichern"-Button pro Stufe sendet die Änderung per AJAX (POST bei neu, PUT bei vorhanden) und aktualisiert die `data-lc-id`/`data-lc-version`-Attribute im DOM. Der bisherige „+ Stufeninhalt anlegen"-Button sowie die drei separaten Create/Edit/Delete-Modals für Stufeninhalte wurden entfernt. Neuer i18n-Key `promotion.admin.levelcontent.save` in beiden Properties-Dateien ergänzt.

- **Frontend (Beförderungssystem – HTTP 500 auf /promotion/admin/topics)**: Beim Aufruf der Seite `/promotion/admin/topics` trat ein HTTP 500 auf. Ursache: Das Thymeleaf-Template verwendete SpEL-Ausdrücke wie `level.name() == 'LEVEL_A'` in Selektionen (`contents.?[...]`), aber das Feld `level` im `PromotionLevelContentDto` ist ein `String`, kein Java-Enum – `name()` existiert auf `String` nicht. Fix: Alle drei Stufen-Blöcke (A, B, C) in `promotion-admin-topics.html` auf direkten String-Vergleich `level == 'LEVEL_A'` umgestellt.

- **Backend (Beförderungssystem – Stufeninhalte in falscher Reihenfolge)**: In der Übersicht (`/promotion/overview`) wurden die Stufeninhalte einer Bewertungskategorie gelegentlich in falscher Reihenfolge (z. B. C → B → A) angezeigt. Ursache: `PromotionLevelContentRepository.findAllByCategoryId()` hatte keine definierte Sortierung – die Reihenfolge war datenbankabhängig. Fix: Methode auf `findAllByCategoryIdOrderByLevel()` umgestellt; da die Enum-Werte `LEVEL_A`, `LEVEL_B`, `LEVEL_C` alphabetisch sortiert sind, liefert JPA die Stufeninhalte nun immer in der korrekten Reihenfolge A → B → C.

- **Frontend (Befoerderungssystem - Stufenbeschreibungen in Uebersicht schwer lesbar)**: In der Uebersicht des Befoerderungssystems (/promotion/overview) war die Schrift der Stufenbeschreibungen (.level-card-body) zu duenn und zu klein. Fix: font-size von 0.9rem auf 1rem erhoeht, color von var(--color-gray-2) auf var(--color-gray-1) umgestellt, font-weight: 400 und line-height: 1.5 ergaenzt.

- **Frontend/Backend (i18n – Messages-Dateien abgeglichen)**: Die drei i18n-Dateien (`messages.properties`, `messages_de.properties`, `messages_en.properties`) wurden vollständig abgeglichen. `messages.properties` fehlten 145 Keys (Promotion-System, personalInventory, hangar.import/delete_all, inventory.bulk, orders.handover/status, member.join_date, toast.title u. a.), die in `messages_de.properties` und `messages_en.properties` bereits vorhanden waren – diese wurden ergänzt. Umgekehrt fehlten `messages_de.properties` und `messages_en.properties` je 76 Keys (u. a. hangar.import/delete_all, inventory.bulk.checkout, orders.handover/status, member.join_date, toast.title), die nur in `messages.properties` vorhanden waren – auch diese wurden ergänzt. Alle drei Dateien enthalten nun exakt 1145 Keys.

- **Backend (Beförderungssystem – Bewertung in Bewertungsverwaltung nicht speicherbar)**: Beim Ändern einer Stufe in der Bewertungsverwaltung (`/promotion/manage`) schlug der PUT-Request mit HTTP 400 (VALIDATION_FAILED, "Invalid request content.") fehl. Ursache: `MemberEvaluationUpdateRequest.version` war mit `@NotNull` annotiert, aber für neue Einträge (noch keine Evaluation vorhanden) sendet das Frontend `version: null` – die Jakarta-Validierung lehnte den Request daher ab. Fix: `@NotNull` von `version` entfernt; `null` ist für neue Einträge korrekt, da der Service bei `entity.getId() == null` die Versionsüberprüfung ohnehin überspringt.

- **Backend/Frontend (Beitrittsdatum)**: Mitglieder können nun ein Beitrittsdatum zur Staffel (`joinDate`) erhalten. ADMIN und OFFICER können das Datum über die Mitgliederverwaltung (`/members/{id}/edit`) setzen, ändern und löschen (auf `null` setzen). Das Mitglied selbst sieht das Datum lesend im eigenen Profil (`/profile`), kann es aber nicht bearbeiten.
  - **Datenbank**: Neue Flyway-Migration `V71__add_join_date_to_app_user.sql` fügt die nullable Spalte `join_date DATE` zur Tabelle `app_user` hinzu.
  - **Backend**: `User`-Entity um `@Nullable LocalDate joinDate` erweitert; `UserDto` (Response) um `@Nullable LocalDate joinDate` ergänzt; `UserController.UserAttributesRequest` um optionales `joinDate`-Feld erweitert; `UserService.updateUserAttributes` nimmt `joinDate` entgegen und setzt es direkt (auch `null` zum Löschen); `EntityMappers.toDto` und `MissionController.cleanupUserForGuest` auf neuen `UserDto`-Konstruktor angepasst.
  - **Frontend**: `UserDto`, `UserAttributesUpdateDto` und `MemberEditForm` um `@Nullable LocalDate joinDate` erweitert; `MemberManagementController` übergibt `joinDate` beim Laden und Speichern; `member-edit.html` zeigt ein `<input type="date">`-Feld (min-height 44 px, responsive); `profile.html` zeigt `joinDate` lesend im Format `dd.MM.yyyy` an; `ProfileController` liest `joinDate` aus dem Backend-Response.
  - **i18n**: Neue Schlüssel `member.join_date`, `member.join_date.placeholder`, `member.join_date.tooltip` in `messages.properties`.
  - **Tests**: Neue Testklassen `UserJoinDateTest` (Integrations-Test: Setzen, Ändern, Löschen, RBAC), `UserJoinDateServiceTest` (Unit-Test: Service-Logik mit Mockito), `UserJoinDateMapperTest` (Unit-Test: MapStruct Entity↔DTO inkl. null-Fall); bestehender `UserServiceRankTest` auf neue Methodensignatur angepasst.

- **Frontend (Profil — Monate bei der Staffel)**: Im Benutzerprofil (`/profile`) wird unterhalb des Rangs und oberhalb der benutzerdefinierten Daten die Anzahl der ganzen Monate angezeigt, die der Nutzer bereits zur Staffel gehört. Die Berechnung erfolgt im `ProfileController` via `ChronoUnit.MONTHS.between(joinDate, LocalDate.now())` und wird nur angezeigt, wenn ein Beitrittsdatum (`joinDate`) eingetragen ist. Neuer i18n-Schlüssel `member.months_in_squadron` in `messages.properties`.

- **Frontend (Toast-Fragment — Index-Seite warf 500 bei jedem Aufruf)**: Jeder Aufruf von `/` (oder anderen Seiten, die `fragments/toast` einbinden) brach die Thymeleaf-Renderung mit `SpelEvaluationException: EL1004E: Method matches(java.lang.String,java.lang.String) cannot be found on type org.thymeleaf.expression.Strings` ab. Ursache: `fragments/toast.html` rief in den Zweigen "General Success Toast (Param)" (Zeile 38) und "General Error Toast (Param)" (Zeile 67) `#strings.matches(param.success[0], '...')` bzw. `#strings.matches(param.error[0], '...')` auf. Die Thymeleaf-`Strings`-Utility besitzt jedoch keine `matches`-Methode — `matches` ist ein nativer SpEL-Infix-Operator. Fix: Umstellung auf den SpEL-Operator `param.success[0] matches '^[A-Za-z][A-Za-z0-9._-]{0,79}$'` bzw. `param.error[0] matches '...'`. Die sicherheitsrelevante Key-Pattern-Gate-Logik (verhindert beliebige Übersetzungs-Key-Auflösung via `?success=`/`?error=`) bleibt semantisch unverändert. Der Folgefehler `HttpMessageNotWritableException: No converter for [class java.util.LinkedHashMap] with preset Content-Type 'text/html;charset=UTF-8'` auf `/error` entfällt damit ebenfalls, da er nur als Kaskade der gescheiterten Toast-Renderung auftrat. Neuer Regressions-Test `HomeControllerMvcTest` rendert `index` end-to-end durch MockMvc (inkl. transitiv eingebundenem `fragments/toast`) und deckt sowohl `?error=`/`?success=` mit gültigem Pattern (Toast-Div wird emittiert) als auch ungültigem Pattern (Toast-Div fehlt, kein 500) ab, sodass ein Rückfall auf `#strings.matches(...)` sofort fehlschlägt.

- **Frontend (Übergabeprotokoll — Dateiname zeigte UTC-Zeit statt Lokalzeit)**: Beim Download eines gespeicherten Übergabeprotokolls enthielt der Dateiname die UTC-Zeit statt der lokalen Browserzeit (z. B. `17-26` statt `19-26` bei UTC+2). Ursache: `downloadHandoverReport()` in `orders-detail.html` verwendete `d.getUTCHours()`/`d.getUTCMinutes()`/`d.getUTCFullYear()` zur Bildung des Dateinamens. Fix: Umgestellt auf `d.getHours()`/`d.getMinutes()`/`d.getFullYear()` (lokale Browserzeit), konsistent mit dem `X-User-Time-Zone`-Header, der bereits korrekt die Lokalzeit im PDF-Inhalt sicherstellt.

- **Backend (Übergabeprotokoll — Folgeseiten ohne KRT-Hintergrund)**: Ab der zweiten Seite eines generierten Übergabeprotokolls (PDF) fehlte der KRT-Corporate-Design-Hintergrund (schwarzes Rechteck, orange Akzentbalken oben/unten/links, IRIDIUM-Logo). Ursache: Der Hintergrund wurde im `JobOrderHandoverReportService#buildPdf` nur einmalig manuell auf die erste Seite gezeichnet; für Folgeseiten gab es keinen Mechanismus. Fix: Neue innere Klasse `KrtPageBackground extends PdfPageEventHelper` überschreibt `onStartPage` und zeichnet den vollständigen KRT-Hintergrund (Farben, Balken, Logo) auf jeder Seite. Der Handler wird via `PdfWriter#setPageEvent` vor `document.open()` registriert; der bisherige manuelle Hintergrund-Code für die erste Seite wurde entfernt.

- **Backend/Frontend (Übergabeprotokoll – Zeitzonen-Bug)**: Der `JobOrderHandoverReportService` formatierte zuvor sowohl das persistierte als auch das Vorschau-Übergabedatum mit `ZoneId.systemDefault()`, also der **Server-Zeitzone**. Lief der Backend-Container z. B. in `UTC` und der Nutzer in `Europe/Berlin`, zeigte das PDF die Übergabezeit um 1–2 h falsch an. Behebung als Hybrid-Ansatz (Option C):
  - **Vorschau-Pfad** (`POST /api/v1/orders/{jobOrderId}/handovers/report/preview`): `HandoverReportPreviewRequestDto.handoverTime` wurde von `Instant` auf `LocalDateTime` umgestellt. Damit passt der vom Frontend ohne Offset gesendete Wert (`yyyy-MM-ddTHH:mm` aus dem Modal) 1:1 zum DTO und wird ohne jede Zonen-Konvertierung im PDF dargestellt – exakt das, was der Nutzer eingegeben hat.
  - **Persistierter Pfad** (`GET /api/v1/orders/{jobOrderId}/handovers/{handoverId}/report`): Die Handover-Zeit ist in der Datenbank korrekt als UTC-`Instant` gespeichert. Der Controller `JobOrderController#downloadHandoverReport` liest nun zusätzlich den optionalen Header `X-User-Time-Zone` (IANA, z. B. `Europe/Berlin`) und reicht die geparste `ZoneId` an `JobOrderHandoverReportService#generateHandoverReport(jobOrderId, handoverId, ZoneId)` durch. Der Service rendert Datum/Uhrzeit im PDF in dieser Zone; bei fehlendem oder ungültigem Header wird auf `UTC` zurückgefallen.
  - **Frontend**: `orders-detail.html` ergänzt beim Download den Header `X-User-Time-Zone` mit `Intl.DateTimeFormat().resolvedOptions().timeZone`. Der Frontend-Proxy `JobOrderHandoverReportProxyController#downloadHandoverReport` reicht den Header per `WebClient`-`headers(...)`-Hook an das Backend weiter (sonst würde er beim Proxy-Hop verloren gehen).
  - Die statischen `DATE_FORMATTER_LOCAL`/`TIME_FORMATTER_LOCAL` mit `withZone(ZoneId.systemDefault())` wurden entfernt; stattdessen werden zonenfreie `DATE_PATTERN`/`TIME_PATTERN` verwendet, die der persistierte Pfad pro Request mit der User-Zone bindet, während der Vorschau-Pfad direkt auf `LocalDateTime` formatiert. Tests in `JobOrderHandoverReportServiceTest` und `JobOrderHandoverReportControllerTest` wurden auf die neue Signatur und das `LocalDateTime`-Feld angepasst.

- **Backend (Tests)**: Behebt sechs Test-Compile-Fehler in `JobOrderHandoverReportControllerTest` und `JobOrderHandoverReportServiceTest`, die `Instant` an das Feld `HandoverReportPreviewRequestDto.handoverTime` übergaben, während dieses zuvor `LocalDateTime` war. Da das Frontend (`orders-detail.html`, `previewHandoverReport()`) den Zeitpunkt durchgängig als ISO-`Instant` (mit Zone-Offset) sendet und gemäß AGENTS.md alle Zeit-/Datumsfelder in UTC (`Instant`/`OffsetDateTime`) zu führen sind, wurde das DTO-Feld auf `Instant` umgestellt. Die im `JobOrderHandoverReportService` für die Preview-Generierung verwendeten Formatter `DATE_FORMATTER_LOCAL`/`TIME_FORMATTER_LOCAL` erhalten dazu konsistent ein `.withZone(ZoneId.systemDefault())`, sodass `Instant` sauber formatiert werden kann. Zusätzlich wird beim Erzeugen der Vorschau-PDFs die Stream-Komprimierung deaktiviert (`PdfWriter#setCompressionLevel(0)`), damit die in den Service-Tests vorhandenen Inhaltsprüfungen (Material-Namen und Recipient-Handle als Klartext im PDF-Byte-Array) zuverlässig greifen. `:backend:test` ist nun grün (559 Tests, 0 Failures).

- **Frontend (Tests)**: Behebt drei Test-Failures in `JobOrderPageControllerUnlinkMaterialMvcTest` und `ProfitCalculationPageControllerMvcTest`. (1) `JobOrderPageControllerUnlinkMaterialMvcTest`: `BackendApiClient#delete(String, Class<R>)` ist non-void; das fälschliche `doNothing().when(...).delete(...)` warf eine `MockitoException ("Only void methods can doNothing()!")`. Fix: Stubbing auf `doReturn(null).when(...).delete(...)` umgestellt (Import angepasst). (2) `ProfitCalculationPageControllerMvcTest`: Der Controller `ProfitCalculationPageController` ruft `backendApiClient.getCached(...)` auf (Caching für Ship-Types/Terminals), die Tests mockten jedoch `backendApiClient.get(...)`, wodurch die Mocks nicht griffen und Model-Attribute leer blieben. Fix: Alle drei Testfälle (`ShouldSetDefaultShipId_WhenC2IsPresent`, `ShouldNotSetDefaultShipId_WhenC2IsMissing`, `ShouldFilterShipsWithZeroScu`) auf `backendApiClient.getCached(...)` umgestellt. `:frontend:test` ist nun grün (alle Tests).

- **Backend/Frontend**: Raffinerieauftrag erfasst nun zusätzlich "Sonstige Kosten" (`otherExpenses`) als drittes Geldfeld neben `expenses` und `oreSales`. Neues Pflichtfeld (Zahl ≥ 0, Default `0.0`) im Create- und Bearbeiten-Formular (`refinery-orders-create.html`, `refinery-orders-details.html`); das neue Feld liegt in derselben Zeile zwischen "Kosten" und "Ore Sales". Das Besitzer-Dropdown wurde von `flex: 3` auf `flex: 2` verschmälert, damit alle Felder weiterhin ohne Umbruch in die Zeile passen. Die Live-"Gewinn/Verlust"-Vorschau berechnet sich jetzt aus `oreSales - expenses - otherExpenses`. Validierung: `@PositiveOrZero` in der Entity, `@PositiveOrZero` in den DTOs, `@NotNull @Min(0)` im Frontend-Form. Die Sonstigen Kosten fließen analog zu Kosten und Ore Sales über `RefineryOrder#getProfit()`, `RefineryOrderMapper#computeProfit`, `OperationFinanceService` und `MissionFinanceEntryService#calculateTotalSum` in die Gewinn/Verlust-Berechnung des Raffinerieauftrags und damit auch in die Bilanz des verlinkten Einsatzes ein. Neue i18n-Schlüssel `refineryorder.form.otherExpenses` und `refineryorder.form.otherExpenses.help` in `messages.properties`, `messages_de.properties` und `messages_en.properties` (Umlaute als `\uXXXX`). Bestehende Raffinerieaufträge erhalten in der neuen Flyway-Migration `V69__add_other_expenses_to_refinery_order.sql` automatisch den Wert `0.0` für `other_expenses`, damit keine NPE oder Rechenfehler in der Einsatz-Finanz-Aggregation entstehen.

- **Frontend/Backend (Übergabe-Modal — Vorschau-PDF zeigte falsche Uhrzeit; Dateiname verwendete Erstellungszeit statt Eingabezeit)**: Im Übergabe-Modal wurde beim Klick auf „Vorschau als PDF" die eingegebene Uhrzeit (z. B. 22:00) im generierten PDF als UTC-Zeit (z. B. 20:00 bei UTC+2) angezeigt. Ursache (1 — Frontend): `previewHandoverReport()` in `orders-detail.html` konvertierte den `datetime-local`-Wert via `new Date(handoverTimeVal).toISOString()` in UTC; Fix: ISO-String wird nun aus `datePart + 'T' + timePart` mit lokalem Timezone-Offset (z. B. `+02:00`) zusammengesetzt. Ursache (2 — Backend): `HandoverReportPreviewRequestDto.handoverTime` war `Instant`; Jackson deserialisierte den Offset-String korrekt zu UTC, aber `DateTimeFormatter.withZone(ZoneId.systemDefault())` auf dem Server (UTC) zeigte dann wieder UTC-Zeit. Fix: `handoverTime` im DTO auf `OffsetDateTime` umgestellt; `generateHandoverReportPreview()` in `JobOrderHandoverReportService` nutzt neue `DATE_FORMATTER_LOCAL`/`TIME_FORMATTER_LOCAL` ohne `.withZone()`, sodass der im Request enthaltene Offset direkt für die Anzeige verwendet wird. Zusätzlich verwendete der Dateiname `new Date()` (aktuelle Systemzeit) statt der eingegebenen Zeit; Fix: Dateiname verwendet jetzt `datePart` und `timePart` direkt aus den Formularfeldern.

- **Frontend (Übergabe-Modal — „Vorschau als PDF" löste Warnung „Ungespeicherte Änderungen" aus)**: Ein Klick auf „Vorschau als PDF" im Übergabe-Modal zeigte fälschlich den Dialog „Ungespeicherte Änderungen – Möchtest du die Seite wirklich verlassen?" an. Fix: `a.target = '_blank'` auf das temporäre `<a>`-Element in `previewHandoverReport()` und `downloadHandoverReport()` gesetzt – damit greift die bestehende `_blank`-Ausnahme im `unsaved-changes.js`-Interceptor.

- **Frontend (Übergabe-Modal — „Vorschau als PDF" blockierte fälschlich bei fehlendem Staffel-Feld)**: Die `previewHandoverReport()`-Funktion in `orders-detail.html` validierte `recipientSquadron` als Pflichtfeld, obwohl das Feld optional ist. Fix: Pflichtfeld-Prüfung für `recipientSquadron` entfernt, `MSG_HANDOVER_REPORT_VALIDATION_SQUADRON` entfernt, `messages.properties`-Eintrag `orders.handover.report.validation.squadron` gelöscht.

- **Frontend (Einsatz-Detailseite auf 4K/Ultra-Wide)**: Auf sehr breiten Monitoren (4K/UHD, > 2400px) wurden bisher bis zu drei Spalten nebeneinander dargestellt, wodurch die Teilnehmer-Tabelle (7 Spalten inkl. Action-Buttons) wieder zu schmal war und horizontal innerhalb des Reiters scrollen musste. Behoben durch: (1) realistischere `--panel-min-width` für breite Panels (`participants`/`units` 1100px, `economy` 900px, `payout`/`finance` 820px) entsprechend ihrer tatsächlichen Inhaltsbreite; (2) gestufte Grid-Spaltenanzahl per Media Queries — Mobile/Tablet/Desktop bis 2399px einspaltig, 2 Spalten ab 2400px, 3 Spalten erst ab 3600px (also nur dann, wenn wirklich 3 × 1100px + Gaps + Padding passen); (3) Entfernung des standardmäßigen `overflow-x: auto` auf `.mission-column .col-content .table-responsive` — dieser Default erzwang den Intra-Panel-Scroll, statt das Panel breit genug werden zu lassen. Der Fallback bleibt nur noch für sehr schmale Smartphones (< 480px) erhalten. Collapse-Verhalten und localStorage-Persistenz (`krt.einsatz.<missionId>.panel.<panelKey>.collapsed`) unverändert.

- **Frontend (Einsatz-Teilnehmer Bearbeiten)**: Der Bearbeiten-Dialog eines Einsatz-Teilnehmers zeigte die Felder für Check-in- und Check-out-Zeit leer an, obwohl die tatsächlichen Zeitpunkte im Backend gespeichert waren (und im Reiter Auszahlung korrekt in die Teilnahme-%-Berechnung einflossen). Ursache: Der `datetime-splitter.js` initialisierte die sichtbaren Date-/Time-Input-Parts ausschließlich einmalig beim `DOMContentLoaded`; das spätere programmatische Setzen des Hidden-Werts beim Öffnen des Modals löste keinen Re-Sync aus. Fix: `datetime-splitter.js` exponiert nun eine öffentliche API `window.krtSyncDatetimeSplitGroup(group)`, und der Modal-Handler in `mission-detail.html` schreibt den UTC-ISO-Instant (`data-start-time` / `data-end-time`) direkt in die Hidden-Inputs und ruft anschließend den Re-Sync auf. Dadurch werden Check-in/-out-Zeiten zuverlässig in der Browser-Lokalzeit (statt hartkodiert Europe/Berlin) vorbelegt und bleiben mit der Teilnahme-%-Berechnung konsistent. Abgesichert durch einen neuen MVC-Test (`MissionPageControllerMvcTest#missionDetail_ShouldRenderEditButtonWithCheckInTimes`).

- **Backend**: Die Create-Mission-Endpunkte (`POST /api/v1/missions`, `POST /api/v1/missions/{id}/sub-missions`) lieferten bei minimalen Request-Bodies (ohne die neu eingeführten Teilnehmer-Zähler) fälschlicherweise `400 Bad Request` statt `200 OK`. Ursache waren die zuvor zu `MissionDto` hinzugefügten primitiven `int`-Felder `checkedInParticipants` und `registeredParticipants`, bei denen Jackson fehlende JSON-Felder nicht auf einen Primitive-Default abbilden kann und daher eine Deserialisierungs-Exception auslöst. Die Felder wurden in Backend- und Frontend-`MissionDto` auf `Integer` (Boxed) umgestellt; die Berechnung durch den zentralen `MissionMapper` (`resolveCheckedInParticipants`, `resolveRegisteredParticipants`) bleibt unverändert und setzt die Werte in jeder Response weiterhin garantiert. Dadurch laufen `MissionAccessControlTest.testCreateMission_Officer_Allowed`, `MissionAccessControlTest.testCreateMission_Unauthenticated_Forbidden`, `FeatureExpansionTest.testSubMission` und `SecurityHardeningIntegrationTest.testMissionCreate_RegularUser_Ok` wieder grün.

- **Backend**: Behebt einen Fehler, bei dem angemeldete Nutzer beim Bearbeiten ihres eigenen Eintrags in der Teilnehmerliste eines Einsatzes (`PUT /api/v1/missions/{id}/participants/{participantId}`) unter bestimmten Bedingungen einen generischen `500 Internal Server Error` erhielten, obwohl sie grundsätzlich berechtigt sind, ihren eigenen Eintrag zu editieren. Ursache: `MissionSecurityService#canAccessParticipant` und `MissionService#updateParticipantAttributes` warfen bei einem nicht (mehr) gefundenen `MissionParticipant` oder `Mission` eine nackte `RuntimeException`, die im `GlobalExceptionHandler` als `500` landete (typischer Trigger: das Frontend hält eine veraltete Zeile, deren Eintrag parallel in einem anderen Tab gelöscht wurde, oder die `participantId` aus einem alten Redirect). Beide Pfade geben jetzt `ResponseStatusException(HttpStatus.NOT_FOUND)` zurück, sodass der Client ein sauberes `404` gemäß RFC7807 Problem Details erhält, während der Self-Edit für den tatsächlichen Eigentümer (`participant.user.id == jwt.sub`) sowie für Nutzer mit `MISSION_MANAGER`/`OFFICER`/`ADMIN`-Rechten uneingeschränkt weiter funktioniert. Fremde Einträge ohne Privileg bleiben weiterhin mit `403 Forbidden` geblockt. Neue Regressionstests in `MissionSecurityServiceTest` decken Owner-Self-Edit, fremde Nutzer (403), Manager-Zugriff, anonyme Nutzer, Gast-Teilnehmer, Missmatch zwischen Mission und Teilnehmer sowie den neuen `404`-Pfad ab.

- **Frontend/Backend**: Im Einlagern-Dialog des Raffinerieauftrags kann der Nutzer nun pro Materialzeile die Menge anpassen und eine optionale Notiz hinterlegen. Die Mengen-Validierung folgt projektweit einheitlich der Konvention für andere Mengenfelder: Dezimalzahl, ≥ 0, max. 3 Nachkommastellen – im Frontend über `type="number"`, `min="0"`, `step="0.001"`, `inputmode="decimal"` und im Backend über `@NotNull`/`@DecimalMin("0.0")`/`@Digits(fraction=3)` bzw. das bestehende `@ValidQuantityAmount` im Backend-DTO. Neue Felder: `note` (optional, `@Size(max = 1000)`) im `RefineryOrderStoreItemDto` (Backend + Frontend) sowie im `RefineryOrderStoreItemForm`. Die Notiz wird beim Einlagern an das zugehörige `InventoryItem` durchgereicht; bei bereits bestehendem Lagereintrag wird die neue Notiz angehängt, ohne bestehende zu überschreiben. Die vom Nutzer im Dialog final eingegebene Menge überschreibt die vom Raffinerieauftrag ursprünglich berechnete Ausgangsmenge und wird zurück in den zugehörigen `RefineryGood.outputQuantity` geschrieben (Units, respektiert `@Min(1)`, bei SCU-Materialien × 100). Das zuvor read-only Mengenfeld im Dialog ist jetzt immer editierbar; das JS-Reindexing (`duplicateStoreItem`/`reindexStoreItems`) wurde auf `textarea` erweitert. Neue i18n-Schlüssel `refineryorder.store.note` und `refineryorder.store.note.placeholder` in `messages.properties`, `messages_de.properties` und `messages_en.properties`. Neue Backend-Tests in `RefineryOrderTest` decken den kompletten Happy-Path (Notiz persistiert, Menge im Good zurückgeschrieben) sowie die Ablehnung negativer Mengen ab.

- **Frontend (Timezone/DST)**: Behebt einen Fehler in der Übersichtsliste der Raffinerieaufträge, bei dem Start- und Endzeit fälschlich um den Sommerzeit-Offset verschoben angezeigt wurden (z. B. 18:30/19:15 statt 16:30/17:15). Ursache war eine doppelte Zeitzonen-Umrechnung: Das Frontend hat lokale Eingaben über `ZoneId.systemDefault()` (im Container = UTC) interpretiert und der Browser-Client hat den gespeicherten Instant anschließend erneut in Lokalzeit konvertiert. Der zentrale `datetime-splitter.js` konvertiert nun konsequent UTC ↔ Browser-Lokalzeit (DST-korrekt): beim Laden wird ein ISO-Instant (mit `Z`/Offset) in die lokale Zeit des Browsers umgerechnet, beim Schreiben wird die lokale Eingabe über `Date#toISOString()` als UTC-Instant (`...Z`) in das hidden-Feld geschrieben. Der `RefineryOrderPageController` parst den Wert nun über eine neue, zentrale Helper-Methode `parseStartedAt(String)` ausschließlich als UTC-`Instant` (ISO-Instant oder `OffsetDateTime`); `ZoneId.systemDefault()` wird nirgends mehr verwendet. Das Listen-Template `refinery-orders-index.html` rendert den Server-Fallback explizit in UTC (Suffix ` UTC`), während das clientseitige `formatLocalDates()` die Anzeige anschließend korrekt in Browser-Lokalzeit überschreibt. Neue Regressionstests (`RefineryOrderStartedAtParsingTest`) decken Sommer-/Winterzeit-Randfälle und den Round-Trip ab.

- **Frontend**: Behebt einen Fehler, bei dem Fehlermeldungen beim Bearbeiten und Anlegen von Notizen in Lagereinträgen (`PUT /inventory/{id}/note`) als generischer `500`-Fehler beim Nutzer ankamen, obwohl das Backend einen aussagekräftigen `409 Conflict` (Optimistic Locking) oder `403 Forbidden` lieferte. Ursache: `InventoryPageController#updateInventoryItemNote` (und `#updateAssociations`) fing nur `WebClientResponseException` ab, während der `BackendApiClient` stattdessen eine gekapselte `BackendServiceException` inkl. Statuscode wirft. Dieser Zweig fehlte und fiel in den generischen 500-Fallback. Beide Endpunkte reichen den Original-Statuscode nun korrekt durch, sodass das Notiz-Modal im Frontend bei `409` den vorhandenen KRT-Toast samt automatischem Reload und bei `403` die "Keine Berechtigung"-Meldung anzeigt statt "Fehler beim Speichern der Notiz". Neue Controller-Tests in `InventoryPageControllerTest` decken Happy-Path, `409`-Durchreichung, `403`-Durchreichung und den generischen `500`-Fallback ab.

- **Frontend**: Behebt einen Fehler, bei dem das Notiz-Bearbeiten-Modal im Lager (`/inventory/my`, `/inventory/all`) nach dem ersten erfolgreichen Speichern bei erneutem Öffnen ohne Seitenreload keine Funktion mehr hatte (jeder weitere Speichern-Klick führte zu `409 Conflict` durch Optimistic Locking, der als `500` im Frontend sichtbar wurde). Die nach dem PUT zurückgelieferte neue `version` wird nun über **alle** DOM-Elemente mit passender `data-id` synchronisiert (Notiz-Button, Ausbuchen-Button, zugehörige Selects). Zusätzlich wurde ein `catch`-Fallback ergänzt, der bei fehlgeschlagener DOM-Synchronisation automatisch einen Seitenreload auslöst, um konsistente `data-version`-Attribute zu garantieren (AGENTS.md: FRONTEND DOM VERSION SYNC).

- **Frontend**: Staffelmitglieder können nun ihren eigenen Eintrag in der Teilnehmerliste eines Einsatzes (`mission-detail.html`) nachträglich bearbeiten (Aufgabenwunsch, Staffel, Auszahlungsart, Bemerkung). Die Sichtbarkeit des "Bearbeiten"-Buttons wird über ein neues Model-Attribut `authUserId` (JWT-`sub`) robust gegen die User-ID des Participants geprüft, statt über `#authentication.name` direkt im Template. Felder wie `plannedMissionJobType`, `startTime` und `endTime` bleiben weiterhin nur für Manager (`mission.canEdit`) bearbeitbar. Die Backend-Route (`PUT /api/v1/missions/{id}/participants/{participantId}` mit `canAccessParticipant`-Check) erlaubt Self-Edit bereits und wurde unverändert wiederverwendet.

- **Frontend**: Filter im persönlichen Lager (`/inventory/my`) und globalen Lager (`/inventory/all`) bleiben nach dem Ausbuchen/Umbuchen/Verkaufen eines Eintrags erhalten. `InventoryPageController#bookOutInventoryItem` übernimmt die aktiven Query-Parameter (`materialIds`, `minQuality`, `jobOrderIds`, `missionIds`, `page`, `size`, `sort`, ...) aus dem Referer-Header und hängt sie an das Redirect-Ziel. Der interne Parameter `fragment` wird bewusst verworfen.

- **Frontend/Backend**: Raffinerieauftrag-Dauer: Die Felder "Stunden" und "Minuten" im Anlage- und Bearbeiten-Formular sind nun mit `0` vorbefüllt und müssen jeweils `>= 0` sein. Die Minuten sind zusätzlich auf `0..59` begrenzt (`@Max(59)`). Die Backend-Entity `RefineryOrder.durationMinutes` sowie das DTO `RefineryOrderDto.durationMinutes` akzeptieren nun `0` (`@PositiveOrZero` statt `@Positive`). HTML-Inputs verwenden `min="0"`, `max="59"` (Minuten), `step="1"` und `inputmode="numeric"`.

- **Backend/Frontend**: Behebt einen Fehler, bei dem Staffelmitglieder beim Eintragen als Teilnehmer eines Einsatzes eine Fehlermeldung ("Guest name is already taken.") erhielten, wenn sie ihren Namen eintippten, ohne einen Eintrag aus der Autovervollständigung zu wählen. Das Backend (`MissionController#addParticipantPublic`) versucht für authentifizierte Nutzer nun zunächst einen case-insensitiven Exakt-Match auf `username` / `displayName` und verknüpft den Teilnehmer bei eindeutigem Treffer als registriertes Mitglied. Bei Mehrdeutigkeit wird `409 Conflict` zurückgegeben; das Frontend zeigt einen eigenen lokalisierten Toast `error.mission.participant.ambiguous`. Anonyme Nutzer sind weiterhin vor Spoofing mit registrierten Namen geschützt.

- **Frontend/Backend**: Notizen/Bemerkungen an Lagereinträgen. Eigentümer können im persönlichen Lager (`/inventory/my`) an ihren eigenen Einträgen eine Notiz hinzufügen, bearbeiten und entfernen. Im globalen Lager (`/inventory/all`) sind die Notizen für alle Nutzer mit Member-Rechten sichtbar; Logistiker (sowie indirekt Officers und Admins) können Notizen an fremden Einträgen hinzufügen, bearbeiten und entfernen. Neuer Endpunkt `PUT /api/v1/inventory/{id}/note` mit Optimistic Locking (`version`), neues Request-DTO `InventoryItemNoteUpdateRequest`, erweiterte `InventoryItemDto.note`, Flyway-Migration `V61__add_inventory_item_note.sql`. KRT-konformes Notiz-Modal mit Live-Zeichenzähler und Toast-Feedback sowie DOM-`data-version`-Sync. Neue i18n-Schlüssel: `inventory.note.label/add/edit/remove/save/cancel/placeholder/empty/confirmRemove.*/success.*/error.*`.

- **Frontend/Backend**: Im persönlichen Lager (`/inventory/my`) wurden die Filter für Material und Mindestqualität ergänzt (Feature-Parität zum globalen Lager `/inventory/all`). Die Filter werden serverseitig ausgewertet (`InventoryItemRepository#findUserByFilters` um `materialIds` / `minQuality` erweitert), sind über den persönlichen Endpunkt `/api/v1/inventory/my-inventory/grouped` als Query-Parameter `materialIds` und `minQuality` nutzbar, mit JobOrder- und Mission-Filtern kombinierbar und bleiben über `history.replaceState` in der URL erhalten. Neue i18n-Schlüssel: `inventory.filter.material`, `inventory.filter.minQuality`.

- **Frontend/Backend**: Behebt einen Fehler, dass im persönlichen Lager (`/inventory/my`) und im globalen Lager (`/inventory/all`) nicht nach zugewiesenen Aufträgen und/oder Einsätzen gefiltert werden konnte. Es ist nun möglich, über neue KRT-konforme Multi-Select-Komponenten mehrere Aufträge und/oder Einsätze gleichzeitig auszuwählen. Die Filterung erfolgt serverseitig (`InventoryItemRepository#findUserByFilters` / erweitertes `findGlobalByFilters`), Filter-Zustände bleiben in der URL erhalten (Query-Parameter `jobOrderIds`, `missionIds`) und lassen sich über einen "Filter zurücksetzen"-Button leeren. Neue i18n-Schlüssel: `inventory.filter.jobOrder`, `inventory.filter.mission`, `inventory.filter.reset`.

- **Frontend/Backend**: Im Admin-Bereich der Materialien wurde eine Checkbox ("Manuelles Rohmaterial") hinzugefügt. Damit können Materialien explizit als Rohmaterialien deklariert werden, ohne dass dieser Status durch UEX-Updates überschrieben wird. Dies ermöglicht es, fehlende UEX-Daten zu korrigieren und diesen Materialien ein veredeltes Material zuzuordnen sowie sie für Raffinerieaufträge zu verwenden.

- **Frontend/Backend**: Das Erstellen eines neuen Teilnehmereintrags bei einem Einsatz belegt das Namens-Feld nun automatisch mit dem Namen des angemeldeten Nutzers (bevorzugt Anzeigename, sonst Username) vor.

- **Frontend**: Beim Hinzufügen oder Bearbeiten eines Teilnehmers in einem Einsatz wird die Staffel im Dropdown automatisch mit "Iridium" vorbelegt, sofern der ausgewählte Nutzer (bzw. der zu bearbeitende Nutzer) die Rolle `MEMBER` besitzt und noch keine Staffel zugewiesen war.

- **UI/UX (Keycloak)**: Added "Forgot Password" link to the Keycloak login page (`login.ftl`), respecting the realm's reset password settings.

- **UI/UX (Keycloak)**: Adjusted the password reset page (`login-reset-password.ftl`) to better conform to the KRT Corporate Design (tabindexes and form classes).

- **UI/UX**: Replaced all combined `datetime-local` inputs across the application with separate `date` and `time` input fields for better mobile usability (touch "Fat-Finger" optimization) and clearer visual structure.

- **Validation**: Added `datetime-splitter.js` to automatically merge date and time fields into ISO-8601 strings and to provide robust client-side validation (e.g., date not in the past, end time after start time).

- **i18n**: Added validation messages for date and time inputs in `messages.properties` (`validation.datetime.past`, `validation.datetime.after`).

- **Backend**: Behebt einen Fehler (500 Internal Server Error / IncorrectResultSizeDataAccessException), der beim Zuweisen eines Auftrags (Joborder) zu einem Lagereintrag auftrat, wenn bereits ein identischer Lagereintrag mit diesem Auftrag existierte. Anstatt abzustürzen, werden die beiden Einträge nun wie vorgesehen korrekt zusammengelegt.

- **Frontend**: Behebt einen Fehler (Optimistic Locking Conflict), durch den ein Lagereintrag nach dem Entfernen einer Auftragszuordnung (Joborder) nicht mehr ausgebucht werden konnte, ohne die Seite neu zu laden. Das "Ausbuchen"-Formular sendet nun die korrekt aktualisierte Version mit.

- **Frontend**: Behebt einen Fehler (Optimistic Locking Conflict) in der Einsatz-Detailansicht. Wenn die Auszahlungsart eines Teilnehmers geändert wurde, hat der anschließende Bearbeitungs-Modal-Dialog eine veraltete Version gesendet. Das Modal wird nun mit der aktuellen Version versorgt.

- **Frontend**: Behebt einen Fehler in der Tabellen-Verwaltung (`mission-data.html`). Das erfolgreiche Umsortieren via Drag-and-Drop löst nun einen Reload aus, um asynchrone Versionskonflikte bei anschließender Bearbeitung der umgeschobenen Einträge zu vermeiden.

- **Frontend**: Im Dropdown-Feld für das Eingangsmaterial bei Raffinerieaufträgen werden nun auch manuell markierte Rohmaterialien (`isManualRawMaterial`) zur Auswahl angeboten.

- **Backend**: Ein Fehler wurde behoben, bei dem die Checkbox "Manuelles Rohmaterial" beim asynchronen Speichern im Backend ignoriert und nach einem Neuladen der Seite im Frontend wieder zurückgesetzt wurde.

- **Frontend/Backend**: Im Admin-Bereich der Materialien stehen im Dropdown für "Veredeltes Material" nun alle Materialien (statt nur REFINED) zur Auswahl, um UEX-Datenfehler bei nicht korrekt markierten Materialien manuell korrigieren zu können.

- **UI/UX (Keycloak)**: Reverted the removal of `<#if realm.resetPasswordAllowed>` condition on the login page (`login.ftl`). The link is now correctly conditioned again. Note: Since Keycloak caches FreeMarker templates by default, a restart of the Keycloak server or disabling the theme cache is required for changes to take effect.

- **Frontend/Backend**: Die automatische Vorbelegung der Iridium-Staffel für Einsatzteilnehmer (beim Erstellen oder Bearbeiten via Autocomplete) berücksichtigt nun korrekterweise auch die aktiven internen Rollen (`KRT_MEMBER`, `OFFICER`, `ADMIN`), statt fälschlicherweise nur auf die Keycloak-Fallback-Rolle `MEMBER` zu prüfen. Zudem wurde ein UI-Bug behoben, durch den die Auswahl im Dropdown nicht zuverlässig das zugehörige Event auslöste.

- **Backend**: Fixed an issue where inventory item amounts were not properly rounded. All calculations in `InventoryItemService` are now rounded to three decimal places (kaufmännisch / HALF_UP) before saving.

- **Database**: Added Flyway migration script `V59__round_inventory_item_amounts.sql` to retroactively round all existing inventory item amounts to three decimal places.

- **Backend**: Fixed an issue where the `calendarLink` field was not saved when updating an existing mission in `MissionService`.

- **Backend**: Fixed material handover logic in `JobOrderHandoverService` for Job Orders. When a specific material is fully handed over, its remaining reserved inventory items are now properly unlinked. Furthermore, if all requested materials of an order are fully handed over, the order is now automatically marked as `COMPLETED`.

- **Backend**: Fixed an issue in `JobOrderService` where inventory items associated with a job order remained locked when the job order was completed or rejected. Now, when a job order transitions to a terminal state (`COMPLETED` or `REJECTED`), `inventoryItemRepository.unlinkJobOrder` is called to free up the associated materials for other orders.

- **Frontend**: Fixed the "Jetzt" (Now) button in the mission detail form (`mission-detail.html`). The button now correctly populates the split date and time inputs for actual start and end times, similar to the refinery order fix.

- **Frontend**: Fixed the "Jetzt" (Now) button in the refinery order creation and detail forms (`refinery-orders-create.html`, `refinery-orders-details.html`). The button now correctly populates the split date and time inputs instead of attempting to set a removed combined input field.

- **Frontend**: Fixed display of refinery order quantities in mission details (`mission-detail.html`). The amount is now dynamically scaled (divided by 100 for SCU) and the unit type (SCU/Stück) is displayed per row instead of being hardcoded in the column header.

- **Frontend**: Changed the redirect destination after successfully storing a refinery order. The user is now redirected back to the refinery management page (`/refinery-orders`) instead of the inventory page (`/inventory`), improving the user workflow.

- **Frontend/Backend**: Fixed an issue where the Job Orders page (`orders-index.html`) was loaded twice due to a JavaScript redirect for the filter parameters. The filter state (`orders_filter_status`) is now managed completely server-side via a cookie in `JobOrderPageController.java`.

- **Frontend**: Fixed "unsaved changes" warning that incorrectly appeared when leaving the page immediately after successfully updating the asynchronous status dropdown in the `orders-detail.html` view.

- **Frontend**: Fixed an issue in `orders-detail.html` where users with edit rights for a `JobOrder` would still see the static status label badge in addition to the interactive dropdown. The static label is now hidden for users with the `LOGISTICIAN` role (who are permitted to change the status).

- **Frontend**: Fixed `minQuality` filter dropdown in `inventory-admin.html` to include `1000` as a valid option (range from 100 to 1000) to match the material quality specification.

- **Frontend**: Fixed HTML max validation limit for "Min. Qualität" (Minimum Quality) in `orders-create.html` and `orders-detail.html`. Increased the maximum allowed input from 100/999 to 1000 to match the backend specification for materials.

- **Frontend**: Fixed `orders-detail.html` edit modal background to use the standard `.modal` class instead of a transparent `.modal-backdrop` for better readability.

- **Frontend**: Fixed label styles (Staffel, Handle, Material, Min. Qualität, Menge) in the `orders-detail.html` edit modal to match the KRT design (Lato, bold, primary color).

- **Frontend**: Fixed the "Staffel" dropdown in the `orders-detail.html` edit modal to display only the squadron name instead of the full `SquadronDto` object string.

- **i18n**: Added missing translations for the "Remove" (Entfernen) material button in `messages.properties` (DE/EN).

- **Frontend**: Fixed 500 server error when viewing inventory lists (global, my, material details, mission details) caused by an invalid SpEL expression combining variables and `#messages.msg` implicitly. Refactored templates to use Thymeleaf literal substitutions `|...|` for safe string concatenation.

- **Frontend**: Fixed 500 server error when opening a refinery order caused by an invalid SpEL expression containing Thymeleaf message variables in the storage dialog.

- **Frontend**: Fixed label styling for `(Stück)` and `(SCU)` quantity Markers in inventory input and refinery order details to inherit the standard orange and bold label styling.

- **Testing**: Fixed legacy `MissionAccessControlTest` and `MissionGuestAccessTest` which failed due to missing required constraints on DTO updates.

- Added Auto-Save feature on the Materials admin page. Changes in the material dropdown menus (Quantity Type, Category, Refined Material) are now saved asynchronously. The manual "Save" buttons have been removed.

- `MaterialUpdateAjaxRequest` DTO and an AJAX endpoint in `AdminMaterialsPageController` to process asynchronous material updates.

- `MissionDto`, `MissionParticipantDto`, `MissionUnitDto`, `MissionCrewDto`, `MissionFinanceEntryDto`, `MissionLeadTypeDto` DTO records for the Mission API.

- Record-based request DTOs: `AddUnitRequest`, `AddParticipantRequest`, `AddParticipantPublicRequest`, `AddCrewRequest`, `UpdateCrewRequest`, `UpdateParticipantRequest`, `AddFinanceEntryRequest`.

- `MissionMapper` (MapStruct, Spring component) for Mission entity-to-DTO conversion.

- Server-side pagination and sorting for mission list and search endpoints using `Pageable` and `PageResponse`.

- OpenAPI annotations (`@Operation`, `@ApiResponse`, `@Tag`) on `MissionController` and `SystemController`.

- `PingResponse.timestamp` now uses `Instant` (UTC) instead of `String`.

- Fixed an issue where submitting the refinery order store form multiple times or overriding the pre-filled yield amount led to duplicated inventory material. Orders already marked as `COMPLETED` can no longer be stored again, and the frontend amount field is now disabled (readonly) if the output quantity is fixed by the order.

- Fixed access control for Officers and Admins in Refinery and Job Order management by utilizing `RoleHierarchy` instead of manual role string checks.

- Restored missing action buttons and enabled owner dropdowns for users with inherited Logistical permissions.

- Corrected role inheritance in both Frontend and Backend controllers to ensure consistent behavior across the application.

- Refactored `MissionCrew` to link to `MissionParticipant` instead of `User`. This ensures that crew members must be registered participants of the mission.

- Updated API response for Mission Crew to include full participant details.

- Added `MaterialType` to `Material` (RAW, REFINED, NO_REFINE).

- Renamed `RefinedGood` to `RefineryGood`.

- Enforced that `RefineryGood` must refer to a `Material` of type `RAW`.

- `MissionController` now returns `MissionDto` / `PageResponse<MissionDto>` instead of leaking JPA entities.

- Reverted `/api/v1/system/ping` to return `Map<String, String>` for backward compatibility until sunset (2026-12-31); `/api/v2/system/ping` returns `PingResponse` with `Instant` timestamp.

- `ManufacturerDto` field aligned with entity (`abbreviation` instead of `code`), resolving ShipMapper warning.

- Migrated remaining hardcoded dependency versions (`springdoc-openapi`, `lombok-mapstruct-binding`, `okhttp3-mockwebserver`) to `refreshVersions` placeholders.

- Resolved MapStruct unmapped property warnings (`code` in ShipMapper, `parentId` in MissionMapper).

### Security

- **GHCR-basierter Pull-Deploy mit systemd-Timer ersetzt das manuelle Folder-Copy + `docker compose --profile prod up --build -d`-Verfahren**: Produktions-Images werden ab sofort von GitHub Actions gebaut, in der GitHub Container Registry abgelegt und vom Server per `:stable`-Tag im 5-Minuten-Takt eingelesen — der Server baut nichts mehr selbst und es wird nichts mehr aus dem Worktree auf den Server kopiert. **Build-Pipeline (`.github/workflows/release-images.yml`)**: Triggert auf Push nach `main` (rolling `:edge` + immutable `:sha-<short>`) und auf `v*.*.*`-Tags (`:1.4.2`, `:1.4`, `:1`, `:latest`). Pro Push laufen `backend` und `frontend` als Matrix-Build parallel via `docker/build-push-action@v6`, `provenance: mode=max` und `sbom: true` hängen SLSA-Provenance und SPDX-SBOM als OCI-Attestations ans Manifest, `cosign sign --yes` signiert keyless via Sigstore-Fulcio (kein langlebiger Key zu rotieren, Identität = Workflow-Ref im Rekor-Transparency-Log), Trivy scannt das gepushte Digest auf `HIGH,CRITICAL` und lädt SARIF in die Security-Tab — Findings blocken den Push nicht (ein gefundener CVE soll keinen Hotfix verzögern), aber `promote.yml` re-verifiziert vor jeder Stable-Promotion. **Promotion-Workflow (`.github/workflows/promote.yml`)**: `workflow_dispatch` mit Pflicht-Input `version` (z.B. `1.4.2`) — kein Rebuild, sondern `docker buildx imagetools create` kopiert das existierende Manifest-Digest auf `:stable`. Davor verifiziert `cosign verify` die Keyless-Signatur via Identity-Regex auf `release-images.yml@refs/.+` und OIDC-Issuer `token.actions.githubusercontent.com`, sodass nur von unserer CI gebaute Images promotbar sind; ein leaked GHCR-PAT der nur `packages: write` hat reicht NICHT, ein Image hochzuladen und dann nach `:stable` zu promoten. Backend + Frontend sind eine `fail-fast`-Matrix gegen Version-Skew, `concurrency: promote-stable` serialisiert zwei parallele Flips global. **Server-Side Deploy (`scripts/deploy.sh` + `iri-deploy.timer` + `iri-deploy.service` + `iri-deploy.logrotate`)**: Polling-Modell statt Push — der Server löst GitHub-seitig nichts aus, ein systemd-Timer feuert alle 5 Minuten ein `deploy.sh`-Oneshot als unprivilegierter `deploy`-User (nur Mitglied der `docker`-Gruppe, kein Shell, kein sudo). Das Skript loggt sich per `read:packages`-PAT aus `/etc/iri/ghcr-pull-token` an GHCR ein, löst `:stable` per `docker buildx imagetools inspect` zu einem konkreten Digest auf (kein Pull), vergleicht mit `/var/lib/iri/last-deployed.digests` und exit 0 wenn unverändert — 287 von 288 täglichen Ticks sind also ein einzelner HEAD-Request gegen GHCR ohne weitere Last. Bei Änderung schreibt es einen Compose-Override `/var/lib/iri/current-digest-pin.yml` mit `image: ghcr.io/.../basetool-{backend,frontend}@sha256:...`, ruft `docker compose pull && docker compose up -d --wait --wait-timeout 180 --no-build`, und führt bei `--wait`-Timeout ein **automatisches Rollback** auf den vorherigen Pin (`previous-digest-pin.yml`) durch — die DB- und Keycloak-Container bleiben dabei laufen, nur `backend` und `frontend` werden recycelt. Die Digest-Pinnung zwischen Resolution und Apply verhindert, dass eine parallele `:stable`-Flip in GHCR mitten in einem Deploy ein halbpromotetes Release anwendet. **Hardening**: Die systemd-Unit hat `NoNewPrivileges=true`, `ProtectSystem=strict`, `ProtectHome=true`, `PrivateTmp=true`, `PrivateDevices=true`, `ProtectKernelTunables=true`, `ProtectKernelModules=true`, `ProtectControlGroups=true`, `RestrictNamespaces=true`, `RestrictRealtime=true`, `LockPersonality=true`, `ReadWritePaths=/var/lib/iri /var/log /var/lock /var/iri`, `MemoryMax=512M` und `CPUQuota=200%`. `RandomizedDelaySec=30s` spreizt den Tick über Hosts (Vorbereitung für Multi-Server-Setups), `Persistent=true` holt verpasste Ticks nach Reboots nach. Logrotate ist auf `copytruncate` konfiguriert weil systemd `StandardOutput=append:` einen offenen FD hält, der bei klassischer `create`-Rotation einfach in die umbenannte Datei weiterschreiben würde. **Image-Hygiene**: Ein neues `.dockerignore` im Repo-Root blockt `**/keystore.p12`, `**/*.p12`, `**/*.jks`, `**/*.pem`, `**/*.key`, `.env`, `.env.*` (außer `.env.example`) und `**/realm-export.json` aus jedem Build-Context — Belt-and-Suspenders gegen die `.gitignore`-Lage, falls ein Dev lokal eine `keystore.p12` neben dem Source liegen hat und `docker build` aufruft. Zusätzlich wurden `backend/src/main/resources/application.yml` und `frontend/src/main/resources/application.yml` von `key-store: classpath:keystore.p12` auf `key-store: ${SERVER_SSL_KEY_STORE:classpath:keystore.p12}` umgestellt: der Classpath-Fallback hält das lokale `./gradlew bootRun`-Setup (mit `keystore.p12` unter `src/main/resources/`) lauffähig, während `docker-compose.yml` im Container `SERVER_SSL_KEY_STORE=file:/run/secrets/keystore.p12` setzt und einen Read-Only-Bind-Mount aus `${IRI_KEYSTORE_HOST_PATH:-./keystore.p12}` einhängt — das Produktions-Zertifikat lebt damit ausschließlich auf dem Host (`/var/iri/secrets/keystore.p12`, Owner `root:10001`, Mode `0640`) und landet niemals im GHCR-Image. **`docker-compose.yml`-Umbau**: Die `x-backend` und `x-frontend` Templates verlieren ihren `build:`-Block und bekommen `image: ghcr.io/${IRI_IMAGE_NAMESPACE:-krt-profit}/basetool-{backend,frontend}:${IRI_BASETOOL_VERSION:-stable}` plus den Keystore-Volume-Mount; ein neuer `docker-compose.build.yml`-Override führt `build:` für lokale Iteration explizit wieder ein (`docker compose -f docker-compose.yml -f docker-compose.build.yml --profile dev up -d --build`). Die Build-Override-Datei wird durch das Server-Bootstrap-Verfahren NICHT auf den Server kopiert — die Produktions-Host hat damit keinen Source-Tree mehr und kann gar nicht erst versehentlich rebuilden. **`.env.example`** dokumentiert die zwei neuen Variablen `IRI_IMAGE_NAMESPACE=krt-profit` (Default) und `IRI_KEYSTORE_HOST_PATH=/var/iri/secrets/keystore.p12`, sowie eine auskommentierte `#IRI_BASETOOL_VERSION=stable` als Rollback-Anker. **Doku**: Neue [`docs/deployment.md`](docs/deployment.md) enthält das vollständige Server-Bootstrap-Runbook (Pakete, `deploy`-User, Directory-Layout mit allen `chown`/`chmod`-Kommandos, Secrets-Provisioning, Timer-Installation, Erst-Deploy), die normale Release-Loop (`git tag` → `release-images.yml` → `gh workflow run promote.yml` → automatischer Pull innerhalb 5 Min), manuelle Deploy-/Rollback-Verfahren, PAT-Rotation-Steps und eine Troubleshooting-Tabelle. README's `Running with Docker Compose (Production)`-Sektion wurde komplett ersetzt durch einen Kurz-Abriss + Verweis auf `docs/deployment.md`; `Running with Docker Compose (Full Dev Stack)` bekam einen "GHCR-pull"- und einen "Local-rebuild"-Modus; `Running the Local Test Stack` wurde so umgebaut, dass der Test-Keystore unter `./keystore.p12` (Repo-Root, key-alias `basetool` statt `backend`, SANs jetzt `localhost,127.0.0.1,backend,frontend`) statt unter `backend/src/main/resources/keystore.p12` erzeugt wird, damit er von der gleichen `IRI_KEYSTORE_HOST_PATH`-Variable wie produktiv konsumiert wird. **`scripts/nightly-update.sh`** wurde als deprecated markiert (Header-Banner verweist auf `deploy.sh` + Timer), bleibt aber im Tree als transitionaler Fallback, bis alle Hosts auf das neue Verfahren migriert sind.

- **Materials-Matrix `/materials/overview`: Terminal-Spalten sind jetzt nach UEX-Planeten-System eingefärbt**: Der Header jeder Terminal-Spalte bekommt einen dezenten Hintergrund-Tint, der den Mutterplaneten widerspiegelt; jede Body-Zelle trägt zusätzlich einen 2px breiten linken Streifen in einer helleren Variante derselben Hue, sodass die Spalten visuell als zusammenhängender Block pro Planet erkennbar sind, ohne die Preise auf farbigem Grund zu rendern. **Auflösung der Planet-Zugehörigkeit:** Backend-JPQL (`MaterialPriceRepository.findAllMatrixItems`) projiziert ein neues `planetName`-Feld via `COALESCE(t.planet_name, mn.planet_name, pl.name)` über zwei zusätzliche `LEFT JOIN`s auf `Moon` (gleicher `name`/`star_system_name`) und `Planet` (Planet, dessen eigener Name dem `Terminal.orbit_name` entspricht) — direkter `terminal.planet_name`, indirekt über `Moon.planet_name` wenn das Terminal auf einem Mond steht (z.B. Daymar → Crusader), sonst über einen gleichnamigen Orbit-Eintrag (z.B. Lagrange-Punkte „Hurston"). Terminals ohne Mutterplaneten (Jump-Points, System-Lagrange) bleiben neutral und sortieren ans Ende ihrer Star-System-Gruppe. **Farbpalette (Hybrid):** Neuer `frontend.controller.PlanetColorResolver` mappt kanonische Star-Citizen-Planeten (Hurston/Crusader/ArcCorp/microTech, Pyro I–VI inkl. Lore-Namen Monox/Bloom/Terminus/Vatra/Adir, Terra, Delamar) auf hand-gepickte Hex-Werte, die zur dunklen IRIDIUM-Theme passen; unbekannte Planeten landen via `String.hashCode()`-basiertem Bucket-Index auf einer von 12 deterministischen HSL-Slots (`planet-hash-0` .. `planet-hash-11`), sodass derselbe Planet immer denselben Tint bekommt. Der Resolver akzeptiert den Star-System-Namen bereits als zweite Hash-Komponente, falls in Zukunft Name-Kollisionen über Systeme hinweg disambiguiert werden müssen. **Sortierung:** `TerminalCol.compareTo` erhält eine neue Ebene zwischen Star-System und Group-Priority (`System → Planet → GroupPriority → Name`), sodass Terminals desselben Planeten in einem ununterbrochenen Block stehen und die Farbstreifen nicht durch zwischengeschobene andere Planeten zerrissen werden. **Tests:** Neuer `PlanetColorResolverTest` (Null-/Blank-Fallback, kanonische Mappings inkl. Case-Insensitivity, deterministischer Hash-Fallback, System-Komponente im Hash); `MaterialsPageControllerTest` um zwei neue Tests erweitert (`getMatrixOverview_ShouldTagTerminalsWithPlanetCssClass` und `getMatrixOverview_ShouldHandleBackendError`), die mit drei Mock-Matrix-Items (Hurston/Unknown/no-planet) die `planetCssClass`-Zuweisung und die "Planet-less ans Ende"-Sortierung pinnen; erweiterter `MaterialMatrixItemDtoTest` prüft das neue Feld im JSON-Roundtrip. **Geänderte Dateien:** `backend/.../model/dto/MaterialMatrixItemDto.java` (neues `planetName`-Feld + erweiterter Tuple-Konstruktor), `backend/.../repository/MaterialPriceRepository.java` (2 LEFT JOINs + COALESCE in der Matrix-Query), `frontend/.../model/dto/MaterialMatrixItemDto.java` (spiegelbildliches Feld), `frontend/.../controller/PlanetColorResolver.java` (neu), `frontend/.../controller/MaterialsPageController.java` (`TerminalCol` um `planetName` + `planetCssClass` erweitert, Sortier-Logik, Builder ruft den Resolver), `frontend/.../templates/materials-overview.html` (CSS-Regeln für 12 kanonische + 12 Hash-Buckets, `th:classappend` auf `<th class="col-terminal">` und `<td class="col-terminal">`, neues `data-planet`-Attribut und planet-erweiterter `title`-Tooltip). **Zusätzlich:** Der globale `main { max-width: 1200px }`-Cap (mit Ultra-Wide-Bump auf `1600px`) wird auf dieser Seite über `main.materials-overview-main { max-width: none }` aufgehoben — die Matrix ist ein Daten-Grid, kein Long-form-Text, und profitiert auf grossen Monitoren von der vollen Viewport-Breite. Die globale `max-width: 80ch`-Regel auf `<p>`-Elementen bleibt unberührt.

- **Admin-Override für UEX-Flags `hasLoadingDock` und `isAutoLoad`**: Offiziere und Admins können die von UEX gelieferten Werte für Loading Dock (Terminal/City/SpaceStation/Outpost/POI) und Autoload (nur Terminal) manuell überschreiben, weil UEX einige Orte mit vorhandenem Ladedock/Autoload fälschlicherweise als nicht damit ausgestattet meldet. Pro überschreibbarem Flag wurde eine `*_overridden BOOLEAN NOT NULL DEFAULT FALSE`-Spalte ergänzt (Migration `V75__add_uex_flag_overrides.sql`); `UexUniverseSyncService` schreibt die Source-Spalte nur noch, wenn das zugehörige Override-Flag `false` ist, sodass manuelle Korrekturen den stündlichen Sweep überleben. Die bestehende Seite `/admin/terminals` zeigt zwei neue Spalten "Loading Dock" und "Autoload"; eine neue Seite `/admin/uex-locations` bündelt die Loading-Dock-Overrides für Cities, Space Stations, Outposts und POIs. Pro Zelle gibt es drei Buttons `[UEX] [Ja] [Nein]` — der aktive Zustand ist hervorgehoben, ein Klick auf `Ja`/`Nein` pinnt den Wert (PATCH gegen `/api/v1/<kind>/{id}/loading-dock?value=…` bzw. `/auto-load?value=…`), ein Klick auf `UEX` gibt die Kontrolle an den Sync zurück (DELETE gegen `/api/v1/<kind>/{id}/loading-dock-override` bzw. `/auto-load-override`). Backend-seitig wurden vier neue REST-Controller (`CityController`, `SpaceStationController`, `OutpostController`, `PoiController`) inklusive Services, MapStruct-Mapper und DTOs hinzugefügt; `TerminalController`/`TerminalService` und die Frontend-`TerminalDto`-Records wurden um die neuen Felder erweitert. Mockito-Tests pinnen den Override-Respect aller fünf `sync*`-Methoden gegen einen ArgumentCaptor (kein Docker, da TestContainers lokal nicht verfügbar). Tritt überall mit `@PreAuthorize("hasAnyRole('OFFICER', 'ADMIN')")` auf, sowohl in den REST-Controllern als auch in den Frontend-Page-Controllern.

- **Test-Coverage für die noch unabgedeckten Backend-Controller `InventoryItemController`, `JobOrderController` und für die in `MissionController` nicht von Security-/Slim-Tests berührten Lifecycle-/Redaction-Pfade (Folge-Etappe zu ANALYSIS 1.2)**: Die erste Etappe (Promotion-Topic/Category/LevelContent/Eligibility + Admin/Hangar/MissionFinanceEntry, Commit b07e633) liess `InventoryItemController` komplett ohne Test, hatte für `JobOrderController` nur den effektiv leeren `JobOrderControllerIntegrationTest.java`-Platzhalter (lokal nicht lauffähig wegen fehlendem Docker) und deckte für `MissionController` nur die `@PreAuthorize`-Gates (`MissionControllerSecurityTest`) und die Slim-Sub-Resource-Endpoints inklusive RSVP-Branches (`MissionControllerSlimEndpointsTest`) ab — die Guest-Redaktion, die Section-Patches, der versionierte Owner-Wechsel und die Anonymous-Filterung der Listen-/Such-Endpunkte blieben offen. Drei neue Pure-Mockito-Test-Klassen schliessen diese Lücken im Stil des bestehenden `HangarControllerTest` (`@ExtendWith(MockitoExtension.class)` + `@InjectMocks` + `@Mock`, Given/When/Then). **(1) `InventoryItemControllerTest`** pinnt die vier nicht-pass-through Behaviours: (a) die `/my-inventory*`-Endpunkte leiten den Owner immer per `UserService.getUserIdFromJwt(...)` aus dem JWT ab — der `ArgumentCaptor`-Capture zeigt, dass die ID NIE aus URL-/Query-Parametern stammt (gleicher Guard wie in `HangarController.getMyShips`); (b) `create`/`book-out`/`update`/`update-delivered`/`update-note` lesen `authHelperService.isLogisticianOrAbove()` am HTTP-Rand und propagieren den Boolean an den Service, der Branch wird sowohl für `true` als auch für `false` ausgeführt (ArchUnit-Regel "kein `SecurityContextHolder` ausserhalb des Auth-Helpers"); (c) der `POST /{id}/book-out`-Endpoint splittet 200/204 anhand des Service-Returns (`null` ⇒ 204, sonst 200) — die Trennung lebt im Controller, nicht im Service, das Verhalten ist Vertrag mit der Frontend-Animation; (d) `POST /bulk-checkout` darf den `isLogistician`-Helper niemals konsultieren, weil das Bulk-Delete bewusst nicht role-overridable ist — `verify(authHelperService, never()).isLogisticianOrAbove()` schützt diese Eigenschaft gegen einen "lass uns überall den gleichen Flag durchreichen"-Refactor. **(2) `JobOrderControllerTest`** (parallel zum leeren Integrationstest-Platzhalter, der den Docker-Pfad dokumentiert) pinnt vier Kontrakte: (a) `createJobOrder` ist `permitAll()` und ruft `verifyNoInteractions(userService, authHelperService)` — kein zukünftiger "lass uns den Ersteller mitloggen"-Refactor darf die Anonymous-Antrag-Form silently brechen; (b) `addAssignee` / `removeAssignee` lösen Self-vs-Logistician am HTTP-Rand auf — Self-Pfad konsultiert den Role-Helper nicht (`verify(authHelperService, never()).isLogisticianOrAbove()`), Other-User-Pfad mit Non-Logistician wirft `AccessDeniedException` BEVOR der Service angefasst wird (`verify(jobOrderService, never()).addAssignee(any(), any())`), Other-User-Pfad mit Logistician läuft durch — die 403 muss am Controller bleiben, sonst kollidiert sie mit der ArchUnit-Regel; (c) `getAllJobOrders` reicht ein fehlendes Status-Filter als `null` an den Service durch, NIE als leere Liste (sonst produziert das SQL `WHERE status IN ()` einen leeren Result-Set und die Queue-Seite ist permanent leer); (d) `downloadHandoverReport` parsed den optionalen `X-User-Time-Zone`-Header mit Try/Catch — gültige IANA-Zone wird durchgereicht, blank und ungültig werden silent zu `null` reduziert (der Service fällt auf UTC zurück), die Content-Disposition-Filename-Pattern (`uebergabeprotokoll-{jobOrderId}.pdf` für die persistierte Variante, `uebergabeprotokoll-vorschau.pdf` ohne Job-Order-ID für die Preview-Variante) sind ebenfalls gepinnt weil sie der einzige Bestandteil des HTTP-Response-Contracts sind, der nicht typgeprüft wird. **(3) `MissionControllerLifecycleTest`** deckt sechs Bereiche, die `MissionControllerSecurityTest` und `MissionControllerSlimEndpointsTest` nicht erreichen: (a) **Guest-Redaktion** — `cleanupMissionForGuest` ist die einzige Stelle, die kontrolliert, was für anonyme Viewer aus der API rausgeht; ein vollständig befüllter `MissionDto` (Owner, Managers, Participants mit PII-Feldern, Inventory-Entries, Refinery-Orders, `canEdit`/`canManageManagers`) wird durch den Redaktions-Pass geschickt, und jedes einzelne PII-Feld (`firstName`, `lastName`, `email`, `description`, `roles`, `permissions`, `lastReadAnnouncementId`, `joinDate`, `isLogistician`, `isMissionManager`) wird per Assertion gegen `null` bzw. `false` gepinnt — die public Callsign-Tuple (`username` + `displayName` + `rank`) bleibt sichtbar; (b) **Guest-Access-Blocks** — interne Missions → 403, `COMPLETED`/`CANCELLED` → 403, in allen Fällen wird `verify(missionMapper, never()).toDto(...)` geprüft, damit eine Mapper-Exception nicht via Exception-Message Daten leaken kann; (c) **Anonymous-Filterung** — `getAllMissions` und `searchMissions` schränken Guests silent auf `PLANNED`+`ACTIVE` non-internal Missionen ein; ein Guest, der einen verbotenen Status (`COMPLETED`) sucht, bekommt eine leere Page (NICHT 403), und der Service wird NIE konsultiert (UI-degradiert-still-Contract); ein Guest mit `["ACTIVE","COMPLETED"]` bekommt nur `["ACTIVE"]` durch (Intersection mit der Allow-Liste, nicht Union — ein Union-Refactor würde COMPLETED-Missions leaken); (d) **Section-Patches** — `patchMissionCore` (5 positionelle Argumente: `name`, `description`, `calendarLink`, `status`, `version`), `patchMissionSchedule` (6 positionelle Argumente: 5 Instants + `version`), `patchMissionFlags` (`isInternal` + `version`) unpacken jeweils ihre Request-Records in den Service-Argumentenliste — `verify(missionService).updateCoreSection(id, "New name", "New description", ...)` pinnt die EXAKTE Argumentenreihenfolge, sodass ein Copy-Paste-Refactor, der `name` und `description` (beide `String`) silent vertauscht, hier sofort rot wird; (e) **Versionierter Owner-Wechsel** — `updateMissionOwner` forwarded die `version` aus dem `UpdateMissionOwnerRequest`-Record (die *Ownership*-Aggregate-Version, NICHT `Mission.version`); um eine versehentliche Pin-auf-`Mission.version` zu vermeiden, nutzt der Test einen bewusst distinkten `42L`-Wert, der nirgends sonst im Test-Setup vorkommt; ein zusätzlicher Test für den deprecateten Legacy-Endpoint `setMissionOwnerLegacy` pinnt explizit `verify(missionService, never()).updateMissionOwner(...)`, damit ein zukünftiger "lass uns auch dem Legacy-Endpoint eine Version geben"-Refactor an dieser Stelle als Compile-Fehler auffällt; (f) **Sonstige nicht abgedeckte Lifecycle-Endpunkte** — `joinMission` (JWT-derived self-enroll), `createSubMission` (DTO→Entity→Service→DTO-Roundtrip-Sequenz), `getUnassignedParticipants` (Mapper-Pass-through), `getNextMission` (200/204-Split + `allowInternal`-Boolean abhängig von der Authentifizierung + Guest-Redaktion-Pass), `deleteMission` (204-Response). **Hinweis zum leeren `JobOrderControllerIntegrationTest.java`**: Die Datei (1 Byte, nur Newline) bleibt im Repo als Platzhalter dafür, dass ein Docker-basierter Integration-Test fehlt; sie wird hier nicht angefasst, weil sie kein eigener Befund der Etappe ist. **Coverage-Effekt**: `InventoryItemController` von 0 % auf 100 % Line/Method/Branch (alle 14 Endpunkte abgedeckt), `JobOrderController` von 0 % auf 100 % Line/Method/Branch (alle 14 Endpunkte abgedeckt, inklusive der `verifyAssigneeAccess`-Private-Branch), `MissionController` von partial (`SecurityTest` + `SlimEndpointsTest`) auf zusätzliche Coverage der Lifecycle-/Redaction-/Section-Pfade — die Guest-Redaktion-Methoden (`cleanupMissionForGuest`/`cleanupParticipantForGuest`/`cleanupUserForGuest`) sind damit zum ersten Mal explizit gepinnt. **Verifikation**: `./gradlew :backend:check` (Spotless + Checkstyle + SpotBugs + alle Backend-Tests) BUILD SUCCESSFUL ohne neue Findings; gezielter Run der drei neuen Tests via `./gradlew :backend:test --tests "...InventoryItemControllerTest" --tests "...JobOrderControllerTest" --tests "...MissionControllerLifecycleTest"` ebenfalls grün.

- **Test-Coverage für die Frontend-Promotion-Controller (`PromotionPageController`, `PromotionProxyController`)**: Beide Klassen hatten bisher 0% JaCoCo-Coverage, obwohl der `PromotionPageController` im aktuellen Branch massiv umgebaut wurde (446 LoC mit fünf neuen Model-Attributen `currentUserRank`, `requiredLevelByCategory`, `categoriesByTopic`, `lastEvaluatedByUser`, `hasEvaluationsByUser` und drei neuen Helper-Methoden `fetchCurrentUserRank`/`compareLevels`/`levelOrdinal`). Zwei neue Test-Klassen schliessen diese Lücke: **`PromotionPageControllerTest`** verifiziert pro Page-Handler die Assembly-Logik gegen einen gemockten `BackendApiClient` — `overview()` (Empty-State, Gruppierung von Rang-Voraussetzungen nach `(fromRank, toRank)` mit aufsteigender Sortierung, Propagation und Null-Fallback für `currentUserRank`), `myEvaluations()` (Build von `evaluationByCategoryId`, Max-Reduce in `requiredLevelByCategory` über mehrere Rules pro Kategorie, Skip von Topic-weiten Rules ohne `categoryId`, Propagation von `currentUserRank`), `manage()` (Topic-then-Category-Order von `allCategories`, korrektes `categoryCountByTopic`, Keying von `evaluationMap` als `userId_categoryId`, Max-`updatedAt` in `lastEvaluatedByUser`, defensive Skip von Members mit `id == null` aus `eligibilityByUser` plus Verifikation der genauen Anzahl an Per-User-GETs), `adminTopics()` und `adminRankRequirements()` (Build von `categoriesByTopic` für die kaskadierenden Dropdowns). Zusätzlich ein End-to-End-Defensive-Test (`overview_degradesGracefully_whenEveryBackendCallThrows`), der sicherstellt, dass die Page auch bei totalem Backend-Ausfall die Template-View zurückliefert statt eine 500 zu werfen. **`PromotionProxyControllerTest`** deckt alle 13 Forwarding-Endpoints des Proxy-Controllers ab (POST/PUT/DELETE für Topics/Categories/Rank-Requirements/Level-Contents plus den Dual-Path-Variable-PUT für Evaluations); pro Endpoint wird `BackendApiClient.post/put/delete` mit der exakten URL inklusive Path-Variables und dem unveränderten Body gegen Mockito verifiziert sowie das `204 No Content` auf DELETE asserted. **Stil**: Pure Mockito-Unit-Tests (`@ExtendWith(MockitoExtension.class)`, `@InjectMocks`, `@Mock`) im Given/When/Then-Pattern, konsistent zu `ProfileControllerTest` und `UserProxyControllerTest`. **Coverage**: `PromotionPageController` von 0% auf 85% (835 von 977 Instructions abgedeckt, die restlichen 15% sind die Catch-Branches in den `fetch*`-Helpers, die per `try/catch (Exception)` aus dem `BackendApiClient` heraus auf leere Listen degradieren), `PromotionProxyController` von 0% auf 100%. **Verifikation**: `./gradlew :frontend:spotlessApply :frontend:checkstyleMain :frontend:test` grün.

### Migration

- **Database**: Neue Flyway-Migration `V70__refinery_order_money_fields_nullable.sql` entfernt die `NOT NULL`/`DEFAULT 0`-Constraints von `refinery_order.expenses`, `refinery_order.other_expenses` und `refinery_order.ore_sales` und stellt bestehende `0`-Datensätze auf `NULL` um, damit "leer" und "0" einheitlich als nicht gesetzt repräsentiert sind.

- **Database**: Neue Flyway-Migration `V69__add_other_expenses_to_refinery_order.sql` fügt die Spalte `other_expenses DOUBLE PRECISION NOT NULL DEFAULT 0` zu `refinery_order` hinzu und setzt `other_expenses = 0` für alle bestehenden Datensätze.

- **Infrastruktur (docker-compose.yml)**: Persistente Bind-Mounts für die Logs von `backend` und `frontend` ergänzt, passend zur bestehenden Ordnerstruktur unter `/var/iri/...` (analog zu `/var/iri/keycloak/log`, `/var/iri/db-backend`, `/var/iri/redis`): `/var/iri/backend/log:/app/logs` für `x-backend` und `/var/iri/frontend/log:/app/logs` für `x-frontend`. Container-Pfad `/app/logs` ergibt sich aus `WORKDIR /app` in beiden Dockerfiles und dem in `logback-spring.xml` konfigurierten relativen Log-Pfad `logs/backend.log` (inkl. Rolling-Files `logs/backend.%d.%i.log.gz`, ERROR-File `logs/backend-error.log` und im Prod-Profil zusätzlich `logs/backend.json`). Damit überleben alle Logfiles (inkl. Rotationen) Container-Recreate/`restart: always` und stehen für Support/Forensik auf dem Host zur Verfügung.

- **Database**: Neue Flyway-Migration `V62__add_ore_sales_to_refinery_order.sql` fügt die Spalte `ore_sales DOUBLE PRECISION NOT NULL DEFAULT 0` zu `refinery_order` hinzu und setzt `ore_sales = 0` für alle bestehenden Datensätze, damit keine NPE oder Rechenfehler in der Einsatz-Finanz-Aggregation entstehen.
